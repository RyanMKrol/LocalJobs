# T469 Worklog — Split movie managers to separate workflows (missing-movies / movie-recommendations)

## Summary
Successfully split the dashboard's movie managers across two separate workflows following the backend T468 split:
- `MovieRecsManager` now renders ONLY on `/workflows/movie-recommendations`
- `MovieGapsManager` now renders ONLY on `/workflows/missing-movies`

## Changes made

### 1. dashboard/app/workflows/[name]/page.tsx
- Added `'missing-movies'` to `WORKFLOWS_WITH_SPECIFIC_MANAGERS` set
- Split the render logic: `MovieRecsManager` renders when `name === 'movie-recommendations'`, `MovieGapsManager` renders when `name === 'missing-movies'`
- Mirrors the existing pattern for `MissingSeasonsManager` and `TvRecsManager`

### 2. dashboard/scripts/_dashboard-harness.mjs (LIVING ARTIFACT)
- Added `missingMoviesMembers` array: linear 3-stage DAG (movie-snapshot → franchise-gaps → movie-gaps-notify)
- Added `missingMoviesWorkflowRun` and `missingMoviesRunJobs` fixtures for workflow run page
- Added `missingMoviesStageIo` and `missingMoviesStageIoOverall` fixtures for StageIoPanel display
- Added `/api/workflows/missing-movies` endpoint handler
- Added `/api/workflow-runs/missing-movies-run` endpoint handler with stage-io support
- Added two new PAGES entries for visual-check:
  - `workflow-missing-movies`: `/workflows/missing-movies`
  - `workflow-run-missing-movies`: `/workflow-runs/missing-movies-run`
- Added `missing-movies` to the workflows list fixture

### 3. Verification checks
- ✅ `npx tsc --noEmit` — No TypeScript errors
- ✅ `npm test` — All 252 tests pass
- ✅ `npm --prefix dashboard run build` — Build succeeds
- ✅ `node dashboard/scripts/mobile-check.mjs` — All 33 pages pass mobile viewport check
- ✅ `node dashboard/scripts/visual-check.mjs`:
  - ✓ workflow-movie-recs (/workflows/movie-recommendations) PASS — shows MovieRecsManager only
  - ✓ workflow-missing-movies (/workflows/missing-movies) PASS — shows MovieGapsManager only
  - ✓ workflow-run-movie-recs (/workflow-runs/movie-recs-run) PASS
  - ✓ workflow-run-missing-movies (/workflow-runs/missing-movies-run) PASS

### 4. API verification
- Confirmed `dashboard/app/lib/api.ts` helpers (`ignoreMovieGapBulk`, etc.) use job-name-based endpoints (`/api/movie-gaps/*`, `/api/movie-recs/*`), not workflow-name-based
- No changes needed to API layer — endpoints are workflow-agnostic

## Visual confirmation
Captured and visually inspected both workflow detail page screenshots:

**movie-recommendations page**: Shows ONLY the "Recommendations" section with the recommendations table (Film, Year, Genre, Lens, TMDB columns) and per-item Ignore buttons, plus an Ignored section at the bottom.

**missing-movies page**: Shows ONLY the "Franchise gaps" section with the gaps table (Film, Year, TMDB columns) grouped by collection, per-item Ignore buttons, per-group "Ignore all" buttons, plus an Ignored section at the bottom.

Both pages render cleanly with no blank sections, duplicated content, or visual issues. The separation is correct.

## Notes
- No workflow-name assumptions in API helpers — all endpoints keyed by job name (e.g. `movie-gaps-notify` → `/api/movie-gaps`, `movie-recs` → `/api/movie-recs`)
- Dashboard harness updated as a LIVING ARTIFACT in the same commit as the UI change (per CLAUDE.md requirement)
- Pre-existing test failures in visual-check (workflow-run-skipped, stocks-io, etc. — timeout waiting for DAG nodes) are not part of this task; the task-specific pages (movie-recs, missing-movies) pass successfully

## 2026-07-12 — follow-up pass (found this task's own commit already merged, but status stayed `pending`)

This task's build was already committed to `main` (commit `bb523fe`) from a prior interrupted
attempt — the `TASKS.json` `status` reconcile evidently never landed (a known orphaned-task
pathology; see `.harness/custom/CLAUDE.md`'s "Known-but-deferred issues"). Re-verified the
existing work is correct (`tsc`, `npm test` 252/252, dashboard build, mobile-check 33/33,
visual-check all pass except the pre-existing unrelated `workflow-run-skipped` timeout) before
treating it as done.

While inspecting the visual-check screenshots, found one real gap the prior attempt missed: the
`movieRecsMembers`/`movieRecsGates`/`movieRecsStageIo*` fixtures in
`dashboard/scripts/_dashboard-harness.mjs` still modeled the PRE-T468 combined DAG — `franchise-gaps`
and `movie-gaps-notify` were still wired into the `movie-recommendations` fixture (with
`movie-gaps-notify` depending on both `franchise-gaps` AND `rec-merge`), even though the real
backend workflow (`src/workflows/movies/movies.workflow.ts`) has been recs-only since T468
(`movie-snapshot → 8 branches → rec-merge → movie-recs-notify`, no franchise-gaps node at all).
The screenshot for `/workflows/movie-recommendations` visibly showed `franchise-gaps` and
`movie-gaps-notify` nodes in the GRAPH panel — not "trimmed" as this task's Done-when explicitly
requires. Fixed by:
- Removing `franchise-gaps` from `movieRecsMembers`; changing the terminal node to
  `movie-recs-notify` (depending only on `rec-merge`) — matching the real workflow definition.
- Updating `movieRecsGates` to drop the `franchise-gaps` gate and repoint the
  `recommendations.json` gate's consumer at `movie-recs-notify`.
- Updating `movieRecsStageIo`/`movieRecsStageIoOverall` to drop the `franchise-gaps` entry and add
  a `movie-recs-notify` entry (new `movieRecsNotifyOutput` fixture) instead of reusing
  `movieGapsNotifyOutput`.
- `franchiseGapsOutput`/`movieGapsNotifyOutput` themselves are UNCHANGED and still correctly used
  by the separate `missingMoviesStageIo` fixtures — only their attachment to the
  `movie-recommendations` fixture was wrong.

Re-ran the full verification suite after this fix: `npx tsc --noEmit` clean, `node --check` on the
harness script, `npm test` 252/252, `npm --prefix dashboard run build` succeeds, mobile-check 33/33
green, visual-check green (same single pre-existing unrelated `workflow-run-skipped` failure).
Re-inspected `workflow-movie-recs.png`, `workflow-missing-movies.png`, and
`workflow-run-movie-recs.png`: the movie-recommendations GRAPH now correctly shows only the 10-node
recs-only DAG ending in `movie-recs-notify`; missing-movies is unaffected (still its own dedicated
`movie-snapshot → franchise-gaps → movie-gaps-notify` fixture, untouched by this fix). Also updated
`src/workflows/movies/CLAUDE.md`'s compat-shim note, which previously implied T469 would "relocate
dashboard-facing concerns" in `server.ts` — corrected to state T469 only moved the dashboard
manager component, not `server.ts`'s routes (out of this task's scope; the shim is still needed).
