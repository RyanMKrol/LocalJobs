# T036 worklog

## Attempt 1 — 2026-06-22

### What was done

Added `white-space: nowrap` (via inline `style={{ whiteSpace: 'nowrap' }}`) to all schedule cells across the dashboard:

- `dashboard/app/jobs/[name]/page.tsx` line 58 — schedule kv row in job detail panel
- `dashboard/app/pipelines/[name]/page.tsx` line 33 — schedule kv row in pipeline detail panel
- `dashboard/app/pipelines/page.tsx` lines 38 + 70 — schedule column in the pipelines table (pipeline rows and standalone job rows)

### Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — clean build

### Result: done
