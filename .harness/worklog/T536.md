# T536 — Slice error bodies in trading212.service.ts

## What was done
Sliced the unsliced error body in `src/services/trading212.service.ts` at two locations (lines 123 and 149) to limit logged error text to ~300 chars, matching the existing pattern in `src/services/claude.ts` (line 91). This is purely defensive — prevents accidental logging of sensitive upstream error responses.

### Changes made
1. **Line 123 (fetchPortfolio):** Changed `throw new Error(\`Trading212 API error ${res.status}: ${await res.text()}\`)` to:
   ```ts
   const body = (await res.text()).slice(0, 300);
   throw new Error(`Trading212 API error ${res.status}: ${body}`);
   ```
2. **Line 149 (fetchInstrumentsMetadata):** Applied identical fix for consistency.
3. **Sweep across all service files:** Confirmed via grep that no other `src/services/*.service.ts` files contain unsliced `${await res.text()}` inside a thrown Error.

## Verification
- ✅ `npx tsc --noEmit` — passed (no type errors)
- ✅ `npm test` — passed (274 tests, 0 failures)
- ✅ Grep sweep: `grep -n "await res.text()" src/services/*.service.ts | grep throw` → only trading212, both fixed
- ✅ No behavior change for well-behaved upstreams (the slice only affects error paths)

## Summary
Task is complete. Both error throws in trading212.service.ts now slice error bodies to ≤300 chars. No other services have the unsliced pattern. All checks pass.
