# T056 — Overview: workflow status pill links to that run

## Attempt 1 — 2026-06-22

### What was done
Changed the status badge on each workflow card in `dashboard/app/page.tsx` (line 173) from a plain `<span>` to an `<a>` wrapping a `<span>`. The link targets `/workflow-runs/{p.last_run.id}`.

- When `p.last_run` is present: badge is a clickable link to that run's detail page.
- When `p.last_run` is null (no runs yet): no badge rendered at all (already the case before, unchanged).

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — all 18 test files, all assertions pass
- `npm --prefix dashboard run build` — clean build

### Result
done
