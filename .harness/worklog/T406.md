# T406 — enrich movie-recommendations notify ledger detail

## What changed
- `src/workflows/movies/stages/notify.ts`: `runNotify`'s two `markWorkItem(..., 'success', ...)`
  calls now populate `detail` with the actual finding, not just identity:
  - franchise-gap entries (`NOTIFY_JOB`): added `title`, `year`, `collectionId`,
    `collectionName`, `tmdbRating` alongside the existing `name`/`markdown`.
  - recommendation entries (`RECS_JOB`): added `lens`, `genre`, `reason`, `tmdbRating`
    alongside the existing `name`/`markdown`/`title`/`year`.
- `src/workflows/movies/stages/notify.test.ts`: added assertions (run 1 for gaps, run R1 for
  recs) that read the ledger row back via `getWorkItem` and assert the enriched `detail` fields
  are present and correct.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — full suite green (185/185 tests, 47 suites), including the updated
  `notify.test.ts` assertions confirming a sample `work_items` row for both
  `movie-gaps-notify` and `movie-recs` carries the enriched fields.
- No live paid calls made — test uses synthetic fixtures + the scratch DB per existing
  test harness conventions.

## Result
done T406
