# T211 — Align Film/Year/TMDB columns across franchise-gap collections

## What was done

Added a `.gaps-table` CSS class in `globals.css` using `table-layout: fixed` with explicit `width`
on the Year (56px), TMDB (56px), and actions (90px) columns via `nth-child` selectors. The Film
column takes the remaining space automatically.

Applied `className="gaps-table"` to the per-collection `<table>` inside `MovieGapsManager` in
`dashboard/app/workflows/[name]/page.tsx`.

No structural changes — each collection still renders its own `<table>`, but all share identical
fixed column proportions now.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (0px overflow)
- Per-item ignore logic untouched (no behavioural change)
