# T003 — Publish the places job code

## Goal
Make the places pipeline **code** public while keeping its `data/` private.

## What I did
- `.gitignore`: removed only the `src/jobs/places/` line. Kept all secret rules,
  `src/jobs/perfumes/`, and `src/jobs/**/data/` (so places data stays ignored).
  Updated the surrounding comment to say places code is now published.
- Staged the 14 places `*.ts` files EXPLICITLY by path (no `git add -A`):
  config, parse, types, places.pipeline, ingest(.job), resolve(.job),
  enrich(.job), llm-enrich(.job), gemini.service, google-places.service.
  Did NOT stage `.DS_Store` or anything under `data/`.
- README: rewrote the "jobs stay private" callout to note places is a published
  example (data always ignored); updated the layout block; replaced the Roadmap
  section with an "Example pipeline: places" section describing the four jobs
  (`places-ingest` → `cid-to-place-id-resolver` → `places-enrich` →
  `enrich-with-llm`) and the required env vars.

## Verification (observed)
- `git check-ignore src/jobs/places/data/out` → `src/jobs/places/data/out`
  (still ignored). Same for raw CSVs and `.DS_Store`.
- `git check-ignore src/jobs/places/enrich.job.ts` → exit 1 (NOT ignored, i.e.
  publishable).
- Staged set = exactly the 14 `.ts` files + README + .gitignore. Grep of the
  staged names for `data/|chrome-profile|.env|.DS_Store|credential|.key|.pem`
  → NONE. Pre-push guard would pass.
- Secret scan of staged places code (`git grep` for AIza/sk-/AKIA/api_key/
  password/Bearer) → no matches. All credentials read from `process.env`
  (`GOOGLE_MAPS_API_KEY`, `GEMINI_API_KEY`, …), none hardcoded.
- No absolute `/Users/` or `/home/` paths in places code; all paths resolved
  relative to the job dir via `resolve(here, 'data')`. No file reads at module
  load, so the code typechecks/imports cleanly with `data/` absent (clean
  checkout safe).
- `npx tsc --noEmit` → clean. `npm test` → all tests passed (registry loads 9
  jobs / 4 services / 2 pipelines).

## Notes
- `.env.example` is outside this task's scope (scope = .gitignore, places/,
  README). The places env vars are documented in the job's `config.ts`, which is
  now public; README points readers there.
