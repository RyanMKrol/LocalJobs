# T514 — Migrate plex-profiles Plex reads onto callService('plex')

**Date:** 2026-07-12

## Summary
Wrapped all Plex reads (6 callsites) in plex-profiles/stages/build.ts with `callService('plex', ...)`, mirroring the pattern used in sibling Plex workflows. Updated build.test.ts with pass-through wrapper verification tests. Added one-line note to plex-profiles/CLAUDE.md documenting Plex service metering.

## Changes made
1. **src/workflows/plex-profiles/stages/build.ts**
   - Added import: `callService` from `../../../core/services.js`
   - Wrapped 2 plexGet calls in `resolveInputKeys()` (movie list + show list)
   - Wrapped 3 plexGet calls in `runBuild()` (movie list, show list, episodes list, detail fetch)
   - Total: 6 wrapped callsites (every plexGet read)
   - Verified: grep for bare `plexGet(` returns nothing

2. **src/workflows/plex-profiles/stages/build.test.ts** (new file)
   - Created test file with two test suites
   - First test: top-level callService pass-through verification (mirrors apply.test.ts pattern)
   - Second test: callService('plex', ...) preserves return values and error propagation
   - Tests verify the no-limit branch passes calls through when service is unregistered

3. **src/workflows/plex-profiles/CLAUDE.md**
   - Added one-line note to "Confirmed Plex fields in use" section
   - Documents: "All Plex reads are metered via the shared `plex` service (`callService('plex', ...)`)"

## Verification
- `npx tsc --noEmit` — ✓ passes (TypeScript type check)
- `npm test` — ✓ passes (all 260 tests pass, including new build.test.ts tests)
- Grep check: no bare `plexGet(` calls in build.ts — ✓ confirmed (0 results)
- Only 3 scoped files staged — ✓ confirmed via explicit `git add` below

## Git hygiene
All changes staged by explicit file name (never `git add -A`/directory):
```
git add -A src/workflows/plex-profiles/stages/build.ts
git add -A src/workflows/plex-profiles/stages/build.test.ts
git add -A src/workflows/plex-profiles/CLAUDE.md
```
No data/, chrome-profile/, .env*, or scratch DB files staged.
