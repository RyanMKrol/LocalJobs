# T618 — vercel-daily-redeploy per-day work_items ledger row

2026-07-17

## What I did

`src/workflows/vercel-daily-redeploy/vercel-redeploy.job.ts`:
- Added a `recordOutcome(key, outcome, extra, status)` helper that writes a small
  JSON summary artifact to `data/out/vercel-redeploy-<day>.json` and calls
  `markWorkItem('vercel-redeploy', key, status, { detail: { outcome, ..., format: 'json',
  path } })`, per the owner-confirmed file-backed-artifact decision recorded in the task
  spec.
- `runVercelRedeploy` now calls `recordOutcome` exactly once per run, keyed by
  `dayKey(now)` (imported from `src/core/dates.ts`), covering all four outcome
  branches:
  - `RYANKROL_CO_UK_PATH` unset → `success` row, `outcome: 'skipped'`,
    `reason: 'missing-config: ...'`
  - checkout path missing on disk → `success` row, `outcome: 'skipped'`,
    `reason: 'missing-config: ...'`
  - `QuotaExceededError` from `callService('vercel', ...)` → `success` row,
    `outcome: 'skipped'`, `reason: 'quota-exhausted: ...'`
  - deploy succeeds → `success` row, `outcome: 'deployed'`, `deployUrl` captured
    from `deployOnce`'s resolved value (previously void — now resolves the parsed
    deploy URL string so it can be threaded up)
  - deploy fails (timeout or non-zero exit) → **both** a `failed` ledger row
    (`outcome: 'failed'`, `reason: <error message>`) **and** the run itself still
    throws (owner-confirmed: write the failed row in addition to the run's own
    failed status, not instead of it)
- Added `opts.now?: Date` to `runVercelRedeploy` so tests can control the day key
  deterministically (mirrors the `stocks-fetch`/`overrides-audit-scan` pattern).
- No `inputKeys()` added — the workflow stays unlimited/scheduled-only per the spec.
- No gate involved (single-stage workflow, no DAG edge) — nothing to wire there.

`src/workflows/vercel-daily-redeploy/vercel-redeploy.test.ts`:
- Added a `describe('per-day work_items ledger (T618)', ...)` block with 6 new tests
  (using the existing `fakeCtx`/`makeFakeChild`/`bypassCallService` helpers,
  driving `spawnFn`/`callServiceFn` injection against the scratch DB): deployed,
  skipped-missing-config (unset var), skipped-missing-config (nonexistent path),
  skipped-quota-exhausted, failed (asserts both the ledger row AND the run
  rejecting), and a same-day-overwrite regression test (asserts
  `created_at` is unchanged across two same-day runs while `detail` reflects the
  second run's outcome — confirms upsert, not duplication).
- All pre-existing tests updated where convenient to pass `now` for determinism;
  none of their assertions changed.

`src/workflows/vercel-daily-redeploy/CLAUDE.md`:
- Replaced the stale "No work_items ledger — pure fire-and-forget trigger, no
  items to track" line with an accurate description of the new per-day outcome
  ledger row, the file-backed JSON artifact convention, the intentionally-empty
  Input side, and the failed-row-in-addition-to-failed-run behaviour.

## Checks run

- `npx tsc --noEmit` — clean, no errors.
- `npm test` — full suite green (all existing tests pass unchanged; the 6 new
  ledger tests pass). Confirmed via TAP output that all 4 outcome branches plus
  the same-day-overwrite case assert correctly against the scratch DB
  (`LOCALJOBS_DB` redirected automatically per `src/config.ts`'s `isTestEnv()` guard
  — no risk to the production DB).
- No live `vercel` CLI invocation, no real checkout, no paid/remote calls — fully
  hermetic per the existing test file's convention (injected `spawnFn` +
  `callServiceFn`).
- `data/out/` under this workflow is gitignored (`src/workflows/**/data/` in
  `.gitignore`); test-run artifacts land there but are never staged/committed.
- Dashboard not touched — no `npm --prefix dashboard run build` needed for this task.

## Remaining

None. Task complete per "## Done when": one ledger row per run, keyed by
`dayKey(now)`, covering all four outcomes, tests added/passing, `tsc`/`npm test`
green, and the workflow's `CLAUDE.md` no longer claims "no work_items ledger".
