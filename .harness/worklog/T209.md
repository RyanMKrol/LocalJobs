# T209 worklog — Surface movie recommendations as their own section

## What was built

Added `GET /api/movie-recs` and `POST /api/movie-recs/:tmdbId/ignore` endpoints
to `src/api/server.ts`, mirroring the existing movie-gaps wiring. Added `MovieRec`,
`MovieRecs` types and `movieRecs()`, `ignoreMovieRec()` client functions to
`dashboard/app/lib/api.ts`. Added `MovieRecsManager` component to
`dashboard/app/workflows/[name]/page.tsx` rendering a distinct "Recommendations"
section above the renamed "Franchise gaps" section. Both sections appear only for
the `movie-recommendations` workflow.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests pass (including 4 new T209 movie-recs endpoint tests)
- `npm --prefix dashboard run build` — builds cleanly (6.57 kB for [name] page)
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass

## Tests observed

- GET returns empty list (HTTP 200) when recommendations.json absent
- GET overlays notified/ignored correctly
- POST /api/movie-recs/:id/ignore marks rec ignored and GET reflects it
- POST rejects non-numeric id with 400
