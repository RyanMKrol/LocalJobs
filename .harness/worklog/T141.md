# T141 worklog — Remove inert per-minute rate limit from claude-cli service

## What was done

Found the service at `src/services/claude-cli.service.ts` (T044 already moved it to the top-level services dir).

Changes made:
1. `src/services/claude-cli.service.ts` — removed `ratePerMinute: 20`, updated description to "Claude Code CLI (`claude -p`). Free under the plan; sequential use, no rate cap." and updated the JSDoc comment to reflect sequential use + no rate cap.
2. `src/jobs/perfumes/claude.ts` — updated a stale inline comment from "rate-limited, $0" to "metered, $0" (the only other reference to the rate-limited characterization).

## Verification

- Grepped for `ratePerMinute.*20` — only residual was the `claude.ts` comment (fixed).
- `npx tsc --noEmit` — clean.
- `npm test` — all 25 test files, all assertions passed (including `services.test.ts` which verifies the no-limit path still records usage).
- The `callService` no-rate-limit path still calls `recordServiceCall(name)`, so the Services dashboard keeps showing claude-cli call counts — confirmed by `services.test.ts` "a no-limit service meters one row per call".
- No test asserted `ratePerMinute: 20` (none existed).
