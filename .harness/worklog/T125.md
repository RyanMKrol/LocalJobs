# T125 worklog — Expressive workflow descriptions for perfumes + places

## What was done

Rewrote the `description` field in both workflow manifests from terse arrows-style
one-liners to expressive 1–3 sentence paragraphs that explain the workflow's goal,
inputs, outputs, and steps.

**perfumes.workflow.ts** — previously: `'Find Fragrantica URL → headless fetch → parse → research + write markdown.'`

New: explains that it builds a rich markdown profile for each perfume, starting from
a list of names, discovers the Fragrantica page, fetches via headless real-Chrome
(Cloudflare clearance), parses notes/accords from the saved HTML, then generates the
profile using the Claude CLI in cycles until no retryable items remain.

**places.workflow.ts** — previously: `'Ingest Takeout → resolve CID→place_id → enrich (Places API) → enrich with LLM (Gemini).'`

New: explains that it enriches Google saved places with structured data and prose
summaries, ingesting Takeout CSVs, resolving CIDs via headless browser, fetching from
Google Places API, then writing Gemini-generated markdown profiles — with daily spend
quotas to drain the backlog without blowing the monthly cap.

README.md was already accurate and detailed for both workflows; no changes needed.

## Checks

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed (no UI changes, no dashboard build needed)
