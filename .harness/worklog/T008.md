# T008 — Dashboard quick fixes

## Attempt 1 — 2026-06-21

### Changes made

**Fix 1 — Dag.tsx: omit status label when no `statusByJob`**
`dashboard/app/components/Dag.tsx`: conditionally render `dag-node-status` div only when `statusByJob` prop is provided. Structure-only view on `/pipelines/[name]` now shows just the job name.

**Fix 2 — runs/[id]: smart back-link for pipeline-context runs**
`dashboard/app/runs/[id]/page.tsx`: when `run.pipeline_run_id` is set, the back-link goes to `/pipeline-runs/<id>` with label "pipeline run" instead of the standalone job page.

**Fix 3 — layout.tsx: Local Jobs wordmark as a home link**
`dashboard/app/layout.tsx`: `<span class="brand">` → `<a href="/" class="brand">`. Updated `globals.css` to set `color: var(--text)` on `.brand` so the link doesn't render in accent blue.

**Fix 4 — Overview: Recent pipeline runs section**
- `src/db/store.ts`: added `listRecentPipelineRuns(limit)` (all pipeline runs, desc)
- `src/api/server.ts`: added `GET /api/pipeline-runs?limit=` route, imported new store fn
- `dashboard/app/lib/api.ts`: added `api.recentPipelineRuns(limit)` method
- `dashboard/app/page.tsx`: replaced "Recent runs" (job runs) section with "Recent pipeline runs" table linking to `/pipeline-runs/<id>`

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — 9 suites, all passed (60 tests)
- `npm --prefix dashboard run build` — clean, all 7 routes compiled

### Result: done
