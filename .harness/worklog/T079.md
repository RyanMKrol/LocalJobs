# T079: Remove stale job-level Chrome profile; keep only framework-level one

## What was done

**Local deletion (no tracked diff):**
- Deleted `src/jobs/perfumes/data/chrome-profile/` — the orphaned job-level profile that no code referenced. Only the framework-level `data/chrome-profile` remains (pointed to by `defaultChromeProfileDir` in `src/core/browser.ts`).

**Regression test (tracked):**
- `src/jobs/perfumes/config.test.ts` already contained the required assertions (lines 28-34):
  - `perfumesConfig.profileDir === defaultChromeProfileDir`
  - `profileDir` is an absolute path
  - `profileDir` does not contain 'perfumes' (not job-local)
  - `profileDir` ends with 'chrome-profile'

These assertions were in place from prior work, so no new test lines were needed.

## Verification

- `grep -r "perfumes/data/chrome-profile"` → no references in code
- `npx tsc --noEmit` → clean
- `npm test` → all 19 test files passed, including `config.test.ts ✓ perfumes profileDir is framework-level (not job-local)`
- `ls src/jobs/perfumes/data/` → only `out/` and `raw/` remain; no `chrome-profile/`

## Outcome

`doneWhen` satisfied:
- `src/jobs/perfumes/data/chrome-profile` no longer exists locally ✓
- `data/chrome-profile` (framework-level) remains and is the only profile any code points at ✓
- Unit test asserts `perfumesConfig.profileDir === defaultChromeProfileDir` ✓
- `npx tsc --noEmit` and `npm test` green ✓
