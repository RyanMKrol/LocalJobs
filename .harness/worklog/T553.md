2026-07-14 T553

## What I did
Deleted the standalone-job execution path that T070 orphaned:

1. `src/core/executor.ts` — removed the exported `runJob()` function, its
   `import { notifyRun }`, and the now-dead `RunResult` interface (existed only
   for `runJob`'s return type). Reworded `runJobForWorkflow`'s overlap-skip
   reason from "a standalone run of this job is already active" to
   "another active run of this job is already in progress" (accurate: two
   near-simultaneous runs of the SAME job name, not a "standalone" concept
   that no longer exists) and updated its doc comment accordingly.
2. `src/core/notifier.ts` — removed `notifyRun()` and `notifyStage()`, plus
   their now-unused imports (`getRun`, `stuckCount`, `workItemCounts`, the
   `RunStatus` type). `notifyWorkflow`/`push`/`sendNtfy`/`sendMacNotification`
   are unchanged.
3. `src/core/types.ts` — removed `'queued'` from the `RunStatus` union (nothing
   ever writes it — confirmed via grep across `src/db/store*`).
4. `dashboard/app/lib/api.ts` — removed `'queued'` from its mirrored
   `RunStatus` union.
5. `dashboard/app/ui.tsx` — removed the `queued: 'Queued'` label and
   `queued: '🕒'` emoji entries.
6. `dashboard/app/workflow-runs/[id]/page.tsx` — dropped the dead
   `r.status !== 'queued'` clause from `completedStages` (only `!== 'running'`
   remains, matching the actual RunStatus union). Inspected line 91-95's
   `<span className="badge queued">`: it's the run-limit badge merely reusing
   the `queued` CSS class name for its grey-pill styling (title says "This run
   was limited to N originating inputs…") — not a status render — so left
   the class/markup untouched, per the task's stated permitted exception.
7. `src/core/executor.test.ts` — updated the file header comment (no longer
   describes a "standalone" path) and rewrote the overlap-guard test to drive
   `runJobForWorkflow` (the sole entrypoint now) instead of the deleted
   `runJob`, asserting `status === 'skipped'` + a recorded skipped run row
   (2 total rows: the perpetual running one + the recorded skip) instead of
   the old `{ skipped, runId: null, reason: 'already running' }` shape.
8. No action needed for `src/db/schema.sql` / `dashboard/app/globals.css`
   (both still mention `queued`) — neither file is in this task's scope list;
   the schema.sql comment is a stale but harmless status-value listing, and
   the globals.css `.badge.queued` rule is the CSS backing the permitted
   run-limit-badge exception in item 6.

## Checks run (all green)
- `npx tsc --noEmit` — clean.
- `npm test` — all suites pass (no regressions; `workflow-executor.test.ts`'s
  T189 comments about `notifyStage` never being called remain valid — that
  function no longer exists at all, so the assertion is trivially still true).
- `npm --prefix dashboard run build` — compiles + prerenders successfully.
- Grep verification (word-boundary, excluding `runJobForWorkflow`/`runJob.ts`/
  `runJobScript`/the `projects-sync` self-clone under `src/workflows/projects-sync/data/`):
  `runJob` and `notifyRun` → zero hits. `notifyStage` → only the two historical
  comment lines in `workflow-executor.test.ts` (expected, per the task's
  done-when). `queued` → only the permitted run-limit-badge exception plus
  unrelated English usages ("T469 is queued") and the two out-of-scope files
  noted above.

## What remains
Nothing outstanding for this task.
