# T425 — dedicated minIntervalMs service for Trading212 instruments-metadata

Added `src/services/trading212-instruments.service.ts` (new file, default-exports a separate
`ServiceDefinition` named `trading212-instruments`) setting `minIntervalMs: 50_000` (env-overridable
via `TRADING212_INSTRUMENTS_MIN_INTERVAL_MS`), matching Trading212's documented 1-request-per-50s
limit for the instruments-metadata endpoint. Left `dailyCap`/`monthlyCap` modest (20/day, 200/month,
env-overridable) as a loop-bug guard. Left the existing `trading212` service (portfolio fetch,
`ratePerMinute`) completely unchanged.

Updated both `fetchInstrumentsMetadata` call sites to route through the new service name instead of
sharing `trading212`'s budget:
- `src/workflows/stocks-sync/stages/stocks-resolve-names.ts`
- `src/workflows/stock-digest/stages/stock-portfolio-snapshot.ts`

(Per the task's own note: T413/T414/T415 had already landed, moving the call sites to
`stocks-resolve-names.ts` and `stock-portfolio-snapshot.ts` rather than the `stocks-snapshot.ts`
path named in the original spec — updated the actual current call sites.)

Added `src/services/trading212-instruments.service.test.ts`: asserts the definition's fields
(name/minIntervalMs/paid/category), and a functional test (mirroring `services.test.ts`'s
`svc-mi` pattern) proving `callService` mechanically throttles a second call within the interval —
registered under a short-interval test copy of the def so the test runs fast rather than waiting
the real 50s.

Updated docs in scope: `src/workflows/stocks-sync/CLAUDE.md`, `src/workflows/stock-digest/CLAUDE.md`
(both note the new dedicated service), and root `CLAUDE.md` (added `trading212-instruments` to the
service `category` enumeration list — the only place root CLAUDE.md mentions Trading212 services by
name; there was no other rate-limit-in-a-code-comment sentence to update there).

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` (full suite) — 211/211 pass, including the new test file and the existing
  `stocks-resolve-names`/`stock-portfolio-snapshot` tests (neither asserted a specific service name,
  so no assertion updates were needed).
- No live/paid Trading212 calls made.
