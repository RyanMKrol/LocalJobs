# T235 worklog

## What was done

Reordered the `MODES` array in `dashboard/app/ui.tsx` so System is first, followed by Light then Dark. Single line change — no behaviour change.

## Verification

- `npx tsc --noEmit` — pass
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — green
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass
