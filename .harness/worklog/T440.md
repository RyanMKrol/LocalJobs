# T440

Rewrote `vercel-redeploy` job's `description` in
`src/workflows/vercel-daily-redeploy/vercel-redeploy.job.ts` into a paragraph covering:
why it uses the real Vercel CLI instead of a Deploy Hook (ryankrol.co.uk disconnected its Vercel
Git integration), reliance on the machine's existing persistent Vercel CLI login (no credential to
provision), the `RYANKROL_CO_UK_PATH` soft-skip behavior, and the job's own internal 10-minute
timeout-and-kill separate from its outer timeout.

Verified: `npx tsc --noEmit` and `npm test` both green.
