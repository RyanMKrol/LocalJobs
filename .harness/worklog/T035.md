# T035: Services — colour free services green

## Attempt 1 (2026-06-22)

### What was done
- Added `free` CSS class to the free-service pill span in `dashboard/app/services/page.tsx` (line 94).
- Added `.pill.free` rule in `dashboard/app/globals.css` using `--green` (#3fb950) and a matching rgba border tint.

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — 16 test files, all passed
- `npm --prefix dashboard run build` — compiled successfully, no errors

### Result
Free-service pills now render green; paid pills remain yellow (unchanged).
