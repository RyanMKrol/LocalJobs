# T186 worklog

## What was built

- `src/db/schema.sql`: Added `service_consumers (service_name, job_name, last_used)` table with UNIQUE constraint on the pair + index on `service_name`.
- `src/db/store.ts`: Added `recordServiceConsumer(serviceName, jobName)` (upsert) and `listServiceConsumers(serviceName)` (returns rows joined to `workflow_jobs`).
- `src/core/services.ts`: In `callService`, reads `process.argv[2]` as the calling job name (the child entrypoint always receives job name there) and calls `recordServiceConsumer` best-effort before running the fn.
- `src/api/server.ts`: Added `GET /api/services/:name/consumers` endpoint — returns consumers grouped by workflow.
- `dashboard/app/lib/api.ts`: Added `ServiceConsumerGroup` interface and `api.serviceConsumers(name)` call.
- `dashboard/app/services/page.tsx`: Service name is now a button; clicking opens a modal `ConsumersPanel` showing workflows + linked jobs with last-used timestamps. Edit limits UI unchanged.
- Tests added to `src/core/services.test.ts` (recordServiceConsumer upsert dedup, listServiceConsumers filtering) and `src/api/server.test.ts` (GET consumers empty + grouped).
- CLAUDE.md + README.md updated.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all passed
- `npm --prefix dashboard run build` — succeeded
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
