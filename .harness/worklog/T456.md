# T456 — 2026-07-07

## What
Removed the `confirm(...)` guard from `MissingSeasonsManager`'s single-item `ignore()`
function in `dashboard/app/workflows/[name]/page.tsx` (was at line 658). Clicking a row's
"✕ Ignore" button now calls the API immediately — busy/disabled state and error surfacing
are unchanged, only the confirm gate is gone.

Left untouched (verified by grep of every `confirm(` in the file):
- MissingSeasonsManager's `unignore()` confirm (line ~671, now shifted up by 2 lines)
- MissingSeasonsManager's bulk `ignoreShow()` "Ignore all" confirm
- All of TvRecsManager / MovieRecsManager / MovieGapsManager's single-item and bulk
  ignore/unignore confirms

## Checks run
- `npx tsc --noEmit` — clean, no output.
- `npm test` — full suite green (registry, gate-coverage, contracts, executor, dag, etc.);
  no `not ok` lines, only test names containing the word "fail"/"failed" as part of their
  description.
- `npm --prefix dashboard run build` — succeeded, produced `/workflows/[name]` route as before.
- `node dashboard/scripts/visual-check.mjs` — 40/41 captures passed. The one failure
  (`workflow-run-skipped` → `/workflow-runs/skipped`, `.rf-dag-node` selector timeout) is
  pre-existing and unrelated: reproduced identically on a clean `git stash` of this change
  (confirmed by re-running the check against the unmodified file). It touches a different
  route (`workflow-runs/[id]`) than the one this task edits (`workflows/[name]`).
- Visually inspected `workflow-missing-tv-seasons.png` — the missing-seasons table, its
  active-item "✕ Ignore" buttons, the Ignored panel, and Un-ignore buttons all render
  correctly with no layout regressions. No new interactive state to add to
  `_dashboard-harness.mjs` — the change is purely behavioral (a removed confirm dialog
  produces nothing new to screenshot; there was no dialog to capture before or after).

## Result
done T456
