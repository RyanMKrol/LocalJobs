# T128 — Review + choose: workflow-run output line-item style (from T116)

Reviewed by the owner on 2026-06-23.

**Chosen style: `meta`** — the `out-meta` style: the output filename rendered as a
link plus small at-a-glance metadata (file size · "click to preview"), clicking
opens the full markdown popover.

Follow-up **T133** will hardcode `meta` as the sole output line-item rendering,
remove the live style toggle and the four unused styles (`filename` / `title` /
`excerpt` / `chip`), and drop their now-unused code + CSS. (Per the T129 convention:
chooser T116 → review T128 → hardcode-the-winner follow-up T133.)
