# T468 — split movie-recommendations into recs-only + missing-movies (gaps-only)

2026-07-12

## Starting state

The checkout was NOT actually clean of prior T468 history: `main` had two earlier build attempts
(`efb3950`+`1f2daf6`, audited PASS in `.harness/worklog/T468.audit.md`) that were both reverted
(`b9ca96f`, `859ee3a`). The second revert (`b9ca96f`, reverting only the second of the two
commits) had left `main` in a broken state that the first revert (`859ee3a`) did not fix: a
leftover empty `src/workflows/missing-movies/` directory, and — critically —
`src/workflows/movies/movies.workflow.ts` still referenced the `franchise-gaps` job while its
`.ts`/`.job.ts`/`.test.ts` files were gone. `npm test` on the starting tree failed at
`registry.test.ts` with "standalone jobs are not allowed" (orphaned `rec-merge`, `movie-gaps-notify`,
`rec-auteur`, etc. — every job below `movie-snapshot` had no valid workflow membership because the
manifest pointed at a non-existent stage).

Confirmed via `git diff efb3950^ HEAD -- src/workflows/movies CLAUDE.md` that current `HEAD` was
byte-identical to the pre-split base (`efb3950^`) except for that dangling `franchise-gaps`
deletion — i.e. no unrelated drift since the earlier (correct, audited) attempt was built.

## What I did

Rather than re-deriving the split from scratch, reused the previously-audited-PASS diff: applied
`git checkout 1f2daf6 -- CLAUDE.md src/workflows/movies src/workflows/missing-movies`, which
reconstructs the exact tree state of the last commit of the earlier attempt (audited PASS) onto the
current clean-except-for-the-dangling-reference base — restoring `franchise-gaps.*` under the new
`missing-movies/stages/` (not `movies/stages/`), fixing the orphan-job breakage as a side effect of
correctly completing the split. Re-verified the result is still correct against the current T468
spec / T467 design record before committing (nothing has moved since; the earlier decisions are
still current).

## Result

- `movie-recommendations` (`src/workflows/movies/`): `movie-snapshot` → 8 rec branches → `rec-merge`
  → `movie-recs-notify` (recs-only terminal notify, new job name `movie-recs-notify`). No more
  `franchise-gaps` member.
- `missing-movies` (new workflow, own folder `src/workflows/missing-movies/`): its own
  `plex-movie-snapshot` (skips `taste-profile.json` — only `franchise-gaps` consumes this
  workflow's snapshot and it never reads the taste profile) → `franchise-gaps` (job name +
  contracts unchanged, now pointed at the new snapshot's output) → `movie-gaps-notify` (job name +
  `NOTIFY_JOB = 'movie-gaps-notify'` ledger key unchanged — zero `work_items` migration needed).
  Weekly schedule `0 9 * * 1` (Monday 09:00, per the T467 design decision — matches
  `missing-tv-seasons`'s cadence).
- `RECS_JOB = 'movie-recs'` / `recKey()` unchanged (recs ledger stays in `movies/recs.ts`).
- Every DAG edge in both workflows has a produces/consumes gate (`gate-coverage.test.ts` passes) —
  `missing-movies/contracts.ts` covers snapshot→gaps and gaps→notify; `movies/contracts.ts` keeps
  snapshot→branch, branch→merge, merge→notify.
- `src/workflows/movies/config.ts` keeps a `gapsOut` compatibility re-export (pointing at
  `missingMoviesConfig.gapsOut`) and `movies/stages/notify.ts` re-exports the legacy
  `NOTIFY_JOB`/`gapKey` constants, purely so `src/api/server.ts` (out of this task's scope) keeps
  resolving without a code change — documented inline in both files and both workflows' `CLAUDE.md`.
- `src/workflows/movies/CLAUDE.md` rewritten recs-only (franchise-gaps DAG description and the
  gaps ignore-to-suppress mechanism moved out); new `src/workflows/missing-movies/CLAUDE.md`
  documents the new workflow's DAG, stages, ledger, and the (unchanged) ignore-to-suppress
  mechanism/endpoints.
- Root `CLAUDE.md` "Shipped example workflows" table split into two rows (movie-recommendations:
  recs-only; missing-movies: franchise-gap audit only) — 15 shipped examples now (was 14); the
  Plex-client shared-workflow count/list bumped 6→7; the T145/T146 ignore-mechanism prose updated
  to reflect which workflow each job now lives under.
- `src/api/server.ts`, `dashboard/`, `ignoreSurfacedItem`/`unignoreSurfacedItem`, and the
  `work_items` ledger are completely untouched, per the design record — the ledger is keyed by job
  name/item key, not workflow membership, so no migration was needed or performed.

## Checks run

- `npx tsc --noEmit` — exit 0, no errors.
- `npm test` — 252/252 pass, 0 fail, 0 cancelled, exit 0.
- Specifically confirmed `src/core/gate-coverage.test.ts` passes (6/6 assertions, including the
  no-orphan-job checks) and `src/workflows/registry.test.ts` passes with no orphan-job or
  duplicate-workflow-name errors — both workflows load cleanly from the real registry.

## Scope note (unchanged from the earlier attempt's finding)

`src/api/server.ts` and `dashboard/` were out of this task's scope (not in the `scope` list), so
the compatibility shims above (`gapsOut` alias, re-exported `NOTIFY_JOB`/`gapKey`) keep those
files' existing imports/reads working unmodified. The follow-up task T469 (already in the backlog,
`dependsOn: [T468]`) moves `MovieGapsManager` to the `missing-movies` detail page and can delete
these shims once `server.ts`'s imports are repointed.
