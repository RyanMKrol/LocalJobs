# T069 worklog — Make the DAG prettier — outlined/boxed job nodes

## Attempt 1 — 2026-06-22

### What was done

Updated `dashboard/app/globals.css` (lines 108–136) to make DAG job nodes render as proper boxed cards with a **left accent bar** for status colouring, as specified in the design note.

**Changes to `.dag-node`:**
- Background: `var(--panel-2)` → `var(--panel)` (slightly more elevated, clearer card surface)
- Added `border-left-width: 3px` (creates the accent bar; left side is 3px, other sides remain 1px)
- Added `padding-left: 14px` (extra breathing room between accent bar and text)
- Added `box-shadow: 0 1px 3px rgba(0,0,0,.3)` (subtle elevation / card feel)
- Added `transition: border-color .15s, box-shadow .15s` (smooth hover)

**Status accent changed from whole-border tinting to left-bar only:**
- `.dag-node.success` → `border-left-color: var(--green)` (was `border-color: rgba(63,185,80,.5)`)
- `.dag-node.running` → `border-left-color: var(--accent)` (was `border-color: rgba(88,166,255,.6)`)
- `.dag-node.failed/.timeout` → `border-left-color: var(--red)` (was `border-color: rgba(...)`)
- Same pattern for backlog states: `.done`, `.needs-human`, `.selected`, `button:hover`

**Hover states updated** to set both `border-color` and `border-left-color: var(--accent)` so the accent bar matches on hover.

**Arrow spacing:** `.dag` gap increased from `10px` to `14px` for breathing room between waves.

### Observed

- `npm --prefix dashboard run build` → clean, no errors
- `npx tsc --noEmit` → clean
- `npm test` → all tests passed (8 jobs, 4 services, 2 workflows)
- `node dashboard/scripts/mobile-check.mjs` → all 9 pages pass

### Result

done
