# T322 — POST /api/workflows/reset-output-all bulk output-reset endpoint

Note: this task was previously attempted and blocked (2026-07-02) because
`T322.scope` in `TASKS.json` did not include `dashboard/app/lib/api.ts`, even
though the spec required the client helper. The scope has since been fixed to
include both `src/api/server.ts` and `dashboard/app/lib/api.ts`; this attempt
completes the task fully.

## What was built

- Added `POST /api/workflows/reset-output-all` in `src/api/server.ts`, registered
  BEFORE the existing `:name`-based `POST /api/workflows/:name/reset-output` route
  (matches `parts[1]==='workflows' && parts[2]==='reset-output-all' && parts.length===3`,
  so it can't be shadowed/misrouted — in practice the two never collide anyway since
  the existing route keys off `parts[3]==='reset-output'` on a 4-segment path).
- Iterates every workflow from `listWorkflows()`. For each:
  - If `workflowRunInProgress(name)` is true → SKIP (status `'skipped'`, reason
    `'active run in progress'`) — does NOT block the reset of any other workflow
    (best-effort across the fleet, not all-or-nothing).
  - Otherwise runs the exact same sequence as the single-workflow endpoint:
    `findWorkflowDataOut` → `resetWorkflowOutput` → `deleteDataOutContents`.
- Responds 200 with `{ ok, totalWorkflows, resetCount, skippedCount, results[] }`,
  each result entry carrying either the reset counts (`itemsDeleted`/`runsDeleted`/
  `wfRunsDeleted`/`filesRemoved`/`outDir`) or the skip `reason`.
- Logs one summary line via `console.log` naming reset vs skipped workflows and why.
- Sits behind the same global `authoriseMutation` loopback/token guard as every
  other POST route (applied once, globally, before routing — no separate guard
  needed for this route).
- Added `api.resetAllWorkflowsOutput()` in `dashboard/app/lib/api.ts`, mirroring
  the existing `api.resetWorkflowOutput(name)` helper: POSTs to the new endpoint
  with no body and returns the typed parsed JSON response, ready for the paired
  Admin-page UI follow-up task to consume.
- Added a unit test in `src/api/server.test.ts` (`T322` block) seeding two
  workflows in the scratch DB — one idle with a work item to reset, one with a
  `workflow_runs` row left in `'running'` status to simulate an active run — and
  asserting: the idle workflow is reset (counts > 0), the active-run workflow is
  skipped with the exact reason string, and its work item is left untouched
  (`isWorkItemDone` still true afterward).

## Verification run

- `npx tsc --noEmit` — clean.
- `npm test` — full suite green: `# tests 153 / # suites 42 / # pass 153 / # fail 0`,
  including the new T322 test
  (`✓ POST /api/workflows/reset-output-all: resets idle workflows, skips active ones`).
  Observed live console output during the test run:
  `[api] reset-output-all: 17 reset, 4 skipped (identical-wf: active run in progress;
  io-api-wf: active run in progress; io3-api-wf: active run in progress; t322-active-wf:
  active run in progress)` — confirms the bulk endpoint correctly walks EVERY workflow
  seeded across the whole test file (not just the two from this task's own block),
  and correctly skips every workflow left with an active run by earlier test blocks.
- No dashboard build needed (no `dashboard/app/**` page-component changes — only the
  plain-TS `lib/api.ts` client helper, per the task's own scope note).
- No live/paid API calls made — pure scratch-DB unit test, no external services touched.

## Scope

Touched only `src/api/server.ts`, `dashboard/app/lib/api.ts` (both in-scope), and
the test file `src/api/server.test.ts` (an allowed `*.test.ts` file), plus this
worklog. No docs in scope for this task.
