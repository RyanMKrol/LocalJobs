# T182 worklog — concurrent top-up branch re-prompts

## What was built

- Added `recsTopUpConcurrency` config (env `MOVIES_RECS_TOPUP_CONCURRENCY`, default 4) to `src/jobs/movies/config.ts`
- Added injectable `topUpConcurrency` to `MergeOpts` for tests
- Added `runBounded<T>` helper in `merge.ts`: worker-pool pattern (N workers drain a queue), runs at most N branches concurrently
- Refactored `buildDefaultTopUp` to use `runBounded` instead of sequential `for` loop; logs each branch as it completes (correct attribution under out-of-order completion)
- Per-branch error handling preserved: a `try/catch` around `collectBranchSuggestions` logs warn and returns (other branches continue)

## Test: merge-concurrency.test.ts

- **Test 1**: Injects `runClaude` with 20ms delay; asserts peak concurrent calls ≤ CAP=2 AND > 1 (i.e., genuinely parallel, not serial). Uses real `buildDefaultTopUp` path via `runClaude` + `topUpConcurrency` injection.
- **Test 2**: Injects a `topUp` that fans 4 fake branches concurrently with different delays; asserts fastest branch completes first, slowest last; asserts each branch's results are attributed to the correct branch id in the logs.
- **Test 3**: Asserts `recsTopUpConcurrency` default is in [1, BRANCHES.length].

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all 4 top-level test suites pass, T182 tests all green
- No live paid API calls made
