# T133 worklog

## Outcome: done

Hardcoded the `meta` output style as the sole output line-item presentation and removed the toggle + four unused styles.

## Changes

**dashboard/app/workflow-runs/[id]/page.tsx**
- Removed `OUTPUT_STYLES`, `useOutputStyle` from import and `OutputStyle` type import
- Removed `outputStyle: OutputStyle` prop from `OutputCell` component
- Collapsed the five style branches down to just the meta branch (filename link + file-size metadata)
- Removed `const [outputStyle, setOutputStyle] = useOutputStyle()` from `IoPanel`
- Removed the `<div className="output-style-bar">` selector block
- Removed `outputStyle={outputStyle}` from the `<OutputCell>` call site

**dashboard/app/ui.tsx**
- Removed `OUTPUT_STYLES` export, `OutputStyle` type, `OUTPUT_STYLE_KEY`, `DEFAULT_OUTPUT_STYLE`, and `useOutputStyle` function (confirmed only imported by the workflow-run page)

**dashboard/app/globals.css**
- Removed selector-bar rules (`.output-style-bar`, `.output-style-label`, `.output-style-btn` + `:hover`/`.active`)
- Removed Style 1 (`.out-filename*`), Style 2 (`.out-title*`), Style 3 (`.md-preview*`), Style 5 (`.out-chip*`) CSS blocks
- Kept Style 4 / meta (`.out-meta*`) rules intact
- Confirmed `.md-preview` was only used in the now-deleted excerpt branch — safe to remove

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — clean build (8/8 pages)
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (0px overflow, 0 boundary-crossing elements)
