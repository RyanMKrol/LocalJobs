# T436

Rewrote `description` on the 2 `projects-sync` jobs to real paragraphs grounded in
`src/workflows/projects-sync/CLAUDE.md` and the stage source:

- `github-sync.job.ts` — now names the GitHub REST endpoint used
  (`GET /users/<GITHUB_USERNAME>/repos`), the filtering/sort behavior, the shared `github`
  service routing per page, and that it re-scans/re-records every run (not idempotent-skip)
  so catalog fields stay fresh.
- `project-summarize.job.ts` — now covers the shallow-clone-into-`data/repos/` step, the
  distinct `runClaudeWithRepoAccess` invocation (cwd set to the repo, `--add-dir`/scoped
  `--allowedTools Read Glob Grep`, no Bash/Write/Edit), the enforced `project.template.md`
  shape contract (validated post-hoc, throws on mismatch), the "never invent facts"
  instruction, and the `pushedAt`-marker idempotency skip.

## Verification
- `npx tsc --noEmit` — clean, no output.
- `npm test` — full suite green (exit 0), including `gate-coverage.test.ts` and the
  `projects-sync` job/contract tests (descriptions aren't asserted verbatim, so no test
  changes were needed).

No paid API calls made. No files touched outside the 2 scoped job files + this worklog.
