# T383

Generalized `stageIoLists` in `src/db/store.ts` to accept `string[]` on both sides
(`outputJobNames`, `inputJobNames`) instead of a single job name for outputs. Both
sides now run the same job-name-IN-list query against `work_items` joined to
`work_item_runs` (run-scoped linkage), unioned across all names — no `root_key`
collapsing/pairing introduced.

Updated the sole caller, `GET /api/workflow-runs/:id/stage-io` in
`src/api/server.ts`, to pass `[jobParam]` — request/response shape for the
existing single-job case is unchanged.

## Verification
- `npx tsc --noEmit`: passes.
- `npm test`: 168/168 pass, including:
  - New assertion block in `src/db/store.test.ts` (sio-* fixture) calling
    `stageIoLists(['sio-b', 'sio-c'], ['sio-a'], runS)` and asserting the returned
    `outputs` is the union of both jobs' rows (4 total: 3 from sio-b + 1 from sio-c).
  - Updated existing `stageIoLists` calls in the same file to the new array-based
    signature (`stageIoLists('sio-b', ...)` → `stageIoLists(['sio-b'], ...)`).
  - New regression test in `src/api/server.test.ts` ("single-job shape unchanged
    after T383 signature change") pinning the exact `{ inputs, outputs,
    predecessorJobs, job }` values for `GET .../stage-io?job=sio-api-b` against the
    existing `sio-api-*` fixture.
- `npm --prefix dashboard run build`: succeeds (dashboard client untouched, calls
  the same endpoint shape).
