# T123 worklog — Backlog list: give harness-buildable cards an amber pill + colour scheme

## Attempt 1 — 2026-06-23

### What was done
- Added `--amber: #e8a020` CSS token to `dashboard/app/globals.css` alongside `--accent`, `--green`, `--red`, `--yellow`.
- Added `.pill.buildable` style (amber text, amber-tinted background, amber border) analogous to existing `.pill.done` and `.pill.in-progress`.
- Updated `TaskCard` in `dashboard/app/backlog/page.tsx`:
  - Added `buildable` boolean: `t.status !== 'done' && !human`.
  - Applied `borderColor: 'var(--amber)'` on the card panel when `buildable`.
  - Rendered `<span className="pill buildable">🤖 buildable</span>` pill for buildable tasks (done cards via `DoneRow` — unaffected).
- Done cards (`DoneRow`) and needs-human cards unchanged.

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass

### Result
done
