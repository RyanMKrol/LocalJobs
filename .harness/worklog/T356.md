# T356 — Boxed empty state for generic workflow Output section

## What changed
`dashboard/app/components/WorkflowOutputSection.tsx`: the empty-state branch (`data && items.length
=== 0`) now renders `<div className="panel"><p className="empty-state-panel">…</p></div>` instead of
a bare `<p className="muted" style={{ fontSize: 13 }}>`, matching the centered-panel convention
already used by `TvRecsManager`'s empty state on the same workflow detail page. Message text
unchanged: "No output yet. Run the workflow — produced items will appear here." No other branch
(loading/error/populated table) touched. No CSS changes needed — `.empty-state-panel` already
provides the centered-flex/padded/min-height-96px styling.

## Scope confirmation
Re-checked `dashboard/app/workflows/[name]/page.tsx`: `WORKFLOWS_WITH_SPECIFIC_MANAGERS` still =
`{ movie-recommendations, missing-tv-seasons, tv-recommendations }`, and the render line is still
`{!WORKFLOWS_WITH_SPECIFIC_MANAGERS.has(name) && <WorkflowOutputSection workflowName={name} />}`.
So this one-file fix applies automatically to every other real workflow: places, perfumes,
listening-digest, projects-sync, claude-warmer, stocks-sync, stock-digest, workouts-sync — no
per-workflow edits needed or made.

## Harness living-artifact check
`dashboard/scripts/_dashboard-harness.mjs` already had (from T347) an `/output-items` fixture route
(populated for `places`, empty for others) and a `PAGES` entry
`{ name: 'workflow-empty-output', path: '/workflows/workouts-sync', waitFor: ['.rf-dag-node'] }`
covering exactly this empty state. No new fixture/PAGES/FLOWS entry was needed — confirmed by
reading the file before finishing (no changes made to it).

## Checks run
- `npx tsc --noEmit` — clean.
- `npm test` — 134/134 pass, 0 fail.
- `npm --prefix dashboard run build` — succeeded.
- `node dashboard/scripts/visual-check.mjs` — all 32 pages captured; visually inspected:
  - `workflow-empty-output.png` (workouts-sync): "No output yet…" now renders as a centered box
    inside the Output panel, matching the TV recs / movie recs empty-state look — no longer
    left-aligned plain text.
  - `workflow.png` (places): populated output table (2 items, "A Resolved Place" / "place:second",
    View button) renders unaffected — confirms the populated branch wasn't touched.
- `node dashboard/scripts/mobile-check.mjs` — all 18 pages pass, 0px horizontal overflow including
  `workflow-empty-output` (workouts-sync).

## Result
done
