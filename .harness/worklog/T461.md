# T461 — 2026-07-08

Extended `PerfumeInput` (src/workflows/perfumes/types.ts) with 8 optional fields sourced from
PerfumeRatings DynamoDB rows: `description`, `rating`, `dateAdded`, `ownership`,
`personalLongevity`, `personalProjection`, `personalSeasons`, `applicationSpots` — renamed to
avoid colliding with build stage / template frontmatter keys of similar name.

Updated `loadPerfumes()` (src/workflows/perfumes/lib.ts) to destructure the 8 raw Dynamo fields
(`description`/`rating`/`date`/`ownership`/`longevity`/`projection`/`seasons`/`applicationSpots`)
alongside the existing required fields, and map each onto its renamed `PerfumeInput` key using the
same lenient per-field pattern as the existing `fragranticaUrl` handling: present + correctly typed
→ included; wrong-typed or absent → silently omitted (item itself still returned). Required-field
validation (`id`/`title`/`designer`/`type`) is unchanged.

Updated `lib.test.ts`:
- First test now asserts all 8 new fields are captured under their renamed keys from a fixture item
  with every field present.
- Added a case asserting a wrong-typed optional field (`rating: 'five'`, `longevity: 'long'`,
  `seasons: 'Fall'`) is silently omitted without dropping the item.
- Added a case asserting an item with none of the 8 optional fields present produces a
  `PerfumeInput` with none of those 8 keys set.

## Checks run
- `npx tsc --noEmit` — clean (all new fields optional, no existing caller of `PerfumeInput`/
  `loadPerfumes` needed changes).
- `npm test` — green, 244/244 tests passing (perfumes `lib.test.ts` included).

## Scope
Touched only src/workflows/perfumes/types.ts, src/workflows/perfumes/lib.ts,
src/workflows/perfumes/lib.test.ts, plus this worklog — matches the task's scope exactly.
