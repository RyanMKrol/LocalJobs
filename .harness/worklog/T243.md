## T243 — Done section reviewed/not-reviewed breakdown

### What was done
Added reviewed/not-reviewed counts to the done section header in `dashboard/app/backlog/page.tsx`.
Changed `✅ Done ({allDone.length})` to `✅ Done ({allDone.length} · {reviewedCount} reviewed · {allDone.length - reviewedCount} not reviewed)`.
`reviewedCount` was already computed on line 166; the change is purely additive in the summary label.

### Verification
- `npm --prefix dashboard run build` — green
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass (backlog explicitly checked)
