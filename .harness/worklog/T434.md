# T434: paragraph-length job descriptions for workouts-sync workflow

Expanded `description` on both jobs in `src/workflows/workouts-sync/`:

- `hevy-sync.job.ts`: now covers pagination against the Hevy API, rate-limiting via
  `src/services/hevy.service.ts`, the work_items idempotency ledger (keyed by Hevy workout id),
  and exactly what "full data" means (title, exercises, sets), plus the append-only history file.
- `workouts-progress.job.ts`: now covers the exact baseline (6 months prior calendar month) vs
  current (most recently completed calendar month) period definitions, the three metrics (best
  single set, total volume, Epley-estimated 1RM) with their precise formulas, the null-weight/reps
  skip rule, and per-calendar-month idempotency.

Both grounded in root `CLAUDE.md`'s workouts-sync table entry and
`src/workflows/workouts-sync/CLAUDE.md`'s full stage documentation, cross-checked against
`hevy-sync.ts` / `workouts-progress.ts` implementations.

## Verification

- `npx tsc --noEmit` — green.
- `npm test` — green (full suite).
- No dashboard changes, no paid API calls made.
