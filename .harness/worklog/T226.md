# T226 worklog — Input→Output filter by item state

## What was built

Added filter chips (All / success / failed / skipped) to the Input→Output mapping panel
on the workflow-run detail page (`/workflow-runs/[id]`).

### Changes
- `dashboard/app/workflow-runs/[id]/page.tsx`: Added `rowEffectiveState` helper (uses
  output status for multi-stage workflows, falls back to input status), `IO_FILTER_STATES`
  constant, `filter` state in `IoPanel`, and chip bar UI. The "State" column now shows
  input status with an inline `→ outputStatus` arrow when they differ (multi-stage).
  Empty-filtered state shows a clear "No {filter} items" message.
- `dashboard/app/globals.css`: Added `.io-filter-bar`, `.io-filter-chip` (active + per-state
  colour variants), `.io-filter-count`, and `.io-state-arrow` rules.

### Verification
- `npx tsc --noEmit`: clean
- `npm test`: all pass
- `npm --prefix dashboard run build`: clean build
- `node dashboard/scripts/mobile-check.mjs`: all 9 pages pass
