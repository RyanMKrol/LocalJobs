# T288 — projects-sync: add clone + Claude per-project markdown summary stage

## What was built

- `src/jobs/projects-sync/stages/project-summarize.ts` — new stage impl:
  - `readCatalog` reads `data/out/projects.json` (written by `github-sync`).
  - Skip check: compares each repo's stored `work_items` `detail.lastCommitSha`
    (actually the catalog's `pushedAt` value, used as the commit-sha-equivalent
    marker — cheaply available from the catalog, no extra GitHub API call needed)
    against the current catalog value. Match → skip (no clone, no Claude call).
  - Mismatch → `cloneOrPullRepo` (shallow clone via `git clone --depth 1`, or
    `git fetch --depth 1` + `reset --hard origin/HEAD` if already cloned) into
    `src/jobs/projects-sync/data/repos/<repo-name>/` (covered by the existing
    `src/jobs/**/data/` gitignore rule — verified with `git check-ignore`).
  - Reads README (`README.md`/`Readme.md`/`README`/`readme.md`), builds a prompt,
    calls `runClaude` from `src/services/claude.ts` (already service-gated via
    `claude-cli`, used as-is, not modified) with model from
    `PROJECTS_SYNC_CLAUDE_MODEL` (default `claude-sonnet-5`).
  - Writes `data/out/<repo-name>.md` and records `work_items` success with
    `detail: { name, markdown, lastCommitSha }` — the standard T110 shape so the
    unified Output section picks it up with no dashboard changes.
  - All external effects (clone/pull, Claude call, README read, markdown write)
    are injectable for hermetic testing.
- `src/jobs/projects-sync/stages/project-summarize.job.ts` — job definition,
  timeout 600s, maxRetries 2.
- `src/jobs/projects-sync/config.ts` — added `reposDir`.
- `src/jobs/projects-sync/projects-sync.workflow.ts` — added `project-summarize`
  as a second member with `dependsOn: ['github-sync']` (2-stage DAG).
- `.env.example` — documented `PROJECTS_SYNC_CLAUDE_MODEL`.
- `CLAUDE.md` / `README.md` — updated the projects-sync description to the
  2-stage shape + sha-based (not calendar-based) idempotency.
- `src/jobs/projects-sync/stages/project-summarize.test.ts` — new hermetic unit
  tests: (a) a repo with a changed marker is cloned + summarized and its
  `work_items` row gets `detail.markdown` set; (b) a repo whose stored marker
  already matches the catalog is skipped — asserts the injected clone/Claude
  mocks were never called; (c) prompt-building includes repo metadata + README.

## Verification performed

- `npx tsc --noEmit` — clean.
- `npm test` — all suites pass (63 tests, including the 3 new
  `project-summarize` tests).
- No live git clone or live Claude CLI call made — all external calls injected
  in tests, matching the existing `github-sync.test.ts` pattern.
- `git check-ignore -v src/jobs/projects-sync/data/repos/throwaway/x.txt`
  confirms the clone destination directory is covered by the existing
  `src/jobs/**/data/` gitignore rule (manually created + removed a throwaway
  file to verify; nothing left behind).
- No `data/out/index.md`-style roll-up file introduced — one `.md` per repo only.

## Result

done T288
