# T361 — rename /services dashboard route to /integrations

## What I did
- `git mv dashboard/app/services/page.tsx dashboard/app/integrations/page.tsx` (removed the now-empty
  `dashboard/app/services/` folder). App Router route path is now `/integrations`.
- `dashboard/app/layout.tsx`: nav link `<a href="/services">Integrations</a>` → `<a href="/integrations">Integrations</a>`.
- `dashboard/next.config.js`: added a `redirects()` block (alongside the existing `rewrites()`)
  redirecting `/services` → `/integrations` with `permanent: false` (307).
- `dashboard/scripts/_dashboard-harness.mjs`: updated all THREE dashboard-route references from
  `/services` to `/integrations` — the `PAGES` entry (`name: 'services'` kept, `path` changed), and
  both FLOWS that load the page first (`service-consumers-modal`, `services-edit-limits-row`). Left
  the `/api/services` + `/api/services/*/consumers` fixture interception untouched (daemon API, not
  the dashboard route).

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 134/134 pass.
- `npm --prefix dashboard run build` — succeeds; build output confirms route `○ /integrations` present
  and no `/services` route.
- `node dashboard/scripts/visual-check.mjs` — all 31 captures pass. Looked at:
  - `services.png` (now captured at `/integrations`): page renders correctly — header nav shows
    "Integrations" as the active-looking tab, heading "Integrations", CLI Tools / Website Scrapes /
    APIs / Uncategorized panels all render as before.
  - `service-consumers-modal.png`: flow drives `/integrations` → clicks the `google-places` service →
    the "Consumers of google-places" modal opens correctly, listing places/perfumes consumers. Confirms
    the interactive state still works from the new route.
- `node dashboard/scripts/mobile-check.mjs` — 18/18 pages pass, `/integrations` (page name `services`)
  shows 0px horizontal overflow, 0 boundary-crossing elements.
- Manual redirect check: ran `next start` on a scratch port, `curl -sI /services` → `307 Temporary
  Redirect` with `location: /integrations`; `curl /integrations` → `200`. Confirms the redirect works
  end-to-end and the new route serves the same content.

## Scope note
Stayed within the declared scope (dashboard/app/services→integrations rename is inside
`dashboard/app/**` route folders implied by the task; layout.tsx, next.config.js, and the harness
script are all explicitly in scope). No other files touched.
