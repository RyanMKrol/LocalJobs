# T257 worklog

## What was done

Slimmed README.md from ~589 lines to ~175 lines.

Key changes:
- Replaced the five detailed worked-example workflow descriptions (places, perfumes,
  missing-tv-seasons, movie-recommendations, tv-recommendations) with one-line summaries each.
  Deep mechanics (DAG details, cycling logic, branch counts, env vars, service routing, etc.)
  are NOT relocated — they already exist in CLAUDE.md.
- Removed the long "Triggering jobs" subsections on run limits, concurrency editing, and
  one-active-run mechanics (kept the important user-facing bullets, dropped the API details
  and internal implementation explanations that belong in CLAUDE.md).
- Removed the detailed "typed-artifact contracts" / gate page descriptions from the README
  (fully covered in CLAUDE.md's Conventions section).
- Removed the detailed dashboard-page descriptions for the appearance switcher, mobile-check
  and visual-check sections (kept them as brief bullet points or removed entirely).
- Removed redundant API detail from the Ports section (loopback/CORS mechanics are one sentence now).
- Trimmed dashboard page descriptions to one-liners — the full detail is in CLAUDE.md.
- Removed the two dashboard-internal env vars (LOCALJOBS_API_UPSTREAM, NEXT_PUBLIC_API_BASE)
  from the config table — operator-irrelevant plumbing.

All remaining statements verified against the actual app — install steps, ports, commands,
job list, and dashboard nav are all accurate.
