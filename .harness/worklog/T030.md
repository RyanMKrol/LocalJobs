# T030 worklog — Move Chrome profile to framework-level location

## Attempt 1 — 2026-06-22

### What was done

Moved the persistent Chrome profile from the perfumes-specific location
(`src/jobs/perfumes/data/chrome-profile/`) to a framework-level path
(`data/chrome-profile/`), env-overridable via `LOCALJOBS_CHROME_PROFILE`.

**Changes:**
- `src/config.ts`: added `chromeProfileDir` (env: `LOCALJOBS_CHROME_PROFILE`, default `data/chrome-profile/`)
- `src/core/browser.ts`: added import of `config` and exported `defaultChromeProfileDir` re-exporting `config.chromeProfileDir`
- `src/jobs/perfumes/config.ts`: changed `profileDir` from `resolve(dataDir, 'chrome-profile')` to `defaultChromeProfileDir` (imported from `../../core/browser.js`)
- `.env.example`: added `LOCALJOBS_CHROME_PROFILE` documentation
- `CLAUDE.md`: updated file map entry for `src/config.ts`; extended headless-browser scrapes convention to document the shared profile
- Tests added:
  - `src/core/browser.test.ts`: asserts `defaultChromeProfileDir` is absolute, ends with `chrome-profile`, not under perfumes
  - `src/jobs/perfumes/config.test.ts`: asserts `perfumesConfig.profileDir` equals `defaultChromeProfileDir` and is not under the perfumes data folder

**gitignore:** no change needed — `data/*` already ignores `data/chrome-profile/` and `src/jobs/**/data/` already ignored the old location.

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — all 16 test files passed (including 2 new assertions)

### Result
done
