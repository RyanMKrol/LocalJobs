## T579 — 2026-07-15

Re-attempt of T542 (which built and fully verified this refactor but reported
`failed:blocked` because CLAUDE.md wasn't in its scope, so the work was never
committed and got discarded by the next tree reset). This attempt has CLAUDE.md
in scope, so it's built fresh and committed in one shot.

### What I did

- Added `dashboard/app/components/useAction.ts` — a `useAction<K>(refetch)` hook
  that owns the `setBusy(key) -> setErr(null) -> await fn() -> await refetch() ->
  catch(setErr) -> finally(setBusy(null))` block that was duplicated ~14 times
  across the four managers, keyed generically so a manager can track multiple
  independent busy dimensions (per-item, per-collection, bulk) with one hook
  instance each. Guards against setState-after-unmount via a mounted ref.
- Added `dashboard/app/components/recSort.ts` — the `RecSortCol` type +
  `sortRecs` helper shared by `TvRecsManager`/`MovieRecsManager`.
- Extracted the four inline manager components out of
  `dashboard/app/workflows/[name]/page.tsx` into their own files:
  `TvRecsManager.tsx`, `MovieRecsManager.tsx`, `MovieGapsManager.tsx`,
  `MissingSeasonsManager.tsx` — each rewired onto `useAction` instead of raw
  `useState` busy/err pairs. `groupByCollection` moved with `MovieGapsManager`;
  `groupByShow` moved with `MissingSeasonsManager`.
- `page.tsx` dropped from 1,144 lines to 328 lines (imports the four managers
  instead of defining them inline); kept `MISSING_SEASONS_WORKFLOWS` and
  `WORKFLOWS_WITH_SPECIFIC_MANAGERS` (still only used in `page.tsx`) and the
  rest of the workflow-detail page unchanged.
- No behaviour/appearance change intended — pure extraction + de-duplication.
- Documented all six new files in CLAUDE.md's T268 shared-component list.
- `dashboard/scripts/_dashboard-harness.mjs` needed no changes — it drives
  pages/fixtures/flows, not internal component names, and none of the four
  managers' PAGES/FLOWS entries changed shape.

### Checks run (all observed, not just launched)

- `npx tsc --noEmit` — clean.
- `npm --prefix dashboard run build` — clean; `/workflows/[name]` route size
  unchanged (7 kB) before and after the refactor.
- `npm test` — 10/10 pass.
- `node dashboard/scripts/mobile-check.mjs` — 32/32 pages pass, including all
  four workflow-detail pages (`workflow-tv-recs`, `workflow-movie-recs`,
  `workflow-missing-movies`, `workflow-missing-tv-seasons`).
- `node dashboard/scripts/visual-check.mjs` — 58/59 captures pass. The one
  failure, `workflow-run-skipped` (`.rf-dag-node` selector timeout on
  `/workflow-runs/skipped`), is unrelated to this refactor's scope
  (`workflow-runs/[id]/page.tsx`, not `workflows/[name]/page.tsx`) — confirmed
  by diffing against a build from the pre-refactor tree, which hits the same
  timeout on the same page.
- Visually inspected (Read tool, as images) all four workflow-detail
  screenshots: `workflow-missing-movies.png`, `workflow-movie-recs.png`,
  `workflow-tv-recs.png`, `workflow-missing-tv-seasons.png`. Each manager
  renders fully — schedule/enabled/concurrency/notify/certified panel, DAG
  graph, run history, the manager's own Output section (active table +
  per-item/per-group Ignore buttons + Ignored section with Un-ignore buttons),
  and the Danger zone — nothing blank, overlapping, or clipped.

### Remaining

Nothing — DoD fully satisfied, committing.
