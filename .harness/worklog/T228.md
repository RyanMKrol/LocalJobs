# T228 worklog

Removed `{p.enabled ? '' : ' (off)'}` from `dashboard/app/workflows/page.tsx` line 76.
Enabled/disabled state is already conveyed by the status pill and enable toggle.

## Verification
- `npm --prefix dashboard run build` — green
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
