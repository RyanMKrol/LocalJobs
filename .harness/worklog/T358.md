# T358 — rename TvRecsManager/MissingSeasonsManager headings to "Output"

## What changed

`dashboard/app/workflows/[name]/page.tsx`:
- `TvRecsManager` heading: `<h2>TV Recommendations</h2>` → `<h2>Output</h2>` (line ~95)
- `MissingSeasonsManager` heading: `<h2>Missing seasons</h2>` → `<h2>Output</h2>` (line ~554)
- Descriptive paragraphs under both left unchanged (still accurate).
- `MovieRecsManager` (`<h2>Recommendations</h2>`) and `MovieGapsManager`
  (`<h2>Franchise gaps</h2>`) deliberately left untouched — that reorganization is a
  separate, not-yet-shaped task blocked on an owner decision about the merge shape.

## Full heading census (post-change)

`grep -n "<h2>" dashboard/app/workflows/[name]/page.tsx`:
- `WorkflowOutputSection.tsx` → "Output" (unchanged, generic component used by every
  non-managed workflow)
- `tv-recommendations` (`TvRecsManager`) → "Output" (fixed by this task)
- `missing-tv-seasons` (`MissingSeasonsManager`) → "Output" (fixed by this task)
- `movie-recommendations` → still "Recommendations" (`MovieRecsManager`) +
  "Franchise gaps" (`MovieGapsManager`) — intentionally untouched, tracked separately
- Non-output headings unaffected: "Graph", "Runs", "Danger zone" (structural, not
  output sections)

This satisfies the "Output" heading contract for every workflow except
movie-recommendations, which is out of scope for this task by design.

## Harness/fixtures

Grepped `dashboard/scripts/_dashboard-harness.mjs` for "TV Recommendations" / "Missing
seasons" — no matches, so no fixture/harness changes were needed. `PAGES` already has
`workflow-tv-recs` and `workflow-missing-tv-seasons` entries that picked up the new
heading text automatically.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 134/134 passing.
- `npm --prefix dashboard run build` — succeeded.
- `node dashboard/scripts/visual-check.mjs` — all 30 pages captured/passed. Visually
  inspected:
  - `workflow-tv-recs.png` — heading now reads "Output", section content (15 active
    recommendations table) renders correctly below it.
  - `workflow-missing-tv-seasons.png` — heading now reads "Output", missing-seasons
    table renders correctly below it.
  - `workflow-movie-recs.png` — confirmed the two `movie-recommendations` headings
    ("Recommendations" and "Franchise gaps") were correctly left untouched, both
    sections render as before.
- `node dashboard/scripts/mobile-check.mjs` — all 18 pages pass (0px overflow, 0
  boundary-crossing elements).
