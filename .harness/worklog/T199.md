# T199 worklog

## What was done

- `src/api/server.ts`: added `collectionExamples: file.collectionExamples ?? {}` to the `GET /api/movie-gaps` JSON response.
- `dashboard/app/lib/api.ts`: added `collectionExamples: Record<string, { title: string; year: number | null }>` to the `MovieGaps` interface.
- `dashboard/app/workflows/[name]/page.tsx`: in `MovieGapsManager`, the `groupByCollection(active).map(...)` callback was changed from an arrow expression to a block with `return`, extracting the collection example and rendering a muted "You own: <title> (<year>)" paragraph beneath each collection heading when an example is present.
- `src/api/server.test.ts`: added two hermetic tests — one asserting `collectionExamples` is returned from a fixture file, one asserting it defaults to `{}` when absent.

## Verification

- `npx tsc --noEmit`: clean
- `npm test`: all tests passed
- `npm --prefix dashboard run build`: clean
- `node dashboard/scripts/mobile-check.mjs`: all 9 pages pass
