# T240 worklog

## What was done

Stacked the LAST RUN cell in the Workflows list so the status pill appears on one line and the relative time on the next.

**`dashboard/app/workflows/page.tsx`** — wrapped the pill+time in a `<span className="last-run-cell">` and gave the time its own `<span className="last-run-time">` (removed the inline `whiteSpace: nowrap` since the column no longer needs it).

**`dashboard/app/globals.css`** — added `.last-run-cell { display: inline-flex; flex-direction: column; gap: 3px; }` and `.last-run-time { font-size: 11px; }` just above the `.badge` block.

## Verification

- `npm --prefix dashboard run build` — green
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass
