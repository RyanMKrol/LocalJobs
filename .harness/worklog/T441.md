# T441: paragraph-length description for plex-space-saver-scan

## What changed

Rewrote `description` on `src/workflows/plex-space-saver/stages/scan.job.ts`
(`plex-space-saver-scan`) from a one-liner into a real paragraph covering:

- Per-title granularity: one row per movie, one row per TV show summing every episode
  across every season.
- Size comes from the Plex API's `Media.Part.size` field, never a filesystem walk.
- Re-scans fresh every run (an audit, not a build), with weekly idempotency handled
  via the `work_items` ledger (ISO calendar week key).
- Strictly report-only — never flags or suggests deletions.

Grounded in root `CLAUDE.md`'s `plex-space-saver` workflow summary and the workflow's
own `src/workflows/plex-space-saver/CLAUDE.md`.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — full suite green (exit code 0), including
  `src/core/gate-coverage.test.ts` and any job-description-related checks.

No live paid-API calls made (not needed — this is a text-only content change with no
behavioral change to the job).
