# T153 worklog

## What was done

Sorted the services array in `dashboard/app/services/page.tsx` by `paid` ASC then `name` localeCompare.
Free services sort first (paid=false → 0), then paid (paid=true → 1), alphabetically within each group.

Change is a single two-line sort on line 34, copying the array before sorting to avoid mutating the polled data.
No other files touched.

## Verification

- `npm --prefix dashboard run build` — clean build, no errors.
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (including /services).
- `npm test` not run (store.ts not touched).
