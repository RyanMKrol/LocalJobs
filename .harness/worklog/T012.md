# T012 worklog — Places: daily schedule + daily spend cap

## Attempt 1 — 2026-06-21

### Scope / doneWhen
- Give the places pipeline a real daily cron (was `schedule: null`, manual-only).
- Enforce a daily spend cap ≈ monthly free allowance / 30 in the enrich loops so a
  daily run can never blow the month. Env-overridable. No live API calls.
- Unit test the cap arithmetic / that the loop stops at the daily cap.

### Changes
- `src/jobs/places/places.pipeline.ts`: `schedule: null` → `'0 3 * * *'` (daily 03:00);
  rewrote the NOTE comment (was "manual on purpose") to explain the daily cadence is
  bounded by the per-stage monthly/30 daily cap.
- `src/jobs/places/config.ts`: added exported `DAILY_SPEND_DIVISOR = 30`. Both paid
  stages now default their daily cap to `Math.floor(monthlyCap / 30)` instead of the
  generic monthly/10:
    - places-enrich: monthly 1000 → daily **33** (was 100).
    - enrich-with-llm: monthly 2000 → daily **66** (was 200).
  Still env-overridable via `PLACES_ENRICH_DAILY_CAP` / `PLACES_LLM_DAILY_CAP`. The
  `google-places` and `gemini` services already derive their caps from these config
  values, so the cross-job service quotas track automatically.
  Enforcement itself already existed in both loops (`capStatus` / `usedToday >= dailyCap`
  graceful stops) — only the default value + rationale changed.
- `src/jobs/places/config.test.ts` (new): asserts the pipeline schedule is the daily
  cron; both daily caps equal `floor(monthly/30)`; the invariant `dailyCap*30 <= monthlyCap`
  (a full month of daily runs fits the monthly cap — the regression monthly/10 would
  break); and exercises the real `capStatus`/`recordUsage` path against the scratch DB
  to prove the loop's stop-condition fires once the daily cap is reached.
- Docs in lockstep: README places section (daily-at-03:00 + monthly/30 rationale);
  CLAUDE.md caps convention now notes daily-scheduled jobs use monthly/30 (vs the
  generic monthly/10 for manual/weekly).

### Verification (no paid calls)
- `npx tsc --noEmit` — clean.
- `npm test` — all suites pass, incl. new `src/jobs/places/config.test.ts`
  ("places daily schedule + monthly/30 spend cap (loop stops at daily cap)") and
  registry still loads (9 jobs, 4 services, 2 pipelines).
- No dashboard change → no dashboard build needed.
- No live Google Places / Gemini calls made; test uses scratch DB + config only.
