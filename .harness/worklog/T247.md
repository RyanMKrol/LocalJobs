# T247 worklog

## What was done

Wrapped the three trailing status elements on done-rows in a `div.done-status-cluster` CSS grid
with fixed column widths (90px | 70px | 82px), so the reviewed pill, done/failed pill, and action
button slot into consistent vertical columns regardless of which label variant is active.

Also merged the two identical `onMarkFailed` button branches (Mark failed / Undo fail) into a
single button that branches on `isFailed` internally — cleaner and ensures the slot is always
occupied (uses `<span />` placeholder when `onMarkFailed` is absent so the grid column stays).

## CSS changes

- Added `.done-status-cluster` grid rule in `globals.css` (three fixed columns, gap 4px)
- Added `.done-status-cluster .pill { text-align: center }` and `.review-toggle { width: 100%; text-align: center }` scoped rules
- Added `@media (max-width: 640px)` override with slightly narrower columns (82px | 62px | 76px)

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all 4 tests pass
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass, 0 overflow
- `node dashboard/scripts/visual-check.mjs` — all 10 screenshots captured

## Visual confirmation

Observed in `backlog-expanded.png`: T050 (`✓ done` + `Mark failed`) and T051 (`✗ failed` + `Undo fail`)
both show the three-column cluster in exact vertical alignment. The `✗ failed` red pill and `Undo fail`
button sit in the same columns as `✓ done` and `Mark failed` on the row above — no jitter.
