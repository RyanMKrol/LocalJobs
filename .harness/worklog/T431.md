# T431: paragraph-length job descriptions for missing-tv-seasons workflow

Rewrote the `description` field of the 3 `missing-tv-seasons` jobs to real 3-6 sentence
paragraphs, grounded in `src/workflows/missing-tv-seasons/CLAUDE.md` and each stage's own
implementation:

- `plex-tv-snapshot` (snapshot.job.ts) — describes the Plex GUID-based scan, tmdbId
  extraction, highest-owned-regular-season calculation, and that it always re-scans fresh
  (no skip-if-done at this stage).
- `tmdb-season-check` (season-check.job.ts) — describes the TMDB lookups via the shared
  rate-limited `tmdb` service, the "aired vs complete" season check, that ended/canceled
  shows are still checked (revivals), and that this stage also recomputes fresh every run.
- `plex-seasons-notify` (notify.job.ts) — describes the "have I already notified this?"
  ledger pattern (not a work-done ledger), the single-digest-per-run behavior, the
  markdown report output, first-run full-backlog announcement, and the ignore-to-suppress
  mechanism.

## Verification

- `npx tsc --noEmit` — clean, no errors.
- `npm test` — 213/213 passing, 0 failures.
- No paid API calls made; no data/ files touched.

## Result

done T431
