# T529 — split src/db/store.ts into src/db/store/*.ts

2026-07-13

## What I did

Pure mechanical move, no behavior/signature/SQL changes:

- Created `src/db/store/lib.ts` — shared path helpers (`WORKFLOWS_ROOT`, `toStoredPath`,
  `normalizeDetailPaths`).
- Created `src/db/store/jobs.ts` — job upsert/get/list/timeout-override.
- Created `src/db/store/runs.ts` — job runs, run logs, global cross-cutting log feed.
  Imports `rollUpWorkflowProgress` from `./workflows.js` (setProgress rolls a member's
  progress up into its parent workflow run).
- Created `src/db/store/workItems.ts` — the per-item idempotency ledger: lineage
  (root_key/parent_key), stuck/ignored/prune, bulk ops, stage IO lists, output items.
- Created `src/db/store/usage.ts` — per-job daily/monthly spend meter (`job_usage`).
- Created `src/db/store/workflows.ts` — workflow definitions, workflow runs, progress
  roll-up, repeatUntilStable no-forward-progress signature, workflow framework logs.
- Created `src/db/store/services.ts` — service definitions, rate/quota meter,
  per-minute/min-interval slot reservation, service consumers, response cache (T451).
- Created `src/db/store/admin.ts` — stale-override audit (T475), workflow output reset
  (T203), full definition deletes (workflow/job/service), read-only DB browser +
  canned queries. Imports `listJobs`/`listWorkflows`/`listServices`/`getWorkflowJobs`
  from the sibling domain files.
- Rewrote `src/db/store.ts` into a pure barrel: `export * from './store/<domain>.js'`
  for all 8 files, zero logic.
- Updated the `src/db/store.ts` file-map row in CLAUDE.md and added a new row for
  `src/db/store/`.

Each sub-file imports the shared `db` singleton from `../index.js` directly (was
`./index.js` in the monolith). Every previously-exported symbol from `store.ts` is
still exported (verified: no import site anywhere in `src/` needed a change — all
~188 import sites of `db/store.js` still resolve every named import through the
barrel's re-exports).

## Checks run

- `npx tsc --noEmit` — clean, no errors.
- `npm test` — 265/265 tests pass, 0 failures (store.test.ts's ~1,473 lines and
  server.test.ts's ~1,899 lines both green, pinning every behavior unchanged).

## What remains

Nothing — task complete. This is deliberately a pure-move commit per the task's
sequencing note (R03): no behavioral fix bundled in.
