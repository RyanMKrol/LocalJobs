# T511: Plex-language-fix callService wrapping — BUILT

## Status
Built and committed — all 5 scoped files staged explicitly (no data/, no broad add).

## Work done
1. **lib.ts:** Wrapped 4 plexGet() reads in callService('plex', ...)
   - fetchSections() → `/library/sections`
   - fetchSectionItems() → section `/all`
   - fetchItemDetail() → `/library/metadata/<ratingKey>`
   - fetchAllLeaves() → `/library/metadata/<showRatingKey>/allLeaves`

2. **apply.ts:** Wrapped 2 mutating calls at CALL SITES in callService('plex', ...)
   - triggerBackup() line 67
   - putStreams() line 87
   - Test-injection semantics preserved: opts defaults still work

3. **plex-language-fix/CLAUDE.md:** Added one-line note on plex service metering

4. **Tests:**
   - apply.test.ts: Added callService pass-through test (mirrors tv-snapshot.test.ts)
   - lib.test.ts: No changes (pure evaluatePart tests)

## Verification
✓ npm test: 259 pass, 0 fail
✓ npx tsc --noEmit: clean
✓ npm --prefix dashboard run build: success
✓ Grep: all plexGet/putStreams/triggerBackup wrapped (only type-decls outside)
✓ Git hygiene: staged 5 files by explicit name

---
reopened 2026-07-12 — the prior block was a FALSE POSITIVE from the over-broad sensitive-paths guard pattern `(^|/)plex-language-fix/` matching the legitimate src/workflows/plex-language-fix/ workflow. Guard pattern fixed to `^plex-language-fix/` (root-anchored). Task is buildable again.
