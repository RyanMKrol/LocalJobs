# T230 worklog — Remove Database view from dashboard

## What was done

Removed the DB browser view entirely:

- Deleted `dashboard/app/db/page.tsx` (and the now-empty `db/` directory)
- Removed nav link `<a href="/db">Database</a>` from `dashboard/app/layout.tsx`
- Removed `TablePage`, `CannedQueryMeta`, `CannedQueryResult` interfaces and `dbTables`/`dbTable`/`dbQueries`/`dbQuery` client methods from `dashboard/app/lib/api.ts`
- Removed `dbTables`/`dbPage` fixtures, `/api/db/tables` and `/api/db/tables/` fixture handlers, and the `database`/`/db` entry from `PAGES` in `dashboard/scripts/mobile-check.mjs`; also removed the now-dead `clickTable` branch
- Removed the four `/api/db/tables` and `/api/db/queries` endpoint handlers from `src/api/server.ts`
- Cleaned up the unused `browseTable`, `listCannedQueries`, `listDbTables`, `runCannedQuery` imports in `src/api/server.ts`

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — clean build, `/db` route absent from output
- Daemon restarted via `launchctl kickstart -k`
- CLAUDE.md: no update needed (no DB-browser-specific text existed there)
