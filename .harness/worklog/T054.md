# T054 — Database view: click a row to see full row data

## Attempt 1 — 2026-06-22

### What was done

Added a row-click modal to the Database view's `ResultTable` component.

**`dashboard/app/db/page.tsx`**
- Split `fmt()` into two helpers: `fmtCell()` (compact, single-line for table cells) and `fmt()` (pretty-printed JSON for the modal).
- Added `RowModal` component: renders a fixed overlay with all columns as key/value rows, full untruncated values, `white-space: pre-wrap` for JSON/multiline.
- Esc key and backdrop-click both dismiss via `useEffect` + `onClose` callback.
- `ResultTable` now has a `selectedRow` state; clicking any `<tr>` sets it; renders `<RowModal>` when set.

**`dashboard/app/globals.css`**
- `.db-row-clickable` — pointer cursor + hover background on table rows.
- `.db-modal-overlay` — fixed full-screen semi-transparent backdrop.
- `.db-modal` — centred card, `min(680px, 100%)` wide, `max-height: 80vh` with scroll.
- `.db-modal-header`, `.db-modal-close`, `.db-modal-body`, `.db-modal-row`, `.db-modal-key`, `.db-modal-val` — modal internals.
- Mobile (`≤640px`): `.db-modal-row` collapses to single column (label above value).

### Verification

- `npx tsc --noEmit` — clean
- `npm test` — 18 test files, all passed
- `npm --prefix dashboard run build` — clean, no type errors
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass

### Result: done
