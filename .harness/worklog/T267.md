# T267 worklog

## What was built

- `src/jobs/claude-warmer/claude-warmer.workflow.ts` — workflow manifest, schedule `*/30 * * * *`, enabled by default, `maxConcurrency: 1`
- `src/jobs/claude-warmer/claude-warm.job.ts` — single job that calls `runClaude('hi', WARM_MODEL)` via the shared `claude-cli` service; handles `QuotaExceededError`, rate-limited results, and unexpected errors — all log a warning and return cleanly
- `src/jobs/claude-warmer/claude-warm.test.ts` — 4 unit tests covering: success path, QuotaExceededError, rateLimited result, unexpected error; all hermetic (no real Claude calls)
- Updated CLAUDE.md (architecture/file-map) and README.md (workflow list) to document the new workflow

## Observations

- `npx tsc --noEmit` — clean
- `npm test` — 74/74 pass (4 new tests added)
- `data/` in the folder is covered by the existing `src/jobs/**/data/` gitignore entry — no extra .gitignore needed
- No local quota cap needed per spec — upstream Claude plan enforces its own limit; the warmer intentionally consumes the real usage window
