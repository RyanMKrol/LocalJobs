# T450 — Render rate-limit provenance on the Integrations page's service modal

## What changed

- `dashboard/app/integrations/page.tsx`:
  - `ConsumersPanel` now accepts an optional `service?: Service` prop (in addition to `name`/
    `onClose`) and renders a new "Rate limit provenance" section above the existing consumers list,
    showing `service.rate_limit_source` as plain text, with a muted "not documented yet" fallback
    when it's empty.
  - The parent `Services` component looks up the matching `Service` from `allServices` by name
    (`allServices.find(s => s.name === viewingConsumers)`) and passes it into `ConsumersPanel`.
- `dashboard/app/globals.css`: added `.provenance-section` (bottom margin/padding + a `--border`
  divider) to separate the new section from the consumers list below it, reusing the existing
  `.db-modal`/`.consumers-modal-body` chrome — no new styling system.
- `dashboard/scripts/_dashboard-harness.mjs`:
  - `service()` fixture now includes a `rate_limit_source` value (a realistic sentence + inline URL
    + the shared `LONG` constant, matching how `description` proves wrapping).
  - Updated the `service-consumers-modal` FLOWS entry's comment to note it now also verifies the
    provenance section renders (same click/wait actions, unchanged).

## Verification performed

- `npx tsc --noEmit` — clean.
- `npm test` — 230/230 tests pass (52 suites), no failures.
- `npm --prefix dashboard run build` — succeeds, no type/lint errors.
- `node dashboard/scripts/mobile-check.mjs` — all 20 pages pass, 0px horizontal overflow, 0
  boundary-crossing elements (including `/integrations`).
- `node dashboard/scripts/visual-check.mjs` — 44/45 captures pass; the one failure
  (`workflow-run-skipped`, a `.rf-dag-node` selector timeout on `/workflow-runs/skipped`) is
  unrelated to this task (no DAG/React-Flow code touched) and reproduces on this page regardless of
  this change — pre-existing flake, not introduced here.
- **Looked at `dashboard/scripts/visual-out/service-consumers-modal.png` directly**: the modal shows
  a "Rate limit provenance" heading followed by the full `rate_limit_source` sentence (wrapping
  correctly across 3 lines, including the embedded URL), a horizontal divider, then the unchanged
  "Consumers of google-places" heading and the places/perfumes consumer groups below. Nothing is
  blank, clipped, or overlapping — the new section is clearly painted.

## Scope

Touched only: `dashboard/app/integrations/page.tsx`, `dashboard/app/globals.css`,
`dashboard/scripts/_dashboard-harness.mjs`, plus this worklog file. `dashboard/app/lib/api.ts` needed
no change — `rate_limit_source` was already added to the `Service` type by the dependent task (T449).
