# T147 worklog

## Prior attempt (already committed)

Changed the back-navigation label from the hardcoded `← workflow run` to
`← {run.workflow_name} run`, identifying the workflow. However this still didn't
identify WHICH run of the workflow, so the label was identical for every run.

## This attempt

Updated the back link on the run-scoped gate detail page
(`dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx`) to include
the short run ID (first UUID segment: `id.split('-')[0]`).

**Change made:** line 144-148 now renders:
```tsx
<a
  href={run ? `/workflow-runs/${id}` : '/workflows'}
  title={run ? id : undefined}
>
  ← {run ? `${run.workflow_name} run · ${id.split('-')[0]}` : 'workflows'}
</a>
```

This produces e.g. `← movies run · 2df4cfbf` with the full UUID in the `title`
tooltip. The definition-level gate page (`/workflows/[name]/gates/...`) was not
changed — it has no run in scope and correctly stays at `← {name}`.

## Verification

- `npm --prefix dashboard run build` — passed, no errors.
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (0px horizontal
  overflow, 0 boundary-crossing elements at 402px viewport).
