# T104 worklog — Remove redundant Validation gates panel

## Attempt 1 — 2026-06-23

### What was done

Removed the `GatePanel` component (lines 18-52) and its usage from
`dashboard/app/workflow-runs/[id]/page.tsx`. The `gates` data is still fetched
and passed to the `Dag` component so gate chips continue to work.

Also removed the now-unused `GateStatus` type import.

No `gateAnchor` symbol existed in code — it was documentation-only.

### Doc updates

- **CLAUDE.md**: replaced "links to that gate's row in the Validation gates panel
  (`gateAnchor` gives the shared DOM id)" with "links directly to that gate's
  dedicated detail page (`/workflow-runs/<id>/gates/<producer>/<key>`)".
- **README.md**: replaced "opens that gate's row in the Validation gates panel"
  with "opens that gate's dedicated detail page".

### Checks run

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — clean, `/workflow-runs/[id]` bundle 1.83 kB
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
