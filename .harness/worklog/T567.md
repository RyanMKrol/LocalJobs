# T567 — retire perfumes' own Claude helper, use shared src/services/claude.ts

2026-07-14

## What I did
- Deleted `src/workflows/perfumes/claude.ts` (the perfumes-local `runClaude`/`extractJson`/
  `unfenceMarkdown` spawn implementation).
- `find-url.ts`, `parse.ts`, `build.ts` now import `runClaude`/`extractJsonObject`/`unfenceMarkdown`
  from `../../../services/claude.js` instead. `extractJson` calls became `extractJsonObject` calls
  (byte-identical body, just the shared name).
- `config.ts` is out of scope for this task (not in the allowed-files list), so its now-dead
  `claudeBin`/`claudeTimeoutMs` fields (`PERFUMES_CLAUDE_BIN`/`PERFUMES_CLAUDE_TIMEOUT_MS`) are left
  in place, unreferenced by any code — nothing reads them anymore. Perfumes now picks up the shared
  `LOCALJOBS_CLAUDE_BIN` and the dashboard-editable `claude-cli` service timeout
  (`claudeTimeoutMs()`) like every other Claude caller (projects-sync, movies recommenders).
- Updated `src/services/claude.ts`'s header comment (removed the "perfumes keeps its own helper —
  migration is a follow-up" note, since it's now done) and the `unfenceMarkdown` doc comment.
- Updated `src/workflows/perfumes/CLAUDE.md`: replaced the "perfumes has its OWN Claude helper"
  invariant with one stating it now uses the shared helper, and fixed the "Files & config" line
  that referenced the now-dead `claudeTimeoutMs` config field.

## Checks run
- `npx tsc --noEmit` — clean, no errors.
- `npm test` — all suites green, including perfumes' `find-url.test.ts`, `parse.test.ts`,
  `build.test.ts`, `parse-detail.test.ts`, `job-throw.test.ts`, `progress.test.ts`,
  `config.test.ts`, `lib.test.ts` (9/9 suites, all pass).
- `grep -rn "spawn(" src/workflows` (excluding `data/repos`) finds no Claude spawn — only
  `vercel-daily-redeploy`'s `vercel` spawn and `projects-sync`'s `git` spawn remain, confirming the
  three-copy Claude-spawn debt is retired.
- Meter parity: read each call site — `find-url.ts:43`, `parse.ts:34`, `build.ts:51` each call
  `runClaude(...)` exactly once per processed item inside their per-item loop; the shared
  `runClaude` gates through `callService('claude-cli', ...)` internally (unchanged semantics vs the
  old perfumes-local helper, which also called `callService` once per `runClaude` call). No doubling
  or dropping.
- Did not make any live Claude CLI calls (perfumes' `dryRun` fixture path / existing test fixtures
  covered the behavioural checks per CLAUDE.md's no-live-paid-calls rule — `claude-cli` is $0 but
  the stage tests already inject a fake Claude, so no real call was needed to verify).

## What remains
Nothing outstanding for this task. `config.ts`'s dead `claudeBin`/`claudeTimeoutMs` fields could be
removed in a follow-up that has `config.ts` in scope.
