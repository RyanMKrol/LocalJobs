# T122 worklog — Overview: remove the redundant per-workflow stuck-count pill

## Attempt 1 — 2026-06-23

**What changed:** Removed the `` {p.stuck > 0 ? ` · ⛔ ${p.stuck} stuck` : ''} `` pill from the
workflow card subtitle in `dashboard/app/page.tsx` (line 191). The dedicated Stuck section below
the workflow cards and the Stuck stat tile remain untouched.

**Verification:**
- `npm --prefix dashboard run build` — passed (compiled successfully, 9 pages generated)
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (0 overflow, 0 boundary crossings)
- `npx tsc --noEmit` — no errors
- `npm test` — all 21 test files passed

**Status:** done
