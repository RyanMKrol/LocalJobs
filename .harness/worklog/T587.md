## T587 — 2026-07-15

Made the "T201: unlimited workflow runs all independent stages without throttling"
subtest in `src/core/workflow-executor.test.ts` robust to machine variance.

### What changed
Replaced the bare absolute-ms ceiling (`elapsed < SLOW_MS * 3`) with a same-run
serial baseline comparison, mirroring the existing "parallelism (T156)" subtest
pattern: run the same 4 `slow-*` stages a second time with `maxConcurrency: 1`
and assert `unlimited * 1.8 < serial` — a relative ratio, no fixed millisecond
ceiling.

### Checks run
- `node --test --import tsx src/core/workflow-executor.test.ts` — passed, 5x in a
  row with no flake.
- `npx tsc --noEmit` — clean.
- `npm test` — full suite green.
- Regression check: temporarily hardcoded `effectiveConcurrency = 1` in
  `runWorkflow` (src/core/workflow-executor.ts) to force the unlimited path
  serial, re-ran the test file — the new assertion correctly TRIPPED (test
  failed as expected), confirming the relative-ratio check still guards the
  actual parallelism guarantee. Reverted the hardcode immediately after
  (`git diff` on that file is empty — no net change to non-test code).
