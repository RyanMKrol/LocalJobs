# T223 worklog — Gate padlock icons on DAG edges

## What was done

Replaced verbose per-edge gate text labels in `DagFlow.tsx` with compact SVG padlock icons:

- Each gated edge now renders a small SVG padlock (shackle arc + body rectangle) instead of the artifact-key string.
- **Colour-coded by state:** failed = red (`var(--red)`), passed = green (`var(--green)`), pending/structural = muted (`var(--muted)`).
- **Failed gates are prominent:** larger icon (16px vs 12px), full opacity, red glow drop-shadow, subtle red body fill; passing/structural are small and opacity 0.45–0.55.
- **Hover behaviour:** opacity rises and icon scales up (CSS transition via `.dag-gate-lock` classes).
- **Tooltip on hover:** native `title` attribute carries `"key — description"` (or just key when no description).
- **Click-through preserved:** anchor element and `href` logic unchanged.
- Edge stroke colour and width also updated to match state (red/2px for failed, grey/1.5px otherwise).

## Checks

- `npx tsc --noEmit` — clean
- `npm test` — all pass
- `npm --prefix dashboard run build` — success
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
