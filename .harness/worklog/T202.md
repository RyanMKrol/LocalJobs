# T202 worklog

## What was done

Removed the static `<span className="muted" style={{ fontSize: 12 }}>run</span>` eyebrow label from
`dashboard/app/runs/[id]/page.tsx` (line 47). The run id, job name, status badge, and rest of the
header are untouched.

## Verification

- `npx tsc --noEmit` — clean
- `npm --prefix dashboard run build` — succeeded
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
