# T034 — Services: explicit 'remove cap' control

## Attempt 1

The changes were already present in the working tree (unstaged). The implementation adds a `✕`
button next to each limit input (rate, daily, monthly) that appears when the field has a value.
Clicking it clears the field to empty, which `parseField` maps to `null` (no cap) on save.

### What was done
- Each edit cell now uses `whiteSpace: nowrap` so the button stays inline
- A `✕` button appears conditionally (`{draft.X !== '' && ...}`) per field
- Clearing to empty correctly sends `null` to `updateServiceLimits` via the existing `parseField`

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — 16 test files, all passed
- `npm --prefix dashboard run build` — clean build, services page is 2.88 kB

Result: done
