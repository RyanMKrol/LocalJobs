# T612 — badge wrap fix

2026-07-16

## What I did

Added `white-space: nowrap;` and `flex-shrink: 0;` to the shared `.badge` rule in
`dashboard/app/globals.css` (lines 116-119), per the spec. No other file touched —
`StatusBadge` markup (`ui.tsx`) and `.badge-emoji` were left as-is.

## Checks run

- `npm --prefix dashboard run build` — succeeded (typecheck + static generation all green).
- `launchctl kickstart -k gui/$(id -u)/com.ryankrol.localjobs-dashboard` — restarted per the
  "always restart what you changed" rule.
- `node dashboard/scripts/visual-check.mjs` — ran multiple times. Observed screenshots:
  - `workflows-list-starting.png`: the Workflows list "Last Run" column pills ("Starting…",
    "✅ Succeeded") render their emoji + label on one line inside the pill shape — no
    emoji-above-label-below wrap.
  - `stage-io-all-stages.png`: every Stage I/O item's status pill ("✅ Succeeded",
    "❌ Failed") in the Inputs & Outputs panel renders on one line, not compressed/wrapped.
  Both confirm the fix.
  - One capture, `workflow-run-skipped` (`/workflow-runs/skipped`), consistently timed out
    waiting for `.rf-dag-node` to appear (`page.waitForSelector` 10s timeout) across three
    separate runs. I isolated this: `git stash`'d the `.badge` change, rebuilt, and ran the
    same page capture again — it failed identically without the CSS change present. This is
    a pre-existing flake in the React Flow DAG rendering for that fixture, unrelated to the
    `.badge` rule (the badge fix touches only inline pill styling, not `.rf-dag-node`). Popped
    the stash to restore the fix afterward and rebuilt again to confirm the final artifact
    matches the intended change.
- `node dashboard/scripts/mobile-check.mjs` — all 32 pages passed with 0px horizontal
  overflow and 0 boundary-crossing elements, confirming `flex-shrink: 0` on `.badge` did not
  introduce any new overflow at the ~402px phone viewport.

## Remaining

Nothing outstanding for this task. The pre-existing `workflow-run-skipped` DAG-node-timeout
flake in `visual-check.mjs` is out of scope for T612 (scope is `dashboard/app/globals.css`
only) and unrelated to this change — worth a separate backlog item if it recurs.
