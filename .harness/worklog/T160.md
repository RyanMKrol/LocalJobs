# T160 worklog — needs-human marker stable blue

## What was done

`dashboard/app/globals.css`:
- Added `--human-blue: #58a6ff;` to `:root` alongside the other stable, theme-invariant palette
  variables (logs palette, etc.). This variable is deliberately NOT present in any joyful theme
  block, so it stays `#58a6ff` regardless of which `--accent` the active theme picks.
- Changed `.pill.human` from `background: var(--accent); border-color: var(--accent);` to
  `background: var(--human-blue); border-color: var(--human-blue);`.

No changes needed in `backlog/page.tsx` — the pill is already using `.pill.human` via the CSS
class (the inline-style reference in the spec was stale from before T159 refactored the card).

## Verification

- `npm --prefix dashboard run build` — clean, no errors.
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass.
