## T462 — 2026-07-08

Wired the 8 owner-authored `PerfumeInput` personal fields (added on `PerfumeInput` by T461)
into the generated profile markdown, kept fully separate from the existing Fragrantica-vs-LLM
confidence blend (`confidenceClause`/`confidenceWeight`/`voteDistribution`, unchanged; `season`/
`longevity`/`sillage` keys unchanged).

What changed:
- `profile.template.md`: added 5 new frontmatter keys under a `Personal (from your own
  PerfumeRatings row)` block — `date_added`, `ownership`, `personal_longevity`,
  `personal_projection`, `personal_seasons` — plus updated the `rating` key's comment (now
  reachable from real data instead of hardcoded null) and the `## Personal Notes`/`## Application`
  placeholder comments to describe the new pass-through.
- `stages/build.ts`: added exported `personalFieldsClause(p: PerfumeInput): string`, mirroring the
  `notesMappingClause`/`confidenceClause` pattern — lists each of the 8 fields' real value with an
  explicit copy-verbatim/no-invention instruction when present, and an honest null/[]/"not recorded
  yet" fallback when absent. Wired into `buildPrompt`'s prompt array in place of the old hardcoded
  "Leave personal scaffolding blank: rating: null, status: owned..." line (that line's `status:
  "owned"` default and blank Personal/Application defaults are now only the new clause's own
  fallback behaviour, not a blanket instruction). Also updated `FALLBACK_TEMPLATE` to include the 5
  new frontmatter keys.
- `stages/build.test.ts`: added coverage for `personalFieldsClause` — a fully-populated fixture
  asserts every one of the 8 values appears verbatim plus the verbatim/no-invention instructions; an
  empty fixture asserts null/[]/"not recorded yet" fallback wording and no fabricated-looking value
  (no stray date pattern, no ownership enum value).
- `src/workflows/perfumes/CLAUDE.md`: updated the `perfumes-build` bullet to describe the new
  personal-fields verbatim pass-through.

Checks run:
- `npm test` — 244/244 passing (includes the new `personalFieldsClause` assertions).
- `npx tsc --noEmit` — clean.
- Manual content-correctness read (no live Claude call — this repo's harness rule against paid
  calls during build/verify): ran `personalFieldsClause` directly via `npx tsx` against a
  fully-populated fixture and an empty fixture, and read the printed clause text for both — the
  populated case echoes every real value (rating 8, "05-03-2024", "Full bottle", 6, 3,
  ["autumn","winter"], the description, the application spots) verbatim with the copy-exact
  instruction; the empty case shows only honest null/[]/"not recorded yet" fallbacks and no
  fabricated value. `PERFUMES_DRY_RUN=1` was not useful for this specific verification since the
  dry-run path in `runBuild` returns a hardcoded stub markdown without ever calling `buildPrompt`
  (so it doesn't exercise the new clause) — the dry-run stub is unaffected by this change. Nothing
  remains outstanding for this task.
