# T349

## Do
Changed `stocks-snapshot.ts`'s per-position `markWorkItem` call (inside the `for (const p of positions)`
loop) to record a `detail` object: `{ name, currentPrice, averageBuyPrice, markdown }`, where `markdown`
is `stocksSyncConfig.portfolioMdPath` (the same shared combined-portfolio report for every position —
deliberate, per the spec).

No changes to `writePortfolio`/`buildPortfolioMarkdown`/JSON or markdown contents.

## Verify
- Added a unit test (`records currentPrice/averageBuyPrice/markdown in the ledger detail`) that runs
  `runStocksSnapshot` with a stub `fetchPortfolio` returning one known position, then reads the ledger
  row back via `getWorkItem` and asserts `detail.currentPrice === 130`, `detail.averageBuyPrice === 100`,
  and `detail.markdown === stocksSyncConfig.portfolioMdPath`.
- `npx tsc --noEmit` — clean.
- `npm test` — 128/128 passing (all suites), including the existing stocks-snapshot suite unaffected.

## Result
done T349
