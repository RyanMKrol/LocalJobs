# T171 worklog

## What was done

Added `open` attribute to the `<details>` elements for the **Harness-buildable** and **Needs a human** sections in `dashboard/app/backlog/page.tsx`. The **Done** section already defaulted to collapsed and was left unchanged.

## Verification

- `npm --prefix dashboard run build` — passed.
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (including `/backlog`).
