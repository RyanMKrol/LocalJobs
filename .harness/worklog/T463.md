# T463 — Plex service definition

2026-07-08

## What I did
- Added `src/services/plex.service.ts` — a self-contained `ServiceDefinition` named
  `plex`, `category: 'api'`, `paid: false`, no `ratePerMinute`/`dailyCap`/`monthlyCap`/
  `minIntervalMs`/`maxJitterMs` (a local LAN server has no meaningful rate limit or spend
  quota — `callService`'s existing "no limit configured" branch already handles metering +
  recording without one). `rateLimitSource` explains this is purely for call-count
  visibility and per-job consumer tracking on the Integrations page.
- Added `src/services/plex.service.test.ts` (mirrors `openfigi.service.test.ts`'s shape
  test) asserting `name`/`category`/`paid` and that no rate/quota fields are set.
- Did not touch any Plex call site (`src/core/plex-client.ts` or the 6 Plex-touching
  workflows) — out of scope for this task per the spec.

## Checks run
- `npx tsc --noEmit` — clean.
- `npm test` — 245/245 passing, 0 failures (includes the existing
  `src/workflows/registry.test.ts` registry-discovery coverage, which continues to pass
  with the new `*.service.ts` file present).

## Remaining
- A follow-up task should wire actual Plex call sites through `callService('plex', ...)`
  for per-job consumer tracking — deliberately not done here per the spec.
