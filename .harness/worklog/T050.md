# T050 worklog

## Attempt 1 — 2026-06-22

### What I did
Added `white-space: nowrap` to the `.btn` CSS class in `dashboard/app/globals.css`.

The class already had `display: inline-flex; align-items: center` which should prevent wrapping in theory, but without `white-space: nowrap` the text content (e.g. "▶ Run") could still wrap if the container is narrow. Adding `nowrap` pins the icon and label to a single line across all `.btn` instances — the workflows page Run button, the overview card Run button, and any other `.btn` in the app.

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — all passed
- `npm --prefix dashboard run build` — clean build

### Result
done
