# T415 — split stock-portfolio-snapshot into fetch/resolve stages

## What changed

- `config.ts`: added `rawPortfolioJsonPath` (`data/out/raw-portfolio.json`).
- `contracts.ts`: added `stockRawPortfolioContract()` (key `stock-raw-portfolio`), updated the
  file's DAG comment to the new 4-stage chain.
- New `stages/stock-portfolio-fetch.ts` + `.job.ts`: fetch-only stage (Invest + optional ISA),
  writes `raw-portfolio.json`, records ONE ledger row per ISO week unconditionally (even at zero
  positions). No ISIN/OpenFIGI calls at all.
- `stages/stock-portfolio-snapshot.ts` repurposed to resolve-only: reads `raw-portfolio.json` via
  a new `readRawPortfolio`/`RawPortfolioReader`, keeps its own credential check (still needed for
  its own `fetchInstrumentsMetadata` call), keeps the ISIN/OpenFIGI resolution logic completely
  unchanged, writes `portfolio.json` as before.
- `stock-portfolio-snapshot.job.ts`: added `consumes: [stockRawPortfolioContract()]`, narrowed
  description to "resolve tickers, build the snapshot".
- `stock-digest.workflow.ts`: 4-job DAG (`stock-portfolio-fetch` → `stock-portfolio-snapshot` →
  `stock-sector-lookup` + `stock-digest-build` fan-in), doc comment updated.
- Tests: new `stock-portfolio-fetch.test.ts` (mirrors `stocks-sync`'s `stocks-fetch.test.ts`
  structure); `stock-portfolio-snapshot.test.ts` adapted to inject `readRawPortfolio` instead of
  `fetchPortfolio`, dropped the Invest/ISA-fetch-specific tests (moved), kept credential-missing
  throw tests + the full ISIN-resolution test block; `contracts.test.ts` adds a
  `stock-raw-portfolio` test block + updates the DAG gate-derivation test to 4 gates.
- `src/workflows/stock-digest/CLAUDE.md`: rewritten DAG section for the new 4-stage pipeline.

## Verification

- `npx tsc --noEmit`: clean.
- `npm test` (full suite): green across multiple runs. One occasional pre-existing flake
  (`T201: unlimited workflow runs all independent stages without throttling` in
  `src/core/workflow-executor.test.ts`, a timing-sensitive concurrency test unrelated to this
  task) was observed on 1 of ~9 runs — confirmed pre-existing/unrelated (not touched by this
  change, not in scope). Fixed one real issue introduced by this task: my new
  `stock-portfolio-fetch.test.ts` set `TRADING212_ISA_API_KEY_ID`/`_SECRET_KEY` env vars in 3
  tests without cleaning them up afterward, which — since `scripts/run-tests.ts` runs all test
  files sequentially in one process (shared `process.env`) and `stock-digest` sorts before
  `stocks-sync` alphabetically — leaked into `stocks-sync/stages/stocks-fetch.test.ts`'s tests,
  causing a spurious double-fetch. Added the same explicit `delete process.env...` cleanup
  `stocks-fetch.test.ts` itself uses; confirmed 6 consecutive clean `npm test` runs afterward.
- Every `stocks-sync` test still passes unchanged — confirms `trading212.service.ts` untouched.
- `stock-digest`'s DAG gate-coverage: 4 member jobs, 4 gates (verified via
  `contracts.test.ts` and live via the API below).
- **Live manual dashboard-triggered run** (real Trading212 + Finnhub + OpenFIGI read-only calls,
  daemon restarted first to load the new code):
  - `raw-portfolio.json`: 9 positions, no `isin`/`resolvedTicker` fields.
  - `portfolio.json`: 9 positions, resolved (e.g. `YNDX_US_EQ` → `resolvedTicker: NBIS`,
    `isin: US...`).
  - `sectors.json`: `YNDX_US_EQ` (queried as `NBIS` via the resolved ticker) resolved to
    `"Technology"` — confirms `stock-sector-lookup` prefers the OpenFIGI-resolved ticker.
  - Workflow run API: 4 gates, all `state: "passed"`, including the new
    `stock-portfolio-fetch → stock-portfolio-snapshot` (`stock-raw-portfolio`) boundary.
  - Digest markdown + facts JSON written for the current ISO week as before.

## Result

done T415
