# T339 — table cell padding: less vertical, more horizontal

## Change
`dashboard/app/globals.css:92` — shared `th, td` rule (used by every table, including
Services and Workflows) changed from `padding: 10px 14px;` to `padding: 8px 18px;`.
Single shared rule, not duplicated per-page (confirmed via grep that neither
`services/page.tsx` nor `workflows/page.tsx` define any page-specific table padding
override). Left the mobile-only override at the `@media (max-width: 640px)` block
(`th, td { padding: 8px 10px; }`, line ~582) untouched — still proportionate for phone
width, not part of the owner's complaint.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 127/127 pass.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — all 27 screenshots captured; read
  `services.png` and `workflows.png` by eye: rows now read as clearly inset from the
  panel's left border (18px horizontal) and visibly more compact vertically (8px) than
  before — matches the owner's complaint (previously "jammed against the left border"
  / "too tall"). Also read `overview.png` (stuck-items table, recent-runs table) and
  `workflow.png` (`/workflows/places`) — no regression, padding reads consistently
  proportionate everywhere a table renders.
- `node dashboard/scripts/mobile-check.mjs` — all 16 pages pass, 0px horizontal
  overflow on every page at 402px viewport (mobile override rule is unaffected by
  this change since it's a separate, more-specific rule inside the media query).

No page/workflow/gate/interactive-state added or removed, so no
`dashboard/scripts/_dashboard-harness.mjs` update needed — pure CSS value tweak on an
existing shared rule.
