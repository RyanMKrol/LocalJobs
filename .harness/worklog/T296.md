# T296 — Increase gate-padlock icon size/stroke weight

## Change
`GateEdge` in `dashboard/app/components/DagFlow.tsx`:
- `lockSize`: `isFailed ? 16 : 12` → `isFailed ? 20 : 16`
- `strokeWidth`: `isFailed ? 1.8 : 1.5` → `isFailed ? 2.1 : 1.8`
- `viewBox` and the shackle/body path/rect shapes untouched.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 23 suites / 72 tests pass.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — all 19 pages captured, no failures.
- Visually inspected `dashboard/scripts/visual-out/workflow-run.png`
  (`/workflow-runs/1`, the existing gate fixture — the fixture harness already
  had a gate showing both green/passed and red/failed states, so no new
  fixture was needed): the green (non-failed) padlock between `places-resolve`
  and `places-enrich` renders visibly larger and bolder than before, and the
  red failed padlock between `places-enrich` and `places-enrich-with-llm` is
  larger too, preserving its relative size advantage. Nothing clipped,
  overlapping, or blank.
- `dashboard/scripts/_dashboard-harness.mjs` unchanged — its existing `gates`
  fixture (run `1`) already exercises both a passed and a failed gate state,
  visible in the `workflow-run` screenshot, so no fixture update was required.

## Result
done T296
