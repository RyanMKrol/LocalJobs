# T051 worklog

## Attempt 1 — 2026-06-22

Wrapped the badge + relative-time span in a `<span style={{ whiteSpace: 'nowrap' }}>` container in `dashboard/app/workflows/page.tsx` line 39. Same approach used for the Run button in T050.

Build, tsc, and npm test all green. Committed as `T051: keep last-run pill + time on one line (white-space:nowrap)`.
