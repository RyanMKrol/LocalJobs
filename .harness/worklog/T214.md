# T214 worklog

## What was built

Scaffolded the `tv-recommendations` workflow under `src/jobs/tv-recs/`:

- `tv-recs.workflow.ts` — workflow named `tv-recommendations`, monthly schedule (`0 9 1 * *`), `maxConcurrency: 4`, single member `tv-snapshot`
- `config.ts` — env-driven paths (`TV_RECS_*` knobs) mirroring `movies/config.ts`, reuses `plexConfig` for connectivity
- `types.ts` — `PlexShow`, `TvTasteProfile`, `TvSnapshotFile`, `TvTasteProfileFile`, `RawSuggestion`, `Recommendation`, plus minimal Plex response shapes
- `lib.ts` — `ensureDirs`, `readJsonFile`, `writeJsonFile`
- `tv-shows.ts` — pure helpers: `buildShowSnapshots`, `buildOwnedSet`, `buildTvTasteProfile`, `decadeOf`, re-exports `extractTmdbId` from plex/plex.ts
- `stages/tv-snapshot.job.ts` — `JobDefinition` named `tv-snapshot`
- `stages/tv-snapshot.ts` — implementation: fetches TV section via `plexGet`, builds snapshot + taste profile, writes both to `data/out/`
- `stages/tv-snapshot.test.ts` — pure helper tests with synthetic Plex fixtures (no live Plex)
- `README.md` + `CLAUDE.md` updated with tv-recommendations workflow docs + file map entry

## live-api verify

Ran `LOCALJOBS_DB=$(mktemp ...) node --import tsx/esm src/runJob.ts tv-snapshot`:

- Fetched **627 shows** from Plex section 5
- All 627 GUID-matched (owned set size 627, 0 without a tmdb:// GUID)
- Taste profile: 36 genres, 1527 roles, 7 decades, 15 countries
- Top genres: Drama 324, Comedy 239, Animation 129, Crime 107, Action 81
- Wrote `src/jobs/tv-recs/data/out/snapshot.json` (280 KB) + `taste-profile.json` (40 KB)
- Both files non-empty ✓

## Test results

- `npx tsc --noEmit` — green
- `npm test` — all tests passed; registry loads cleanly (24 jobs, 5 services, 5 workflows)
