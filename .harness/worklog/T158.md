# T158 worklog

## What was done

Reversed the T140 hiding behaviour for empty/null YAML frontmatter fields in the markdown preview popover.

### Changes

**`dashboard/app/workflow-runs/[id]/page.tsx`**
- Removed `visibleFields` filter (`parsed.fields.filter(([, v]) => !isFmEmpty(v))`) — all fields now render.
- Updated `renderFmValue` to return `<span className="md-fm-null">null</span>` for effectively-empty values (empty string, `'null'`, `'~'`, `[]`), instead of passing them through as-is.
- Kept `isFmEmpty` (now used only by `renderFmValue`), array rendering (comma-separated), and grid alignment from T140.

**`dashboard/app/globals.css`**
- Added `.md-fm-null { font-weight: 700; color: var(--amber); font-style: italic; }` — bold amber italic so missing-data fields visibly stand out.

### Verification

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — clean
- `node dashboard/scripts/mobile-check.mjs` — 9/9 pages pass
