# T132 worklog — Backlog: hardcode spin caret, remove toggle

## What was done

1. **`dashboard/app/ui.tsx`** — removed the entire caret toggle block: `CARET_STYLES`, `CaretStyle` type,
   `CARET_STYLE_KEY`, `DEFAULT_CARET_STYLE`, and `useCaretStyle`. Nothing else imported them.

2. **`dashboard/app/backlog/page.tsx`**:
   - Removed `useCaretStyle` and `CARET_STYLES` from the import line
   - Removed `const [caretStyle, setCaretStyle] = useCaretStyle()` from the component body
   - Deleted the `<div className="caret-style-bar">` block (~lines 170-184) with its `CARET_STYLES.map` buttons
   - Replaced `<div className={`caret-${caretStyle}`}>` wrapper with a plain `<div>`
   - The `review-filter-bar` and its `caret-style-label`/`caret-style-btn` classes were left intact

3. **`dashboard/app/globals.css`**:
   - Removed the 5-style caret block (lines 175-195): bold, accent, circle, boxed, and the old prefixed spin
   - Removed `.caret-style-bar` rule (kept `.caret-style-label`, `.caret-style-btn` for the 'Show' filter)
   - Updated `.section-heading-summary` to embed the spin style unconditionally:
     - `display: flex; align-items: center` for optical vertical centring (no more `vertical-align: middle`)
     - `margin: 0 0 12px -8px; padding: 4px 8px` for clean symmetric hover area (8px left of caret, 8px right of title)
     - Removed the old default `::before { content: '▾ '; ... }` fallback
   - `::before` now uses `flex-shrink: 0; margin-right: 6px` (up from 4px) for balanced spacing
   - Hover highlight and rotation rules target `details .section-heading-summary` directly

## Observed results

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — build succeeded, /backlog at 2.29 kB
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (including /backlog)
