# T552 — 2026-07-14

## What was done

1. **schema.sql comment fix** (no schema change): `runs.status` comment now lists
   `skipped` (written by `recordSkippedRun`/`setRunNoop` in `src/db/store/runs.ts`),
   and `runs.trigger` comment now lists `workflow` (written for member skip/gate
   rows). Left `workflow_runs`' comments untouched per the spec's instruction —
   out of scope to verify/alter those here.

2. **`hasColumn(db, table, col)` helper** added and exported from `src/db/index.ts`.
   Implemented as `SELECT 1 FROM pragma_table_info(?) WHERE name = ?` (verified the
   parameterised `pragma_table_info(?)` table-valued function binds cleanly under
   this repo's better-sqlite3 version via a quick manual smoke test — no fallback
   needed). Re-reads column info fresh from SQLite on every call.

3. Removed the three cached arrays `svcCols`, `wfCols`, `jobCols` in `openDb()` and
   rewrote every additive-column guard (services/workflows/jobs, including the
   `_overridden_at` cluster and the `certified` block) to call `hasColumn(db, table,
   col)` instead. `runCols` (used once, immediately after declaration, no staleness
   window) was left as-is — not part of the sharp-edge this task fixes. All ALTER
   TABLE statements, defaults, and ordering are unchanged — pure refactor of the
   existence check.

4. Added a new regression test case to `src/db/migrate-existing-db.test.ts`
   (imports `hasColumn` from `./index.js`) proving `hasColumn` stays fresh across
   an intervening `ALTER TABLE`: on a throwaway table it returns false for a
   not-yet-added column, then after adding the column via `ALTER TABLE ... ADD
   COLUMN` the SAME call returns true, and a hypothetical duplicate guard using it
   is shown to be a no-op (`assert.doesNotThrow`) rather than a duplicate-ALTER
   crash. All prior test cases remain green.

## Checks run

- `npx tsc --noEmit` — clean, no errors.
- `npm test` — full suite green (all suites pass, including the new T552 case).
- Manually ran `npx tsx src/db/migrate-existing-db.test.ts` directly — 22/22
  passed, confirming the new case executed.

## Scope

Touched only `src/db/index.ts`, `src/db/schema.sql`, and
`src/db/migrate-existing-db.test.ts`, per the task's scope gate.
