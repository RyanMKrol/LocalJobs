# T189 worklog

## What was done

Removed per-stage push notifications from `runWorkflow`:

1. **`src/core/workflow-executor.ts`**: Removed `notifyStage` import, deleted the
   `lastNotifiedStatus` dedup map and `maybeNotifyStage` helper, removed the two
   `maybeNotifyStage(...)` call sites (in `onSettle` and `onSkip`). Only
   `notifyWorkflow` at the end of the run remains.

2. **`src/core/workflow-executor.test.ts`**: Added T189 test — imports
   `_setFetchForTest`/`_resetFetchForTest` from notifier, enables ntfy temporarily,
   intercepts fetch to capture push titles, runs a two-stage workflow, asserts zero
   per-stage pushes and exactly one aggregate workflow push.

3. **`CLAUDE.md`**: Updated the per-stage-dedup bullet and both file-map rows for
   `workflow-executor.ts` and `notifier.ts`.

## Verification

- `grep -n notifyStage src/core/workflow-executor.ts` → empty (confirmed)
- `npx tsc --noEmit` → clean
- `npm test` → all tests passed (including new T189 test)
