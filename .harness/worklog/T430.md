# T430 — paragraph-length job descriptions for the `places` workflow

## Do

Rewrote the `description` string of all 4 `places` job files to real, grounded paragraphs, based
on `src/workflows/places/CLAUDE.md` and each stage's own code:

- `src/workflows/places/stages/ingest.job.ts` (`places-ingest`)
- `src/workflows/places/stages/resolve.job.ts` (`cid-to-place-id-resolver`)
- `src/workflows/places/stages/enrich.job.ts` (`places-enrich`)
- `src/workflows/places/stages/llm-enrich.job.ts` (`enrich-with-llm`)

Each paragraph covers: what the stage reads/writes, step-by-step behavior, external
services/credentials involved, and notable behavior (idempotency, pacing, spend governance, gate
role, lineage/rootKey handling) — plain prose, no markdown syntax.

## Verify

- `npx tsc --noEmit` — pass.
- `npm test` — 213/213 tests pass, 0 failures.
- No behavior changed — only the `description` string literals in the 4 job files. This is
  pure-content authoring per the task's Overview; the render mechanism was already built by a prior
  task.

## Result

done T430
