# T084 worklog

## What was done

Removed the leading `#` from back-navigation labels in two places:

1. `dashboard/app/ui.tsx:76` — `backFrom` helper: changed `` `#${segs[1]}` `` → `segs[1]`
2. `dashboard/app/runs/[id]/page.tsx:21` — fallback label: changed `` `#${run.workflow_run_id}` `` → `run.workflow_run_id`

Searched the entire dashboard for any other `#`-prefixed id patterns in nav/label contexts — none found.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
