# T058 — Back-nav: drop "workflow run" words, show just the run id

## Attempt 1 — 2026-06-22

### Changes
- `dashboard/app/ui.tsx` line 59: changed `workflow run ${segs[1]}` → `#${segs[1]}`
- `dashboard/app/runs/[id]/page.tsx` line 21: changed `workflow run ${run.workflow_run_id}` → `#${run.workflow_run_id}`

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — build succeeded

### Result
done
