# T452 — remove plex-language-no-track-flag job

2026-07-07

## What I did
- Deleted `src/workflows/plex-language-fix/stages/no-track-flag.ts`, `.job.ts`, and `.test.ts`.
- Removed the `plex-language-no-track-flag` member from `plex-language-fix.workflow.ts`'s `jobs`
  array and its mention from the file's top-of-file doc comment. The workflow is now exactly
  `plex-language-scan` → `plex-language-apply`.
- Removed the two mutating endpoints `POST /api/plex-no-track/:itemKey/ignore` and
  `/unignore` from `src/api/server.ts`, plus the now-unused `NO_TRACK_JOB` import.
- Checked `contracts.ts` — there was never a gate contract for the
  `plex-language-scan` → `plex-language-no-track-flag` edge (only the
  scan → apply contract exists), so nothing to remove there.
- Updated `src/workflows/plex-language-fix/CLAUDE.md`: collapsed the "three members" section to
  "two members", removed the whole no-track-flag section, and added a short note that the job was
  deliberately removed (owner decision — the signal wasn't worth keeping) so a future reader
  doesn't mistake it for an oversight.
- Confirmed a repo-wide grep for `no-track-flag`/`plex-no-track`/`plex-language-no-track-flag`
  is now empty outside of `src/workflows/projects-sync/data/repos/` (a gitignored, registry-excluded
  clone artifact from a past `projects-sync` run — not tracked code, out of scope, and excluded from
  `data/` scans by the registry/tsconfig/test-runner rule).

## Checks run
- `npx tsc --noEmit` — clean, no errors.
- `npm test` — 230/230 passing, 0 failures (includes `src/core/gate-coverage.test.ts` and the job
  registry test suite).

## Remaining
- None for this task. `scan.ts`/`apply.ts` logic changes are explicitly out of scope (T453).
