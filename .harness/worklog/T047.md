# T047 worklog

## Attempt 1 — 2026-06-22

### What was done

- Added `.pill.done` (green, rgba(63,185,80)) and `.pill.in-progress` (accent blue, rgba(88,166,255)) CSS classes to `dashboard/app/globals.css`
- Refactored `TaskCard` in `dashboard/app/backlog/page.tsx` to accept an `isNext: boolean` prop
- Added `findNextEligible(tasks)`: finds the first pending, gate-less task whose every `dependsOn` entry has `status === 'done'`
- Done tasks show `✓ done` green pill; next-eligible shows `▶ in progress` blue pill with a subtle blue border on the card; pending stays neutral
- Ran: `npx tsc --noEmit` ✓, `npm test` ✓ (all tests passed), `npm --prefix dashboard run build` ✓, mobile-check ✓ (all 9 pages pass)

### Result

Committed as `850ee38`.
