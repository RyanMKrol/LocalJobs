# T161 worklog — franchise-gaps owned-example anchor

## What was built

Added a recognisable "you own" anchor per collection to the franchise-gaps report.

### Files changed

- `src/jobs/movies/types.ts` — added optional `collectionExamples?: Record<string, { title; year }>` to `FranchiseGapsFile`
- `src/jobs/movies/movies.ts` — new `collectionOwnedExample(collection, owned)` pure helper: picks the earliest owned part from a collection's TMDB parts list; null when owner has none
- `src/jobs/movies/stages/franchise-gaps.ts` — imports `collectionOwnedExample`; in Pass 2, for each collection with gaps, calls the helper and builds a `collectionExamples` map written to the gaps JSON
- `src/jobs/movies/stages/notify.ts` — `writeReport` gains a `collectionExamples` param; renders `_You own: <title> (<year>)_` under each collection heading that has an entry
- `src/jobs/movies/movies.test.ts` — adds `collectionOwnedExample` tests: earliest-wins, null when no owned parts, dateless parts sort last
- `src/jobs/movies/stages/franchise-gaps.test.ts` — asserts `collectionExamples` carries the expected owned example (Saw, 2004)
- `src/jobs/movies/stages/notify.test.ts` — passes `collectionExamples` in the gaps fixture; asserts "You own: Saw (2004)" appears in the report

## Verification

`npx tsc --noEmit` — clean.  
`npm test` — all 33 test files passed, all assertions green.

No dashboard changes (report is a markdown artifact, not a UI component) — dashboard build not required.
