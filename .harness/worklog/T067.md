# T067 worklog — Backlog view: make every section collapsible

## Attempt 1 — 2026-06-22

### What was done

Applied collapsible sections to all three sections on the `/backlog` list view using native HTML `<details>/<summary>`:

- **Harness-buildable** (`<details open>`) — starts expanded, collapses on click
- **Needs a human** (`<details open>`) — starts expanded, collapses on click  
- **Done** — was already `<details>` (collapsed by default), converted to same pattern

All three sections use the new `.section-heading-summary` CSS class which matches the existing `h2` visual style (same font size, muted colour, uppercase, letter-spacing) while adding a chevron indicator (▾ open / ▸ closed). The `<h2>` tags were replaced by `<summary>` inside `<details>` for all three sections.

Each section toggles independently — no shared state.

### Verification

- `npx tsc --noEmit` — passed (no type errors)
- `npm test` — 18 test files, all passed
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass including `/backlog`

### Files changed

- `dashboard/app/backlog/page.tsx` — replaced `<h2>` + content with `<details open>/<summary>` for all three sections
- `dashboard/app/globals.css` — added `.section-heading-summary` class (chevron + h2-like styling)
