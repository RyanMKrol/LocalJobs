# T576 — collapse listening-digest's two near-identical passes into a shared runPass()

2026-07-15

## What I did
- In `src/workflows/listening-digest/stages/listening-digest.ts`, extracted the duplicated
  per-pass body (fetch top albums -> fetch top tracks -> filterRealAlbums ->
  renderDigestMarkdown -> write `<outDir>/listening-digest-<key>.md` -> markWorkItem) into a
  single `runPass(ctx, { period, key, label, detailName, outDir, username, fetchTopAlbums,
  fetchTopTracks, now })` helper.
- `runListeningDigest` now calls `runPass` twice: once for the 1-month pass and once for the
  trailing pass, threading the trailing period/key/label exactly as before.
- Preserved a subtlety in the original code: the trailing pass's `work_items` `detail.name` uses
  the BASE month label (`Listening digest (trailing 3 months) — ${label}`), not the "(Trailing 3
  Months)"-suffixed label used for the markdown heading — so `runPass` takes an explicit
  `detailName` param rather than deriving it from `label`, keeping the ledger detail byte-identical
  to pre-refactor behaviour while the markdown heading label still varies per pass.
- Left `monthKey`/`monthLabel`/`toArray`/`filterRealAlbums`/`renderDigestMarkdown` and the fetcher
  factories unchanged, per the spec.
- Did not touch idea B28 (month-label fix) — out of scope for this task.

## Checks run
- `npx tsc --noEmit` — passes, no errors.
- `npm test` — full suite passes (10 test files / all green), including
  `listening-digest.test.ts`'s existing 6 sub-suites (`toArray`, `monthKey / monthLabel`,
  `filterRealAlbums`, `renderDigestMarkdown`, `runListeningDigest — environment guards`,
  `runListeningDigest — normal behaviour`) unchanged and passing — confirming output filenames,
  ledger keys, and detail content are unchanged (the "re-running the same month" and "trailing
  3-month" tests specifically assert both files/ledger rows).

## What remains
Nothing outstanding for this task.
