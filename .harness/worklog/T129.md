# T129 worklog — pair chooser tasks with needs-human review tasks

## What was done

Documentation-only change. Added a new convention bullet in two places:

1. **CLAUDE.md** — "Autonomous build harness" section, before the "Definition of Done mirrors CI"
   bullet. Describes the rule: any backlog task that builds multiple options for the owner to choose
   between must be paired with a `needs-human` review task that `dependsOn` it.

2. **.harness/HARNESS.md** — new sub-section before §9 "Result protocol", titled "Backlog
   authoring: pair chooser tasks with review tasks (T129)". Mirrors the rule and cross-references
   CLAUDE.md + the concrete examples (T099/T113/T116 → T126/T127/T128).

## Verification

- `npx tsc --noEmit` → clean
- `npm test` → all tests passed (registry loads 8 jobs, 4 services, 2 workflows; all other suites green)
- No dashboard change so no build needed.
