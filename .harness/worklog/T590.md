# T590 — 2026-07-16

## What I did
Added the run's absolute start time to the workflow-run detail page's existing header
summary `<p className="sub">` line in `dashboard/app/workflow-runs/[id]/page.tsx`, per the
owner-confirmed placement in the spec: appended
` · Started ${fmtAbsolute(run.started_at)}` after the existing duration segment, guarded
with `run?.started_at ? ... : ''` so it degrades gracefully before `run` has loaded (mirrors
the existing `run?.duration_ms != null ? ... : ''` guard pattern on the same line).
`fmtAbsolute` was already imported on this page (added by the dependency task,
absolute-timestamps-a) — no new import needed.

No changes were needed to `dashboard/scripts/_dashboard-harness.mjs`: the existing
`/workflow-runs/1` (and other workflow-run) fixtures already set `started_at`, so the new
segment is exercised by the existing PAGES entries without any new page/fixture/flow.

## Checks run
- `npx tsc --noEmit` — clean, no output.
- `npm test` — 10/10 tests pass.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — 58/59 captures pass. The one failure
  (`workflow-run-skipped`, a `.rf-dag-node` selector timeout) was verified to be a
  PRE-EXISTING flake unrelated to this change: reproduced identically with `git stash`
  applied (i.e. against `main` before this edit).

## Visual confirmation (observed)
Opened `dashboard/scripts/visual-out/workflow-run.png` (route `/workflow-runs/1`). The
header summary line now reads:
`3 of 3 stages · 100% · 45.7s · Started 22 Jun 2026, 10:00`
— rendered inline on the existing sub-line, visible immediately below the page header
without scrolling to or opening the Framework Logs panel, no broken/undefined text, nothing
clipped or overlapping.

## Remaining
None — task complete.
