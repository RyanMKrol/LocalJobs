# T340 — shared `.panel + .panel` spacing rule

## Change
- Added `.panel + .panel { margin-top: 28px; }` in `dashboard/app/globals.css` right after the
  base `.panel` rule.
- Removed the now-redundant inline `style={index > 0 ? { marginTop: 28 } : undefined}` from
  `dashboard/app/workflows/page.tsx` (category-panel `.map`), leaving `<div className="panel" key={key}>`.
  Services page (`dashboard/app/services/page.tsx`) already rendered its category panels as plain
  adjacent siblings with no spacing mechanism — it now picks up the same CSS rule automatically,
  no changes needed there.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 127/127 pass.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — all 27 screenshots captured; visually inspected:
  - `services.png` — CLI TOOLS / WEBSITE SCRAPES / APIS / UNCATEGORIZED panels now have a clear
    28px gap between them (previously touching borders).
  - `workflows.png` — SECOND BRAIN / RECOMMENDATIONS / REGULAR MAINTENANCE / UNCATEGORIZED panels
    show the identical gap as before (now CSS-driven instead of inline), visually unchanged.
  - `overview.png`, `workflow.png` (places detail), `workflow-run.png`, `backlog.png` — all other
    panels are separated by `<h2>` headings or non-panel wrapper elements (grids/flex), so
    `.panel + .panel` never fires there (no adjacent `.panel` siblings) — confirmed no doubled or
    new unwanted gaps anywhere.
  - Grepped every `className="panel"` usage across `dashboard/app` (page.tsx, backlog, workflows,
    workflows/[name], workflow-runs/[id], WorkflowOutputSection, runs/[id], jobs/[name],
    services) — none of the other occurrences are direct adjacent DOM siblings of another `.panel`
    (each is guarded by a heading, conditional render, or separate section), so the new rule only
    affects Workflows (unchanged look) and Services (fixed).
- `node dashboard/scripts/mobile-check.mjs` — all 16 pages pass, 0 horizontal overflow, 0
  boundary-crossing elements.

No `dashboard/scripts/_dashboard-harness.mjs` changes needed — no new page/flow/state was added,
just a CSS spacing fix on existing pages already covered by PAGES.
