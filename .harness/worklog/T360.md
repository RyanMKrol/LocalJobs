# T360 — Add remark-gfm so dashboard markdown popovers render GFM tables

## What changed
- `dashboard/package.json`: added `remark-gfm@^4.0.1` to dependencies; ran
  `npm install --prefix dashboard` to update `dashboard/package-lock.json`.
- Wired `remarkPlugins={[remarkGfm]}` into all four `<ReactMarkdown>` usages:
  - `dashboard/app/backlog/page.tsx` — `TaskSpec` (spec content) and the build-log
    popover (worklog content).
  - `dashboard/app/workflow-runs/[id]/page.tsx` — `MarkdownModal`.
  - `dashboard/app/components/WorkflowOutputSection.tsx` — its own `MarkdownModal`.
- Did NOT add `rehype-raw` or any raw-HTML passthrough — confirmed via grep that
  `rehype-raw` appears nowhere in `dashboard/package.json` or any `.tsx` file
  (the only hits are pre-existing comments documenting the deliberate exclusion).
  The T115 XSS-safety property is unchanged.
- `dashboard/scripts/_dashboard-harness.mjs`:
  - Extended the workflow-run `/output` fixture's synthetic markdown content with
    a small GFM pipe table (Ticker/Account/Quantity, mirroring stocks-sync's
    `portfolio.md` shape).
  - Added a new `FLOWS` entry `workflow-run-output-table` (`/workflow-runs/1`,
    `viewport: true`) that clicks `.out-meta-link` to open the output popover and
    waits for `.md-body table` to be visible before the screenshot.

## Verification
- `npx tsc --noEmit` — clean, no errors.
- `npm test` — 134/134 passing (41 suites), no regressions.
- `npm --prefix dashboard run build` — succeeded (Next.js 15.5.19, all routes
  compiled/prerendered).
- `node dashboard/scripts/visual-check.mjs` — all 31 captures passed (including
  the new `workflow-run-output-table`). Opened
  `dashboard/scripts/visual-out/workflow-run-output-table.png` and visually
  confirmed the table renders as a real bordered `<table>` with header row
  (TICKER/ACCOUNT/QUANTITY) and two data rows (AAPL/invest/10, VUSA/isa/5) — no
  visible `|`/`---` pipe-syntax characters. Also spot-checked `backlog-expanded.png`
  and `workflow-run.png` (the baseline for the same route) — both render exactly
  as before, no regression from the theme popover styling or table CSS.
- `node dashboard/scripts/mobile-check.mjs` — all 18 pages pass at 402×874, 0px
  horizontal overflow, 0 boundary-crossing elements. `.md-body table` in
  `globals.css` already has `width: 100%` with no fixed intrinsic width, so the
  new table shrinks to fit the modal at phone width with no CSS changes needed.

## Result
done T360
