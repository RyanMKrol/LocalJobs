# T380 — reorder logs page level tag before source label

## Change
Swapped JSX order in `dashboard/app/logs/page.tsx`'s `lines.map` block so each log line now
renders `timestamp → [LEVEL] → [source] → message` (was `timestamp → [source] → [LEVEL] →
message`). Kept the existing spacing nodes, just relocated with their blocks: `.ts`'s own
margin-right separates timestamp from the level tag, `.log-lvl-tag`'s own margin-right plus an
explicit `{' '}` separates the level tag from the source label, and the existing `{' '}` separates
the source label from the message. No CSS changes.

## Verification
- `npx tsc --noEmit` — pass
- `npm test` — 153/153 pass
- `npm --prefix dashboard run build` — pass
- `node dashboard/scripts/visual-check.mjs` — all 34 pages captured, including `logs.png`
- Read `dashboard/scripts/visual-out/logs.png`: confirmed all 6 fixture log lines (info/warn/error,
  both job sources like `[places-resolve]`/`[tv-rec-merge]` and workflow source
  `[movie-recommendations]`) render as `<timestamp> [LEVEL] [source] message`, e.g.
  `6/22/2026, 10:00:00 AM [ERROR] [places-enrich] Places API returned 429 — quota exceeded for the day`.
  Nothing blank/overlapping/clipped.
- No changes needed to `_dashboard-harness.mjs`/PAGES/FLOWS — existing `logs` page entry + fixture
  already cover all level/source combinations.

## Result
done T380
