# T504 — Cache google-places Place Details reads for 22 hours

## What was done
1. Added `cacheTtlMs: 79_200_000` (22 hours) to the `google-places` service definition in `src/services/google-places.service.ts`.
2. Added a stable `cacheKey` to the `callService('google-places', ...)` call site in `src/workflows/places/stages/enrich.ts`, keyed by place_id: `` `google-places:details:${placeId}` ``.
3. Created a comprehensive test file `src/services/google-places.service.test.ts` with two tests:
   - Verifies that the service definition sets a 22-hour cacheTtlMs
   - Verifies that a repeated Place Details fetch for the same place_id within 22h is served from service_cache, not a second live call

## Checks run
- `npx tsc --noEmit` — **PASS** (no type errors)
- `npm test` — **PASS** (all 253 tests pass, including 2 new google-places.service tests)

## Verification performed
The caching mechanism is now enabled for Google Places Place Details reads:
- The service definition correctly declares the 22-hour cache TTL
- The call site passes a stable, place_id-based cache key
- The test suite hermetically verifies that repeated calls with the same cache key hit the cache on the second invocation instead of making a second live API call
- All existing tests still pass

## Outcome
The implementation matches the task specification exactly. Caching is now active for Google Places Place Details reads, which will avoid re-paying for the same place metadata during development re-runs within a 22-hour window.
