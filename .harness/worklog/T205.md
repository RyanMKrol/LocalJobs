# T205

Re-scoped 2026-06-26: this task was rewritten from the old "prototype a joined-output view (3
switchable options)" into a straight build of a unified, de-duped output section on every workflow
detail page (no chooser, no toggle). The graph-view prototyping that used to be conflated here now
lives in its own chooser task (T220 → reviewed by T206 → hardcoded by T207). Prior attempt marker
cleared so the loop re-attempts the new, narrower scope.
