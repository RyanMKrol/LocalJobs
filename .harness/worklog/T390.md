# T390 — Extract shared IgnoredSection component + fix padding

## What changed

- Added `dashboard/app/components/IgnoredSection.tsx` — a shared component owning
  only the panel chrome (`.panel.ignored-section` + heading/subtitle), with the
  table content passed as `children` (each manager's table shape differs).
- Added CSS to `dashboard/app/globals.css`: `.ignored-section` (padding: 16px 18px),
  `.ignored-section-heading` (margin: 0 0 4px), `.ignored-section-subtitle`
  (margin: 0 0 10px) — placed AFTER `.movie-gaps-scroll .panel` in the cascade so it
  wins when nested inside that wrapper (same specificity, last-declared wins).
- Replaced all 4 duplicated inline-styled "Ignored (N)" blocks in
  `dashboard/app/workflows/[name]/page.tsx` (`TvRecsManager`, `MovieRecsManager`,
  `MovieGapsManager`, `MissingSeasonsManager`) with `<IgnoredSection>`, preserving
  each manager's own subtitle wording verbatim.
- Updated `CLAUDE.md`'s "Shared dashboard UI components (T268)" bullet list to add
  `IgnoredSection`.

## Verification performed

- `npx tsc --noEmit` — clean.
- `npm test` — 168/168 passing, 0 failures.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — 42/43 captures pass. The one failure
  (`workflow-run-skipped`, `.rf-dag-node` selector timeout on `/workflow-runs/skipped`)
  is PRE-EXISTING and unrelated to this task: confirmed by `git stash`-ing all my
  changes, rebuilding, and re-running visual-check — the same single failure
  reproduced against the untouched `main` tree. Restored my changes after
  confirming (`git stash pop`).
  - Visually inspected `workflow-movie-recs.png`, `workflow-tv-recs.png`, and
    `workflow-missing-tv-seasons.png` (the 3 workflows with Ignored sections).
    All 4 "Ignored (N)" panels (movie recs, movie franchise gaps, tv recs, missing
    seasons) now show clear breathing room between the panel border and both the
    heading and subtitle text — including `MovieGapsManager`'s and
    `MissingSeasonsManager`'s panels, which previously had ZERO padding from any
    source (not wrapped in `.movie-gaps-scroll`).
- `node dashboard/scripts/mobile-check.mjs` — all 20 pages pass, 0px horizontal
  overflow, 0 boundary-crossing elements at 402px viewport.
- No new FLOWS entry was needed — `workflow-movie-recs`/`workflow-tv-recs`/
  `workflow-missing-tv-seasons` are existing baseline PAGES entries in
  `_dashboard-harness.mjs` whose fixtures already include an ignored row, so the
  Ignored section already appears in the baseline screenshot for all 3 pages.

## Result

done T390
