# T318

Bumped the movies workflow's recommender-branch Claude model fallback from `claude-sonnet-4-6` to `claude-sonnet-5`, matching the newer default used elsewhere in the repo (workouts-progress, project-summarize).

## Changes
- `src/jobs/movies/config.ts:35` — `recsModel` fallback now `'claude-sonnet-5'`.
- `.env.example` (movies recommendation layer section) — comment + commented example line now document `claude-sonnet-5`.

`src/jobs/tv-recs/config.ts` and `src/jobs/perfumes/config.ts` left untouched (out of scope).

## Verification
- `grep -rn "claude-sonnet-4-6" src/jobs/movies` — no matches (no test asserted the old default).
- `npx tsc --noEmit` — passes.
- `npm test` — 87/87 tests pass.
