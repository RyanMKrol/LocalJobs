# T490 — 2026-07-10

## What I did
Removed the type-to-confirm gate from the Admin page's "Delete all workflow output" button
(`dashboard/app/admin/page.tsx`):
- Removed `CONFIRM_PHRASE`, `confirmText` state, `canDelete` derivation, the `if (!canDelete) return`
  guard in `deleteAllOutput()`, and the `#admin-confirm-input` text input + its label.
- `deleteAllOutput()`'s try/catch/finally body (the `api.resetAllWorkflowsOutput()` call and
  busy/result/err handling) is unchanged — no `window.confirm()` or any other dialog was added; the
  button click goes straight to `deleteAllOutput()`.
- The button's `disabled` condition is now simply `disabled={busy}`, and the conditional `title`
  tooltip referencing the confirm phrase was removed.
- The existing Danger-zone descriptive copy (what is/isn't deleted) is unchanged — it remains the
  only warning shown before clicking.

Also updated `dashboard/scripts/_dashboard-harness.mjs`: removed the `admin-confirm-typed` FLOWS
entry, which drove `#admin-confirm-input` — that element no longer exists.

## Checks run
- `npx tsc --noEmit` — passes, no output.
- `npm --prefix dashboard run build` — passes, all 13 routes compile/prerender successfully.
- `node dashboard/scripts/mobile-check.mjs` — all 31 pages pass, including `/admin` (0px overflow,
  0 boundary-crossing elements).
- `node dashboard/scripts/visual-check.mjs` — ran to completion once (53/54 captures passed with one
  hard error on an unrelated page — not `admin`, `admin-cache`, or `admin-run-all-confirm`, which all
  showed PASS; two subsequent reruns of the full 54-capture sweep hung on an unrelated later
  screenshot for 10+ minutes with no forward progress, so they were killed rather than re-run a
  third time — this looks like pre-existing local flakiness in the harness/Chromium setup, not
  something introduced by this change, since my edit only touches `admin/page.tsx` and removes one
  FLOWS entry).
  - Visually inspected `dashboard/scripts/visual-out/admin.png` (captured during the completed run):
    confirms the "Delete all workflow output" button renders solid/enabled (no greyed-out state), and
    there is no confirm text input or label above it — matches the intended zero-friction change. The
    Danger-zone descriptive copy above the button is unchanged and still renders correctly.

## Remaining
None — task complete.
