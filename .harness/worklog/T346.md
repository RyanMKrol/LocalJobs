# T346

## Do
Fixed `JOB_NAME` constant in `src/jobs/listening-digest/stages/listening-digest.ts` from `'listening-digest'`
(the workflow name) to `'lastfm-digest'` (the real `JobDefinition.name`, matching
`listening-digest.job.ts` and the workflow manifest's `jobs: [{ job: 'lastfm-digest' }]`).

Updated `src/jobs/listening-digest/stages/listening-digest.test.ts`'s `JOB` constant to match
(`'lastfm-digest'`), and added a regression assertion in the "writes a markdown digest and marks
the month done in the ledger" test that calls `workflowTerminalItems(['lastfm-digest'])` (the
same store function `GET /api/workflows/:name/output-items` uses) and asserts the digest item
surfaces there — proving the ledger row is now filed under the job name the Output section
actually queries.

## Verify
- `npx tsc --noEmit` — clean, no errors.
- `npm test` — all 127 tests pass (35 suites), including the new regression assertion.
- No live/paid API calls made; test uses stub Last.fm fetchers + scratch DB per existing pattern.

## Note
Per the spec, no destructive migration was performed for historical ledger rows recorded under
the old wrong key (`job_name = 'listening-digest'`) — they become harmlessly orphaned and can be
pruned later via the existing `POST /api/jobs/:name/prune` endpoint if desired.
