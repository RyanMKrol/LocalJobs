# T208 worklog — Backlog: mark a needs-human task done

## What was built

- **`.harness/human-done.json`** — new owner-owned committed overlay file (`id → { done: true, at }`)
- **`src/api/server.ts`** — added:
  - `HUMAN_DONE_PATH` constant and `humanDonePathFor()` helper
  - `HumanDoneEntry` interface, `readHumanDone()`, `setHumanDoneEntry()`, `writeHumanDoneEntry()`
  - `defaultCommitHumanDone()` — same pattern as `defaultCommitReviews`, reuses `commitReviewsFile`
  - `createApiServer` accepts `humanDonePath` and `commitHumanDone` injectable opts
  - `readBacklog()` updated to overlay both reviews.json AND human-done.json; done implies reviewed
  - `POST /api/backlog/:id/done` endpoint (needs-human gate validated, serialized, committed+pushed)
- **`dashboard/app/lib/api.ts`** — added `done?: boolean` to `BacklogTask` and `markBacklogDone(id)` client method
- **`dashboard/app/backlog/page.tsx`** — added "Mark done" button on needs-human tasks not already done; human-done tasks render as done+reviewed
- **`src/api/server.test.ts`** — 6 hermetic unit tests (no real git/network via injectable commitHumanDone)
- **`CLAUDE.md`** and **`.harness/HARNESS.md`** — documented the new human-done overlay alongside reviewed

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all passed (including 6 new T208 tests)
- `npm --prefix dashboard run build` — clean

## Observed

All 6 T208 tests confirmed:
- `setHumanDoneEntry` sets only target id, preserves rest
- `readHumanDone` returns `{}` on absent file, reads correctly when present
- `POST /api/backlog/:id/done` sets overlay, `GET /api/backlog` reports done+reviewed=true
- Non-needs-human tasks rejected with 400
- Invalid task id format rejected with 400
- Human-done task without reviews.json still shows reviewed=true
