# T192 — Move mode control to last in Theme Picker

Reordered the sections in `ThemeControls` (`dashboard/app/ui.tsx`) so the picker reads:
1. Theme (family)
2. Font
3. Mode (Dark / Light / System)
4. Reduce motion toggle

No logic, persistence, labels, or behaviour changed — pure visual reorder.

## Checks
- `npx tsc --noEmit` — clean
- `npm --prefix dashboard run build` — success
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
