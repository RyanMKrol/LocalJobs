# T126 — Review + choose: validation-gate graph style (from T099)

Reviewed by the owner on 2026-06-23.

**Chosen style: `key`** — the `gs-key` style: a tiny pill showing just the artifact
key (no producer name), state-coloured (passed/failed/pending/structural) and
clickable through to the gate detail page.

Follow-up **T134** will hardcode `key` as the sole gate display style on both graph
views, remove the live style toggle and the four unused styles (`icon` / `dot` /
`connector`(Arrow) / `lock`), and drop their now-unused code + CSS (including the
connector arrow-colouring path). (Per the T129 convention: chooser T099 → review
T126 → hardcode-the-winner follow-up T134.)
