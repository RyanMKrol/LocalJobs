# T559 — 2026-07-14

## What I did
Brought `src/workflows/movies/stages/merge.ts` to parity with
`src/workflows/tv-recs/stages/tv-rec-merge.ts` on TMDB per-suggestion search-failure
handling (R01 finding B13):

- Added a `searchFailed: number` field to `VerifyCounters`.
- Incremented it in `verifyInto`'s per-suggestion `catch` block (the same branch that
  already logs `✗ "<title>" — search failed: ...` and `continue`s to the next
  suggestion — per-item skip-and-continue behavior unchanged).
- Initialized `searchFailed: 0` alongside the other counters in `runMerge`.
- At the end of `runMerge`, after the summary log block and after
  `writeJsonFile(recsOut, out)` has already run (so the list is still always written),
  throw `new Error(\`TMDB search failed for ${counters.searchFailed} suggestion(s) this
  run — see warn logs above for titles.\`)` when `searchFailed > 0` — byte-identical
  message format to `tv-rec-merge.ts`.
- Updated the `runMerge` JSDoc to describe the new fail-on-search-error behavior instead
  of claiming unconditional resilience.
- Updated `movies/CLAUDE.md`'s Stage 3 (`rec-merge`) section to document the same.

Did NOT touch `tv-recs/stages/tv-rec-merge.ts` (already had this behavior — used as the
reference) and did NOT extract a shared module — that's explicitly a later unit per the
spec.

## Tests
Added to `movies/stages/merge.test.ts` (all hermetic — injected `searchMovie`, no live
TMDB, scratch DB via `npm test`'s harness):
- A new suggestion batch where one title's injected `searchMovie` throws and a second
  title after it resolves successfully. Asserts `runMerge(...)` rejects with a message
  matching `/TMDB search failed for 1 suggestion\(s\) this run/`, AND that the second
  (post-failure) suggestion still made it into the written `recommendations.json` —
  proving the per-item continue/skip behavior survives and the list is still written
  before the throw.
- A follow-up call to the original all-succeeding fixture asserting `runMerge` resolves
  without throwing and still writes the list (unchanged behavior when `searchFailed ===
  0`).

## Checks run (all observed to completion)
- `npx tsc --noEmit` — clean, no output.
- `npm test` — full suite green (`✓ all tests passed`), including the new
  `movies/stages/merge.test.ts` assertions:
  - `✓ merge throws on a per-suggestion TMDB search failure, after processing the rest of the batch`
  - `✓ merge does not throw when all TMDB searches succeed`
  Verified `tv-recs/stages/tv-rec-merge.ts` still passes unchanged (`git status` shows it
  untouched) and its own `T423` search-failure tests still pass.

## Scope
Only touched the 3 allowed files: `src/workflows/movies/stages/merge.ts`,
`src/workflows/movies/stages/merge.test.ts`, `src/workflows/movies/CLAUDE.md` (plus this
worklog). No `data/`, `.env`, or private-job files staged.
