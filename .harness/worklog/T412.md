# T412 — hevy-sync success/failure detail

## What changed

`src/workflows/workouts-sync/stages/hevy-sync.ts`:
- Success `markWorkItem` call now records `detail: { name, startTime, exerciseCount, setCount }`
  for the synced workout (computed via a reduce over `workout.exercises[].sets.length`, no extra
  loop over exercises).
- Failure `markWorkItem` call now records `detail: { name, error: msg }`, matching the
  places/perfumes failure-detail convention.

`src/workflows/workouts-sync/stages/hevy-sync.test.ts`:
- Extended `'syncs a new workout, marks it done, and writes it to the history file'` to fetch the
  ledger row via `getWorkItem(JOB, id)`, parse its `detail` (stored as a JSON string), and assert
  `name`, `exerciseCount`, and `setCount`.

`workouts-progress.ts` was verified already-compliant per the spec — no change made there.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 187/187 passing, including the new detail assertion.
- No live paid-API calls made; test uses the existing stub-fetcher hermetic harness already in
  `hevy-sync.test.ts`.
