# T260 worklog

## What was done

Removed `.mono` class from the three elements in the Consumers modal in `dashboard/app/services/page.tsx`:
- Service name: `<span className="mono">{name}</span>` → plain text inside `<strong>`
- Workflow links: `<a … className="mono">` → `<a …>` (kept `fontWeight: 600`)
- Job links: `<a … className="mono">` → `<a …>`

Limit-input fields elsewhere on the Services page (`className="mono limit-input"`) were NOT changed.

Added to `dashboard/scripts/_dashboard-harness.mjs`:
- `/api/services/:name/consumers` fixture returning two workflow groups (places + perfumes) with realistic job entries
- `service-consumers-modal` FLOW that clicks the google-places button and waits for `.db-modal` to appear, capturing a viewport screenshot

## Checks run

- `npx tsc --noEmit` — pass (no output)
- `npm test` — 70 tests pass, 0 fail
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 10 pages pass
- `node dashboard/scripts/visual-check.mjs` — all 15 pages/flows pass

## Visual confirmation

Viewed `dashboard/scripts/visual-out/service-consumers-modal.png`. The "Consumers of google-places" modal renders with proportional theme font throughout:
- "Consumers of google-places" heading — theme font ✓
- Workflow names "places" and "perfumes" — theme font (blue links) ✓
- Job names "places-enrich", "places-enrich-with-llm", "perfumes-build" — theme font (blue links) ✓

No monospace/code font visible in the modal. The limit-input fields on the Services table remain `.mono` as intended.
