# T376 — vertically center DAG node content

## Change

`dashboard/app/globals.css`: added `display: flex; flex-direction: column; justify-content: center;`
to the `.dag-node` rule (line 235 area), so leftover `min-height` space is distributed evenly above
and below the node's content instead of accumulating entirely at the bottom.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 153/153 pass.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — all 34 pages captured; opened and inspected:
  - `workflow.png` (definition-view, `/workflows/places`): `places-resolve` / `places-enrich` /
    `places-enrich-with-llm` node text is now vertically centered in each box — even padding above
    and below the job name, no more top-heavy look.
  - `workflow-run.png` (run-scoped, `/workflow-runs/1`): two-line content (job name + status, e.g.
    "places-enrich" / "FAILED") remains well-balanced within each node, no overlap or clipping — no
    regression from the change.
- `node dashboard/scripts/mobile-check.mjs` — 18/18 pages pass, 0 boundary-crossing elements
  (confirms the flex change doesn't interact badly with the `.dag-node { min-width: 0; }` mobile
  override).
- No change needed to `dashboard/scripts/_dashboard-harness.mjs` — no new page/state introduced,
  purely a styling fix to already-screenshotted elements.
- No change needed to `.dag-node-name`/`.dag-node-status` margins — the rendered result looked
  balanced as-is in both the one-line and two-line cases.
