# T304 — rename Backlog section headings

## Do
Renamed two literal JSX heading strings in `dashboard/app/backlog/page.tsx`:
- `⏳ Waiting on dependencies ({waiting.length})` → `⏳ Waiting on Human Tasks ({waiting.length})`
- `🔒 Needs a human ({human.length})` → `🔒 Human Tasks ({human.length})`

No other text, variable names, or CSS classes changed.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 85/85 pass, 0 fail.
- `npm --prefix dashboard run build` — succeeded.
- `node dashboard/scripts/visual-check.mjs` — all 19 pages captured PASS.
  Inspected `dashboard/scripts/visual-out/backlog.png`: both renamed headings
  render correctly — "⏳ WAITING ON HUMAN TASKS (2)" and "🔒 HUMAN TASKS (2)"
  (uppercased by CSS, as expected for section headings), emoji intact, counts
  correct, nothing else on the page regressed.
