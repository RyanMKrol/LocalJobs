# T286 — rename .claude/commands/* with local-jobs- prefix

Renamed all 5 command files (`git mv`) that existed at build time:
- idea.md → local-jobs-idea.md
- convert-ideas.md → local-jobs-convert-ideas.md
- loop-recover.md → local-jobs-loop-recover.md
- mark-task-failed.md → local-jobs-mark-task-failed.md
- pre-loop-checkin.md → local-jobs-pre-loop-checkin.md (this one didn't exist in the spec's
  authoring-time list but was present on disk — renamed per the "rename every .md file that
  exists at build time" instruction)

Updated every live cross-reference to the old `/command-name` invocation form in:
- CLAUDE.md
- .harness/CLAUDE.md
- .harness/HARNESS.md
- .harness/designs/manual-fail-signal.md
- the command files themselves (cross-references between them, including a literal file-path
  reference to `.claude/commands/loop-recover.md` in local-jobs-pre-loop-checkin.md)

Left `.harness/TASKS.json` untouched (out of scope; it has two historical task titles/scope
entries mentioning the old command names — those are the loop's own data, not living docs).
Also left `.harness/tasks/*.md` and `.harness/worklog/**` untouched per the spec's exclusions.

Verified no remaining hits outside excluded paths via:
`grep -rn --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=worklog -E
'/idea\b|/convert-ideas\b|/loop-recover\b|/mark-task-failed\b|/pre-loop-checkin\b' .`
(only hits left are in `.harness/TASKS.json`, which is out of scope).

Ran `npx tsc --noEmit` and `npm test` — both green (74/74 tests pass). No dashboard/ changes,
so no dashboard build needed.
