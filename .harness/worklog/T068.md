# T068 worklog

## What was done

Simplified the workflow-run Member runs section in `dashboard/app/workflow-runs/[id]/page.tsx`:

- Replaced `groupByStage` (which returned all runs per stage) with `latestByStage` (returns one run per stage, the latest).
- The `latestByStage` function iterates `members` (ascending start-time order) and uses a `Map` with `set` overwriting — so the last write per `job_name` wins (latest run), and Map's first-insertion key order preserves stage order.
- Removed `expanded` state, `toggleExpand` function, and the entire expand/collapse button + older-run rows from the table.
- Removed the 6th "toggle" column from the table header (`colSpan` updated from 6 → 5).
- Removed the `useState` import (no longer needed).

Earlier-cycle runs remain in the DB and are reachable via each job's own run history (e.g. `/runs/:id`).

## Verification

- `npx tsc --noEmit` — passed (no errors)
- `npm test` — all 18 test files, all assertions passed
- `npm --prefix dashboard run build` — compiled successfully, all 8 pages generated
