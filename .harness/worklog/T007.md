# T007 worklog — Docs audit: README + CLAUDE.md

## Attempt 1 — 2026-06-21

### Drifts found (code wins over docs)

1. **Public/private split** — CLAUDE.md Secrets section said "Only `src/jobs/demo.job.ts`
   is tracked — every other `*.job.ts` is gitignored." FALSE. The `places/` and
   `perfumes/` subfolders are tracked (their code is public). Only root-level
   `src/jobs/*.job.ts` files are gitignored (except demo). The "How to add a job"
   privacy callout also wrongly said `src/jobs/places/` is gitignored wholesale.

2. **Architecture diagram** — no mention of `pipeline-executor` or the
   pipeline-as-DAG model alongside the single-job executor path.

3. **File map (CLAUDE.md)** — seven entries missing entirely:
   - `src/db/index.ts` (connection + schema bootstrap)
   - `src/core/dag.ts` (pipeline DAG: topo sort + cycle detection)
   - `src/core/pipeline-executor.ts` (orchestrate pipeline runs)
   - `src/core/services.ts` (callService: cross-job rate-limit + quota)
   - `src/jobs/*.pipeline.ts` (pipeline manifests)
   - `src/jobs/*.service.ts` (service definitions)
   Also stale: `src/db/schema.sql` listed only `jobs/runs/run_logs` when there
   are nine tables; `src/core/types.ts` and `src/jobs/registry.ts` didn't mention
   pipelines or services.

4. **Conventions** — `Services` pattern missing. `callService` / `*.service.ts`
   for cross-job shared APIs was not documented anywhere in CLAUDE.md.

5. **Dashboard pages + layout (README.md)** — four new pages not listed:
   Pipelines, Pipeline detail, Pipeline run, Services. Layout tree missing
   `dag.ts`, `pipeline-executor.ts`, `services.ts`; stale schema.sql + registry
   description.

### Fix
- CLAUDE.md: architecture diagram/bullets, file map (7 new entries + updated
  descriptions), secrets section, privacy callout, new Services convention.
- README.md: dashboard pages (4 added), layout tree (3 new files + updated
  registry/schema lines).

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — 67 tests across 9 files, all passed
