# T435 — listening-digest job description

Rewrote `lastfm-digest`'s description into a real paragraph covering: the two Last.fm
endpoints called (user.getTopAlbums + user.getTopTracks, period=1month), the "one song on
repeat" filter (album excluded if a single track >=70% of plays), why there's no DynamoDB
persistence (website reads Last.fm aggregation directly), and idempotency keying (YYYY-MM
via work_items ledger).

Verification:
- `npx tsc --noEmit` — clean, no output.
- `npm test` — passed (exit code 0).

No dashboard changes, no other files touched — pure content authoring per scope.
