# T481 — 2026-07-10

Reworked the perfumes profile's personal-data frontmatter/body layout and rating scale.

## What changed
- `profile.template.md`: merged the two "Personal" frontmatter blocks into one, renamed
  `rating`→`personal_rating`, `date_added`→`personal_date_added`, `ownership`→`personal_ownership`,
  removed the always-`"owned"` `status` key entirely, and moved the block to sit right after the
  researched/crawled frontmatter fields (before Provenance). Moved the body's `## Personal Notes`
  and `## Application` sections to sit immediately after `## Overview` instead of at the bottom.
- `stages/build.ts`: `personalFieldsClause()` now emits `personal_rating`/`personal_date_added`/
  `personal_ownership` instructions and no longer emits a `status` line. `FALLBACK_TEMPLATE`
  mirrors the same renamed/dropped/reordered frontmatter + body-section shape.
- `lib.ts`: `loadPerfumes()` now halves the raw Dynamo `rating` (`rating / 2`) before it reaches
  `PerfumeInput`, so it lines up with Fragrantica's 0-5 `community_rating` scale.
- `types.ts`: updated `PerfumeInput`'s `rating`/`dateAdded`/`ownership` doc comments to describe
  the halved 0-5 scale and the `personal_`-prefixed frontmatter key each maps to (kept the existing
  TS property names, same precedent as `personalLongevity`/`personalProjection`/`personalSeasons`).
- `CLAUDE.md`: updated the `perfumes-build` DAG description to name the new frontmatter keys, the
  removed `status` field, the halved rating, and the new field/section positioning.
- Updated `stages/build.test.ts` and `lib.test.ts` to match the renamed field expectations and the
  halved rating value.

## Checks run
- `npx tsc --noEmit` — clean.
- `npm test` — 246/246 passing, 0 failures.

## Remaining
Nothing outstanding for this task.
