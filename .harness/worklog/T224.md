# T224 worklog — Full validation-gate coverage across all workflows

## What was done

Audited all four workflows for gate coverage:

- **places**: Already complete — ingest→resolve (`places-normalized`), resolve→enrich (`resolved-place-ids`), enrich→llm-enrich (`enriched-places`). No changes needed.
- **perfumes**: Already complete — find-url→fetch (`fragrantica-urls`), fetch→parse (`fragrantica-pages`), parse→build (`fragrantica-data`). No changes needed.
- **missing-tv-seasons (plex)**: Already complete — snapshot→season-check (`plex-snapshot`), season-check→notify (`tmdb-missing-seasons`). `src/jobs/plex/contracts.ts` existed and all stage jobs were wired. No changes needed.
- **tv-recommendations**: No contracts existed. Created `src/jobs/tv-recs/contracts.ts` with three factories and wired them into all stage jobs.

## tv-recs contracts added

- `tvSnapshotContract()` — key `tv-snapshot` — tv-snapshot → 8 branch jobs edge
- `tvBranchSuggestionsContract(branchId)` — key `tv-recs:<branchId>` — each branch → tv-rec-merge edge (8 gates total)
- `tvRecommendationsContract()` — key `tv-recs-recommendations` — tv-rec-merge → tv-recs-notify edge

All contracts are shape-only + empty-tolerant per the task spec. Branch contracts accept empty `suggestions: []` (a gracefully-failed branch still writes the file); the recommendations contract accepts an empty `recommendations: []` (nothing passed the quality bar). A missing file (crash before write) is still a true gate failure.

## Files changed

- `src/jobs/tv-recs/contracts.ts` — new file (three contract factories)
- `src/jobs/tv-recs/stages/tv-snapshot.job.ts` — added `produces: [tvSnapshotContract()]`
- `src/jobs/tv-recs/stages/recommend.ts` — added `consumes`/`produces` in `makeBranchJob`
- `src/jobs/tv-recs/stages/tv-rec-merge.job.ts` — added `consumes: BRANCHES.map(...)`, `produces: [tvRecommendationsContract()]`
- `src/jobs/tv-recs/stages/tv-recs-notify.job.ts` — added `consumes: [tvRecommendationsContract()]`

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- Reviewed that `deriveGates` in `dag.ts` will pick up 10 new gates for tv-recommendations:
  - 1 × tv-snapshot → each of 8 branches (8 gates)
  - 8 × branches → tv-rec-merge (8 gates, one per `tv-recs:<branchId>` key consumed)
  - 1 × tv-rec-merge → tv-recs-notify
  Total: 17 gates for tv-recommendations workflow.
