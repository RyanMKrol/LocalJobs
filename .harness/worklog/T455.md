# T455 — Group the Ignored panel + add 'Un-ignore all' across all 4 output managers

2026-07-07

## What I did
- `dashboard/app/lib/api.ts`: added the 4 client methods for the bulk unignore endpoints
  shipped in T454 — `unignoreMovieGapBulk`, `missingSeasonsUnignoreBulk`,
  `unignoreMovieRecBulk`, `unignoreTvRecBulk`.
- `dashboard/app/components/IgnoredSection.tsx`: added an optional `action` prop (a
  header-row slot) so a manager can render a flat "Un-ignore all" button next to the
  "Ignored (N)" heading without duplicating the panel chrome.
- `dashboard/app/workflows/[name]/page.tsx`:
  - `MovieGapsManager` — replaced the flat ignored-list table body with
    `groupByCollection(ignored)`, reusing the same `table-group-header` row pattern as
    the active list, with a per-group "↺ Un-ignore all" button (shown only when the
    group has >1 item, mirroring the existing bulk-ignore convention) calling
    `api.unignoreMovieGapBulk`.
  - `MissingSeasonsManager` — same treatment using `groupByShow(ignored)` and
    `api.missingSeasonsUnignoreBulk`.
  - `MovieRecsManager` / `TvRecsManager` — stayed flat (no natural grouping key), added
    one flat "↺ Un-ignore all" button in the `IgnoredSection` header (via the new
    `action` prop), visible whenever `ignored.length > 0`, calling
    `api.unignoreMovieRecBulk` / `api.unignoreTvRecBulk`.
  - All new bulk handlers confirm() with a count-bearing message ("Un-ignore all N
    ...? They will be treated as brand-new and may be recommended/reported/notified
    again."), and use a dedicated busy state + the existing `err` surface, matching the
    shape of the existing per-group bulk-ignore handlers.
- `dashboard/scripts/_dashboard-harness.mjs`: added a second ignored item to the
  `movieGaps` fixture's "An Ignored Collection" and to the `missingSeasons` fixture's
  "An Ignored Show" group, so the per-group "Un-ignore all" button (only rendered for
  groups with >1 item) is actually reachable in the existing baseline PAGES screenshots
  — no new FLOWS entry needed since the Ignored panel renders inline with no
  interaction required to reveal it.

## Checks run
- `npx tsc --noEmit` — clean.
- `cd dashboard && npx tsc --noEmit` — one pre-existing unrelated error in
  `scripts/nav-check.test.ts` (unused `@ts-expect-error`), confirmed present before my
  change too (out of scope).
- `npm test` — 241/241 tests pass.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — 40/41 captures pass; the one failure
  (`workflow-run-skipped`, a `.rf-dag-node` wait timeout) reproduces identically on a
  clean `git stash` of my changes, confirming it's pre-existing and unrelated.
  - Read `workflow-movie-recs.png`: MovieRecsManager shows a flat "↺ Un-ignore all"
    button beside "Ignored (1)"; MovieGapsManager's Ignored (2) panel groups
    "An Ignored Film"/"Another Ignored Film" under "An Ignored Collection" with a
    group-header "↺ Un-ignore all" button — all painted correctly, nothing clipped.
  - Read `workflow-missing-tv-seasons.png`: MissingSeasonsManager's Ignored (2) panel
    groups both ignored seasons of "An Ignored Show" under one header with a
    group-level "↺ Un-ignore all" button, individual rows keep their own "↺ Un-ignore".
  - Read `workflow-tv-recs.png`: TvRecsManager shows the flat "↺ Un-ignore all" button
    beside "Ignored (1)".
- `node dashboard/scripts/mobile-check.mjs` — all 19 pages pass, no overflow.

## Remaining
None — task complete.
