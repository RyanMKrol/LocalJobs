# T522 — 2026-07-13

## What I did
In `dashboard/app/integrations/page.tsx`, changed the `Bar` component's `cap == null`
branch from returning only `<span className="muted">no limit</span>` to also rendering
the `used` count: `<div className="mono">{used} <span className="muted">no limit</span></div>`.
No fraction/progress bar is shown (there's no denominator), consistent with the task spec.
The `cap != null` branch (used/cap + `.ubar` progress bar) is unchanged. Timeout column
untouched. No API/store change needed — `used` was already passed for every column.

## Checks run
- `npm --prefix dashboard run build` — green.
- `node dashboard/scripts/mobile-check.mjs` — all 32 pages pass (services page: 0px
  overflow, 0 boundary-crossing elements).
- `node dashboard/scripts/visual-check.mjs` — all pages captured with no errors.
- Read `dashboard/scripts/visual-out/services.png` directly and confirmed with my own
  eyes: `claude-cli` (rate/min, rate/day, rate/month all uncapped) now shows `12 no
  limit`, `42 no limit`, `123456 no limit` respectively instead of bare "no limit";
  `plex` and `legacy-service` (also fully uncapped) show the same pattern. Capped
  services (`fragrantica`, `gemini`, `google-places`) still render `used / cap` with
  the coloured `.ubar` progress bar, unchanged. Timeout column (rightmost numeric
  column before the Edit limits button) unaffected in all rows.

## What remains
Nothing — task complete. No harness-script changes were needed since 'services' was
already a PAGES entry with no-limit-service fixtures (claude-cli, legacy-service, plex).
