# T140 worklog — frontmatter rendering polish

## Attempt 1 (2026-06-24)

### Changes made

**`dashboard/app/globals.css`**
- Replaced `.md-fm` flex+column layout with `display: grid; grid-template-columns: max-content 1fr` so all values share a single aligned column
- Changed `.md-fm-row` to `display: contents` so `dt`/`dd` become direct grid participants
- Removed per-row flex properties; added `align-self: baseline` on key/val for proper text alignment
- Added mobile breakpoint rules: `.md-fm { grid-template-columns: 1fr }` + top margin on non-first rows to visually separate pairs when stacked

**`dashboard/app/workflow-runs/[id]/page.tsx`**
- Added `isFmEmpty(v)`: hides rows whose value is `""`, `"null"`, `"~"`, or a JSON empty array `[]`
- Added `renderFmValue(v)`: detects JSON array values (e.g. `["woody","spicy"]`) and renders as a comma-separated string; falls back to raw string
- `MarkdownModal` now filters with `isFmEmpty` before rendering and uses `renderFmValue` per cell

### Verification
- `npx tsc --noEmit`: clean
- `npm test`: 4/4 pass
- `npm --prefix dashboard run build`: succeeded
- `node dashboard/scripts/mobile-check.mjs`: all 9 pages pass (0px overflow, 0 boundary-crossing)
