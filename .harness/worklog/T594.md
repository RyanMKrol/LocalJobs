# T594 worklog

## Work done
Removed the `items.length > 1 &&` guards from two group-header action buttons in `dashboard/app/components/GroupedManager.tsx`:
1. Line 113–122: Active-side "Ignore all" button now renders unconditionally
2. Line 161–169: Ignored-side "Un-ignore all" button now renders unconditionally

## Verification

### Definition of Done checks
✓ `npx tsc --noEmit` — passed
✓ `npm test` — all 10 tests passed
✓ `npm --prefix dashboard run build` — build succeeded
✓ Visual check: `node dashboard/scripts/visual-check.mjs` — completed successfully

### Visual confirmation
Reviewed screenshots generated by visual-check:

**missing-tv-seasons workflow detail page:**
- Group headers now show "✕ Ignore all" buttons for ALL groups, including single-season shows
- Examples: "a-really-long-unbroken-...(2015)", "Fargo (2014)", "The Wire (2002)" all show the button
- Ignored section shows "↺ Un-ignore all" buttons on group headers like "An ignored Show (2010)"
- Buttons are properly positioned, visible, and not causing layout issues

**missing-movies workflow detail page:**
- Group headers now show "✕ Ignore all" buttons for ALL groups
- Examples: single-film collections and multi-film collections (e.g., "The Bourne Collection") both show the button
- Ignored section shows "↺ Un-ignore all" buttons on group headers
- Layout is clean, buttons are visible and well-positioned

No structural issues observed. The change is purely removing the conditional render — the underlying handlers and button behavior remain unchanged.
