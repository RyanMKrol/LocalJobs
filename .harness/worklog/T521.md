# T521 — 2026-07-13

## What I did
Added the certified 🏅 medal Pill next to each workflow card's name on the Overview/home
page (`dashboard/app/page.tsx`), reusing the exact markup already used on the Workflows
list page: `{p.certified ? <Pill kind="certified" title="Certified" style={{ marginLeft: 6 }}>🏅</Pill> : null}`.
Imported `Pill` from `./components/Pill`. No API/store/type changes needed — `certified`
was already on the workflows payload.

## Checks run
- `npx tsc --noEmit` — clean.
- `npm test` — all green.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/mobile-check.mjs` — all 32 pages pass (no overflow/boundary
  issues introduced).
- `node dashboard/scripts/visual-check.mjs` — 53/54 captures pass; the 1 failure
  (`workflow-run-skipped`, a `.rf-dag-node` selector timeout) is pre-existing and
  unrelated to this change (no DAG nodes rendered for that fixture's skipped-run page).

## Visual confirmation (observed)
Read `dashboard/scripts/visual-out/overview.png`: the 🏅 medal renders cleanly beside
the workflow name on every certified card (claude-warmer, places, movie-recommendations,
missing-movies, workouts-sync, legacy-job — all default `certified: 1` from the harness's
`workflow()` factory), positioned with a small left margin, not clipped or overlapping the
status pill below it. The `perfumes` card — explicitly set to `certified: 0` in the
harness fixtures — correctly shows no badge. The Workflows-list and detail-page rendering
were not touched.

## Remaining
Nothing outstanding for this task.
