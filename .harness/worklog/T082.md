# T082 — Workflow-run gates panel: keep 'detail →' on one line

## Attempt 1 — 2026-06-22

### What I did
Added `style={{ whiteSpace: 'nowrap' }}` to the `<a>` tag for the "detail →" link in the Validation gates panel (`dashboard/app/workflow-runs/[id]/page.tsx` line 41).

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — built successfully
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass

### Result
done
