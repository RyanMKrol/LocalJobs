# T492 worklog

## Done
- Changed `claude-warmer` workflow schedule from `'*/30 * * * *'` (every 30 minutes) to `'0 8,16 * * *'` (08:00 and 16:00 daily)
- Updated the workflow file's doc comment to describe the new twice-daily cadence and its rationale
- Updated `src/workflows/claude-warmer/CLAUDE.md` to reflect the new schedule and reasoning
- Updated the root `CLAUDE.md` workflow table entry for claude-warmer

## Verification
- `npx tsc --noEmit` passed (no TypeScript errors)
- `npm test` passed (all 85 test files, no failures; includes gate-coverage tests confirming claude-warmer's single-job DAG is valid)

## Notes
- The schedule is now code-owned in the manifest; existing dashboard overrides (if any, where `schedule_overridden=1`) would remain and need manual reset via the dashboard by the owner — out of scope for this code change per the task spec
