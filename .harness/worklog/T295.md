# T295 — claude-warm job: more verbose logging

## What changed
- `src/jobs/claude-warmer/claude-warm.job.ts`:
  - Extracted the prompt literal into `WARM_PROMPT = 'hi'` (alongside existing `WARM_MODEL`),
    used by both the log line and the `runClaude(...)` call so they can't drift.
  - Initial log line now reads:
    `Sending warm prompt to Claude (model: ${WARM_MODEL}): "${WARM_PROMPT}"`
  - Each of the three outcome log lines (rate-limited, failed, succeeded) now also includes
    `(model: ${WARM_MODEL})` so they're self-contained when read on the run's log view.
  - Quota-exceeded and unexpected-error catch branches also updated to include the model.
  - No change to retry count, timeout, or control flow — only log message text.
- `src/jobs/claude-warmer/claude-warm.test.ts` (test file — always in scope):
  - Mirrored the same log-message changes in the inline `runJobWith` helper (this test
    replicates the job's logic rather than importing it, per the existing pattern in this file).
  - Added assertions that the initial log includes both the model name and the literal
    prompt text (`"hi"`), and that each outcome log line includes the model name.

## Verification
- `npx tsc --noEmit` — pass.
- `npm test` — pass (72/72, including the 4 claude-warm tests with new assertions).
- No paid Claude CLI calls made — all runClaude interactions in the test are injected stubs.

## Result
done T295
