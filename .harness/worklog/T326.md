# T326 — remove "TMDB status" column from Missing Seasons tables

## What changed
`dashboard/app/workflows/[name]/page.tsx`, `MissingSeasonsManager`:
- Active-items table: removed `<th>TMDB status</th>` header and the
  `<td><Pill kind="info">{meta.tmdbStatus}</Pill></td>` cell from each season row.
  Updated the group-header row's `colSpan={3}` → `colSpan={2}` to match the now
  2-column table (Season, action) — the redundant-Ignore-all-on-single-item-groups
  follow-up hadn't landed yet, so the group-header row still spans a fixed 2 cols.
- Ignored-items table: removed the `<th>TMDB status</th>` header and the
  `<td>{s.tmdbStatus}</td>` cell for consistency with the active table.
- `Pill` was rendered only via `meta.tmdbStatus` in this file; after removal it's
  unused, so the `import { Pill } from '../../components/Pill'` line was removed too.
- Did NOT touch `MovieGapsManager`'s `<th>TMDB</th>` (numeric rating column,
  explicitly "context only") — confirmed unchanged, still renders 6.1/7.2/7.6 etc.
- Did NOT touch the `tmdbStatus` field in the `MissingSeason` type or `lib/api.ts` —
  display-only change, data contract intact.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 91/91 pass.
- `npm --prefix dashboard run build` — succeeds, no unused-import/lint errors.
- `node dashboard/scripts/visual-check.mjs` — all 23 captures pass; opened
  `workflow-missing-tv-seasons.png` — both the active table (Season, action) and
  the Ignored table (Show, Season) render with no TMDB status column and correct
  alignment (no blank leftover column, group-header row spans correctly). Opened
  `workflow-movie-recs.png` — Franchise Gaps table's TMDB rating column
  (Film/Year/TMDB/action) is unchanged from before.
- `node dashboard/scripts/mobile-check.mjs` — all 15 pages pass, 0px overflow.
- No `_dashboard-harness.mjs` changes needed — no page/workflow/gate added or
  removed, no new interactive state introduced (existing PAGES entry covers the
  missing-tv-seasons detail page).
