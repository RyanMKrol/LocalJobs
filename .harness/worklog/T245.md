# T245 worklog — Workflows list: centre the Last-run column contents

## Changes

- `dashboard/app/globals.css`: added `align-items: center` to `.last-run-cell` so the
  pill and relative-time text centre themselves within their flex column.
- `dashboard/app/workflows/page.tsx`: added `style={{ textAlign: 'center' }}` to the
  `<th>Last run</th>` header and its corresponding `<td>`.

## Visual confirmation

Ran `npm --prefix dashboard run build && node dashboard/scripts/visual-check.mjs`.
Reviewed `dashboard/scripts/visual-out/workflows.png` — the "LAST RUN" column header,
"Succeeded" pill, and "7d ago" relative-time text are all horizontally centred and
aligned with each other. Other columns (Workflow, Stages, Schedule, Next) are unchanged.

## Checks

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — clean
- `node dashboard/scripts/visual-check.mjs` — all pages pass
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass (0px overflow)
