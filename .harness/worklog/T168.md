# T168 worklog — Rename movies workflow to movie-recommendations

## What was done

1. **Manifest** (`src/jobs/movies/movies.workflow.ts`): changed `name: 'movies'` → `name: 'movie-recommendations'`.

2. **DB migration** (`src/db/index.ts`): added `migrateRenameMoviesWorkflow()` — idempotent, FK-safe transaction that renames `workflows`, `workflow_jobs`, and `workflow_runs` rows from `'movies'` → `'movie-recommendations'`. Follows the exact same pattern as `migrateRenamePlexWorkflow` (T151).

3. **Test** (`src/db/migrate-existing-db.test.ts`): seeded a `movies` workflow + job + run in the legacy fixture, added assertions that the migration renames it to `movie-recommendations` with run history preserved. Updated row-count assertions to reflect the three additional seeded rows.

4. **Dashboard** (`dashboard/app/workflows/[name]/page.tsx`): updated `name === 'movies'` → `name === 'movie-recommendations'` for the `MovieGapsManager` gate.

5. **README.md** and **CLAUDE.md**: updated all references from `movies` (as a workflow name/identifier) to `movie-recommendations`. Folder name `src/jobs/movies/` stays unchanged.

## Empirical checks

- `npx tsc --noEmit` — clean
- `npm test` — all tests pass (including `migrate-existing-db.test.ts` with the new T168 assertion)
- `npm --prefix dashboard run build` — clean build
