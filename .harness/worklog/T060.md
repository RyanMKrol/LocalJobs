# T060 — Backlog view: add a Done section

## Attempt 1 — 2026-06-22

### What was done

Updated `dashboard/app/backlog/page.tsx`:

- Added a third **Done** section below "Needs a human" showing all `status: 'done'` tasks sorted by id.
- Filtered `buildable` and `human` sections to exclude done tasks (previously they included done tasks because the filter only checked `gate`).
- Done section is collapsed by default using a native `<details>`/`<summary>` element — avoids JS state and is accessible.
- Each done task renders as a compact one-line `DoneRow` component (id + title + done pill) rather than the full `TaskCard`, keeping the list scannable even at 40+ entries.
- Updated the subtitle stats line to include the done count.

### Verification

- `npx tsc --noEmit` — clean
- `npm test` — all passed (8 jobs, 4 services, 2 workflows)
- `npm --prefix dashboard run build` — clean, /backlog route 3.09 kB

### Outcome

done
