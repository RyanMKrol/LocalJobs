# T278 worklog — duplicate-idea detection in /convert-ideas

## What was done

Extended `.claude/commands/convert-ideas.md` Step 0 with an explicit de-dup pass:
- Before converting anything, scan the full worklist for semantically overlapping bullets.
- Surface suspected duplicate groups to the owner via `AskUserQuestion` — never auto-merge.
- Clearly distinguished from the related-ideas → `dependsOn` path: only genuine duplicates
  (same underlying change appearing twice) are merged/dropped; related-but-distinct ideas are not.

Added a one-line (three-line) note to `.harness/CLAUDE.md`'s "Ideas inbox & the two-step flow"
section (under the de-dup bullet before Phase 2) summarising the same distinction.

## Verification

No code change — command-doc and convention only. `/idea` (capture-side) is unchanged.
`npx tsc --noEmit` and `npm test` are not affected by doc-only changes; no dashboard change.

Done-when criteria:
- [x] `convert-ideas.md` Step 0 includes a de-dup pass with semantic similarity, AskUserQuestion,
      and an explicit note that related-but-distinct ideas are NOT duplicates (they get dependsOn).
- [x] `.harness/CLAUDE.md` two-step-flow section notes that convert starts with a de-dup pass.
- [x] No code change; `/idea` untouched.
