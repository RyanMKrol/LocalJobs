# T520 — Merge /admin-cache into /admin

2026-07-13

## What I did
- Moved the service-cache UI from `dashboard/app/admin-cache/page.tsx` into
  `dashboard/app/admin/page.tsx` as two new sections:
  - "Cached responses" — per-service `service_cache` row-count table (with a
    Total row), loaded via `api.serviceCacheCounts()` on mount.
  - A second, separate danger-zone panel: "Clear all cached responses", wired
    to `api.clearServiceCache()` with the same `window.confirm` guard and
    success/error messaging as before.
- Kept the two destructive actions functionally distinct: "Delete all
  workflow output" still calls `api.resetAllWorkflowsOutput()`, "Clear all
  cached responses" still calls `POST /api/cache/clear` — no shared logic,
  no change to either endpoint or its semantics.
- Deleted `dashboard/app/admin-cache/` entirely (the whole route folder).
- Removed the now-dead cross-link from the old cache page pointing back at
  `/admin` (it no longer exists, along with the rest of that page).
- Updated `dashboard/scripts/_dashboard-harness.mjs`: removed the
  `{ name: 'admin-cache', path: '/admin-cache' }` PAGES entry. The existing
  `/api/cache` and `/api/cache/clear` fixtures needed no changes — they were
  already generic and now serve `/admin` directly.
- Updated the root `CLAUDE.md` file-map entry for `dashboard/app/*` to
  describe the single `/admin` page hosting both actions, replacing the old
  "deliberately separate from /admin" wording.
- No nav change needed — `layout.tsx` never linked to `/admin-cache`.

## Checks run
- `npx tsc --noEmit` — clean.
- `npm test` — all green.
- `npm --prefix dashboard run build` — succeeds; route table confirms
  `/admin-cache` is gone and `/admin` builds.
- `node dashboard/scripts/mobile-check.mjs` — all 32 pages pass (no
  `admin-cache` entry anymore), `/admin` passes with 0px overflow.
- `node dashboard/scripts/visual-check.mjs` — 53/54 captures pass. The one
  failure (`workflow-run-skipped`, a `page.waitForSelector` timeout) is
  pre-existing on `main` before this change (confirmed via `git stash` +
  re-run) and unrelated to this task.
- Read `dashboard/scripts/visual-out/admin.png` directly: confirmed both new
  sections render correctly — "Cached responses" shows the per-service table
  (gemini: 12, google-places: 4, Total: 16) between "Run everything" and
  "Danger zone"; "Danger zone" now shows two stacked red-bordered panels,
  "Delete all workflow output" and "Clear all cached responses", each with
  its own button and description. Nothing blank, overlapping, or clipped.

## Remaining
Nothing — task complete.
