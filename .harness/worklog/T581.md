# T581 — Delete dead DB browser and canned queries code

## What I did

1. **Examined the task spec** — a re-attempt of T554 with corrected file path (R03/T529 moved the dead code into `src/db/store/admin.ts`, which was not in T554's scope).

2. **Verified no external references:**
   - Grep confirmed zero references to `listDbTables`, `isKnownTable`, `browseTable`, `CANNED_QUERIES`, `listCannedQueries`, `runCannedQuery`, `TablePage`, or `CannedQuery*` outside admin.ts and store.test.ts.
   - Confirmed no `/api/db` route in src/api/server.ts (already gone since T230).
   - The barrel store.ts re-export (`export * from './store/admin.js'`) re-exports everything, so deleting the symbols removes them from the public API.

3. **Identified imports used by the dead code:**
   - `db` is still used by `listStaleOverrides`, `deleteWorkflowCompletely`, `deleteJobCompletely`, `deleteServiceCompletely`
   - `listJobs` is still used by stale-override audit
   - `getWorkflowJobs` and `listWorkflows` are still used by stale-override audit
   - `listServices` is still used by stale-override audit
   
   All four imports are safe to keep.

4. **Deleted from src/db/store/admin.ts:**
   - Lines 264–412 (the entire "Read-only DB browser" and "Canned queries" sections)
   - Functions deleted: `listDbTables`, `isKnownTable`, `browseTable`, `listCannedQueries`, `runCannedQuery`
   - Types deleted: `TablePage`, `CannedQueryDef`, `CannedQueryMeta`, `CannedQueryResult`
   - Constants deleted: `CANNED_QUERIES`

5. **Deleted from src/db/store.test.ts:**
   - Line 6: Removed imports `browseTable, listDbTables, listCannedQueries, runCannedQuery`
   - Lines 836–865: Removed the entire "read-only DB browser" test block (console log included)
   - Lines 867–935: Removed the entire "canned (predefined) read-only queries" test block (console log included)

6. **Ran verification checks:**
   - `npx tsc --noEmit` — passed (no type errors)
   - `npm test` — passed (all tests green, no "TS1116" unused import warnings)

## Checks performed

✅ TypeScript: `npx tsc --noEmit` (clean)
✅ Tests: `npm test` (all green)
✅ Grep sweep: zero references to deleted symbols (outside the deleted code)
✅ Confirmed: no `/api/db` route in `src/api/server.ts`
✅ Confirmed: barrel store.ts still healthy (re-exports all remaining admin.ts symbols)

## Result

`done T581` — deleted 148 lines of dead code + tests, committed to `main`.
