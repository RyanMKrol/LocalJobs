# T392: bump Plex request timeout default 30s -> 5min

## Changes
- `src/jobs/plex/config.ts:34` — `requestTimeoutMs` fallback default changed from `30_000` to `300_000`.
- `.env.example` — `PLEX_REQUEST_TIMEOUT_MS` comment/example updated to `300000` (5 minutes).
- `PROBE_TIMEOUT_MS` in `src/jobs/plex/client.ts` left untouched (still `1_500` fallback), per spec.

## Verification
- `npx tsc --noEmit` — pass.
- `npm test` — 168/168 tests pass.
- No doc mentions of `PLEX_REQUEST_TIMEOUT_MS`/`30000`/`30_000` exist outside `config.ts`/`client.ts`/`.env.example`, so no further doc updates needed (confirmed via grep before editing).

## Restart
Daemon restart command to run after this commit lands:
`launchctl kickstart -k gui/$(id -u)/com.ryankrol.localjobs`
