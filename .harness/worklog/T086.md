# T086 worklog

## Task
Make the 'Last run' status pill on the Workflows list page a clickable link to that workflow run.

## Attempt 1 — 2026-06-22

### Change made
`dashboard/app/workflows/page.tsx` line 39: replaced the `<span className={`badge ${...}`}>` with `<a href={`/workflow-runs/${p.last_run.id}`} className={`badge ${...}`}>`. The `WorkflowRun` type in `api.ts` already exposes `id: string`, so no API or type changes were needed. The relative-time text and never/no-run case are unchanged.

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — 19 files, all passed
- `npm --prefix dashboard run build` — succeeded, `/workflows` page 2.53 kB
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages PASS (0px overflow)

### Result
done
