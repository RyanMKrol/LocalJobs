# T478 — 2026-07-10

## What I did
- `src/db/store.ts`: added `serviceCacheCounts()` (per-service row counts, grouped/ordered)
  and `clearServiceCache(serviceName?)` (deletes all rows, or just one service's rows;
  returns the count deleted). Deliberately kept separate from `resetWorkflowOutput`
  (T203) — that function is unchanged and still never touches `service_cache`.
- `src/api/server.ts`: added `GET /api/cache` (read-only counts) and
  `POST /api/cache/clear` (accepts optional `{ serviceName }`, returns
  `{ ok: true, cleared: N }`). Both live inline before the `workflow-runs` routes;
  the clear endpoint sits behind the same global loopback/token mutation guard as
  every other POST endpoint (no new guard code needed — it's applied generically
  to all POSTs earlier in the request handler).
- `dashboard/app/lib/api.ts`: added `api.serviceCacheCounts()` and
  `api.clearServiceCache(serviceName?)` client helpers.
- `dashboard/app/admin-cache/page.tsx` (new): shows a per-service cached-row-count
  table + total, and a "Clear all cached responses" danger-zone button gated by a
  single `window.confirm()` (lighter friction than the `/admin` page's typed
  confirmation phrase, per the spec's note about this session's admin-page
  confirmation-friction ideas). Explicitly documents in its own copy that it is
  separate from `/admin`'s "Delete all workflow output" action.
- `dashboard/scripts/_dashboard-harness.mjs`: added an `/api/cache` + `/api/cache/clear`
  fixture, and added `admin-cache` to the `PAGES` list (living-artifact rule).
- Root `CLAUDE.md`: noted the new `/admin-cache` route + its purpose in the
  `dashboard/app/*` file-map row.
- Added store tests (`src/db/store.test.ts`, T478 block) for
  `serviceCacheCounts`/`clearServiceCache` (per-service scoping + clear-all), and
  API tests (`src/api/server.test.ts`, T478 block) for `GET /api/cache` and
  `POST /api/cache/clear` (scoped clear + clear-all).

## Verified unchanged
Read `resetWorkflowOutput` (src/db/store.ts) and its `/api/workflows/:name/reset-output`
+ `/api/workflows/reset-output-all` endpoints — none of them reference `service_cache`;
confirmed the existing T203 tests in `server.test.ts` still pass unmodified, so the
danger-zone "Delete all workflow output" action on `/admin` is untouched by this change.

## Checks run
- `npx tsc --noEmit` — clean.
- `npm test` — 246/246 tests pass (includes the new T478 store + API test blocks).
- `npm --prefix dashboard run build` — succeeds; `/admin-cache` appears in the route
  table as a new static route.
- `node dashboard/scripts/mobile-check.mjs` — 31/31 pages pass, including `admin-cache`
  (0px overflow, 0 boundary-crossing elements).
- `node dashboard/scripts/visual-check.mjs` — read `visual-out/admin-cache.png`: the
  page renders correctly — nav bar intact, "Service cache" heading, the cached-responses
  table (gemini=12, google-places=4, Total=16 from the synthetic fixture) fully visible,
  and the red-bordered danger-zone panel with the "Clear all cached responses" button
  painted and legible. Nothing blank/overlapping/clipped.
  One unrelated pre-existing failure in the same run: `workflow-run-skipped`
  (`/workflow-runs/skipped`) times out waiting for `.rf-dag-node` — reproduced this
  same failure by `git stash`-ing ALL of this task's changes and rebuilding from a
  clean tree, confirming it predates this change and isn't caused by it (that route/
  component isn't in this task's scope and wasn't touched).

## Remaining / known gaps
- The spec suggested linking the new page "from the main admin page or the nav" —
  both `dashboard/app/admin/page.tsx` and `dashboard/app/layout.tsx` are OUTSIDE this
  task's scope list, so no link was added from either; `/admin-cache` is reachable by
  direct URL only. Flagging this rather than touching an out-of-scope file.

done T478
