# T488 — Add mechanical inputKeysService enforcement test

## Overview

Created `src/core/root-inputkeys-service.test.ts` to enforce that every job declaring `inputKeys()` also declares a valid `inputKeysService` field pointing to a real, registered service.

## Work done

1. **Analyzed the existing pattern**: Reviewed `src/core/gate-coverage.test.ts` to understand the registry walk pattern and failure semantics.

2. **Created the test file** (`src/core/root-inputkeys-service.test.ts`):
   - Loads the registry (jobs, services) exactly like `gate-coverage.test.ts`
   - Walks every job in the registry
   - For any job with a truthy `inputKeys()` field:
     - Asserts `inputKeysService` is a non-empty string (not missing or falsy)
     - Asserts that string matches the `name` of a real `ServiceDefinition` from the loaded services
   - Fails the whole suite with a clear, job-naming error message if either check fails

3. **Verified the test has teeth**:
   - Ran initial suite: test passed cleanly (all 5 inputKeys-declaring jobs already comply from T487)
   - Temporarily removed `inputKeysService: 'fs'` from `cid-to-place-id-resolver` to verify the test would catch the violation
   - Confirmed the test failed with a clear error message naming the specific job
   - Reverted the temporary change
   - Re-ran full suite: all tests pass cleanly

## Checks run

- `npm test`: All 105 tests pass, including the new root-inputkeys-service test ✓
- `npx tsc --noEmit`: No type errors ✓

## Remaining

Nothing — T488 is complete. The test is production-ready and now guards against future violations of the `inputKeysService` convention.
