# T498 — 2026-07-10

Built the owner-facing UI for the `certified` flag (dependency: T497's `/certify` endpoint +
`api.updateWorkflowCertified` client helper, already landed).

## What I did
- `dashboard/app/workflows/[name]/page.tsx`: added `toggleCertified()` (mirrors `toggleNotify()`),
  a new click-to-toggle "Certified" row in the `.kv` details panel (same affordance as
  Enabled/Notifications), and a `<Pill kind="certified">🏅 Certified</Pill>` badge next to the
  `<h1>{name}</h1>` in the header row, rendered only when `p?.certified` is truthy.
- `dashboard/app/workflows/page.tsx`: added the same `<Pill kind="certified">🏅</Pill>` (icon-only,
  compact) next to the workflow name link in the list table.
- `dashboard/app/globals.css`: added `.pill.certified` using the existing `--green` custom
  property, matching `.pill.on`/`.pill.done`'s styling exactly.
- `dashboard/scripts/_dashboard-harness.mjs`: the shared `workflow()` fixture factory now defaults
  `certified: 1` (so `places` — the canonical example workflow used across most PAGES entries —
  exercises the certified/badge-present state everywhere), and the `perfumes` fixture explicitly
  overrides `certified: 0` so the badge-absent state is also exercised on both the Workflows list
  and (indirectly, since it shares the same component) the detail page. No new FLOWS entry was
  needed — the badge and the toggle row are both visible in the static baseline screenshot; there's
  no distinct "toggled" state worth its own capture (matches the existing Notifications row's
  granularity, which also has no dedicated FLOWS entry).

## Checks run
- `npx tsc --noEmit` — clean.
- `npm test` — all green (ran in background, exit 0).
- `npm --prefix dashboard run build` — clean, no new route/type errors.
- `node dashboard/scripts/mobile-check.mjs` — all 31 pages pass, 0 overflow.
- `node dashboard/scripts/visual-check.mjs` — 52/53 captures pass; the one failure
  (`workflow-run-skipped`, a DagFlow-node wait timeout) reproduces identically on `main` with my
  changes stashed out, confirming it's a pre-existing flake unrelated to this task.
- **Visually inspected** `visual-out/workflow.png` (`/workflows/places`): the 🏅 Certified pill
  renders cleanly next to the `<h1>`, not clipped/overlapping; the new "Certified" row in the
  details panel shows the checked checkbox + "certified ✓ (click to un-certify)" label, consistent
  with the Enabled/Notifications rows above it.
- **Visually inspected** `visual-out/workflows.png` (`/workflows`): `places` (and every other
  fixture workflow using the factory default) shows the small 🏅 badge inline with its name;
  `perfumes` — the deliberately non-certified fixture — shows no badge, confirming both states
  render distinctly and correctly in the same table.

No functional side effects were added — certifying only flips the stored boolean via the existing
T497 endpoint; no change to scheduling, notifications, or run behavior.
