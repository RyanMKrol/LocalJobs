# T513 — Wrap missing-tv-seasons Plex reads in callService

## Summary
Wrapped both `plexGet` calls in `src/workflows/missing-tv-seasons/stages/snapshot.ts` with `callService('plex', ...)` to meter them via the shared plex service, mirroring the existing `callService('tmdb', ...)` pattern in the sibling season-check stage.

## Work Done
1. **snapshot.ts**: Added import of `callService` from `../../../core/services.js`
2. **snapshot.ts**: Wrapped the shows listing `plexGet` call with `callService('plex', () => plexGet(...))`
3. **snapshot.ts**: Wrapped the flat episode listing `plexGet` call with `callService('plex', () => plexGet(...))`
4. **snapshot.test.ts**: Updated imports to add `registerService` from `../../../core/services.js` and `syncService` from `../../../db/store.js`
5. **snapshot.test.ts**: Added test setup to register the 'plex' service before running ledger tests
6. **snapshot.test.ts**: Updated comment to reference the callService wrapper
7. **CLAUDE.md**: Added one-line note documenting that Plex reads are metered via the shared plex service

## Verification
- ✓ TypeScript compilation passes (`npx tsc --noEmit`)
- ✓ `snapshot.test.ts` passes all tests (3/3 passing)
- ✓ No bare `plexGet(` calls remain in snapshot.ts (verified via grep)
- ✓ Only 3 required files staged by explicit name (no `data/`, `.env*`, or directory-wide staging)
- ✓ Arguments, return-value handling, and error handling unchanged — only wrapping
- ✓ Mirrors the sibling `season-check.ts` `callService('tmdb', ...)` pattern exactly

## Done
All acceptance criteria met:
- Both plexGet reads wrapped in callService('plex', ...)
- snapshot.test.ts passes
- CLAUDE.md updated with one-line plex service note
- Only 3 scoped files staged by name
