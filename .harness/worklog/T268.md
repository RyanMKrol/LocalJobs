# T268 — Shared dashboard UI component library

## Inventory

Scanned all 10 dashboard page.tsx files + ui.tsx + existing components/:

**Run button** — 3 separate implementations:
- `app/page.tsx:194` — `btn secondary`, no running-state awareness
- `app/workflows/page.tsx:89-90` — `btn btn-run`, running disabled/title pattern
- `app/workflows/[name]/page.tsx:789-790` — `btn btn-run`, running + busy state

**Pill chips** — 10+ bare `<span className="pill ...">` usages across:
- `app/workflows/page.tsx:72` — on/off enabled state
- `app/backlog/page.tsx:112,125,126,146,150,152,210` — dep-waiting, buildable, human, reviewed/unreviewed, failed, done, tag pills
- `app/services/page.tsx:153,154` — paid/free, edited

**Modal chrome (db-modal*)** — 4 places independently repeat the overlay/header/body/close pattern:
- `app/ui.tsx` (StuckPopover + ThemeControls)
- `app/workflow-runs/[id]/page.tsx`
- `app/components/WorkflowOutputSection.tsx`
- `app/services/page.tsx`

**Existing shared layer:** `dashboard/app/ui.tsx` (StatusBadge, StuckPopover, ThemeControls, CopyLogsButton, etc.), `dashboard/app/components/DagFlow.tsx`, `WorkflowOutputSection.tsx`.

## What became components

Created:
- `dashboard/app/components/RunButton.tsx` — `<RunButton>` wrapping `.btn.btn-run`, consolidates the isRunning/busy/title/label logic
- `dashboard/app/components/Pill.tsx` — `<Pill>` wrapping `.pill`, `kind` prop maps to modifier class

NOT extracted in this task (per spec — migration is T269–T277):
- Modal chrome: would need a `<Modal>` component; deferred
- Individual page migrations: deferred to T269–T277

## Convention

Documented in CLAUDE.md (dashboard conventions section): new dashboard UI must use `<RunButton>` and `<Pill>` rather than bare spans/buttons; new shared elements go in `dashboard/app/components/`.

## Checks

- `npx tsc --noEmit` — PASS (no output)
- `npm test` — PASS (74 tests, 0 failures)
- `npm --prefix dashboard run build` — PASS (clean, all 11 routes)
- `node dashboard/scripts/visual-check.mjs` — PASS (16/16 pages captured)

Visual confirmation: all 16 screenshots reviewed. No pages changed appearance — this task only adds new component files that aren't yet used by any page, so the rendered output is identical to before. The harness PAGES/FLOWS/fixtures needed no update (no page surface changed).
