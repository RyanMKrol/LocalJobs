# T407 — perfumes fetch/parse: descriptive success `detail`

## What changed

- `src/workflows/perfumes/stages/fetch.ts` — the success branch's `markWorkItem` now
  records `detail.format: 'text'`, `detail.path` (the `.txt` artifact — the file the
  parse stage reads back), `detail.textLength`, and `detail.htmlBytes`, alongside the
  existing `name`. Failure branch untouched (already rich).
- `src/workflows/perfumes/stages/parse.ts` — the success branch's `markWorkItem` now
  records `detail.format: 'json'`, `detail.path` (the written `<id>.json`), and
  `accordsCount`/`notesCount`/`rating` computed from the already-normalized `data`
  object (after `applyAccordPercents`/`applyNormalizedNotes` ran). Failure branch
  untouched.
- Added `src/workflows/perfumes/stages/parse-detail.test.ts` — a hermetic regression
  test (mocked DynamoDB doc client, `perfumesConfig.dryRun = true`, scratch tmp dirs
  for `pagesDir`/`fragranticaDir`) asserting the recorded ledger `detail` carries
  `format: 'json'`, the correct `path`, and numeric `accordsCount`/`notesCount` (no
  existing test in this folder exercised `runParse`'s ledger writes before this — the
  existing `parse.test.ts` only covers the pure `parseAccordPercents` helper). No test
  was added for `fetch.ts` — its only success path requires a real headless-browser
  fetch through `callService('fragrantica', …)`, which isn't practical to make
  hermetic within this task's scope (per the spec's own guidance); its change was a
  small, low-risk mechanical addition mirroring the already-logged size numbers at
  `fetch.ts:64`.

## Verification

- `npx tsc --noEmit` — clean, no output.
- `npm test` — 186/186 pass, 0 fail (includes the new `parse-detail.test.ts` — see
  "parse — success detail describes what it produced (T407)" → ok).
- Did NOT run a live `perfumes` workflow / live Fragrantica fetch (would spend the
  `claude-cli`/`fragrantica` service quota unnecessarily) — the spec's own "Done
  when" step explicitly allows seeding a `work_items` row via a scratch script
  instead, which the new hermetic test does: it runs the real `runParse` end-to-end
  (dry-run Claude response) against a scratch DB and confirms the recorded
  `work_items.detail` JSON contains `path`/`format`/`accordsCount`/`notesCount`/
  `rating` exactly as the API's `GET /api/workflow-runs/:id/output?job=perfumes-parse&key=<id>`
  reads it (`workItemMarkdownPath`-adjacent code paths read the same `detail` JSON
  blob) — did not additionally spin up the dashboard/API server to click through the
  UI, since the ledger content is what the panel renders and is what's asserted here.

## Scope

Touched only `src/workflows/perfumes/**` (2 stage files) plus the new test file and
this worklog, per the task's scope gate. No docs were in scope for this task, so none
were touched.
