# T403 — collapse stocks-snapshot ledger to one row per day

## What changed

- `src/jobs/stocks-sync/stages/stocks-snapshot.ts`: added a `dayKey(date)` helper (UTC
  `YYYY-MM-DD`). Replaced the per-position `markWorkItem` loop with a single
  `markWorkItem(JOB_NAME, dayKey(now), 'success', { detail: { name, positionCount, totalValue,
  resolvedCount, markdown } })` call at the end of `runStocksSnapshot`. `runStocksSnapshot` now
  accepts an injectable `now?: Date` option (defaults to `new Date()`) so the day key is
  deterministic in tests. Per-position `ctx.log` narration is kept (repo's "verbose by default"
  convention) — only the ledger write collapsed.
- Removed `stocksSnapshotInputKeys()` from `stocks-snapshot.ts` and the `inputKeys` property from
  `stocks-snapshot.job.ts` (plus its now-unused import) — a single daily combined snapshot isn't
  meaningfully limitable per-ticker, matching the `stock-digest`/`plex-space-saver` idiom (neither
  declares `inputKeys()`). `positionKey` import dropped from `stocks-snapshot.ts` (still used by
  `stocks-watch.ts` and inside `stocks-snapshot.ts`'s own log lines — wait, no, log lines don't use
  it either now; only `stocks-watch.ts` still needs it).
- Read `stocks-watch.ts` end-to-end before touching anything: confirmed it reads
  `portfolio.json` directly off disk and tracks its own separate `stocks-watch` ledger keyed by
  `positionKey`/`${key}::notified` — untouched, not part of this task's scope, and the diff touches
  no file under that stage.
- `stocks-snapshot.test.ts`: added a `dayKey` describe block; rewrote every ledger assertion to key
  off `dayKey(now)` instead of `positionKey(...)`, added same-day-overwrite and different-day tests,
  and a `positionCount`/`totalValue`/`resolvedCount` detail assertion. Kept all pure-helper
  (`normalizePosition`/`priceDiff`/`buildPortfolioMarkdown`) and ISIN/OpenFIGI resolution coverage,
  adjusting only the final ledger assertions to the new collapsed shape.
- Root `CLAUDE.md`'s `stocks-sync` write-up updated to describe the new per-day collapsed ledger
  shape and loss of limitability, noting `positionKey` survives only on `stocks-watch`'s own ledger.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 180/180 passing (46 suites), including the rewritten `stocks-snapshot.test.ts`.
- No live Trading212/paid calls made — all tests use injected stub fetchers/writers + the scratch DB.
- `stocks-watch.ts`/`stocks-notify` files are unmodified (confirmed by reading them and by the diff).
- `data/out/portfolio.json`/`portfolio.md` writing logic (`writePortfolio`) is unchanged — only
  ledger bookkeeping changed.
- Restarted the daemon (`launchctl kickstart -k gui/$(id -u)/com.ryankrol.localjobs`) so the change
  is live.

## Result

done T403
