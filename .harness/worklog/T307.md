# T307 — fix workflows list category-panel spacing

## Change

`dashboard/app/workflows/page.tsx`: computed the non-empty `CATEGORY_GROUPS` list first
(map → filter), then rendered with an index, applying `style={{ marginTop: 28 }}` on each
panel after the first (mirroring the `marginTop: 28` precedent in `backlog/page.tsx`). No new
CSS rule/class added.

## Visual confirmation

- `npm --prefix dashboard run build && node dashboard/scripts/visual-check.mjs` — all 19 pages
  captured, no errors.
- Inspected `dashboard/scripts/visual-out/workflows.png`: consecutive category panels ("Second
  brain", "Recommendations", "Regular maintenance", "Uncategorized") now have a clear, even gap
  between them; the first panel ("Second brain") sits directly under the page header with no
  extra top margin.
- Checked the `<h2>` category heading spacing against the panel's top border in the same
  screenshot — it already has comfortable padding above/below the heading text, so per the task
  spec I left heading spacing untouched (no speculative CSS change).
- No new `PAGES`/`FLOWS` entry needed — the existing `workflows` fixture already exercises
  multiple categories (T293).

## Other checks

- `node dashboard/scripts/mobile-check.mjs` — all 13 pages pass, 0px horizontal overflow on
  `/workflows` (and every other page).
- `npx tsc --noEmit` — clean.
- `npm test` — 85/85 pass.
- `npm --prefix dashboard run build` — succeeds.

## Result

done
