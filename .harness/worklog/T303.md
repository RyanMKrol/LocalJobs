# T303: prevent "used / cap" wrap on the Services page

## Change
- `dashboard/app/services/page.tsx`: added `whiteSpace: 'nowrap'` to the `Bar` component's
  `used / cap` `<div>` inline style (matches the file's existing inline-style convention —
  the rest of `Bar` and this page already style via inline `style={}`, not CSS classes).
- `dashboard/scripts/_dashboard-harness.mjs`: bumped the `service()` fixture's `used_month`
  from `1234` to `123456` so the Rate/month column's fixture actually exercises a long
  "used / cap" pairing (`123456 / 3000`) wide enough to have wrapped pre-fix.

## Verification
- `npm --prefix dashboard run build` — passed.
- `node dashboard/scripts/visual-check.mjs` — all 19 pages captured; opened
  `dashboard/scripts/visual-out/services.png` and confirmed every row's Rate/min, Rate/day,
  and Rate/month "used / cap" text renders on a single line, including the long
  `123456 / 3000` value for gemini/google-places.
- `node dashboard/scripts/mobile-check.mjs` — all 13 pages pass, 0px horizontal overflow,
  0 boundary-crossing elements (no new overflow introduced).
- `npx tsc --noEmit` — clean.
- `npm test` — 85/85 passed.

## Result
done T303
