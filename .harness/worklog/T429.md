# T429 — paragraph-length job descriptions on /jobs/[name] and /runs/[id]

## What changed

- `dashboard/app/runs/[id]/page.tsx`: added a second, slow (30s) poll for the run's job record
  (`api.job(run.job_name)`) and render `jobData?.job?.description` as a `.sub` paragraph directly
  under the job-name/status-badge row (guarded so nothing renders when a job has no description).
- Root `CLAUDE.md`'s "How to add a job" section: updated the `JobDefinition` code example's
  `description` field to document the paragraph-length convention and name both render surfaces
  (`/jobs/[name]`, `/runs/[id]`).
- Rewrote all 4 perfumes job descriptions (`perfumes-find-url`, `perfumes-fetch`, `perfumes-parse`,
  `perfumes-build`) as real, accurate 3-6 sentence paragraphs, grounded in
  `src/workflows/perfumes/CLAUDE.md` and each stage's own `run()`/companion `.ts` implementation —
  no invented behavior. Plain prose, no markdown syntax.
- No changes needed to `src/core/types.ts` (`description?: string` was already correct) or to
  `dashboard/scripts/_dashboard-harness.mjs` (the existing `job()` fixture already returns a
  non-trivial description string and the synthetic `/api/jobs/:name` router already serves it, so
  the `run`/`job` fixture pages already exercised the new render path with no fixture change).

## Verification performed

- `npx tsc --noEmit` — clean.
- `npm test` — full suite green (background run, exit code 0).
- `npm --prefix dashboard run build` — succeeds, all 13 routes build.
- `node dashboard/scripts/mobile-check.mjs` — all 20 pages pass, including `job` (`/jobs/places-enrich`)
  and `run` (`/runs/1`).
- `node dashboard/scripts/visual-check.mjs` — 44/45 captures pass. The one failure
  (`workflow-run-skipped` timing out waiting for `.rf-dag-node`) is PRE-EXISTING and unrelated to
  this change — confirmed by `git stash`-ing my diff and re-running visual-check against the
  unmodified tree: the identical single failure reproduces there too.
- Visually inspected `dashboard/scripts/visual-out/job.png` and `run.png`: the job page's
  description paragraph renders exactly as before (unchanged behavior); the run page now shows the
  same description text (a placeholder "Enrich resolved places — ..." string from the harness
  fixture) as a `.sub` paragraph directly beneath the job name / status badge row, wrapping cleanly
  with no overlap or clipping — matches the `/jobs/[name]` convention.

## Result

done T429
