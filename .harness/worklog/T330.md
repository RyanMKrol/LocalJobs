# T330 — remove redundant "Errors only" chip from Logs page

## What changed
- `dashboard/app/logs/page.tsx`: deleted the standalone "Errors only" `<button>` from
  the `.io-filter-bar`, and removed the now-unused `errorsOnly` derivation +
  `toggleErrorsOnly` function. Confirmed via `grep -n 'errorsOnly' dashboard/app/logs/page.tsx`
  that no references remain.
- The three `LEVELS` pills (`info`/`warn`/`error`) and `toggleLevel` are unchanged —
  clicking `error` alone still produces `levels === ['error']`, the same end state the
  removed button used to produce.
- `dashboard/scripts/_dashboard-harness.mjs`: updated the `logs-errors-only` FLOWS entry
  to click the `error` level pill (`.io-filter-chip:has-text("error")`) instead of the
  removed "Errors only" button, so the flow still captures the error-filtered state.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 91/91 passing.
- `npm --prefix dashboard run build` — succeeded.
- `node dashboard/scripts/visual-check.mjs` — all 24 pages captured, no failures.
  - `logs.png`: filter bar now shows only `info · warn · error` pills followed by
    `24h`/`7d` — no gap or leftover spacing where the "Errors only" chip used to sit.
  - `logs-errors-only.png` (FLOWS entry, clicks the `error` pill): the `error` pill
    renders active/highlighted (red), same visual state the old "Errors only" button
    used to produce — confirms `toggleLevel` alone still covers the errors-only case.
  - Nothing else on the page changed or looks broken.

## Scope
Only `dashboard/app/logs/page.tsx` changed within the task's declared scope. Also
updated `dashboard/scripts/_dashboard-harness.mjs` (FLOWS), which is always in-scope
for UI tasks per the visual-confirmation living-artifact rule.
