# T106 worklog

## Attempt 1 — 2026-06-23

### What I found
`dashboard/app/services/page.tsx` line 8 in the `Bar` component returned `<span className="muted">no cap</span>` when `cap == null`. All other no-limit text in the file (input placeholders and ✕ clear-button `title` attributes) already said "no limit" — likely updated by T074.

### Change made
Single-line change: `no cap` → `no limit` in the `Bar` null-cap branch.

### Verification
- `npx tsc --noEmit` — clean
- `npm test` — all 21 test files passed
- `npm --prefix dashboard run build` — clean build, no warnings

### Result
done
