# T031 worklog

## What was done

Trimmed README.md:
- Removed the `## Layout` file-tree section entirely (was ~33 lines of source-code map)
- Merged `## Example pipeline: places` and `## Example pipeline: perfumes` (previously ~65 lines of dense prose) into a single `## Worked example pipelines` section (~25 lines): one short paragraph per pipeline + a shared note on typed-artifact contracts
- Result: 317 → 246 lines (net −71 lines, −95 raw deletions after new content)

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed (no code changed, docs-only)
