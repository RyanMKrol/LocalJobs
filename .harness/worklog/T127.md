# T127 — Review + choose: backlog section-header caret style (from T113)

Reviewed by the owner on 2026-06-23.

**Chosen style: `spin`** — the `.caret-spin` style: a rotating `›` chevron (rotates
between collapsed/expanded) with a subtle hover highlight on the whole summary row.

Two refinements requested alongside hardcoding it (carried into the follow-up):
- The caret sits slightly **below** the vertical centre of the section title text and
  looks jarring — centre it optically against the title.
- Tidy the **horizontal padding** around the caret so the spacing is visually pleasing.

Follow-up **T132** will hardcode `spin` as the sole caret, remove the live style toggle
and the four unused styles (`bold` / `accent` / `circle` / `boxed`), and apply the two
fixes above. (Per the T129 convention: chooser T113 → review T127 → hardcode-the-winner
follow-up T132.)
