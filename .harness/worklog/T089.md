# T089 worklog — Gate detail: make per-expectation 'actual' result stand out

## What was done

T088 had already landed (code/JSON values wrapped in `.code-block` on the gate detail page).

T089 redesigned the "actual" value presentation so it's visually prominent rather than muted inline text:

**page.tsx**: Wrapped the actual value in a `<span className="gate-expect-actual-wrap">` containing:
- A new `<span className="gate-expect-actual-label">actual</span>` — the emphasized label
- The existing `<code className="code-block gate-expect-actual">` code block from T088

**globals.css**: Replaced `.gate-expect-actual { margin-top: 3px; }` with:
- `.gate-expect-actual-wrap` — flex column, 4px gap, 10px top margin (clear separation from detail text)
- `.gate-expect-actual-label` — font-size 11px, font-weight 700, text-transform uppercase, letter-spacing .06em (bold uppercase "ACTUAL" label, no muting)
- `.gate-expect-actual {}` — empty (margin now handled by wrap)

Result: each expectation now shows label → detail (muted) → [blank space] → **ACTUAL** → code block value. The "ACTUAL" label is bold + uppercase, clearly separated, and harmonises with T088's code-block treatment.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all 19 test files, all assertions passed
- `npm --prefix dashboard run build` — build succeeded
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (0 overflow, 0 boundary crossings)
