# T101 worklog — Gate detail: apply side-card code styling to middle Gate container

## What was done

In `dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx`, the middle
"Gate" card displayed the artifact key (`gate.key`) as a `<p className="gate-summary">`
containing a `<span className="mono">` — plain monospace text with no background or border.

The side cards (SideCard component) already use `<code className="code-block gate-format">`
for their code-shaped content (`shape.format`), with the styled box (background, border,
border-radius, padding) from the `code-block` class.

**Change:** replaced `<p className="gate-summary"><span className="mono">{gate.key}</span></p>`
with `<code className="code-block gate-format">{gate.key}</code>` in the middle gate card.

The prose description (`gate.description`) stays as a `<p className="muted">` — unchanged.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — build succeeded
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
