# T048 worklog

## Attempt 1 — 2026-06-22

### What was done

Edited `dashboard/app/page.tsx`:

1. **Stuck section**: wrapped the `<h2>` + `<div class="panel">` block in
   `{stuck.length > 0 && (<>...</>)}`. When no stuck items exist the section is
   completely absent from the DOM; the count tile at the top is unchanged.
   Also simplified: the conditional border colour (`stuck.length ? red : undefined`)
   collapsed to always-red (the block only renders when there ARE stuck items),
   and the two-branch empty-state message collapsed to one ("hidden by filter").

2. **Ignored section**: added `&& ignored.length > 0` to the existing
   `{activeFilter === 'ignored' && ...}` guard, so clicking the Ignored tile
   when no items are ignored shows nothing (not an empty table). Removed the
   now-unreachable empty-state `<tr>`.

### Verification

- `npx tsc --noEmit` — clean
- `npm test` — 18 files, all passed
- `npm --prefix dashboard run build` — clean (8 static/dynamic routes)

### Result

Committed as `956d2f4` — ready for loop to push + gate CI.
