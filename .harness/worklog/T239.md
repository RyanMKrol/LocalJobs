# T239 worklog

## What changed

`dashboard/app/globals.css` — `.io-footnote`:
- Added `display: flex; align-items: center;` so the caption text sits vertically centred in its band.
- Changed `padding: 8px 14px 0` → `padding: 8px 14px` (symmetric top/bottom) so the band has equal breathing room above and below the text.

## Verification

- `npm --prefix dashboard run build` — green.
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass.
