# T432 — paragraph-length job descriptions for movies (movie-recommendations) workflow

## What was done

Rewrote `description` for all 11 jobs in the `movies` workflow to real 3-6 sentence paragraphs,
grounded in `CLAUDE.md`'s `movie-recommendations` section and each stage's actual code:

- `src/workflows/movies/stages/branches.ts` — all 8 branch specs (`rec-random-1/2/3`, `rec-auteur`,
  `rec-canon`, `rec-thin-genre`, `rec-older-era`, `rec-world-cinema`): what makes each branch's lens
  distinct, what it reads (taste profile + lens-targeted owned subset + already-suggested history),
  and what it writes for `rec-merge` to pool. Noted the graceful-skip (`return null`) behavior for
  the 4 targeted branches that depend on a qualifying subset existing.
- `src/workflows/movies/stages/snapshot.job.ts` (`movie-snapshot`) — the Plex GUID matching, the two
  output files, and the no-skip-if-done recompute-fresh behavior.
- `src/workflows/movies/stages/franchise-gaps.job.ts` (`franchise-gaps`) — the two-pass TMDB
  Collections audit, no quality filter, quota-graceful degradation, output shape.
- `src/workflows/movies/stages/merge.job.ts` (`rec-merge`) — the fan-in, the verification pipeline
  (TMDB search / owned check / already-recommended check / quality bar), dedup + genre balancing,
  and the bounded top-up re-prompt loop.
- `src/workflows/movies/stages/notify.job.ts` (`movie-gaps-notify`) — the combined digest, the
  ignore-ledger filtering, and the "notify once" idempotency semantics.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 213/213 passing, 0 failures (existing suite; no new tests needed since this is a
  pure content/documentation change to `description` string literals, not behavior).

## Result

done T432
