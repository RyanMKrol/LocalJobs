# T212 worklog

## What was done

Bumped `.cron-badge` `gap` from `4px` to `8px` in `dashboard/app/globals.css`.

This makes the expression↔`?` spacing equal to the `?`↔`(off)/(on)` spacing (the status label
sits outside the `CronBadge` span, so its gap came from the parent flex container, which was
already larger).

## Verification

- `npm --prefix dashboard run build` — green
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
- Visual: the gap between the cron text and `?` icon now matches the gap between `?` and the
  `(off)`/`(on)` label on the Workflows list page.
