# T393 — fix forgotten `'error'` log level on two catch blocks

## Do

Fixed both call sites named in the spec:

1. `src/jobs/projects-sync/stages/project-summarize.ts` (line 247) — per-repo
   `catch (e)` block now passes `'error'` as `ctx.log`'s second argument.
2. `src/jobs/workouts-sync/stages/hevy-sync.ts` (line 164) — per-workout
   `catch (err)` block now passes `'error'` as `ctx.log`'s second argument.

Confirming grep (`grep -rn 'log(\`error\|log('"'"'error\|log("error' src --include="*.ts"`,
excluding `src/jobs/projects-sync/data/**` and `*.test.ts`) found only these two
call sites — both now fixed, no other instances.

## Tests added

- `project-summarize.test.ts`: extended the existing "rejects a summary missing a
  required section" test with a test-local `ctx.log` override that records
  `(message, level)` pairs, asserting the failure log line is recorded at `'error'`
  level.
- `hevy-sync.test.ts`: added a new test ("logs a per-workout failure at 'error'
  level when recording it throws") that forces the per-workout catch block via a
  workout with a `null` id (violates `work_items.item_key`'s NOT NULL constraint
  in `markWorkItem`). Note: the catch block's own recovery `markWorkItem(...,
  'failed')` call hits the same constraint and re-throws, so the whole
  `runHevySync` call rejects — the test uses `assert.rejects` and verifies the
  error log line was recorded (at `'error'` level) before that happened.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 169/169 passing (was 168/168 + 1 new test file addition net; all green).
- No dashboard changes, so no dashboard build needed.
- No live paid-API calls made — all hermetic (scratch DB + stub fetchers/git/claude).

## Daemon restart

Restarted the daemon per root CLAUDE.md ("Always restart what you changed") so
the fix is live: `launchctl kickstart -k gui/$(id -u)/com.ryankrol.localjobs`.

Result: done T393
