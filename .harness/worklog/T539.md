# T539 — Node version pinning (engines + .nvmrc)

## What was done
✓ Added `"engines": { "node": ">=22 <23" }` to `package.json`
✓ Added `"engines": { "node": ">=22 <23" }` to `dashboard/package.json`
✓ Created `.nvmrc` at repo root containing `22`

## Verification
- `npm test` — all 99 tests passed ✓
- `npx tsc --noEmit` — clean, no type errors ✓
- `npm --prefix dashboard run build` — build successful, all pages generated ✓
- `.nvmrc` exists at repo root with content `22` ✓
- Both package.json files declare matching `engines.node` range ✓

The engines declaration is advisory (warns under npm, does not enforce by default), which matches the intended behavior per the task spec. All build and test checks pass with no behavior change.
