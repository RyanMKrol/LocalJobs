# T310 — gate padlock background fill for all states

## Change
In `dashboard/app/components/DagFlow.tsx`'s `GateEdge`, added a `lockFill` derived from
`lockColor` via `color-mix(in srgb, ${lockColor} 18%, transparent)` and applied it
unconditionally to the padlock body `<rect>` (previously only the failed state got a
tinted fill; passed/pending rendered `fill="none"`).

No other lines touched: shackle `<path>`, `lockSize`, `strokeWidth`, `viewBox`, and the
`EdgeLabelRenderer` wrapper's own background style are all unchanged from the current
(post-T296) state.

## Verification
- `npx tsc --noEmit` — pass.
- `npm test` — 85/85 pass.
- `npm --prefix dashboard run build` — pass.
- `node dashboard/scripts/visual-check.mjs` — all 19 pages captured, no failures.
  Inspected `dashboard/scripts/visual-out/workflow-run.png`: the existing `gates` fixture
  in `_dashboard-harness.mjs` already has one `passed` gate (places-resolve→places-enrich)
  and one `failed` gate (places-enrich→places-enrich-with-llm) both visible in this single
  screenshot. Confirmed with my own eyes: the passed padlock now shows a subtle green-tinted
  background behind its body, and the failed padlock shows its (pre-existing) red-tinted
  background — both visually distinct from the surrounding `var(--panel-2)` wrapper chip.
  No fixture change was needed since both states were already co-visible in one screenshot.
- This is a pure load-time visual change (no new interaction/state), so no new `FLOWS` entry
  was needed — confirmed via the above screenshot review.
