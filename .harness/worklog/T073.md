# T073 worklog — Backlog view: pending shows pending, not in progress

## Attempt 1 — 2026-06-22

**Prior history:** This fix was applied in commit 2e65bbb then reverted in e37e40c. Task asks to re-apply exactly.

**Changes made** (`dashboard/app/backlog/page.tsx`):
1. `statusPill(t, isNext)` → `statusPill(t)`: removed `isNext` param and the `▶ in progress` branch; added a comment explaining why (TASKS.json has no `in_progress` status)
2. `TaskCard`: removed `isNext` prop and dropped the isNext border color (`rgba(88,166,255,.4)`)
3. `DagDetailPanel`: kept `isNext` prop (used for the detail panel border in DAG view), but changed `statusPill(t, isNext)` → `statusPill(t)` so the pill shows real status
4. Call sites in main component: dropped `isNext={t.id === nextId}` and `isNext={false}` from TaskCard usages

**DAG view:** unaffected — `BacklogDag` (Dag.tsx) has its own node rendering with the "next" highlight; `DagDetailPanel` keeps its `isNext` border for visual context; the pill in the detail panel now shows actual status.

**Verification:**
- `npm --prefix dashboard run build` → clean (no errors, no TS errors)
- `npx tsc --noEmit` → clean
- `npm test` → 19 files, all passed

**Result:** done
