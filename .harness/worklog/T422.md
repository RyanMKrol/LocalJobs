# T422 — project-summarize must fail its run when any repo fails

## What was done

- Added a `failed` counter in `runProjectSummarize` (`src/workflows/projects-sync/stages/project-summarize.ts`),
  incremented in the existing per-repo `catch` block (which still logs + `markWorkItem(..., 'failed', ...)`
  exactly as before — no change to per-item handling).
- Updated the final summary log line to also report the failed count.
- Added a `throw` at the end of `runProjectSummarize` when `failed > 0`, summarizing how many of the
  attempted repos failed this run — this correctly marks the RUN failed, blocks downstream DAG
  dependents, and engages the job's own retry budget (a retry naturally skips already-`success` items
  via the existing marker check).
- Left `github-sync.ts` untouched per the spec (no per-item failure path there to fix).

## Tests added/updated

- `src/workflows/projects-sync/stages/project-summarize.test.ts`:
  - Updated the existing "rejects a summary missing a required section" test to assert the call now
    rejects (previously it resolved normally) — still asserts the per-item ledger row is `'failed'`.
  - Added a new test reproducing the original incident shape (one repo, `summarizeWithRepoAccess`
    throws mirroring `error: failed to summarize ...`) — asserts `runProjectSummarize` rejects with a
    message naming the failed count, and the per-repo `work_items` row is still `'failed'`.
  - Added a mixed-outcome test (one successful repo + one failing repo in the same run) — asserts the
    successful repo's row is `'success'` with `detail.markdown` set, the failing repo's row is
    `'failed'`, and the overall call still throws.

## Verification

- `npx tsc --noEmit` — clean, no errors.
- `npm test` — 209/209 passing (all suites green), including the new/updated project-summarize tests.
- No live paid API calls made; all git-clone/Claude-CLI calls are stubbed per the existing hermetic
  test harness in this file.
