# T246 worklog

## What was built

Added an **Enabled** column to the Workflows list page (`dashboard/app/workflows/page.tsx`) rendered
as a pill: green "on" when `p.enabled` is truthy, muted "off" when falsy.

## CSS changes (`dashboard/app/globals.css`)

Added two new pill variants after the existing `.pill.failed` rule:

```css
.pill.on  { color: var(--green); background: rgba(63,185,80,.12); border-color: rgba(63,185,80,.3); }
.pill.off { color: var(--muted); background: var(--panel-2); border-color: var(--border); }
```

Both are fully theme-variable-driven (no hardcoded colours).

## Harness update (`dashboard/scripts/_dashboard-harness.mjs`)

Updated the `/api/workflows` fixture so the second workflow (`perfumes`) has `enabled: 0`, ensuring
both "on" and "off" pill states are exercised in the visual and mobile checks.

## colSpan update

Bumped the empty-state row `colSpan` from 6 → 7 to match the new column count.

## Checks run

- `npx tsc --noEmit` — clean
- `npm test` — all 4 tests pass
- `npm --prefix dashboard run build` — successful build
- `node dashboard/scripts/visual-check.mjs` — all 10 pages captured
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass (0px overflow, 0 boundary-crossing elements)

## Visual confirmation

Viewed `dashboard/scripts/visual-out/workflows.png`:
- `places` row shows a green "on" pill in the Enabled column
- `perfumes` row shows a muted/grey "off" pill
- Both pills render clearly, correctly sized, no overflow or clipping
- Table layout is clean and all columns (Workflow, Enabled, Stages, Schedule, Last run, Next, button) are visible
