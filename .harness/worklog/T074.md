# T074 worklog

## What was done

Fixed inconsistent placeholder text in the Services page edit-limits form (`dashboard/app/services/page.tsx`).

- Line 101: `placeholder="none"` → `placeholder="no limit"`
- Line 105: `placeholder="no cap"` → `placeholder="no limit"`
- Line 109: `placeholder="no cap"` → `placeholder="no limit"`
- Also updated the three ✕ button `title` attributes from "Set to no cap" → "Set to no limit" for the daily/monthly inputs, matching the rate input which already said "Set to no limit".

All three inputs now consistently show "no limit" when blank, and the ✕ clear buttons all say "Set to no limit".

## Verification

- `npm --prefix dashboard run build` — passed (8/8 static pages generated, no errors)
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
