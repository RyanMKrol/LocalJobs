# T543 — Collapse the four managers into a generic RecsManager + GroupedManager

2026-07-15

## What was built (verified, but not committed — see Blocked below)

- Added `dashboard/app/components/RecsManager.tsx` — a generic `RecsManager<T extends RecItem>`
  parameterized by `{ heading, noun, tmdbPath, fetchData, ignore, unignore, unignoreBulk }`,
  replacing `TvRecsManager.tsx` and `MovieRecsManager.tsx` (deleted).
- Added `dashboard/app/components/GroupedManager.tsx` — a generic `GroupedManager<T, G, D>`
  parameterized by `{ heading, description, fetchData, getGeneratedAt, getItems, isIgnored,
  itemKey, groupBy, groupKey, renderGroupLabel, activeColumns, ignoredColumns, renderCells,
  emptyBeforeGenerated, summaryLine, noActiveMessage, ignoredSubtitle, ignoreItem, unignoreItem,
  ignoreGroup, unignoreGroup }`, replacing `MovieGapsManager.tsx` and `MissingSeasonsManager.tsx`
  (deleted).
- `dashboard/app/workflows/[name]/page.tsx`: the four call sites became thin config objects
  (`TV_RECS_CONFIG`, `MOVIE_RECS_CONFIG`, `MOVIE_GAPS_CONFIG`, `MISSING_SEASONS_CONFIG`) rendering
  `<RecsManager config={...} />` / `<GroupedManager config={...} />`. `groupByCollection` /
  `groupByShow` moved into page.tsx alongside their configs (groupByShow now groups full item
  arrays instead of season numbers, since the generic component's `renderCells` operates on items
  directly).
- Reused the existing `SortTh`, `IgnoredSection`, `Pill` (unused directly but available), and the
  R05-a `useAction` hook — no new state-management pattern introduced.
- `dashboard/scripts/_dashboard-harness.mjs` did NOT need any changes — the rendered DOM/URL
  surface is unchanged (same routes, same fixtures), only the component internals were
  consolidated.

## Checks run (all green)

- `npx tsc --noEmit` — clean (the 5 pre-existing errors in `OutputRenderer.test.ts`,
  `StageIoLists.test.ts`, `ui.test.ts` ×2, `nav-check.test.ts` were confirmed present on
  unmodified `main` via `git stash` — unrelated to this change).
- `npm --prefix dashboard run build` — succeeds, `/workflows/[name]` route builds cleanly.
- `npm test` (root) — 10/10 tests pass.
- `node dashboard/scripts/mobile-check.mjs` — all 32 pages pass (no horizontal overflow /
  boundary-crossing elements) both before and after rebuild.
- `node dashboard/scripts/visual-check.mjs` — 58/59 captures pass. The one failure
  (`workflow-run-skipped`, a `.rf-dag-node` selector timeout on `/workflow-runs/skipped`) was
  confirmed to be a PRE-EXISTING, unrelated flake by re-running the exact same check against
  unmodified `main` (`git stash` / `git stash pop`) — identical single failure there too.

## Visual confirmation (observed)

Read all four target screenshots directly:
- `workflow-movie-recs.png` — `movie-recommendations` detail page: page-level `<h2>Output</h2>`
  wrapper + the manager's own `<h3>Recommendations</h3>`, Film/Year/Genre/Lens/TMDB sortable
  table, `/movie/` TMDB links, Ignored section with "Un-ignore all" — matches pre-R05 layout.
- `workflow-tv-recs.png` — `tv-recommendations` detail page: manager-owned `<h2>Output</h2>`,
  Show/Year/Genre/Lens/TMDB columns, `/tv/` TMDB links — matches.
- `workflow-missing-movies.png` — `missing-movies` detail page: page-level `<h2>Output</h2>` +
  manager `<h3>Franchise gaps</h3>`, grouped-by-collection table (Film/Year/TMDB), "You own: ..."
  example text on the active group header only, per-collection "Ignore all" (only when >1 film),
  Ignored section with its own grouped table — matches.
- `workflow-missing-tv-seasons.png` — `missing-tv-seasons` detail page: manager-owned
  `<h2>Output</h2>`, grouped-by-show table (Season only), TMDB-linked show titles with year,
  per-show "Ignore all" — matches.

All four render pixel-equivalent to the pre-R05 look; nothing blank, overlapping, or clipped.

## Blocked — scope does not cover the doc update the spec requires

The task's `## Done when` says "`_dashboard-harness.mjs` and `CLAUDE.md` are current." The
harness script needed no change (confirmed above). But `CLAUDE.md` (repo root) is NOT in T543's
`scope` (`dashboard/app/components/`, `dashboard/app/workflows/[name]/page.tsx`,
`dashboard/scripts/_dashboard-harness.mjs` only), and it genuinely does need updating: its
"Shared dashboard UI components" section (around lines 1310–1333) names `TvRecsManager.tsx`,
`MovieRecsManager.tsx`, `MovieGapsManager.tsx`, `MissingSeasonsManager.tsx` by file path — all
four are deleted by this change, so leaving those references would make the doc actively wrong
about the current component layout (violates this repo's "Documentation is part of every
change" rule).

Per the harness's own instructions ("if a needed doc is NOT in scope, do NOT edit it — record
`failed:blocked` noting the missing doc" / HARNESS.md §8.1: "the first file touched outside
[scope] now blocks the task immediately — one attempt, no retry"), I did not touch `CLAUDE.md`
and am not committing this work.

**For a human:** either (a) add `CLAUDE.md` to T543's `scope` and re-run so the same refactor
can rewrite the "Shared dashboard UI components" bullet for `RecsManager`/`GroupedManager`, or
(b) update `CLAUDE.md` by hand to remove the four deleted-file references and describe the two
new generic components, then author a small follow-up task (or amend T543) to land the code
change alongside it. All the code above is fully built, tested, and visually verified — it is
not committed, so nothing needs reverting; a re-run starts cold per protocol and will reproduce
the same result.

---
failed:blocked T543 — agent reported failed:blocked — needs CLAUDE.md update (out of scope) — the spec's Done-when requires CLAUDE.md's component list to be kept current (it names the 4 deleted manager files by path), but CLAUDE.md is not in T543's scope; code/tests/build/mobile-check/visual-check are all complete and green (see worklog/T543.md) but not committed per the scope-gate protocol.
