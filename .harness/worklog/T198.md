# T198 worklog

## Change

Removed the `{workflowName} run · ` prefix from the `WorkflowRunBackLink` label in `dashboard/app/ui.tsx` (line 136). The label now shows only the short id (`shortId`). The href and fallback behaviour are unchanged.

## Checks

- `npx tsc --noEmit`: clean
- `npm --prefix dashboard run build`: success
- `node dashboard/scripts/mobile-check.mjs`: all 9 pages pass
