# T409 — github-sync success detail

## What changed
- `src/workflows/projects-sync/stages/github-sync.ts`: the per-repo success `markWorkItem`
  call now records `detail: { name, description, language, topics, url, pushedAt }` (the
  fields already computed on `CatalogEntry`), instead of no detail at all. Per the
  "no file, discovered/computed value" branch of the detail convention — this is a shared
  catalog file (`data/out/projects.json`), so the fix surfaces the repo's own catalog
  fields directly rather than pointing at the shared file with a `path`/`format` pair.
- `project-summarize.ts` confirmed already compliant (already sets
  `detail: { name, markdown, lastCommitSha }`) — untouched, per spec.

## Tests
- Updated `github-sync.test.ts`'s "writes the catalog and marks each repo done in the
  ledger" test to read the row back via `getWorkItem(JOB, repoId)`, parse `detail`, and
  assert `description`/`language`/`url`/`topics` match the fixture repo.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 187/187 pass (0 fail).
- Spot-check via live workflow run / dashboard not performed (no live GitHub credentials
  available in this headless run; the new test directly exercises `markWorkItem`'s
  recorded `detail` via the DB, which is what the dashboard's Inputs & Outputs panel and
  the `output` API endpoint both read from — same code path, verified via test instead
  of a live run).
