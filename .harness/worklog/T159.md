# T159 worklog

## Attempt 1 — 2026-06-24

### What was done

Refactored `dashboard/app/backlog/page.tsx`:

- Removed `TaskCard` (always-expanded full card used for buildable + needs-human) and `DoneRow` (the expand-on-click row for done tasks).
- Introduced a single `CollapsibleRow` component that handles all three sections. Collapsed state shows: chevron, task id, title, buildable/needs-human marker pill, difficulty pill, and (for done tasks) the reviewed pill + "Mark as reviewed" toggle + "✓ done" pill.
- Expanded state reveals: `dependsOn`, rendered spec markdown, tags — same as the old `DoneRow`.
- All three `<details>` sections now default to **collapsed** (removed `open` from the buildable and needs-human sections).
- Each section now wraps its rows in a `.panel` container matching the done section style.
- Added `.pill.human` CSS class in `globals.css` for the "🔒 needs human" pill.
- Removed the now-unused `statusPill` helper.

### Checks run

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — clean
- `node dashboard/scripts/mobile-check.mjs` — 9/9 pass (including backlog)
