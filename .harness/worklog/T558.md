# T558 — Delete create-dynamo-tables.mjs and improve dynamodb.service.ts

## Summary
Completed all three parts of the task:

1. **Deleted scripts/create-dynamo-tables.mjs** — confirmed it provisions Listens/Projects DynamoDB tables for deleted pipelines that nothing references. A grep of the active codebase (excluding data/repos self-clone) shows no references to these table names outside the deleted script. The only live DynamoDB use is perfumes' read of PerfumeRatings table, which this script never created.

2. **Added intentional-unreachable comments** — marked the code after each policy throw in dynamodb.service.ts with an explicit comment:
   - dynamoPut (~line 92)
   - dynamoDelete (~line 154)
   - dynamoBatchWrite (~line 167)
   These comments document that the code is kept for when the policy gate is lifted, clarifying intent for readers and linters.

3. **Credential construction now throws loudly** — in getClient() (~lines 40-46), replaced the silent `secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY ?? ''` fallback with an explicit guard:
   - If AWS_ACCESS_KEY_ID is set but AWS_SECRET_ACCESS_KEY is missing, throw immediately with a clear message naming both env vars
   - This prevents confusing downstream signature errors and makes misconfiguration fail fast

## Verification
- ✓ `npx tsc --noEmit` — clean, no type errors
- ✓ `npm test` — all tests pass (11/11 suites, 0 failures)
- ✓ scripts/create-dynamo-tables.mjs is deleted
- ✓ No Listens/Projects DynamoDB table name references outside self-clone
- ✓ Perfumes PerfumeRatings read path still compiles and references the live dynamoScan helper

## Done when checklist
- ✓ npx tsc --noEmit is green
- ✓ npm test is green
- ✓ scripts/create-dynamo-tables.mjs is gone
- ✓ grep for Listens|Projects table names finds no live referent outside self-clone
- ✓ dynamodb.service.ts has explicit intentional-unreachable comments after each write-helper throw
- ✓ Credential construction throws clear error when AWS key id set & secret missing
- ✓ Perfumes PerfumeRatings read path still compiles
