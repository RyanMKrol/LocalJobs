# T512: tv-recs Plex→callService migration

## What was built

Wrapped the TV library snapshot `plexGet` call in `src/workflows/tv-recs/stages/tv-snapshot.ts` inside `callService('plex', ...)` to meter Plex reads through the shared plex service quota/rate limits, matching the callService('tmdb', ...) pattern used in sibling workflow stages.

### Changes made:

1. **tv-snapshot.ts:**
   - Added `callService` import from `../../../core/services.js`
   - Wrapped the `plexGet<PlexAllResponse<PlexShowMeta>>(...)` call inside `callService('plex', () => plexGet(...))` at lines 28-30
   - No behavior change beyond the wrapping — arguments, return-value handling, and error handling preserved

2. **tv-snapshot.test.ts:**
   - Added `callService` import to support new test
   - Added test case verifying the callService('plex', ...) wrapper passes through correctly when the service is unregistered (test environment behavior)
   - Test confirms the wrapper doesn't break functionality

3. **tv-recs/CLAUDE.md:**
   - Updated line 61 to note that `plex` service is now used alongside `tmdb` and `claude-cli`

## Verification

✅ `npm test` passed — all tests green, including new callService wrapper test
✅ `npx tsc --noEmit` passed — no type errors
✅ `npm --prefix dashboard run build` passed — dashboard builds successfully
✅ Grep confirms no bare `plexGet(` calls remain in tv-snapshot.ts (all wrapped in callService)
✅ Only 3 scoped files staged (tv-snapshot.ts, tv-snapshot.test.ts, CLAUDE.md)

## Git hygiene

Staged files explicitly by name only:
- src/workflows/tv-recs/stages/tv-snapshot.ts
- src/workflows/tv-recs/stages/tv-snapshot.test.ts
- src/workflows/tv-recs/CLAUDE.md

No data/, chrome-profile/, .env*, or other gitignored paths touched.
