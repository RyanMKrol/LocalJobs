# T242 — Polish expanded backlog task card

## What was done

Added a `.task-expand-body` CSS class in `dashboard/app/globals.css` with:
- `padding: 12px 16px 14px` — consistent inner padding on all sides including bottom
- `border-radius: 6px` — full corner rounding (previously only bottom corners were rounded at 4px)
- `margin-bottom: 6px` — small gap between expanded card and the next row
- `background: var(--panel-2)` and `border: 1px solid var(--border)` — card appearance using theme variables

Replaced the inline `style` on the expansion `<div>` in `dashboard/app/backlog/page.tsx` with `className="task-expand-body"`.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — clean
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass (backlog included)
