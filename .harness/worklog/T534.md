# T534 — GET /api self-describing route table

2026-07-13

## What I did
Added a `GET /api` entry to the `routes` table in `src/api/server.ts`. Its
handler returns `{ routes: routes.map(r => ({ method: r.method, pattern:
r.pattern })) }`, derived live from the same `routes` array the matcher uses
(no separate list to keep in sync).

Added a test in `src/api/server.test.ts` asserting:
- `GET /api` returns 200
- `body.routes` is a non-empty array of `{ method, pattern }` objects
- the known `GET /api/workflows/:name` pattern is present in the list

## Checks run
- `npx tsc --noEmit` — green
- `npm test` — 274/274 passing (0 failed)

## What remains
Nothing — task complete.
