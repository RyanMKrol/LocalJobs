# T200 worklog

## What was done

Added a polished section container to both `MovieGapsManager` and `MissingSeasonsManager` in
`dashboard/app/workflows/[name]/page.tsx`, and extended the shared CSS in `globals.css`.

### CSS changes (`globals.css`)

- Added `.output-section` — a section-level container with `border: 1px solid var(--border)`,
  `background: var(--panel-2)`, `border-radius: 6px`, and `padding: 16px 18px`. Drives every
  colour from an existing CSS variable so it works across all 6 theme palettes (3 families × 2 modes).
- Added `.output-section table` — `border-collapse: collapse; width: 100%`.
- Added `.output-section table th, .output-section table td` — `padding: 6px 10px; border: 1px solid var(--border)`.
- Added `.output-section table thead th` — `background: var(--panel)` for a subtle header distinction.
- Added `.output-section .panel` — an explicit `border: 1px solid var(--border)` so the per-collection/
  per-show cards are visibly outlined inside the section container.

### TSX changes (`page.tsx`)

Replaced the bare `<>…</>` fragment return in both `MovieGapsManager` and `MissingSeasonsManager`
with a `<div className="output-section">…</div>` wrapper. No other logic changed.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — succeeds
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass at 402px
