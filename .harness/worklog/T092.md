# T092: UI — Cancel button for a running workflow

## What was done

Added a Cancel button to the workflow-run detail page:

1. **`dashboard/app/lib/api.ts`** — added `cancelWorkflowRun(id)` client method that
   POSTs to `/api/workflow-runs/:id/cancel` (mirrors `runWorkflow`/`toggleWorkflow`).

2. **`dashboard/app/workflow-runs/[id]/page.tsx`** — added `useState` + `busy` state,
   a `cancel()` async function (mirrors the `run()` pattern from the workflow detail page),
   and a `✕ Cancel` button rendered in the header row next to the status badge, visible
   ONLY when `run.status === 'running'`. Button is disabled during the in-flight request
   (1200ms guard). The 2s poll picks up the `cancelled` status automatically.

3. **`dashboard/app/globals.css`** — added `.btn.btn-danger` style (red background, white
   text) to visually distinguish the Cancel action from the primary Run button.

4. **`README.md`** — updated the Workflow run detail bullet in the Dashboard pages section
   to describe the Cancel button.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — 19 test files, all passed
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass at 402px
