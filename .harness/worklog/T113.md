# T113 worklog — Backlog section headers: 5 toggleable caret styles

## Attempt 1 — 2026-06-23

### What was done

Implemented 5 distinct, clearly-more-visible caret/disclosure-indicator styles for
the collapsible Backlog section headers, with a live toggle persisted to localStorage.

**Pattern followed:** mirrors the T99 gate-style chooser exactly (same hook shape,
same CSS class bar, same localStorage persistence, same evaluation-aid note).

### Files changed

- **`dashboard/app/ui.tsx`**: Added `CARET_STYLES` (5 entries), `CaretStyle` type,
  `CARET_STYLE_KEY = 'localjobs.caretStyle'`, `DEFAULT_CARET_STYLE = 'bold'`, and
  `useCaretStyle()` hook (same shape as `useGateStyle`).

- **`dashboard/app/globals.css`**: Added CSS for all 5 styles (`.caret-{id}` selectors
  on a wrapper div):
  1. **bold** — `▸/▾` at 18px, opacity 1, `color: var(--text)` — simple high-contrast upgrade
  2. **accent** — `▶/▼` at 13px, `color: var(--accent)` — fills in the accent colour
  3. **circle** — `⊕/⊖` at 18px, accent colour — circled add/remove toggle
  4. **boxed** — `▸/▾` inside an accent-bordered pill (inline-block `::before` with border + border-radius)
  5. **spin** — `›` rotated 0→90deg via CSS transform + `transition: 0.2s`, plus hover
     highlight on the entire summary row

  Also added `.caret-style-bar`, `.caret-style-label`, `.caret-style-btn` classes
  (mirrors `.gate-style-*`).

- **`dashboard/app/backlog/page.tsx`**:
  - Imports `useCaretStyle`, `CARET_STYLES` from `../ui`
  - Calls `useCaretStyle()` in the component
  - Adds `<div className="caret-style-bar">` selector (5 buttons with hints) in the
    header row, shown only when `view === 'list'`
  - Wraps the 3 `<details>` in `<div className={`caret-${caretStyle}`}>`

### Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed (33 tests)
- `npm --prefix dashboard run build` — clean, /backlog bundle 2.29 kB
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass, no overflow
- Dashboard restarted to pick up new build

### Notes for the follow-up task

Once the user picks a favourite caret style, the follow-up task should:
1. Remove `CARET_STYLES`, `CaretStyle`, `CARET_STYLE_KEY`, `useCaretStyle` from `ui.tsx`
2. Remove the `.caret-style-bar/label/btn` CSS + all `.caret-{id}` CSS blocks
3. Hardcode the winning style's CSS into `.section-heading-summary::before` /
   `details:not([open]) .section-heading-summary::before` directly
4. Remove the `useCaretStyle` call and `CARET_STYLES` import + selector bar from `backlog/page.tsx`
5. Remove the `<div className={`caret-${caretStyle}`}>` wrapper (or keep it as a plain `<>`)
