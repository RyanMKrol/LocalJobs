# T359 worklog

## What was done

In `dashboard/app/workflows/[name]/page.tsx`:

1. `MovieRecsManager`'s top-level `<h2>Recommendations</h2>` → `<h3 style={{ fontSize: 15, marginTop: 20 }}>Recommendations</h3>`.
2. `MovieGapsManager`'s top-level `<h2>Franchise gaps</h2>` → `<h3 style={{ fontSize: 15, marginTop: 20 }}>Franchise gaps</h3>`.
3. Render call site (previously two sibling `{name === 'movie-recommendations' && <Manager />}` lines)
   now wraps both managers under one outer heading:
   ```tsx
   {name === 'movie-recommendations' && (
     <>
       <h2>Output</h2>
       <MovieRecsManager />
       <MovieGapsManager />
     </>
   )}
   ```
   Confirmed via grep that `MovieRecsManager`/`MovieGapsManager` are each used at exactly this one
   call site, so demoting their internal headings was safe.
4. Both empty-state messages now match `TvRecsManager`'s precedent
   (`<div className="panel"><p className="empty-state-panel">…</p></div>`), same message text as
   before:
   - `MovieRecsManager`: "No recommendations yet. Run the workflow manually — the recommended films
     will appear here."
   - `MovieGapsManager`: "No audit has run yet. This workflow runs monthly (or run it manually) — the
     detected franchise gaps will appear here."
5. No changes to `WorkflowOutputSection.tsx`, `TvRecsManager`, or `MissingSeasonsManager` — out of
   scope for this task (handled by sibling tasks output-section-consistency-1/3, both already `done`
   per TASKS.json).
6. `dashboard/scripts/_dashboard-harness.mjs`: no changes needed. The existing `workflow-movie-recs`
   PAGES entry (`/workflows/movie-recommendations`) already exercises the populated-state screenshot
   with the new heading structure. For the empty-state fix (step 4), I judged adding new
   fixture-selection machinery to force `movieRecs.generatedAt = null` disproportionate for this one
   case — verified the empty-state markup by code inspection instead, since it is now byte-identical
   in structure to `TvRecsManager`'s empty state (page.tsx:105-111), which IS already visually
   confirmed via the existing `tv-recs-empty` PAGES/FLOWS entry. Both panels share the same
   `.panel`/`.empty-state-panel` CSS, so `tv-recs-empty.png` stands as visual proof the wrapper
   classes render correctly (centered panel, no clipping/overlap) for this component family.

## Full per-workflow "Output" heading census (idea #10b close-out)

Confirmed by grepping `<h2>Output</h2>` occurrences across the dashboard:
- `dashboard/app/components/WorkflowOutputSection.tsx:108` — the generic manager, used by every
  workflow NOT in `WORKFLOWS_WITH_SPECIFIC_MANAGERS` (places, perfumes, listening-digest,
  projects-sync, claude-warmer, stocks-sync, stock-digest, workouts-sync).
- `dashboard/app/workflows/[name]/page.tsx:95` — `TvRecsManager` (tv-recommendations), already
  "Output" from a prior task (T357/T358 confirmed `done` in TASKS.json).
- `dashboard/app/workflows/[name]/page.tsx:558` — `MissingSeasonsManager` (missing-tv-seasons),
  already "Output" from a prior task.
- `dashboard/app/workflows/[name]/page.tsx:916` — NEW, added by this task, wrapping
  `MovieRecsManager` + `MovieGapsManager` for movie-recommendations.

Every workflow detail page now shows exactly one section headed literally "Output". This is the
final piece of idea #10(b)'s "every workflow, one Output section" contract.

## Verification performed

- `npx tsc --noEmit` — clean, no errors.
- `npm test` — 134/134 tests pass (no test changes needed; this task has no new logic, only markup).
- `npm --prefix dashboard run build` — succeeds, no new warnings.
- `node dashboard/scripts/visual-check.mjs` — all 30 pages captured, no failures. Opened
  `dashboard/scripts/visual-out/workflow-movie-recs.png` and visually confirmed: ONE "OUTPUT" `<h2>`
  heading appears in the Output section, with "Recommendations" and "Franchise gaps" rendering as
  clearly smaller/lighter `<h3>` subheadings beneath it (visually subordinate to the outer h2, not
  the same weight as the previously-removed top-level h2s). Both tables render fully intact with
  their FILM/YEAR/GENRE/LENS/TMDB columns and orange "✕ Ignore" / "✕ Ignore all" buttons all present
  and clickable-looking. Both "Ignored (N)" subsections and the Danger Zone are unaffected.
- `node dashboard/scripts/mobile-check.mjs` — all 18 pages pass, including `workflow-movie-recs`
  (0px horizontal overflow, 0 boundary-crossing elements).

## Scope compliance

Only touched `dashboard/app/workflows/[name]/page.tsx` (listed in scope). No changes to
`dashboard/scripts/_dashboard-harness.mjs` were needed (also in scope, but nothing to change — see
point 6 above). No other files edited.
