# T233 worklog

## What was built

Split the buildable (non-done, non-gated) tasks on the Backlog page into two sections:

- **Ready** — all `dependsOn` deps are done (or task has no deps)
- **Waiting on dependencies** — ≥1 dep not yet done

Waiting tasks show a muted `needs: T214` pill inline. The summary line now reads "N ready · N waiting" instead of "N harness-buildable".

## Changes

- `dashboard/app/backlog/page.tsx`: built `doneIds` set, split `buildable` into `ready`/`waiting`, updated summary line, added two separate `<details>` sections, passed `unmetDeps` to `CollapsibleRow`, rendered dep pills.
- `dashboard/app/globals.css`: added `.pill.dep-waiting` (muted, font-size 11px).

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all pass
- `npm --prefix dashboard run build` — clean
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass
