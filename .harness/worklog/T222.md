# T222 worklog

## What was done

Disabled zoom/pan on the React Flow DAG in `dashboard/app/components/DagFlow.tsx`:
- Removed `Controls` import and `<Controls>` element
- Added `zoomOnScroll={false}`, `zoomOnPinch={false}`, `zoomOnDoubleClick={false}`, `panOnScroll={false}`, `panOnDrag={false}` to `<ReactFlow>`
- Kept `fitView` and `fitViewOptions` — the full DAG is still sized to the container
- Node/gate links remain clickable (`elementsSelectable` stays false but `<a>` hrefs in node content work via pointer-events)

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — clean
- `node dashboard/scripts/mobile-check.mjs` — 9/9 pages pass
