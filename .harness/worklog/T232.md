# T232 worklog

## What was done

Changed the sort for the done list in `dashboard/app/backlog/page.tsx` (line ~154).

Previously: `a.id.localeCompare(b.id)` — lexical order only.

Now: sort by reviewed-status first (not-reviewed = 0, reviewed = 1, so unreviewed float to top), then by numeric task id ascending (extracting the `###` from `T###` via `parseInt`).

## Checks

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — clean
- `node dashboard/scripts/mobile-check.mjs` — all 8 pages pass
