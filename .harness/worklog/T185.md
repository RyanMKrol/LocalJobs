# T185 worklog — recent workflow runs table height cap

## What was done

Added a `.recent-runs-scroll` CSS class to `globals.css` mirroring the existing `.io-table-scroll`
pattern from T148:
- `max-height: 480px` (~12 rows)
- `overflow-y: auto; overflow-x: auto;`
- Sticky thead via `thead th { position: sticky; top: 0; background: var(--panel); z-index: 1; }`

In `page.tsx`, wrapped the recent workflow runs `<table>` in `<div className="recent-runs-scroll">`.
The outer `.panel` div is unchanged so horizontal scrolling on mobile still works.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — 4/4 pass
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass (0px overflow, 0 boundary-crossing)
