# T195 worklog

## What was done
Added `.io-empty-state` CSS class to `globals.css` with muted colour (`var(--muted)`),
`text-align: center`, and `padding: 24px 16px`. Applied it to the `<p>` in the
Input→Output panel's empty-state branch in `workflow-runs/[id]/page.tsx`.

## Checks
- `npx tsc --noEmit`: clean
- `npm test`: 4/4 pass
- `npm --prefix dashboard run build`: success
- `node dashboard/scripts/mobile-check.mjs`: all 9 pages pass
