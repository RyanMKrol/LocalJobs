# T001 — Green CI baseline on the public checkout

## Goal
Make `npx tsc --noEmit` + `npm test` pass on a CLEAN PUBLIC checkout (only
tracked files: framework + demo job; all private jobs/services/pipelines absent).

## Findings on entry
- Working tree clean; `git status` ahead of origin by 1 commit.
- `src/jobs/registry.test.ts` was ALREADY count-agnostic (added in commit
  a48a2ff "add registry load-guard test") — it asserts arrays + `jobs.length>=1`,
  not the old hardcoded 9/4/2 counts.
- No module does real work / needs `data/` at import time: the registry walks the
  filesystem and only imports discovered `*.job.ts` etc. On a clean checkout that
  is just `demo.job.ts`.

## Change made
- Strengthened `registry.test.ts` to match the task `do` precisely: assert the
  **demo job loads by name** via `getJobDefinition('demo')` (in addition to the
  existing array/length guards), instead of only `length>=1`. Counts remain
  un-asserted (private files vary).

## Verification (empirical, observed)
Simulated the CI runner with a clean public checkout via
`git archive HEAD | tar -x` into a temp dir + overlay of the edited test file,
then `npm ci` and ran the verify commands:

- Clean checkout contained ONLY `src/jobs/demo.job.ts` (no private jobs/services/pipelines).
- `npx tsc --noEmit` → exit 0
- `npm test` → exit 0; registry test logged `registry loads cleanly (1 jobs, 0 services, 0 pipelines)`
- Local (private jobs present): `tsc` exit 0, `npm test` exit 0
  (`9 jobs, 4 services, 2 pipelines`).

No live paid-API calls were made. No docs needed updating (behaviour/conventions
unchanged — only a stricter, more accurate test assertion).

Result: done (committed, not pushed). doneWhen's "CI green on pushed commit" is
gated by the loop after push.
