# T535: Remove redundant getWorkflow call in GET /api/workflows/:name handler

## Work Done
- Identified redundant `getWorkflow(p.name)?.max_concurrency` call in src/api/server.ts line 1120
- Replaced with `p.max_concurrency` since `p` already holds the fully-populated workflow row from line 1110
- Confirmed the change eliminates one DB query per workflow-detail fetch while keeping response identical

## Verification Completed
- `npx tsc --noEmit` ✓ (no type errors)
- `npm test` ✓ (274 tests passed, exit code 0)
- Response shape unchanged: same properties returned to the caller
- No other properties accessed from the removed `getWorkflow()` call

## Notes
Simple optimization: removes a wasted DB query on every GET /api/workflows/:name request by using the already-fetched workflow row. The ternary logic remains unchanged:
- When `wfDef` exists: use `p.max_concurrency ?? wfDef.maxConcurrency ?? DEFAULT_WORKFLOW_CONCURRENCY`
- When `wfDef` is null: use `p.max_concurrency ?? DEFAULT_WORKFLOW_CONCURRENCY`

Both branches now read from `p` directly instead of the first branch making an extra DB call.
