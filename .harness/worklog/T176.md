# T176 worklog

## What was done

Added verbose logging to the `rec-merge` top-up rounds in `src/jobs/movies/stages/merge.ts`.

### Changes

**`buildDefaultTopUp`** — added per-branch logging: logs the number of branches being re-prompted, the exclusion context (N already-collected/owned/considered titles), and per-branch suggestion counts (`• branch-id (lens) → N suggestion(s)`).

**Top-up loop** — added:
- Named cross-round dedup decisions: `⟳ "Title (Year)" [lens] — already seen this run — skipped.`
- Running total after each round's verification: `• round N: +M verified → K/target.`
- Stop-reason tracking and logging (`• top-up stopped: <reason>.`) for all exit paths: target reached, max rounds hit, error, no new suggestions.
- Summary line now includes stop reason: `top-up rounds 2 (target 15 reached)`.

The `TopUpFn` type already had `round: number` as the second parameter; `buildDefaultTopUp` was ignoring it — now it uses it in the log message.

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all passed (including merge-quality.test.ts top-up scenarios)
- No behaviour change; logging only.
