# T366 — validation gates for stocks-sync

Added `src/jobs/stocks-sync/contracts.ts` with two factory functions, mirroring
`src/jobs/places/contracts.ts`'s idiom (`readJson`/`fromChecks`, optional path param,
declared `shape` + per-expectation `checks[]` + `sample`):

- `stocksPortfolioContract(file = stocksSyncConfig.portfolioJsonPath)` — key
  `stocks-portfolio`. Validates `portfolio.json` is a plain JSON array of
  `NormalizedPosition`s; an empty array passes (0 open positions is legitimate).
- `stocksFreshBreachesContract(file = stocksSyncConfig.freshBreachesJsonPath)` — key
  `stocks-fresh-breaches`. Validates `fresh-breaches.json` is a plain JSON array of
  `BreachLine`s; an empty array passes (no fresh breaches is the normal/healthy state).

Wired into the 3 stage jobs:
- `stocks-snapshot.job.ts`: `produces: [stocksPortfolioContract()]`
- `stocks-watch.job.ts`: `consumes: [stocksPortfolioContract()]`,
  `produces: [stocksFreshBreachesContract()]`
- `stocks-notify.job.ts`: `consumes: [stocksFreshBreachesContract()]`

Added `src/jobs/stocks-sync/contracts.test.ts` (mirrors `src/core/contracts.test.ts`):
synthetic fixtures via `mkdtempSync`/`writeFileSync`, no live Trading212 call. Covers
pass cases (well-formed array, empty array — both `ok: true`) and fail cases (missing
file, invalid JSON, non-array top-level, malformed entries) for both contracts, plus a
`buildDag` + `deriveGates` regression test asserting the stocks-sync DAG yields exactly
2 gates matching the `stocks-snapshot→stocks-watch` and `stocks-watch→stocks-notify`
edges.

## Verified

- `npx tsc --noEmit` — clean.
- `npm test` — 146/146 passing, including all 14 new `contracts.test.ts` assertions
  (confirmed by name in output).
- No live Trading212 API call anywhere in the new code/tests — contracts only read
  local `data/out/*.json` files via `existsSync`/`readFileSync`.

Result: done.
