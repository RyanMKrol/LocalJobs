## T578 — 2026-07-15

Re-attempt of T517 (which blocked on missing-movies/stages/snapshot.ts:29 having a bare
`plexGet` call at the time). That gap was closed by the prerequisite per-workflow migration
tasks landing (most recently T577's missing-movies migration).

### Verification performed

Ran a repo-wide, NAME-ONLY grep for `plexGet`, `plexPutStreams`, `triggerButlerBackup`
(deliberately not trying to match the generic type parameter with a `<[^>]*>` regex, per
the spec's warning about nested generics like `PlexAllResponse<PlexMovieMeta>` defeating
that pattern), excluding `src/core/plex-client.ts` itself.

Hand-inspected every call site across all seven Plex-touching workflows:

- `missing-movies/stages/snapshot.ts:37` — `plexGet<...>` wrapped in
  `callService('plex', () => ...)` (lines 36–40). ✓
- `tv-recs/stages/tv-snapshot.ts:46` — same wrapper shape. ✓
- `missing-tv-seasons/stages/snapshot.ts:50,59` — both wrapped. ✓
- `movies/stages/snapshot.ts:44` — wrapped. ✓
- `plex-space-saver/stages/scan.ts:76,83,90` — all three wrapped. ✓
- `plex-profiles/stages/build.ts:44,46,91,96,101,156` — all six wrapped
  (`callService('plex', () => plexGet<...>(...))` inline form). ✓
- `plex-language-fix/lib.ts:41,49,58,67` — all four `plexGet` calls wrapped. ✓
- `plex-language-fix/stages/apply.ts:67,87` — `triggerBackup()`/`putStreams(...)` calls
  (the injectable local aliases) wrapped in `callService('plex', ...)`. The
  `opts.putStreams ?? plexPutStreams` / `opts.triggerBackup ?? triggerButlerBackup`
  lines (20–21, 38–39) are the excluded default-assignment lines named in the spec —
  they only choose which function the wrapped call later invokes, they don't call it. ✓

Excluded per the spec: `src/core/plex-client.ts` (implementation), the `??` default
lines in `plex-language-fix/stages/apply.ts`, all `import` lines, comment/test-doc
references, and the `src/workflows/projects-sync/data/repos/LocalJobs/**` clone
directory (a shallow git clone of this repo produced by the projects-sync workflow's
own test data — matches the documented `data/`-scanning gotcha in root CLAUDE.md).

**Result: no genuinely-bare Plex call remains.** The migration (T571–T577) is complete
across all 7 Plex-touching workflows.

### Docs

Added a one-line note to root `CLAUDE.md`'s `src/core/plex-client.ts` file-map entry
(~line 298) documenting that the shared `plex` service now fronts every
`plexGet`/`plexPutStreams`/`triggerButlerBackup` call, giving call-count metering +
per-job consumer tracking on the Integrations page.

Confirmed `src/core/plex-client.ts` is byte-unchanged (`git status --porcelain` shows
only `CLAUDE.md` modified).

### Checks

- `npx tsc --noEmit` — clean, no output.
- `npm test` — all 106 test files pass, 0 failures.

Only `CLAUDE.md` staged (explicit path, no `git add -A`/`git add .`).
