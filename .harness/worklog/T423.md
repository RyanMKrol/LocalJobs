# T423

Added a dedicated `searchFailed` counter to `VerifyCounters` in
`src/workflows/tv-recs/stages/tv-rec-merge.ts`, incremented in `verifyInto`'s
catch branch (the branch that fires for any non-`QuotaExceededError` from
`search()`), distinct from the existing soft-drop counters and from
`quotaHit`.

At the end of `runTvRecMerge`, after `recommendations.json` is written and the
summary is logged, throw a summarizing `Error` when `counters.searchFailed > 0`
(covers both the base verification pass and every top-up round, since they all
share the same `counters` object).

Soft drops (hallucinated/owned/already-recommended/below quality bar) and a
graceful `quotaHit` stop with zero real search failures remain normal,
successful outcomes — unchanged.

Left `tv-recs-notify.ts` untouched per the spec (no comparable per-item
silent-swallow gap there).

Added two unit tests to `tv-rec-merge.test.ts`:
- a `searchTv` mock throwing a plain `Error` for one suggestion → `runTvRecMerge` rejects
  naming the failure count.
- a run with only soft drops (no throwing `search()` calls) still resolves normally.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — all green (`# fail 0`), including the two new T423 tests.

## Result
done T423
