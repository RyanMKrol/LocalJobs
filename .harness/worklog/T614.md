# T614 — Author idempotencyNote for all 17 workflow manifests

Date: 2026-07-16

## What I did

Added a literal `idempotencyNote: '<sentence>'` field to all 17 `*.workflow.ts` manifests
listed in the task scope, placed alongside `description`/`schedule` (same style as `category`).

Before writing each note, re-read that workflow's own `src/workflows/<folder>/CLAUDE.md` in full
(and, for a couple, the manifest's own header doc-comment) to confirm the verified drafts in the
spec still match current behavior. All 17 verified drafts from the spec matched the current
CLAUDE.md docs and code exactly — no drift found, so every note was used as-authored in the spec
(only trivial apostrophe-escaping applied for the TS string literals).

Files touched (all 17 in scope):
places, perfumes, missing-tv-seasons, missing-movies, movies (movie-recommendations),
tv-recs (tv-recommendations), workouts-sync, listening-digest, projects-sync, claude-warmer,
stocks-sync, stock-digest, vercel-daily-redeploy, plex-space-saver, plex-language-fix,
plex-profiles, overrides-audit.

## Checks run

- `npx tsc --noEmit` — passed, no errors.
- `npm test` — full suite green (all existing tests, including gate-coverage and workflow-manifest
  shape tests, pass unaffected since `idempotencyNote` is a purely additive optional field on
  `WorkflowDefinition`, wired end-to-end by the dependency task T613).
- Restarted the daemon (`launchctl kickstart -k gui/$(id -u)/com.ryankrol.localjobs`) and confirmed
  via `GET /api/workflows/:name` that `idempotency_note` is populated and non-empty for 5 sampled
  workflows (places, perfumes, claude-warmer, stocks-sync, overrides-audit) — text matches what was
  authored in the manifest.
- Rebuilt + restarted the dashboard (`npm --prefix dashboard run build` +
  `launchctl kickstart -k gui/$(id -u)/com.ryankrol.localjobs-dashboard`) and used a headless
  Playwright check against `http://localhost:4788/workflows/places` to confirm the note text
  ("Each saved place is tracked individually…") actually renders in the browser DOM below the info
  panel — matches the API response for that workflow.

## Result

done T614
