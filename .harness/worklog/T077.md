# T077: Backlog list — make Done items clickable

## Attempt 1 — 2026-06-22

### What I did

Converted `DoneRow` in `dashboard/app/backlog/page.tsx` (lines 65-73) from a
flat non-interactive div to an inline-expandable component:

- Added `useState(false)` for `expanded` tracking inside `DoneRow`.
- Header row gains `cursor: pointer`, `role="button"`, `aria-expanded`, and a
  `▸`/`▾` chevron to signal interactivity.
- Clicking the row toggles an inline detail area showing `do`, `doneWhen`,
  `dependsOn`, and `tags` on a `var(--panel-2)` tinted background.
- Collapsed default stays one-line-per-task (id · title · ✓ done pill) — no
  change to the Done section's scannability.
- No new CSS needed — reused existing `.pill`, `.muted`, `var(--border)`,
  `var(--panel-2)`.

### Verification

- `npx tsc --noEmit` — clean.
- `npm test` — all tests passed.
- `npm --prefix dashboard run build` — `/backlog` 2.18 kB, clean.
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages PASS (backlog
  included), 0 overflow, 0 boundary crossings at 402px viewport.
