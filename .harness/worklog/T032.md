# T032 — Don't count cancelled runs as failed

## Attempt 1 — 2026-06-22

### What was done

Changed `dashboard/app/page.tsx`:

- Added `'cancelled'` to the `Filter` union type
- Removed `'cancelled'` from the `failed` count — `failed` now only counts `['failed', 'partial']`
- Added a `cancelled` key to `counts` counting only `status === 'cancelled'` runs
- Updated `visiblePipelines` filter: `failed` no longer matches `cancelled`; added `cancelled` branch
- Updated `visiblePipelineRuns` filter: same split
- Added `cancelled: 'Cancelled'` to `filterLabels`
- Added a dedicated Cancelled tile in the stat cards row, between Failed and Stuck

`ui.tsx` required no changes.

### Verification

- `npx tsc --noEmit` — clean
- `npm test` — 16 files, all assertions passed
- `npm --prefix dashboard run build` — clean production build

### Result

done
