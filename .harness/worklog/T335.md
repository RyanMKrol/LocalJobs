# T335 — rename "Services" label to "Integrations"

Pure display-string rename, route/API/filenames unchanged.

## Changes
- `dashboard/app/layout.tsx`: nav link text `Services` → `Integrations` (href stays `/services`).
- `dashboard/app/services/page.tsx`: `<h1>Services</h1>` → `<h1>Integrations</h1>`; lightly reworded the
  sub-description to "Shared external dependencies and tools with cross-job rate limits + quotas...".
- Left `dashboard/scripts/_dashboard-harness.mjs` PAGES/FLOWS `name` fields as-is (cosmetic-only rename
  was optional per spec; skipped to minimize risk — `path` values were already `/services` and unaffected).

## Verification
- `npx tsc --noEmit` — pass.
- `npm test` — 92/92 pass.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — all 27 pages captured, no failures. Read
  `visual-out/services.png`: nav bar reads "Integrations", `<h1>Integrations</h1>` renders, table/panels
  render normally with no overlap/clipping. Read `visual-out/overview-theme-toggle.png` (dark mode): nav
  bar also shows "Integrations" correctly, no regression elsewhere on the page.

No route, folder, API endpoint, SQLite table, or `src/services/*.service.ts` filename was touched.
