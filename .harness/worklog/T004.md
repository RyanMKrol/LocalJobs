# T004 — Publish the perfumes job code

## Goal
Make the perfumes job CODE public (deps T002/T003 done). Remove only the
`src/jobs/perfumes/` line from `.gitignore`; track the `*.ts` files +
`profile.template.md`; keep `data/` (incl. `chrome-profile/`) private.

## What I did
- Confirmed deps: T002 (template self-contained) + T003 (places published) both `done`.
- Verified the perfumes code is publishable: no absolute `/Users/...` paths, no
  hardcoded secrets — `config.ts` reads everything from `process.env`, and
  `templatePath` resolves to the in-project `profile.template.md`
  (`PERFUMES_TEMPLATE_PATH` overridable). The only grep hits for "cookie" etc.
  are code comments about Cloudflare clearance, not credentials.
- `.gitignore`: removed the `src/jobs/perfumes/` ignore line; merged the comment
  with the places one. Kept `src/jobs/**/data/` and all secret rules.
- Staged 16 `.ts` files + `profile.template.md` EXPLICITLY (by glob, no `git add -A`).
- README: privacy note now lists places **and** perfumes; layout tree adds the
  `perfumes/` entry; added an "Example pipeline: perfumes" section
  (find-url → fetch → parse → build, data/ stays gitignored).
- `.harness/LIMITATIONS.md` already carries the "publishing exposes Fragrantica
  scraping" trade-off (accepted) — no change needed.

## Verification (observed)
- `npx tsc --noEmit` → clean (printed `TSC OK`).
- `npm test` → `✓ all tests passed` (incl. perfumes config + registry: 9 jobs,
  4 services, 2 pipelines).
- `git check-ignore src/jobs/perfumes/data/...` + `.../chrome-profile/Local State`
  → still ignored. `git check-ignore` on `fetch.ts` / `profile.template.md`
  → nothing (now trackable).
- Privacy guard: `git diff --cached --name-only | grep -iE 'data/|chrome-profile|.env|credential|.pem|.key'`
  → NONE. Nothing under `data/` staged.

## Result
done — code published, data still private, typecheck + tests green, committed (not pushed).
