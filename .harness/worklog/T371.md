# T371

Added `src/jobs/CLAUDE.md`, a directory-scoped doc mirroring `src/services/CLAUDE.md`'s
pattern, stating the "every adjacent job pair needs a validation gate" rule as a hard
requirement, with the trivial `check(): { ok: true }` minimum-bar carve-out explicit.
Names `src/core/gate-coverage.test.ts` as the mechanical enforcement (confirmed by
reading the file — it walks every registry-loaded workflow's DAG and asserts every
edge has a matching gate via `deriveGates`, failing `npm test` on a miss). Points to
`src/jobs/perfumes/stages/fetch.job.ts` as the shortest wiring example, plus
`src/jobs/places/contracts.ts`, `src/jobs/perfumes/contracts.ts`, and the newer
`stocks-sync`/`projects-sync`/`workouts-sync`/`stock-digest` contracts files.

Also updated root `CLAUDE.md`'s "Validation gates between workflow stages" bullet with
a one-line pointer to the new `src/jobs/CLAUDE.md` and a mention of the
`gate-coverage.test.ts` mechanical enforcement, since that section was written before
the test existed.

Verification: `npx tsc --noEmit` clean, `npm test` — 146/146 passing. No code changes,
pure documentation.

Result: done
