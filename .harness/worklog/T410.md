# T410 — stocks-watch: add detail to the ::notified ledger rows

## What changed

`src/workflows/stocks-sync/stages/stocks-watch.ts`, inside `runStocksWatch`'s per-position loop:

- Fresh-breach `markWorkItem(WATCH_JOB, notifiedKey, 'success')` now passes a `detail` object
  (`name`, `ticker`, `account`, `gainPct`, `averageBuyPrice`, `currentPrice`) describing the breach
  just recorded as notified.
- Reset `markWorkItem(WATCH_JOB, notifiedKey, 'skipped')` now passes a `detail` object (`name`,
  `ticker`, `account`, `gainPct`) describing why the notified-flag was reset.
- The per-run check row (`markWorkItem(WATCH_JOB, key, 'success', { detail: { gainPct, breaching } })`)
  was left unchanged — already compliant.
- `stocks-snapshot.ts` and `stocks-notify.ts` were left untouched per the spec (already compliant /
  deliberately record no ledger rows).

`src/workflows/stocks-sync/stages/stocks-watch.test.ts`: added an assertion in test (b) (fresh
breach) that reads the `::notified` ledger row via `getWorkItem` and confirms `detail` is non-null
and contains `gainPct` (number) + `ticker`.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — full suite green, including `src/workflows/stocks-sync/stages/stocks-watch.test.ts`
  (all subtests pass, including the new detail assertion).
- Spot-checked the updated file: neither `notifiedKey` call is missing a `detail` object; the
  per-run check row and both file-output stages (`stocks-snapshot.ts`, `stocks-notify.ts`) are
  unchanged.

## Result

done T410
