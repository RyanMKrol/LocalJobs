# T324 — strengthen `.table-group-header` contrast

## Change
`dashboard/app/globals.css` — `.table-group-header td` now:
- `background: color-mix(in srgb, var(--accent) 16%, var(--panel-2))` (was flat `var(--panel-2)`)
- `border-top: 1px solid var(--border)`
- `border-bottom: 1px solid color-mix(in srgb, var(--accent) 40%, var(--border))`
- kept `font-weight: 600` as the secondary cue

CSS-only; no changes to `page.tsx` (row HTML/colSpan untouched). Since both
`MissingSeasonsManager` and `MovieGapsManager` render `<tr className="table-group-header">`,
this single rule fixes both tables.

## Checks
- `npx tsc --noEmit` — pass.
- `npm test` (root) — 91/91 pass.
- `npm --prefix dashboard run build` — succeeds.
- `node dashboard/scripts/visual-check.mjs` — all 23 pages pass. Opened
  `workflow-movie-recs.png` and `workflow-missing-tv-seasons.png` (light/sunny-8bit
  default mode): the "Ignore all" group-header rows (collection name / show name)
  now read as a clearly distinct warm-orange band with a visible top/bottom border,
  separate from both the page background and the plain data rows beneath — not just
  bold text on a near-identical fill as before.
- Manual dark-mode check: started `next start -p 4798` against the live daemon
  API, drove Playwright with `localStorage.setItem('localjobs.mode','dark')` before
  navigating to `/workflows/movie-recommendations`, and screenshotted the Franchise
  Gaps table (temp screenshots, not committed). The group-header rows show a clear
  warm brown/tan band against the near-black dark-mode data rows, with the border
  visible — contrast holds in both modes.
- `node dashboard/scripts/mobile-check.mjs` — all 15 pages pass, 0px horizontal
  overflow, 0 boundary-crossing elements (no regression from the new border).

## Harness artifacts
No `PAGES`/`FLOWS` update needed — this is a pure CSS style change to an existing,
already-captured row (`workflow-movie-recs` / `workflow-missing-tv-seasons` in
`_dashboard-harness.mjs` already cover the affected tables); no new interactive
state was introduced.
