# T119 worklog — Workflows view: click stuck count → StuckPopover

## Attempt 1 (2026-06-23)

### What was done

- **`src/api/server.ts`**: Extended `GET /api/stuck` to support `?workflow=<name>` query param. When present, resolves the workflow's member jobs via `getWorkflowJobs` and filters stuck items to those jobs. Returns 404 for unknown workflow names.

- **`dashboard/app/lib/api.ts`**: Added `api.stuckForWorkflow(workflow: string)` — calls `GET /api/stuck?workflow=<name>`. Existing `api.stuck(job?)` unchanged (no existing callers broken).

- **`dashboard/app/workflows/page.tsx`**: 
  - Added `useState` for `openWorkflow` (which workflow's popover is open) and `popoverItems` (the fetched stuck items).
  - Made the stuck badge a `<button class="btn-link">` that calls `openStuck(p.name)` to fetch and open the popover when count > 0.
  - Renders `<StuckPopover>` with `scope={{ type: 'workflow', workflow: openWorkflow }}` when open.
  - `onAction` callback re-fetches stuck items for the same workflow after per-item or bulk actions.

- **`dashboard/app/globals.css`**: Added `.btn-link` utility — a button with no chrome (no background/border/padding) that looks like inline text, with dotted underline for affordance.

### Verification

- `npx tsc --noEmit` — clean
- `npm test` — 21 files, all assertions passed
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages PASS at 402px viewport

### Result: done
