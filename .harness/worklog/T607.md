## T607 — 2026-07-16

### What I did
`StageIoLists.tsx`'s Inputs column always rendered a fixed "No inputs — this is
the root stage." empty-state text, even for a non-root stage whose predecessor
simply recorded zero ledger rows this run. Fixed by distinguishing the two
cases using the `predecessorJobs` array already returned by the stage-io API:

- Extracted a small pure helper, `inputsEmptyText(predecessorJobs: string[])`
  (exported for testability), used by `StageIoBlock`'s Inputs column:
  - `predecessorJobs.length === 0` → "No inputs — this is the root stage."
    (the DAG's genuine root wave).
  - `predecessorJobs.length > 0` → "No inputs recorded this run." (reusing the
    exact string already used by `StageIoOverallBlock`'s Inputs empty state).
- `StageIoOverallBlock` (the "Overall" tab) was already using "No inputs
  recorded this run." unconditionally — left unchanged (it's not exhibiting the
  root-stage bug the task describes).

### Tests / checks
- Added unit tests for `inputsEmptyText` in `StageIoLists.test.ts` (empty
  predecessors → root text; non-empty predecessors → the recorded-this-run
  text).
- `npx tsc --noEmit` — clean.
- `npm test` — full suite green (109 test files). Note: an unrelated,
  pre-existing flaky assertion in `src/api/server.test.ts`
  ("POST /api/cache/clear {}: clears every remaining service") intermittently
  fails when the full suite runs back-to-back — confirmed by stashing my
  changes and reproducing the same flake on a clean `main` checkout, and by
  running `src/api/server.test.ts` alone repeatedly (always passes). Unrelated
  to this task; a clean full-suite run was captured for the final state.
- `npm --prefix dashboard run build` — clean.
- `node dashboard/scripts/mobile-check.mjs` — all 32 pages pass.

### Visual verification (required — this changes rendered UI)
Updated `dashboard/scripts/_dashboard-harness.mjs`'s `missing-movies` stage-io
fixture: `movie-gaps-notify` now records `inputs: []` (was previously a
non-empty franchise-gaps row) to reproduce the exact bug scenario the task
describes — a non-root stage whose predecessor recorded no rows this run.
Added two new FLOWS entries and ran `node dashboard/scripts/visual-check.mjs`,
then read both screenshots:

- `stage-io-root-stage-empty-inputs.png` (clicks the `movie-snapshot` chip,
  the DAG's real root, zero predecessors, zero inputs): Inputs column reads
  **"No inputs — this is the root stage."** — correct, unchanged behavior.
- `stage-io-non-root-empty-inputs.png` (clicks the `movie-gaps-notify` chip,
  predecessor `franchise-gaps`, zero input rows recorded this run): Inputs
  column reads **"No inputs recorded this run."** — this is the fix; previously
  this stage would have incorrectly shown the root-stage text.

Both screenshots render cleanly — no blank/overlapping/clipped content. The
one hard-error capture the visual-check script reports
(`workflow-run-skipped`) is a pre-existing failure unrelated to this change
(confirmed identical on a clean `main` stash).

### What remains
Nothing — task complete. (Per the spec, `movie-gaps-notify`'s real-world case
is expected to self-resolve to the non-empty-inputs path once the separate
`recs-ledger-rebuild-h` task lands; this task fixes the general mislabeling
for any stage that hits the same empty-but-not-root state, which is what the
fixture/screenshots above demonstrate.)
