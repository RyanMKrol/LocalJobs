# T083 worklog

## Task
On the individual run page (/runs/[id]), the page header was a generic `<h1>Run</h1>`. Changed it to display `run.job_name` instead, with a small muted "run" subtitle below for context.

## Change
`dashboard/app/runs/[id]/page.tsx` line 42: wrapped the `<h1>` in a `<div>` and replaced the literal "Run" with `{run.job_name}`. Added `<span className="muted" style={{ fontSize: 12 }}>run</span>` as a subtitle. Run ID and StatusBadge remain in the same row.

## Verification
- `npx tsc --noEmit` — passed
- `npm test` — 19 test files, all passed
- `npm --prefix dashboard run build` — built successfully
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages passed, including /runs/1
