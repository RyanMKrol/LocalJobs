# T121 worklog — Remove backlog DAG view (list-only)

## What was done

Removed the DAG view from the Backlog page entirely. The page is now list-only.

### Files changed

**`dashboard/app/backlog/page.tsx`**
- Removed `import { BacklogDag }` from Dag.tsx
- Removed `view` state (`'list' | 'dag'`)
- Removed `selectedId` state and `selectedTask` derived value
- Removed `DagDetailPanel` component (only used in the DAG branch)
- Removed the List/DAG view toggle (`<div className="view-toggle">`)
- Removed the conditional `{view === 'dag' ? ... : ...}` — replaced with the list content directly
- Caret-style bar is now always shown (was previously gated on `view === 'list'`)

**`dashboard/app/components/Dag.tsx`**
- Removed `BacklogTask` from imports (no longer needed)
- Removed `computeBacklogWaves` helper function
- Removed backlog layout constants (`BL_NODE_W`, `BL_NODE_H`, `BL_COL_GAP`, `BL_ROW_GAP`, `BL_PAD`, `BL_COL`)
- Removed `BacklogDag` export and its entire body (~100 lines)
- The workflow `Dag` export and all shared `.dag*` CSS remain intact

**`dashboard/app/globals.css`**
- Removed `/* backlog dag node states */` block (`.dag-node.done/needs-human/selected`, `button.dag-node:hover`)
- Removed `/* backlog DAG (T103) */` block (`.backlog-graph`, `.backlog-graph-edges`, `.backlog-edge`, `.backlog-gnode*`, `.backlog-node-*`)
- Removed `/* legend dots */` block (`.dag-legend.*`) — only used by the removed DAG legend
- Removed `/* list/dag view toggle */` block (`.view-toggle`) — only used by the removed toggle
- Removed the mobile comment about `.backlog-graph` scrolling horizontally

**`README.md`** — updated Backlog page description to list-only (removed DAG view mention)

## Verification

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed
- `npm --prefix dashboard run build` — clean build
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass

## Notes

T076 and T103 improved the backlog DAG; this task deliberately removes it as it added little value
given the low number of inter-task dependencies. The list view (T077 clickable done items,
T113 section-header carets) is preserved intact.
