## T562 — extract shared weekKey/dayKey into src/core/dates.ts

2026-07-14

### What I did
- Created `src/core/dates.ts` exporting `weekKey(date: Date): string` (ISO-8601 week key)
  and `dayKey(date: Date): string` (UTC calendar-day key) — the single shared definitions,
  copied verbatim from the three identical `weekKey` copies (stock-digest/lib.ts,
  plex-space-saver/stages/scan.ts, overrides-audit/stages/scan.ts) and the one `dayKey`
  definition (stocks-sync/stages/stocks-snapshot.ts).
- `stock-digest/lib.ts`: now imports `weekKey` from `../../core/dates.js` and re-exports it
  (`weekLabel` still derives from it) — every existing `from '../lib.js'` import (stages +
  tests) keeps resolving unchanged.
- `plex-space-saver/stages/scan.ts`: imports `weekKey` from core and re-exports it, so
  `scan.test.ts`'s `import { weekKey } from './scan.js'` keeps resolving.
- `overrides-audit/stages/scan.ts`: same treatment — imports + re-exports `weekKey`, so
  `scan.test.ts`'s `import { formatAge, weekKey } from './scan.js'` keeps resolving.
- `stocks-sync/stages/stocks-snapshot.ts`: imports `dayKey` from core and re-exports it, so
  `stocks-fetch.ts`/`stocks-resolve-names.ts`/their tests, which import `dayKey` from
  `./stocks-snapshot.js`, keep resolving.
- `stock-digest/CLAUDE.md`: updated the `lib.ts` file-map line to note `weekKey` is now a
  re-export from `src/core/dates.ts`.
- Wrote `src/core/dates.test.ts`: same-week Monday/Friday equality, plus three ISO-week
  year-boundary cases (2025-01-01 → 2025-W01; 2025-12-29 → 2026-W01; 2027-01-01 → 2026-W53)
  and two `dayKey` cases (time-of-day ignored; two different UTC days differ).

### Skipped (deliberately out of scope for this task)
- `monthKey` was NOT touched. `listening-digest`'s `monthKey(date: Date)` and
  `workouts-sync`'s `monthKey(year: number, monthIndex0: number)` have different
  signatures — per the task spec's explicit guardrail, mismatched signatures must not be
  forced together. Neither file is in this task's scope list either.
- Root `CLAUDE.md`'s file map: not edited — it is not in this task's scope list, and the
  file map's `src/core/` row doesn't enumerate individual modules (only `src/core/types.ts`
  etc. get their own row for contract-level types), so there was nothing load-bearing to
  update there; flagging in case a human wants to add an explicit `src/core/dates.ts` row
  later as a scope-out follow-up.

### Verification
- `npx tsc --noEmit` — clean, no errors.
- `npm test` — all suites pass (previously-existing weekKey/dayKey assertions in
  stock-digest/plex-space-saver/overrides-audit/stocks-sync tests still pass unchanged,
  confirming no key-value regressions), plus the new `src/core/dates.test.ts` suite (6
  tests, all green).
- `grep`-confirmed exactly one `function weekKey` and one `function dayKey` definition
  remain in the tracked source tree (both in `src/core/dates.ts`); every other reference is
  now an import/re-export.

### What remains
Nothing outstanding for this task's scope.
