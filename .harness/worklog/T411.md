# T411 — tv-recs-notify: enrich success detail

## What changed

`src/workflows/tv-recs/stages/tv-recs-notify.ts` — the per-notified-rec `markWorkItem`
success call now records `lens`, `genre`, `reason`, `tmdbRating`, and `tmdbUrl` (via the
existing `tmdbLink()` helper) in `detail`, alongside the existing `markdown`/`title`/
`year`. `name` no longer duplicates `lens` in a display string now that it's a structured
field.

## Tests

Extended `src/workflows/tv-recs/stages/tv-recs-notify.test.ts` run-1 assertions to query
the recorded `work_items.detail` row directly (via `db` from `../../../db/index.js`) and
assert `reason`/`genre`/`tmdbRating`/`lens`/`tmdbUrl` match the source `Recommendation`
fixture.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 187/187 pass, including the updated tv-recs-notify assertions.
- Spot-check: confirmed the enriched detail shape via the extended unit test (direct
  `work_items` row read) rather than a live workflow run — no Plex/TMDB/paid calls
  needed since the notify stage's ledger-write logic is fully exercised by the existing
  hermetic fixture-driven test.

## Result

done T411
