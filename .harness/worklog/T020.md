# T020 — Dashboard: remove the Jobs tab

## What was done

- Removed `<a href="/jobs">Jobs</a>` from the nav in `dashboard/app/layout.tsx`.
- Deleted `dashboard/app/jobs/page.tsx` (the flat job list).
- Updated the back link in `dashboard/app/jobs/[name]/page.tsx` from `← Jobs` to `← Pipelines` (since the jobs list no longer exists).
- Updated `dashboard/app/components/Dag.tsx`: DAG nodes now always have a link — when a `runIdByJob` entry exists they link to `/runs/{runId}` (run-detail, existing behaviour); otherwise they fall back to `/jobs/{name}` (job-detail page), making every node in the pipeline definition view clickable.
- Added a "Standalone jobs" section to `dashboard/app/pipelines/page.tsx`: polls `api.jobs()` (5 s), filters to `!j.pipeline`, and renders a table with name/link, schedule, last-run status, and next-run time — identical pattern to the pipelines table.

## Verification

- `npx tsc --noEmit` → clean
- `npm test` → 13 files, all passed
- `npm --prefix dashboard run build` → clean, `/jobs` route gone, `/jobs/[name]` present

## Result

done
