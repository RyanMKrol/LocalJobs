# T439

Rewrote the `description` field of the 3 stock-digest job files into real paragraph-length prose,
grounded in `src/workflows/stock-digest/CLAUDE.md`'s per-stage detail and each stage's own code:

- `stock-portfolio-snapshot.job.ts` — now explains it's the second of two Trading212 stages, reads
  raw-portfolio.json from stock-portfolio-fetch, resolves ISIN→ticker via OpenFIGI through the
  rate-limited trading212-instruments service, why the resolved ticker matters downstream, and the
  soft-skip behavior on empty/missing credentials.
- `stock-sector-lookup.job.ts` — now explains the Finnhub industry lookup, ticker preference order
  (resolved real-world ticker over raw Trading212 ticker) and why, per-ticker ledger idempotency, and
  the soft-skip when `FINNHUB_API_KEY` is unset.
- `stock-digest-build.job.ts` — now explains the fan-in from both upstream stages, the code-computed
  winners/losers ranking and sector-diversification section, the facts-JSON persisted before the
  Claude call, the soft ticker-hallucination cross-check, and per-ISO-week idempotency.

No behavior changes — description strings only.

## Verification

- `npx tsc --noEmit` — clean, no output.
- `npm test` — 213/213 passing, 0 failures.

Pure content authoring task; no paid API calls needed for verification.
