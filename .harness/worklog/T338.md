# T338 — sector/diversification breakdown for stock-digest via Finnhub

## What was built
- `src/services/finnhub.service.ts` — new self-contained `ServiceDefinition` for Finnhub's free
  company-profile API, reading `FINNHUB_API_KEY` from env with generous free-tier
  rate/daily/monthly defaults (30/min, 500/day, 5000/month), all env-overridable.
- `src/jobs/stock-digest/stages/stock-sector-lookup.ts` (+ `.job.ts` + `.test.ts`) — new stage:
  reads `stocks-sync`'s `portfolio.json`, dedupes to distinct tickers, calls
  `callService('finnhub', ...)` against `GET /stock/profile2` for each not-yet-resolved ticker
  (idempotent per ticker via the `work_items` ledger), writes `data/out/sectors.json`. Soft-skips
  cleanly (WARN, no crash) when the portfolio is missing/empty or `FINNHUB_API_KEY` is unset. A
  failed per-ticker lookup is recorded `failed` (retryable next run) without aborting the stage.
- `src/jobs/stock-digest/stock-digest.workflow.ts` — now a two-stage DAG:
  `stock-sector-lookup` → `stock-digest-build` (`dependsOn`).
- `src/jobs/stock-digest/config.ts` — added `sectorsJsonPath`.
- `src/jobs/stock-digest/stages/stock-digest-build.ts` — reads `sectors.json`, adds
  `sectorBreakdown()` (% of total portfolio value per resolved Finnhub industry, excluding
  unresolved tickers), threads it through `buildFacts`/`StockDigestFacts`, and
  `buildDigestPrompt` adds a "## Diversification" section (with an explicit note that this is
  Finnhub's own industry classification, not a formal GICS sector) only when at least one sector
  is resolved — otherwise the section/breakdown is simply omitted, no crash.
- Tests: `stock-sector-lookup.test.ts` (portfolio/sector-map reads, per-ticker idempotency
  including a new-ticker-appears case, dedupe across accounts, missing-key/missing-portfolio
  soft-skip, failed-lookup non-crash), plus new cases in `stock-digest-build.test.ts` for
  `sectorBreakdown` math and the end-to-end degrade-gracefully / include-section paths.
- Docs updated: root `CLAUDE.md` (stock-digest section rewritten for the two-stage DAG + finnhub
  service), `README.md` (workflow list entry), `.env.example` (`FINNHUB_API_KEY` + optional
  rate/cap overrides, with a link to Finnhub's free-tier signup).

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 127/127 passing (all suites), including the new stock-digest-build and
  stock-sector-lookup tests.
- No live Finnhub/Trading212/Claude calls made — all tests use injected fetch/claude stubs and
  the scratch DB, per the repo's paid/external-call verification convention.
- `src/services/CLAUDE.md` documents the trading212 read-only rule specifically; it is NOT in
  this task's scope list, so it was intentionally left untouched (the finnhub service is
  metadata-only, not a broker, so that file's rule doesn't apply to it anyway).

## Not done / follow-ups
- None — the task's Done-when criteria are all met.
