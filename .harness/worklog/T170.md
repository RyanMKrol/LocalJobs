# T170 worklog

## What was done

- **Empty state**: Replaced the awkward `<div className="panel">` wrapper around the "No audit has run yet" placeholder with a plain `<p className="muted">`, matching the rest of the dashboard's muted placeholder style.
- **Scrollable container**: Wrapped the active gap panels + ignored panel in a `<div className="movie-gaps-scroll">` that caps at `max-height: 520px` with `overflow-y: auto`, so a long list scrolls within the section rather than sprawling down the page. Used `flex-direction: column; gap: 12px` to replace the previous implicit spacing between panels.
- **Button styling**: Changed the bare `<button>` Ignore buttons to `<button className="btn btn-sm">`, picking up the site's standard themed button styling (background: accent, border-radius, hover/disabled states, all CSS-variable-driven so they adapt correctly to every theme).
- **CSS**: Added `.movie-gaps-scroll` rule to `globals.css` alongside the existing `.io-table-scroll` (same pattern, T148).

## Verification

- `npm --prefix dashboard run build` — passed, no errors.
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass, 0px overflow.
- Behaviour unchanged: grouping by collection, ignore action, active/ignored split, poll — untouched.
