# T022 worklog

## Attempt 1 — 2026-06-21

### What was done

Implemented grouped/collapsed member-runs list in `dashboard/app/pipeline-runs/[id]/page.tsx`.

**Design:**
- `groupByStage()` helper groups the `Run[]` array by `job_name`, reversing each group so latest run is first.
- Main table shows one row per stage (the latest run).
- If a stage has older runs (prior cycles), a small expand button shows `▼ +N`; clicking it reveals the older runs inline with reduced opacity.
- Expanded rows show `▲ hide` to collapse again.
- `useState<Set<string>>` tracks which stages are expanded.

### Checks

- `npx tsc --noEmit` — clean
- `npm test` — all tests passed (9 jobs, 4 services, 2 pipelines)
- `npm --prefix dashboard run build` — clean (7 static pages generated)

### Result

Committed as `cfa942e` — T022 done.
