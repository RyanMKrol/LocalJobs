# T583 — Add inputKeysService field to JobDefinition + retrofit onto every job that declares inputKeys()

## Summary

Added the optional `inputKeysService?: string` field to `JobDefinition` in `src/core/types.ts`, with
comprehensive doc comment explaining its purpose: naming the ServiceDefinition that the job's
inputKeys() call is routed through, proving it queries the LIVE external source rather than this
workflow's own prior output. Retrofitted the field onto all 5 jobs that declare `inputKeys()`:

1. **perfumes/find-url.job.ts** → `inputKeysService: 'dynamodb'`
2. **places/resolve.job.ts** → `inputKeysService: 'fs'`
3. **plex-language-fix/discover.job.ts** → `inputKeysService: 'plex'`
4. **projects-sync/github-sync.job.ts** → `inputKeysService: 'github'`
5. **plex-profiles/build.job.ts** → `inputKeysService: 'plex'`

Verified that plex-profiles' `resolveInputKeys()` already routes through `callService('plex', ...)`,
so no additional callService wrap was needed (T474/T578 already covered it).

## Documentation updates (in the same commit)

- Updated all 5 touched workflows' own CLAUDE.md files to note the `inputKeysService` value on their
  root stages' `inputKeys()` descriptions
- Updated root `src/workflows/CLAUDE.md` with a new section documenting the field and its
  enforcement (T488, a later task)

## Verification

- `npx tsc --noEmit` → ✅ green
- `npm test` → ✅ all tests passed (10 suites, 10 tests, 0 failures)

The infrastructure (callService routing) is already in place for all 5 jobs — this task is a pure
field addition + documentation, exactly as scoped.
