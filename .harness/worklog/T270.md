# T270 worklog

## What was done

Migrated `dashboard/app/workflows/page.tsx` to use shared components from T268:

- Added imports for `RunButton` and `Pill` from `../components/`.
- Replaced `<span className={`pill ${p.enabled ? 'on' : 'off'}`}>` with `<Pill kind={...}>`.
- Replaced the inline run button conditional (two `<button className="btn btn-run">` variants) with
  `<RunButton isRunning={...} onClick={...} label="▶ Run" />`.

No visual or behavioural changes — purely a like-for-like swap.

## Checks

- `npx tsc --noEmit`: ✓ clean
- `npm test`: ✓ 74 pass, 0 fail
- `npm --prefix dashboard run build`: ✓ clean
- `node dashboard/scripts/visual-check.mjs`: ✓ all 16 pages captured

## Visual confirmation

Inspected `dashboard/scripts/visual-out/workflows.png`. The page renders identically:
- "on" pill (green outlined) and "off" pill (grey outlined) are painted correctly in the Enabled column.
- "▶ Run" buttons are painted in blue in the action column for both rows.
- No blank/overlapping/clipped elements.

No changes needed to `_dashboard-harness.mjs` (selectors/surface unchanged).
