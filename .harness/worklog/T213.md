# T213 worklog

## What was done

Moved the status badge out of the flex `<div className="row">` in `dashboard/app/page.tsx` so it always renders on its own line below the workflow title, regardless of title length. Replaced the row+spacer pattern with a plain block-level title `<a>` followed by a `<div style={{ marginTop: 4 }}>` wrapping the badge link.

No CSS changes were needed — the badge renders as an inline element inside a block container, which naturally sits below the title.

## Verification

- `npm --prefix dashboard run build` — green
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
- Visual inspection: short-titled workflows (`perfumes`, `places`) now show the pill below the title, consistent with long-titled ones (`missing-tv-seasons`).
