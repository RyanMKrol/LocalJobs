2026-07-14 — T555

## What I did
Implemented the full task in the working tree, then reverted it (see "Blocked" below) so the
tree is left clean:
1. Ported `isFmEmpty`/`renderFmValue` (MarkdownModal.tsx:26-42) into
   `dashboard/app/components/OutputRenderer.tsx`, wired `renderFmValue` into
   `MarkdownOutputBody`'s frontmatter `<dd className="md-fm-val">` cell (previously the bare
   `{v}`). `.md-fm-null` already exists in globals.css — no CSS change needed.
2. Confirmed zero importers of `MarkdownModal` (`grep -rln "MarkdownModal" dashboard --include="*.tsx" --include="*.ts"`
   returned nothing beyond the module + its own test file), then deleted
   `dashboard/app/components/MarkdownModal.tsx` and its companion
   `dashboard/app/components/MarkdownModal.test.ts` (a test file — always in scope, and it must
   go with the component since it imports `parseFrontmatter` from it).
3. Reworded the CLAUDE.md shared-components section (`renderOutputBody`/`OUTPUT_RENDERERS`
   entry) to describe `renderFmValue`/`isFmEmpty` as living in `OutputRenderer.tsx`, removed the
   `<MarkdownModal>` bullet, and reworded the T458-era "forced content through MarkdownModal"
   line to not reference the (now nonexistent) component by name.
4. Added a new `OutputRenderer.test.ts` assertion: renders a `markdown`-format output whose
   frontmatter has an empty/null value and a JSON-array value, and asserts the HTML contains the
   `md-fm-null` class + `null` placeholder text for the empty value, and the comma-joined array
   text (`a, b`) with no raw `["a","b"]` JSON in the output.

## Checks run (all green on the above diff, before reverting)
- `npx tsx --test dashboard/app/components/OutputRenderer.test.ts` — 7/7 pass, including the new
  frontmatter assertion.
- `npx tsc --noEmit` (root) — clean.
- `npm test` (root) — 9/9 pass.
- `npm --prefix dashboard run build` — succeeded.
- `node dashboard/scripts/visual-check.mjs` — 58/59 captures pass; the one failure
  (`workflow-run-skipped`, a `.rf-dag-node` selector timeout) is pre-existing and unrelated —
  reproduced identically on `main` before this change (confirmed via `git stash` + a baseline
  run). Visually inspected `workflow-output-section-popover.png`: the frontmatter popover
  (opened via the existing FLOWS entry) renders the `NAME` key/value row and markdown body
  correctly through the now-ported `MarkdownOutputBody` path — no regression.

## Blocked — scope gap
`dashboard/package.json`'s `"test"` script hardcodes
`../node_modules/.bin/tsx app/components/MarkdownModal.test.ts` as one of four chained test
invocations:
```
"test": "../node_modules/.bin/tsx app/components/OutputRenderer.test.ts && ... && ../node_modules/.bin/tsx app/components/MarkdownModal.test.ts"
```
This script is what CI actually runs (`.github/workflows/ci.yml:44`, `npm --prefix dashboard
test`). Deleting `MarkdownModal.test.ts` (required by the task's own Done-when: "grep finds zero
importers/references to MarkdownModal anywhere") makes this script fail outright
(`ERR_MODULE_NOT_FOUND`), which I reproduced locally — `npm --prefix dashboard test` crashes
immediately once the file is gone. Fixing it means removing the `MarkdownModal.test.ts` clause
from `dashboard/package.json`'s `test` script, but `dashboard/package.json` is NOT in this task's
scope list (`dashboard/app/components/MarkdownModal.tsx`, `OutputRenderer.tsx`,
`OutputRenderer.test.ts`, `CLAUDE.md` only), and it is not a test file itself (per the scope
note, only test files are always-in-scope).

Per the harness instructions ("If you genuinely need a file that is not listed, do NOT edit it:
record `failed:blocked` ... so a human can fix the scope"), I reverted all working-tree changes
(clean `git status`) and am not committing. A human needs to add `dashboard/package.json` to
T555's scope (or split out a follow-up task) so the `test` script's `MarkdownModal.test.ts`
clause can be dropped in the same change as the deletion.

---
failed:blocked T555 — agent reported failed:blocked — — deleting MarkdownModal.tsx/.test.ts (required by the task) breaks dashboard/package.json's "test" script, which hardcodes app/components/MarkdownModal.test.ts and is what CI runs (npm --prefix dashboard test) — that file is out of T555's scope; needs the scope widened to include dashboard/package.json
