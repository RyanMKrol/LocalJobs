# T582 — 2026-07-15

Re-attempt of T555 (blocked because dashboard/package.json was out of scope). Same
work, with dashboard/package.json now in scope so the fix lands in one change.

## What I did
1. Ported `isFmEmpty`/`renderFmValue` from `MarkdownModal.tsx` into
   `dashboard/app/components/OutputRenderer.tsx`, and wired `renderFmValue` into
   `MarkdownOutputBody`'s frontmatter `<dd className="md-fm-val">` cell (previously
   rendered the raw string). No CSS change needed — `.md-fm-null` already existed.
2. Confirmed zero other importers of `MarkdownModal` (`grep -rln MarkdownModal`
   only matched `MarkdownModal.tsx`/`.test.ts`, `CLAUDE.md`, and
   `dashboard/package.json`'s test script), then deleted
   `dashboard/app/components/MarkdownModal.tsx` and
   `dashboard/app/components/MarkdownModal.test.ts`.
3. Edited `dashboard/package.json`'s `test` script to drop the
   `MarkdownModal.test.ts` clause, keeping the other three chained invocations
   (`OutputRenderer.test.ts`, `StageIoLists.test.ts`, `ui.test.ts`) unchanged and
   in order.
4. Updated `CLAUDE.md`'s shared-components section: removed the `<MarkdownModal>`
   bullet entirely and reworded the `renderOutputBody`/`OUTPUT_RENDERERS` bullet
   so it no longer names `MarkdownModal` (now says "a full-preview popover
   component") and documents that `parseFrontmatter`/`isFmEmpty`/`renderFmValue`
   live only in `OutputRenderer.tsx` (T582). `grep -rn MarkdownModal` across
   `dashboard/` + `CLAUDE.md` (excluding `data/repos`) returns zero hits.
5. Added an assertion to `OutputRenderer.test.ts` ("markdown format: frontmatter
   renders null placeholder for empty values and joins JSON arrays") — renders a
   markdown-format output whose frontmatter has a `null` value and a JSON-array
   value, asserting the HTML contains the `md-fm-null` "null" placeholder for the
   empty value and the array joined as `a, b` (not the raw `["a","b"]` JSON text).
   Ran it in isolation first to confirm it exercises real behaviour (all 7
   OutputRenderer tests green, including the new one).

## Checks run
- `npx tsc --noEmit` — clean.
- `npm --prefix dashboard test` (the full chained script: OutputRenderer →
  StageIoLists → ui, no MarkdownModal clause) — 25/25 passing.
- `npm --prefix dashboard run build` — succeeds, all routes generated.
- `grep -rn 'MarkdownModal' dashboard/ CLAUDE.md ... | grep -v data/repos` — zero
  hits.
- `node dashboard/scripts/visual-check.mjs` — 58/59 captures passed; the one
  failure (`workflow-run-skipped`, a `page.waitForSelector` timeout on
  `.rf-dag-node`) is an unrelated DAG-flow node-render timeout on a synthetic
  "skipped" run fixture, not touching markdown/frontmatter rendering at all.
  Visually inspected `workflow-output-section-popover.png` and
  `workflow-run-output-table.png` — the output preview popover (backed by
  `OutputRenderer`'s `MarkdownOutputBody`) renders correctly: title, frontmatter
  key/value row (`NAME` → `A Resolved Place`), and markdown body, all painted
  as expected. No blank/overlapping/clipped content.
- (Mid-task, a `git stash`/build/`stash pop` sequence I ran to compare against a
  pre-change baseline got killed by a tool timeout before the `pop` ran, briefly
  reverting the working tree to the stashed state. Caught it via the file-change
  system reminder and ran `git stash pop` to restore all edits, then re-ran
  `tsc`/`npm test`/`npm run build` to confirm everything was still green after
  restoring — all passed.)

## Result
`MarkdownModal.tsx`/`.test.ts` deleted; `renderFmValue`/`isFmEmpty` now live only
in `OutputRenderer.tsx`; `package.json`'s test script and `CLAUDE.md` both match
reality.
