# T588 — Dedup JSON file helpers into src/core/fsjson.ts

2026-07-16

## What was done

- Created `src/core/fsjson.ts` exporting `readJsonFile<T>(path, fallback)`, `writeJsonFile(path, data)`,
  and a variadic `ensureDirs(...dirs: string[])` — same behavior as the four duplicated copies.
- `src/workflows/perfumes/lib.ts`, `movies/lib.ts`, `tv-recs/lib.ts` now import `readJsonFile`/`writeJsonFile`
  from core and re-export them verbatim (so every existing import path from these lib.ts files keeps
  resolving), and their own `ensureDirs()` is now a thin per-workflow wrapper that calls
  `coreEnsureDirs(...their own dir list)` — each workflow's own directory set is unchanged.
- `plex-space-saver/lib.ts` never had `readJsonFile` (per the spec) — only `writeJsonFile`/`ensureDirs`
  were deduped there; its own `readBaseline` (a different, tolerant JSON reader) is untouched.
- Added `src/core/fsjson.test.ts`: file-absent fallback, write/read round-trip, and `ensureDirs`
  creating multiple (including nested) directories.
- Updated root `CLAUDE.md` file map with a one-line entry for `src/core/fsjson.ts`, following the
  existing convention next to `src/core/browser.ts`/`src/core/plex-client.ts`.

## Checks run

- `npx tsc --noEmit` — clean.
- `npm test` — full suite green (10 test files incl. new `src/core/fsjson.test.ts`; all pass).
- `npx tsx --test src/core/fsjson.test.ts` — 3/3 pass individually.
- `grep -n "function readJsonFile\|function writeJsonFile" src/workflows/{perfumes,movies,tv-recs,plex-space-saver}/lib.ts`
  — no matches; only each lib's own `ensureDirs()` wrapper function remains (by design, since each
  workflow passes a different directory list).

## Remaining

Nothing — task complete.
