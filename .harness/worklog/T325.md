# T325 — omit redundant "Ignore all" on single-item groups

## What changed

`dashboard/app/workflows/[name]/page.tsx`:
- `MovieGapsManager`: the per-collection group-header "✕ Ignore all" button is now only
  rendered when `films.length > 1`. Single-film collections keep the header row (name +
  "You own: ..." example text) but omit the button.
- `MissingSeasonsManager`: the per-show group-header "✕ Ignore all" button is now only
  rendered when `nums.length > 1`. Single-season shows keep the header row (title/link)
  but omit the button.

No changes to `groupByShow`/`groupByCollection`, the `ignoreShow`/`ignoreCollection`
handlers, or button behavior when it IS shown (2+ items).

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 91/91 passing (23 suites).
- `npm --prefix dashboard run build` — succeeded.
- `node dashboard/scripts/visual-check.mjs` — all 23 captures passed; inspected
  `workflow-missing-tv-seasons.png` and `workflow-movie-recs.png`:
  - Missing seasons: "The Wire" (2 seasons, fixture tmdbId 500) still shows "✕ Ignore all";
    the LONG-title show (1 season, tmdbId 501) and "Fargo" (1 season, tmdbId 502) show no
    group button, only the single row's own "✕ Ignore" — header row alignment unaffected.
  - Franchise gaps: "The Bourne Collection" (2 films) still shows "✕ Ignore all"; the
    single-film LONG-named collection ("Some Sequel") shows no group button, only the row's
    own "✕ Ignore" — the "You own: ..." text still renders correctly on multi-item headers.
  - Fixtures in `dashboard/scripts/_dashboard-harness.mjs` already covered both single- and
    multi-item cases for both tables (movieGaps collections 1 vs 2; missingSeasons tmdbId
    500 vs 501/502) — no harness changes needed.
- `node dashboard/scripts/mobile-check.mjs` — all 15 pages pass (0 overflow, 0
  boundary-crossing elements).
