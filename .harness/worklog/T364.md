# T364 — vercel-daily-redeploy workflow

## What was built

- `src/jobs/vercel-daily-redeploy/vercel-redeploy.job.ts` — single job, reads
  `VERCEL_DEPLOY_HOOK_URL`, soft-skips (WARN log, no throw) when unset, otherwise POSTs
  with no body/auth header via native `fetch`, throws on non-2xx or network error.
  `timeoutMs: 30_000`, `maxRetries: 1`, no work_items ledger (mirrors `claude-warm.job.ts`).
- `src/jobs/vercel-daily-redeploy/vercel-daily-redeploy.workflow.ts` — one-stage workflow,
  `category: 'regular-maintenance'`, `schedule: '0 23 * * *'`.
- `src/jobs/vercel-daily-redeploy/vercel-redeploy.test.ts` — hermetic, mocks global `fetch`,
  covers all 4 cases from the spec (unset/success/non-2xx/network-error).
- `.env.example` — added `VERCEL_DEPLOY_HOOK_URL=` with explanatory comment near the
  Finnhub/Hevy block.
- `README.md` — new bullet in the workflow list.
- `CLAUDE.md` — new paragraph in "What this project is", file-map table entry, and the
  "public repo ships" note updated to include `vercel-daily-redeploy/`.

## Verification observed

- `npx tsc --noEmit` — clean, no errors.
- `npm test` — 146/146 tests pass (previously 142; +4 new tests in
  `vercel-redeploy.test.ts`), including the new suite:
  - soft-skip when env unset (fetch never called, WARN logged)
  - success path (POST with no body/headers, logs success)
  - non-2xx throws
  - network error (fetch rejection) throws
- No live network calls made in verification — `fetch` fully mocked in the test file.
- Did not attempt to provision a real Vercel Deploy Hook URL (per spec, that's a separate
  needs-human task).

## Result

done T364
