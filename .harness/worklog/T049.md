# T049 — Sweep repo for lingering "pipeline" usage

## Outcome: complete (no changes needed)

## Sweep findings

Full `grep -ri pipeline` run across all in-scope paths:
- `src/` (non-migration files): **no results** ✓
- `dashboard/`: **no results** ✓
- `README.md`: **no results** ✓
- `CLAUDE.md`: **no results** ✓
- `docs/`: directory does not exist ✓

## Legitimate residual references (excluded per task spec)

`src/db/index.ts` and `src/db/migrate-workflow.test.ts` retain old table names
(`pipelines`, `pipeline_jobs`, `pipeline_runs`, `pipeline_run_logs`, column names
`pipeline_name`/`pipeline_run_id`, index names `idx_pipeline_*`) — these are
REQUIRED by the T038 rename migration to perform the rename. Changing them would
break the migration. They are explicitly excluded by the task.

## Verification

- `npx tsc --noEmit`: **pass**
- `npm test` (18 files, all tests): **pass**
