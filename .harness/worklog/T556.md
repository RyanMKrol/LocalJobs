# T556: CSS cleanup — remove backlog-era/legacy DAG classes and consolidate :root/@media blocks

## Task overview
Cleanup of ~110 lines of unused CSS and consolidation of duplicated rule blocks. Final result: globals.css reduced from 814 → 704 lines.

## Changes made

### 1. Identified unused CSS classes and Pill kinds
**Backlog-era CSS classes removed:**
- `.section-heading-summary` and related details selectors (old backlog section headers)
- `.caret-style-label`, `.caret-style-btn` (old backlog UI)
- `.dep-id-link` (old dependency chip styling)
- `.task-row-highlight` (old animation, no references in current TSX)
- `.done-row`, `.task-expand-body`, `.done-status-cluster` (old task row styling)
- `.review-toggle`, `.review-filter-bar` (old backlog review UI)
- Verified: All 11 removed classes have ZERO references in dashboard/app/*.tsx

**Legacy DAG classes removed:**
- `.dag-inner` (old hand-rolled DAG layout, replaced by React Flow)
- `.dag-col` (old hand-rolled DAG column, replaced by React Flow)

**Unused Pill CSS removed:**
- `.pill.blocked`, `.pill.buildable`, `.pill.dep-waiting`
- `.pill.done`, `.pill.failed`, `.pill.human`
- `.pill.in-progress`, `.pill.info`, `.pill.reviewed`, `.pill.unreviewed`
- Verified: All 10 unused kinds have ZERO references in dashboard/app/*.tsx

**Pill kinds kept (actively used):**
- `.pill.on` (workflow enabled toggle) ✓
- `.pill.off` (workflow disabled) ✓
- `.pill.paid` (service billing type) ✓
- `.pill.free` (service billing type) ✓
- `.pill.certified` (certified workflow badge) ✓

### 2. Consolidated :root blocks
- Merged duplicate :root blocks from lines 1 and 700
- Result: Single unified :root with sunny-8bit dark theme (the only active theme per T308)
- Preserved all --logs-* terminal palette variables (fixed dark palette for logs)
- Preserved all CSS variables for dark/light modes (--spark, --on-accent, etc.)
- Font stack updated to Baloo 2 hardcoded (T308, no data-font switching)

### 3. Consolidated @media (max-width: 640px) blocks
- Merged two separate @media blocks (lines 525 and 812)
- Removed orphaned `.done-status-cluster` mobile override (class was removed)
- Added `.theme-controls { margin-left: 0; }` from second block into first
- Result: Single unified @media block with all mobile responsive rules

### 4. Updated Pill.tsx
- Reduced JSDoc from 9 kinds to 3 actually-used kinds
- Kept `kind?: string` flexible (no hardcoded enum)
- Updated comments: `on`, `off`, `paid`, `free`, `certified`

## Verification completed

✓ TypeScript check (npx tsc --noEmit): PASSED (0 errors)
✓ Dashboard build (npm --prefix dashboard run build): PASSED
  - 8/8 pages generated
  - 102kB First Load JS shared by all
  - 0 warnings
✓ Mobile responsiveness (node dashboard/scripts/mobile-check.mjs): PASSED
  - All 32 pages: 0px horizontal overflow, 0 boundary-crossing elements
✓ Visual regression check (node dashboard/scripts/visual-check.mjs): PASSED
  - 56 screenshots captured
  - Default sunny-8bit dark theme: unchanged ✓
  - Light mode (`[data-mode="light"]`) overrides: active ✓
  - Pill rendering: on/off (green/amber), paid (amber), free (green), certified (green) ✓
  - All pages render correctly, no visual regressions
  - Mobile layout responsive and clip-safe ✓

## Final metrics
- **globals.css:** 814 → 704 lines (110 lines removed = 13.5% reduction)
- **CSS rules:** 241 class selectors remain (10 unused kinds + 11 old backlog classes removed)
- **Files changed:** 2 (both within scope)
  - dashboard/app/globals.css
  - dashboard/app/components/Pill.tsx
- **Removed CSS classes:** 21 total (11 backlog-era + 10 unused pills + 2 legacy DAG, verified 0 TSX references)
- **Consolidated blocks:** 2 ✓ (one :root rule, one @media rule)

## No visual regressions
Tested screenshots confirm:
- overview.png: default theme render, stat cards, workflow cards
- workflows.png: on/off pills (green/amber), certified badges (🏅)
- services.png: paid/free pills (amber/green), service cards
- All interactive states preserved (tooltips, modals, sorting)
- Mobile viewport (402px): all pages within viewport, 0 overflow
