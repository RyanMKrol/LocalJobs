# T276 — Migrate Backlog page to shared component library

Replaced ad-hoc `<span className="pill ...">` chips in `dashboard/app/backlog/page.tsx` with the
shared `<Pill>` component from `dashboard/app/components/Pill.tsx` (T268): dep-waiting, buildable,
human, reviewed/unreviewed, done, failed, and the plain tag pills. No `RunButton` usage applies on
this page (no run-trigger control exists here).

Verified like-for-like (no visual/behavioural change):
- `npx tsc --noEmit` — clean.
- `npm test` — 74/74 pass.
- `npm --prefix dashboard run build` — clean.
- `node dashboard/scripts/mobile-check.mjs` — all 10 pages pass, backlog included.
- `node dashboard/scripts/visual-check.mjs` — all 16 shots captured; opened `backlog.png` and
  `backlog-expanded.png` and confirmed pills render identically (buildable/needs-human/dep-waiting
  chips, and the expanded Done section's reviewed/done/failed pills + Mark failed/Undo fail buttons)
  with no clipping/overlap.

No harness/mobile-check surface change (no new page/interactive state), so
`_dashboard-harness.mjs` was left untouched.
