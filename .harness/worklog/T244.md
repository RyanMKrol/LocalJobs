# T244 worklog

## What was done

Added a `viewport: true` per-flow flag to the FLOWS harness system:

- **`_dashboard-harness.mjs`**: added `viewport: true` to the `overview-theme-picker` flow with a brief comment explaining why (modal backdrop only covers the viewport; fullPage would show un-dimmed content below the fold).
- **`visual-check.mjs`**: in `capture()`, derive `fullPage = !spec.viewport` and use it in both the normal screenshot call and the error-fallback screenshot call.

## Verification

Ran `node dashboard/scripts/visual-check.mjs` — all 10 captures passed (8 PAGES + 2 FLOWS).

Visually inspected `overview-theme-picker.png`: the Appearance modal is centered over the viewport, the dimmed backdrop (grey overlay) covers the **full frame** — no un-dimmed page content below the fold. The modal content (theme/font/mode picker) is fully visible and correctly rendered.

All other captures (PAGES + `backlog-expanded`) remain fullPage as before — verified visually, no regressions.
