# T178 worklog

## What was done

Removed the trailing progress message hint from the run detail page progress bar.

Changed `dashboard/app/runs/[id]/page.tsx` line 56:
- Before: `{run.progress}% {run.progress_msg && `· ${run.progress_msg}`}`
- After: `{run.progress}%`

No other pages rendered `progress_msg` — confirmed via grep across all dashboard `.tsx`/`.ts` files.

## Verification

- `npx tsc --noEmit` — clean
- `npm --prefix dashboard run build` — success
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
