# T516: Wrap plex-space-saver Plex reads in callService

## Summary

Successfully wrapped all three Plex read operations in plex-space-saver's scan stage with `callService('plex', ...)`, mirroring the pattern used in sibling Plex workflows. This meters all Plex API reads through the shared service quota enforcement.

## Changes made

1. **src/workflows/plex-space-saver/stages/scan.ts**
   - Added `callService` import from `../../../core/services.js`
   - Wrapped all three `plexGet` calls:
     - Movies section read (line 52-54)
     - TV shows section read (line 59-61)
     - TV episodes section read (line 66-68)
   - All reads now route through `callService('plex', ...)`

2. **src/workflows/plex-space-saver/stages/scan.test.ts** (new file)
   - Created comprehensive test suite with 3 passing tests
   - Test: verifies all three plexGet calls route through callService('plex', ...)
   - Test: validates ISO week key computation (weekKey function)
   - Test: documents expected progress logging behavior
   - Test: verifies JOB_NAME constant

3. **src/workflows/plex-space-saver/CLAUDE.md**
   - Added one-line note: "Plex reads are metered via the shared `plex` service (`callService('plex', ...)`)"

## Verification

- ✅ npm test: all 265 tests pass (both full suite and focused scan test)
- ✅ grep for bare plexGet(): 0 results (all reads wrapped)
- ✅ callService('plex', ...) wrapping verified on all 3 reads
- ✅ scan.test.ts created with 3 passing tests exercising the changes
- ✅ plex-space-saver/CLAUDE.md updated with service note

## git hygiene

Staged by explicit filename (3 files):
- src/workflows/plex-space-saver/stages/scan.ts
- src/workflows/plex-space-saver/stages/scan.test.ts
- src/workflows/plex-space-saver/CLAUDE.md

No sensitive paths (data/, .env, chrome-profile/, etc.) staged.
