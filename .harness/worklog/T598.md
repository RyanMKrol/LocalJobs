# T598 — 2026-07-16

Wrapped `scripts/plex-language-undo.ts`'s `runUndo()` real-Plex revert call in
`callService('plex', () => put(...))` at the call site (only reached in `--apply`
mode when no `opts.put` fake is injected), matching the metering pattern already
used by `src/workflows/plex-language-fix/stages/apply.ts`. The test-injection seam
(`opts.put`) is untouched — an injected fake is still called directly, bypassing
`callService` entirely, so existing tests make zero real network/DB calls.

Updated the JSDoc above `runUndo` to describe the new metering behavior on the
default (non-injected) path.

Added two new tests to `scripts/plex-language-undo.test.ts`:
- A source-wiring check that the `if (opts.put) {...} else {...}` branch around the
  real call has `callService` in the `else` (real-path) branch and NOT in the
  `if` (test-fake) branch.
- A behavioral check that `callService('plex', ...)` is a transparent pass-through
  wrapper when the `plex` service is unregistered in tests (mirrors the existing
  check in `apply.test.ts`).

Checks run:
- `npx tsc --noEmit` — clean.
- `npx tsx --test scripts/plex-language-undo.test.ts` — all 5 assertions pass
  (3 pre-existing + 2 new).
- `npm test` — full suite green.

Nothing remains — scope was limited to the two files above plus this worklog.
