## T471 — 2026-07-09

Re-attempt of T342 (blocked twice purely on stale scope arrays). Extracted a shared
`CategoryTable` component and migrated `dashboard/app/workflows/page.tsx` +
`dashboard/app/integrations/page.tsx` to use it.

### What I did
- Added `dashboard/app/components/CategoryTable.tsx`: wraps the `.panel` + `<h2>` +
  `<table>/<thead>` scaffold shared by both pages' `CATEGORY_GROUPS` render loop.
  Props: `label`, `columns: { key, label, align?, width? }[]`, optional
  `tableClassName`, `children` for the `<tbody>`. Renders a `<colgroup>` only when a
  column declares a `width` (needed by integrations' fixed-width columns; workflows
  has none). Reuses the existing `.panel + .panel` spacing rule — no new CSS.
- Migrated `workflows/page.tsx`'s per-category block (8 columns, no explicit
  widths) and `integrations/page.tsx`'s per-category block (6 columns, all with
  `width`, plus `tableClassName="services-table"`) to render through it, preserving
  all existing row markup/interactivity (RunButton, Pill, stuck popover trigger,
  service consumer modal trigger, inline limit editing) unchanged as `children`.
- Added a one-line note to CLAUDE.md's T268 shared-components list documenting
  `CategoryTable`.

### Checks run
- `npx tsc --noEmit` (root, daemon) — clean.
- `cd dashboard && npx tsc --noEmit` — only 3 pre-existing unrelated errors
  (react-dom/server types, StageIoLists.test.ts, nav-check.test.ts) — none touch
  the migrated files.
- `npm test` (root) — 246/246 pass.
- `npm --prefix dashboard run build` — succeeds, `/workflows` and `/integrations`
  routes build fine.
- `node dashboard/scripts/visual-check.mjs` — 42/43 pass. The one failure
  (`workflow-run-skipped`, a `.rf-dag-node` selector timeout on
  `/workflow-runs/skipped`) is PRE-EXISTING and unrelated: reproduced identically
  on a `git stash` of this change (confirmed via stash/build/check/pop before
  re-applying). Looked at `workflows.png` and `services.png` (integrations) —
  both render exactly as before: same panel-per-category grouping, same table
  columns/headers, same row content, same spacing between stacked panels. Also
  spot-checked `services-edit-limits-row.png` (inline-edit interactive state —
  renders correctly inside the new wrapper) and `overview-theme-toggle.png` (an
  unrelated page — unaffected).
- `node dashboard/scripts/mobile-check.mjs` — 19/19 pages pass, including
  `workflows` and `services` (`/integrations`).
- Confirmed CLAUDE.md's T268 section now lists `CategoryTable` alongside
  `RunButton`/`Pill`.

No other files touched; migrating other tables with hand-rolled `<table>` markup
is left as future follow-up per the spec.
