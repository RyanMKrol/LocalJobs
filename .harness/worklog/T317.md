# T317 — pin projects-sync Claude model + effort

## What changed
- `src/services/claude.ts`: added optional `effort?: string` param to `runClaude`/`spawnClaude`;
  extracted arg-building into exported pure `buildClaudeArgs(model, effort?)` (only appends
  `--effort <level>` when provided, keeps `--dangerously-skip-permissions` last).
- `src/services/claude.test.ts` (new): asserts `buildClaudeArgs` omits `--effort` when unset and
  includes `['--effort', 'medium']` when set.
- `src/jobs/projects-sync/stages/project-summarize.ts`: added `claudeEffort` export (env
  `PROJECTS_SYNC_CLAUDE_EFFORT`, default `medium`), widened `ClaudeSummarizer` type to accept an
  optional third `effort` param, threaded it into the `summarize(...)` call site.
- `.env` (local only, gitignored, not staged): added `PROJECTS_SYNC_CLAUDE_MODEL=claude-sonnet-5`
  and `PROJECTS_SYNC_CLAUDE_EFFORT=medium`.
- `.env.example`: documented `PROJECTS_SYNC_CLAUDE_EFFORT` (commented, with valid-values note)
  next to the existing `PROJECTS_SYNC_CLAUDE_MODEL` line.
- `CLAUDE.md`: extended the projects-sync description to mention the new effort env var.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 87/87 passing, including the new `src/services/claude.test.ts` and the existing
  `project-summarize.test.ts` (its stub `summarize` signatures still satisfy the widened optional
  third param with no changes needed).
- Confirmed no other `runClaude` callers (movies, tv-recs, workouts-sync, claude-warmer) were
  touched — they still call with 2 args, and since `effort` is optional and unused in
  `buildClaudeArgs` when omitted, their CLI invocation is byte-identical to before.
- `git check-ignore .env` exits 0; `.env` was edited locally but never staged.
- Did not run `claude --help` live (no need — the `--effort` flag's existence/values were given
  in the task spec as already-confirmed background).

## Scope
Touched only files listed in the task's scope: `src/services/claude.ts`,
`src/services/claude.test.ts` (new), `src/jobs/projects-sync/stages/project-summarize.ts`, `.env`
(local, unstaged), `.env.example`, `CLAUDE.md`, plus this worklog.
