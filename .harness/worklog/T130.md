# T130 worklog — Backlog: replace model/escalation display with colour-coded difficulty pill

## Attempt 1 — 2026-06-23

### What was done

Replaced `resolveRung` and `escalationPath` helpers in `dashboard/app/backlog/page.tsx` with:
- `resolveDifficulty(model, effort, defaults)` — maps first-attempt rung to a `Difficulty` enum
- `difficultyPill(t, defaults)` — renders a colour-coded pill

Difficulty mapping:
- sonnet@any (or unknown) → **easy** (green)
- opus@high or unspecified → **medium** (yellow)
- opus@xhigh → **hard** (red)
- opus@max → **very hard** (near-black `#22272e` with light `#cdd9e5` text + `#444c56` border — readable on dark theme)

Added four CSS classes to `dashboard/app/globals.css` (`.pill.diff-easy/medium/hard/very-hard`).

Removed the escalation path `<span class="mono">` from `TaskCard`; the pill sits in the header row alongside `buildable` and `status`.

### Checks
- `npx tsc --noEmit` — clean
- `npm test` — all passed
- `npm --prefix dashboard run build` — clean
- `node dashboard/scripts/mobile-check.mjs` — all 9 pages pass
