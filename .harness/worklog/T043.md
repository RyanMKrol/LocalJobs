# T043 — Joint review of the perfumes pipeline

Reviewed by the owner on 2026-06-23. The perfumes pipeline (find-url → fetch → parse →
build, with `repeatUntilStable` cycling, the fragrantica service for pacing, and the
shared headless-Chrome launch) was walked through together and the owner is comfortable
with how it works.

Follow-ups captured during the review of the pipeline's output (spun out as their own
backlog tasks rather than blocking this review):
- **T138** — gate detail page: collapse identical producer/consumer sides.
- **T139** — run-scope the workflow-run Input→Output panel (the "first cut" global-ledger
  mapping).
- **T140** — tidy the output-preview frontmatter (column alignment, array values, hide
  null/empty fields).

Marked done + reviewed.
