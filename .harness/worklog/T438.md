# T438

Rewrote the `description` field on the 4 stocks-sync job files with paragraph-length prose
grounded in `src/workflows/stocks-sync/CLAUDE.md` and each stage's actual current code (the
workflow is now 5 stages — stocks-fetch → stocks-resolve-names → stocks-snapshot → stocks-watch →
stocks-notify — so descriptions were grounded in the current DAG, not the stale wording quoted in
the task spec, which pre-dated the stocks-resolve-names split):

- `stocks-fetch.job.ts` — Trading212 Invest+ISA fetch, account tagging, read-only broker call,
  daily ledger row written unconditionally.
- `stocks-snapshot.job.ts` — pure report builder from named-positions.json to portfolio.json/.md,
  no resolution calls of its own, not limitable.
- `stocks-watch.job.ts` — per-run gain computation, fresh-breach tracking via the separate
  `::notified` ledger key, unconditional ledger row.
- `stocks-notify.job.ts` — terminal no-op-unless-breach push stage, outputJob override noted.

## Verification

- `npx tsc --noEmit` — clean.
- `npm test` — 213/213 passing, 0 failures.
- No paid/live API calls made; only read existing source + docs.
