# T433 — tv-recs paragraph-length job descriptions

Rewrote `description` for all 11 tv-recs jobs to real 3-6 sentence paragraphs grounded in
`CLAUDE.md`/`src/workflows/tv-recs/CLAUDE.md` and each stage's own code:

- 8 branch specs in `src/workflows/tv-recs/stages/branches.ts` (3 random serendipity branches +
  creator-completion, top-genre-canon, thin-genre, older-era, world-tv).
- `tv-snapshot.job.ts`, `tv-rec-merge.job.ts`, `tv-recs-notify.job.ts`.

Verification:
- `npx tsc --noEmit` — clean, no errors.
- `npm test` — 213/213 passing, 0 failures.

No functional/behavioral code changed — description strings only, within scope
(branches.ts + the 3 non-branch job files).
