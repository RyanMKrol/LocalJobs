# T026 worklog — Docs audit: end-state sweep of README + CLAUDE.md

## Attempt 1 — 2026-06-21

### Drifts found (code wins over docs)

1. **CLAUDE.md "What this project is" — stale content:**
   - "writing to DynamoDB" — the places pipeline writes enriched JSON + markdown
     to local `data/` files, not DynamoDB. Never used DynamoDB.
   - "Those will be added here as jobs. Until then there is a `demo` job for
     testing." — STALE. Both `places` and `perfumes` pipelines are already shipped
     as worked examples. The project is no longer in a "waiting to add jobs" state.

2. **README.md "Dashboard pages" — multiple drifts:**
   - Listed "**Jobs** — every job, schedule, enabled state, last/next run" as a
     dashboard page. There is NO `/jobs` index listing page in the nav or routes.
     The nav is: Overview · Pipelines · Services · Database. Job information
     (standalone jobs) is on the Pipelines page; individual job detail is at
     `/jobs/<name>` (accessible via links only).
   - Overview bullet: missing the clickable filtering stat tiles (T021). The
     overview tiles are interactive — clicking Running/Succeeded/Failed/Stuck
     filters the pipeline cards and run table below.
   - Pipeline run detail bullet: missing grouped/collapsed member-run history
     (T022). Older pipeline cycle runs are collapsed per stage (click to expand).
   - Version 1.0.0 not mentioned anywhere in README.

### Already accurate (no changes needed)
- Daily places schedule (03:00) — ✓ matches code `0 3 * * *`
- Consolidated service-level caps / no per-job caps on service-routed calls — ✓
- Manual ledger-prune + dismiss/unstick stuck items — ✓ in both docs
- First-class pipeline progress roll-up — ✓ in CLAUDE.md conventions + README
- Shared headless-browser helper (`src/core/browser.ts`) — ✓ in both docs
- Read-only DB browser — ✓ in README Database section
- Editable service limits — ✓ in README Services section
- Tailscale remote-access + security-hardening model — ✓ README has full section
- CLAUDE.md file map, conventions, architecture — ✓ (updated in T007)
- Schema tables (11 tables) — ✓ match `src/db/schema.sql`

### Fixes applied

**CLAUDE.md:**
- "What this project is": replaced DynamoDB reference with accurate output
  (enriched JSON + markdown to local files); replaced "will be added" with
  accurate shipped-state description of places + perfumes pipelines.

**README.md:**
- Added "**v1.0.0.**" to the intro paragraph.
- Rewrote "Dashboard pages" section:
  - Added nav line: Overview · Pipelines · Services · Database
  - Updated Overview bullet to describe clickable filtering stat tiles
  - Updated Pipelines bullet to include Standalone jobs section
  - Updated Pipeline run detail to mention grouped/collapsed member-run history
  - Changed "Jobs" (non-existent listing page) to "Job detail" (accessible via
    links, no nav entry) — with accurate description

### Verification
- `npx tsc --noEmit` — exit 0 (clean)
- `npm test` — all tests passed (14 test files)
