## T566 — 2026-07-14

Collapsed `src/workflows/projects-sync/claude-repo.ts` into `src/services/claude.ts`:

- Added an internal `spawnClaudeCli(prompt, args, {bin, timeoutMs, cwd})` primitive in
  `src/services/claude.ts` holding the single spawn/timeout/parse implementation (stdin-piped
  prompt, `--output-format json` envelope parse, `RATE_LIMIT_RE` detection, SIGKILL on timeout) —
  behavior unchanged from both prior copies.
- `runClaude(prompt, model, effort)` now calls `spawnClaudeCli` with `buildClaudeArgs(model, effort)`,
  `bin: claudeBin`, `cwd: tmpdir()`, `timeoutMs: claudeTimeoutMs()` — external signature/behavior
  unchanged.
- Added `runClaudeWithRepoAccess(prompt, model, repoDir, effort)` to `src/services/claude.ts`,
  moving `buildRepoAccessArgs` + `REPO_ACCESS_ALLOWED_TOOLS` over from `claude-repo.ts`. It calls
  `spawnClaudeCli` with `buildRepoAccessArgs(model, repoDir, effort)`, `bin: claudeBin`,
  `cwd: repoDir`, `timeoutMs: claudeTimeoutMs()` — this REPLACES `claude-repo.ts`'s module-load read
  of `LOCALJOBS_CLAUDE_TIMEOUT_MS`, so the dashboard-editable `claude-cli` service timeout now
  governs repo-access summaries the same way it already governs plain `runClaude` calls.
- Moved `unfenceMarkdown` into `src/services/claude.ts` (exported) so a future perfumes migration
  (R02b) can consume it from here too.
- Repointed `src/workflows/projects-sync/stages/project-summarize.ts` and its test's imports from
  `../claude-repo.js` / `../../../services/claude.js` (the RepoAccess helpers) to
  `../../../services/claude.js`. Deleted `claude-repo.ts` entirely (no re-export shim).
- Updated `src/workflows/projects-sync/CLAUDE.md` to describe the moved helper + the timeout-override
  fix.
- Added a new test in `src/services/claude.test.ts` asserting the timeout resolution order: a
  dashboard/service-effective override wins over the env-var-style fallback, which wins over the bare
  `300000` default (via `effectiveServiceTimeoutMs`/seeded `services` row).

Checks run:
- `npx tsc --noEmit` — clean.
- `npm test` — all suites pass, including `src/services/claude.test.ts` and
  `src/workflows/projects-sync/stages/project-summarize.test.ts` (0 failures each).
- `grep -rn 'spawn(' src --include='*.ts' | grep -v /data/repos/ | grep -i claude` shows exactly two
  spawn sites: `src/services/claude.ts` (the shared primitive) and `src/workflows/perfumes/claude.ts`
  (unchanged, out of scope — perfumes migration is a separate follow-up per the spec).

Nothing remains outstanding for this task.
