# T218 worklog

## What was built

- `src/jobs/tv-recs/stages/tv-recs-notify.ts` — `runTvRecsNotify` impl: reads `recommendations.json`, drops owner-ignored + already-notified recs, sends ONE digest via `push()`, marks each notified show `success` in the `tv-recs` ledger (announce-exactly-once), writes `data/out/reports/tv-recommendations.md` with its path in `detail.markdown`, appends to history file.
- `src/jobs/tv-recs/stages/tv-recs-notify.job.ts` — job definition (name `tv-recs-notify`).
- `src/jobs/tv-recs/stages/tv-recs-notify.test.ts` — 5 hermetic scenarios: first-run digests whole backlog, re-run sends nothing, new rec notifies once, ignore-to-suppress excludes from report+notify, ignoring a previously-notified rec removes it from report.
- `src/jobs/tv-recs/tv-recs.workflow.ts` — added `{ job: 'tv-recs-notify', dependsOn: ['tv-rec-merge'] }`; updated description.
- `src/jobs/tv-recs/config.ts` — added `reportDir: resolve(dataDir, 'out', 'reports')`.
- `src/jobs/tv-recs/lib.ts` — `ensureDirs()` now includes `reportDir`.
- `README.md` + `CLAUDE.md` — updated to describe the complete `tv-recommendations` workflow DAG and all stages.

## Verification

- `npx tsc --noEmit` → clean
- `npm test` → all 4 test suites pass (including the new tv-recs-notify suite with all 5 scenarios)
