# T437 — claude-warm job description

## What I did
Rewrote the `description` field of `src/workflows/claude-warmer/claude-warm.job.ts`'s
`claude-warm` job from a two-sentence label into a real paragraph explaining:
- Why it exists: Claude plans meter usage in a 5-hour rolling window; firing a cheap
  "hi" prompt every 30 minutes during off-hours keeps that window already running/reset
  by the time a real job needs Claude.
- It enforces no local quota/spend cap — the upstream Claude plan is the sole limiter.
- A hit rate limit/quota is an expected, non-fatal soft-fail: logged as a warning, run
  exits cleanly (not an error), since a hit limit just confirms the window is active.

Grounded in the job's own code (rateLimited / !ok / QuotaExceededError branches all just
log + return) and the workflow's own `src/workflows/claude-warmer/CLAUDE.md`.

## Verification
- `npx tsc --noEmit` — clean.
- `npm test` — 213/213 passing, 0 failures.
- No paid API calls made; no data/ files touched.

## Scope
Only touched `src/workflows/claude-warmer/claude-warm.job.ts` plus this worklog file, per
the task's scope gate.
