# T527 — tv-recs-notify: fail run on a failed digest push

2026-07-13

## What I did
- `src/workflows/tv-recs/stages/tv-recs-notify.ts`: after logging the digest push result,
  added `if (!res.ok) throw new Error(\`Digest push failed — ${res.error}\`);` BEFORE the
  ledger-mark loop and the `appendHistory` call. Mirrors the pattern already in
  `src/workflows/movies/stages/notify.ts` / `missing-tv-seasons/stages/notify.ts`.
  On success, behaviour is unchanged (mark loop, appendHistory, report write, progress/log
  all run exactly as before — the report write already happens before the push and is
  unaffected).
- `src/workflows/tv-recs/stages/tv-recs-notify.test.ts`: added a new case (Run 6) that
  injects a failing `pushFn` (`{ ok: false, error: 'ntfy unreachable' }`) and asserts:
  the run rejects with the expected error message; the new rec's `isWorkItemDone` stays
  `false`; and `recs-history.json` is byte-identical to before the run.

## Checks run
- `npx tsc --noEmit` — clean.
- `npm test` — 265/265 passing (all suites), including the new tv-recs-notify case and
  the existing success-path tests (unaffected).

## Remaining
Nothing outstanding for this task.
