{
  "_doc": "LOCAL-ONLY backlog for the in-place Ralph loop (gitignored; the shell owns task status — the agent must NOT edit this file). Schema: docs/HARNESS.md §8. All tasks are verifiable with NO live paid-API calls.",
  "version": 1,
  "defaults": {
    "model": "claude-opus-4-8",
    "effort": "high",
    "escalation": [
      {
        "model": "claude-opus-4-8",
        "effort": "xhigh"
      }
    ]
  },
  "tasks": [
    {
      "id": "T001",
      "title": "Green CI baseline on the public checkout",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "scaffold",
        "ci"
      ],
      "model": "claude-opus-4-8",
      "effort": "high",
      "scope": [
        "src/jobs/registry.test.ts",
        "src/**/*.test.ts",
        ".github/workflows/ci.yml"
      ],
      "verify": [
        "npx tsc --noEmit",
        "npm test"
      ],
      "do": "Make the test suite + typecheck pass on a CLEAN PUBLIC checkout (only public files present — most jobs are still gitignored at this point, so the CI runner sees only the framework + demo job). registry.test.ts currently asserts hardcoded counts (9 jobs / 4 services / 2 pipelines) that only hold when the private jobs are present locally — make it robust to whatever public fileset exists (assert the demo job loads + the registry validates without throwing, not exact private counts). Fix any module that does real work / needs data/ at import time so importing it in CI doesn't fail.",
      "doneWhen": "On a checkout containing only tracked files, `npx tsc --noEmit` and `npm test` both pass; GitHub Actions 'CI' is green on the pushed commit."
    },
    {
      "id": "T002",
      "title": "Self-contain the perfume profile template",
      "status": "done",
      "dependsOn": [
        "T001"
      ],
      "gate": null,
      "tags": [
        "coding",
        "privacy"
      ],
      "model": "claude-opus-4-8",
      "effort": "high",
      "scope": [
        "src/jobs/perfumes/config.ts",
        "src/jobs/perfumes/profile.template.md",
        "src/jobs/perfumes/build.ts",
        ".env.example",
        "CLAUDE.md"
      ],
      "verify": [
        "npx tsc --noEmit",
        "npm test"
      ],
      "do": "The repo must be self-contained — it must NOT reference other folders on the machine. perfumes/config.ts hardcodes templatePath='/Users/ryankrol/Development/perfume-markdown/research/_TEMPLATE.md' (leaks the machine's topography). Copy that template's CONTENT into a tracked in-project file src/jobs/perfumes/profile.template.md, change config.ts to resolve it RELATIVE to the perfumes job dir (no absolute path, no external repo), with an optional PERFUMES_TEMPLATE_PATH env override defaulting to the in-project file. Keep build.ts's FALLBACK_TEMPLATE. Document PERFUMES_TEMPLATE_PATH in .env.example. NOTE: the perfumes folder is still gitignored at this point — make the edits in place; the actual publish happens in T004.",
      "doneWhen": "No absolute/external-repo path remains in any job code (`grep -rn '/Users/' src/jobs` is clean except inside data/); perfumes build still loads a template; typecheck + tests pass."
    },
    {
      "id": "T003",
      "title": "Publish the places job code",
      "status": "done",
      "dependsOn": [
        "T001"
      ],
      "gate": null,
      "tags": [
        "publish",
        "privacy"
      ],
      "model": "claude-opus-4-8",
      "effort": "high",
      "scope": [
        ".gitignore",
        "src/jobs/places/",
        "README.md"
      ],
      "verify": [
        "npx tsc --noEmit",
        "npm test"
      ],
      "do": "Make the places job CODE public. Remove ONLY the 'src/jobs/places/' line from .gitignore (keep 'src/jobs/**/data/' and all secret rules). `git add` the places .ts files EXPLICITLY by path (NEVER `git add -A`); confirm via `git status` that nothing under src/jobs/places/data/ is staged. Verify a clean checkout still builds: the newly-public code must typecheck and the suite must stay green with no data/ present (use fixtures, no live API). Update README's note that 'jobs stay private' to reflect that places is now public.",
      "doneWhen": "src/jobs/places/*.ts is tracked; src/jobs/places/data/** remains ignored (verify with `git check-ignore src/jobs/places/data/out`); typecheck + tests green; the pre-push guard would pass (no sensitive path staged)."
    },
    {
      "id": "T004",
      "title": "Publish the perfumes job code",
      "status": "done",
      "dependsOn": [
        "T002",
        "T003"
      ],
      "gate": null,
      "tags": [
        "publish",
        "privacy"
      ],
      "model": "claude-opus-4-8",
      "effort": "high",
      "scope": [
        ".gitignore",
        "src/jobs/perfumes/",
        "README.md",
        "docs/LIMITATIONS.md"
      ],
      "verify": [
        "npx tsc --noEmit",
        "npm test"
      ],
      "do": "Make the perfumes job CODE public (depends on T002 having removed the external template path). Remove ONLY the 'src/jobs/perfumes/' line from .gitignore (keep data/ + secret rules). `git add` the perfumes .ts files + profile.template.md EXPLICITLY; confirm nothing under src/jobs/perfumes/data/ (especially chrome-profile/) is staged. Note in docs/LIMITATIONS.md that publishing exposes Fragrantica-scraping technique (accepted).",
      "doneWhen": "src/jobs/perfumes/*.ts + profile.template.md tracked; src/jobs/perfumes/data/** (incl. chrome-profile) still ignored; typecheck + tests green; no sensitive path staged."
    },
    {
      "id": "T005",
      "title": "Unit tests for the framework core",
      "status": "done",
      "dependsOn": [
        "T001"
      ],
      "gate": null,
      "tags": [
        "testing"
      ],
      "model": "claude-opus-4-8",
      "effort": "high",
      "scope": [
        "src/core/executor.test.ts",
        "src/core/pipeline-executor.test.ts",
        "src/core/scheduler.test.ts",
        "src/core/services.test.ts",
        "src/core/notifier.test.ts"
      ],
      "verify": [
        "npm test"
      ],
      "do": "Build out unit-test coverage for the framework core (currently only dag.ts + store helpers are tested). Cover: the executor (timeout-kill, retries, event parsing), the pipeline-executor (skip/aggregation/repeat-until-stable paths), the scheduler (croner triggers respect `enabled`), the service limiter callService (quota + throttle + min-interval spacing), and the notifier's header sanitiser. Use fake timers / injected clocks and stub child processes / network — NO live external calls. Keep tests against the scratch DB only.",
      "doneWhen": "New *.test.ts files exercise the above paths; `npm test` is green; the new suites meaningfully assert behaviour (not just smoke imports)."
    },
    {
      "id": "T006",
      "title": "Validation gates between pipeline stages",
      "status": "done",
      "dependsOn": [
        "T005"
      ],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "model": "claude-opus-4-8",
      "effort": "high",
      "scope": [
        "src/core/types.ts",
        "src/core/pipeline-executor.ts",
        "src/core/dag.ts",
        "src/core/*.test.ts"
      ],
      "verify": [
        "npx tsc --noEmit",
        "npm test"
      ],
      "do": "Add typed-artifact validation gates between dependent pipeline stages: let a job declare what it `produces`/`consumes`, and have the DAG/pipeline executor validate the contract at each boundary so an external-format drift (Takeout CSV layout, Fragrantica page structure) fails LOUD at the exact gate instead of passing bad data downstream. Make a failed gate a first-class, surfaced failure (run status + notification). Unit-test both a passing contract and a drifted/failing one.",
      "doneWhen": "Contracts are declarable + enforced between stages; a violation produces a clear surfaced failure; tests cover pass + fail; typecheck + tests green; docs updated."
    },
    {
      "id": "T007",
      "title": "Docs audit — README + CLAUDE.md vs current repo",
      "status": "done",
      "dependsOn": [
        "T004"
      ],
      "gate": null,
      "tags": [
        "docs"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "high",
      "scope": [
        "README.md",
        "CLAUDE.md"
      ],
      "verify": [
        "npx tsc --noEmit"
      ],
      "do": "Audit README.md + CLAUDE.md against the current repo state and fix drift: the pipelines + services model, the ntfy notify topic/config, the work-ledger / usage-meter / stuck-items / notifier features, the dashboard pages list, the file map, and the now-PUBLIC places/perfumes jobs (the old 'all real jobs are gitignored' framing is no longer fully true — correct it honestly). Keep claims matched to code.",
      "doneWhen": "Both docs accurately describe the current architecture, jobs, features, and public/private split; no stale claims remain."
    },
    {
      "id": "T008",
      "title": "Dashboard quick fixes",
      "status": "done",
      "dependsOn": [
        "T001"
      ],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "high",
      "scope": [
        "dashboard/"
      ],
      "verify": [
        "npm --prefix dashboard run build"
      ],
      "do": "Four small, self-contained dashboard fixes: (1) the pipeline detail graph (/pipelines/[name]) should show STRUCTURE only — omit the node status label when no `statusByJob` is provided (Dag.tsx); (2) when viewing a stage's logs from a pipeline run (/runs/[id] with a pipeline_run_id), the top-left back-link should return to /pipeline-runs/[pipeline_run_id], not the standalone job; (3) make the top-left 'Local Jobs' wordmark a link to the overview; (4) the overview 'Recent runs' list should show recent PIPELINE runs (link to /pipeline-runs/[id]).",
      "doneWhen": "All four behaviours implemented; `npm --prefix dashboard run build` passes; typecheck clean."
    },
    {
      "id": "T009",
      "title": "Perfumes: populate accord percentages",
      "status": "done",
      "dependsOn": [
        "T004"
      ],
      "gate": null,
      "tags": [
        "coding",
        "perfumes"
      ],
      "scope": [
        "src/jobs/perfumes/parse.ts",
        "src/jobs/perfumes/parse.job.ts",
        "src/jobs/perfumes/types.ts",
        "src/jobs/perfumes/parse.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "The parse stage emits accords: [{name, pct}] but pct is always null. Pull each accord's strength (the coloured bar's width %) off the cached Fragrantica page so accords carry weight. Parse against the already-fetched HTML under src/jobs/perfumes/data — NO live scrape.",
      "doneWhen": "Accords carry a numeric pct parsed from the bar width on a cached fixture; pct is null only when genuinely absent on the page; a unit test asserts the parsed percentages against a committed/synthetic HTML fixture."
    },
    {
      "id": "T010",
      "title": "Perfumes: handle empty Fragrantica notes sections in markdown",
      "status": "done",
      "dependsOn": [
        "T004"
      ],
      "gate": null,
      "tags": [
        "coding",
        "perfumes"
      ],
      "scope": [
        "src/jobs/perfumes/parse.ts",
        "src/jobs/perfumes/build.ts",
        "src/jobs/perfumes/build.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Some Fragrantica notes-pyramid sections come back empty. Decide and implement how that is reflected in the built markdown — handle honestly: don't fabricate notes, don't silently drop the section. Verify against cached/synthetic fixtures, no live scrape.",
      "doneWhen": "An empty notes-pyramid section produces a clear, honest markdown result (section omitted or explicitly marked empty — never fabricated); a unit test covers an empty-section fixture and a populated one."
    },
    {
      "id": "T011",
      "title": "Perfumes: weight Fragrantica vs LLM research by confidence",
      "status": "done",
      "dependsOn": [
        "T004"
      ],
      "gate": null,
      "tags": [
        "coding",
        "perfumes"
      ],
      "scope": [
        "src/jobs/perfumes/build.ts",
        "src/jobs/perfumes/config.ts",
        "src/jobs/perfumes/lib.ts",
        "src/jobs/perfumes/build.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Niche houses have low vote counts = low-confidence community data. In the build step, down-weight low-sample Fragrantica signal against the LLM's own web research using a CONTINUOUS confidence weight (e.g. votes/(votes+k)). ALSO compute the ecosystem-wide vote/weight distribution across the scraped Fragrantica corpus to calibrate what counts as high vs low weight, and feed that distribution in as an input. Make the weighting explicit in the output. Verify against cached fixtures, no live calls.",
      "doneWhen": "Build blends Fragrantica vs LLM via a continuous sample-size confidence weight calibrated against the corpus-wide distribution; the chosen weighting is explicit in the built profile; unit tests cover a low-sample and a high-sample fixture and assert the relative weighting."
    },
    {
      "id": "T012",
      "title": "Places: daily schedule + daily spend cap",
      "status": "done",
      "dependsOn": [
        "T003"
      ],
      "gate": null,
      "tags": [
        "coding",
        "places"
      ],
      "scope": [
        "src/jobs/places/places.pipeline.ts",
        "src/jobs/places/config.ts",
        "src/jobs/places/enrich.ts",
        "src/jobs/places/llm-enrich.ts"
      ],
      "design": null,
      "verify": [],
      "do": "The places pipeline is schedule:null (manual-only). Give it a real DAILY cron schedule ('0 3 * * *') so no pipeline is manual-only, and ensure a daily spend cap derived from the monthly free Places allowance (~ monthly/30) is enforced so a daily run can't blow the month. Env-overridable per the caps convention. No live API calls in verification.",
      "doneWhen": "Places pipeline schedule is a daily cron (not null); a daily cap (≈ monthly free allowance / 30) is configured and enforced in the enrich loops; a unit test asserts the cap arithmetic / that the loop stops at the daily cap; config is env-overridable."
    },
    {
      "id": "T013",
      "title": "Finish service migration: drop per-job caps + backfill service_usage",
      "status": "done",
      "dependsOn": [
        "T003",
        "T012"
      ],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "scope": [
        "src/jobs/places/enrich.ts",
        "src/jobs/places/llm-enrich.ts",
        "src/core/services.ts",
        "src/db/store.ts",
        "scripts/backfill-service-usage.ts",
        "src/db/store.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Remove the redundant per-job capStatus/recordUsage from places-enrich (google-places) and enrich-with-llm (gemini) so the service-level quota is the SINGLE source of truth and the QuotaExceededError soft-fail path is genuinely live (currently shadowed by the job cap). THEN write a backfill script that tops up THIS MONTH's service_usage from job_usage (google-places←places-enrich, gemini←enrich-with-llm) WITHOUT double-counting calls already recorded to service_usage (idempotent), run it in-place against the live data/jobs.db, and self-verify. NO live paid API calls — this only reads/writes the local meter tables.",
      "doneWhen": "Per-job capStatus/recordUsage removed from both jobs; service quota is the sole governor and the QuotaExceededError path is exercised by a unit test; the backfill script is committed, was run in-place, and before/after service_usage vs job_usage counts (reconciled, no double-count) are logged to .harness/worklog/T013.md."
    },
    {
      "id": "T014",
      "title": "Framework: manual prune of orphaned ledger rows",
      "status": "done",
      "dependsOn": [
        "T005"
      ],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "scope": [
        "src/db/store.ts",
        "src/core/types.ts",
        "src/api/server.ts",
        "src/db/store.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Build a framework feature to prune work_items whose item_key is no longer in a job's current input (the case that orphaned the 3 old-id perfume rows after their ids were corrected). A job exposes its current input key-set; a MANUAL-triggered prune (API/CLI, never automatic) removes work_items not in that set and surfaces what it removed. The user clears the 3 orphaned perfume rows via this feature.",
      "doneWhen": "A manual prune removes work_items whose item_key is absent from the job's current input set and keeps current ones; it is never triggered automatically; the removal is surfaced (API/log); unit tests cover keeps-current + removes-orphaned."
    },
    {
      "id": "T015",
      "title": "Framework: shared headless-browser helper (src/core/browser.ts)",
      "status": "done",
      "dependsOn": [
        "T004"
      ],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "scope": [
        "src/core/browser.ts",
        "src/jobs/perfumes/fetch.ts",
        "src/jobs/perfumes/fragrantica.service.ts",
        "src/core/browser.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Promote the Chrome-profile learnings from the perfumes fetch (persistent profile keeps Cloudflare clearance, real-Chrome channel, jittered pacing, rate=reputation-not-detection) into a shared src/core/browser.ts helper, and refactor the perfumes fetch to use it. Behaviour-preserving extraction. NO live fetch in verification.",
      "doneWhen": "A reusable src/core/browser.ts encapsulates persistent-profile + real-Chrome-channel + jittered-pacing launch; perfumes fetch is refactored onto it with no behaviour change; typecheck + tests green without any live browser/network call."
    },
    {
      "id": "T016",
      "title": "Framework: first-class pipeline progress percentages",
      "status": "done",
      "dependsOn": [
        "T005"
      ],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "scope": [
        "src/core/pipeline-executor.ts",
        "src/core/types.ts",
        "src/db/store.ts",
        "src/core/pipeline-executor.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Make progress percentage a first-class citizen of the pipeline (today the perfumes pipeline just shows 0%). Give progress sensible denominators and a real-time roll-up from member jobs, so a pipeline run reports meaningful overall progress instead of a flat 0%.",
      "doneWhen": "Pipeline progress is computed from member-job progress with sensible denominators and rolls up in real time; a unit test asserts the roll-up across member stages; the value is persisted/surfaced for the dashboard."
    },
    {
      "id": "T017",
      "title": "Manually dismiss stuck items",
      "status": "done",
      "dependsOn": [
        "T005",
        "T008"
      ],
      "gate": null,
      "tags": [
        "coding",
        "framework",
        "dashboard"
      ],
      "scope": [
        "src/db/schema.sql",
        "src/db/store.ts",
        "src/api/server.ts",
        "dashboard/app/page.tsx",
        "src/db/store.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Some items will never process (genuinely bad data). Add a way to MANUALLY mark a stuck item as permanently dismissed so it drops off the dashboard front page. MUST be manual-only, never automatic, and distinct from 'unstick' (which retries). Likely a new dismissed status on work_items plus an API + dashboard control.",
      "doneWhen": "A stuck item can be manually dismissed and then no longer appears in the stuck list/front page; dismissal is never automatic and is distinct from unstick/retry; unit tests cover dismiss + that a dismissed item stays off the stuck list."
    },
    {
      "id": "T018",
      "title": "Dashboard: edit service rate limits",
      "status": "done",
      "dependsOn": [
        "T008"
      ],
      "gate": null,
      "tags": [
        "coding",
        "dashboard"
      ],
      "scope": [
        "src/db/store.ts",
        "src/api/server.ts",
        "dashboard/app/services/page.tsx",
        "src/db/store.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "The Services page is read-only. Add the ability to adjust a service's rate_per_minute / daily_cap / monthly_cap from the UI, persisted to the services table (overriding the code default). Reconcile code-sync vs user-edits the same way the job enabled-toggle does (user override wins / is preserved across code sync).",
      "doneWhen": "Service rate_per_minute / daily_cap / monthly_cap are editable from the Services page and persisted; a code-sync does not clobber a user override (reconciled like the enabled toggle); unit tests cover persistence + the reconcile rule."
    },
    {
      "id": "T019",
      "title": "Dashboard: read-only DB browser",
      "status": "done",
      "dependsOn": [
        "T008"
      ],
      "gate": null,
      "tags": [
        "coding",
        "dashboard"
      ],
      "scope": [
        "src/api/server.ts",
        "src/db/store.ts",
        "dashboard/app/db/page.tsx",
        "dashboard/app/lib/api.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Add a read-only SQLite table viewer page to the dashboard so the local DB can be browsed without building a feature per query. List tables and page through rows via a strictly read-only API path (no arbitrary mutation, no write SQL). Respect the existing read-only-client model.",
      "doneWhen": "A dashboard page lists the DB tables and shows paged rows via a read-only API; there is no write/mutation path exposed; the dashboard build passes."
    },
    {
      "id": "T020",
      "title": "Dashboard: remove the Jobs tab",
      "status": "done",
      "dependsOn": [
        "T008"
      ],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/jobs/",
        "dashboard/app/layout.tsx",
        "dashboard/app/page.tsx",
        "dashboard/app/components/Dag.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "Pipelines are the unit of work; jobs should only be viewed via their pipeline. Drop the /jobs tab + flat top-level job list, but keep per-job run/log drill-down reachable from the pipeline DAG nodes (standalone jobs as a section there).",
      "doneWhen": "The /jobs flat tab + list is removed; per-job run/log drill-down is still reachable from pipeline DAG nodes; the dashboard build passes with no dangling links to the removed tab."
    },
    {
      "id": "T021",
      "title": "Dashboard: clickable overview stat tiles that filter",
      "status": "done",
      "dependsOn": [
        "T008"
      ],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/page.tsx",
        "dashboard/app/ui.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "The four overview tiles (Running / Succeeded / Failed runs / Stuck items) should be clickable and filter the lists below to just that category.",
      "doneWhen": "Each of the four tiles is clickable and filters the lists below to its category (with a way to clear the filter); the dashboard build passes."
    },
    {
      "id": "T022",
      "title": "Dashboard: collapse the pipeline-run member-runs list",
      "status": "done",
      "dependsOn": [
        "T008"
      ],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/pipeline-runs/[id]/",
        "dashboard/app/ui.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "For a repeat-until-stable pipeline (perfumes), each cycle re-runs all stages, so one pipeline run accumulates ~one row per stage per cycle (can be ~100). It is accurate but overwhelming — group/collapse the member-runs list (e.g. latest per stage, or grouped by cycle/stage) rather than a flat 100-row list.",
      "doneWhen": "The pipeline-run member-runs list is grouped/collapsed (e.g. latest per stage per cycle) instead of a flat long list, while still letting you reach each member run; the dashboard build passes."
    },
    {
      "id": "T023",
      "title": "Security hardening of dashboard + API",
      "status": "done",
      "dependsOn": [
        "T001"
      ],
      "gate": null,
      "tags": [
        "coding",
        "security"
      ],
      "scope": [
        "src/api/server.ts",
        "src/config.ts",
        "src/api/server.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Harden the dashboard + API so remote actors can't abuse the run/toggle endpoints. Tighten CORS (currently '*') to an allowlist/same-origin, confirm the API binds to 127.0.0.1, and guard the mutating run/toggle endpoints (loopback/token check). Keep the read-only dashboard client working. Pairs with the Tailscale remote-access task.",
      "doneWhen": "CORS is no longer '*' (allowlist/same-origin), the API bind address is confirmed loopback (configurable), and the run/toggle endpoints reject unauthorised callers; unit tests cover a disallowed origin and an unauthorised mutation being rejected."
    },
    {
      "id": "T024",
      "title": "Remote-accessible dashboard via Tailscale",
      "status": "done",
      "dependsOn": [
        "T023"
      ],
      "gate": null,
      "tags": [
        "coding",
        "infra"
      ],
      "scope": [
        "dashboard/",
        "scripts/",
        "README.md"
      ],
      "design": null,
      "verify": [],
      "do": "Make the dashboard reachable over a Tailscale tailnet (not the open internet) via `tailscale serve`. Keep the dashboard bound to localhost and keep the API on loopback (127.0.0.1) — NEVER expose the API. Add API-proxying so the dashboard serves its data through its OWN origin (e.g. Next.js rewrites / route handlers): a remote browser talks only to the dashboard, and the dashboard server (on the Mini) talks to 127.0.0.1:4789. Document the one-time `tailscale serve` setup (and that Funnel must stay OFF) in the README. The machine-side tailscale up / tailscale serve is operational; this task delivers the code + docs.",
      "doneWhen": "Dashboard data loads end-to-end when the browser can reach ONLY the dashboard origin (API proxied through the dashboard; API still bound to 127.0.0.1 and never exposed); README documents the `tailscale serve` setup with Funnel off and the T023 guards in front; dashboard build + typecheck pass. (Live tailnet reachability is verified operationally, not in CI.)"
    },
    {
      "id": "T025",
      "title": "Set package versions to 1.0.0",
      "status": "done",
      "dependsOn": [
        "T001"
      ],
      "gate": null,
      "tags": [
        "chore"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "package.json",
        "dashboard/package.json"
      ],
      "design": null,
      "verify": [],
      "do": "Both packages are pinned at 0.1. Bump the root and dashboard package.json version fields to 1.0.0.",
      "doneWhen": "Both package.json version fields read 1.0.0; typecheck and the dashboard build pass."
    },
    {
      "id": "T026",
      "title": "Docs audit — end-state sweep of README + CLAUDE.md",
      "status": "done",
      "dependsOn": [
        "T001",
        "T002",
        "T003",
        "T004",
        "T005",
        "T006",
        "T007",
        "T008",
        "T009",
        "T010",
        "T011",
        "T012",
        "T013",
        "T014",
        "T015",
        "T016",
        "T017",
        "T018",
        "T019",
        "T020",
        "T021",
        "T022",
        "T023",
        "T024",
        "T025"
      ],
      "gate": null,
      "tags": [
        "docs"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "high",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "README.md",
        "CLAUDE.md"
      ],
      "design": null,
      "verify": [
        "npx tsc --noEmit"
      ],
      "do": "Final end-state documentation sweep after all feature work has landed. Re-audit README.md + CLAUDE.md against the SHIPPED repo and fix any drift the per-task lockstep updates missed: the daily places schedule, the consolidated service-level caps (per-job caps gone), the manual ledger-prune + dismiss-stuck-items features, first-class pipeline progress, the shared headless-browser helper, the dashboard changes (no Jobs tab, filtering tiles, collapsed member-runs, read-only DB browser, editable service limits), the Tailscale remote-access + security-hardening model, and the 1.0.0 version. Keep every claim matched to code.",
      "doneWhen": "README.md + CLAUDE.md accurately describe the final shipped architecture, jobs, schedules, caps, dashboard pages, remote-access/security posture, and version; no stale claim from any earlier task remains; typecheck clean."
    },
    {
      "id": "T027",
      "title": "Validation gates: declare contracts on real jobs so gates fire",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "scope": [
        "src/jobs/perfumes/*.ts",
        "src/jobs/places/*.ts",
        "src/core/types.ts",
        "src/core/*.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "T006 built the gate MECHANISM but no real job declares contracts, so zero gates fire. Declare produces/consumes ArtifactContracts on the perfumes + places pipeline stages. Each contract is SHAPE + NON-EMPTY: assert the produced artifact exists, is non-empty, and has its expected fields/columns (e.g. the Takeout CSV has its expected headers; a parsed perfume has name + notes; a resolved place has a place_id) — enough to catch real format drift without brittle full-schema checks. Verify against cached data/ fixtures — NO live calls.",
      "doneWhen": "Perfumes + places stages declare produces/consumes shape+non-empty contracts; gates derive and fire between stages (pipeline start-log gate count > 0); a unit test proves a satisfied contract passes and a drifted/empty one fails the consumer; no live API/scrape in verification."
    },
    {
      "id": "T028",
      "title": "Validation gates: surface on the DAG graph + failure logs",
      "status": "done",
      "dependsOn": [
        "T027"
      ],
      "gate": null,
      "tags": [
        "dashboard",
        "framework"
      ],
      "scope": [
        "src/core/dag.ts",
        "src/api/server.ts",
        "dashboard/app/components/Dag.tsx",
        "dashboard/app/pipeline-runs/[id]/page.tsx",
        "dashboard/app/lib/api.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Represent validation gates on the pipeline DAG graph: render a gate marker on the producer->consumer edge in a pipeline RUN, make a FAILED gate visually distinct (e.g. red), and make the gate-failure run/logs reachable by clicking it. Expose gate state via the API so the dashboard can render it. Gates show only on a pipeline RUN, never on the structure-only /pipelines/[name] view.",
      "doneWhen": "Gates appear on the pipeline-run DAG between producer->consumer; a failed gate is visually distinct and links to its failure logs; gates do NOT show on the structure-only /pipelines/[name] graph; dashboard build passes."
    },
    {
      "id": "T029",
      "title": "Per-item progress: emit ctx.progress(i/N) + per-item logs across all jobs",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "scope": [
        "src/jobs/perfumes/*.ts",
        "src/jobs/places/*.ts",
        "src/core/types.ts",
        "src/core/*.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Jobs that process N items don't report progress as they go — e.g. perfumes-find-url works 41 items but never logs `1/41 …` and the run % doesn't advance. Make every item-processing job emit `ctx.progress(i/N*100)` and a per-item log line as it works (sensible denominators), not just a final summary. Apply across ALL such jobs, not just find-url.",
      "doneWhen": "Item-processing jobs emit per-item progress + a per-item log line; the run % advances during the run (verified against cached fixtures / scratch DB, no live calls); covers every item-loop job."
    },
    {
      "id": "T030",
      "title": "Move the Chrome profile to a shared framework location",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "src/core/browser.ts",
        "src/jobs/perfumes/fetch.ts",
        "src/config.ts",
        ".gitignore"
      ],
      "design": null,
      "verify": [],
      "do": "The persistent Chrome profile lives inside the perfumes job (src/jobs/perfumes/data/chrome-profile). Move it to a framework-level location — a top-level data/chrome-profile/ resolved in src/core/browser.ts and env-overridable (e.g. LOCALJOBS_CHROME_PROFILE) — so it isn't perfumes-specific and other scrapers (e.g. the places resolver) can reuse the warmed, trusted profile. Keep the new location gitignored; perfumes fetch keeps working against it.",
      "doneWhen": "The Chrome profile path is framework-level (top-level data/chrome-profile, env-overridable) not under src/jobs/perfumes/data; the new path is gitignored; perfumes fetch still uses it; typecheck + tests green."
    },
    {
      "id": "T031",
      "title": "Trim the README — it's a wall of text",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "docs"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "README.md"
      ],
      "design": null,
      "verify": [],
      "do": "The README is a wall of text. Trim it: REMOVE the ## Layout file-tree entirely, and present the worked-example pipelines compactly/scannably (a short line each, not paragraphs). Keep the essential what-it-is / how-to-run / ports / config content and the dashboard pages list. Aim for a noticeably shorter, scannable doc.",
      "doneWhen": "The Layout file-tree is gone; example pipelines are compact; the core what-it-is / how-to-run / install content remains accurate; the README is materially shorter."
    },
    {
      "id": "T032",
      "title": "Don't count cancelled runs as failed",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/page.tsx",
        "dashboard/app/ui.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "Cancelled runs are currently lumped into the overview Failed tile. A cancelled run is a deliberate stop, not a failure. Stop counting cancelled as failed, and give it its OWN clickable Cancelled overview stat tile (filtering like the other tiles), consistent with how status is aggregated elsewhere.",
      "doneWhen": "Cancelled runs no longer count as Failed; a dedicated clickable Cancelled tile shows + filters them; dashboard build passes."
    },
    {
      "id": "T033",
      "title": "Dismissed vs ignored: park ignored items, show only on overview",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "coding",
        "framework",
        "dashboard"
      ],
      "scope": [
        "src/db/schema.sql",
        "src/db/store.ts",
        "src/api/server.ts",
        "dashboard/app/page.tsx",
        "dashboard/app/pipelines/page.tsx",
        "dashboard/app/pipelines/[name]/page.tsx",
        "dashboard/app/jobs/[name]/page.tsx",
        "src/db/store.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Manually-ignored items (the Ignore action) currently reappear as stuck and inflate stuck counts on the pipelines tab + pipeline/job detail. There is ONE ignore concept (not two): stuck = automatic after repeated failures; ignored = a manual park. Fix the root cause so an ignored item STAYS ignored (ignore persists; the stuck query excludes ignored; a re-run does not resurrect it), is NEVER counted as stuck, and is shown ONLY on the overview via a new fifth Ignored tile (which filters to them).",
      "doneWhen": "Ignored items never count as stuck and don't appear outside the overview; a 5th clickable Ignored overview tile shows + filters them; an ignored item stays ignored across re-runs; unit tests cover persistence + exclusion-from-stuck."
    },
    {
      "id": "T034",
      "title": "Services: explicit 'remove cap' control",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/services/page.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "On the Services edit-limits form it's unclear how to clear a cap back to 'no cap' (inputs just show placeholder text). Add an explicit control (e.g. a 'No cap' / clear button per field) that sets the value to no-cap (null) unambiguously, instead of relying on typing/blanking.",
      "doneWhen": "Each limit field has a clear UI way to set it back to no-cap (null); saving persists null; dashboard build passes."
    },
    {
      "id": "T035",
      "title": "Services: colour free services green",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/services/page.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "On the Services page, colour the 'free' service pill green (paid stays as-is) so free vs paid reads at a glance.",
      "doneWhen": "Free-service pills render green; paid unchanged; dashboard build passes."
    },
    {
      "id": "T036",
      "title": "Keep cron schedule expressions on one line",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/globals.css",
        "dashboard/app/pipelines/[name]/page.tsx",
        "dashboard/app/jobs/[name]/page.tsx",
        "dashboard/app/services/page.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "Cron schedule expressions (e.g. perfumes `0 2 * * *`) wrap onto two lines even on a wide desktop. Keep the schedule on one line where possible (e.g. white-space:nowrap on the schedule cell/mono span). Keep this in mind for the mobile styling pass too.",
      "doneWhen": "Schedule cron expressions render on a single line on desktop; dashboard build passes."
    },
    {
      "id": "T037",
      "title": "Drop standalone jobs — everything is a pipeline",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "coding",
        "framework"
      ],
      "scope": [
        "src/jobs/registry.ts",
        "src/core/scheduler.ts",
        "src/db/store.ts",
        "src/api/server.ts",
        "dashboard/app/pipelines/page.tsx",
        "src/core/*.test.ts",
        "CLAUDE.md",
        "README.md"
      ],
      "design": null,
      "verify": [],
      "do": "Remove the concept of standalone (non-pipeline) jobs: EVERY job must belong to a pipeline declared in an explicit *.pipeline.ts manifest (a single job = a one-stage pipeline WITH its own manifest — no implicit wrapping). Drop the dashboard Standalone jobs section, the job-level standalone schedule/enable path (pipelines own scheduling), and the registry/scheduler handling for non-member jobs. A job with no manifest is a config error — fail loud at load. Now that demo is gone there are no standalone jobs to migrate.",
      "doneWhen": "No standalone-job code path remains; every job must be declared in a *.pipeline.ts manifest (single-job pipelines included); a job with no manifest fails loudly at load; the dashboard has no Standalone section; scheduler/registry handle only pipeline-driven jobs; typecheck + tests green; docs updated."
    },
    {
      "id": "T038",
      "title": "Rename 'pipeline' → 'workflow' everywhere",
      "status": "done",
      "dependsOn": [
        "T037"
      ],
      "gate": null,
      "tags": [
        "coding",
        "refactor"
      ],
      "scope": [
        "src/",
        "dashboard/",
        "src/db/schema.sql",
        "README.md",
        "CLAUDE.md"
      ],
      "design": null,
      "verify": [],
      "do": "Rename the pipeline concept to workflow everywhere: types (PipelineDefinition->WorkflowDefinition), DB tables (pipelines, pipeline_jobs, pipeline_runs, pipeline_run_logs -> workflow*) with a migration that PRESERVES existing rows, store functions, the discovery convention *.pipeline.ts -> *.workflow.ts (rename the existing local perfumes/places manifest files too, or they won't be discovered), pipeline-executor.ts -> workflow-executor, API routes (/api/pipelines, /pipeline-runs -> /api/workflows, /workflow-runs), dashboard routes/pages + all labels, the pipeline field on jobs, and all docs. Clean break — no old-name redirects needed. Mechanical but wide. Do this AFTER T037 so the model lands clean.",
      "doneWhen": "No 'pipeline' naming remains for the concept (code, DB tables, routes, UI, docs, *.workflow.ts files); a DB migration moves existing pipeline_* data to workflow_* without loss; typecheck + tests + dashboard build green; docs updated."
    },
    {
      "id": "T039",
      "title": "Programmatic input ingestion into a pipeline's data/raw",
      "status": "pending",
      "dependsOn": [],
      "gate": "needs-human",
      "tags": [
        "coding",
        "framework",
        "needs-human"
      ],
      "scope": [
        "src/api/server.ts",
        "src/core/types.ts",
        "src/jobs/places/*.ts",
        "src/jobs/perfumes/*.ts",
        "CLAUDE.md"
      ],
      "design": null,
      "verify": [],
      "do": "Add a way to push fresh input into a pipeline's data/raw without manual file copying. The mechanism (API endpoint vs CLI vs pull-from-source) is deferred to a human decision before building.",
      "doneWhen": "Mechanism decided with the human, then built so fresh input can be pushed into a pipeline's data/raw without manual copying."
    },
    {
      "id": "T040",
      "title": "Mobile dashboard styling pass",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui",
        "testing"
      ],
      "scope": [
        "dashboard/"
      ],
      "design": null,
      "verify": [
        "run-app"
      ],
      "do": "Make the dashboard responsive on mobile. Write an automated check that loads each dashboard page in a headless browser at an iPhone-17-class viewport (~402px wide) against a scratch DB and asserts basic mobile styling: no horizontal overflow, no text crossing element boundaries, no weird truncation/clipping. The in-place agent RUNS this check locally and records the per-page result in its worklog — it does NOT need to be added to CI. Then fix the responsive CSS (tables, stat cards, nav, DAG graph, schedule cells) until it passes.",
      "doneWhen": "An automated iPhone-17-viewport (~402px) check covers every dashboard page and asserts no horizontal overflow / no boundary-crossing / no truncation; the agent ran it locally and recorded the page-by-page outcome in .harness/worklog/T040.md; the dashboard passes it; dashboard build green."
    },
    {
      "id": "T041",
      "title": "Decide + wire up where pipeline output goes",
      "status": "pending",
      "dependsOn": [],
      "gate": "needs-human",
      "tags": [
        "framework",
        "needs-human"
      ],
      "scope": [
        "src/jobs/"
      ],
      "design": null,
      "verify": [],
      "do": "Decide where pipeline output ultimately lands (Obsidian vault / static site / cloud sync) and build the step that delivers it there, instead of leaving output in data/out. The destination is a human decision first.",
      "doneWhen": "Destination decided with the human; the delivery step is built and writes output there."
    },
    {
      "id": "T042",
      "title": "More intuitive remote dashboard access (alias)",
      "status": "pending",
      "dependsOn": [],
      "gate": "needs-human",
      "tags": [
        "infra",
        "needs-human"
      ],
      "scope": [
        "dashboard/",
        "scripts/",
        "README.md"
      ],
      "design": null,
      "verify": [],
      "do": "Make remote dashboard access friendlier than typing the tailnet IP:port on mobile — a Tailscale MagicDNS hostname, a `tailscale serve` nice URL, or a phone home-screen shortcut. Partly operational (Tailscale admin / the machine), not CI-verifiable.",
      "doneWhen": "The dashboard is reachable from the phone via a friendly alias/shortcut (human confirms)."
    },
    {
      "id": "T043",
      "title": "Joint review of the perfumes pipeline",
      "status": "pending",
      "dependsOn": [],
      "gate": "needs-human",
      "tags": [
        "review",
        "needs-human"
      ],
      "scope": [
        "src/jobs/perfumes/"
      ],
      "design": null,
      "verify": [],
      "do": "Walk through how the (overnight-built) perfumes pipeline actually works, together — it hasn't been reviewed jointly. A conversation, not a build.",
      "doneWhen": "The perfumes pipeline has been walked through together and any follow-ups captured."
    },
    {
      "id": "T044",
      "title": "Folder-hierarchy check-in",
      "status": "pending",
      "dependsOn": [],
      "gate": "needs-human",
      "tags": [
        "review",
        "needs-human"
      ],
      "scope": [
        "."
      ],
      "design": null,
      "verify": [],
      "do": "Review the repo's file/folder organisation together and confirm it's laid out the way the owner wants. Subjective, no pass/fail.",
      "doneWhen": "Repo layout reviewed together and confirmed / adjustments captured."
    },
    {
      "id": "T045",
      "title": "Dashboard styling — make it more joyful",
      "status": "pending",
      "dependsOn": [],
      "gate": "needs-human",
      "tags": [
        "dashboard",
        "ui",
        "needs-human"
      ],
      "scope": [
        "dashboard/"
      ],
      "design": null,
      "verify": [],
      "do": "Check in on the dashboard's overall look & feel and make it more joyful. Subjective design work needing the owner's eye.",
      "doneWhen": "Look & feel reviewed with the owner and agreed changes applied."
    },
    {
      "id": "T046",
      "title": "Investigate why the Claude CLI service has a per-minute rate limit",
      "status": "pending",
      "dependsOn": [],
      "gate": "needs-human",
      "tags": [
        "investigation",
        "needs-human"
      ],
      "scope": [
        "src/jobs/perfumes/claude-cli.service.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Investigate why the claude-cli service is configured with a per-minute rate limit (rate_per_minute). Work out whether that cap is actually necessary/correct — is the Claude CLI genuinely rate-limited per minute, or is the value arbitrary/leftover? Then decide whether to keep, adjust, or remove it.",
      "doneWhen": "The reason for the claude-cli per-minute rate limit is understood and a decision is recorded (keep / adjust / remove)."
    },
    {
      "id": "T047",
      "title": "Backlog view: green done icon + in-progress indicator",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/backlog/page.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "On the dashboard Backlog page (/backlog), make the done status badge/icon GREEN (it currently renders neutral), and add a distinct IN-PROGRESS icon. Note: TASKS.json only has pending/done (no in_progress status), so derive in-progress client-side as the single next-eligible task — the first not-done task in array order whose dependsOn are all done and which is not gated (i.e. the one the loop builds next) — and badge it distinctly from plain pending. (If a richer live currently-building signal is wired later, prefer that.)",
      "doneWhen": "Done tasks show a green done badge/icon on /backlog; the next-eligible task shows a distinct in-progress icon; plain pending tasks stay neutral; the dashboard build passes."
    },
    {
      "id": "T048",
      "title": "Overview: hide the Stuck-items section when empty",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/page.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "On the overview page, hide the entire \"Stuck items\" SECTION (its heading + list/empty-state) when there are zero stuck items — render the section only when there is at least one stuck item. Keep the Stuck stat TILE/count at the top regardless. Apply the same empty-section hiding to the Ignored section if it has its own dedicated section.",
      "doneWhen": "When nothing is stuck, the overview shows no \"Stuck items\" heading or section (just the count tile up top); when something is stuck the section renders as before; the dashboard build passes."
    },
    {
      "id": "T049",
      "title": "Sweep the repo for lingering \"pipeline\" usage (rename to workflow)",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "refactor",
        "cleanup"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "src/",
        "dashboard/",
        "docs/",
        "README.md",
        "CLAUDE.md",
        "src/db/schema.sql"
      ],
      "design": null,
      "verify": [],
      "do": "After the pipeline→workflow rename (T038), do a COMPLETE repo scan for any remaining use of the word \"pipeline\" and convert it to \"workflow\" across the board — code identifiers, types, API routes, UI labels/headings, comments, and docs (README.md / CLAUDE.md / docs) / schema. EXCLUDE legitimate residual references: the DB migration that renames the old `pipelines`/`pipeline_*` tables MUST still name them to perform the rename — leave those; and node_modules, .git, data/, and historical .harness/worklog/ notes are out of scope.",
      "doneWhen": "A `grep -ri pipeline` over tracked source + docs (excluding the migration’s necessary old-table references, node_modules, data/, and worklog) returns nothing; every stray identifier/label/route now reads \"workflow\"; typecheck + tests + dashboard build pass."
    },
    {
      "id": "T050",
      "title": "Workflows page: get the Run button onto one line",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/workflows/page.tsx",
        "dashboard/app/page.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "On the workflows page the ▶ Run button is chunky — the play icon and the word \"Run\" wrap onto two lines. Make it render on a SINGLE line (e.g. white-space:nowrap / inline-flex / tighter padding / smaller icon — styling is open, the user is not opinionated). Apply the same fix to any other Run button that wraps (e.g. the workflow cards on the overview).",
      "doneWhen": "The Run button renders on one line (icon + label, no wrap) on the workflows page (and any other card where it wrapped); dashboard build passes."
    },
    {
      "id": "T051",
      "title": "Workflows page: clean up the last-run column (pill + time)",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/workflows/page.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "On the workflows page, the last-run column shows a status pill (running/success) plus a relative time (e.g. \"8 minutes ago\") that currently wraps awkwardly across two lines. Make it clean: EITHER force the pill + time onto one line (white-space:nowrap), OR stack them deliberately (pill on its own line, time on the next). Pick whichever reads best — the user is fine with either as long as it is not an awkward wrap.",
      "doneWhen": "The last-run cell renders cleanly (pill + time on one line, or deliberately stacked) with no awkward mid-value wrap; dashboard build passes."
    },
    {
      "id": "T052",
      "title": "Workflows page: centre the Stages + Schedule column values",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/workflows/page.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "On the workflows page table, centre-align the VALUES in the \"Stages\" and \"Schedule\" columns (they read better centred). Leave the \"Last run\" and \"Next\" column values LEFT-aligned as they are. Centre the matching headers too if that reads cleaner.",
      "doneWhen": "Stages + Schedule column values (and ideally their headers) are centre-aligned on the workflows page; Last run + Next stay left-aligned; dashboard build passes."
    },
    {
      "id": "T053",
      "title": "Database view: surface useful canned queries (not a SQL editor)",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "coding",
        "framework",
        "dashboard"
      ],
      "scope": [
        "src/db/store.ts",
        "src/api/server.ts",
        "dashboard/app/db/page.tsx",
        "dashboard/app/lib/api.ts",
        "src/db/store.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "The Database view currently only does full per-table scans, which are hard to parse. Add a set of useful PREDEFINED/canned queries surfaced on that page — NOT a free-form SQL editor — covering the most useful read-only views, e.g.: recent failed runs, stuck + ignored items by job, service usage/spend this month vs caps, workflow-run durations/outcomes, work_items counts by status per job. Implement them as NAMED, server-side, strictly read-only (SELECT-only) queries — the same safety model as the existing table browser, never arbitrary SQL from the client — exposed via the API and rendered on the DB page alongside the table browser.",
      "doneWhen": "The Database view offers a set of named common queries (read-only) with results rendered; no free-form/arbitrary SQL is accepted from the client; the existing table browser still works; a unit test covers each canned query against the scratch DB; typecheck + tests + dashboard build pass."
    },
    {
      "id": "T054",
      "title": "Database view: click a row to see full row data (truncation fix)",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/db/page.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "On the Database view table browser, long cell values (especially the message column on e.g. workflow-run logs) are truncated with no way to read the full value. Make each row clickable to open a popover/modal showing the COMPLETE row data — every column, untruncated (a key/value list or pretty-printed JSON). The full row data is already returned by the read-only API; this is purely a display affordance, closeable (Esc / click-away).",
      "doneWhen": "Clicking a row in the DB table browser opens a popover/modal showing the full untruncated value of every column; it is dismissable; the table browser otherwise works as before; dashboard build passes."
    },
    {
      "id": "T055",
      "title": "Backlog view: show each task’s escalation path",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "src/api/server.ts",
        "dashboard/app/lib/api.ts",
        "dashboard/app/backlog/page.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "On the Backlog view, show each task’s ESCALATION PATH (the model/effort ladder the loop climbs on repeated failure), not just the starting model. Render the EFFECTIVE ladder — rung 0 (task model/effort, else defaults) then each escalation rung — resolving per-task `escalation` else `defaults.escalation` (same resolution the loop’s task_ladder does). This needs `/api/backlog` to ALSO return the backlog `defaults` (it currently returns only `tasks`) so inherited model/escalation can be resolved, and the BacklogTask type to include `escalation`. Also: where a task INHERITS the default model/effort, show the ACTUAL resolved value (e.g. \"opus/high\"), never the literal word \"default\" — so the view always reflects the real current default and updates automatically if `defaults` changes.",
      "doneWhen": "Each task on /backlog shows its escalation path (e.g. \"sonnet/medium → opus/high\") resolving per-task vs defaults inheritance; a task that inherits the default shows the REAL resolved model/effort (never the word \"default\"); a task with no escalation shows just its single rung; the API returns defaults; dashboard build + typecheck pass."
    },
    {
      "id": "T056",
      "title": "Overview: workflow status pill links to that run",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/page.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "On the overview page, each workflow card shows a status pill (running / success / cancelled / …) for its last/current run, but there is no one-click way to reach that run — you have to click the workflow, find the run, then Details. Make the pill a LINK straight to that specific workflow run (/workflow-runs/{last_run.id}) — i.e. whichever run it references (the currently-running one, else the most recent). The run id is already on the card’s last_run.",
      "doneWhen": "On the overview, clicking a workflow card’s status pill navigates directly to that workflow run’s detail page in one click; a card with no run yet shows a non-link pill (no dead link); dashboard build passes."
    },
    {
      "id": "T057",
      "title": "Validation gates: clickable detail + comprehensive logging",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "coding",
        "framework",
        "dashboard"
      ],
      "scope": [
        "src/core/",
        "src/api/server.ts",
        "src/db/store.ts",
        "dashboard/app/workflow-runs/[id]/page.tsx",
        "dashboard/app/components/Dag.tsx",
        "dashboard/app/lib/api.ts",
        "src/core/*.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "Validation gates now render on a workflow run (T028) but you cannot INSPECT them — no way to see what a gate validated, its logs, or to manually double-check it. Add comprehensive gate logging + clickable gate detail: (1) the workflow executor logs, per gate check, the contract key, producer→consumer, what it ASSERTED (the contract description) and the pass/fail result + any violation detail, into the workflow run’s framework logs; (2) expose per-gate detail via the API (contract key + description, state, and the relevant member-run/log refs); (3) make EVERY gate on the run DAG clickable to a detail view showing what it validates and its outcome — passing and pending gates too, not only failed ones (T028) — so you can confirm a gate is doing what you expect.",
      "doneWhen": "Each validation gate on a workflow run is clickable and opens a detail view showing what it validates (contract key + description, producer→consumer) and its outcome; the executor logs each gate check (assertion + result/violation) to the run’s framework logs; passing/pending gates are inspectable, not just failed ones; a unit test covers the gate logging/detail; typecheck + tests + dashboard build pass."
    },
    {
      "id": "T058",
      "title": "Back-nav: drop \"workflow run\" words, show just the run id",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/ui.tsx",
        "dashboard/app/runs/[id]/page.tsx"
      ],
      "design": null,
      "verify": [],
      "do": "When you open a job/run from within a workflow run, the back-nav header reads \"← workflow run {id}\" (lowercase). Drop the words \"workflow run\" — show just the run id (a leading \"#\" is fine, e.g. \"← #42\"). Update both the backFrom helper’s workflow-run label (ui.tsx) and the runs/[id] fallback that also builds \"workflow run {id}\".",
      "doneWhen": "The back-link to a workflow run shows just the run id (no \"workflow run\" words); both the ?from-derived label and the runs/[id] fallback are updated; dashboard build passes."
    },
    {
      "id": "T059",
      "title": "Status labels: consistent success/failure wording (parity)",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/"
      ],
      "design": null,
      "verify": [],
      "do": "Status terminology lacks parity: the UI shows success as \"OK\" in places, which does not pair with \"failed\". Standardize the human-facing success/failure labels across the WHOLE dashboard to a consistent pair — use \"Succeeded\" / \"Failed\" (matching the existing overview tile) everywhere a run/gate status is shown (StatusBadge, status pills, gate states, etc.), and keep the other labels in consistent tense (Running / Cancelled / Pending). Best done by centralizing a statusLabel() map in ui.tsx and using it at every render site rather than printing the raw status. DISPLAY ONLY — do NOT change the underlying status VALUES (\"success\"/\"failed\"/\"cancelled\"/… in the types / DB / API), only how they render.",
      "doneWhen": "Success renders consistently as \"Succeeded\" (no stray \"OK\") and failure as \"Failed\" across every dashboard view; other statuses read in consistent tense; underlying status values unchanged; dashboard build passes."
    },
    {
      "id": "T060",
      "title": "Backlog view: add a Done section",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/backlog/page.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "On the Backlog view (/backlog), add a THIRD section \"Done\" alongside \"Harness-buildable\" and \"Needs a human\", listing all completed (status:done) tasks so the page shows a complete history of work done. Done tasks must appear ONLY under Done — the Harness-buildable and Needs-a-human sections show only NOT-done tasks. Order Done by id; it will be long (~40+), so keep it compact/scannable (a one-line row per task; collapsing it by default is fine).",
      "doneWhen": "/backlog shows three sections — Harness-buildable, Needs a human, Done; done tasks appear only under Done (not under buildable); the other two sections list only pending tasks; the section counts are correct; dashboard build passes."
    },
    {
      "id": "T061",
      "title": "Backlog view: a DAG / dependency graph of the tasks",
      "status": "done",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/backlog/page.tsx",
        "dashboard/app/components/Dag.tsx",
        "dashboard/app/globals.css"
      ],
      "design": null,
      "verify": [],
      "do": "Add a DAG / graph view of the backlog on /backlog that shows the task dependency hierarchy visually. Each node is the task ID; lay nodes out in dependency (topological) order with edges for `dependsOn` — the existing components/Dag.tsx wave-layout can be reused/generalized. Colour nodes by status (done / pending / needs-human). On hover (or click) over a node, show more detail about that task — title, model + escalation, status, and the do/doneWhen summary. Offer it alongside the existing list (a toggle, or a second section). Data is already in /api/backlog (tasks + dependsOn).",
      "doneWhen": "/backlog offers a DAG view: nodes labelled by task id, edges for dependsOn, nodes coloured by status, and per-node detail on hover/click; the existing list view still works; dashboard build passes."
    },
    {
      "id": "T062",
      "title": "Daily self-inspection workflow: review run logs → propose/fix framework issues",
      "status": "pending",
      "dependsOn": [],
      "gate": "needs-human",
      "tags": [
        "framework",
        "workflow",
        "needs-human"
      ],
      "scope": [
        "src/jobs/"
      ],
      "design": null,
      "verify": [],
      "do": "Add a workflow that runs ONCE PER DAY and inspects the new logs produced by other workflows runs since the last inspection (run_logs / workflow framework logs on the box), evaluates any errors for FRAMEWORK-LEVEL problems (not job-specific bad-data), and — where a fix is safe — proposes and applies it. Envisioned as a one-step workflow whose single job instructs Claude (headless) to read the recent logs and propose fixes. NEEDS-HUMAN: discuss the design first — how Claude runs autonomously and safely APPLIES/commits framework fixes (vs just proposing), how to distinguish framework-level vs job-level errors, how it interacts with the build loop / CI / the always-push rule, guardrails against runaway or unreviewed changes, and how findings are surfaced (dashboard / notification / a drafted task in the backlog).",
      "doneWhen": "Design agreed with the human; then a daily workflow reviews new run logs, identifies framework-level errors, and proposes (and where safe, applies) fixes within the agreed guardrails."
    },
    {
      "id": "T063",
      "title": "Validation gate: open a dedicated page (not an in-page scroll)",
      "status": "pending",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/workflow-runs/[id]/page.tsx",
        "dashboard/app/components/Dag.tsx",
        "dashboard/app/lib/api.ts",
        "src/api/server.ts"
      ],
      "design": null,
      "verify": [],
      "do": "On a workflow run, clicking a validation gate currently just SCROLLS to an in-page \"Validation Gates\" section. Make it behave like clicking a job node — navigate to its OWN dedicated page (e.g. a /workflow-runs/[id]/gates/[key] route, or /gates/...) that shows that gate in full: what it validates (contract key + description), the two stages it bridges, its state, and its logs. Refactor the in-page section into that dedicated page + route (add a read-only API route for a single gate if needed).",
      "doneWhen": "Clicking a validation gate on a workflow run navigates to a dedicated gate-detail PAGE (not an in-page scroll), consistent with how a job node opens its own page; the page shows the gate contract/description, the stages it bridges, state, and logs; dashboard build passes."
    },
    {
      "id": "T064",
      "title": "Validation gates: reframe producer/consumer as input/output",
      "status": "pending",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui",
        "framework"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/",
        "src/core/",
        "src/core/*.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "The validation-gate detail/logs label the two sides \"producer\" and \"consumer\", which is jargon-y. Reframe as INPUT/OUTPUT: the producer side validates the upstream stage’s OUTPUT; the consumer side validates the downstream stage’s INPUT. Relabel the UI and the gate-check log lines accordingly — e.g. \"output (from <stage>)\" / \"input (to <stage>)\" — so it reads intuitively. Display/wording only; keep the underlying produces/consumes contract model + field names unchanged.",
      "doneWhen": "Gate detail + gate-check log lines read in input/output terms instead of producer/consumer; the underlying contract data model is unchanged; dashboard build + any affected tests pass."
    },
    {
      "id": "T065",
      "title": "Validation gate: visual representation of what is being checked",
      "status": "pending",
      "dependsOn": [
        "T063",
        "T064"
      ],
      "gate": null,
      "tags": [
        "dashboard",
        "ui",
        "framework"
      ],
      "scope": [
        "dashboard/app/",
        "src/core/types.ts",
        "src/core/",
        "src/api/server.ts",
        "dashboard/app/lib/api.ts",
        "src/core/*.test.ts"
      ],
      "design": null,
      "verify": [],
      "do": "On the validation-gate page the producer/consumer log lines do not make it obvious WHAT the gate checks. Add a clear, VISUAL representation of the contract: a left-to-right flow — upstream stage OUTPUT → [the gate: what it validates] → downstream stage INPUT — so you can see at a glance what data the gate expects and whether it is satisfied. Surface the contract’s expected shape (key + description + the fields/columns/non-empty it asserts) and, where feasible, a small sample/summary of the ACTUAL artifact that flowed, with pass/fail per expectation. This likely needs the produces/consumes ArtifactContract to optionally declare a machine-readable expected shape (fields/columns) for display, and the gate to report expected-vs-actual rather than just ok/violations. Builds on T063 (dedicated gate page) + T064 (input/output framing).",
      "doneWhen": "The gate page visually shows, left-to-right, output → gate(what it checks) → input, making it obvious what the gate validates and what actually flowed (expected shape + a sample/summary + pass/fail per expectation); contracts can declare an expected shape for display; typecheck + tests + dashboard build pass."
    },
    {
      "id": "T066",
      "title": "Workflow-run DAG: fix the graph layout on mobile/narrow widths",
      "status": "pending",
      "dependsOn": [],
      "gate": null,
      "tags": [
        "dashboard",
        "ui"
      ],
      "model": "claude-sonnet-4-6",
      "effort": "medium",
      "escalation": [
        {
          "model": "claude-opus-4-8",
          "effort": "high"
        }
      ],
      "scope": [
        "dashboard/app/components/Dag.tsx",
        "dashboard/app/globals.css",
        "dashboard/app/workflow-runs/[id]/page.tsx"
      ],
      "design": null,
      "verify": [
        "run-app"
      ],
      "do": "On a workflow-run page, the DAG graph (components/Dag.tsx — left-to-right waves) looks misaligned on narrow/mobile widths: the vertical centring of nodes/waves reads as visually off as the width shrinks. Make it responsive — EITHER fix the vertical alignment of the left-to-right layout at narrow widths, OR switch to a TOP-DOWN (vertical) layout below a small-screen breakpoint (lean: top-down likely reads better on a phone since multi-node waves do not fit horizontally). The agent picks whichever looks best. Validate visually: render the page at an iPhone-class viewport (~402px) AND at desktop width and confirm the graph is cleanly aligned + legible at both (reuse the T040 viewport-screenshot approach; screenshot-verifiable, not needs-human).",
      "doneWhen": "The workflow-run DAG renders cleanly aligned + legible at a narrow/mobile viewport (~402px) and at desktop width, with no awkward vertical-centring misalignment; validated via viewport screenshots at both widths recorded in the worklog; dashboard build passes."
    }
  ]
}
