{
  "_about": "Canonical, controlled facet vocabulary for the difficulty auto-tuning system. SOURCE OF TRUTH: the add-to-backlog authoring step picks facet values ONLY from this file; the calibration/policy validate against it; a human curates it (deliberate additions only, never ad-hoc per task). Facets are the join key that lets the policy generalise escalation history from past tasks to new ones. needs-human (gated) tasks are CARVED OUT entirely — they never run through the loop, so they get no facets and never enter calibration.",
  "version": 1,
  "facets": {
    "layer": {
      "cardinality": "exactly-one",
      "desc": "WHERE the change lives — inferable largely from the task's scope file paths.",
      "values": {
        "ui":              { "desc": "dashboard CSS / components / visual layout (dashboard/app/**)", "difficulty": "low-med" },
        "dashboard-logic": { "desc": "dashboard client data/state/api-client (dashboard/app/lib/*, polling, non-visual)", "difficulty": "med" },
        "api":             { "desc": "daemon HTTP routes (src/api/server.ts)", "difficulty": "med" },
        "job":             { "desc": "example-workflow stage logic (src/jobs/**)", "difficulty": "med" },
        "service":         { "desc": "shared rate-limit/quota services (src/services/**)", "difficulty": "med-high" },
        "framework":       { "desc": "executor/scheduler/dag/workflow-executor/core (src/core/**)", "difficulty": "high" },
        "db":              { "desc": "schema/store/migrations (src/db/**)", "difficulty": "high" },
        "harness":         { "desc": "the .harness/ loop + scaffolding, backlog authoring", "difficulty": "varies" }
      }
    },
    "work-type": {
      "cardinality": "exactly-one",
      "desc": "WHAT KIND of change it is — the main thing the authoring LLM reasons about.",
      "values": {
        "style":      { "desc": "visual / CSS / layout only", "difficulty": "very-easy" },
        "logging":    { "desc": "log verbosity / messages", "difficulty": "very-easy" },
        "docs":       { "desc": "README / CLAUDE / spec text", "difficulty": "very-easy" },
        "config":     { "desc": "env / config knobs", "difficulty": "easy" },
        "component":  { "desc": "new/changed UI component or page", "difficulty": "med" },
        "endpoint":   { "desc": "new/changed API route", "difficulty": "med" },
        "bugfix":     { "desc": "fix a specific defect", "difficulty": "med-varies" },
        "feature":    { "desc": "multi-file new capability", "difficulty": "high" },
        "migration":  { "desc": "schema / data migration", "difficulty": "high" },
        "llm-prompt": { "desc": "recommender / LLM prompt logic", "difficulty": "high" },
        "refactor":   { "desc": "rename / move / restructure", "difficulty": "high" }
      }
    },
    "risk": {
      "cardinality": "zero-or-more",
      "desc": "Danger modifiers; each pushes difficulty UP. Used by the policy only once a cell is dense.",
      "values": {
        "touches-schema":   { "desc": "scope includes src/db/schema.sql or src/db/index.ts (the T098 trap)" },
        "touches-executor": { "desc": "scope includes core/executor, workflow-executor, dag, or scheduler" },
        "full-stack":       { "desc": "spans db + api + dashboard in one task" },
        "cross-cutting":    { "desc": "many files / a shared convention" }
      }
    }
  },
  "policy": {
    "_about": "Difficulty auto-tuning policy knobs. The policy picks, per task, the CHEAPEST tier on the ladder below whose historical first-attempt success rate for the task's (layer × work-type) cell is >= floor, requiring at least minN samples; otherwise it falls back to the authored difficulty (cold start). Bias is toward cheap — the escalation ladder is the safety net.",
    "floor": 0.75,
    "minN": 6
  },
  "tiers": {
    "_about": "The GLOBAL difficulty ladder, cheapest → most expensive. The policy chooses a START tier from here per task; escalation walks UP it. The authored model/effort is the cold-start start tier. Project-specific (override per project).",
    "ladder": [
      { "model": "claude-sonnet-4-6", "effort": "low" },
      { "model": "claude-sonnet-4-6", "effort": "medium" },
      { "model": "claude-sonnet-4-6", "effort": "high" },
      { "model": "claude-opus-4-8", "effort": "medium" },
      { "model": "claude-opus-4-8", "effort": "high" },
      { "model": "claude-opus-4-8", "effort": "xhigh" },
      { "model": "claude-opus-4-8", "effort": "max" }
    ]
  }
}
