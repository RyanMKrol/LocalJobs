## Do

Rename the TV-season workflow from the un-intuitive **`plex`** to a clear, descriptive name. It
currently shows as "plex" at `/workflows/plex`, which says nothing about what it does (and is
doubly confusing now that a separate *movies* workflow also uses Plex). Rename it to
**`missing-tv-seasons`** (the owner is fine with "something similar" — keep it a clean
lowercase-kebab slug since the workflow `name` is the id, the URL slug, and the DB key). Do:

1. **Manifest:** change `name: 'plex'` → `name: 'missing-tv-seasons'` in
   `src/jobs/plex/plex.workflow.ts` (and its `description` if it still says "plex"). Leave the
   member JOB names (`plex-tv-snapshot`, `tmdb-season-check`, `plex-seasons-notify`) unchanged —
   they're internal and their `work_items` are keyed by job name, so renaming them would orphan the
   ledger; only the WORKFLOW name changes. The `src/jobs/plex/` folder can stay as-is (optional to
   rename; the registry discovers by glob, not folder name — if you do rename the folder, fix all
   imports + the `data/` path and keep it a separate, clearly-scoped step).
2. **DB migration (preserve history, no orphan):** add an idempotent migration in
   `src/db/index.ts` that renames the workflow in the DB so existing runs aren't orphaned under a
   now-manifest-less `plex`: `UPDATE workflows SET name = 'missing-tv-seasons' WHERE name = 'plex'`
   plus the same for `workflow_jobs.workflow_name` and `workflow_runs.workflow_name` (all
   `WHERE … = 'plex'`). Guard it so it only runs when the old `plex` row exists (idempotent on
   re-runs). Without this, syncing the renamed manifest leaves the old `plex` workflow row + its
   historical runs lingering in the dashboard.
3. **References:** update any other reference to the workflow name `'plex'` — tests, README's
   worked-example section, `.env.example` comments, CLAUDE.md if it names it. (The Plex env vars
   `PLEX_HOST` etc. keep their names — those are about the Plex *server*, not the workflow id.)
4. Ensure `src/db/migrate-existing-db.test.ts` still passes (it drives the real `openDb` against a
   pre-seeded old-shape DB) — and ideally seed a `plex` workflow + run in that fixture (or a new
   small test) and assert the migration renames it to `missing-tv-seasons` with the run history
   preserved.

(If the owner later wants a pretty human title like "Missing TV Seasons Discovery" distinct from
the slug, that's a separate display-`title` feature on `WorkflowDefinition` — out of scope here;
this task just makes the slug/id intuitive.)

## Done when

The TV-season workflow is named `missing-tv-seasons` (manifest `name`), served at
`/workflows/missing-tv-seasons`, with its member jobs unchanged; an idempotent DB migration renames
the existing `plex` workflow + its `workflow_jobs`/`workflow_runs` rows so prior run history carries
over and no orphaned `plex` workflow lingers; all references to the old name are updated; the
migrate-existing-db guard passes (covering the rename), and `npx tsc --noEmit` + `npm test` are
green. (The daemon picks up the new name on restart.)
