## Do

On the workflow-run page, the input->output mapping panel (added by T095) currently shows an output that's basically a copy of the input id — no real value. Make the OUTPUT side expressive: show a PREVIEW of the produced artifact (for the example workflows the final stage writes a markdown file, e.g. data/out/markdown/<id>.md — preview its title / first lines / a short excerpt), and make clicking the output open the FULL markdown in a popover/modal (reuse the existing RowModal/db-modal pattern + styling). The input side can stay roughly as-is. Add a READ-ONLY API endpoint to fetch a given run item's output content, and make it PATH-SAFE: resolve the file only within the job's known output directory (e.g. its data/out), reject any path traversal, read files only (no paid/remote calls, safe to load). Resolve the output path from the T095 IO mapping / work_items detail. PREFER showing the markdown as readable text (monospace, scrollable) in the popover to avoid adding a markdown-renderer dependency (repo is dependency-light); a light in-house render is optional. Works for the perfumes + places example workflows. NOTE: harmonise with T095 (the panel) and reuse the db page's modal where possible.

## Done when

On the workflow-run page the output side of the IO mapping shows a meaningful preview of the produced artifact (e.g. the markdown title/excerpt), and clicking it opens the full markdown in a popover/modal; the content is served by a read-only API restricted to the job's output directory (no path traversal, files only, no paid/remote calls); it works for the perfumes and places workflows; npx tsc --noEmit, npm test, the dashboard build, and mobile-check are all green.
