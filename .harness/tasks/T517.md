## Overview
Final closeout of the Plex→callService migration (the T464→T474→T499 chain, blocked twice by a git-hygiene guard trip). The six per-workflow migration units (review-t499-a..f) each did their own workflow's code + test + its OWN CLAUDE.md. This unit does the single root-CLAUDE.md file-map note that no per-workflow unit owns, and serves as the repo-wide 'migration complete' verification gate that downstream tasks (T477, T485, T487) depend on. It is deliberately a tiny, docs-only unit so it cannot trip the git-hygiene guard.

## Do
Add a one-line note to the root CLAUDE.md's src/core/plex-client.ts file-map entry (~line 296) that the shared plex service now sits in front of plexGet/plexPutStreams/triggerButlerBackup calls (every Plex-touching workflow routes its Plex reads/mutations through callService('plex', ...), giving call-count metering + per-job consumer tracking on the Integrations page). BEFORE writing, verify the six code units all landed: run a repo-wide grep for plexGet(, plexPutStreams(, or triggerButlerBackup( (allowing a `<` type-parameter) that is NOT immediately inside a callService('plex', ...) wrapper, excluding src/core/plex-client.ts itself (where the implementations live), the `?? plexPutStreams`/`?? triggerButlerBackup` default-assignment lines in apply.ts, and any src/workflows/*/data/ clone directories (per the data/-scanning gotcha) — this grep must return NOTHING. If it finds a stray bare call, the corresponding per-workflow unit did not fully land; fail:blocked with the specific file/line rather than papering over it here (this unit does not fix stage code — it only documents and verifies). Confirm src/core/plex-client.ts is byte-unchanged. GIT HYGIENE: stage ONLY CLAUDE.md by explicit name — NEVER `git add -A`/`git add .`.

## Done when
The repo-wide grep for a bare plexGet(/plexPutStreams(/triggerButlerBackup( call (outside a callService('plex', ...) wrapper, outside src/core/plex-client.ts, outside apply.ts's `??` default lines, outside data/ clone dirs) returns nothing — proving all six per-workflow units landed. src/core/plex-client.ts is byte-unchanged. Root CLAUDE.md's plex-client.ts file-map entry notes the plex service now fronts the Plex calls. Only CLAUDE.md is staged.
