## Do

In `dashboard/app/workflows/[name]/page.tsx`, rewrite `MissingSeasonsManager` (currently ~line 512-657) so the ACTIVE missing-seasons list renders as ONE flat, bordered table wrapped in a single `<div className="panel">` — matching the visual pattern of this same page's own `Runs` table (~line 884-901: `<div className="panel"><table>…</table></div>`, one `<thead>`, one row per run, `Status | Trigger | Started | Duration | (link)` columns). This REPLACES the current per-show `<div className="panel" key={meta.tmdbId}>` + nested `<table>` loop (~line 577-622) sitting inside a `.movie-gaps-scroll` wrapper (~line 577) — that wrapper/loop-of-panels is exactly what must go away for the ACTIVE list.

Owner-approved target shape (already decided in the interview, do not re-litigate): ONE `<table>`, with a full-width ROW-GROUP HEADER row per show, followed immediately by that show's season rows — NOT a separate 'Show' column repeated on every row.

Concretely:
- Wrap the whole active list in ONE `<div className="panel"><table>…</table></div>`. Delete the `.movie-gaps-scroll` div and the per-show `.panel` divs for the ACTIVE section only. The separate 'Ignored' section below (~line 628-653) is UNCHANGED — it's already a single flat `<table>`, do not touch it.
- `<thead>`: `<tr><th>Season</th><th>TMDB status</th><th></th></tr>` — there is deliberately NO 'Show' column; the show identity is carried by the row-group header row, not repeated per season row.
- `<tbody>`: iterate `groupByShow(active)` (keep this helper at ~line 493-503 unchanged) and for each `[meta, nums]` emit:
  1. ONE header row: `<tr className="table-group-header"><td colSpan={3}>` containing a flex row (reuse the existing `style={{ display:'flex', alignItems:'baseline', justifyContent:'space-between', gap:8 }}` layout) with (a) the show title + year, and (b) the existing '✕ Ignore all' button. Keep the `ignoreShow(meta, nums)` handler and `busyShow` state EXACTLY as they are today (~line 537-549) — only the surrounding JSX shell changes, not the logic/confirm() dialog/API call.
  2. Then one `<tr>` per season number in `nums`, same per-row content as today (`Season {season}` + notified marker, TMDB status, '✕ Ignore' button calling `ignore(row)` — keep the `ignore(s)` handler and `busy` state at ~line 522-535 unchanged).
- Add a new CSS rule in `dashboard/app/globals.css` (near the existing `.movie-gaps-scroll`/`.gaps-table` rules, ~line 172-181) for the row-group header: `.table-group-header td { background: var(--panel-2); font-weight: 600; }` (adjust padding/border as needed to look right against the surrounding `table`/`th`/`td` rules at ~line 91-95). This is a NEW, GENERIC class name — a SIBLING task is independently flattening `MovieGapsManager` (the franchise-gaps section on the movie-recommendations page) using the SAME `.table-group-header` class name for visual consistency between the two sections. Since the two tasks are built independently and may land in either order: if `.table-group-header` does NOT yet exist in `globals.css` when you do this work, add it (as above); if it ALREADY exists (the sibling task landed first), reuse it as-is rather than duplicating or renaming it.

Contrast fixes (the owner confirmed this approach — apply within this component only, do NOT touch theme CSS variables in `globals.css`):
1. The show title `<a>` (linking to `https://www.themoviedb.org/tv/${meta.tmdbId}`) currently inherits the global `a { color: var(--accent); }` rule (globals.css ~line 65). Add `style={{ color: 'var(--text)' }}` to it so it renders in the full-contrast text color instead of the theme accent color. Keep it a real link (same href, `target="_blank" rel="noreferrer"`) — the owner explicitly wants it to stay clickable, just recolored.
2. The 'TMDB status' cell currently renders as `<td className="muted">{meta.tmdbStatus}</td>` — flat, low-contrast text directly on the panel background. Replace it with the shared `<Pill>` component (`import { Pill } from '../../components/Pill.js'` or the correct relative path from this file — check the existing import style in this file for other shared components/`.js` extension convention per this repo's NodeNext rule) using a NEW pill kind: `<Pill kind="info">{meta.tmdbStatus}</Pill>`. Add a `.pill.info { color: var(--text); background: var(--panel-2); border-color: var(--border); }` rule next to the other `.pill.*` kind rules in `globals.css` (~line 261-278) — note this explicitly sets `color: var(--text)`, NOT the base `.pill` rule's `color: var(--muted)` (line 261), because `--muted` is the low-contrast color being fixed. Optionally add `info` to the 'Common kinds' doc-comment list in `dashboard/app/components/Pill.tsx`.
3. Leave the 'notified' secondary marker (`<span className="muted">notified</span>`) as-is — it's minor secondary metadata, the same convention the `Runs` table itself uses for its Trigger/Started columns; it is not the readability problem being fixed here.

ROOT-CAUSE NOTE — record this, do not act on it in this task: the low-contrast look is worst in the `sunny-8bit` theme family's LIGHT mode (`html[data-theme="sunny-8bit"][data-mode="light"]` in `globals.css`, ~line 679-687). Measured contrast: `--accent` (`#e8821f`) against `--panel` (`#fff9ec`) is ~2.6:1; `--muted` (`#9c7e44`) against `--panel` is ~3.65:1. Both are under the WCAG AA floor of 4.5:1 for body text. The existing backlog tasks T308/T309 ('hardcode dashboard theme to sunny-8bit+baloo') plan to make sunny-8bit the ONLY theme — once that lands this contrast gap becomes permanent and app-wide, not just on this page. Do NOT edit the sunny-8bit palette's `--accent`/`--muted` values as part of THIS task (out of scope — it's a global theme concern, this task is scoped to one component) — instead, write this measurement clearly into `.harness/worklog/<this-task's-id>.md` so it's visible to whoever works T308/T309 next.

Finally, update `dashboard/scripts/_dashboard-harness.mjs` (the SINGLE shared living-artifact fixture file — see its own header comment and the 'LIVING ARTIFACT RULE' in root `CLAUDE.md`):
- Add a `/api/missing-seasons` fixture to the `fixtureFor()` function — it does not exist today (confirm via `grep -n "missing-seasons" dashboard/scripts/_dashboard-harness.mjs` returning nothing before your change). Return `{ generatedAt: NOW, shows: [...] }` (matching the `MissingSeasons` shape in `dashboard/app/lib/api.ts`'s `MissingSeason`/`MissingSeasons` interfaces: `{ tmdbId, title, year, season, tmdbStatus, notified, ignored }`). Include: at least 3 distinct shows; at least one show with 2+ missing seasons (to exercise multiple rows under one row-group header); at least one row with `notified: true`; at least one row with `ignored: true` (to exercise the 'Ignored' panel below); reuse the file's existing `LONG` constant for one show's title, following the same stress-test convention already used by the other fixtures in this file (e.g. `stuckItem`, `workflow`).
- Add a PAGES entry: `{ name: 'workflow-missing-tv-seasons', path: '/workflows/missing-tv-seasons', waitFor: ['.rf-dag-node'] }` to the exported `PAGES` array (mirror the shape of the existing `workflow-movie-recs`/`workflow-tv-recs` entries) — this page is not currently covered by either `mobile-check.mjs` or `visual-check.mjs`.
- No new `FLOWS` entry is required — the flattened table introduces no new interactive state beyond a baseline page load (the Ignore/Ignore-all buttons were already plain, unstyled-popover buttons before this change).

## Done when

- `npx tsc --noEmit` and `npm test` pass from the repo root; `npm --prefix dashboard run build` passes.
- `node dashboard/scripts/mobile-check.mjs` passes (no phone-width overflow introduced by the new table).
- `node dashboard/scripts/visual-check.mjs` runs clean AND you have actually LOOKED at the generated `dashboard/scripts/visual-out/workflow-missing-tv-seasons.png` screenshot (the new PAGES entry) with your own eyes and confirmed: (a) the active missing-seasons list renders as ONE single bordered table in ONE panel — no separate boxed/rounded panel per show; (b) each show appears as one full-width row-group header row immediately followed by its season rows, inside that same table; (c) the show-title link and the TMDB-status pill are both clearly legible (full-contrast, not a washed-out orange-on-cream or muted-on-panel look) in whatever theme the hermetic harness's default (unseeded) run renders; (d) the default theme (no `data-theme` set) still looks correct — this fix must not regress the untouched default theme.
- Re-read the diff and confirm the `ignore(s)`/`ignoreShow(meta, nums)` handlers, their `busy`/`busyShow` state, and their `api.missingSeasonsIgnore`/`api.missingSeasonsIgnoreBulk` calls + `confirm()` dialogs are BYTE-IDENTICAL in behavior to before this change — only the surrounding JSX table shell changed, not the ignore logic.
- The separate 'Ignored' table below the active list is confirmed unchanged (still its own single flat table, same columns, same content).
- `grep -n "missing-seasons" dashboard/scripts/_dashboard-harness.mjs` shows both a `/api/missing-seasons` fixture branch in `fixtureFor()` and a `workflow-missing-tv-seasons` entry in `PAGES` — neither existed before this task.
- `.harness/worklog/<this-task's-id>.md` explicitly records the sunny-8bit light-mode contrast measurement (~2.6:1 accent-on-panel, ~3.65:1 muted-on-panel, both under WCAG AA 4.5:1) as a flag for whoever builds T308/T309.
- Confirm via `grep -n "MovieGapsManager\|gaps-table" dashboard/app/workflows/\[name\]/page.tsx` that `MovieGapsManager` and its `.gaps-table`/franchise-gaps rendering were NOT touched by this task — that's the sibling task's scope. If `.table-group-header` did not already exist, confirm you added it generically (not scoped to missing-seasons-only naming) so the sibling task can reuse it.
