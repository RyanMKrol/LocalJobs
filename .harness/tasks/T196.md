## Do

On the workflow detail page header (`dashboard/app/workflows/[name]/page.tsx`), the workflow
description text sits too close to the **Run** button on the right — it looks cramped. Add a bit more
horizontal spacing/gap between the description block and the Run button (and the other header
controls) so the text doesn't run up against the button. Fix via the header layout's gap/padding in
the component or `dashboard/app/globals.css`; keep it responsive (on phone width the header stacks, so
ensure the change doesn't introduce overflow there).

## Done when

- On the workflow detail page, there is clear horizontal breathing room between the description text
  and the Run button — no longer cramped.
- Phone-width layout is unaffected/sensible (no overflow), `node dashboard/scripts/mobile-check.mjs`
  green; `npm --prefix dashboard run build` succeeds and `npx tsc --noEmit` is clean.
