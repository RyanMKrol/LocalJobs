## Do

On the workflows page the ▶ Run button is chunky — the play icon and the word "Run" wrap onto two lines. Make it render on a SINGLE line (e.g. white-space:nowrap / inline-flex / tighter padding / smaller icon — styling is open, the user is not opinionated). Apply the same fix to any other Run button that wraps (e.g. the workflow cards on the overview).

## Done when

The Run button renders on one line (icon + label, no wrap) on the workflows page (and any other card where it wrapped); dashboard build passes.
