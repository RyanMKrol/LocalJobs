## Overview
Re-attempt of T342, which was fully implemented and verified twice but blocked TWICE purely on stale scope arrays, never on the actual work being too hard or ambiguous: first because CLAUDE.md (required by the spec's own Done-when) was missing from scope, then — after that was fixed — because scope still named dashboard/app/services/page.tsx, which the unrelated task T361 had renamed to dashboard/app/integrations/page.tsx in the interim, and the T342 scope array was never updated to track the rename. This follow-up starts with the CORRECT, current file paths already in scope so the same mechanical trap can't recur.

## Do
The owner asked: "Can we maybe use a shared component for tables like this, because I feel like the styling issues are pretty consistent whenever we try to create a new table?" — raw `<table>` markup is independently authored in multiple files under `dashboard/app`. This task's scope is deliberately LIMITED to the two pages under active discussion — `dashboard/app/workflows/page.tsx` and `dashboard/app/integrations/page.tsx` (NOTE: this is the CURRENT name of the page formerly called `services/page.tsx` before T361's rename — the route is now `/integrations`, confirm the actual current filename before starting in case it has moved again) — as the first proof of the pattern; other files with their own `<table>` markup are explicitly OUT OF SCOPE for this task (do not touch them; migrating them is left as future follow-up work if the owner likes this first migration). Both pages already share the exact same shape: a `CATEGORY_GROUPS` constant (label + key), filter workflows/services into groups, render one `<div className="panel">` per non-empty group containing an `<h2>{label}</h2>` and a `<table>` with a `<thead>`/`<tbody>`. Create a new component `dashboard/app/components/CategoryTable.tsx` (following the existing `RunButton.tsx`/`Pill.tsx` convention in that folder — a small focused component, documented with a header comment explaining its props, exported as a named export), that encapsulates: the `.panel` wrapper, the `<h2>` category label, spacing between stacked instances (reuse the existing `.panel + .panel` CSS rule in globals.css rather than re-inventing it — do not add new spacing CSS), and the `<table>`/`<thead>` scaffold. Design it generically: accept the column headers and a way to render each row (e.g. `columns: { key: string; label: string; align?: 'left'|'center' }[]` plus `children` for the `<tbody>`, letting each page keep its own `<tr>`/`<td>` markup and interactivity) — prefer the simpler shape that removes duplicated boilerplate over a kitchen-sink generic table component; neither current call site needs row-level genericism. Migrate BOTH `dashboard/app/workflows/page.tsx` and `dashboard/app/integrations/page.tsx` to render their per-category groups through the new component instead of their own hand-rolled `<div className="panel">...<table>...` JSX, preserving all existing behaviour (click handlers, editing state, Pill/RunButton usage, sticky stuck-item popovers, etc. — the component only owns the STRUCTURAL/STYLING wrapper, not page-specific interactivity). After migrating, add a one-line note to root `CLAUDE.md`'s existing "Shared dashboard UI components (T268)" convention list (alongside `RunButton`/`Pill`) documenting the new component, per this repo's Definition of Done requiring docs to be updated in the same change. CLAUDE.md is included in this task's scope from the start, specifically to avoid the scope-gap that blocked the original T342 attempt.

## Done when
`npx tsc --noEmit` and `npm test` pass. `npm --prefix dashboard run build` succeeds. Run `node dashboard/scripts/visual-check.mjs` and look at the `/workflows` and `/integrations` screenshots in `dashboard/scripts/visual-out/` — confirm both pages render EXACTLY as they did before the migration (same panel spacing, same content, same layout) — this is a refactor, not a visual change. Also spot-check a couple of OTHER pages' screenshots to confirm nothing broke (the new component is additive; it shouldn't touch unrelated pages). Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green. Confirm root `CLAUDE.md`'s T268 shared-components section now mentions the new component (`CategoryTable`) alongside `RunButton`/`Pill`. This is a `layer:ui` task — visual confirmation via the screenshots is a hard requirement.
