## Do

Fix the workflows list page's category-grouping layout in
`dashboard/app/workflows/page.tsx` (the `CATEGORY_GROUPS.map(...)` loop, currently ~lines
64-123). Each non-empty category renders a `<div className="panel" key={key}>` containing
`<h2>{label}</h2>` immediately followed by a `<table>`. `.panel` (see
`dashboard/app/globals.css`) has a border + `overflow: hidden` but **no margin of its own**, so
consecutive rendered panels sit flush against each other — no vertical gap between e.g. the
"Second brain" and "Recommendations" sections (confirmed in the existing
`dashboard/scripts/visual-out/workflows.png` baseline screenshot).

Add spacing between panels using an **inline `style` prop**, matching the precedent in
`dashboard/app/backlog/page.tsx` (which uses `style={{ marginTop: 28 }}` between its
`<details>` sections) — do NOT add a new CSS rule/class for this. Pick a reasonable
`marginTop` value (e.g. `24`-`28`) and confirm it visually via the check below; there is no
mandated exact number. Apply the margin only BETWEEN panels — the first rendered category
panel should not gain extra top margin it doesn't need (the page's `<h1>`/`<p className="sub">`
already provide the spacing above it). Since some `CATEGORY_GROUPS` entries can be empty and
return `null`, you'll likely need to compute the list of non-empty groups first (e.g. via
`.filter(...)` before `.map(...)`, or track a rendered-index) so "first visible panel" is
correct regardless of which categories happen to be empty for the current data.

While you're looking at the rendered page for the visual-check step below, also check whether
the category `<h2>` heading text hugs the panel's top border with no breathing room (the idea
that prompted this task claimed it does, though it isn't obviously wrong in the current baseline
screenshot). **Only** touch the heading's spacing (e.g. adjust its margin, in
`dashboard/app/globals.css` or via a scoped rule) if you can actually see it's a problem in the
rendered screenshot — don't change it speculatively if it already looks fine.

## Done when

- Consecutive category panels on the Workflows page (`/workflows`) render with a clear, even
  vertical gap between them — no two panel borders touch.
- The fix is an inline `style` prop on the panel wrapper(s) in
  `dashboard/app/workflows/page.tsx`, mirroring the Backlog page's `marginTop` precedent (not a
  new global CSS rule), and does not add extra unwanted space above the FIRST rendered panel.
- After `npm --prefix dashboard run build`, run `node dashboard/scripts/visual-check.mjs` and
  look at `dashboard/scripts/visual-out/workflows.png` (the existing fixture already exercises
  multiple categories per T293 — no new `PAGES`/`FLOWS` entry should be needed) — confirm by eye
  that a visible gap now separates each category panel. If, and only if, that same screenshot
  shows the `<h2>` category title hugging its panel's top border with no padding, fix that too and
  re-confirm in a fresh screenshot; otherwise leave heading spacing untouched.
- Run `node dashboard/scripts/mobile-check.mjs` and confirm no new horizontal overflow is
  introduced at phone width.
- `npm test`, `npx tsc --noEmit`, and `npm --prefix dashboard run build` all pass.
