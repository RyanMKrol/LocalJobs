## Do

Sort the **done** list on the Backlog view first by reviewed-status, then by task id. Not-reviewed
tasks at the top, reviewed below; within each group, **task id ascending** sorted NUMERICALLY by the
`T###` number (so T9 < T10, not lexical). The `reviewed` flag comes from `GET /api/backlog` (the T136
`.harness/reviews.json` overlay; absent → false; human-done derive reviewed=true). The done-list
rendering/sort lives in `dashboard/app/backlog/page.tsx`.

## Done when

- The done section lists not-reviewed tasks first, then reviewed, each group ordered by ascending
  numeric task id.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
