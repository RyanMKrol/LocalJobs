## Do

Extend `stocks-snapshot` (`src/jobs/stocks-sync/stages/stocks-snapshot.ts`, from T289) to also fetch
the owner's Stocks & Shares ISA account positions, not just the Invest account. Confirmed root cause:
Trading212 API keys are scoped ONE KEY/SECRET PAIR PER ACCOUNT (Invest and Stocks ISA are separate
accounts under one login, each needing its own separately-generated API credentials) — the current
code only ever fetched the Invest account because it only had Invest credentials.

1. A second, OPTIONAL credential pair now exists in the local `.env` (already added, do not touch
   `.env` itself — it's gitignored):
   `TRADING212_ISA_API_KEY_ID` / `TRADING212_ISA_API_SECRET_KEY`. These are ALSO documented
   (placeholder-only, commented out since optional) in `.env.example` already — do not duplicate,
   just reference them.
2. In `runStocksSnapshot` (`stocks-snapshot.ts`), read `TRADING212_ISA_API_KEY_ID`/
   `TRADING212_ISA_API_SECRET_KEY` from env. If BOTH are set, call `fetchPortfolio` a SECOND time
   using those credentials (same endpoint, `GET /api/v0/equity/portfolio`, same HTTP Basic auth
   scheme — reuse the existing `fetchPortfolio`/`callService('trading212', ...)` function as-is, just
   invoke it twice with different credentials) to get the ISA account's positions. If either ISA env
   var is unset, skip the ISA fetch entirely (this must remain optional/backward-compatible — someone
   without ISA credentials configured should see identical behavior to today, Invest-only).
3. Extend the broker-agnostic `NormalizedPosition` shape (currently `{ ticker, quantity,
   averageBuyPrice, currentPrice, currentValue }`) with an `account: 'invest' | 'isa'` field, and tag
   every normalized position with which account it came from.
4. **Ledger-key collision risk:** `work_items` for `stocks-snapshot` (and the check/notify stages from
   T300, `stocks-watch`/`stocks-notify`) are currently keyed by `ticker` alone. If the SAME ticker is
   held in BOTH the Invest and ISA accounts, a ticker-only key would collide. Change the ledger key
   (and any other per-position lookup/grouping across `stocks-snapshot.ts`, `stocks-watch.ts`, and any
   new stage T300 added) to a composite key, e.g. `${account}:${ticker}`, everywhere a position is
   currently identified by bare `ticker` — check T300's implementation (already landed by the time you
   build this, since this task `dependsOn` it) for exactly which files/functions need updating to stay
   consistent.
5. Merge both accounts' positions into ONE combined list, write it to `data/out/portfolio.json` as
   before (now each entry has the `account` field), and update `buildPortfolioMarkdown` to show the
   account in the table (e.g. an "Account" column, or group rows under an "Invest" / "ISA" heading —
   your call on the exact markdown layout, but the account must be visibly distinguishable per row).
6. Update root `CLAUDE.md`'s `stocks-sync` description and `README.md`'s bullet to mention the
   optional second ISA account and its env vars.

## Done when

- A unit test with two injected fake `fetchPortfolio` calls (one per account) demonstrates: when both
  ISA env vars are set, `data/out/portfolio.json` contains positions from BOTH accounts, each tagged
  with the correct `account` field; when either ISA env var is unset, only Invest positions appear
  (unchanged from today's behavior) and NO second fetch call is made.
- A unit test demonstrates the SAME ticker appearing in both accounts produces two distinct, non-
  colliding ledger entries (composite-keyed), and that `stocks-watch`/`stocks-notify` (from T300)
  still correctly evaluate gain% and notify per position without cross-account confusion.
- `data/out/portfolio.md` visibly distinguishes which account each row belongs to.
- Root `CLAUDE.md` and `README.md` mention the optional ISA account support and its env vars.
- `npm test` and `npx tsc --noEmit` pass.
- Manually confirmed against the real Trading212 ISA account (credentials already in local `.env`)
  that a real GET call succeeds and returns real ISA positions — record what was observed (tickers,
  quantities) in this task's worklog.
