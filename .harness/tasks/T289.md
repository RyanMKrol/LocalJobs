## Do

Add a new `stocks-sync` workflow that pulls the owner's stock portfolio from Trading212
(https://docs.trading212.com/api) via a **strictly read-only** integration and writes a local
snapshot — no DynamoDB, matching this repo's current local-markdown-first direction.

1. **New service** `src/services/trading212.service.ts` — a self-contained `ServiceDefinition`
   (mirror the shape of `src/services/github.service.ts`): `name: 'trading212'`, a description that
   explicitly states this is a READ-ONLY integration and includes a pointer to the API docs
   (https://docs.trading212.com/api), `ratePerMinute`/`dailyCap`/`monthlyCap` read from env with
   conservative defaults (this is a free API), `paid: false`.
2. **New workflow** `src/jobs/stocks-sync/stocks-sync.workflow.ts` — `name: 'stocks-sync'`, a daily
   `schedule` (e.g. `'0 7 * * *'`), `maxConcurrency: 1`, one member job for now: `{ job:
   'stocks-snapshot' }`. (The schedule is user-editable from the dashboard after the fact — don't
   treat the exact cron time as load-bearing.)
3. **New stage** `src/jobs/stocks-sync/stages/stocks-snapshot.job.ts` + `stocks-snapshot.ts` (+
   `.test.ts`):
   - Read `TRADING212_API_KEY_ID` / `TRADING212_API_SECRET_KEY` from env (both already present in
     the local, gitignored `.env` — throw a clear error if either is missing).
   - **Start your research at the live API docs: https://docs.trading212.com/api** — this is the
     canonical source for the exact endpoint path, auth header scheme, and response shape; do not
     guess any of it. Before finalizing the parser, make ONE real GET call against the live account
     (credentials are already in `.env`) to confirm the actual response shape.
   - Call Trading212's read-only endpoint for the owner's current open equity positions (per the
     docs, this is expected to be an "Equity → Portfolio" style endpoint returning each open
     position). **Current price/value should come directly from this same response** — Trading212's
     own app needs a current price + unrealized P&L per position, so its portfolio/positions
     endpoint is expected to already return the current market price and/or value alongside
     quantity and average buy price. Prefer this over adding any separate market-data API or a
     second credential — confirm via the docs + your one real test call, and ONLY reach for another
     Trading212 endpoint (e.g. an instrument/quote lookup, if one exists) if the portfolio response
     genuinely omits current price; do not integrate a third-party price feed for this.
   - Gate every call through `callService('trading212', () => ...)` (the standard shared-service
     pattern — see `fetchAllRepos`'s use of `callService('github', ...)` in
     `src/jobs/projects-sync/stages/github-sync.ts` for the exact idiom).
   - Normalize each position into a **broker-agnostic** shape (field names not specific to
     Trading212, since a second broker may be added later): `{ ticker: string, quantity: number,
     averageBuyPrice: number, currentPrice: number, currentValue: number }`.
   - Write the normalized array to job-local `data/out/portfolio.json` (structured — a later stage
     will consume this) AND a flat, human-readable `data/out/portfolio.md`: one row per position
     showing ticker, quantity, average buy price, current price, AND the price difference —
     `currentPrice - averageBuyPrice` (both the absolute amount and the percentage change) — so the
     report shows at a glance how each position has moved since it was bought. This first build is
     still otherwise minimal (a flat list, not a narrative) — the 30%-gain notification LOGIC lives
     in T290, this task just surfaces the raw difference as a column.
   - Record one `work_items` ledger row per `ticker` (status `success`), via `markWorkItem`, so this
     stage is the limitable root (`inputKeys()` returning the current ticker list).
   - **Absolutely NO `POST`/`PUT`/`PATCH`/`DELETE` request may be made to the Trading212 API
     anywhere in this stage or the new service — GET-only.** See the root `CLAUDE.md` "Broker /
     trading APIs are READ-ONLY, always" rule.
4. `TRADING212_API_KEY_ID`/`TRADING212_API_SECRET_KEY` are already documented (placeholder-only) in
   `.env.example` — do not duplicate; add a one-line pointer comment there to the CLAUDE.md read-only
   rule if not already present, AND a link to https://docs.trading212.com/api so a future reader
   knows where these credentials come from and where the API is documented.
5. Add a `stocks-sync` paragraph to root `CLAUDE.md`'s workflow list (matching the style of the
   other workflow paragraphs) and a matching bullet to `README.md`'s workflow list: Trading212
   read-only portfolio snapshot, writes a local `data/out/portfolio.md` + `.json`, no DynamoDB, runs
   daily (schedule editable from the dashboard).
6. **Create `src/services/CLAUDE.md`** (this directory has no `CLAUDE.md` yet) stating, unambiguously,
   that any broker/trading-account service defined in this directory — starting with
   `trading212.service.ts` — is **read-only, always**: no `POST`/`PUT`/`PATCH`/`DELETE` request, no
   placing/cancelling/modifying an order, no account mutation of any kind, only `GET`/read calls.
   State that this applies to every broker integration added here, present or future, not just
   Trading212. Include the link to https://docs.trading212.com/api as the reference for that
   integration specifically, so anyone extending it knows where to look. This is a LOCAL
   reinforcement of the same rule already added to the root `CLAUDE.md` ("Broker / trading APIs are
   READ-ONLY, always") — the point of putting it here too is that Claude Code loads the nearest
   `CLAUDE.md` when working directly inside `src/services/`, so the rule surfaces automatically to
   any future session or agent touching this file, not just one that happened to read the root doc
   first.

## Done when

- `src/jobs/stocks-sync/stocks-sync.workflow.ts` exists with a `schedule` and one member job
  `stocks-snapshot`.
- A unit test with an injected fake Trading212 fetch (mirror the `ReposFetcher`-style injectable
  pattern in `github-sync.ts`) demonstrates the stage produces `data/out/portfolio.json` (the
  normalized array) and `data/out/portfolio.md` (one row per position, including a price-difference
  column showing `currentPrice - averageBuyPrice` as both an absolute value and a percentage), plus
  one `work_items` row per ticker.
- `grep -rniE "method:\s*['\"](post|put|patch|delete)['\"]" src/jobs/stocks-sync/
  src/services/trading212.service.ts` returns nothing — confirms no mutating HTTP verb is used
  anywhere against the Trading212 API.
- `src/services/trading212.service.ts` exists as a `ServiceDefinition` (mirroring
  `github.service.ts`'s shape) with a description stating read-only use.
- Root `CLAUDE.md` and `README.md` both describe the new `stocks-sync` workflow (Trading212,
  read-only, local markdown+JSON output, no DynamoDB, daily schedule).
- `src/services/CLAUDE.md` exists and explicitly states the read-only rule for broker/trading
  services in that directory, naming `trading212.service.ts`.
- Manually confirmed against the real Trading212 account (credentials already in local `.env`) that
  one real GET call succeeds and `data/out/portfolio.md` contains real position rows — record what
  was observed in this task's worklog.
- `npm test` and `npx tsc --noEmit` pass.
