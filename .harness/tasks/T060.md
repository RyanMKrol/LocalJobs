## Do

On the Backlog view (/backlog), add a THIRD section "Done" alongside "Harness-buildable" and "Needs a human", listing all completed (status:done) tasks so the page shows a complete history of work done. Done tasks must appear ONLY under Done — the Harness-buildable and Needs-a-human sections show only NOT-done tasks. Order Done by id; it will be long (~40+), so keep it compact/scannable (a one-line row per task; collapsing it by default is fine).

## Done when

/backlog shows three sections — Harness-buildable, Needs a human, Done; done tasks appear only under Done (not under buildable); the other two sections list only pending tasks; the section counts are correct; dashboard build passes.
