## Overview
The 30% gain threshold that stocks-watch uses to flag a position breach is hardcoded — the only tuning knob of its kind that isn't env-overridable. Add a STOCKS_WATCH_BREACH_PCT env var (default 30), wire it in, and document it.

## Do
Make the breach threshold env-overridable via a new STOCKS_WATCH_BREACH_PCT env var (default 30). In src/workflows/stocks-sync/stages/stocks-watch.ts, replace the hardcoded `export const BREACH_THRESHOLD_PCT = 30` (line 12) so the value is seeded from process.env.STOCKS_WATCH_BREACH_PCT parsed as a number, falling back to 30 for an unset/invalid/non-positive value — preferably threading it through the workflow's config.ts (stocksSyncConfig) to match the 'config reads its own limits from env' convention, or reading it directly if simpler. Every existing use of BREACH_THRESHOLD_PCT (gainPct/isBreaching and the two markWorkItem detail computations) must pick up the env value. Add STOCKS_WATCH_BREACH_PCT to .env.example in the stocks-sync section with a placeholder and a one-line comment ('# Gain % above average buy price to flag a breach (default 30)'). Update src/workflows/stocks-sync/CLAUDE.md and README.md's stocks-sync section (which currently state a fixed 30% / '≥30%') to note the threshold is env-overridable via STOCKS_WATCH_BREACH_PCT.

## Done when
stocks-watch's breach threshold reads from STOCKS_WATCH_BREACH_PCT (default 30 when unset/invalid), and every breach computation uses that value. A unit test sets the env to a non-default value and asserts breach detection follows it (and defaults to 30 when unset). .env.example, stocks-sync/CLAUDE.md, and README.md all document the new var. Behaviour is unchanged at the default. Existing tests green; npx tsc --noEmit and npm test pass.
