## Do

In the dashboard Theme Picker (`ThemeControls` in `dashboard/app/ui.tsx`, opened from the header 🎨
control), the Dark / Light / System mode control currently sits in the MIDDLE of the picker. Move it
to be the **last section** of the picker (below the theme-family and font controls), so the picker
reads theme → font → mode. Purely a re-order of the existing controls — do not change the mode logic,
the persisted keys, or any behaviour, only the visual order of the sections within the popover.

## Done when

- In the 🎨 Theme Picker popover, the Dark / Light / System mode control is rendered as the last
  section (after the theme-family and font sections); no control's behaviour, persistence, or labels
  changed.
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green.
