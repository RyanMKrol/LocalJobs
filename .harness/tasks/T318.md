## Do

This is a small, standalone config-default bump, unrelated to any other task — the movies (movie-recommendations) workflow's recommender-branch Claude model fallback is out of date compared to the rest of the repo.

`src/jobs/movies/config.ts:35` currently reads:
```ts
recsModel: process.env.MOVIES_RECS_MODEL ?? 'claude-sonnet-4-6',
```
Most other Claude-model fallbacks added more recently in this repo already default to `'claude-sonnet-5'` (e.g. `src/jobs/workouts-sync/stages/workouts-progress.ts:11`'s `WORKOUTS_PROGRESS_CLAUDE_MODEL` fallback, and `src/jobs/projects-sync/stages/project-summarize.ts:15`'s `PROJECTS_SYNC_CLAUDE_MODEL` fallback). `MOVIES_RECS_MODEL` is a leftover on the older `claude-sonnet-4-6` default.

1. Change `src/jobs/movies/config.ts:35` from `'claude-sonnet-4-6'` to `'claude-sonnet-5'`.
2. Update `.env.example` (around lines 74-76): the comment `# The (free) Claude model the recommender branches use. Default claude-sonnet-4-6.` and the commented example line `# MOVIES_RECS_MODEL=claude-sonnet-4-6` both need their `claude-sonnet-4-6` changed to `claude-sonnet-5` so the documented default matches the code.
3. Search the repo for any test asserting the old default (`grep -rn "claude-sonnet-4-6" src/jobs/movies`) and update it if one exists.

Do NOT touch `src/jobs/tv-recs/config.ts:27` (`TV_RECS_MODEL`) or `src/jobs/perfumes/config.ts:24-25` (`PERFUMES_MODEL_FIND`/`PERFUMES_MODEL_PARSE`) even though they also still default to `'claude-sonnet-4-6'` — those are out of scope for this task; only `MOVIES_RECS_MODEL` was requested.

## Done when

- `npx tsc --noEmit` passes with no errors.
- `npm test` passes (including any movies-workflow test suite touching `recsModel`/the default).
- `grep -n "recsModel" src/jobs/movies/config.ts` shows `'claude-sonnet-5'` as the fallback.
- `grep -n "MOVIES_RECS_MODEL\|claude-sonnet-4-6" .env.example` shows the movies section now documents `claude-sonnet-5` as the default (both the comment and the example line), and shows NO remaining `claude-sonnet-4-6` reference under the movies recommendation-layer section.
- `src/jobs/tv-recs/config.ts` and `src/jobs/perfumes/config.ts` are unmodified (their `claude-sonnet-4-6` defaults are intentionally left alone — out of scope).
