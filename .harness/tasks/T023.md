## Do

Harden the dashboard + API so remote actors can't abuse the run/toggle endpoints. Tighten CORS (currently '*') to an allowlist/same-origin, confirm the API binds to 127.0.0.1, and guard the mutating run/toggle endpoints (loopback/token check). Keep the read-only dashboard client working. Pairs with the Tailscale remote-access task.

## Done when

CORS is no longer '*' (allowlist/same-origin), the API bind address is confirmed loopback (configurable), and the run/toggle endpoints reject unauthorised callers; unit tests cover a disallowed origin and an unauthorised mutation being rejected.
