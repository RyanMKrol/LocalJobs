## Overview
Four structurally-identical ignore/unignore endpoint families in server.ts (~330 lines, roughly lines 534-870) — movie-gaps, movie-recs, tv-recs, missing-seasons — have already DRIFTED: movie-gaps + missing-seasons have `ignore-bulk` but movie-recs + tv-recs only have `unignore-bulk`, and the 14-line missing-seasons bulk validator is duplicated verbatim (~828-841 and ~853-866). Collapse them into one config-driven registration so ALL FOUR families expose the SAME verb set (ignore, unignore, ignore-bulk, unignore-bulk) — closing the drift as a feature.

## Do
Building on the route table (this unit depends on conv-api-refactor-route-table), define an `AUDIT_FAMILIES` config array of ~4 objects each with `{ base, job, keyFromParams }` — e.g. `{ base: 'movie-gaps', job: MOVIE_GAPS_JOB, key: (p) => gapKey(num(p.id)) }`, `{ base: 'movie-recs', job: MOVIE_RECS_JOB, key: (p) => recKey(num(p.id)) }`, `{ base: 'tv-recs', job: TV_RECS_JOB, key: (p) => tvRecKey(num(p.id)) }`, `{ base: 'missing-seasons', job: PLEX_SEASONS_JOB, key: (p) => pairKey(num(p.id), num(p.season)) }`. Loop over it to register the four verbs per family (ignore → ignoreSurfacedItem, unignore → unignoreSurfacedItem, ignore-bulk → ignoreSurfacedItems, unignore-bulk → unignoreSurfacedItems, all already exported from store). Factor the duplicated missing-seasons bulk-body validator into ONE shared helper. Note the two families' key funcs take different param shapes (id vs id+season) — keep the per-family `key` closure handle that. The GET list endpoints for each family (which build per-item `notified`/`ignored` overlays) stay as-is or can also be folded if clean, but the priority is the 4 mutating verbs. NET DELETION must be ≥200 lines. All existing endpoints must respond byte-identically. Add tests for the TWO newly-uniform bulk verbs (movie-recs ignore-bulk, tv-recs ignore-bulk) by copying an existing family's bulk cases.

## Done when
all four audit families (movie-gaps, movie-recs, tv-recs, missing-seasons) expose all four verbs (ignore, unignore, ignore-bulk, unignore-bulk); the four families are registered from one shared config loop (no per-family copy-paste blocks remain); the missing-seasons bulk validator exists once; net line deletion in server.ts is ≥200 lines; existing audit-family server.test.ts cases pass byte-identically AND new tests cover movie-recs ignore-bulk + tv-recs ignore-bulk; `npx tsc --noEmit` + `npm test` green. (Owner decision 2026-07-12: the ~200-line net-deletion figure is a SOFT target / guideline, NOT a hard pass-fail gate.)
