## Do

Make the workflow manager run **independent stages in parallel** by default. The DAG executor
ALREADY supports bounded parallelism (`executeDag` launches up to `concurrency` jobs at once), and
`runWorkflow` passes `concurrency: def.maxConcurrency ?? 1` — but the default is **1**, so every
workflow runs strictly sequentially even when the DAG has no edge between stages. Observed: the
movies workflow's `franchise-gaps` + the 8 recommender branches all depend ONLY on
`movie-snapshot`, so after it finishes they're all ready — yet they run one-after-another instead
of together.

Fix it generally:
1. **Raise the default concurrency** in `src/core/workflow-executor.ts` from `1` to a bounded sane
   value (e.g. `def.maxConcurrency ?? 4`) so independent same-wave stages run in parallel by
   default. This only affects workflows that HAVE independent stages — strictly-linear ones
   (places, perfumes, the TV-seasons workflow) are unchanged (one ready stage at a time anyway).
2. **Keep the per-workflow override** (`maxConcurrency` on `WorkflowDefinition`) — a workflow can
   raise it (e.g. the movies workflow could set a higher value for its 8 branches) or set `1` to
   force sequential. Document the field + the new default in `CLAUDE.md`. Optionally set
   `maxConcurrency` explicitly on the movies workflow so its branches fan out as intended.
3. **Bounded — protect the box.** Each parallel stage spawns its own child process, so keep the cap
   modest (don't run all 8 Claude-CLI branches at once on a Mac Mini). The chosen default should be
   a safe ceiling; the executor already queues the excess.
4. **Verify cross-process coordination still holds under parallelism.** Services (`callService` →
   the SQLite `service_usage` meter) coordinate rate limits + quotas ACROSS processes, so parallel
   stages hitting a paid API (Gemini) or the Claude CLI still respect the shared limits — confirm
   this and note it (parallelism must not let paid stages over-spend; the service quota is the
   governor regardless of concurrency).

Tests: a workflow with ≥2 independent stages (no edge between them) runs them **concurrently** —
assert more than one is in-flight at the same time and that total wall-time reflects overlap, up to
the concurrency cap; a job whose dependency hasn't finished still waits; the per-workflow
`maxConcurrency` override is honoured (e.g. `1` forces sequential). Reuse/extend the existing
`executeDag`/workflow-executor tests.

## Done when

A workflow's independent stages (no `dependsOn` between them, dependencies satisfied) run in
parallel up to a bounded default concurrency (raised from 1), with `maxConcurrency` still
overridable per workflow and documented in CLAUDE.md; strictly-linear workflows are unaffected; the
movies gaps + recommender branches fan out instead of running in sequence; service-layer rate/quota
coordination still holds across parallel stages (paid APIs can't over-spend); a unit test proves
concurrent execution + the override; `npx tsc --noEmit` and `npm test` are green.
