## Do

The back-navigation link (e.g. on /runs/[id]) shows '← #<id>' — a '#' symbol prefixing an id. This comes from the shared backFrom helper (ui.tsx line ~76): when arriving from a workflow run it builds the label as `#${segs[1]}` (the workflow-run id with a leading '#'). Remove the '#' so the label is just the id. Because backFrom is the shared helper behind the run AND job page back-links, fixing it there applies the change across all pages. Grep the dashboard for any other place that renders a '#'-prefixed id in a nav/back link and remove the '#' there too, so the change is consistent everywhere (not just the run view).

## Done when

Back-navigation labels show the id with no leading '#' (e.g. '← <id>' not '← #<id>'), consistently across the run and job pages and anywhere else a '#'-prefixed id appeared in nav; the link target is unchanged; dashboard build passes and mobile-check stays green.
