## Overview
On a workflow run's own detail page there is currently no way to see when the run actually started without scrolling down and opening the Framework Logs panel. The owner wants the run's start time shown near the page header instead.

## Do
In dashboard/app/workflow-runs/[id]/page.tsx, add the run's start time to the header summary area (the block spanning roughly lines 84-103: the `<h1>Workflow run</h1>` row, the StatusBadge/run-limit-badge/Cancel-button row, and the `<p className="sub">{completedStages} of {totalStages} stages · {progress}% · {fmtDuration(...)}</p>` line). OWNER-CONFIRMED PLACEMENT: extend that existing `<p className="sub">...</p>` summary line to also include the run's absolute start time (not a separate new element), formatted via the `fmtAbsolute` helper added in the dependency task (absolute-timestamps-a) — e.g. append ` · Started <fmtAbsolute(run.started_at)>` so the line reads like '1 of 1 stages · 100% · 9.3s · Started 15 Jul 2026, 16:00'. Guard it the same way the existing duration segment is guarded (only render when `run` / `run.started_at` is present, mirroring the `run?.duration_ms != null ? ... : ''` conditional pattern already used on that line) so the summary degrades gracefully before the run data has loaded. Do not duplicate this new segment elsewhere on the page (the existing Member Runs table's own 'When' column, using fmtAbsolute per the dependency task, is a separate per-stage display and is left as-is) — this task is only about the header/summary line addition.

## Done when
The workflow-run detail page's header summary line (the '<N> of <M> stages · <P>% · <duration>' `<p className="sub">` element) additionally shows the run's absolute start time formatted via `fmtAbsolute`, visible without scrolling to or opening the Framework Logs panel, and degrades gracefully (renders no broken/undefined text) when `run` or `run.started_at` is not yet loaded. `npx tsc --noEmit` and `npm test` are green and `npm --prefix dashboard run build` succeeds. As a layer=ui auto-covered task, the loop's visual-verify step must show the header line now including a readable absolute start-time string on a real workflow-run detail page screenshot.
