## Do

Build the **`tv-recs-notify`** stage — the monthly digest push for the `tv-recommendations` workflow —
mirroring `src/jobs/movies/stages/notify.ts` (`movie-gaps-notify`), and complete the workflow manifest.

Create `src/jobs/tv-recs/stages/tv-recs-notify.job.ts` (name `tv-recs-notify`) + `tv-recs-notify.ts`
+ tests. It must, mirroring the movies notify:
- Read the verified `recommendations.json` from T217, drop anything the owner has manually IGNORED
  (`ignoredItemKeys('tv-recs')`) and anything already notified, and push ONE digest of just the NEW
  recommendations (title, year, lens/reason, TMDB link) via the notifier (`src/core/notifier.ts`).
- Use the `work_items` ledger as a **"have I already notified this show?"** log keyed by the show's
  TMDB id under the `tv-recs` ledger job: mark each notified rec `success` so it's announced exactly
  once (first run = the whole current backlog), and record the produced markdown report's path in
  `detail.markdown` (T110) under `data/out/reports/`.
- Idempotent + re-runnable; report progress + log verbosely.

Then add `{ job: 'tv-recs-notify', dependsOn: ['tv-rec-merge'] }` to
`src/jobs/tv-recs/tv-recs.workflow.ts` so the full DAG is
`tv-snapshot → (8 branches) → tv-rec-merge → tv-recs-notify`. Update `README.md` + `CLAUDE.md` to
describe the now-complete `tv-recommendations` workflow (job list, architecture, file map).

## Done when

- `tv-recs-notify` sends exactly ONE digest of the new recommendations, marks each notified show
  `success` in the `tv-recs` ledger (so it's never re-notified), excludes owner-ignored shows, and
  writes a markdown report under `data/out/reports/` with its path recorded in the work item's
  `detail.markdown`.
- `tv-recs.workflow.ts` wires the complete chain `tv-snapshot → 8 branches → tv-rec-merge →
  tv-recs-notify`; the registry loads and the daemon starts.
- `expectsTest`: `tv-recs-notify.test.ts` proves the notify-once selection (a rec already marked
  `success` / already `ignored` is NOT re-notified; a fresh rec is) from synthetic fixtures, scratch
  DB only — mirroring `src/jobs/movies/stages/notify.test.ts`.
- `npm test` and `npx tsc --noEmit` are green; `README.md` + `CLAUDE.md` describe the complete
  workflow.
