## Overview
The admin page's fleet-wide 'Delete all workflow output' button (dashboard/app/admin/page.tsx) currently stays disabled until the owner types the exact phrase 'DELETE ALL OUTPUT' into a text box. Remove that type-to-confirm gate entirely, going to zero friction with no replacement dialog of any kind, per the owner's explicit preference for fewer confirmations across the dashboard.

## Do
In dashboard/app/admin/page.tsx: remove the CONFIRM_PHRASE constant (line 8), the confirmText state (line 15), the canDelete derivation (line 26), the labeled text input block (id="admin-confirm-input" and its <label>, currently lines 190-203), and the `if (!canDelete) return` guard inside deleteAllOutput() (line 29). Keep deleteAllOutput()'s existing try/catch/finally body (the actual api.resetAllWorkflowsOutput() call and busy/result/err state handling) unchanged, with NO added window.confirm() or any other confirmation dialog of any kind - the button click goes straight to deleteAllOutput(). Update the 'Delete all workflow output' button (currently lines 204-211) so its disabled state is simply `disabled={busy}` (drop `!canDelete` from the condition) and remove the conditional `title` tooltip that referenced the confirm phrase. The existing Danger-zone descriptive copy (lines 168-188) that already explains what is/isn't deleted should stay as-is - it remains the only warning the owner sees before clicking. Also update dashboard/scripts/_dashboard-harness.mjs: the 'admin-confirm-typed' FLOWS entry (around line 761-772) drives `#admin-confirm-input`, which no longer exists - remove that flow entry entirely (the interactive state it demonstrated no longer exists, per the living-artifact rule).

## Done when
dashboard/app/admin/page.tsx no longer references CONFIRM_PHRASE, confirmText, or canDelete; the text input with id="admin-confirm-input" and its label are gone; the 'Delete all workflow output' button is enabled whenever `!busy` (no longer requires any typed phrase) and clicking it calls api.resetAllWorkflowsOutput() DIRECTLY with no intervening dialog of any kind (no window.confirm(), no re-added text gate) - confirmed by reading the onClick handler and component tree, not just a grep. dashboard/scripts/_dashboard-harness.mjs no longer contains a FLOWS entry that fills `#admin-confirm-input` (the 'admin-confirm-typed' entry is removed). `npx tsc --noEmit` and `npm --prefix dashboard run build` both pass. `node dashboard/scripts/mobile-check.mjs` and `node dashboard/scripts/visual-check.mjs` both stay green and the admin page screenshot is visually inspected to confirm the button renders correctly enabled with no leftover input box.
