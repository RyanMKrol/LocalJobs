## Do

Remove the **Database view** from the dashboard entirely — every UI-driven view of the database. The
owner has never used it. Remove:
- the `/db` route/page (`dashboard/app/db/page.tsx`),
- its nav/header link (in the dashboard layout/nav, e.g. `dashboard/app/layout.tsx` or a nav component),
- its client data calls in `dashboard/app/lib/api.ts`,
- the `/db` entry from `dashboard/scripts/mobile-check.mjs`,
- and any `src/api/server.ts` endpoint(s) that exist SOLELY to feed the DB-browsing view (raw table/row
  dumps) — but KEEP every operational endpoint the rest of the dashboard relies on.
Check for any other raw-DB/table-browser surfaces beyond `/db` and remove those too.

## Done when

- There is no `/db` page, no nav link to it, and no client/API code that exists only to serve it.
- All other dashboard pages + operational endpoints still work; mobile-check page list no longer includes `/db`.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, `npm test`, and mobile-check green; daemon restarted
  if a server route was removed; CLAUDE.md/file-map updated.
