## Do

On the home/overview page (`dashboard/app/page.tsx`), the **recent workflow runs** table renders all
its rows inline, so a long history takes over the page. **Cap it to ~12 rows tall and scroll the
rest** — it shouldn't be entirely visible at once.

- Wrap the recent-runs table in a **bounded-height container** sized to ~12 rows, with `overflow-y:
  auto`, so beyond ~12 the list scrolls inside the panel instead of growing the page (same approach
  as the T148 IO-panel cap / T170 recs section).
- Keep the table header visible/sticky above the scroll area if practical, so column headers stay put
  while scrolling.
- Keep the existing columns, row content, links, and ordering (most recent first) unchanged — only
  the height is capped + made scrollable.
- No horizontal page overflow; the table still scrolls horizontally inside its panel on narrow
  screens as today. Mobile-responsive at ~402px.

## Done when

The home page's recent workflow runs table is capped at roughly 12 rows tall with the remainder
scrolling inside the panel (not the whole page), header staying visible while scrolling where
practical; columns/content/links/ordering are unchanged; it's mobile-safe; `npm --prefix dashboard
run build` passes and `node dashboard/scripts/mobile-check.mjs` stays green.
