## Do

In `src/jobs/listening-digest/stages/listening-digest.ts:15`, the local constant `const JOB_NAME = 'listening-digest';` is WRONG — it matches the workflow name, not the actual registered job name. The real job name (the `JobDefinition.name` in `src/jobs/listening-digest/stages/listening-digest.job.ts:5`, and the name the workflow manifest `src/jobs/listening-digest/listening-digest.workflow.ts` references via `jobs: [{ job: 'lastfm-digest' }]`) is `'lastfm-digest'`. Every `markWorkItem(JOB_NAME, ...)` call in this file (currently one, at line ~219-221) records ledger rows under the wrong job_name string, so the workflow detail page's unified Output section (T205, `workflowTerminalItems` in `src/db/store.ts`, queried via `GET /api/workflows/:name/output-items`) — which correctly queries by the DAG's real terminal-wave job name (`'lastfm-digest'`) — never finds any matching rows, and the Output section always renders empty for this workflow.

Fix: change `const JOB_NAME = 'listening-digest';` to `const JOB_NAME = 'lastfm-digest';`. That is the entire functional fix — do not change the `markWorkItem` call sites themselves, they already reference `JOB_NAME` correctly, only the constant's VALUE is wrong.

Note on existing data (informational, no action required): this workflow has no read-then-skip idempotency guard keyed on `JOB_NAME` (it always re-fetches Last.fm and re-writes `markWorkItem` for the current calendar month, overwriting rather than skip-checking), so changing the constant does NOT break idempotency or cause reprocessing of past months. It DOES mean any historical ledger rows already recorded under the old wrong key (`job_name = 'listening-digest'`) become orphaned (no longer matching any real job name) — this is harmless (they simply stop being queried) and can optionally be cleaned up later via the existing manual prune endpoint (`POST /api/jobs/:name/prune`, see root `CLAUDE.md`'s "Pruning orphaned ledger rows" convention) — do not attempt a destructive data migration as part of this task.

Also update the test file `src/jobs/listening-digest/stages/listening-digest.test.ts`: it independently declares its own `const JOB = 'listening-digest';` at line 57 (used at line 200: `assert.ok(isWorkItemDone(JOB, '2026-07', 3), ...)`), which encodes the SAME wrong string and happens to currently pass only because it matches the (buggy) production constant. Change it to `const JOB = 'lastfm-digest';` so the test continues to exercise the real, now-corrected behavior. Add or extend a test asserting that after `runListeningDigest(...)` completes, `isWorkItemDone('lastfm-digest', monthKey(now))` is true (i.e. the ledger row really is filed under the job name the Output section actually queries) — this is the regression guard for this exact bug class.

## Done when

- `JOB_NAME` in `src/jobs/listening-digest/stages/listening-digest.ts` equals `'lastfm-digest'` (matches `JobDefinition.name` in `listening-digest.job.ts` and the workflow manifest's member job name).
- `src/jobs/listening-digest/stages/listening-digest.test.ts`'s `JOB` constant is updated to `'lastfm-digest'` and a test proves `markWorkItem`/`isWorkItemDone` uses the corrected job name after a run.
- A live or hermetic check confirms `GET /api/workflows/listening-digest/output-items` (or the equivalent `workflowTerminalItems(['lastfm-digest'])` store call) returns the digest item after a run, instead of always being empty.
- `npx tsc --noEmit` and `npm test` both pass.
