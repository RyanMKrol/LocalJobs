## Do

**Trim the appearance selector** (the theme/font/motion switcher from T142 — `ThemeControls` in
`dashboard/app/ui.tsx`, the palettes + font maps in `dashboard/app/globals.css`, and the `next/font`
loading + pre-paint script in `dashboard/app/layout.tsx`) down to the small curated set the owner has
chosen:

- **Themes — keep exactly THREE:** `default` (the untouched dark look), **`pixel-picnic`**, and
  **`sunny-8bit`**. **Remove** the rest — `candy-bright`, `soft-pastel`, `midnight-neon`,
  `joyful-dark`: delete their `html[data-theme="…"]` palettes + any theme-specific CSS, and drop them
  from the switcher's theme options.
- **Light + dark mode per theme, auto-flipping by the viewer's time of day.** Each of the 3 kept
  themes must have a **light** and a **dark** variant (6 palettes total), and the dashboard
  **automatically selects light or dark based on the VIEWER's local time of day** (e.g. light during
  the day, dark in the evening/night):
  - Build a light + dark palette for each of `default`, `pixel-picnic`, `sunny-8bit`. `default`-dark
    = the original pre-T142 dark look (so night-time default stays the familiar dark); `default`-light
    = a new light counterpart. `pixel-picnic` and `sunny-8bit` each get a light + dark palette
    consistent with the family's character.
  - The light/dark choice is **client-side from the viewer's local time** — pick a sensible split
    (e.g. day = light ~07:00–19:00, dark otherwise). Apply it **before first paint** (extend the
    pre-paint no-flash script) so there's no flash, and re-evaluate on load (ideally also if the page
    is open across the threshold).
  - The switcher chooses the theme **FAMILY** (`default` / `pixel-picnic` / `sunny-8bit`); the
    light/dark mode is derived automatically from the time of day. (An optional manual light/dark
    override is a nice-to-have, not required.)
  - This **relaxes** the old T142 "default is always the dark look" rule per the owner's request:
    default now flips light/dark by time, but its DARK mode must still match the original dark look,
    and logs keep their fixed dark-terminal palette in BOTH modes.
- **Fonts — keep exactly THREE:** **System default** (the unset/default font), **Baloo 2** (rounded
  body), and **Space Mono**. **Remove** the other font options (pixelify-nunito, silkscreen-quicksand,
  vt323-fredoka, fredoka-nunito, jetbrains-quicksand): drop them from the switcher AND stop loading
  their `next/font` faces in `layout.tsx` (don't ship fonts no option uses). Pair Baloo 2 sensibly
  (Baloo 2 for body, a fitting display — Baloo 2 itself or a kept face); Space Mono keeps its
  mono-everywhere pairing.
- **Keep** the reduce-motion toggle and the pre-paint no-flash script, and the T142 hard rules: the
  untouched DEFAULT (no attributes) renders exactly as the pre-T142 dark look; logs keep their fixed
  dark-terminal palette; pixel/display faces only ever map to `--font-display`, never body/table/log
  text.
- Honour prior feedback: keep the section-header font WEIGHT not-too-thick on the Baloo 2 option
  (headers read crisp, not chunky).
- Update CLAUDE.md's theme/font doc (currently "6 themes / 8 fonts") to the trimmed **3 themes / 3
  fonts**. Mobile-safe.

(This supersedes T154 — the owner picked keepers from the existing set rather than iterating to more
variants.)

## Done when

The appearance switcher offers exactly 3 theme FAMILIES (`default`, `pixel-picnic`, `sunny-8bit`) and
3 fonts (system default, Baloo 2, Space Mono); each theme has a light + dark palette and the
dashboard auto-selects light/dark from the viewer's local time of day (applied before first paint, no
flash; default-dark still matches the original dark look; logs stay dark-terminal in both modes); all
other themes/fonts are removed from the switcher, their CSS deleted, and their `next/font` faces no
longer loaded; the reduce-motion toggle + no-flash script still hold; CLAUDE.md reflects the trimmed
set + the auto light/dark behaviour; `npm --prefix dashboard run build` passes and
`node dashboard/scripts/mobile-check.mjs` stays green.
