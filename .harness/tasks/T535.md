## Overview
The GET /api/workflows/:name handler (src/api/server.ts ~lines 967-984) calls `getWorkflow(parts[2])` into `p` at ~968, then redundantly calls `getWorkflow(p.name)` AGAIN at ~978 to read `.max_concurrency` while it already holds the fully-populated row `p`. Harmless but a wasted DB query on every workflow-detail fetch.

## Do
In server.ts's GET /api/workflows/:name handler (~line 977-978, inside the `rawEff` ternary), replace the second `getWorkflow(p.name)?.max_concurrency` with `p.max_concurrency` — `p` is the same row and already has the column. Confirm no other property is read off the second call. Nothing else changes; response shape identical.

## Done when
GET /api/workflows/:name calls getWorkflow exactly once; the response for a workflow detail is byte-identical to before (existing server.test.ts workflow-detail cases green); `npx tsc --noEmit` + `npm test` green.
