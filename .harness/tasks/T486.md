## Overview
Fixes `projects-sync`'s self-referential `inputKeys()` trap — the tightest version of the trap found in the 3-workflow audit, since the SAME stage both writes the file `inputKeys()` reads back (not even an upstream-stage read like `places`). This is the cheapest of the three trap fixes: `github-sync.ts` already has a working `callService('github', ...)` call for its main sync work, so no new service is needed.

## Do
Current (broken) state, verified this session: `githubSyncInputKeys()` in `src/workflows/projects-sync/stages/github-sync.ts:208-217` reads back `data/out/projects.json` (via `projectsSyncConfig.catalogPath`) — a file the SAME stage (`runGithubSync`) writes via `writeCatalog` earlier in its own run, returning `[]` on any read error including file-not-found. Change `githubSyncInputKeys()` to call the live GitHub API directly instead — reuse the SAME `callService('github', ...)` wrapper already present at `github-sync.ts:81` for the stage's main per-page GitHub REST fetch (list the owner's repos live), rather than reading back the derived catalog file. A first-page or lightweight paginated call is sufficient for candidate enumeration (it doesn't need every field the full sync fetches, just enough to derive the same key set `writeCatalog` currently persists). Update `projects-sync/CLAUDE.md` to describe the new live-source `githubSyncInputKeys()`.

## Done when
`githubSyncInputKeys()` in `src/workflows/projects-sync/stages/github-sync.ts` no longer reads `data/out/projects.json` — it calls the live GitHub API through the existing `callService('github', ...)` wrapper already used by this file's main sync logic. A test scenario proves the fix: simulate a reset (delete `data/out/projects.json`), confirm `githubSyncInputKeys()` still returns the correct repo-key candidate list from a live API call, not an empty array. `npx tsc --noEmit` and `npm test` are green including updated coverage in `src/workflows/projects-sync/stages/github-sync.test.ts` (mocking the live API call the same way the existing sync tests already mock GitHub responses). `projects-sync/CLAUDE.md` documents the new live-source root selection. This task does NOT add the `inputKeysService` field itself (that's T487, which does not depend on this task specifically but does depend on it being done before retrofitting `projects-sync`'s field value).
