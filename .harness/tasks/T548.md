## Overview
When the daemon is unreachable, make the five dashboard detail pages show the same 'Cannot reach the daemon API' message the list pages already show, instead of a silently-blank skeleton.

## Do
The list pages (workflows/page.tsx:60, integrations/page.tsx:152, logs/page.tsx:102) render '⚠ Cannot reach the daemon API ({error}).' when usePoll returns an error, but five detail pages destructure only `data` and discard `error`, leaving a blank skeleton when the daemon is down: dashboard/app/workflows/[name]/page.tsx (usePoll at :852), dashboard/app/workflow-runs/[id]/page.tsx (:52 and the second poll at :64), dashboard/app/jobs/[name]/page.tsx (:13-15), dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx, and dashboard/app/workflows/[name]/gates/[producer]/[key]/page.tsx. Destructure `error` on each and render the SAME '⚠ Cannot reach the daemon API ({error}).' markup/class the list pages use (reuse, don't invent new copy). Extend dashboard/scripts/_dashboard-harness.mjs fixtures so these detail routes can return failures, so visual-check can capture the daemon-down state (living-artifact rule).

## Done when
All five detail pages surface the daemon-down message identical to the list pages when the polled fetch errors; _dashboard-harness.mjs fixtures cover the failure case; visual-check screenshots show each of the five pages rendering the message instead of a blank skeleton (LOOK at them); npx tsc --noEmit, build, and mobile-check are green.
