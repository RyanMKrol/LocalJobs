## Do

On the Backlog page (`/backlog`), each collapsible section is a `<details>` with a
`<summary className="section-heading-summary">` header ('🤖 Harness-buildable', '🔒 Needs
a human', '✅ Done') followed by its task cards. The section-header **hover highlight**
(added with the spin caret in T132 — `globals.css`:
`details .section-heading-summary:hover { background: rgba(255,255,255,.06) }`) currently
tints ONLY the summary/title row. Change it so hovering the section HEADER tints the
**entire section** — the header PLUS all of its expanded task cards when the section is
open — so the whole group reads as one hovered unit instead of just a strip behind the
title.

Implementation (use best judgement, but the trigger stays the HEADER):
- Drive the highlight off the header hover but apply the background to the whole
  `<details>`, e.g. `details:has(> summary.section-heading-summary:hover) { background:
  rgba(255,255,255,.06) }` (`:has()` is fine — the dashboard targets modern Chromium /
  the mobile-check headless Chromium). Give the `<details>` the padding / `border-radius`
  it needs so the background reads as a cohesive panel around the header + cards (the
  summary's own `padding`/`margin` may need adjusting so the title still lines up). Keep
  the highlight SUBTLE and make sure the task cards stay fully legible on top of it (they
  have their own borders/backgrounds — the section tint must sit behind them, not wash
  them out).
- When the section is COLLAPSED, hovering the header still just highlights the header
  (there's no expanded content) — that should keep working naturally.
- Preserve everything the header already does: the spin caret glyph + its open/closed
  rotation, the `details[open]` colour change, keyboard accessibility (the summary stays
  focus/Enter-toggleable), and the existing per-section `margin-top` spacing between the
  three sections (don't let the new background bleed the three sections into each other).
- Stay phone-width responsive — no horizontal overflow, nothing crossing a boundary.

Don't touch the `.caret-style-label` / `.caret-style-btn` classes — they're reused by the
'Show' reviewed-filter on the same page.

## Done when

On `/backlog`, hovering a section header tints that whole section — the header and all its
expanded task cards while open (and just the header while collapsed) — as one cohesive,
subtle highlight rather than only the title row; the task cards stay legible, the three
sections stay visually separated, and the spin caret rotation, `details[open]` styling,
and keyboard toggling all still work. `npm --prefix dashboard run build` passes and
`node dashboard/scripts/mobile-check.mjs` stays green at phone width.
