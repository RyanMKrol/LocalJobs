## Overview
The movie-recommendations and tv-recommendations workflows leave their snapshot, recommender-branch, and merge stages absent from the run page's Input/Output panel because those stages record no work_items rows. Add one combined ledger row per stage per run so the dashboard actually shows what each stage did.

## Do
For BOTH movies (src/workflows/movies/stages/) and tv-recs (src/workflows/tv-recs/stages/): add a single combined markWorkItem('success', ...) call per run to the snapshot stage (snapshot.ts / tv-snapshot.ts), each recommender-branch stage (recommend.ts's runBranch), and the merge stage (merge.ts / tv-rec-merge.ts), following the stocks/stock-digest per-run-key pattern (key by a stable per-run value like the run's ISO date/month; a manual same-period re-run upserts the same row). Detail shapes: snapshot -> { name, movies|shows: <count>, path: <snapshot.json abs path>, format: 'json' }; each branch -> { name, suggestions: <n>, path: <recs/<branch-id>.json abs path> } keyed per branch id; merge -> { name, balanced: <n>, path: <recommendations.json abs path>, format: 'json' }. Reference the file each stage already writes via detail.path/format per the T262 + 'stage detail describes what it produced' conventions (pass the natural absolute path; markWorkItem normalizes it). Do NOT alter the notify-stage ledger (the 'have I recommended/ignored this?' log) or the workflows' non-limitable, live-discovery, re-scan-fresh idempotency model — this is purely additive. Update each workflow's CLAUDE.md to note that snapshot/branch/merge now record one combined visibility row per run. COORDINATION: idea R01 may extract a shared recommender pipeline; if it lands first, add these rows there instead — but do NOT hard-depend on R01, the addition is correct either way.

## Done when
Each of the movies + tv-recs snapshot, branch, and merge stages records exactly one combined work_items row per run with a detail that names the artifact + a count + its path/format. snapshot.test.ts / tv-snapshot.test.ts assert the snapshot row's shape. Building + running node dashboard/scripts/visual-check.mjs and LOOKING at the movie-recommendations and tv-recommendations workflow-RUN page screenshots confirms the StageIoPanel now shows populated Input/Output rows for those 10 stages instead of being blank. Notify-stage ledger + idempotency model unchanged. Both CLAUDE.md files updated. Existing tests green; npx tsc --noEmit, npm test, and npm --prefix dashboard run build all pass.
