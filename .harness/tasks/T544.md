## Overview
Stop the two setTimeout(setBusy(false), 1200) calls on the Run and Cancel buttons from firing after the component unmounts.

## Do
workflows/[name]/page.tsx:953 (inside runWorkflow's finally) and workflow-runs/[id]/page.tsx:60 (inside cancelWorkflowRun's finally) both call setTimeout(() => setBusy(false), 1200), which fires setState after unmount if the user navigates away. Clear the timer on unmount, or route these two handlers through the R05-a useAction hook which owns the busy timer and guards against setState-after-unmount. The transient busy label behaviour (button shows 'Started…' / busy for ~1.2s) must be preserved.

## Done when
Neither setTimeout can call setState after unmount (timer cleared or handled by the guarded useAction hook); the Run and Cancel buttons still show their transient busy state; npx tsc --noEmit and npm --prefix dashboard run build are green; visual-check confirms the buttons render correctly.
