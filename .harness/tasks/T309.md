## Do

Follow-up to T308 (which hardcoded sunny-8bit+baloo as the dashboard's one and only look and
replaced the theme picker with a binary light/dark sun-icon toggle). Now that no other theme/font/
motion state is reachable, DELETE the dead code and CSS it left behind, per this repo's convention
of removing confirmed-unused code rather than leaving dead paths around:

- `dashboard/app/globals.css`: remove the `pixel-picnic` family CSS block (both dark-base and
  light-override rules) and the `default` family's light-mode override rule (the sunny-8bit
  palette is now simply `:root`/`[data-mode="light"]`, done in T308 — nothing keys off
  `data-theme` anymore, so any leftover `html[data-theme="..."]` selectors should be gone). Remove
  the Space Mono font-variable CSS (`--font-spacemono`, the `html[data-font="spacemono"]` rule),
  and simplify the `html[data-font="baloo"]` selector's declarations by folding them directly onto
  their base selectors (unconditional) since baloo is now the only font. Remove the
  `data-motion="reduced"` rule block (the animation/transition kill-switch) and the emoji-minimise
  gating: `.badge-emoji`/`.joy-emoji` currently default to `display: none` and are only revealed
  via `html[data-theme]:not([data-theme="default"])` selectors (~globals.css lines 689-780) —
  since joyful accents/emoji are now permanently on (sunny-8bit was the "joyful" theme family),
  fold those reveal rules + the other "joyful accent" rules gated the same way (card radius,
  hover-lift, table row accent, success-badge spark flash, progress-bar sheen, stat-number pop)
  directly onto their base selectors, unconditional, instead of leaving a permanently-true
  `:not([data-theme="default"])` check lying around.
- `dashboard/app/layout.tsx`: remove any leftover Space Mono / `data-theme`/`data-font`/
  `data-motion` references in the `PREPAINT` script or font loading if T308 left any for later
  cleanup (check first — it may already be fully clean).
- `dashboard/app/ui.tsx`: delete `useTheme`, `useFont`, `useMotion`, the `THEMES`/`FONTS`
  arrays, and the `ThemeId`/`FontId` types. Keep whatever binary-mode logic T308's toggle
  actually uses (e.g. `useMode`/`resolveMode`/`ModeId` if still referenced) — only remove what's
  genuinely unreferenced after T308.
- `dashboard/scripts/_dashboard-harness.mjs`: trim `seedTheme`'s now-unused `theme`/`font`/
  `motion` params (keep `mode`), and remove any fixture/comment text describing the deleted
  states.
- Confirm no other file references the deleted symbols/attributes (`grep -rn` for
  `pixel-picnic`, `spacemono`/`Space_Mono`, `data-theme`, `data-motion`, `useMotion`, `THEMES`,
  `FONTS`, `ThemeId`, `FontId` across `dashboard/app/` and `dashboard/scripts/`).

## Done when

- `npx tsc --noEmit` and `npm test` pass; `npm --prefix dashboard run build` succeeds.
- `grep -rn -e pixel-picnic -e spacemono -e Space_Mono -e data-theme -e data-motion -e useMotion -e THEMES -e FONTS -e ThemeId -e FontId dashboard/app dashboard/scripts` returns no matches.
- The rendered dashboard is visually UNCHANGED from the end of T308 (same sunny-8bit+baloo look,
  same light/dark toggle behavior, emoji/joyful accents still visible) — run
  `node dashboard/scripts/visual-check.mjs` and look at the screenshots (default load + the
  toggled-mode FLOWS entry from T308) to confirm nothing regressed or disappeared.
- Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green.
