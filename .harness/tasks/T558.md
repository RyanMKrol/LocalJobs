## Overview
Delete scripts/create-dynamo-tables.mjs (it provisions Listens/Projects tables for deleted pipelines that nothing references), and while in dynamodb.service.ts, comment the deliberate unreachable code after each policy-throw and make credential construction throw loudly when an AWS key id is set but its secret is missing instead of silently falling back to ''.

## Do
1. Delete scripts/create-dynamo-tables.mjs (confirm nothing references it — grep the repo, excluding the self-clone; it's the only referent of the Listens/Projects table names). 2. In src/services/dynamodb.service.ts, after each policy `throw new Error(...)` in the write helpers (dynamoPut ~74 and the others ~137/149/154), add an explicit comment marking the following code as deliberately unreachable (kept for when the policy gate is lifted). 3. In the DynamoDB client credentials block (~40-46), replace the silent `secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY ?? ''` fallback with a loud throw when AWS_ACCESS_KEY_ID is set but AWS_SECRET_ACCESS_KEY is absent (a clear message naming both env vars), so a misconfiguration fails fast instead of producing a confusing downstream signature error. Do NOT touch the live perfumes PerfumeRatings read path.

## Done when
npx tsc --noEmit and npm test are green. scripts/create-dynamo-tables.mjs is gone (or re-scoped to only PerfumeRatings per the owner's answer) and a grep for the Listens/Projects DynamoDB table names finds no live referent outside the self-clone. dynamodb.service.ts has an explicit intentional-unreachable comment after each write-helper throw, and its credential construction throws a clear error when the AWS key id is set but the secret is missing. The perfumes PerfumeRatings read still compiles.
