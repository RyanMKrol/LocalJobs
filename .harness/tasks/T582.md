## Overview
Re-attempt of T555, blocked because its scope excluded dashboard/package.json — deleting MarkdownModal.test.ts (required by the task) breaks package.json's "test" script (the exact script CI runs), and that file wasn't in scope so the agent correctly stopped rather than edit outside its bounds. This unit is the same corrected work with dashboard/package.json added to scope.

## Do
1. Copy the isFmEmpty + renderFmValue helpers (currently MarkdownModal.tsx:26-42) into dashboard/app/components/OutputRenderer.tsx and use renderFmValue in MarkdownOutputBody's frontmatter `<dd className="md-fm-val">` cell instead of the bare `{v}`. The `md-fm-null` class already exists in globals.css — no CSS change needed. 2. Delete dashboard/app/components/MarkdownModal.tsx AND its companion dashboard/app/components/MarkdownModal.test.ts entirely (confirm zero other importers first via grep). 3. Edit dashboard/package.json's "test" script to remove the ` && ../node_modules/.bin/tsx app/components/MarkdownModal.test.ts` clause (keep the other three chained test invocations — OutputRenderer.test.ts, StageIoLists.test.ts, ui.test.ts — unchanged and in the same order). 4. In CLAUDE.md, remove/correct the shared-components-section lines that document MarkdownModal as live (the '<MarkdownModal>' bullet and the T458-era references that mention forcing content through MarkdownModal by name) — reword so the docs match reality: renderFmValue now lives in OutputRenderer.tsx; there is no live MarkdownModal component. 5. Add an assertion to dashboard/app/components/OutputRenderer.test.ts covering the ported behavior.

## Done when
MarkdownModal.tsx and MarkdownModal.test.ts are both deleted, and grep finds zero references to "MarkdownModal" anywhere in the repo (dashboard/ + CLAUDE.md + dashboard/package.json), excluding the projects-sync self-clone under data/repos. renderFmValue/isFmEmpty exist ONLY in OutputRenderer.tsx (and its test). dashboard/package.json's "test" script no longer references MarkdownModal.test.ts and still runs the other three test files. The new OutputRenderer.test.ts assertion renders a markdown-format output whose frontmatter contains (a) an empty/null value and (b) a JSON-array value, and asserts the rendered HTML shows the md-fm-null 'null' placeholder for the empty value AND the array joined as comma-separated text (not raw ["a","b"]). npx tsc --noEmit, npm --prefix dashboard test (the FULL chained script, not just the one test file), and npm --prefix dashboard run build are all green. CLAUDE.md no longer describes MarkdownModal as a live component. Visual-check screenshots confirm the output popover frontmatter still renders correctly.
