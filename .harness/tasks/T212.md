## Do

Increase the spacing between a crontab expression and its hint icon so it matches the (larger-looking)
gap before the `(off)`/`(on)` status label. `.cron-badge` in `dashboard/app/globals.css` (~line 242)
uses a single `gap: 4px` for the whole inline-flex row, while the `(off)`/`(on)` label is rendered
OUTSIDE the `CronBadge` span (`CronBadge` in `dashboard/app/ui.tsx` ~line 224 renders only the mono
expression + the `.cron-help` `?` icon), so the expression sits visually too close to the `?`. Bump
the expression↔hint spacing — e.g. raise the `.cron-badge` `gap`, or add a left margin on
`.cron-help` — so the text↔`?` gap visually matches the `?`↔`(off)` gap. CSS-only change (plus
`ui.tsx` only if a class tweak is needed). Keep it mobile-responsive.

## Done when

- On a page showing a schedule (e.g. the Workflows list / overview cards), the gap between the
  crontab text and the `?` hint icon visually matches the gap between the `?` and the `(off)`/`(on)`
  label — no longer cramped. Verify on the running dashboard.
- The `?` hint still shows the human-readable schedule on hover/focus/tap, and the layout is unchanged
  at phone width: `node dashboard/scripts/mobile-check.mjs` passes.
- `npm --prefix dashboard run build` is green.
