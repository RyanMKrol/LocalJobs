## Do

In `dashboard/app/logs/page.tsx`, remove the standalone "Errors only" filter chip from the Logs page's filter bar (`.io-filter-bar`). It duplicates the existing `error` level pill: clicking `error` alone in the `LEVELS` row already produces the exact same filtered state (`levels === ['error']`) that the "Errors only" button produces, and the owner has flagged the two controls sitting side-by-side (`info · warn · error · Errors only`) as confusing/redundant.

Concretely:
1. Delete the `<button>` block rendering "Errors only" (currently around lines 110-116, right after the `LEVELS.map(...)` block and before `<span className="spacer" />`).
2. Delete the now-unused `errorsOnly` derivation (`const errorsOnly = levels.length === 1 && levels[0] === 'error';`) and the `toggleErrorsOnly` function (`function toggleErrorsOnly() { setLevels(errorsOnly ? [] : ['error']); }`) — currently around lines 76-79 — PROVIDED nothing else in the file references them (confirm via a search for `errorsOnly`/`toggleErrorsOnly` before deleting; as of authoring this task they are used nowhere else).
3. Leave the three `LEVELS` pills (`info` / `warn` / `error`) and their existing `toggleLevel` behaviour completely unchanged — they alone already cover "filter to errors only" (click the `error` pill by itself) as well as any other level combination.
4. Do not change the window-range pills (`24h`/`7d`), the scope row, or any other part of the page.

This is a small, surgical deletion — no new state, no new endpoints, no styling changes beyond removing the one button.

## Done when

- `dashboard/app/logs/page.tsx` no longer renders an "Errors only" button/chip anywhere in the filter bar.
- `errorsOnly` and `toggleErrorsOnly` no longer exist in the file (confirmed removed, not just unused — `grep -n 'errorsOnly' dashboard/app/logs/page.tsx` returns nothing).
- The three level pills (`info`, `warn`, `error`) still render and still toggle via `toggleLevel` exactly as before — clicking `error` alone still filters the log list to error-level lines only (same end-state the removed button used to produce).
- `npx tsc --noEmit` and `npm test` are green (repo-wide Definition of Done).
- `npm --prefix dashboard run build` succeeds (dashboard changed).
- Per the `layer:ui` floor: after building, run `node dashboard/scripts/visual-check.mjs` and LOOK at the resulting screenshot(s) for the `/logs` page in `dashboard/scripts/visual-out/` — confirm with your own eyes that (a) the "Errors only" pill is gone from the filter bar, (b) the `info`/`warn`/`error` pills are still present and visually intact (no layout gap/overlap left behind by the removal), and (c) nothing else on the page looks broken. Record what you saw in the task's worklog entry.
- No other file changes outside `dashboard/app/logs/page.tsx` (this task's scope).
