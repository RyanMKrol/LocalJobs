## Do

This is a pure display-string rename — a label/wording change only. It does NOT change the route path, folder name, API endpoint paths, or any internal identifier.

Scope decision (settled from the idea's own framing — the idea explicitly reads as "purely a naming/label change... not a proposal to change what's tracked there or how the underlying mechanism works"):
- The route stays `/services` (do NOT rename the `dashboard/app/services/` folder or the `href="/services"` nav link target). Changing the URL would break bookmarks/deep-links for no functional benefit and isn't required to fix the owner's actual complaint (that the word "Services" mislabels CLI tools / website scrapes to a reader).
- The internal `src/services/*.service.ts` folder, filenames, the `services` SQLite table, `callService`, `ServiceDefinition`, and every `/api/services/...` HTTP endpoint path are UNCHANGED — this is a dashboard-facing label change only, not a rename of the underlying services mechanism.

What DOES change (in `dashboard/app`):
1. `dashboard/app/layout.tsx` — the nav link's VISIBLE text at line ~51: change `<a href="/services">Services</a>` to `<a href="/services">Integrations</a>` (keep `href="/services"` exactly as-is — only the link text changes).
2. `dashboard/app/services/page.tsx`:
   - The `<h1>Services</h1>` heading (~line 137) becomes `<h1>Integrations</h1>`.
   - The `<p className="sub">...</p>` description directly under it may be lightly reworded if it still reads naturally with the new heading (e.g. "Shared external dependencies..." is fine as-is, or tweak to something like "Shared external dependencies and tools with cross-job rate limits + quotas..." — use judgement, keep it short, don't rewrite the whole page).
   - Do NOT rename the `Services` component/function name, the `sortServices`/`allServices` variables, the `Service`/`ServiceConsumerGroup` types in `dashboard/app/lib/api.ts`, or any CSS class — this task is a visible-text-only rename, mirroring how T304 renamed only the Backlog page's section headings without touching routes, variables, or API shapes.
   - Leave the on-page `CATEGORY_GROUPS` labels ("CLI tools", "Website scrapes", "APIs", "Uncategorized") as-is — they already correctly distinguish the underlying kinds; this task is about the page-level label, not the category chips.

Also update `dashboard/scripts/_dashboard-harness.mjs` (the living harness artifact — required by repo convention whenever dashboard UI-facing text changes on a captured page): no PAGES/FLOWS path changes are needed since the route is unchanged, but if you want the screenshot filenames to read naturally you MAY rename the `name` field of the `'services'` PAGES entry and the `'service-consumers-modal'` FLOWS entry to something like `'integrations'` / `'integrations-consumers-modal'` — purely cosmetic, optional, and only touch this if it doesn't risk breaking anything else in that file (leave `path` values as `/services` either way).

## Done when

- The dashboard nav bar shows "Integrations" (not "Services") as the link text, still pointing at `/services`.
- The page at `/services` shows an `<h1>Integrations</h1>` heading (not "Services").
- No route, folder name, API endpoint path, SQLite table name, or `src/services/*.service.ts` filename changed — `git diff` touches only `dashboard/app/layout.tsx`, `dashboard/app/services/page.tsx`, and optionally the cosmetic `name` fields in `dashboard/scripts/_dashboard-harness.mjs` described above.
- `npx tsc --noEmit` and `npm test` pass.
- `npm --prefix dashboard run build` succeeds.
- Build the dashboard, then run `node dashboard/scripts/visual-check.mjs`, and LOOK at the screenshots it writes to `dashboard/scripts/visual-out/` (the `services` page screenshot, and any other page showing the nav bar, e.g. `overview`) — confirm the nav tab reads "Integrations" and the page heading reads "Integrations", nothing overlaps/clips, and no other page regressed.
