## Do

Remove the concept of standalone (non-pipeline) jobs: EVERY job must belong to a pipeline declared in an explicit *.pipeline.ts manifest (a single job = a one-stage pipeline WITH its own manifest — no implicit wrapping). Drop the dashboard Standalone jobs section, the job-level standalone schedule/enable path (pipelines own scheduling), and the registry/scheduler handling for non-member jobs. A job with no manifest is a config error — fail loud at load. Now that demo is gone there are no standalone jobs to migrate.

## Done when

No standalone-job code path remains; every job must be declared in a *.pipeline.ts manifest (single-job pipelines included); a job with no manifest fails loudly at load; the dashboard has no Standalone section; scheduler/registry handle only pipeline-driven jobs; typecheck + tests green; docs updated.
