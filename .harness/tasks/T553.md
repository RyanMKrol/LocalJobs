## Overview
Delete the leftover standalone-job execution path that T070 orphaned: the never-called runJob() in the executor, the notifyRun()/notifyStage() functions only it (or nothing) used, and the impossible 'queued' RunStatus that nothing ever writes — plus fix the misleading overlap-skip message.

## Do
1. In src/core/executor.ts: delete the exported runJob() function (~lines 73-88) and its `import { notifyRun }` (line 13). Reword runJobForWorkflow's overlap skip reason from 'a standalone run of this job is already active' to describe the real cause (another workflow's member run of this shared job is already active). 2. In src/core/notifier.ts: delete notifyRun() (~69-94) and notifyStage() (~112-134) and any now-unused imports/helpers they alone used. 3. In src/core/types.ts: remove 'queued' from the RunStatus union (line 2). 4. In dashboard/app/lib/api.ts: remove 'queued' from the RunStatus union (line 10). 5. In dashboard/app/ui.tsx: remove the queued label (line 14) and queued emoji (line 36) entries. 6. In dashboard/app/workflow-runs/[id]/page.tsx: the completedStages filter (line 81) can drop the `&& r.status !== 'queued'` clause; INSPECT line 91's `badge queued` span first — if it is the run-limit badge merely reusing the `queued` CSS class (not a status render), leave the class/markup and only ensure no dead status logic remains; if it genuinely renders a 'queued' run status, remove it. 7. Restart-note is documentation-only (loop doesn't restart) — no action.

## Done when
npx tsc --noEmit, npm test, and npm --prefix dashboard run build are all green. A grep for runJob (word-boundary, excluding runJobForWorkflow and the projects-sync self-clone), notifyRun, notifyStage, and 'queued' across src/ and dashboard/app returns no remaining references (the run-limit badge, if it reuses the `queued` class name, is the one permitted exception and is called out). runJobForWorkflow's skip reason no longer mentions 'standalone'. No test regressions (the workflow-executor.test.ts comments about notifyStage-never-called stay valid).
