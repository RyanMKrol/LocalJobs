## Overview
Close two CI gaps: setup-node's npm cache keys only on the root lockfile so the dashboard install never hits cache, and there is no npm audit anywhere on a LAN-exposed always-on box. Add a dual cache-dependency-path and an npm audit step for both package roots, with blocking-vs-warn per the owner's decision.

## Do
1) On the setup-node step in .github/workflows/ci.yml, add `cache-dependency-path: ['package-lock.json', 'dashboard/package-lock.json']` so the dashboard install hits cache on unchanged lockfiles. 2) Add an `npm audit --omit=dev --audit-level=high` step for BOTH the root and dashboard package roots. 3) Wire its blocking behavior per the owner's answer to the npm-audit question: blocking = the step's non-zero exit fails the job; non-blocking = `|| true` / continue-on-error so it warns without failing. Do NOT default silently — wait for the owner decision (pending-questions topic:other). Owner decision (2026-07-12): the npm audit step is NON-BLOCKING — surface/annotate advisories in the CI log but do NOT fail the build (use `|| true` or continue-on-error), so a high advisory in a transitive prod dep does not red every run.

## Done when
The CI dashboard-install step hits the npm cache on unchanged lockfiles (both lockfiles listed in cache-dependency-path); an `npm audit --omit=dev --audit-level=high` step runs for both roots and blocks-or-warns exactly per the owner's decision; CI stays green on the current clean tree. The blocking-vs-warn behavior is an explicit open owner question (pending-questions topic:other) — implement only per that answer. The npm audit step runs for both package roots but is non-blocking (advisories are surfaced, CI stays green).
