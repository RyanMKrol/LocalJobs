## Overview
claude-warmer currently fires every 30 minutes all day to keep Claude's 5-hour rolling usage window warm; the owner has decided to replace that always-on cadence with two fixed daily triggers timed so a fresh window is running by the time real work starts.

## Do
In src/workflows/claude-warmer/claude-warmer.workflow.ts, change the WorkflowDefinition's `schedule` field from `'*/30 * * * *'` to `'0 8,16 * * *'` (croner cron syntax, verified valid — fires at 08:00 and 16:00 local time daily). Update the file's leading doc comment (currently describing 'issue one minimal Claude CLI prompt every 30 minutes... fires proactively during off-hours') to describe the new twice-daily cadence and its rationale: firing at 16:00 starts a fresh 5-hour rolling window that resets at ~21:00, and firing at 08:00 covers daytime starts, so the owner's Claude usage window is already a couple of hours old (not just-started) by the time they sit down to do intensive work, reducing the chance of running out of usage credits mid-session. Update src/workflows/claude-warmer/CLAUDE.md's description of the cadence (currently 'every 30 minutes') to match the new schedule and rationale. Update the root CLAUDE.md's workflow table entry for claude-warmer (the row reading 'Proactive Claude usage-window warmer, every 30 minutes') to reflect the new twice-daily schedule instead of 'every 30 minutes'.

## Done when
src/workflows/claude-warmer/claude-warmer.workflow.ts's `schedule` field is exactly `'0 8,16 * * *'` and its doc comment no longer says 'every 30 minutes' / 'off-hours', instead describing the 08:00 and 16:00 firing times and the usage-window-priming rationale above. src/workflows/claude-warmer/CLAUDE.md no longer states a 30-minute cadence and instead documents the twice-daily (08:00/16:00) schedule and why. The root CLAUDE.md workflow table row for claude-warmer no longer reads 'every 30 minutes' and instead reflects the new schedule. `npx tsc --noEmit` and `npm test` remain green (no test currently asserts the literal cron string, but confirm none breaks). No dashboard changes are needed since this is a manifest default, not a dashboard-only override — but note in the task report that any existing dashboard `schedule_overridden=1` row for claude-warmer in a running instance would still need a one-time manual reset via the dashboard (out of scope for this code change).
