## Do

Promote the Chrome-profile learnings from the perfumes fetch (persistent profile keeps Cloudflare clearance, real-Chrome channel, jittered pacing, rate=reputation-not-detection) into a shared src/core/browser.ts helper, and refactor the perfumes fetch to use it. Behaviour-preserving extraction. NO live fetch in verification.

## Done when

A reusable src/core/browser.ts encapsulates persistent-profile + real-Chrome-channel + jittered-pacing launch; perfumes fetch is refactored onto it with no behaviour change; typecheck + tests green without any live browser/network call.
