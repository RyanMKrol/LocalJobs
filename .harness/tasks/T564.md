## Overview
The tolerant readPortfolio helper (reads a NormalizedPosition[] from a JSON path, tolerant of a missing/empty file) is duplicated verbatim in three stage files — stock-digest/stages/stock-sector-lookup.ts (~lines 17-28), stock-digest/stages/stock-digest-build.ts (~lines 183-194, whose comment says 'mirrors stocks-watch's readPortfolio'), and stocks-sync/stages/stocks-watch.ts (~lines 45-52). R07 proposes one readPortfolio — in stock-digest/lib.ts, or a small shared stocks lib both workflows import (they already share the Trading212 service). Pure move + import updates, no behavior change.

## Do
Consolidate readPortfolio to a single definition. Preferred location: src/workflows/stock-digest/lib.ts (which already exists and holds the shared weekKey/weekLabel for stock-digest) — export readPortfolio(path): NormalizedPosition[] there with the identical tolerant-of-missing/empty-file behavior the three copies share. Then have stock-digest/stages/stock-sector-lookup.ts and stock-digest/stages/stock-digest-build.ts import it from ../lib.js and delete their local copies. For stocks-sync/stages/stocks-watch.ts (a DIFFERENT workflow), import readPortfolio from the stock-digest lib IF the NormalizedPosition type resolves cleanly across workflows (both already share src/services/trading212.service.ts, so the type likely lives in a shared services module — confirm the import doesn't create a workflow->workflow coupling the repo forbids; the two workflows already share the SERVICE, so a shared-service-level helper is fine, but a workflow importing another workflow's lib.ts is not the established pattern). If a cross-workflow import would be an anti-pattern, instead place readPortfolio in the shared services layer (e.g. alongside the trading212 service's exported NormalizedPosition type) and have all three stages import it from there — that keeps it a shared-service concern, not a workflow coupling. Whichever location, verify readPortfolio behaves identically (same tolerance for missing file, empty file, and malformed content) and that stocks-watch's downstream logic is unchanged. Update stock-digest/CLAUDE.md's Files section if it enumerates lib.ts's exports.

## Done when
readPortfolio is defined exactly once (in stock-digest/lib.ts or a shared services module — NOT duplicated per stage, and NOT introducing a workflow->workflow import that violates the repo's no-cross-workflow-coupling rule; a shared-SERVICE-level helper is acceptable). All three call sites (stock-sector-lookup.ts, stock-digest-build.ts, stocks-watch.ts) use the single definition with identical tolerant behavior. A repo-wide grep finds no remaining duplicate `function readProfile`/`function readPortfolio` definitions across those stages. Existing stocks-sync + stock-digest tests stay green (they are the regression net for unchanged portfolio-read behavior). Definition of Done: `npx tsc --noEmit` and `npm test` both green.
