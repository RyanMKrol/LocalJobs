## Do

Make the dashboard responsive on mobile. Write an automated check that loads each dashboard page in a headless browser at an iPhone-17-class viewport (~402px wide) against a scratch DB and asserts basic mobile styling: no horizontal overflow, no text crossing element boundaries, no weird truncation/clipping. The in-place agent RUNS this check locally and records the per-page result in its worklog — it does NOT need to be added to CI. Then fix the responsive CSS (tables, stat cards, nav, DAG graph, schedule cells) until it passes.

## Done when

An automated iPhone-17-viewport (~402px) check covers every dashboard page and asserts no horizontal overflow / no boundary-crossing / no truncation; the agent ran it locally and recorded the page-by-page outcome in .harness/worklog/T040.md; the dashboard passes it; dashboard build green.
