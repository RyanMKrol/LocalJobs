## Overview
Extend the admin page's existing 'Cached responses' panel (dashboard/app/admin/page.tsx) so the owner can browse actual cached rows (cache_key + pretty-printed response_json value per row), not just the current per-service counts — while leaving the existing count table and the destructive 'Clear all cached responses' action exactly as they are.

## Do
In dashboard/app/lib/api.ts, add a client method (next to the existing `serviceCacheCounts`/`clearServiceCache`, ~line 617-623) that calls the new `GET /api/cache/rows` endpoint, e.g. `serviceCacheRows: (service?: string) => get<{ rows: Array<{ service_name: string; cache_key: string; cached_at: string; response_json: string; live: boolean }> }>('/api/cache/rows' + (service ? `?service=${encodeURIComponent(service)}` : ''))`. In dashboard/app/admin/page.tsx, extend the existing 'Cached responses' panel (~line 176 onward, the one rendering the per-service count table from cacheCounts) so each service row in that count table has a way to expand and view its individual cache rows — e.g. a 'Browse' button/toggle per service-name row that, when clicked, fetches serviceCacheRows(serviceName) and renders a nested table below it listing cache_key, cached_at, and a Live/Stale `<Pill>` (reuse the shared `<Pill>` component per dashboard/app/components/Pill.tsx's convention, sourced from the row's `live` field) per row, each with a collapsible <details> (or click-to-expand) block showing the response_json value pretty-printed (JSON.parse + JSON.stringify(..., null, 2) inside a monospace <pre>, falling back to the raw string if it fails to parse — same fallback pattern used by the existing JsonOutputBody renderer in dashboard/app/components/OutputRenderer.tsx, reuse that component/pattern if practical rather than re-implementing pretty-print logic). This is purely additive to the existing panel — do NOT change the existing count table, the cacheTotal row, or the separate 'Clear all cached responses' danger-zone panel elsewhere on the page. New state (which service is expanded, per-service fetched rows, loading/error) follows the same local useState pattern already used elsewhere on this page (see cacheCounts/cacheLoadErr/cacheBusy).

## Done when
On http://localhost:4788/admin, the 'Cached responses' panel's per-service count table gains a way (a button/toggle per service) to expand and see that service's individual service_cache rows — cache_key, cached_at, and a Live/Stale pill listed per row (reflecting each row's actual configured TTL, not a generic age), with a per-row expandable/collapsible view of the response_json value pretty-printed as formatted JSON (not a single unbroken line). The existing count table, cacheTotal row, and the separate 'Clear all cached responses' danger-zone button/panel are visually and functionally unchanged. `npx tsc --noEmit`, `npm --prefix dashboard run build` are green, and `node dashboard/scripts/mobile-check.mjs` passes (the admin page must still not overflow at phone width) — this page is in the dashboard-harness PAGES list already (confirm dashboard/scripts/_dashboard-harness.mjs still covers /admin; update it if the new expand interaction is worth its own FLOWS entry).
