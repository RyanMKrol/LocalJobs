## Overview

Part of a repo-wide sweep giving every job a real paragraph-length `description` (surfaced on
`/jobs/[name]` and `/runs/[id]`) instead of a one-liner. This task covers the 2 jobs in the
`projects-sync` workflow. The mechanism was already built by the foundational task this depends on —
this is pure content authoring.

## Do

Rewrite the `description` string of each of the following 2 job files to a real 3-6 sentence
paragraph. Ground each in root `CLAUDE.md`'s detailed `projects-sync` section and the stage's own
code. Plain prose only, no markdown syntax:

- `src/workflows/projects-sync/stages/github-sync.job.ts` (`github-sync`) — currently: "Fetch GitHub
  repos, filter forks/archived/private, sort by activity, and write the filtered catalog to
  data/out/projects.json. Idempotent per repoId." — expand to mention the GitHub API endpoint used and
  that it re-scans every run so metadata (description, topics) stays fresh.
- `src/workflows/projects-sync/stages/project-summarize.job.ts` (`project-summarize`) — currently:
  "Shallow-clone each cataloged repo and use the Claude CLI to write a one-project markdown summary to
  data/out/<repo-name>.md. Idempotent per repo — skips a repo whose last-processed marker (pushedAt)
  already matches the catalog value." — expand to cover the distinctive `runClaudeWithRepoAccess`
  invocation shape (real, scoped, READ-ONLY filesystem tool access via `--add-dir`/`--allowedTools`,
  no Bash/Write/Edit), the enforced `project.template.md` shape contract, and the "no invented facts"
  honesty instruction.

## Done when

- Both `projects-sync` jobs have a real, accurate paragraph description grounded in their actual
  behavior — not a one/two-liner.
- `npx tsc --noEmit` and `npm test` are green.
