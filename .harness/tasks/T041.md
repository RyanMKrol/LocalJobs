## Do

Decide where pipeline output ultimately lands (Obsidian vault / static site / cloud sync) and build the step that delivers it there, instead of leaving output in data/out. The destination is a human decision first.

## Done when

Destination decided with the human; the delivery step is built and writes output there.
