## Do

Retire DynamoDB from the `projects-sync` workflow. In `src/jobs/projects-sync/stages/github-sync.ts`
(and its `.job.ts` wrapper), keep the existing GitHub fetch (`fetchAllRepos`) + filter/sort
(`filterAndSortRepos`, no forks/archived/private, sorted by activity) unchanged, but remove the
`dynamoPut`/`DynamoPutter` upsert entirely — no more write to `PROJECTS_TABLE`. Replace it with:
(1) writing the filtered catalog to a job-local `data/out/projects.json` (array of the filtered
repo objects, or a trimmed projection of the fields the next stage needs — id, name, full_name,
description, html_url, language, topics, pushed_at, default branch), and (2) recording one
`work_items` ledger row per `repoId` via `markWorkItem` (status `success`), so this stage stays the
limitable root (`inputKeys()` returning the current repo id list). Remove the `PROJECTS_TABLE` env
var from `.env.example` (confirmed unused anywhere else in the repo) and the `dynamodb.service.ts`
import from this job. Change the workflow's `schedule` in `projects-sync.workflow.ts` from
`0 5 * * *` (daily) to a weekly cron (e.g. `0 5 * * 0`, Sunday 05:00) and update its description /
doc comment to say weekly, not daily. Update root `CLAUDE.md`'s `projects-sync` paragraph (currently
describes a daily Dynamo upsert) and the matching bullet in `README.md` to reflect: no DynamoDB,
writes a local catalog file, runs weekly. Update `src/jobs/projects-sync/stages/github-sync.test.ts`
to cover the new catalog-write + ledger-record behavior instead of asserting a Dynamo `putItem` call
(inject a fake filesystem-write / use a scratch data dir the way other job tests do, or test the
pure catalog-building function in isolation — don't let the test write into the real job `data/`
tree).

## Done when

- `src/jobs/projects-sync/stages/github-sync.ts` (or its renamed equivalent — renaming is fine as
  long as the workflow's single member job still exists and is wired in `projects-sync.workflow.ts`)
  contains no reference to `dynamoPut`, `DynamoPutter`, or `PROJECTS_TABLE`.
- Running the stage (unit test, injected fake `fetchRepos`) produces a `data/out/projects.json` file
  containing the filtered/sorted repo list, and one `work_items` row per repo id with status
  `success`.
- `PROJECTS_TABLE=` is removed from `.env.example`; `grep -rn PROJECTS_TABLE src .env.example
  CLAUDE.md README.md` returns no matches.
- `projects-sync.workflow.ts`'s `schedule` is a weekly cron (not `0 5 * * *`), and its doc
  comment says weekly, not daily.
- Root `CLAUDE.md`'s `projects-sync` paragraph and `README.md`'s `projects-sync` bullet both
  describe the local-catalog behavior and weekly cadence — no remaining mention of DynamoDB or a
  daily schedule for this workflow.
- `npm test` and `npx tsc --noEmit` pass.
