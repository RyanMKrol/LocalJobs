## Do

In `dashboard/app/backlog/page.tsx`, rename two literal JSX section-heading strings only — no
other changes:

- The "Waiting" section heading (`⏳ Waiting on dependencies ({waiting.length})`, currently around
  line 420, inside the `<details>` block rendering the `waiting` array) becomes
  `⏳ Waiting on Human Tasks ({waiting.length})`.
- The "Needs a human" section heading (`🔒 Needs a human ({human.length})`, currently around line
  442, inside the `<details>` block rendering the `human` array) becomes
  `🔒 Human Tasks ({human.length})`.

Keep the emoji prefixes and the `({...length})` count suffix exactly as they are today — only the
words change. Do NOT rename the underlying `waiting`/`human` variables, any CSS class names, or
the explanatory `<p className="sub">` blurb under the "Needs a human" heading ("The loop skips
these — work them manually."). This is a pure display-string rename; nothing else in the file
should change.

## Done when

- The two section headings in `dashboard/app/backlog/page.tsx` read exactly
  `⏳ Waiting on Human Tasks ({waiting.length})` and `🔒 Human Tasks ({human.length})`.
- No other text, variable name, or CSS class in the file changed (diff is limited to the two
  heading strings).
- `npx tsc --noEmit` and `npm test` pass.
- `npm --prefix dashboard run build` succeeds.
- Build the dashboard, then run `node dashboard/scripts/visual-check.mjs`, and look at the
  screenshot it writes for the Backlog page (`dashboard/scripts/visual-out/`) — confirm both
  renamed headings render correctly (with their emoji and counts) and nothing else on the page
  regressed.
