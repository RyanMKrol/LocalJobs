## Overview
Fold the two extracted recs managers into one generic RecsManager and the two grouped managers into one generic GroupedManager, so each shape has a single implementation driven by a small config object.

## Do
After R05-a, the four managers live in their own files. TvRecsManager vs MovieRecsManager are ~95% identical (only API method names, the TMDB URL segment /tv/ vs /movie/, the noun, and the heading differ) — replace both with one generic RecsManager<T> taking { heading, noun, tmdbPath, fetch, ignore, unignore, unignoreBulk }. MovieGapsManager vs MissingSeasonsManager share the grouped-table shape (group header + Ignore-all/Un-ignore-all + per-item buttons + IgnoredSection) — replace both with one generic GroupedManager<T> taking { groupBy, itemKey, columns, and the ignore/unignore/ignore-bulk API fns }. Each of the four call-sites in page.tsx becomes a thin config. Reuse existing SortTh, IgnoredSection, Pill, and the R05-a useAction hook. Update _dashboard-harness.mjs if the surface shifts; keep CLAUDE.md's component list current.

## Done when
Exactly one implementation of the recs-manager shape and one of the grouped-gaps shape remain, each parameterized; all four workflow detail pages render PIXEL-EQUIVALENT to the pre-R05 look (LOOK at visual-check screenshots); npx tsc --noEmit, npm --prefix dashboard run build, mobile-check, and visual-check are green; _dashboard-harness.mjs and CLAUDE.md are current.
