## Do

In `dashboard/app/workflows/[name]/page.tsx`'s `MissingSeasonsManager` component, remove the "TMDB status" column — the owner reviewed the shipped Missing Seasons table (T315) and found this column (values like "Returning Series"/"Ended") unhelpful. This change applies ONLY to Missing Seasons. Do NOT touch `MovieGapsManager`'s `<th>TMDB</th>` column — that is a numeric rating (e.g. "8.2"), explicitly described in the page copy as "context only", a materially different column with a different purpose; it stays exactly as-is.

Specifically, in `MissingSeasonsManager`:
- Active-items table (currently ~lines 586-638): remove the `<th>TMDB status</th>` header from the `<tr><th>Season</th><th>TMDB status</th><th></th></tr>` head row, and remove the corresponding `<td><Pill kind="info">{meta.tmdbStatus}</Pill></td>` cell from each season row. Update the group-header row's `colSpan={3}` (on `<tr className="table-group-header"><td colSpan={3}>`) to `colSpan={2}` to match the now-2-column table (Season, action) — or `colSpan={1}` if task gap-tables-styling-followup-2 (hiding the redundant per-group Ignore-all button for single-item groups) has already landed and this row can render with no trailing action column in that case; verify the actual current column count in the file rather than assuming.
- Ignored-items table (currently ~lines 644-668): for consistency, also remove the "TMDB status" column there (`<tr><th>Show</th><th>Season</th><th>TMDB status</th></tr>` head row and the `<td>{s.tmdbStatus}</td>` cell) — leaving the column in one Missing Seasons table but not the other would be an inconsistent UI regression.
- After your edit, re-check whether `Pill` (imported at the top of the file as `import { Pill } from '../../components/Pill';`) is still used anywhere else in `page.tsx` — as of writing this is its only use in this file. If it is now unused, remove the import too (don't leave a dead import that trips lint/build).
- Do NOT remove the underlying `tmdbStatus` field from the `MissingSeason` type or the API layer (`dashboard/app/lib/api.ts`) — only stop RENDERING it; this task is scoped to display only, not the data contract.

## Done when

- `npx tsc --noEmit` and `npm test` pass; `npm --prefix dashboard run build` succeeds with no unused-import lint/build errors.
- Run `node dashboard/scripts/visual-check.mjs` and LOOK at the screenshot(s) covering the `missing-tv-seasons` workflow detail page — confirm BOTH the active-items table and the Ignored table no longer show a "TMDB status" column/pill, and the remaining columns render cleanly with correct alignment (no leftover empty column, no misaligned group-header `colSpan`).
- Confirm the Franchise Gaps table (`movie-recommendations` workflow detail page) is UNCHANGED — its TMDB rating column still renders exactly as before; include that page's screenshot in your look-and-confirm pass too.
- Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green.
