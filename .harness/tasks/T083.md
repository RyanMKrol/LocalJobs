## Do

On the individual run page (/runs/[id], runs/[id]/page.tsx line ~42) the page header is a generic '<h1>Run</h1>'. Change the header to the job's name (run.job_name is already available on the page) so the title identifies which job this is a run of. Keep the run id visible (the muted mono span beside the header) and keep the StatusBadge; if useful, keep a small 'run' qualifier as a subtitle so it's still clear this is a single run. Note jobs have no separate title field — run.job_name is the job's identity.

## Done when

On /runs/[id] the page header shows the job name (run.job_name) rather than the literal word 'Run'; the run id and status badge remain visible; dashboard build passes and mobile-check stays green.
