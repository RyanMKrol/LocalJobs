## Do

On the Backlog page (`dashboard/app/backlog/page.tsx`), the **✅ Done** section renders tasks as
COMPACT ROWS that expand on click (`DoneRow`), but the **🤖 Harness-buildable** and **🔒 Needs a
human** sections render full, always-expanded **cards** (`TaskCard`). Make those two lists work the
SAME way as the Done list — compact rows that you click to expand.

- Generalise the Done list's compact-row + click-to-expand pattern (`DoneRow`) so all three
  sections use it (factor a shared collapsible row component rather than duplicating).
- A **collapsed** row shows the essentials (task id, title, the difficulty pill, and the
  buildable / needs-human marker); **clicking** it expands to reveal the full detail — the rendered
  `## Do` / `## Done when` spec markdown, `dependsOn`, tags — exactly as a Done row expands today.
- Keep the three-section grouping, the per-section collapsible headers, and the done-row "Mark as
  reviewed" toggle / reviewed pill / reviewed filter. Default collapsed/expanded state per section
  can match today's (done collapsed; the others can default collapsed too now that they expand).
- Mobile-responsive.

## Done when

All three backlog sections (harness-buildable, needs-a-human, done) use the same compact-row +
click-to-expand interaction — the buildable and needs-human lists no longer render full cards by
default; a collapsed row shows id/title/difficulty/marker and expands to the full spec + metadata;
the done-section reviewed toggle/pill/filter still work; `npm --prefix dashboard run build` passes
and `node dashboard/scripts/mobile-check.mjs` stays green.
