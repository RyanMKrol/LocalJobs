## Overview
The Stage I/O panel's empty-Inputs state always shows the fixed text 'No inputs — this is the root stage' (dashboard/app/components/StageIoLists.tsx:204), which is factually wrong for a non-root stage whose predecessor simply recorded no ledger rows this run (e.g. movie-gaps-notify, the 3rd stage, showed this exact wrong message before franchise-gaps had any rows to pair against) — worth a general fix since the same misleading label can recur for any future stage whose predecessor is legitimately empty.

## Do
In dashboard/app/components/StageIoLists.tsx, the `emptyText="No inputs — this is the root stage."` at line 204 is used unconditionally for a stage's empty Inputs list, regardless of whether that stage actually IS the DAG's root (no predecessors at all) or merely HAS predecessors that recorded zero rows this run. Distinguish the two cases using the same `predecessorJobs`/`predecessors` data the component already receives from the stage-io API response (server.ts's stage-io handler already returns `predecessorJobs` — confirm the exact prop name StageIoLists.tsx receives it under). When `predecessors.length === 0`, keep the current 'No inputs — this is the root stage.' text (accurate). When `predecessors.length > 0` but the returned `inputs` list is empty, show a different, accurate message instead — e.g. 'No inputs recorded this run.' (the SAME text already used elsewhere in this file at line 255 for a different empty state — reuse that exact string for consistency rather than inventing a third variant).

## Done when
A stage with ≥1 predecessor whose ledger recorded no rows this run shows 'No inputs recorded this run.' (not 'this is the root stage'). A stage with genuinely zero predecessors (the DAG's actual root wave) still shows 'No inputs — this is the root stage.' Verify by building the dashboard and visually confirming both states — a `layer:ui`/`workType:bugfix` visual-surface change, so per the harness's visual-verification rule this is auto-covered by the loop's build/audit visual-verify hook; no extra visualVerify flag needed. This unit is independent of recs-ledger-rebuild-h (which should make movie-gaps-notify's specific case self-resolve to the non-empty path) — it fixes the general mislabeling for any OTHER stage that hits the same empty-but-not-root state.
