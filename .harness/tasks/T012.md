## Do

The places pipeline is schedule:null (manual-only). Give it a real DAILY cron schedule ('0 3 * * *') so no pipeline is manual-only, and ensure a daily spend cap derived from the monthly free Places allowance (~ monthly/30) is enforced so a daily run can't blow the month. Env-overridable per the caps convention. No live API calls in verification.

## Done when

Places pipeline schedule is a daily cron (not null); a daily cap (≈ monthly free allowance / 30) is configured and enforced in the enrich loops; a unit test asserts the cap arithmetic / that the loop stops at the daily cap; config is env-overridable.
