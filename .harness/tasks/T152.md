## Do

Remove the bespoke **`/movie-gaps` dashboard page** and its dedicated API. The movies workflow
(T145) was built with a one-off Movie-gaps page + custom endpoints, but it should surface its
output the **same way every other workflow does** — as a markdown artifact under the job's
`data/out/`, viewable through the standard workflow-run **output preview** (the T110 markdown
popover). There's no reason this workflow needs its own page; storing the data like places /
perfumes / the TV-seasons workflow is enough.

Remove:
- The page `dashboard/app/movie-gaps/page.tsx` and the **nav link** in `dashboard/app/layout.tsx`
  (`<a href="/movie-gaps">Movie gaps</a>`).
- The client methods in `dashboard/app/lib/api.ts` (`movieGaps`, `movieGapsIgnore`) and the
  `MovieGaps` type if now unused.
- The dedicated endpoints in `src/api/server.ts` — `GET /api/movie-gaps` and
  `POST /api/movie-gaps/:tmdbId/ignore` — and the now-unused imports they pulled in
  (`moviesConfig`, `MOVIE_GAPS_JOB`/`gapKey` from `movies/stages/notify`, `FranchiseGapsFile`).

Ensure the data is still surfaced the standard way:
- The movies workflow's notify stage must write a **markdown report to `data/out/`** (the
  franchise gaps, grouped by collection) and record its path in the work item's `detail.markdown`
  (T110) — so it appears in the run's Input→Output / output preview like every other workflow's
  artifact. If it already writes a markdown report, just confirm the `detail.markdown` wiring; if
  it only wrote JSON, add the markdown report.
- **The "ignore a surfaced gap" capability** went through the removed `POST /api/movie-gaps/…/ignore`
  + the page. Don't silently lose it: either (a) route it through the existing GENERIC ignore
  (the stuck/ignore mechanism + `ignoreSurfacedItem`) so a gap can still be dismissed without a
  bespoke page, or (b) if that's non-trivial, drop the per-gap ignore for now and record a worklog
  note proposing it as a follow-up (the notify dedup still prevents repeat notifications). Don't
  leave a dangling endpoint with no UI.

Update README/CLAUDE if they describe a Movie-gaps page. Keep the deterministic franchise-gap
*detection* itself (T145) intact — this task only removes the bespoke presentation, not the
workflow.

## Done when

The `/movie-gaps` page, its nav link, its client methods, and its dedicated API endpoints are
gone, with no dead imports left in `server.ts`; the movies workflow surfaces its franchise gaps as
a `data/out/` markdown artifact recorded in `detail.markdown` and viewable via the standard
workflow-run output preview (like places/perfumes); the ignore-a-gap capability is either rerouted
through the generic ignore mechanism or explicitly deferred in a worklog note (no dangling
endpoint); docs updated; `npx tsc --noEmit`, `npm test`, and `npm --prefix dashboard run build` are
green and `node dashboard/scripts/mobile-check.mjs` stays green.
