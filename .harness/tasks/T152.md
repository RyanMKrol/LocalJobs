## Do

The dedicated **`/movie-gaps` dashboard page** (built with T145/T146) is a **top-level nav item**,
but it only relates to ONE workflow (movies) — it shouldn't be a first-class citizen of the whole
dashboard. Its actual purpose (now understood): a place to **manage** the movies workflow's outputs
— specifically to manually **ignore** franchise gaps / recommendations the owner doesn't want
surfaced again. **Move that management UI into the movies workflow detail page
(`/workflows/movies`)** and remove the dedicated top-level page + nav link.

Do:
1. **Remove the top-level page + nav.** Delete `dashboard/app/movie-gaps/page.tsx` and the nav link
   in `dashboard/app/layout.tsx` (`<a href="/movie-gaps">Movie gaps</a>`). This management UI must
   NOT be a global dashboard nav entry.
2. **Add a management section to the movies workflow page.** On the workflow detail page
   (`dashboard/app/workflows/[name]/page.tsx`), when viewing the **movies** workflow, render a
   "Recommendations & gaps" (manage-outputs) section that lists the current franchise gaps +
   recommendations with a per-item **Ignore** control, so the owner can dismiss ones they don't
   want. Reuse the EXISTING API and client methods — `GET /api/movie-gaps` (current gaps/recs +
   their notified/ignored state) and `POST /api/movie-gaps/:tmdbId/ignore` (`api.movieGaps`,
   `api.movieGapsIgnore`) — just call them from the workflow page instead of the deleted page.
   - The workflow detail page is generic across workflows, so **gate the section so it only appears
     for the workflow that has manageable outputs** — `name === 'movies'`, or (cleaner if feasible) a
     small declarative signal on the workflow definition / API so it isn't hardcoded to a string.
     Keep it consistent with the page's existing sections (run history etc.).
3. **Keep the ignore-to-suppress behaviour** intact — ignoring an item drops it from future
   reports/notifications (`ignoreSurfacedItem`). That capability is the whole point; don't lose it.
4. Mobile-responsive. Update README / CLAUDE if they reference a top-level Movie-gaps page (it's now
   a section of the movies workflow page).

(The franchise-gaps markdown report can still be a `data/out` artifact viewable via the standard
run output preview — but THIS task is specifically about relocating the manage/ignore UI to the
workflow's own page, NOT removing the capability.)

## Done when

The dedicated top-level `/movie-gaps` page and its nav link are gone; the **movies** workflow detail
page (`/workflows/movies`) has a section listing the franchise gaps + recommendations with per-item
manual **Ignore** controls (backed by the existing `/api/movie-gaps` endpoints), so the owner
manages this one workflow's outputs from the workflow's own page; the section appears ONLY for the
movies workflow (not other workflows); ignore-to-suppress still works; it's mobile-responsive;
`npm --prefix dashboard run build` passes, `node dashboard/scripts/mobile-check.mjs` stays green, and
docs are updated.
