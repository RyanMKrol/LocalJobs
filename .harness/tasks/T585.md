## Overview
Re-attempt of T563, blocked because its scope named src/workflows/plex-space-saver/stages/scan.ts (which only IMPORTS the JSON helpers) instead of src/workflows/plex-space-saver/lib.ts (which actually DEFINES writeJsonFile/ensureDirs there) — the builder correctly needed to touch lib.ts and was scope-blocked for it. Separately, T563's spec assumed its dependency T561 (the recommender extraction) would delete movies/lib.ts and tv-recs/lib.ts wholesale, removing two of the four duplicate copies before this task ran. T561 has since landed (commit 70eaa0c) but did NOT touch either file — both still exist with the full readJsonFile/writeJsonFile/ensureDirs trio duplicated verbatim, identical to before. So there are actually FOUR duplicated copies to fold in, not two: src/workflows/perfumes/lib.ts, src/workflows/movies/lib.ts, src/workflows/tv-recs/lib.ts, and src/workflows/plex-space-saver/lib.ts (which only duplicates ensureDirs + writeJsonFile, no readJsonFile).

## Do
Create src/core/fsjson.ts exporting three helpers with their existing signatures/behavior, unchanged: readJsonFile<T>(path: string, fallback: T): T — returns the parsed JSON at path, or fallback when the file is absent; writeJsonFile(path: string, data: unknown): void — writes JSON.stringify(data, null, 2); ensureDirs(...dirs: string[]): void — mkdirSync each with { recursive: true } (make it variadic/array-accepting since movies/tv-recs each call it with multiple directories while perfumes and plex-space-saver differ — every existing call site's actual directory set must stay identical after the change, just expressed as args to the shared function). Then replace all four duplicated copies with imports from src/core/fsjson.ts: src/workflows/perfumes/lib.ts, src/workflows/movies/lib.ts, src/workflows/tv-recs/lib.ts (all three currently define all three helpers), and src/workflows/plex-space-saver/lib.ts (currently defines only ensureDirs + writeJsonFile — import just those two; it does not define/use readJsonFile). In every case, delete the local function bodies but KEEP each workflow lib re-exporting readJsonFile/writeJsonFile/ensureDirs (or plex-space-saver's two) from its own lib.ts, so every existing import path used by that workflow's stages/tests keeps resolving unchanged — do not touch call sites outside each lib.ts file. Add src/core/fsjson.test.ts covering readJsonFile's file-absent-fallback path and a writeJsonFile/readJsonFile round-trip — this is the one behavior worth a direct unit test; the four workflows' own existing tests remain the integration regression net for their call sites. Update the root CLAUDE.md file map to list src/core/fsjson.ts if the file map enumerates core modules (it currently lists modules like src/core/browser.ts, src/core/plex-client.ts — follow that same one-line convention).

## Done when
src/core/fsjson.ts exports readJsonFile / writeJsonFile / ensureDirs as the single definition. A repo-wide grep (excluding src/core/fsjson.ts itself and node_modules/data folders) finds no remaining duplicate `function readJsonFile` / `function writeJsonFile` / `function ensureDirs` definitions in src/workflows/perfumes/lib.ts, src/workflows/movies/lib.ts, src/workflows/tv-recs/lib.ts, or src/workflows/plex-space-saver/lib.ts — each imports from core instead. Every existing import path that pulled these from one of those four workflow libs still resolves (their stages/tests are unmodified and still pass). Workflow outputs are unchanged on fixtures (existing per-workflow tests green). src/core/fsjson.test.ts exists and covers the file-absent fallback and a write/read round-trip. Root CLAUDE.md file map references src/core/fsjson.ts if the file map enumerates core modules. `npx tsc --noEmit` and `npm test` both green.
