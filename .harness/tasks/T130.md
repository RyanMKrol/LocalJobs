## Do

On the Backlog page each task card shows the model + the full escalation path (escalationPath/resolveRung → e.g. 'sonnet/medium → opus/high', backlog/page.tsx ~line 59) — it's hard to parse and not useful to the reader. Replace it with a single colour-coded DIFFICULTY pill derived from the FIRST-ATTEMPT model+effort (the task's model/effort, falling back to defaults.model/effort), and HIDE the escalation path entirely. Difficulty buckets: easy | medium | hard | very hard, mapped from the initial rung — suggested mapping (use best judgement for a clean rule): sonnet@medium → easy; opus@high → medium; opus@xhigh → hard; opus@max → very hard. Colour-code them: easy=green, medium=yellow, hard=red, very hard=black/near-black — but ensure ALL are READABLE on the dark theme (a pure-black pill would vanish, so use a dark pill with a light border/label for 'very hard'). Apply to the task cards (TaskCard, and DagDetailPanel if it still exists), remove the escalation-path display, and drop now-unused helpers (escalationPath/resolveRung) if nothing else uses them. Harmonise with T123's 'buildable' pill so the pills sit nicely together.

## Done when

Each backlog task card shows a single colour-coded difficulty pill (easy/medium/hard/very hard → green/yellow/red/black, all legible on the dark theme) derived from the first-attempt model+effort; the model string and escalation path are no longer shown anywhere on the backlog; dashboard build passes and mobile-check stays green.
