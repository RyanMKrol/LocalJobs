## Do

In the per-run **Input → Output mapping** panel (`dashboard/app/workflow-runs/[id]/page.tsx`, the
table rendered around line 252 with the footer note "One output is shown per input…"), hovering the
**last** row leaves a visible ~half-centimetre gap between the highlighted row and the footer note,
whereas hovering the **top** row shows no such gap. Make the last row's hover highlight sit flush
against the footer note exactly like the top row — i.e. remove the bottom gap so the row highlight
and the footer are adjacent. Likely a stray bottom margin/padding/border-spacing on the last row or
between the table body and the footer note; fix it in the relevant CSS (`dashboard/app/globals.css`)
or the component markup.

## Done when

- Hovering the last row of the Input→Output mapping table no longer leaves a gap before the footer
  note — the highlight is flush with the footer, matching the top row's behaviour.
- The fix is symmetric (top and bottom rows behave the same on hover) and doesn't shift the rest of
  the table layout.
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green.
