## Do

Rename the **`movies`** workflow to **`movie-recommendations`** — a clearer name than the bare
"movies". (Note: this workflow does BOTH franchise-gap detection AND recommendations; the owner has
chosen `movie-recommendations` as the name anyway. Keep both capabilities — only the workflow's
name changes.) Mirror exactly how T151 renames the plex workflow.

1. **Manifest:** change `name: 'movies'` → `name: 'movie-recommendations'` in
   `src/jobs/movies/movies.workflow.ts` (and its `description` if it just says "movies"). Leave the
   member JOB names (`movie-snapshot`, `franchise-gaps`, `merge`/recommender branches, `notify`,
   etc.) unchanged — their `work_items` are keyed by job name, so renaming them would orphan the
   ledger; only the WORKFLOW name changes. The `src/jobs/movies/` folder can stay as-is (optional to
   rename; the registry discovers by glob, not folder name — if you do rename it, fix all imports +
   the `data/` path as a separate, clearly-scoped step).
2. **DB migration (preserve history, no orphan):** add an idempotent migration in
   `src/db/index.ts` that renames the workflow in the DB so existing runs aren't orphaned under a
   now-manifest-less `movies`: `UPDATE workflows SET name = 'movie-recommendations' WHERE name =
   'movies'` plus the same for `workflow_jobs.workflow_name` and `workflow_runs.workflow_name` (all
   `WHERE … = 'movies'`). Guard so it only runs when the old `movies` row exists (idempotent on
   re-runs). Follow the T098 rule (data migration after schema bootstrap).
3. **References:** update every reference to the workflow name `'movies'` — tests, the README
   "worked example workflows" section + file map, CLAUDE.md, and notably the **dashboard**: T152
   adds a movies-management section gated on the workflow name (`name === 'movies'`) and the
   `/workflows/movies` URL — update those to `movie-recommendations`. (If T152 used a declarative
   per-workflow signal instead of a string, this is simpler.) The Plex/TMDB-style env vars keep
   their names — those are about the data sources, not the workflow id.
4. Ensure `src/db/migrate-existing-db.test.ts` still passes, and ideally seed a `movies` workflow +
   run in a fixture and assert the migration renames it to `movie-recommendations` with run history
   preserved.

(This task is positioned LAST in the backlog — alongside T151 — so every task that references the
`movies` workflow by name/URL builds before the rename happens.)

## Done when

The workflow is named `movie-recommendations` (manifest `name`), served at
`/workflows/movie-recommendations`, with its member jobs unchanged and both franchise-gaps +
recommendations intact; an idempotent DB migration renames the existing `movies` workflow +
`workflow_jobs`/`workflow_runs` rows so prior run history carries over with no orphaned `movies`
workflow; all references to the old name (tests, docs, the T152 dashboard section + URL) are
updated; the migrate-existing-db guard passes; `npx tsc --noEmit` and `npm test` are green (plus
`npm --prefix dashboard run build` for the dashboard reference updates).
