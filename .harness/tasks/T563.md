## Overview
readJsonFile / writeJsonFile / ensureDirs are duplicated verbatim across several workflow libs — movies/lib.ts and tv-recs/lib.ts (both DELETED wholesale by the recommender extraction, unit d), perfumes/lib.ts, and plex-space-saver (a writeJsonFile). R07 proposes a single core module (src/core/fsjson.ts, or fold into an existing core module) with readJsonFile, writeJsonFile, ensureDirs. Sequenced AFTER unit d so it doesn't fight the two lib.ts copies the extraction removes. Pure move + import updates, no behavior change.

## Do
Create src/core/fsjson.ts exporting the three helpers with their existing signatures/behavior: readJsonFile<T>(path, fallback): returns the parsed JSON or the fallback when the file is absent; writeJsonFile(path, data): writes JSON.stringify(data, null, 2); ensureDirs(...dirs): mkdirSync each with { recursive: true }. Then replace the remaining duplicated copies with imports: perfumes/lib.ts (import from core, delete local copies OR re-export if perfumes stages import them from perfumes/lib.ts — keep the perfumes public surface intact so its stages/tests keep resolving), and plex-space-saver's writeJsonFile (import the shared one). Do NOT touch movies/lib.ts or tv-recs/lib.ts here — unit d already removed/relocated those workflows' JSON-helper usage as part of the recommender extraction (that's why this unit dependsOn unit d). Verify no call site changes behavior: the movies ensureDirs iterates multiple dirs while others may differ, so make the shared ensureDirs accept a variadic/array of dirs and adapt call sites, keeping each caller's set of directories identical. Add a small src/core/fsjson.test.ts covering readJsonFile's file-absent-fallback path and writeJsonFile round-trip (this is the one behavior worth a direct test; existing per-workflow tests remain the integration regression net). Update the root CLAUDE.md file map to list src/core/fsjson.ts if the file map enumerates core modules.

## Done when
src/core/fsjson.ts exports readJsonFile / writeJsonFile / ensureDirs as the single definition; a repo-wide grep finds no remaining duplicate `function readJsonFile` / `function writeJsonFile` / `function ensureDirs` definitions outside core (perfumes and plex-space-saver import/re-export the shared ones; movies/tv-recs were already handled by unit d). Every existing import path that pulled these from a workflow lib still resolves. Workflow outputs are unchanged on fixtures (existing per-workflow tests green). src/core/fsjson.test.ts covers the file-absent fallback and a write/read round-trip. Root file map references src/core/fsjson.ts if applicable. Definition of Done: `npx tsc --noEmit` and `npm test` both green.
