## Overview

The generic workflow "Output" section (`dashboard/app/components/WorkflowOutputSection.tsx`, shared
by every workflow detail page except the three with dedicated managers) shows its "no output yet"
message as plain left-aligned text with no visual container, unlike the already-correct, precedented
centered-panel convention used elsewhere on the same pages (e.g. TV recs' empty state). Bring the
generic Output section's empty state in line with that convention.

## Do

1. In `dashboard/app/components/WorkflowOutputSection.tsx`, find the empty-state branch (currently
   around line 116-120):
   ```tsx
   {data && items.length === 0 && (
     <p className="muted" style={{ fontSize: 13 }}>
       No output yet. Run the workflow — produced items will appear here.
     </p>
   )}
   ```
   Replace it with the same `.panel` + `.empty-state-panel` wrapper pattern already used (and
   confirmed correct/working) at `dashboard/app/workflows/[name]/page.tsx:105-111`
   (`TvRecsManager`'s empty state):
   ```tsx
   {data && items.length === 0 && (
     <div className="panel">
       <p className="empty-state-panel">
         No output yet. Run the workflow — produced items will appear here.
       </p>
     </div>
   )}
   ```
   Keep the exact same message text — only the wrapper/class changes. Do not touch the loading state
   (nothing renders here while `data` is still `undefined`, which is correct and unrelated to this
   fix), the error branch, or the populated (`items.length > 0`) table branch.
2. `.empty-state-panel` (`dashboard/app/globals.css:152`) already provides the centered-flex,
   padded, min-height-96px styling — no CSS changes are needed, this task is a markup-only swap in
   the TSX.
3. This is a single shared component used by EVERY workflow that does NOT have a dedicated output
   manager — confirmed via `dashboard/app/workflows/[name]/page.tsx`'s
   `WORKFLOWS_WITH_SPECIFIC_MANAGERS` set (`movie-recommendations`, `missing-tv-seasons`,
   `tv-recommendations` are excluded; every other real workflow — `places`, `perfumes`,
   `listening-digest`, `projects-sync`, `claude-warmer`, `stocks-sync`, `stock-digest`,
   `workouts-sync` — renders `<WorkflowOutputSection workflowName={name} />` and gets this fix
   automatically because it's the exact same file/component, not a per-workflow override). No
   per-workflow changes are needed or expected — confirm this understanding still holds (re-check the
   `WORKFLOWS_WITH_SPECIFIC_MANAGERS` set and the `!WORKFLOWS_WITH_SPECIFIC_MANAGERS.has(name) &&
   <WorkflowOutputSection …>` render line hasn't changed shape) before declaring done, satisfying the
   "validate this holds for every workflow" ask without needing 8 separate manual checks (they all run
   through this one file).
4. `dashboard/scripts/_dashboard-harness.mjs` ALREADY has (from a prior task, T347) a dedicated
   `output-items` fixture route (populated for `places`, empty for everything else — see
   `fixtureFor`'s `pathname.endsWith('/output-items')` branch) and a `PAGES` entry
   `{ name: 'workflow-empty-output', path: '/workflows/workouts-sync', waitFor: ['.rf-dag-node'] }`
   that exercises exactly this empty state. No new fixture or `PAGES`/`FLOWS` entry is needed for this
   task — the living-artifact file is already current for this change; just confirm (read the file)
   that this is still true before finishing, since the fixture file may have moved on since this spec
   was written.

## Done when

- `WorkflowOutputSection.tsx`'s empty-state branch renders `<div className="panel"><p
  className="empty-state-panel">…</p></div>` instead of a bare `<p className="muted">`, with the same
  message text.
- No other branch (loading, error, populated table) changed.
- `npx tsc --noEmit` and `npm test` pass.
- `npm --prefix dashboard run build` succeeds.
- **Visual confirmation (this is a `layer: ui` task):** run `node dashboard/scripts/visual-check.mjs`
  and look at BOTH `workflow-empty-output.png` (the `workouts-sync` page — confirm the "No output
  yet…" message now renders as a centered box matching the TV recs / movie recs empty-state look, not
  left-aligned text) AND `workflow.png` (the `places` page — confirm the now-unaffected populated
  output table still renders correctly). Record what you saw in the task's worklog.
- `node dashboard/scripts/mobile-check.mjs` stays green (no new horizontal overflow).
- Since this is a single shared component, explicitly note in the worklog that you re-confirmed (via
  `WORKFLOWS_WITH_SPECIFIC_MANAGERS` in `page.tsx`) which real workflows render this component, so the
  fix is validated as applying to all of them (places, perfumes, listening-digest, projects-sync,
  claude-warmer, stocks-sync, stock-digest, workouts-sync) — not just the one screenshotted.
