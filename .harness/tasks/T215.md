## Do

Build the **recommender infrastructure libraries** for the `tv-recommendations` workflow under
`src/jobs/tv-recs/`, mirroring the movies recommender's shared logic. These are pure library modules
(NO `*.job.ts` stages yet — those land in T216), so they add no workflow members and don't touch the
manifest.

Create, adapted for TV from their movies equivalents:
- `recs.ts` — the stratified library sampler, per-genre output balancer, cross-branch dedupe, and
  seeded-shuffle helpers, mirroring `src/jobs/movies/recs.ts` (`stratifiedSample`, `topGenres`,
  `thinGenres`, `creatorsOwnedAtLeast` (the TV analog of `directorsOwnedAtLeast`), `balanceByGenre`,
  `dedupeRawByTitleYear`, `mergeLens`, `seededShuffle`, `primaryGenre`). Operate on the `PlexShow` /
  `TvTasteProfile` types from T214.
- `stages/branches.ts` — the branch DEFINITIONS for the 8-way fan-out (mirror
  `src/jobs/movies/stages/branches.ts`): 3 stratified-random serendipity branches + 5 targeted —
  **creator/showrunner-completion** (TV analog of auteur), **top-genre-canon**, **thin-genre
  round-out**, **older-era classics**, and **world/international TV**. Each branch describes how it
  samples/biases the snapshot + taste profile and the prompt framing it uses.
- `stages/recommend.ts` — the shared recommender implementation each branch job calls: assemble the
  branch's prompt from the snapshot/taste sample, call Claude through the shared `claude-cli` service
  via `src/services/claude.ts` (`runClaude`/`extractJsonObject`) — NEVER call the CLI directly, so the
  rate-limit + monthly quota are enforced globally — and return parsed `RawSuggestion[]`. Mirror
  `src/jobs/movies/stages/recommend.ts`.

Recommend TV SHOWS the owner does NOT already own (exclude the owned TMDB-id set from the snapshot).

## Done when

- `src/jobs/tv-recs/recs.ts`, `stages/branches.ts`, and `stages/recommend.ts` exist and compile
  (`npx tsc --noEmit` green), exporting the sampler/balancer/dedupe helpers + the 8 branch
  definitions + the shared recommender entry point, all typed against the T214 `PlexShow`/
  `TvTasteProfile`/`RawSuggestion` types.
- `recommend.ts` routes every Claude call through `callService('claude-cli', …)` via
  `src/services/claude.ts` (no direct CLI invocation).
- `expectsTest`: `recs.test.ts` covers the pure sampler/balancer/dedupe logic (stratified sample is
  ≈balanced across strata; dedupe collapses title+year dupes; genre balancing respects the cap) from
  synthetic fixtures, scratch DB only — mirroring `src/jobs/movies/recs.test.ts`.
- `npm test` and `npx tsc --noEmit` are green. (No manifest change — these are libraries, not stages.)
