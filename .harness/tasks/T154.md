## Do

Iterate on the joyful dashboard themes + fonts (built in T142, reviewed by the owner in T045).
Round-1 verdict: the keeper theme families are **Pixel Picnic** and **Sunny 8-bit**; the keeper
font style is **rounded** (the owner likes **Fredoka** and **Nunito**). This is the next CHOOSER
round to narrow further — reuse the EXISTING live, persisted switcher infrastructure from T142
(`data-theme` / `data-font` on `<html>`, the `use*Style` hooks, the pre-paint script), just swap
the option sets.

**THEMES — narrow to the two winning families, 10 total:**
- KEEP `pixel-picnic` and `sunny-8bit` exactly as built.
- Add **4 variant iterations of EACH** — variations on that family's vibe (palette warmth/cool,
  accent hue, dot-grid / texture intensity, contrast, card radius/shadow) while staying clearly in
  the family. So: pixel-picnic + 4 pixel-picnic variants, sunny-8bit + 4 sunny-8bit variants = 10.
  Give them clear names (e.g. `pixel-picnic`, `pixel-picnic-meadow`, `pixel-picnic-dusk`, …;
  `sunny-8bit`, `sunny-8bit-sunset`, …).
- REMOVE the other T142 themes (`candy-bright`, `soft-pastel`, `midnight-neon`, `joyful-dark`) —
  the owner is narrowing to these two families. Keep the untouched `default` dark baseline.
- All 10 must keep the T142 guarantees: dense data (run tables, logs, gate DAG, stat tiles) stays
  legible, mobile-safe at ~402px, and the joyful accents (spark/emoji/hover) intact.

**FONTS — narrow to ROUNDED, the style the owner likes:**
- KEEP **Fredoka** and **Nunito**.
- Add **8 more rounded body-font options** in the same friendly/rounded family so the owner can
  narrow the rounded look — e.g. Quicksand, Baloo 2, Comfortaa, Varela Round, Rubik, Mulish,
  Poppins, Lexend, Dosis (pick 8 distinct rounded sans; load via `next/font`). The **rounded body
  font is the differentiator** — pair each with a sensible display font (a pixel display like
  Pixelify Sans suits the Pixel Picnic vibe, or the rounded font itself for a cleaner heading).
- **KEEP + iterate on Space Mono.** The owner also likes **Space Mono** — keep a few options that
  use it and explore variations on it (e.g. Space Mono everywhere; Space Mono body paired with a
  pixel/rounded DISPLAY font for headings; lighter weight / tighter or looser tracking). So the font
  set is "mostly rounded, plus a small Space Mono cluster to iterate on", not rounded-only.
- REMOVE the OTHER non-rounded body options (VT323, JetBrains as body) — narrowing to the rounded
  family **and** Space Mono. (Pixel fonts may still appear as DISPLAY/heading fonts within a
  pairing.)
- Keep dense data legible at body size.
- **Font WEIGHT — headers are too thick.** Round-1 feedback: the section headers (e.g. the
  "DONE WHEN"-style headings / `.section-heading-summary`, `h1`/`h2`) render too heavy/bold with the
  current rounded fonts. Include options that use a **thinner/lighter weight on headers** — same
  rounded fonts (Fredoka/Nunito et al.) but a lighter heading weight (e.g. 400–500 instead of
  600–700), so headings read crisp rather than chunky. Treat header weight as a dimension to vary
  across the rounded options (a few thinner-header variants), and make sure the chosen heading
  weight is actually available for that font (load the needed weights via `next/font`). Body text
  stays readable.

EVALUATION aid (same as T142): live-switchable, persisted, mobile-safe; the review (T045, which now
depends on this) gates the eventual hardcode-the-winner follow-up. Note in the worklog this is
iteration 2 (T142 → T045 review → T154).

## Done when

The theme switcher offers **10** themes — `pixel-picnic` + 4 variants and `sunny-8bit` + 4 variants,
with the other T142 themes removed and the `default` baseline kept — and the font switcher offers a
narrowed set (Fredoka, Nunito + 8 more rounded sans, PLUS a small Space Mono cluster to iterate on;
the other non-rounded body fonts — VT323/JetBrains-as-body — removed),
all live-switchable + persisted + mobile-safe with dense data legible; `npm --prefix dashboard run
build` passes and `node dashboard/scripts/mobile-check.mjs` stays green.
