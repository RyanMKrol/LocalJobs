## Do

Make the workflow-run **Input → Output mapping** genuinely scoped to THE RUN you're looking
at. Today `GET /api/workflow-runs/:id/io` → `workItemIoRows(firstWave, lastWave)`
(`src/db/store.ts`) reads the ENTIRE `work_items` ledger for the first/last-wave jobs and
joins by `root_key` with NO run filter — so on a run page it shows every input the workflow
has EVER processed, not this run's. The panel even confesses it ("First-cut mapping:
reflects the global work-item ledger, not scoped to this run…"). Decision (interview
2026-06-23): genuinely run-scope it by recording which run advanced each work item, then
filtering the mapping to this run. The blocker is that `work_items` is a cumulative,
idempotent ledger keyed by `(job_name, item_key)` with NO run linkage. Add that linkage and
scope the query. Do all of:

1. **Record run→work-item attribution.** Add an append-only linkage table — e.g.
   `work_item_runs (workflow_run_id TEXT, job_name TEXT, item_key TEXT, root_key TEXT, at
   TEXT, UNIQUE(workflow_run_id, job_name, item_key))` — in `src/db/schema.sql`
   (`CREATE TABLE IF NOT EXISTS`, plus an index on `workflow_run_id`; a brand-new table +
   its index may both live in schema.sql since the column exists on creation for fresh AND
   existing DBs — this does NOT hit the T098 "index on a migration-added column" trap).
   Record a row whenever a work item is advanced **within a workflow run**: `markWorkItem`
   (`store.ts`) runs CHILD-side, where `LOCALJOBS_WORKFLOW_RUN_ID` is already in the env
   (runJob.ts reads it). Give `markWorkItem` an optional `workflowRunId` param defaulting
   to `process.env.LOCALJOBS_WORKFLOW_RUN_ID || null`; when present, after the work-item
   upsert, INSERT-OR-REPLACE the linkage row using the SAME resolved `root_key` the upsert
   computed. Jobs' existing `markWorkItem(...)` call sites stay UNCHANGED (env default);
   the param is for explicit testing. A standalone (non-workflow) run has no run id → no
   linkage row (unchanged behaviour).

2. **Run-scope the IO query.** Change `workItemIoRows` to accept the `workflowRunId` and,
   when given, restrict to the **roots this run advanced**: take the distinct `root_key`s
   from `work_item_runs` for that run, then build one IO row per root — resolving the input
   (first-wave ledger row by root) and the output (last-wave ledger row by root) from the
   ledger as today (so an output produced in an EARLIER run still shows for a root this run
   touched). Keep one-output-per-input (fan-out collapse) but word it honestly. If the run
   has NO linkage rows — an OLD run created before this feature, or a re-run that advanced
   nothing new — do NOT silently dump the global ledger: return an empty `io` with a
   `scope` signal (e.g. `{ scoped: false }` / a distinct note) so the UI can explain it.

3. **API.** `GET /api/workflow-runs/:id/io` passes the run id into `workItemIoRows` and
   returns whether the result is run-scoped (e.g. `scoped: boolean`) plus an accurate,
   non-dev-speak `note`. Reads only — still safe to poll.

4. **Dashboard.** Update the IoPanel on `dashboard/app/workflow-runs/[id]/page.tsx`:
   - It now genuinely shows THIS run's inputs and their outputs — retitle/word it so it no
     longer implies (or needs to apologise for) being the whole ledger.
   - **Fix the caveat styling**: today it renders `⚠ First cut — {note}` and the note ALSO
     starts "First-cut mapping:" (the dev-speak "first cut" appears twice and leaked into
     the product). Remove "first cut" entirely; render the remaining honest caveat (one
     output per input) as a clean, subtle footnote/callout (new minimal CSS in
     `globals.css`), not a raw muted line.
   - Handle the empty/unscoped states gracefully: a run that advanced nothing new →
     "this run processed no new items"; an OLD run with no per-run data →
     "per-run input/output isn't recorded for runs created before this feature" (don't show
     a misleading global dump). Keep it mobile-responsive.

5. **Docs.** Update `CLAUDE.md` (`work_items` stays the cumulative idempotent ledger; a new
   `work_item_runs` linkage attributes which workflow run advanced each item, recorded by
   `markWorkItem` from `LOCALJOBS_WORKFLOW_RUN_ID`; the run IO panel is now genuinely
   run-scoped; pre-feature runs show no per-run IO) and the schema/file-map + `README.md`'s
   run-page description.

6. **Tests.** Cover: `markWorkItem` records a `work_item_runs` row when a run id is
   present and none when absent; `workItemIoRows(run)` returns ONLY roots that run advanced
   (two runs touching different items each see only their own); the unscoped/old-run
   fallback returns empty + `scoped:false` (no global dump); the API returns run-scoped rows
   + the scope flag. Ensure `src/db/migrate-existing-db.test.ts` stays green with the new
   table (it'll be created by schema.sql's `CREATE TABLE IF NOT EXISTS` on the old DB).

## Done when

The Input → Output panel on a workflow-run page shows ONLY the inputs that run actually
processed (and their outputs), driven by a new `work_item_runs` linkage that `markWorkItem`
records from `LOCALJOBS_WORKFLOW_RUN_ID`; a run that advanced nothing new and a pre-feature
run both render an honest explanatory empty state rather than the whole-ledger dump; the
caveat no longer says "first cut" anywhere and is shown as a clean styled footnote; existing
job `markWorkItem` call sites are unchanged; `work_items` remains the cumulative ledger.
CLAUDE.md + README document the new linkage + run-scoped panel; unit tests cover the linkage
recording, the run-scoped query, and the unscoped fallback; the migrate-existing-db guard
stays green; and `npx tsc --noEmit`, `npm test`, `npm --prefix dashboard run build`, and
`node dashboard/scripts/mobile-check.mjs` are all green.
