## Do

The README is a wall of text. Trim it: REMOVE the ## Layout file-tree entirely, and present the worked-example pipelines compactly/scannably (a short line each, not paragraphs). Keep the essential what-it-is / how-to-run / ports / config content and the dashboard pages list. Aim for a noticeably shorter, scannable doc.

## Done when

The Layout file-tree is gone; example pipelines are compact; the core what-it-is / how-to-run / install content remains accurate; the README is materially shorter.
