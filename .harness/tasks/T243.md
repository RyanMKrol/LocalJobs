## Do

On the Backlog view, add a **reviewed vs not-reviewed breakdown** to the done section header (today it
shows just a count, e.g. "Done (209)"). Show e.g. "Done (209 · 182 reviewed · 27 not reviewed)" so the
remaining review queue is visible at a glance. The `reviewed` flag is already on each task from
`GET /api/backlog` (the T136 `.harness/reviews.json` overlay; absent → false; human-done derive
reviewed=true), so the counts are derived client-side in `dashboard/app/backlog/page.tsx`.

## Done when

- The done-section header shows the total plus a reviewed / not-reviewed breakdown, derived from the
  per-task `reviewed` flag.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
