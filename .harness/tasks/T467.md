## Overview
Before any code is written, pin down the concrete shape of the movie-recommendations → (movie-recommendations + missing-movies) split so the backend restructuring task has an unambiguous spec, given several genuinely open architectural questions the source idea flagged and a real risk of mishandling the T145 ignore-ledger if job names change.

## Do
Read src/workflows/movies/movies.workflow.ts, src/workflows/movies/CLAUDE.md, src/workflows/tv-recs/tv-recs.workflow.ts + CLAUDE.md, and src/workflows/missing-tv-seasons/plex.workflow.ts + CLAUDE.md as the reference pattern being mirrored. Also read src/workflows/movies/stages/notify.ts (the combined-digest logic, NOTIFY_JOB='movie-gaps-notify'), src/workflows/movies/recs.ts (RECS_JOB='movie-recs'), and the /api/movie-gaps* + /api/movie-recs* handlers in src/api/server.ts to confirm how the ignore/notified ledgers are keyed.

The owner has already decided the following (2026-07-08) — record these verbatim, do not re-litigate:
1. New workflow name: `missing-movies`. Schedule: WEEKLY (matching missing-tv-seasons' cadence, NOT the monthly cadence franchise-gaps currently runs at inside movie-recommendations — this is a deliberate cadence CHANGE, more frequent gap alerts than today). Category: `recommendations` (matching the other Plex-recs-style workflows).
2. Snapshot approach: duplicate the Plex movie snapshot into its OWN job for missing-movies (mirroring tv-snapshot vs plex-tv-snapshot), rather than sharing movie-snapshot's output across both workflows — confirmed OK for now.
3. Folder layout: separate folders — new `src/workflows/missing-movies/` folder, `src/workflows/movies/` trimmed to recs-only — mirroring every other shipped workflow being one folder = one workflow.
4. Confirm NO migration of existing work_items rows is needed: the movie-gaps-notify job (ledger key NOTIFY_JOB) and movie-recs ledger key (RECS_JOB) keep their exact current string values even after movie-gaps-notify's JobDefinition moves to the new workflow's manifest — the ledger is keyed by job_name/item_key only, not by workflow membership, so as long as the job's `name` field and the two ledger-key constants are UNCHANGED, every existing ignored/notified row keeps resolving with zero data migration. This must be spelled out explicitly in the design record since getting it wrong risks silently orphaning the owner's existing ignore decisions.
5. Whether the new snapshot job should skip building taste-profile.json (franchise-gaps.ts never reads it — only the 8 recommender branches do), to avoid dead work — still open, decide during this task.
6. Dashboard split (MovieGapsManager moving to the new workflow's detail page): the owner confirmed the DoD includes the dashboard split in the same pass (see split-movie-workflows-backend/-dashboard below) — not deferred.

Note: the owner separately asked for a standalone, non-blocking needs-human task evaluating whether the framework should GAIN a cross-workflow job-sharing mechanism in general (see split-movie-workflows-job-sharing-eval below) — this is independent research, not a prerequisite for this split, which proceeds with the duplicate-snapshot approach regardless of that evaluation's outcome.

Record the final decision in this task's worklog entry in enough detail that a fresh agent building split-movie-workflows-backend needs no further judgment calls — every name, file path, schedule string, and the ledger-continuity guarantee should be spelled out explicitly.

## Done when
A clear, unambiguous decision is recorded (in the task worklog) covering: new workflow name (`missing-movies`) + WEEKLY schedule + `recommendations` category; snapshot duplication approach (confirmed: duplicate, own job) + whether taste-profile is skipped; folder layout (confirmed: separate `src/workflows/missing-movies/` folder); explicit confirmation that NOTIFY_JOB/RECS_JOB ledger key strings and the movie-gaps-notify job's `name` field stay unchanged (no migration); and confirmation the dashboard split is in scope for this pass (not deferred). No code changes required for this task — it is a decision record only.
