## Do

Show each task's **worklog** in the expanded Backlog row (especially useful for DONE tasks — the build
summary). Today the expansion renders the spec markdown (`## Do`/`## Done when`) + orchestration fields.
Add the worklog alongside it. `GET /api/backlog` (`src/api/server.ts`) already inlines the spec as
`specContent` via `readTaskSpec` (confined to `.harness/tasks/*.md`); add an analogous `worklogContent`
read of `.harness/worklog/<id>.md` (committed; path-confined the same way). Render it in the expanded
cell in `dashboard/app/backlog/page.tsx` next to the spec.

## Done when

- Expanding a task shows its worklog (`.harness/worklog/<id>.md`) rendered as markdown, alongside the
  spec, dependsOn, tags, etc. — tasks with no worklog simply show nothing extra.
- The worklog read is path-confined to `.harness/worklog/*.md` (no traversal).
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, `npm test` green; daemon restarted (API change).
