## Overview
Re-attempt of T585 (itself a re-attempt of T563), blocked a SECOND time by scope-creep on the exact same task lineage — this time the diff correctly touched root CLAUDE.md (to add the new src/core/fsjson.ts file-map entry, as T585's own spec explicitly instructed) but CLAUDE.md was missing from scope. T563 was blocked for the same class of miss on a different file (plex-space-saver/lib.ts). The underlying refactor (fold readJsonFile/writeJsonFile/ensureDirs from perfumes/lib.ts, movies/lib.ts, tv-recs/lib.ts, and plex-space-saver/lib.ts into src/core/fsjson.ts) is unchanged and still fully valid — src/core/fsjson.ts still does not exist and all four workflow libs still duplicate the helpers verbatim, confirmed by grep against current main.

## Do
Identical work to T585's spec: create src/core/fsjson.ts exporting readJsonFile<T>(path, fallback), writeJsonFile(path, data), and ensureDirs(...dirs) with existing signatures/behavior unchanged (ensureDirs variadic/array-accepting so movies/tv-recs' multi-dir calls and perfumes/plex-space-saver's differing calls all still pass the identical directory set as args). Replace the duplicated function bodies in all four workflow libs (perfumes/lib.ts, movies/lib.ts, tv-recs/lib.ts — all three currently define all three helpers; plex-space-saver/lib.ts — currently defines only ensureDirs + writeJsonFile, no readJsonFile) with imports from src/core/fsjson.ts, and keep each workflow lib RE-EXPORTING the same names from its own lib.ts so every existing import path used by that workflow's stages/tests keeps resolving unchanged — do not touch call sites outside each lib.ts file. Add src/core/fsjson.test.ts covering readJsonFile's file-absent-fallback path and a writeJsonFile/readJsonFile round-trip. Update the root CLAUDE.md file map to add a one-line entry for src/core/fsjson.ts, matching the existing convention used for src/core/browser.ts and src/core/plex-client.ts (both listed in the file-map table around line 297-298) — this CLAUDE.md edit is now IN SCOPE, which is the one substantive change from T585's spec; everything else is unchanged.

## Done when
src/core/fsjson.ts exports readJsonFile / writeJsonFile / ensureDirs as the single definition. A repo-wide grep (excluding src/core/fsjson.ts itself and node_modules/data folders) finds no remaining duplicate `function readJsonFile` / `function writeJsonFile` / `function ensureDirs` definitions in src/workflows/perfumes/lib.ts, src/workflows/movies/lib.ts, src/workflows/tv-recs/lib.ts, or src/workflows/plex-space-saver/lib.ts — each imports from core instead. Every existing import path that pulled these from one of those four workflow libs still resolves (their stages/tests are unmodified and still pass). Workflow outputs are unchanged on fixtures (existing per-workflow tests green). src/core/fsjson.test.ts exists and covers the file-absent fallback and a write/read round-trip. Root CLAUDE.md file map has a one-line entry for src/core/fsjson.ts. `npx tsc --noEmit` and `npm test` both green.
