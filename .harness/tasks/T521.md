## Overview
Render the existing 'certified' medal badge on the Overview/home page's workflow cards too, so a certified workflow is recognisable there — not only on the Workflows list and detail pages.

## Do
In dashboard/app/page.tsx, the Overview 'Workflows' section maps each workflow to a card whose header is `<Link href={`/workflows/${p.name}`}><strong>{p.name}</strong></Link>` (around line 181) with no certified badge. Add the certified medal beside the workflow name, reusing the EXACT existing badge markup used on the Workflows list page (dashboard/app/workflows/page.tsx line 92): `{p.certified ? <Pill kind="certified" title="Certified" style={{ marginLeft: 6 }}>🏅</Pill> : null}`. Import Pill from './components/Pill' (dashboard/app/page.tsx does not yet import it — add the import). The `certified` boolean is already present on the workflows payload consumed by api.workflows() (see api.ts `certified?: number`), so no API/store/type work is needed — this is a pure UI addition. Do NOT change the Workflows-list or detail-page rendering. The Overview page is already a PAGES entry ('overview', path '/') and the workflows fixtures in _dashboard-harness.mjs already carry `certified` on some workflows, so no harness change is required — but confirm the 'overview' visual-check screenshot exercises a certified workflow card. Build the dashboard, run node dashboard/scripts/mobile-check.mjs and node dashboard/scripts/visual-check.mjs, and LOOK at the 'overview' screenshot to confirm the medal renders beside a certified workflow's name (painted/visible, not clipped).

## Done when
A workflow marked certified shows the same 🏅 medal Pill (kind='certified') beside its name on its Overview/home-page workflow card, using the existing certified flag from the /api/workflows payload and the existing Pill component; non-certified workflows show no badge; the Workflows-list and detail-page rendering are unchanged; and dashboard build + mobile-check + visual-check are green with the 'overview' screenshot showing the medal on a certified card.
