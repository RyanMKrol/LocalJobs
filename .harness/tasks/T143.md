## Do

NEEDS-HUMAN review of the joyful dashboard theme/font options built by T142. Open the
dashboard on BOTH desktop AND your phone (over Tailscale — this is accessed remotely from a
phone), switch between the six themes (2 dark + 4 bright) and the 5–10 display+body font
pairs, toggle the reduce-motion/minimal-emoji control, and CHOOSE: a preferred theme, a
preferred font pairing, and your reduce-motion/emoji preference. While reviewing, pay
particular attention to (a) legibility of the dense run tables, streaming logs, gate DAG and
stat grids under each theme/font, and (b) phone-width usability — no overflow, readable small
text, reachable controls. Record the chosen theme + font + motion preference (e.g. in this
task's worklog) so a follow-up buildable task can hardcode them and remove the switchers +
unused themes/fonts. The loop skips this — it's a manual visual review.

## Done when

You have reviewed all six themes and the font pairings on BOTH desktop and your phone,
confirmed dense data stays legible, and recorded the chosen theme + font pairing + motion
preference; a follow-up can then hardcode the winners and drop the switchers + unused
options.
