## Do

NEEDS-HUMAN evaluation. On the movies workflow graph (`/workflows/movies`), a validation-gate mark
is rendered attached to the **gaps-notify** job ALONE (under a single node) instead of ON THE ARROW
between two jobs (producer → consumer), which is how gates normally appear. Work out WHY, and
whether it's correct-but-confusing or a bug. Candidates to check:
- (a) It's a real gate whose producer→consumer edge is **non-adjacent**, so the Dag's documented
  fallback — "render the gate under the consumer node rather than on a single inter-wave arrow"
  (`dashboard/app/components/Dag.tsx`) — kicks in.
- (b) The movies DAG **shape** (a fan-out/fan-in — e.g. the 8 recommender branches + merge, T146)
  puts the gate's two jobs in non-adjacent waves.
- (c) A **mis-derived or spurious gate** — a `produces`/`consumes` contract pair in the movies
  workflow that shouldn't exist, or is mis-keyed, so `deriveGates` emits a gate that doesn't sit on
  a clean edge.

Inspect the movies workflow's `produces`/`consumes` contracts, the `deriveGates` output for it, the
wave layout, and the Dag gate-placement logic. Then DECIDE: correct rendering (and maybe a
presentation tweak so a single-node gate reads clearly), or a real bug (in the contracts / gate
derivation / placement) — and record the decision (in this task's worklog) so a follow-up buildable
task can fix it if needed. The loop skips this — manual diagnosis.

## Done when

The reason the movies graph shows a validation gate under the gaps-notify job (not between two jobs)
is understood and a decision is recorded — correct-but-presentational vs a contract/gate-derivation/
placement bug — with a follow-up task drafted if a fix is warranted.
