## Overview
On a limited manual run of projects-sync, the first stage (github-sync) marks every repo in the ledger instead of only the selected roots, so the run's Input/Output panel misrepresents what was actually processed. Fix it to respect the run's root selection, mirroring places-ingest.

## Do
In src/workflows/projects-sync/stages/github-sync.ts, wrap the ledger-marking loop (currently `for (const entry of entries) { markWorkItem(...) }`, ~lines 189-204) so it only marks entries where ctx.rootAllowed(entry.repoId) is true — mirroring how places-ingest scopes its ledger to selected roots. The catalog file write (writeCatalogFn(entries), ~line 180) and the entries.length===0 early-return MUST stay operating on the FULL entries list: data/out/projects.json remains complete; only the ledger is scoped to the run's roots. On an unlimited/scheduled run, ctx.rootAllowed returns true for everything (the set is null), so behaviour is unchanged. Keep the per-entry progress/log lines counting over whatever is actually marked. This is the only T094 first-stage-rule violation in the repo; every key-changing stage's rootKey usage is already clean.

## Done when
github-sync only records work_items for repoIds that ctx.rootAllowed permits, while data/out/projects.json still lists every catalog entry regardless of the limit. A new unit test in github-sync.test.ts drives ctx.rootAllowed to allow only a subset and asserts markWorkItem is called for exactly that subset while the catalog write covers all entries. Existing tests green; npx tsc --noEmit and npm test pass.
