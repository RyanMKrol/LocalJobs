## Do

Roll out full validation-gate coverage so EVERY edge in EVERY workflow graph is a validated boundary
(movie-recommendations already has this). Gates derive from each job's `produces`/`consumes` artifact
contracts, so add the missing contracts per workflow, mirroring the movies pattern:
- Audit `places` (`src/jobs/places/contracts.ts`) and `perfumes` (`src/jobs/perfumes/contracts.ts`) —
  confirm a contract on EVERY edge; add any missing.
- Add contracts for `missing-tv-seasons`/plex (`src/jobs/plex/`) and `tv-recommendations`
  (`src/jobs/tv-recs/`) — create `*.contracts.ts` and wire `produces`/`consumes` on each stage.
Keep contracts **shape-only + empty-tolerant** (validate the artifact exists/parses/has the expected
array; an empty or gracefully-failed hand-off is NOT a violation) so resilient fan-out stages aren't
blocked. Gates are derived by `deriveGates` (`src/core/dag.ts`) and surfaced via
`GET /api/workflows/:name` → `DagFlow`.

## Done when

- Every dependency edge in places, perfumes, missing-tv-seasons, and tv-recommendations has a derived
  gate (verify via `GET /api/workflows/:name` `.workflow.gates`).
- New contracts are shape-only + empty-tolerant; no resilient stage is newly blocked.
- `npx tsc --noEmit` and `npm test` green; CLAUDE.md/contract docs updated as needed.
