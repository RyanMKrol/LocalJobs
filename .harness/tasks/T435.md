## Overview

Part of a repo-wide sweep giving every job a real paragraph-length `description` (surfaced on
`/jobs/[name]` and `/runs/[id]`) instead of a one-liner. This task covers the single job in the
`listening-digest` workflow. The mechanism was already built by the foundational task this depends
on — this is pure content authoring.

## Do

Rewrite the `description` of `src/workflows/listening-digest/stages/listening-digest.job.ts`
(`lastfm-digest`) into a real 3-6 sentence paragraph. Currently: "Build a monthly markdown digest of
top albums/tracks from Last.fm (period=1month) and write it to data/out/. Idempotent per calendar
month via the work_items ledger." Expand it to cover: the two Last.fm endpoints it calls
(`user.getTopAlbums` + `user.getTopTracks`), the "one song on repeat" false-positive filter (albums
where a single track accounts for ≥70% of the album's plays), why there's no DynamoDB persistence
(the owner's website already reads Last.fm's aggregation directly), and the idempotency keying
(`YYYY-MM`). Ground it in root `CLAUDE.md`'s `listening-digest` section and the job's own code. Plain
prose only, no markdown syntax.

## Done when

- The `lastfm-digest` job has a real, accurate paragraph description grounded in its actual behavior —
  not a one-liner.
- `npx tsc --noEmit` and `npm test` are green.
