## Do

The parse stage emits accords: [{name, pct}] but pct is always null. Pull each accord's strength (the coloured bar's width %) off the cached Fragrantica page so accords carry weight. Parse against the already-fetched HTML under src/jobs/perfumes/data — NO live scrape.

## Done when

Accords carry a numeric pct parsed from the bar width on a cached fixture; pct is null only when genuinely absent on the page; a unit test asserts the parsed percentages against a committed/synthetic HTML fixture.
