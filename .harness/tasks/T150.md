## Do

A workflow run where **nothing succeeded** is wrongly reported as `partial` instead of `failed`.
The final-status rollup in `src/core/workflow-executor.ts` (~line 351) is:

```ts
const status = controller.signal.aborted
  ? 'cancelled'
  : statuses.length === 0 ? 'failed'
  : statuses.every((s) => s === 'success') ? 'success'
  : 'partial';
```

So `partial` is the catch-all for "not all succeeded" — which lumps together TWO different
outcomes: "some stages succeeded, some didn't" (genuinely partial) and "**no** stage succeeded at
all" (which is a plain failure). Example (real): the plex run with `plex-tv-snapshot=failed`,
`tmdb-season-check=skipped`, `plex-seasons-notify=skipped` reported `partial` even though nothing
ran successfully.

Fix the rollup so `partial` requires at least one success:
- all members `success` → **`success`**
- at least one `success` but not all → **`partial`**
- **no** member `success` (all failed / skipped / a mix) → **`failed`**

i.e. insert a `statuses.some((s) => s === 'success') ? 'partial' : 'failed'` branch in place of the
bare `'partial'`. Keep the existing `cancelled` (aborted) and empty-members (`failed`) cases
unchanged. (The root stage always runs, so an all-`skipped` run can't legitimately occur — but the
"no success → failed" rule handles any failed/skipped combination correctly regardless.)

Add/adjust a unit test in `src/core/workflow-executor.test.ts` covering the three buckets:
all-success → `success`; first-stage-fails-rest-skipped → **`failed`** (the regression); and
mixed success+failure → `partial`. Update any existing test or doc that assumed the old
"not-all-success ⇒ partial" semantics. If the dashboard/README describes run statuses, make sure
`partial` is still described as "some succeeded, some didn't" (now accurate).

## Done when

A workflow run is recorded `failed` when no member stage succeeded (e.g. first stage fails and the
rest skip), `partial` only when at least one stage succeeded but not all, and `success` when all
did; `cancelled` (aborted) and no-members behaviour are unchanged. A unit test covers the
no-success→`failed`, mixed→`partial`, and all→`success` cases; `npx tsc --noEmit` and `npm test`
are green.
