## Overview
Re-attempt of T550, blocked (audit-FAILed 4x across sonnet-medium and opus-medium, MAX_ATTEMPTS exhausted at the top tier). T550's own Done-when required a whole-tree grep-clean sweep for bare pill/badge status spans, but its scope omitted dashboard/app/page.tsx, which has two of exactly the pattern being removed (lines 199 and 275, `<span className={`badge ${status}`}>...`, using the same statusLabel helper as the converted ones in workflows/page.tsx and workflows/[name]/page.tsx). Every attempt correctly did the in-scope work (StatusBadge type widening, cast removal) but could not satisfy the grep-clean criterion without touching an out-of-scope file, so the audit correctly FAILed every time on an unmet, machine-checkable Done-when item — not a builder mistake. This re-attempt widens scope to include page.tsx so the original whole-tree grep-clean requirement is actually achievable.

## Do
Do NOT touch the Workflow.category field (already done, api.ts:237 — out of this task's scope entirely). (1) admin/page.tsx has a bare `<span className={`pill ${r.status === 'reset' ? 'on' : 'off'}`}>{r.status}</span>` (around line 266, in the reset-results table) — replace with <Pill kind={r.status === 'reset' ? 'on' : 'off'}>{r.status}</Pill>, matching the existing <Pill> usage already in the same file. (2) StatusBadge (ui.tsx, currently `export function StatusBadge({ status }: { status: RunStatus })`) is typed too narrowly for workflow statuses (e.g. 'partial'), which forced bare `<span className={`badge ${status}`}>` spans instead of <StatusBadge> at THREE call sites: dashboard/app/page.tsx (both the profile/place row at ~line 199 and the run-history row at ~line 275 — both use a local `statusLabel(status)` helper for their inner text; StatusBadge already renders its own emoji+label from STATUS_EMOJI/label maps, so check whether statusLabel's output should be dropped in favour of StatusBadge's own labelling, or whether StatusBadge needs the label text passed through — resolve by matching how workflows/page.tsx already handles the identical statusLabel+badge combination it converted), workflows/[name]/page.tsx (~line 1090), and components/StageIoLists.tsx (~line 148). It ALSO forced `status as RunStatus` casts at workflow-runs/[id]/page.tsx (~line 90) and both spots in workflow-runs/[id]/gates/[producer]/[key]/page.tsx (~lines 154, 173). Widen the StatusBadge prop type to accept every status value actually passed at these call sites (RunStatus | WorkflowRunStatus, plus whatever the gate page's `gate.state` type is if it differs — its own STATUS_EMOJI/label maps must cover every value passed, verify this rather than assuming), replace all bare badge spans (page.tsx x2, workflows/[name]/page.tsx, StageIoLists.tsx) with <StatusBadge>, and delete all three `as RunStatus` casts now that the type accepts the real value directly. Finish with a grep across the WHOLE dashboard/app tree for bare pill/badge spans (see verify) and confirm it is clean — this time it is achievable since page.tsx is in scope.

## Done when
No bare `className="pill ..."` or `className={`badge ...`}` spans remain anywhere in dashboard/app (grep-clean, confirmed via the verify grep above) EXCEPT: StatusBadge's own definition in ui.tsx, the static non-status badges (`className="badge queued"` and the bare `className="badge"` "definition" label on the gate page), and Pill.tsx's doc-comment text. StatusBadge's prop type accepts every status value actually passed to it at every call site with no `as RunStatus`/similar cast anywhere in dashboard/app. npx tsc --noEmit is green. dashboard build + mobile-check + visual-check all green, and visual-check screenshots for the dashboard home page, admin page, workflow-detail, workflows-list, and stage-io panel show the pills/badges rendering identically to before (same colors/labels/emoji) — not just present in the DOM.
