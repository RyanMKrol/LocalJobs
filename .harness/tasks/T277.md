## Do

Migrate the two **gate detail** pages — `dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx`
(run-scoped) and `dashboard/app/workflows/[name]/gates/[producer]/[key]/page.tsx` (definition-scoped) —
to use the shared UI component library created in T268 (pills, panels, back-link, etc. wherever they
apply). Like-for-like swap: no visual or behavioural change. These two pages are structurally similar,
so migrate both together.

## Done when

- Both gate pages use the shared components from `dashboard/app/components/` (per T268) instead of
  re-implementing equivalents; they render identically.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green; `node dashboard/scripts/visual-check.mjs` green. NOTE: the gate pages aren't currently in
  the visual-check `PAGES` list — add a `PAGES` entry (with a fixture gate) for at least one gate page
  in `dashboard/scripts/_dashboard-harness.mjs` so the migration is screenshot-verified, then confirm
  it renders unchanged.
