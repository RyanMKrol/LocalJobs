## Overview
The original T388 bundled two separable things: fixing the mobile-check regression blocker (review-t388-a) and sweeping fixture coverage across every remaining workflow. Splitting them means this fixture-coverage sweep is no longer blocked by an unrelated mobile-check false positive discovered along the way — a task this broad (originally touching up to ~6+ workflows' worth of new fixtures in one shot) is also more tractable done as its own focused unit once the check itself is trustworthy.

## Do
As of this task, cross-check `dashboard/scripts/_dashboard-harness.mjs`'s `fixtureFor()`/PAGES against the full current workflow list (every `src/workflows/*/*.workflow.ts` file, or `GET /api/workflows` on a running daemon) to find which workflows still lack a `/stage-io` fixture set and a PAGES entry exercising their run-detail page — at minimum this includes `missing-tv-seasons`, `workouts-sync`, `listening-digest`, `projects-sync`, and `plex-space-saver`, but re-derive the list fresh rather than trusting this enumeration (workflows may have been added or renamed since). For each still-missing workflow, add a `<name>Members`/`<name>WorkflowRun`/`<name>StageIo` fixture set mirroring the existing `stockDigest*`/`movieRecs*`/`singleStage*` patterns already in the file, and a PAGES entry pointing at its run-detail page — unless an existing PAGES entry already covers a run of that workflow. Pick a DAG shape for each fixture that's genuinely representative of that workflow's real member-job structure (e.g. missing-tv-seasons is a linear scan+notify shape; projects-sync is linear catalog+summarize) rather than copy-pasting an unrelated shape. Run the full local Definition-of-Done sweep — `npx tsc --noEmit`, `npm test`, `npm --prefix dashboard run build`, `node dashboard/scripts/mobile-check.mjs`, `node dashboard/scripts/visual-check.mjs` — and fix anything that regresses.

## Done when
Every workflow this repo ships (per `src/workflows/*/*.workflow.ts`) has a `/stage-io` fixture set and a PAGES entry exercising its run-detail page in `dashboard/scripts/_dashboard-harness.mjs`. `node dashboard/scripts/mobile-check.mjs` reports ALL pages passing (0 horizontal overflow, 0 boundary-crossing elements) — the full regression check, not a partial one. `node dashboard/scripts/visual-check.mjs` runs successfully AND you look through the full set of screenshots in `dashboard/scripts/visual-out/` for every NEWLY-added workflow's run page this task covers — confirm each renders sensibly (tabs present, at least one populated Inputs/Outputs column, nothing blank/overlapping/clipped). Record what you saw in this task's worklog entry, naming each workflow you actually looked at. `npx tsc --noEmit` and `npm test` both pass.
