## Do

The owner asked: "Can we maybe use a shared component for tables like this, because I feel like the styling issues are pretty consistent whenever we try to create a new table?" — grep confirms raw `<table>` markup is independently authored in 8 files under `dashboard/app` (`workflows/page.tsx`, `workflows/[name]/page.tsx`, `workflow-runs/[id]/page.tsx`, `components/WorkflowOutputSection.tsx`, `jobs/[name]/page.tsx`, the overview `page.tsx`, `services/page.tsx`, and `ui.tsx`). This task's scope is deliberately LIMITED to the two pages under active discussion — `dashboard/app/services/page.tsx` and `dashboard/app/workflows/page.tsx` — as the first proof of the pattern; the OTHER 6 files are explicitly OUT OF SCOPE for this task (do not touch them; migrating them is left as future follow-up work if the owner likes this first migration). Both pages already share the exact same shape: a `CATEGORY_GROUPS` constant (label + key), filter workflows/services into groups, render one `<div className="panel">` per non-empty group containing an `<h2>{label}</h2>` and a `<table>` with a `<thead>`/`<tbody>`. Create a new component, e.g. `dashboard/app/components/CategoryTable.tsx` (following the existing `RunButton.tsx`/`Pill.tsx` convention in that folder — a small focused component, documented with a header comment explaining its props, exported as a named export), that encapsulates: the `.panel` wrapper, the `<h2>` category label, spacing between stacked instances (reuse the `.panel + .panel` CSS rule from the sibling padding/spacing task rather than re-inventing it — this task depends on that landing first specifically so the padding/spacing values are already finalized and this component doesn't need to re-derive them), and the `<table>`/`<thead>` scaffold. Design it generically: it should accept the column headers and a way to render each row (e.g. `columns: { key: string; label: string; align?: 'left'|'center' }[]` plus a `renderRow: (item) => ReactNode` per item, OR simply accept `children` for the `<tbody>` and just own the outer `.panel`+`<h2>`+`<table><thead>` scaffold if that's simpler and less over-engineered — prefer the simpler shape that still removes the duplicated boilerplate; don't build a kitchen-sink generic table component when the two current call sites don't need one). Migrate BOTH `services/page.tsx` and `workflows/page.tsx` to render their per-category groups through the new component instead of their own hand-rolled `<div className="panel">...<table>...` JSX, preserving all existing behaviour (click handlers, editing state, Pill/RunButton usage, sticky stuck-item popovers, etc. — the component only owns the STRUCTURAL/STYLING wrapper, not page-specific interactivity). After migrating, add a one-line note to root `CLAUDE.md`'s existing "Shared dashboard UI components (T268)" convention list (alongside `RunButton`/`Pill`) documenting the new component, per this repo's Definition of Done requiring docs to be updated in the same change.

## Done when

`npx tsc --noEmit` and `npm test` pass. `npm --prefix dashboard run build` succeeds. Run `node dashboard/scripts/visual-check.mjs` and look at the `/services` and `/workflows` screenshots in `dashboard/scripts/visual-out/` — confirm both pages render EXACTLY as they did before the migration (same padding/spacing from the earlier tasks, same content, same layout) — this is a refactor, not a visual change, so the screenshots should look like a continuation of the prior two tasks' results, not a regression. Also spot-check a couple of OTHER pages' screenshots to confirm nothing broke (the new component is additive; it shouldn't touch unrelated pages). Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green. Confirm root `CLAUDE.md`'s T268 shared-components section now mentions the new component. This is a `layer:ui` task — visual confirmation via the screenshots is a hard requirement, and is especially important here since a pure refactor accidentally changing pixel output would be an easy mistake to miss by reading the diff alone.
