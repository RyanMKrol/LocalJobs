## Do

Change the `project-summarize` stage's Claude invocation from the current no-tool, prompt-embedded-text approach to a real filesystem-tool-access mode scoped to the cloned repo directory, so Claude explores the actual project (package.json, source layout, other docs) itself rather than relying only on catalog metadata + an embedded README slice. This task depends on `projects-sync-summarize-1` (the enforced output template) — the template it must still produce is defined there; this task changes ONLY how the content to fill it is gathered.

Context you need: the shared `runClaude` helper (`src/services/claude.ts`) always spawns `claude -p --output-format json --model <model> --dangerously-skip-permissions` with `cwd: tmpdir()` and pipes the prompt via stdin — Claude gets NO filesystem access in that mode. That helper is used by OTHER workflows too (perfumes build stage, movies recommender branches) which must NOT gain broad tool access as a side effect of this change. Confirmed available CLI flags (run `claude --help` to re-verify if needed): `--add-dir <directories...>` (grant tool access to specific directories) and `--allowedTools`/`--allowed-tools <tools...>` (allow-list specific tools, e.g. `Read Glob Grep`). Do NOT modify `runClaude`'s exported signature or default behavior — its existing callers must be unaffected.

1. Add a NEW, separate helper for this one use case instead of changing `runClaude`'s contract. Suggested shape: a small new module `src/jobs/projects-sync/claude-repo.ts` exporting something like `runClaudeWithRepoAccess(prompt: string, model: string, repoDir: string): Promise<ClaudeResult>` that spawns the CLI directly with:
   - `cwd` = the actual cloned repo directory (the stage already has this — `repoCloneDir(entry.name)` — from the existing clone/pull step).
   - `--add-dir <repoDir> --allowedTools Read Glob Grep` (read-only exploration tools ONLY — explicitly do NOT include `Bash`, `Write`, `Edit`, or any mutating/tool that could modify files or run commands; this must stay strictly read-only and scoped to that one directory).
   - Reuse the same spawn/timeout/JSON-envelope-parsing logic `src/services/claude.ts`'s internal `spawnClaude` already implements (factor out a shared helper if that's clean, e.g. a parameterized spawn function both `runClaude` and this new helper call with different `args`/`cwd` — but do NOT change `runClaude`'s exported behavior for existing callers).
   - Still gate the call through the existing `claude-cli` service (`callService('claude-cli', () => ...)` from `src/core/services.ts`) so the shared rate-limit/quota meter continues to govern it — do not bypass `src/core/services.ts`.
2. Update `runProjectSummarize` in `src/jobs/projects-sync/stages/project-summarize.ts` to call this new path instead of the current `summarize(prompt, claudeModel)` (`ClaudeSummarizer`) call. Keep it injectable for tests — add a new optional `opts` field (e.g. `opts.summarizeWithRepoAccess`) mirroring the existing `opts.summarize` injection pattern, so `project-summarize.test.ts` can stub the CLI call without spawning a real process.
3. Rework `buildSummaryPrompt` (or its replacement) since Claude now has its own read access to the repo: tell Claude explicitly that the repo is available at its current working directory and it should explore it (package.json, source layout, README, other docs it finds) to fill in the template from `projects-sync-summarize-1` (`src/jobs/projects-sync/project.template.md`). KEEP embedding the catalog metadata (name, description, language, topics, last-pushed, URL) in the prompt text — Claude has no other way to learn the GitHub-side fields from local file exploration alone. You may drop the inline README-text embedding now that Claude can read the file itself (prefer this — simpler prompt, no duplication) but state your choice clearly in the final work.
4. Keep the post-generation template validation added in `projects-sync-summarize-1` unchanged and apply it to output from this new invocation path too (reuse the existing validation function/logic — do not duplicate it).
5. Update `project-summarize.test.ts`: the injection points change shape (a repo-access-based Claude call doesn't take a `readme` argument the way the old `summarize(prompt, model)` call did) — update or replace the relevant tests. Add a test asserting the new call is invoked with the correct `cwd` and the correct `--add-dir`/`--allowedTools` argument list (inject a stub/spy at the spawn boundary, mirroring how `runGit`/`cloneOrPullRepo` are already tested via an injectable seam), and assert that NO mutating tool (`Bash`, `Write`, `Edit`) appears in the allowed-tools list passed to the CLI.
6. Update root `CLAUDE.md`'s `projects-sync` description to describe the new invocation shape (Claude explores the cloned repo directly via scoped read-only tool access, rather than a text-only prompt) and explicitly note this is a DIFFERENT invocation shape from the shared `runClaude`/`claude-cli` service usage elsewhere in the repo (still gated through the same `claude-cli` service quota, just a different CLI flag set/cwd).

## Done when

- Claude's summarize call for `project-summarize` runs with `cwd` set to the cloned repo directory and `--add-dir`/`--allowedTools Read Glob Grep` (or equivalent read-only tool scoping) — verified by a unit test asserting the actual invocation arguments/cwd via an injected spawn seam, not just that some call happened.
- No `Bash`/`Write`/`Edit`/other mutating tool is present in the allowed-tools list for this call (asserted in a test).
- `src/services/claude.ts`'s exported `runClaude` signature and behavior are UNCHANGED for its existing callers (perfumes, movies) — confirm via `npx tsc --noEmit` and that the existing perfumes/movies test suites still pass unmodified.
- The new call still routes through `callService('claude-cli', ...)` so quota/rate-limit governance is preserved (verify by inspection/test — the call is still metered).
- The generated output still passes the template validation added in `projects-sync-summarize-1` (reused, not duplicated).
- `project-summarize.test.ts` passes with injected/stubbed Claude calls — no real process spawn and no real network/API call in the test suite.
- Root `CLAUDE.md`'s `projects-sync` description reflects the new invocation shape and notes it differs from the shared `runClaude` usage elsewhere.
- `npm test` and `npx tsc --noEmit` pass.
