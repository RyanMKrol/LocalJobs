## Overview
Make api.ts path encoding uniform, factor the five bespoke post-with-server-error fetch functions into one helper, and stop the output modals from silently closing on a fetch failure.

## Do
In dashboard/app/lib/api.ts: (6) encodeURIComponent the interpolated name/job in the endpoints that don't — job() (:440), jobRuns() (:441), stuck(job) (:445), ignored(job) (:447), workflow() (:509), runWorkflow() (:539), toggleWorkflow() (:540) — matching the other 13 endpoints. (9) The five functions updateWorkflowSchedule (:544), updateWorkflowConcurrency (:557), updateWorkflowNotify (:569), updateWorkflowCertified (:581), updateJobTimeout (:595) each hand-roll the same fetch + JSON parse + `if (!res.ok) throw new Error(data.error || res.statusText)` block — extract a single generic postWithServerError<T>(path, body) helper and route all five through it (preserving each one's return shape). (10) In dashboard/app/components/StageIoLists.tsx (:184, :234) and dashboard/app/components/WorkflowOutputSection.tsx (~:61-63), a modal-content fetch failure does `.catch(() => setModal(null))`, silently closing the modal so a View click flashes and vanishes. Instead keep the modal open and render an in-modal error message.

## Done when
Every api.ts path interpolation encodeURIComponents its name/job; the five update* fetch fns share one postWithServerError helper with no duplicated fetch/error-parse blocks; a failed output/stage-io modal fetch shows an in-modal error state instead of closing; npx tsc --noEmit, build, mobile-check, and visual-check are green (LOOK at the modal screenshots).
