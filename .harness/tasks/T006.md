## Do

Add typed-artifact validation gates between dependent pipeline stages: let a job declare what it `produces`/`consumes`, and have the DAG/pipeline executor validate the contract at each boundary so an external-format drift (Takeout CSV layout, Fragrantica page structure) fails LOUD at the exact gate instead of passing bad data downstream. Make a failed gate a first-class, surfaced failure (run status + notification). Unit-test both a passing contract and a drifted/failing one.

## Done when

Contracts are declarable + enforced between stages; a violation produces a clear surfaced failure; tests cover pass + fail; typecheck + tests green; docs updated.
