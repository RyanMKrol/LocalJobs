## Overview
src/workflows/movies/ and src/workflows/tv-recs/ are near-total copies — ~1,300 duplicated lines across 6 file pairs (recs.ts pure helpers, stages/recommend.ts branch logic, stages/merge.ts vs tv-rec-merge.ts, branches.ts 8-branch shape, notify.ts digest/report, snapshot.ts, lib.ts). Extract a generic recommender pipeline into src/core/recommender/ (src/core is the established cross-workflow home, cf. plex-client.ts; the registry only globs *.job.ts/*.workflow.ts so nothing in src/core is auto-discovered) and re-wire both workflows onto it, deleting ~1,100 lines. This is a PURE structural move: the two prep units already aligned the behavioral divergences (searchFailed throw, history schema), T527 lands the B11 push-failure guard, and unit c settled the model-default policy — so this extraction must be behavior-preserving.

## Do
Create a generic recommender pipeline under src/core/recommender/, per the R01 proposed design: types.ts (a RecommenderMedia base + a RecommenderDomain<M,P> interface carrying id/branches/paths/tuning plus the per-domain functions search()/genreName()/itemsOf()/tmdbUrl() and the directorsOwnedAtLeast↔creatorsOwnedAtLeast slot); pure.ts (the byte-identical pure helpers currently duplicated in movies/recs.ts and tv-recs/recs.ts — mulberry32, seededShuffle, stratifiedSample, normTitle, dedupeRawByTitleYear, balanceByGenre, mergeLens, topGenres, thinGenres — moved once, generic over the domain types); branch.ts (parseSuggestions, runBranch(ctx,domain,spec), collectBranchSuggestions, makeBranchJob(domain,id)); merge.ts (runMerge with the UNIFIED searchFailed+throw semantics that unit a already brought movies into parity with — the two merge stages differ only in TMDB search endpoint /search/movie vs /search/tv, year vs first_air_date_year, and the genre-id table, all of which become domain params); notify.ts (runRecsNotify with the push-ok-then-mark guard — the B11-fixed shape from T527 — plus the aligned {tmdbId,title,year,at} history from unit b, digest/report/appendHistory once). Route all Claude calls through the shared src/services/claude.ts runClaude helper (already used by movies). Then re-wire BOTH workflows to consume the shared pipeline: each keeps its own config.ts (env var names), a branches.ts shrunk to lens PROMPT TEXT + domain wiring, thin *.job.ts wrappers, and its own contracts.ts (gate contracts stay per-workflow). Movies keeps its recs-only shape (franchise-gaps already moved to missing-movies per T468) and composes its digest around runRecsNotify's rec half. Honor the model-default policy recorded by unit c in the single shared model-selection path (aligned default, or per-domain default from each config). KEEP ALL JOB NAMES STABLE (movie-snapshot, rec-*, rec-merge, movie-recs-notify, tv-snapshot, tv-rec-*, tv-rec-merge, tv-recs-notify) so the work_items ledger + recs-history continuity are preserved and dashboard/scripts/_dashboard-harness.mjs needs NO change. Add unit tests for the shared modules under src/core/recommender/ (pure.test.ts, merge.test.ts, notify.test.ts) driven by synthetic fixtures; keep the existing per-stage tests running against the thin wrappers until parity is proven, then consolidate the duplicated pure-helper tests. Update BOTH workflow CLAUDE.mds to point at src/core/recommender/ as the shared home and note what stays per-workflow (config/lens/contracts). Also update the root CLAUDE.md file map + the 'Shipped example workflows' notes if the shared-recommender home is worth indexing there (thin index only — per-workflow detail stays in the workflow CLAUDE.mds). expectsTest note: the acceptance bar includes a GOLDEN-FILE before/after test per domain — a test that runs each workflow's merge/notify path against a FIXED snapshot fixture + seeded RNG and asserts the produced recommendations.json / digest / recs-history rows are byte-identical to a captured pre-refactor baseline, proving the extraction changed no output. Owner decision (2026-07-12): ALIGN both movies and tv-recs on model claude-sonnet-5 in the shared pipeline (remove the old tv-recs claude-sonnet-4-6 default).

## Done when
src/core/recommender/ exists with types.ts + pure.ts + branch.ts + merge.ts + notify.ts, and both movies and tv-recs consume it (each retaining only config.ts + lens-prompt wiring + thin *.job.ts wrappers + own contracts.ts). A repo-wide grep finds NO duplicated pure helpers (mulberry32/seededShuffle/stratifiedSample/normTitle/dedupeRawByTitleYear/balanceByGenre/mergeLens/topGenres/thinGenres exist once, in src/core/recommender/pure.ts) and ONE implementation each of the merge and notify logic. All job names are UNCHANGED (ledger/history continuity; _dashboard-harness.mjs untouched). A golden-file test per domain proves each workflow produces byte-identical recommendations.json + digest + recs-history rows for a fixed snapshot fixture + seeded RNG, before vs after the refactor. The model default in the single shared selection path matches unit c's recorded decision. Both workflow CLAUDE.mds (and the root file map, if updated) reflect the new shared home. Definition of Done: `npx tsc --noEmit`, `npm test`, and `npm --prefix dashboard run build` (only if any dashboard file changed — expected none) all green. Both workflows use claude-sonnet-5 as the recommender model default (no claude-sonnet-4-6 reference remains).
