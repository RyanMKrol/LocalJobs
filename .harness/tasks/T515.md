## Overview
One slice of the Plex→callService migration blocked twice (T474, T499) by a pre-push git-hygiene guard trip (sensitive/gitignored path staged, no code landed). The spec was always correct; the fix is to shrink the diff per unit so staging named files is trivial. This unit is the movies workflow's single Plex read across 3 files.

## Do
In src/workflows/movies/stages/snapshot.ts, wrap the plexGet<PlexAllResponse<PlexMovieMeta>>(...) movie-library snapshot read in callService('plex', () => plexGet<...>(...)), importing callService from ../../../core/services.js. Mirror the callService('tmdb', ...) wrapping present in the sibling stage movies/stages/franchise-gaps.ts. Do not change arguments, return-value handling, or error handling beyond the wrap. Update snapshot.test.ts to account for the callService('plex', ...) wrapper (the no-limit branch passes the call through — mirror the sibling tmdb test handling). Add a one-line note to movies/CLAUDE.md that its Plex reads are metered via the shared plex service. GIT HYGIENE: stage ONLY these 3 files by explicit name — NEVER `git add -A`/`git add .`/a directory; never stage anything under data/, chrome-profile/, .env*, or a scratch DB.

## Done when
snapshot.ts's plexGet read is wrapped in callService('plex', ...); a grep for a bare plexGet( in snapshot.ts returns nothing; snapshot.test.ts passes; movies/CLAUDE.md notes the plex service; only the 3 scoped files are staged.
