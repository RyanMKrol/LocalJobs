## Overview
With the owner's chosen 'Top of mind' placement/treatment recorded (from the prior review task) and the storage/edit page already built, this task finishes the feature: keep only the winning variant on the Overview page, remove the other four plus the query-param switch, and wire it to render the REAL saved content read-only instead of dummy text.

## Do
Read the owner's chosen variant number from the prior `needs-human` review task's outcome (worklog note / recorded decision). In `dashboard/app/page.tsx`, remove all FIVE variant implementations except the chosen one, remove the `?tom=N` query-param switch entirely, and make the surviving variant unconditional (always rendered when there's content to show). Replace its dummy hardcoded markdown string with a live fetch of the real content via `api.topOfMind()` (added in the storage task, which now also returns an `expired: boolean` flag) — read-only rendering only, no edit affordance on the Overview page itself (editing stays on the dedicated `/top-of-mind` page). Per the owner's confirmed empty-state choice: **hide the section entirely** when there's nothing to show — don't render any placeholder/hint. Treat EXPIRED content exactly the same as empty for this purpose: hide the section when `expired: true` OR `content` is blank (the API's `content` field is already `''` when expired, so in practice checking 'is content non-empty' is sufficient — don't separately branch on `expired` unless you need it for something else, but don't rely on `expired` being un-set either, since the API is the source of truth for what counts as displayable). Update `dashboard/scripts/_dashboard-harness.mjs`: remove the five per-variant `PAGES`/`FLOWS` entries added by the chooser task and replace with a synthetic `GET /api/top-of-mind` fixture (non-empty, non-expired dummy content) feeding the single surviving Overview rendering, per the living-artifact rule.

## Done when
The Overview page shows exactly one 'Top of mind' section (the owner's chosen placement) when content exists and is not expired, with no leftover query-param switching code or unused variant markup. It renders the ACTUAL saved content from `GET /api/top-of-mind` (verify by setting content via the `/top-of-mind` edit page and confirming it appears on Overview on next load) as rendered markdown, read-only. The section is hidden entirely (no placeholder, no empty box) both when no content has ever been saved AND when the saved content has expired (`expired: true` from the API) — verify both cases against the synthetic fixture / a manual check. `npx tsc --noEmit`, `npm test`, and `npm --prefix dashboard run build` all pass. `node dashboard/scripts/mobile-check.mjs` and `node dashboard/scripts/visual-check.mjs` both pass; inspect the Overview screenshot and confirm the section renders correctly with real content.
