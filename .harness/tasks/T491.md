## Overview
The admin page's 'Run all workflows' button (dashboard/app/admin/page.tsx) currently requires two clicks: one to reveal an inline Confirm/Cancel panel, then a second to actually dispatch the run. Since running all workflows is explicitly described in the page copy as non-destructive, collapse this to a single button that calls confirmRunAll() (or equivalent) directly on click.

## Do
In dashboard/app/admin/page.tsx: remove the runAllConfirming state (line 21). Replace the current two-branch conditional render (the `!runAllConfirming` single-button block at lines 93-101, and the `runAllConfirming` inline yellow-bordered Confirm/Cancel panel at lines 103-122) with ONE always-rendered 'Run all workflows' button whose onClick calls confirmRunAll() directly. confirmRunAll() itself (line 44) needs no logic changes - it already calls api.runAllWorkflows(runAllLimit) and manages runAllBusy/runAllResult/runAllErr with no confirmation guard of its own; just drop its trailing `setRunAllConfirming(false)` (line 51) since the state no longer exists. Update the button to show busy-state text while runAllBusy is true (e.g. 'Starting...', mirroring the old Confirm button's busy label) and keep `disabled={runAllBusy}`. Simplify the runAllLimit number input's disabled condition (line 88) from `disabled={runAllBusy || runAllConfirming}` to `disabled={runAllBusy}`. Also update dashboard/scripts/_dashboard-harness.mjs: the 'admin-run-all-confirm' FLOWS entry (around line 773-785) clicks the button expecting an inline confirm panel to appear - since that panel no longer exists, remove this flow entry (the remaining baseline 'admin' PAGES entry still screenshots the page's default state, which now includes the single always-clickable button).

## Done when
dashboard/app/admin/page.tsx no longer references runAllConfirming anywhere; there is exactly one 'Run all workflows' button rendered unconditionally, and clicking it calls confirmRunAll() directly (dispatching api.runAllWorkflows(runAllLimit) with no intermediate panel); the runAllLimit input's disabled condition no longer includes a runAllConfirming check. dashboard/scripts/_dashboard-harness.mjs no longer contains the 'admin-run-all-confirm' FLOWS entry (or it's replaced with something meaningful for the new single-click flow, per the living-artifact rule). `npx tsc --noEmit` and `npm --prefix dashboard run build` both pass. `node dashboard/scripts/mobile-check.mjs` and `node dashboard/scripts/visual-check.mjs` both stay green and the admin page screenshot is visually inspected to confirm only a single button renders with no leftover confirm/cancel panel.
