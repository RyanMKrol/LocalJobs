## Do

Increase the size and stroke weight of the SVG gate-padlock icon rendered on workflow DAG graphs, in
`GateEdge` in `dashboard/app/components/DagFlow.tsx` (~lines 80-119). It currently sizes/weights via:

```
const lockSize = isFailed ? 16 : 12;
...
strokeWidth={isFailed ? 1.8 : 1.5}
```

Increase BOTH states (not just the non-failed one), preserving the failed state's existing relative
size advantage over the normal/passed state. Change to:

```
const lockSize = isFailed ? 20 : 16;
...
strokeWidth={isFailed ? 2.1 : 1.8}
```

The `viewBox="0 0 16 16"` and the two `<path>`/`<rect>` shape definitions (shackle arc + body) stay
unchanged — only `lockSize` (the rendered `width`/`height`) and `strokeWidth` change; increasing
`width`/`height` while keeping the same `viewBox` scales the whole icon up uniformly.

## Done when

- `lockSize` is `20` when `isFailed` and `16` otherwise (was `16`/`12`).
- `strokeWidth` is `2.1` when `isFailed` and `1.8` otherwise (was `1.8`/`1.5`).
- The `viewBox` and path/rect shape data are unchanged.
- After `npm --prefix dashboard run build`, run `node dashboard/scripts/visual-check.mjs` and look at
  a workflow-run or workflow-detail screenshot that shows a gate padlock in
  `dashboard/scripts/visual-out/` — confirm the padlock renders visibly larger/bolder than before
  for at least one non-failed gate (this is the state most people will see day-to-day; the T223
  postmortem specifically warns that a gate icon can be present in the DOM but effectively invisible
  — don't just trust the diff, look at the rendered pixels).
- `dashboard/scripts/_dashboard-harness.mjs` — confirm its fixtures already exercise a gate state (no
  new fixture is needed if a gate already appears in an existing screenshot; only touch it if no
  current fixture shows a gate at all).
- `npm test`, `npx tsc --noEmit`, and `npm --prefix dashboard run build` all pass.
