## Do

The Backlog page's **needs-human** marker (the "🔒 needs human" pill background + the card's left/
border accent) should be **blue**, but it currently shows **green**. Root cause: it's styled with
`var(--accent)` (`dashboard/app/backlog/page.tsx` ~lines 58 and 64 — `borderColor: human ?
'var(--accent)'` and the pill `background: 'var(--accent)'`), and the active **joyful theme** (T142
— e.g. Pixel Picnic has soft green/peach accents) **overrides `--accent`**, so the needs-human
marker inherits the theme's green. Make the needs-human marker a **stable blue regardless of
theme**.

- Style the needs-human pill background + the card's needs-human border with an explicit **blue**
  rather than `var(--accent)` — e.g. introduce a dedicated `--human-blue` (or `--accent-blue`)
  variable defined once as blue (the dashboard's canonical `#58a6ff`) and NOT overridden by the
  joyful theme blocks, and use it for the needs-human marker.
- Verify it renders blue under the **default** dark theme AND every **joyful** theme (pixel-picnic,
  sunny-8bit, and the T154 variants) — the themes' `--accent` overrides must not bleed into the
  needs-human marker.
- Leave the **buildable** (amber) and **done** (green) markers unchanged. Mobile-responsive.

## Done when

The needs-human backlog marker (pill + card accent) renders **blue** under the default theme and
under all joyful themes (no longer green under Pixel Picnic etc.), driven by a stable blue rather
than `var(--accent)`; the buildable/done markers are unchanged; `npm --prefix dashboard run build`
passes and `node dashboard/scripts/mobile-check.mjs` stays green.
