## Overview
The owner explicitly wants each recommender branch's Outputs to be the actual recommendations it suggested, not just a count — 'the output should be the recommendations that they are outputting.' Once recs-ledger-rebuild-a makes recordBranchLedgerRow's hook actually fire, extend it to record one row per raw suggestion instead of a single count-only row.

## Do
In each domain's recommend.ts, rewrite recordBranchLedgerRow (movies: recommend.ts:73-89; tv-recs: recommend.ts:75-91) to read the branch's written suggestions file (same existsSync/JSON.parse/`file.suggestions` array it already reads) and call markWorkItem ONCE PER raw suggestion in `file.suggestions`, instead of once for the whole branch. Each raw suggestion (RawSuggestion: `{ title, year, reason, lens }` — src/core/recommender/types.ts) has NO tmdb id yet at this stage (that's assigned later in merge.ts's verify step), so key each row by something branch-local and stable across a same-day re-run — e.g. `` `${dayKey(now)}::${index}` `` (index in the suggestions array) or a normalized-title key scoped under the branch id (the branch id IS the job_name already, so no cross-branch collision risk). Put the suggestion's own fields in `detail` (title, year, reason, lens) per root CLAUDE.md's 'every stage's success detail must describe what THAT STAGE produced' convention. Preserve a combined per-run marker too if useful for backward compatibility with any existing dashboard assumption of one row per day — but the PRIMARY output must now be the per-suggestion rows; do not leave the panel showing only a count. Keep the branch's error/skip cases (no targets, Claude failure, unparseable output — branch.ts's early-return writeBranchFile calls) recording SOMETHING sensible (e.g. zero rows, or one row noting the skip reason) rather than crashing — these are legitimate, resilient outcomes per branch.ts's own docs ('It NEVER throws on LLM trouble').

## Done when
For a branch run that writes N non-empty suggestions, the work_items ledger ends up with N rows for that branch's job name (not 1), each row's detail carrying that specific suggestion's title/year/reason/lens. A branch run that writes 0 suggestions (skip/error case) does not crash and records a sensible, non-misleading outcome (either zero rows, or a single row whose detail clearly states why nothing was suggested — do not fabricate placeholder titles). Add/extend a unit test asserting the N-rows-per-N-suggestions behavior for at least one movies branch and one tv-recs branch.
