## Overview

On the Integrations page, when the owner clicks a service to see who consumes it, also show WHERE
that service's rate/quota numbers came from — documented, empirically observed, or a guess — using
the `rate_limit_source` field the dependent plumbing task adds to every service row.

## Context (confirmed current)

- `dashboard/app/integrations/page.tsx` already has a click-to-open interaction on each service row:
  clicking the service-name button (`onClick={() => setViewingConsumers(s.name)}`, ~line 177) opens
  `ConsumersPanel` (defined ~lines 33-77), a modal titled "Consumers of {name}" that fetches
  `GET /api/services/:name/consumers` and lists which workflows/jobs call it. This is the natural
  place to add the provenance info, since it's already the click-triggered surface for "tell me more
  about this service."
- `ConsumersPanel` currently takes only `{ name, onClose }` — it does NOT have access to the full
  `Service` object (description, `rate_limit_source`, etc.), only the consumers list it fetches
  itself. The parent `Services` component already holds the full list in `allServices` (from
  `usePoll(() => api.services(), 3000)`).
- The dependent task (already built by the time you pick this up) adds `rate_limit_source: string` to
  the `Service` type in `dashboard/app/lib/api.ts` and populates it for every current service via
  `GET /api/services`.
- `dashboard/scripts/_dashboard-harness.mjs` already has a `service()` fixture helper (~line 71-75)
  used by both `mobile-check.mjs` and `visual-check.mjs`, and a FLOWS entry named
  `service-consumers-modal` (~line 909-921) that clicks the `google-places` service button and
  screenshots the resulting modal — this existing flow will automatically capture your change once
  the fixture data includes a `rate_limit_source` value, but the fixture itself needs updating (see
  Do below) and its comment needs to stop saying only "Consumers of …".

## Do

### 1. Pass the full service through to the modal

Change `setViewingConsumers` (and the `ConsumersPanel` props) so the modal has access to the matching
`Service` object's `rate_limit_source` (and `description`, if useful) — either by storing the whole
`Service` object instead of just its name, or by looking it up from `allServices` by name inside the
render (simplest: keep storing the name, and have the parent find `allServices.find(s => s.name ===
viewingConsumers)` and pass that object into `ConsumersPanel` as a new `service` prop alongside `name`
and `onClose`).

### 2. Render a "Rate limit provenance" section in the modal

Inside `ConsumersPanel` (or rename it if you prefer, e.g. to reflect it's no longer JUST a consumers
view — your call, keep it simple), add a section above or below the existing "Consumers of {name}"
content:
- A heading, e.g. **"Rate limit provenance"**.
- The `rate_limit_source` text rendered as a paragraph (plain text is fine — the strings the
  plumbing task writes are prose, may include a bare `https://...` URL inline as visible text; you
  do not need to build a full markdown/link-detection renderer for this, a plain `<p>` is sufficient
  unless you judge linkifying is easy and worth it).
- A graceful fallback (e.g. muted "not documented yet") for the rare case a service has an empty
  `rate_limit_source` (shouldn't happen once the plumbing task lands for all current services, but
  don't crash on it).
- Keep the existing Consumers section and its "No consumers recorded yet…" empty state unchanged.

Reuse the existing `.db-modal-overlay`/`.db-modal` chrome (no new CSS system) — a couple of inline
styles or a small addition to `globals.css` for spacing between the two sections is fine, matching
this file's existing inline-style idiom.

### 3. Update the living-artifact fixture (non-negotiable per this repo's UI convention)

In `dashboard/scripts/_dashboard-harness.mjs`:
- Add a `rate_limit_source` value to the `service()` fixture helper (~line 71-75) — use something
  realistic and long enough to prove wrapping/spacing works (reuse the file's `LONG` constant the way
  `description` already does), e.g.
  `rate_limit_source: 'Documented at https://developers.google.com/maps/documentation/places/web-service/usage-and-billing — ' + LONG`.
- Update the `service-consumers-modal` FLOWS entry's comment (~line 909-912) since the modal now shows
  more than just consumers — briefly note it also verifies the rate-limit provenance section renders.
  The existing click/wait actions don't need to change (same button, same `.db-modal` wait) since this
  is the same interaction surface, just more content in it.

## Done when

- Clicking a service name on `/integrations` opens a modal that shows BOTH a "Rate limit provenance"
  section (the service's `rate_limit_source` text) and the existing Consumers list.
- `dashboard/scripts/_dashboard-harness.mjs`'s `service()` fixture and the `service-consumers-modal`
  FLOWS comment are updated to reflect the new content.
- `npm --prefix dashboard run build` succeeds.
- `node dashboard/scripts/mobile-check.mjs` passes (no horizontal overflow at phone width, including
  with the modal open).
- `node dashboard/scripts/visual-check.mjs` runs clean AND you have actually LOOKED at the
  `service-consumers-modal` screenshot in `dashboard/scripts/visual-out/` and confirmed the "Rate
  limit provenance" section is visibly painted with real text, not blank/clipped/overlapping —
  record what you saw in the task's worklog entry.
