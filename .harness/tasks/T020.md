## Do

Pipelines are the unit of work; jobs should only be viewed via their pipeline. Drop the /jobs tab + flat top-level job list, but keep per-job run/log drill-down reachable from the pipeline DAG nodes (standalone jobs as a section there).

## Done when

The /jobs flat tab + list is removed; per-job run/log drill-down is still reachable from pipeline DAG nodes; the dashboard build passes with no dangling links to the removed tab.
