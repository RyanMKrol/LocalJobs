## Do

Align the trailing status pills/buttons on the **Backlog** done rows into tidy, fixed columns so they
line up vertically across rows. In `CollapsibleRow` in `dashboard/app/backlog/page.tsx`, the `isDone`
block renders, inline in a `.row` with `gap` + `flexWrap`: a reviewed-status pill
(`👁 reviewed` / `not reviewed`), then a state pill (`✓ done` or the red `✗ failed`), then an action
button (`Mark failed` / `Undo fail`). Because `✗ failed` is wider than `✓ done` and `Undo fail`
differs in width from `Mark failed`, the slots jitter row-to-row. Give the trailing cluster a fixed
column treatment (e.g. CSS grid with set column widths, or a min-width per pill/button slot) so the
three slots — reviewed-status, done/failed-status, action — are a consistent width and align across
all done rows regardless of label text. Styles live in `dashboard/app/globals.css` (`.pill.*`,
`.review-toggle`).

## Done when

- On the Backlog "Done" section, the reviewed pill, the done/failed pill, and the action button line
  up in consistent vertical columns across every done row (a `✗ failed` + `Undo fail` row aligns with
  a `✓ done` + `Mark failed` row).
- It stays mobile-responsive (no new phone-width overflow; the `@media (max-width: 640px)` block) and
  theme-variable-driven.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build and the `backlog-expanded.png`
  screenshot in `dashboard/scripts/visual-out/` (the flow that expands the Done section, showing both
  a done and a failed row) confirms the pills/buttons are aligned in columns; the harness
  (`dashboard/scripts/_dashboard-harness.mjs`) is updated if the UI surface changed.
