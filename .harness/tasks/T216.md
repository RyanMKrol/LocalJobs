## Do

Add the **8 recommender branch stages** to the `tv-recommendations` workflow and wire them into the
manifest. Each is a thin `*.job.ts` that delegates to the shared `recommend.ts` + a branch definition
from `branches.ts` (both built in T215), mirroring the movies `rec-*.job.ts` files in
`src/jobs/movies/stages/`.

Create under `src/jobs/tv-recs/stages/` (default-exporting a `JobDefinition` each, unique job names):
`tv-rec-random-1`, `tv-rec-random-2`, `tv-rec-random-3`, `tv-rec-creator`, `tv-rec-canon`,
`tv-rec-thin-genre`, `tv-rec-older-era`, `tv-rec-world`. Each reads the snapshot + taste profile from
T214, runs its branch via `recommend.ts`, and writes its raw suggestions to a per-branch file under
`data/out/recs/` (mirror the movies branch outputs). Report per-item progress + log verbosely.

Then update `src/jobs/tv-recs/tv-recs.workflow.ts` to add all 8 branches as members, each
`dependsOn: ['tv-snapshot']`, so they fan out from the snapshot (capped at `maxConcurrency: 4`) —
exactly like the movies manifest. The registry must still load cleanly (every job belongs to the
workflow; the workflow references only jobs that now exist).

## Done when

- All 8 `tv-rec-*.job.ts` stages exist under `src/jobs/tv-recs/stages/`, each delegating to the shared
  `recommend.ts`/`branches.ts` (no duplicated recommender logic), and each writes its branch
  suggestions to `data/out/recs/`.
- `tv-recs.workflow.ts` lists `tv-snapshot` + the 8 branches (each `dependsOn: ['tv-snapshot']`); the
  registry loads with no orphan-job / missing-job error and the daemon starts.
- Verify against the live Claude CLI with the SMALLEST sample: run ONE branch job once and confirm it
  produces parsed suggestions without exceeding the `claude-cli` monthly quota (the `live-api` verify).
- `npm test` and `npx tsc --noEmit` are green.
