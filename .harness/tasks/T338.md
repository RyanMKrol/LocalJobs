## Do

Extend the `stock-digest` workflow (built in stock-portfolio-digest-1) with a sector/diversification breakdown section in the weekly report, per the owner's explicit request for ALL THREE content types (holdings read, performance/movers, AND sector breakdown). Trading212's portfolio endpoint does not return sector/industry data, so this requires a new external read-only data source.

Chosen data source (a concrete design decision — use it, don't re-litigate): **Finnhub** (`https://finnhub.io`), specifically its free `GET /stock/profile2?symbol=<TICKER>&token=<KEY>` company-profile endpoint, which returns a `finnhubIndustry` field usable as this workflow's sector/industry grouping (be honest in the UI/report copy that this is Finnhub's industry classification, not a formal GICS sector, if there's a meaningful difference worth noting — don't overclaim precision). Finnhub's free tier is generously rate-limited (documented ~60 calls/minute), comfortably covering a weekly lookup over a personal portfolio's ticker count (read-only, GET-only — no mutating calls, consistent with this repo's broker/data-API conventions even though Finnhub is metadata not a broker).

Implementation:
- `src/services/finnhub.service.ts` — a new, self-contained, top-level `ServiceDefinition` (sibling of `trading212.service.ts`/`google-places.service.ts`), reading `FINNHUB_API_KEY` from env, with sensible env-overridable `ratePerMinute`/`dailyCap`/`monthlyCap` defaults appropriate for a free-tier metadata API (generous — this is a cheap, low-volume lookup, not a paid-spend-governed call). GET-only. Add `FINNHUB_API_KEY` (placeholder) to `.env.example` with a short comment on what it's for and a link/name of the free-tier signup (Finnhub).
- `src/jobs/stock-digest/stages/stock-sector-lookup.job.ts` + `stock-sector-lookup.ts` (+ `.test.ts`) — a new stage, added to `stock-digest.workflow.ts` as `{ job: 'stock-sector-lookup' }` with `stock-digest-build` updated to `{ job: 'stock-digest-build', dependsOn: ['stock-sector-lookup'] }` (sector data must be available before the report is narrated). For each distinct ticker currently held (read `stocksSyncConfig.portfolioJsonPath`, same cross-workflow read pattern as task 1), call the Finnhub service via `callService('finnhub', ...)` to resolve its industry, and write the ticker→industry map to `data/out/sectors.json`.
- Idempotency: a 'resolve once, skip if already done' pattern keyed by ticker in the `work_items` ledger (mirrors a resolver stage like places' CID resolver) — a ticker already recorded `success` is NOT re-looked-up on subsequent runs (industry classification rarely changes; this also conserves the free-tier quota). New tickers appearing in the portfolio are looked up fresh. This stage is idempotent/skippable per the standard ledger convention — no special expiry logic needed for a first version.
- Update `stock-digest-build.ts` to read `sectors.json` (if present) and, when data exists for at least one held ticker, add a diversification section to the structured summary fed to `runClaude` — % of total portfolio value grouped by industry/sector. If `sectors.json` is missing or a given ticker has no resolved sector (lookup failed / new position), the report should degrade gracefully (omit that ticker from the breakdown, or omit the whole section if no sectors are known at all) rather than fail the run.
- Be verbose with `ctx.log()` per the repo-wide logging convention (each ticker looked up, industry returned, cache hits skipped, degraded-report cases explained).

Docs (Definition of Done, part of this same task): update the `stock-digest` section added in task 1 (root `CLAUDE.md` + `README.md`) to describe the two-stage DAG (`stock-sector-lookup` → `stock-digest-build`), the new `finnhub` service + its `FINNHUB_API_KEY` credential, and the sector/diversification section of the report. Add `finnhub` to CLAUDE.md's services list alongside gemini/google-places/fragrantica/claude-cli/trading212 if such a list exists (check `src/services/CLAUDE.md` too, per its own conventions for documenting read-only external APIs).

## Done when

`src/services/finnhub.service.ts` exists as a self-contained `ServiceDefinition` reading `FINNHUB_API_KEY` from env with rate/quota limits; `.env.example` documents `FINNHUB_API_KEY`. `src/jobs/stock-digest/stages/stock-sector-lookup.job.ts` resolves each currently-held ticker's industry via `callService('finnhub', ...)`, is idempotent per ticker via the `work_items` ledger (skips already-resolved tickers), and writes `data/out/sectors.json`. `stock-digest.workflow.ts` is updated so `stock-digest-build` depends on `stock-sector-lookup`. `stock-digest-build`'s narrated report includes a sector/diversification breakdown (% of portfolio value per industry) when sector data is available, and degrades gracefully (no crash, sensible omission) when it is not. `npx tsc --noEmit` and `npm test` (including new unit tests for the sector-lookup stage's idempotency + the diversification % math) pass. Root `CLAUDE.md`, `README.md`, and (if applicable) `src/services/CLAUDE.md` are updated to describe the new `finnhub` service and the two-stage `stock-digest` DAG.
