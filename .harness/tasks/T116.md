## Do

On a workflow run, the output column of the input-to-output mapping (the OutputCell from T110) currently renders an inline preview (📄 title · excerpt · file), and it's unclear what it should look like. Produce FIVE distinct presentations of the output LINE ITEM and let the user toggle between them live to choose a favourite (persist the choice, e.g. localStorage, with a small style selector — same chooser pattern as T99/T113). Use best judgement; suggested directions (mix/refine): (1) FILENAME ONLY + a 'Preview' button that opens the popover; (2) a file icon + the document title (no excerpt), whole row clickable to preview; (3) title + short excerpt (the current style); (4) filename as a link plus small metadata (size/date); (5) a compact chip (file icon + filename) that shows the excerpt on hover and opens the full popover on click. EVERY style must keep the click-to-preview popover working (the T110 popover / T115 markdown render), handle the no-artifact fallback gracefully, work for the perfumes + places workflows, and stay mobile-responsive. This is an EVALUATION aid — note that once the user picks a winner a FOLLOW-UP task will hardcode it and remove the toggle + unused styles.

## Done when

On a workflow-run page the user can switch between five distinct output line-item styles via a persisted toggle; each keeps the click-to-preview popover working and handles the no-output fallback; it works for the perfumes and places workflows and stays mobile-responsive; dashboard build passes and mobile-check stays green.
