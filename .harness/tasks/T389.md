## Overview

`StageIoPanel` has now replaced the old joined `IoPanel` as the default for every workflow (T386-T388, all verified). This task removes the now-dead old mechanism entirely: the `IoPanel` React component, its backing `workItemIoRows` store function, and the `GET /api/workflow-runs/:id/io` endpoint — along with the "one output shown per input, fan-out collapsed to its first output" caveat that was the whole reason this replacement effort started. Because this removes shared framework code, a separate human-review task (T405, `dependsOn: ["T389"]`) exists to sign off on the deletion after this task lands — build this task normally, you do not need to wait for or perform that review yourself.

## Do

**Verify nothing else references what you're about to delete, BEFORE deleting anything:**
- `grep -rn "workItemIoRows\|IoPanel\b\|WorkflowIo\b" src/ dashboard/app/` (excluding the definitions themselves and their own tests) — confirm the ONLY remaining references are inside the files you're about to remove/edit. If anything else depends on `workItemIoRows` or the `/io` endpoint (a script, a doc example treated as a contract, another page), STOP and report it instead of deleting — do not silently break another consumer.

**If clear, remove:**
- `IoPanel` (the component, and its helper functions `itemLabel`, `priceDetail`, `formatPriceDetail`, `OutputCell`, `rowEffectiveState`, `sortIoRows`, `IO_FILTER_STATES`, `IoSortCol` — check which are ONLY used by `IoPanel` vs. still needed elsewhere first) from `dashboard/app/workflow-runs/[id]/page.tsx`.
- `workItemIoRows` and its private helpers (`toIoRow`, the `LedgerRow` type if unused elsewhere) from `src/db/store.ts`, plus its test coverage in `src/db/store.test.ts` (the T139 run-scoped IO test block).
- The `GET /api/workflow-runs/:id/io` handler from `src/api/server.ts`, plus its tests in `src/api/server.test.ts`.
- The `IoRow`/`WorkflowIo` types and `workflowRunIo` client function from `dashboard/app/lib/api.ts`, IF nothing else imports them.
- The `.io-*` CSS classes in `dashboard/app/globals.css` that were ONLY used by the old joined table (`.io-table-scroll`, `.io-filter-bar`, `.io-filter-chip*`, `.io-state-arrow`, `.io-footnote`) — do NOT remove `.io-job-filter-bar`/`.io-job-filter-chip`/`.io-empty-state`, which `StageIoPanel` still uses.
- The `io-panel-job-scoped`/`io-panel-sorted-by-state` FLOWS entries and any now-dead fixture-only-used-by-the-old-panel code in `dashboard/scripts/_dashboard-harness.mjs` (`workflowIo`, `workflowIoSkipped`, `workflowIoScopedToEnrich`, `workflowIoStocks`, and the `/io`-suffix routing branch in `fixtureFor`) — but only the parts genuinely unused after deletion; some of those fixture OBJECTS (e.g. `stocksMembers`) may still be shared with the `/stage-io` fixtures added in earlier tasks, so check before deleting a shared const.

## Done when

- `grep -rn "workItemIoRows\|IoPanel\b\|GET /api/workflow-runs/:id/io\b" src/ dashboard/app/` returns nothing (or only comments/docs explicitly noting the removal, e.g. an updated root `CLAUDE.md` line).
- `npx tsc --noEmit`, `npm test`, and `npm --prefix dashboard run build` all pass.
- `node dashboard/scripts/mobile-check.mjs` and `node dashboard/scripts/visual-check.mjs` both pass — look at the screenshots one more time to confirm every workflow's run page still renders correctly with the old code physically gone (not just unreferenced).
- Root `CLAUDE.md`'s "Shared dashboard UI components" section and the `stock-digest.workflow.ts` file comment (which currently describe `StageIoPanel` as an ALTERNATIVE to the generic `IoPanel`) are updated to reflect that it is now the ONLY panel, not an alternative.
- Leave a clear one-paragraph summary in the worklog of exactly what was deleted and the grep command output proving nothing else referenced it — T405 (the follow-up human review task) reads this worklog rather than re-deriving it from scratch.
