## Overview
Across the T415-era stocks-sync and stock-digest stages, log messages bake 'info:'/'warn:' prefixes into their text (sometimes 'warn:' text logged at info level so the dashboard can't colour it), and stocks-watch's per-run check ledger rows have no name/ticker so the IO panel shows them blank. Sweep out the prefixes + pass the real level arg, and give the ledger rows a label.

## Do
(A) In every stage of src/workflows/stocks-sync/stages/ and src/workflows/stock-digest/stages/, remove the literal 'info:'/'warn:'/'error:' prefixes from ctx.log message strings and pass the correct level arg to ctx.log for any line that means a non-info level (info is the default). Explicitly fix stocks-resolve-names.ts:82 and stock-portfolio-snapshot.ts:106, which log 'warn:'-prefixed text at the default info level with no level arg — they should log at 'warn'. (B) In src/workflows/stocks-sync/stages/stocks-watch.ts, the per-run position-check markWorkItem (~lines 136-138) records detail { gainPct, breaching } with no label — add a name/ticker so the row isn't blank in the IO panel, e.g. detail { name: label, ticker: position.ticker, account: position.account, gainPct: gain, breaching: gain >= BREACH_THRESHOLD_PCT }, matching the shape the nearby reset-row markWorkItem already uses. No behaviour, schema, or ledger-key change — purely the detail blob + log formatting.

## Done when
No ctx.log call in either workflow's stages carries a baked-in 'info:'/'warn:'/'error:' text prefix, and every intended warn/error line passes the matching level arg (grep confirms). stocks-watch's per-run check ledger rows carry a ticker/name in detail; a stocks-watch.test.ts assertion confirms the label is present. Existing tests green; npx tsc --noEmit and npm test pass.
