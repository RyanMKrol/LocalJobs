## Do

On the Backlog page (`dashboard/app/backlog/page.tsx`), the three sections — **🤖 Harness-buildable**,
**🔒 Needs a human**, **✅ Done** — are each collapsible, but currently ALL THREE default to
**collapsed/closed**. Change the per-section default so:

- **Harness-buildable** → **open / expanded** by default.
- **Needs a human** → **open / expanded** by default.
- **Done** → **collapsed** by default (history you open on demand).

The buildable + needs-human lists are the active work you want to see immediately on load, so they
should be expanded; only Done starts closed.

- Set the per-section default open-state accordingly (it's a per-section collapse default in the
  page state). The section headers still toggle as they do now.
- This is consistent with T159 (which turns those two sections' rows into compact click-to-expand
  rows): the SECTIONS default open; the individual rows within them can still be compact. If T159
  has already landed, just adjust the section defaults; if not, this stands alone.
- Mobile-responsive.

## Done when

On the Backlog page the Harness-buildable and Needs-a-human sections are expanded by default and the
Done section is collapsed by default; all three section headers still toggle open/closed; `npm
--prefix dashboard run build` passes and `node dashboard/scripts/mobile-check.mjs` stays green.
