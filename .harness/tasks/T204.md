## Do

Rebuild the workflow graph view so it accurately represents the real DAG — both true
dependencies/concurrency AND wide-graph layout — replacing the hand-rolled wave layout in
`dashboard/app/components/Dag.tsx`. Two problems to fix together (they share one root cause: the
hand-rolled layout):

1. **False wave barriers.** The current wave-column layout implies a whole wave must finish before the
   next starts, which is wrong. Example (movie-recommendations): `franchise-gaps` depends ONLY on
   `movie-snapshot`, while `rec-merge` depends on the 8 `rec-*` branches — so `rec-merge` can start
   while `franchise-gaps` is still running. The graph must reflect each stage's ACTUAL `dependsOn`
   edges and genuine concurrency, not artificial per-wave barriers — i.e. draw real edges between a
   node and its actual prerequisites, so a viewer can see what truly gates what.
2. **Wide-graph layout.** Graphs wider than their container currently read poorly (the T181
   boustrophedon "snake" attempt didn't land well — see the missing-tv-seasons graph at wide desktop
   width). The rebuilt layout must handle wide graphs legibly on desktop. (The phone layout already
   stacks top-down into a single column and is fine — keep that sensible.)

You MAY adopt a graph/DAG-layout library (e.g. a React flow/elk/dagre-style layout) instead of the
hand-rolled approach if it produces a clearer result — a dashboard-side npm dependency is acceptable
here (this is a UI concern; it does NOT violate the repo's "no Docker/db/queues" rule). Whatever the
approach: preserve everything the current graph carries — node status colouring, links from each node
to its job/run page, and the gate marks on the connecting edges (run-view gates link to the run-scoped
gate page; definition-view gates link to the run-agnostic page) — and keep it working in all theme
families × light/dark, and mobile-responsive.

Update CLAUDE.md's graph/Dag notes (and remove/replace the T181 snake description) to match the new
approach.

## Done when

- The workflow graph draws each stage's real `dependsOn` edges and shows genuine concurrency — a stage
  whose prerequisites are met is visually independent of unrelated still-running stages in the "same
  wave" (verify with movie-recommendations: `franchise-gaps` and `rec-merge` are not implied to be
  ordered by a wave barrier).
- Wide graphs (e.g. a long pipeline, and the missing-tv-seasons graph at wide desktop width) render
  legibly; the phone top-down single-column layout still works.
- Node status colouring, node→job/run links, and gate marks on edges (linking to the correct gate
  pages) are all preserved; it looks correct across all theme families in light + dark.
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green; CLAUDE.md graph notes updated. If a layout
  library is added, it's a dashboard dependency only.
