## Do

On the Services page (/services) 'Edit limits' form the three input placeholders are inconsistent: the Rate/min input uses placeholder="none" while Rate/day and Rate/month both use placeholder="no cap" (page.tsx lines ~101/105/109). Make the three placeholders use consistent wording so a blank field reads the same way across all three (e.g. all 'no limit'); keep the existing blank-input-means-null behaviour and the per-field ✕ 'set to no limit/no cap' clear buttons working.

## Done when

On the Services edit-limits form, the Rate/min, Rate/day and Rate/month inputs show consistent placeholder text for the empty (no-limit) state; blank still maps to null and the ✕ clear buttons still work; dashboard build passes and the mobile-check stays green.
