## Do

Fix the styling of the **Input → Output mapping** section on the workflow-run detail page
(`dashboard/app/workflow-runs/[id]/page.tsx` + its CSS in `dashboard/app/globals.css`). Two issues
(see screenshot):

1. **Inconsistent corners.** The bordered table container has mismatched corner radii — the top
   corners are rounded but the bottom corners (and/or the table inside) don't follow, so the rounded
   border is squared off at the bottom and the corners don't line up. Make the radius **consistent on
   all four corners**, and ensure the table CLIPS to the rounded container (e.g. `overflow: hidden`
   on the bordered wrapper, or matching radii on the first/last header+row cells) so the inner table
   edges don't poke past the rounded border. The inner box's radius should also sit consistently
   within the outer section panel.
2. **Footnote hugging the sides.** The caption at the bottom — *"One output is shown per input
   (fan-out is collapsed to its first output)."* — is flush against the left/right edges with no
   horizontal padding, unlike the rest of the section. Give it the **same horizontal inset/padding**
   as the section's other content so it lines up with the table rather than touching the panel edge.

Keep it consistent across the default + joyful themes (the radii/padding should read correctly under
each), and **mobile-responsive** at ~402px (no overflow; the table still scrolls inside its
container per the existing behaviour).

## Done when

The Input → Output mapping panel has consistent corner radii (the bordered table clips cleanly to
its rounded container, no squared-off/mismatched corners) and the bottom footnote is inset to match
the rest of the section instead of hugging the edges; it looks correct under the default + joyful
themes and at phone width; `npm --prefix dashboard run build` passes and
`node dashboard/scripts/mobile-check.mjs` stays green.
