## Do

Both packages are pinned at 0.1. Bump the root and dashboard package.json version fields to 1.0.0.

## Done when

Both package.json version fields read 1.0.0; typecheck and the dashboard build pass.
