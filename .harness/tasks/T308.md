## Do

Hardcode the dashboard's appearance to the `sunny-8bit` theme family + `baloo` font, and replace
the theme picker (`ThemeControls` in `dashboard/app/ui.tsx`, rendered from
`dashboard/app/layout.tsx`) with a single sun/moon icon button that toggles light/dark mode
directly — no popover/modal, no theme-family or font choice surfaced anywhere.

Concretely:
- `dashboard/app/globals.css`: make the sunny-8bit dark palette (currently under the
  `html[data-theme="sunny-8bit"]` selector, ~line 669) the base `:root` palette, and its light
  override (currently `html[data-theme="sunny-8bit"][data-mode="light"]`, ~line 679) becomes a
  plain `[data-mode="light"]` rule — no `data-theme` attribute is written or read anywhere after
  this task. Leave the `pixel-picnic`/`default` family CSS blocks and `data-font`/`data-motion`
  rules in place for now (untouched, just unreachable) — deleting that dead CSS is a separate
  follow-up task (T309, which depends on this one). Don't delete anything beyond what's listed
  here; the goal is "one look, ships correctly" first.
- `dashboard/app/layout.tsx`: stop loading `Space_Mono` (keep only `Baloo_2`). The pre-paint
  inline script (`PREPAINT`) should drop its `data-theme`/`data-font`/`data-motion` lines
  entirely and keep only the `data-mode` line, still deriving from `localjobs.mode` in
  localStorage, falling back to `prefers-color-scheme` when unset (same fallback as today).
- `dashboard/app/ui.tsx`: replace the whole `ThemeControls` component (and its
  `useTheme`/`useFont` calls) with a small icon-button component (keep the name `ThemeControls`
  or rename — your call) that renders ONE clickable sun/moon icon, no modal. It reads/writes the
  SAME `localjobs.mode` localStorage key via the existing `useMode`-style mechanism, but
  simplified to strictly BINARY: clicking flips light↔dark and persists an explicit choice — there
  is no "System" option to pick or revert to once toggled (mirrors how an explicit Light/Dark
  choice already behaves today; only remove the third "System" menu entry, don't add a way back to
  it). The untouched/first-load state (before any click) still derives from
  `prefers-color-scheme`, exactly like today's default. You may keep `resolveMode`-equivalent
  pure logic if useful for a test, but the FONTS/THEMES arrays, `ThemeId`/`FontId` types, and
  `useTheme`/`useFont`/`useMotion` hooks becoming unused is expected — leave them in place
  (unused) for T309 to remove; do not import them from the new toggle component.
- `dashboard/scripts/_dashboard-harness.mjs`: the existing `overview-theme-picker` FLOWS entry
  opens `.theme-trigger` → `.theme-modal`, both of which no longer exist. Replace it with a FLOWS
  entry that clicks the new toggle button and captures the resulting (opposite-of-default) mode —
  e.g. `overview-theme-toggle` clicking the new toggle control and waiting for the `data-mode`
  attribute (or an equivalent visible signal) to flip before the screenshot. Leave `seedTheme`
  as-is for now (T309 will trim its now-unused `theme`/`font`/`motion` params).

## Done when

- `npx tsc --noEmit` and `npm test` pass; `npm --prefix dashboard run build` succeeds.
- Loading the dashboard with no localStorage set renders the sunny-8bit look + Baloo 2 font, in
  dark or light mode matching the OS `prefers-color-scheme` (no flash of a different theme/font on
  load).
- The header shows exactly one sun/moon toggle button (no picker popover, no Theme/Font/Mode-list/
  motion sections anywhere) that flips light↔dark on click, persists the explicit choice under
  `localjobs.mode` in localStorage, and survives a reload.
- `dashboard/scripts/_dashboard-harness.mjs`'s FLOWS list has a working entry exercising the
  toggled (non-default) mode (replacing the removed `overview-theme-picker` entry).
- Run `node dashboard/scripts/visual-check.mjs` and actually LOOK at the screenshots for the
  default-load state AND the new toggled-mode FLOWS entry in
  `dashboard/scripts/visual-out/` — confirm sunny-8bit+baloo renders correctly in both, and the
  sun/moon icon visibly reflects the current mode (not just present in the DOM — T223's lesson).
- Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green.
