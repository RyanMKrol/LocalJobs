## Overview
The owner explicitly wants the merge stage's Outputs to be the actual final, verified, balanced recommendations it produced — 'the output should be the things that it's recommending' — not the current single bulk marker.

## Do
In src/core/recommender/merge.ts's runMerge (shared by both movies' rec-merge and tv-recs' tv-rec-merge, confirmed by the single markWorkItem call site), the current terminal write is a single bulk row: `` markWorkItem(domain.mergeJob-equivalent-jobname, dayKey(now), 'success', { detail: { balanced: N, path, format: 'json' } }) `` at merge.ts (the terminal ctx.log/write block, around lines 352-372 — confirm the exact markWorkItem call line since the idea text describes it at merge.ts:73 which does not match a bulk-marker call at this reading; re-verify against the CURRENT file before editing, the function may have moved). Note: on this reading of merge.ts (lines 1-377), NO markWorkItem call currently exists in runMerge at all — merge.ts writes `recommendations.json` via writeJsonFile (line 354) and logs a summary, but records NO work_items row of any kind for the merge stage today. Confirm this against the live file first; if there genuinely is no existing bulk marker, this unit is simply 'ADD one row per final recommendation' rather than 'convert bulk to per-item.' Either way: after the `balanced` list is finalized (merge.ts:352-353, `const verified = [...byTmdb.values()]; const out: RecommendationsFile = {...}`), call markWorkItem ONCE PER item in `balanced`, keyed by the recommendation's own tmdbId (reuse `recKey(tmdbId)` from src/core/recommender/pure.ts, the same key notify.ts already uses at notify.ts:74 `isWorkItemDone(domain.recsJob, recKey(r.tmdbId), 1)` — but use the MERGE stage's own job name, not domain.recsJob, to avoid colliding with notify's separate 'notified' ledger keyspace described in recs-ledger-rebuild-e below), with detail carrying the recommendation's own fields (title, year, genre, reason, lens, tmdbRating) per root CLAUDE.md's per-stage detail convention. The merge stage's own DAG job name is 'rec-merge' (movies) / 'tv-rec-merge' (tv-recs) — confirm these exact strings from movies.workflow.ts / tv-recs.workflow.ts before using them as the markWorkItem job_name argument (do NOT reuse domain.recsJob, which is a DIFFERENT keyspace scoped by tmdbId at the recommendation-notification level, not the merge level — recs-ledger-rebuild-e's job-name-mismatch fix concerns THAT keyspace, this unit concerns the merge stage's own).

## Done when
After a merge run producing K balanced recommendations, work_items has K rows for the merge stage's own job name ('rec-merge' or 'tv-rec-merge'), each keyed by that recommendation's tmdbId, with detail describing that specific recommendation (title/year/genre/reason/lens/rating). A re-run on the same day with the same balanced set upserts the same K rows (idempotent), not duplicates. Add/extend a unit test in merge.test.ts asserting K work_items rows exist after a run with a known balanced-list fixture.
