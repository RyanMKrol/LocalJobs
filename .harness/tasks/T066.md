## Do

On a workflow-run page, the DAG graph (components/Dag.tsx — left-to-right waves) looks misaligned on narrow/mobile widths: the vertical centring of nodes/waves reads as visually off as the width shrinks. Make it responsive — EITHER fix the vertical alignment of the left-to-right layout at narrow widths, OR switch to a TOP-DOWN (vertical) layout below a small-screen breakpoint (lean: top-down likely reads better on a phone since multi-node waves do not fit horizontally). The agent picks whichever looks best. Validate visually: render the page at an iPhone-class viewport (~402px) AND at desktop width and confirm the graph is cleanly aligned + legible at both (reuse the T040 viewport-screenshot approach; screenshot-verifiable, not needs-human).

## Done when

The workflow-run DAG renders cleanly aligned + legible at a narrow/mobile viewport (~402px) and at desktop width, with no awkward vertical-centring misalignment; validated via viewport screenshots at both widths recorded in the worklog; dashboard build passes.
