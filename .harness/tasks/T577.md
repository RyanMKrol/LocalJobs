## Overview
The Plex->callService migration (T464->T474->T499, decomposed into per-workflow units T511-T516) was authored as covering '6 workflows', but there are SEVEN Plex-touching workflows. missing-movies (split out of movie-recommendations in T468 as the 7th) was never given a migration unit, so its snapshot stage's Plex read is still a bare plexGet call. This is the missing seventh unit that unblocks the T517 verification gate. Its franchise-gaps stage already routes tmdbGet through callService('tmdb', ...); only the snapshot's plexGet is un-wrapped.

## Do
Wrap the single bare Plex read in src/workflows/missing-movies/stages/snapshot.ts (currently `const resp = await plexGet<PlexAllResponse<PlexMovieMeta>>(...)` at ~line 29) in `callService('plex', () => plexGet<...>(...))`, exactly matching how the six sibling snapshot stages already do it (see src/workflows/movies/stages/snapshot.ts for the verbatim pattern — same file was split from this one in T468). Import callService from '../../../core/services.js'. Add/extend a unit test in snapshot.test.ts that asserts the plexGet call is routed through callService('plex', ...) (mirror src/workflows/movies/stages/snapshot.test.ts's 'verifies that callService("plex", ...) is used to wrap plexGet calls' test). Update src/workflows/missing-movies/CLAUDE.md to note the snapshot stage now routes its Plex read through callService('plex', ...) (matching the note each of the six sibling workflows' own CLAUDE.md files got). Do NOT touch any other stage or file. GIT HYGIENE: stage only the three scope files by explicit name — never `git add -A`/`git add .`.

## Done when
src/workflows/missing-movies/stages/snapshot.ts's Plex read is wrapped in callService('plex', () => plexGet(...)) identical in shape to the six sibling snapshot stages; a snapshot.test.ts test asserts the plexGet call routes through callService('plex', ...) and passes; missing-movies/CLAUDE.md notes the callService('plex') routing. `npx tsc --noEmit` and `npm test` both green.
