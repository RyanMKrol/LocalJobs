## Do

Migrate the **Backlog** page (`dashboard/app/backlog/page.tsx`) to use the shared UI component library created in T268 — replace its
ad-hoc Run button / pills / action buttons / panels / modal-popover chrome with the shared components
(`<RunButton>`, `<Pill>`, etc.) wherever they apply on this page. This is a **like-for-like swap**:
do NOT change the rendered appearance or behaviour. The shared components wrap the existing
CSS-variable class idiom, so the look stays identical and theme-variable-driven.

## Done when

- `dashboard/app/backlog/page.tsx` uses the shared components from `dashboard/app/components/` (per T268) instead of
  re-implementing run buttons / pills / etc.; the page renders and behaves **identically** (no visual
  or behavioural change, including any existing interactions).
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build and confirms the page is
  unchanged (the `backlog.png` + `backlog-expanded.png` screenshots). Update `dashboard/scripts/_dashboard-harness.mjs` (`PAGES`/fixtures/`FLOWS`) only
  if the page's selectors/surface changed.
