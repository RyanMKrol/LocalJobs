## Overview
On the Integrations (Services) page, still show a limitless service's actual per-period usage count instead of dropping it and rendering only 'no limit', so metered usage is visible even when there's no cap to show it against.

## Do
In dashboard/app/integrations/page.tsx, the `Bar` component (line ~10) currently returns `<span className="muted">no limit</span>` when `cap == null`, hiding the usage figure entirely; when a cap IS set it renders `<used> / <cap>` plus a `.ubar` progress bar. Change the `cap == null` branch to STILL show the used count (the `used` prop, which is passed for every column — rate_last_min / used_today / used_month, all already present in the /api/services payload for limitless services since callService meters usage regardless of cap), rendered WITHOUT a fraction or progress bar (there's no denominator). Use a clear presentation consistent with the column layout — the number plus a small muted 'no limit' qualifier (match the exact wording to the answer to the DoD question; default to showing the count with a muted 'no limit' note, e.g. `<span className="mono">{used}</span> <span className="muted">no limit</span>`). Apply to all three rate columns (they all pass `used`), since each already calls `<Bar used=... cap=... />`. No API/store change is needed — the used counts are already in the payload (a service WITH a limit already displays them). Do not touch the timeout column. The Integrations page is already a PAGES entry ('services', path '/integrations') and _dashboard-harness.mjs fixtures already include no-limit services (claude-cli, legacy-service, plex), so the visual-check already exercises this case — no harness change needed. Build the dashboard, run node dashboard/scripts/mobile-check.mjs and node dashboard/scripts/visual-check.mjs, and LOOK at the 'services' screenshot to confirm a no-limit service now shows its usage count.

## Done when
On the Integrations page, a service with no cap in a given rate column shows its actual per-period usage count (instead of only 'no limit' with no number), with no fraction/progress bar; services WITH limits still render `<used> / <cap>` + bar unchanged; the timeout column is unchanged; and dashboard build + mobile-check + visual-check are green with the 'services' screenshot showing usage counts on a limitless service (e.g. claude-cli).
