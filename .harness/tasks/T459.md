## Overview
This is a process/convention fix, not a code change: when a backlog task is scoped to define a brand-new workflow, or to restructure an existing workflow's DAG (add/remove/reshape stages), the scoping step currently has no explicit prompt to design the stages' Input/Output ledger shape up front — so it gets discovered later, expensively, via a dashboard screenshot of an already-shipped workflow whose work_items key or detail blob turns out illegible (the exact history behind ideas #6/#15/#16/#17/#18 and the plex-language-fix restructuring discussion that produced this idea). Add a checklist requirement to this repo's own harness customization overlay so the next workflow-defining/restructuring task is scoped with this settled from the start.

## Do
Edit `.harness/custom/CLAUDE.md` (NOT any plugin-owned file — this is the repo's own customization overlay, auto-loaded via `.harness/CLAUDE.md`'s `@custom/CLAUDE.md` import, and never touched by `implementation-harness-upgrade`). Add a new section (e.g. under a heading like 'Scoping a new or restructured workflow — design the I/O shape up front') that states, as a required checklist item for anyone (human or the `implementation-harness-add-to-backlog` / `implementation-harness-convert-ideas` skills) scoping a task that defines a NEW workflow or RESTRUCTURES an existing workflow's DAG (adds/removes/reshapes stages): before finalizing the task's spec, explicitly settle, per new or changed stage: (1) the `work_items` ledger item KEY it will use and its granularity (e.g. per-show vs per-episode — call out that granularity choices can silently reintroduce a per-item external-API-call inefficiency later if not decided up front, per root CLAUDE.md's existing idempotency conventions), and (2) what fields the stage's SUCCESS `detail` blob will contain, per the existing root CLAUDE.md rule (2026-07, 'Every stage's success detail must describe what THAT STAGE produced — not just restate the item's identity') — i.e. does it write a file (`detail.markdown`/`detail.path`+`detail.format`) or a discovered/computed value, and what are its exact keys. Require these two points to be captured as an explicit section of the task's spec MD (`.harness/tasks/TNNN.md`) alongside `## Do`/`## Done when`, so a fresh builder agent implements the stage's ledger writes correctly the first time instead of guessing. Do NOT include a requirement to choose between the generic `IoPanel` and `StageIoPanel` — per root CLAUDE.md's own T386 note, `StageIoPanel` is now the default Inputs & Outputs treatment for EVERY workflow's run page (the old `IoPanel` fallback is dead code), so that choice no longer needs deciding per-workflow. Keep the addition concise (a short subsection, not a restatement of the existing root CLAUDE.md conventions it references) and cross-reference root CLAUDE.md's relevant existing rules (the T110 markdown-artifact `detail.markdown` convention, the 2026-07 'detail must describe what it produced' rule, and the run→work-item lineage/`rootKey` conventions) by name rather than duplicating their text.

## Done when
`.harness/custom/CLAUDE.md` contains a clearly headed section requiring that any task scoped to define a new workflow or restructure an existing workflow's DAG must, before being finalized, explicitly decide and record (in the task's spec MD) each new/changed stage's `work_items` ledger item key + granularity and its success `detail` blob's field contents — cross-referencing root CLAUDE.md's existing detail/idempotency conventions rather than duplicating them, and explicitly NOT reintroducing the now-stale IoPanel-vs-StageIoPanel choice. The addition is committed.
