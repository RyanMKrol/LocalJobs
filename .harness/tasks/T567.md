## Overview
Perfumes still ships its own copy of the Claude-CLI spawn/parse helper (the documented but never-done follow-up). This unit deletes it and points the three perfumes stages at the now-unified shared helper in src/services/claude.ts, which also brings perfumes builds under the dashboard-editable claude-cli timeout.

## Do
Delete src/workflows/perfumes/claude.ts. Update its three callers -- find-url.ts, parse.ts, build.ts -- to import runClaude (and extractJsonObject in place of extractJson, and unfenceMarkdown) from '../../../services/claude.js'. Perfumes' runClaude(prompt,model) maps to the shared runClaude(prompt,model) (shared signature has an extra optional effort arg that perfumes simply doesn't pass). Perfumes' extractJson maps to the shared extractJsonObject (byte-identical body). unfenceMarkdown is the copy R02a moved into src/services/claude.ts. Decide PERFUMES_CLAUDE_TIMEOUT_MS / PERFUMES_CLAUDE_BIN handling: default to the shared editable claude-cli timeout (claudeTimeoutMs()) and LOCALJOBS_CLAUDE_BIN per the R02 idea; if the owner confirmed keeping the perfumes-specific env vars as more-specific fallbacks, wire that explicitly -- otherwise drop them and remove the now-dead perfumesConfig.claudeTimeoutMs/claudeBin fields. Update src/workflows/perfumes/CLAUDE.md: remove the 'perfumes has its OWN Claude helper / migrating it is a not-yet-done follow-up' invariant and state it now uses the shared helper.

## Done when
src/workflows/perfumes/claude.ts is deleted and find-url.ts/parse.ts/build.ts import the shared helper from services/claude.js. grep -rn 'spawn(' over src/workflows (excluding data/repos) finds NO claude spawn -- the three-copy debt is fully retired. npx tsc --noEmit and npm test pass with the perfumes stage tests green. Meter parity verified: each of find-url/parse/build still issues exactly one callService('claude-cli', ...) per processed item (no doubling or dropping) -- confirm by reading the call sites, since perfumes already gated through the same service. perfumes/build and perfumes/parse produce unchanged output for their existing fixture inputs (their tests stay green). perfumes/CLAUDE.md no longer claims a separate helper and the LIMITATIONS/CLAUDE.md follow-up note is resolved.
