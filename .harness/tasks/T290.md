## Do

Add a second stage, `stocks-watch`, to the `stocks-sync` workflow built in T289
(`src/jobs/stocks-sync/stocks-sync.workflow.ts`), which notifies the owner — via a single push —
whenever any held position's current price has risen 30% or more above its average buy price since
it last crossed that threshold.

1. Wire the new member into `stocks-sync.workflow.ts`'s `jobs` list so it runs after
   `stocks-snapshot` in the DAG (use the same `dependsOn` member-edge shape another multi-stage
   workflow manifest already uses, e.g. `src/jobs/places/places.workflow.ts` or
   `src/jobs/tv-recs/tv-recs.workflow.ts`, for the exact field name/shape).
2. Add `src/jobs/stocks-sync/stages/stocks-watch.job.ts` + `stocks-watch.ts` (+ `.test.ts`). It
   reads `data/out/portfolio.json` (written by `stocks-snapshot` in T289: an array of `{ ticker,
   quantity, averageBuyPrice, currentPrice, currentValue }`).
3. For each position compute `gainPct = (currentPrice - averageBuyPrice) / averageBuyPrice * 100`.
4. **Idempotency — a "re-scan + notification-log" stage** (same shape as the `missing-tv-seasons`
   workflow's notify stage, per root `CLAUDE.md`): maintain a `work_items` ledger row per `ticker`
   for THIS stage (`stocks-watch`), used purely as a "have I already notified this breach episode"
   log, not a work-done log:
   - A position with `gainPct >= 30` whose ledger row is NOT currently marked notified is a **fresh
     breach** — include it in this run's notification and mark its row `success` (notified).
   - A position with `gainPct >= 30` whose ledger row IS already marked notified is **not** a fresh
     breach — skip it (don't re-notify every run while it stays above the threshold).
   - A position whose ledger row is marked notified but whose CURRENT `gainPct < 30` has dropped
     back below the threshold: **clear/reset its ledger row** (delete it or mark it in a way
     `isWorkItemDone` no longer treats as done) so a LATER re-crossing above 30% notifies again.
5. If one or more fresh breaches are found this run, send **exactly ONE** push — not one per
   position — via the existing `push(title, body, opts)` helper exported from
   `src/core/notifier.ts` (already the mechanism other long-running jobs use for custom milestone
   pushes; do NOT rely on the generic per-run aggregate `notifyWorkflow`, which is content-agnostic
   per the existing T189 convention documented in root `CLAUDE.md`). The body must list every
   fresh-breaching ticker with its gain, one line each, e.g. `AAPL +32% since last buy ($150.00 →
   $198.00)`.
6. If there are NO fresh breaches this run, send no notification at all (this is the owner's
   explicit ask — "I would only like a notification when that has happened").
7. Update root `CLAUDE.md`'s `stocks-sync` paragraph (added in T289) and `README.md`'s bullet to
   describe the second stage: notifies once per breach episode (not repeatedly) when a held
   position's current price is ≥30% above its average buy price, and resets so a future re-breach
   after dropping back below 30% notifies again.

## Done when

- `stocks-sync.workflow.ts` has 2 member jobs, and `stocks-watch` genuinely `dependsOn`
  `stocks-snapshot` in the DAG (confirm the workflow's derived topological order places
  `stocks-snapshot` first).
- A unit test with injected fake portfolio data (an array matching T289's normalized shape) and a
  stubbed/injected `push` call demonstrates all of:
  a. a position newly crossing `gainPct >= 30` triggers exactly one `push()` call whose body
     mentions that ticker;
  b. a position already marked notified that is STILL `>= 30` triggers NO further `push()` call on
     a subsequent run;
  c. a position that drops back under 30% after being notified has its ledger row cleared, and a
     LATER run where it re-crosses 30% triggers a fresh notification;
  d. a run where no position is `>= 30` calls `push()` zero times;
  e. a run where MULTIPLE positions freshly cross `>= 30` in the same run triggers exactly ONE
     `push()` call (not one per position), whose body lists all of them.
- Root `CLAUDE.md` and `README.md` both describe the notify-once-per-breach-episode behavior
  (including the reset-on-drop-below-threshold rule).
- Manually run the two-stage workflow once end-to-end against the real Trading212 account
  (credentials already in local `.env`) and confirm it completes without error — record what was
  observed (e.g. no real position was near the 30% threshold, so no push fired) in this task's
  worklog.
- `npm test` and `npx tsc --noEmit` pass.
