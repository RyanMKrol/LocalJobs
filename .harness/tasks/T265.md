## Do

**(needs-human — edits `.harness/loop.sh`, the loop's own driver, which CI/LOCAL_DOD don't cover, so
the owner/Claude builds it directly in a session rather than the loop self-editing.)**

Fix the harness over-waiting after a Claude usage-limit hit. **Root cause (confirmed):** the
rate-limit backoff in `.harness/loop.sh` (the `rc == 10` branch, ~lines 669–676) is **pure
exponential doubling** — `rl_sleep` starts at `RL_BACKOFF_MIN` (300s) and doubles each hit up to
`RL_BACKOFF_MAX` (18000s ≈ 5h). It does **NOT** parse the reset time Claude reports, even though
`RL_RE` already matches `"resets at"` and `harness.env`'s `RL_BUFFER` ("extra wait after a parsed
reset") is defined but **never used**. So once the backoff has climbed (e.g. to 40–80 min), the loop
sleeps far past the actual reset — the observed "waits over an hour after the limit actually resets."

Fix: when `run_claude` returns 10 (rate-limited), **parse the reset time out of the captured output**
(`$WORKLOG/.claude-out` — the same text `RL_RE` matched; capture a real message to see the exact
format, e.g. "resets at 3:00pm" / a timestamp), compute the sleep as `(reset_time - now) + RL_BUFFER`,
and sleep that long (resume right after the window refreshes). **Fall back to the existing exponential
backoff only when no reset time can be parsed.** Mind timezones (the printed reset time vs local
clock) and a sane floor/cap (never sleep negative; cap at `RL_BACKOFF_MAX`). Use `RL_BUFFER` as the
post-reset cushion (now actually wired in). Apply the same logic to the auditor's rate-limit wait
(~line 564-567, the `rlpoll` loop) if practical.

## Done when

- On a rate-limited (`rc==10`) return, the loop parses Claude's reported reset time and sleeps until
  ~that time + `RL_BUFFER` (resuming shortly after the window resets), instead of the blind
  exponential value; it falls back to exponential backoff only when the reset time is unparseable.
- `RL_BUFFER` is actually used; timezone handling is correct; the sleep can't go negative and is
  capped at `RL_BACKOFF_MAX`.
- `bash -n .harness/loop.sh` passes.
- **Behavioural verification (record in the worklog):** feed a synthetic captured rate-limit message
  containing a known "resets at <time>" into the parse path (e.g. via a sourced helper / a fixture
  `.claude-out`) and confirm the computed wait targets that reset time + `RL_BUFFER` (NOT the
  exponential value); and that a message with no reset time falls back to exponential. No live
  rate-limit needed.
- Docs note the new behaviour where the backoff is described (`harness.env` comment / `HARNESS.md`).
