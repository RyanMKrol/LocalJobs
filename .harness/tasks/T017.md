## Do

Some items will never process (genuinely bad data). Add a way to MANUALLY mark a stuck item as permanently dismissed so it drops off the dashboard front page. MUST be manual-only, never automatic, and distinct from 'unstick' (which retries). Likely a new dismissed status on work_items plus an API + dashboard control.

## Done when

A stuck item can be manually dismissed and then no longer appears in the stuck list/front page; dismissal is never automatic and is distinct from unstick/retry; unit tests cover dismiss + that a dismissed item stays off the stuck list.
