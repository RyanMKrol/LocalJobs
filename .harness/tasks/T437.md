## Overview

Part of a repo-wide sweep giving every job a real paragraph-length `description` (surfaced on
`/jobs/[name]` and `/runs/[id]`) instead of a one-liner. This task covers the single job in the
`claude-warmer` workflow. The mechanism was already built by the foundational task this depends on —
this is pure content authoring.

## Do

Rewrite the `description` of `src/workflows/claude-warmer/claude-warm.job.ts` (`claude-warm`) into a
real 3-6 sentence paragraph. Currently: "Issue one minimal Claude CLI prompt to start/maintain the
5-hour usage window. Handles upstream rate-limit / quota errors defensively (logs + exits cleanly)."
Expand it to explain WHY this job exists (Claude accounts have a 5-hour rolling usage window; firing a
cheap prompt every 30 minutes during off-hours means the window is already running/reset by the time
real work needs Claude), that it has no local quota cap (the upstream Claude plan enforces its own
limit), and that a hit limit is a soft-fail (job exits cleanly, not an error). Ground it in root
`CLAUDE.md`'s `claude-warmer` section and the job's own code. Plain prose only, no markdown syntax.

## Done when

- The `claude-warm` job has a real, accurate paragraph description grounded in its actual behavior —
  not a two-liner.
- `npx tsc --noEmit` and `npm test` are green.
