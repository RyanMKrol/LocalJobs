## Do

**Follow-up to T225 (built but the owner marked it FAILED — `skipped` was not plumbed through).**
Repro the owner confirmed: run the `places` workflow with a limit of 5 → it selects 5 places that are
**already fully processed**, does NO actual work for them, yet the run + its stages + the items still
report **succeeded**. "Succeeded" for a run that accomplished nothing is wrong and misleading.

Investigate and fix the root cause (don't just relabel). Key anchors:
- `isRootPending` / `rootHasOutstandingWork` / `rootReachedTerminal` and `selectPendingRoots` in
  `src/db/store.ts` (the limited-run root selection) — an already-complete root is being re-selected.
  Strong suspect: the `skipped` `WorkStatus` (`src/db/store.ts` ~`WorkStatus` + the comment "`skipped`
  means soft-stopped … NOT done; retry when quota resets", and `isWorkItemDone` returns `false` for
  `skipped`). A root whose items are `skipped` looks perpetually "pending/outstanding", so a limited
  run keeps re-picking it, re-skips it (no real work), and the stage/run still settles `success`.
- The run/stage status: a member stage that processed **zero** items this run (its `todo` was empty
  because everything was already done/skip-worthy) currently settles `success`. The places stages
  mark items in `src/jobs/places/stages/*.ts` (`enrich.ts` marks `success`/`skipped`/`failed`).
- The workflow-run + member status surfacing (`src/core/workflow-executor.ts` roll-up; the run-detail
  view `dashboard/app/workflow-runs/[id]/page.tsx` + `dashboard/app/ui.tsx`) shows these as succeeded.

Decide and apply a coherent model so that **a unit of work that did nothing (nothing left to do /
already complete) is surfaced as `skipped` / "nothing to do", NOT `success`** — at the run level, the
member-stage level, and the per-item IO display — AND a limited run does not perpetually re-select
roots that are already complete or have only non-retryable `skipped`/given-up items. ⚠️ Be careful not
to break the EXISTING `skipped` meanings (an upstream-failed member; a quota soft-stop that SHOULD
retry later) — distinguish "soft-stopped, retry later" from "no-op, nothing to do" rather than
conflating them.

## Done when

- A unit test (scratch DB) proves: a root that is already fully processed through the terminal stage
  is NOT returned by `selectPendingRoots`; and a root whose only remaining rows are non-retryable
  (e.g. given-up / already-handled `skipped`) is likewise not perpetually re-selected. `expectsTest`.
- When a (limited or normal) workflow run does NO actual work for the selected items, the run and its
  stages are reported as **`skipped` / "nothing to do"**, NOT `succeeded`, and the run-detail view
  reflects that (the IO panel does not show those items as freshly "succeeded" when nothing happened).
- The existing `skipped` semantics (upstream-dependency-skipped member; quota soft-stop that retries
  when quota resets) still behave as before — verified by their existing tests staying green.
- Repro check recorded in the worklog: a limited `places` run over already-processed places no longer
  reports success-with-no-work (it reports skipped/nothing-to-do).
- `npx tsc --noEmit` and `npm test` are green; CI green. If the run-view display changed,
  `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` +
  `node dashboard/scripts/visual-check.mjs` green, and the harness fixtures
  (`dashboard/scripts/_dashboard-harness.mjs`) updated to exercise a skipped/no-op run if needed.
