## Do

**Follow-up to T225 (built but the owner marked it FAILED — `skipped` was not plumbed through).**
**BACKEND half of the fix** (the dashboard surfacing is the dependent task T281 — do NOT touch
dashboard files here; they are out of scope).

Repro the owner confirmed: run the `places` workflow with a limit of 5 → it selects 5 places that are
**already fully processed**, does NO actual work for them, yet the run + its stages + the items still
settle **succeeded**. "Succeeded" for a run that accomplished nothing is wrong and misleading.

Investigate and fix the root cause (don't just relabel). Key anchors:
- `isRootPending` / `rootHasOutstandingWork` / `rootReachedTerminal` and `selectPendingRoots` in
  `src/db/store.ts` (the limited-run root selection) — an already-complete root is being re-selected.
  Strong suspect: the `skipped` `WorkStatus` (`src/db/store.ts` ~`WorkStatus` + the comment "`skipped`
  means soft-stopped … NOT done; retry when quota resets", and `isWorkItemDone` returns `false` for
  `skipped`). A root whose items are `skipped` looks perpetually "pending/outstanding", so a limited
  run keeps re-picking it, re-skips it (no real work), and the stage/run still settles `success`.
- The run/stage status roll-up in `src/core/workflow-executor.ts`: a member stage that processed
  **zero** items this run (its `todo` was empty because everything was already done/skip-worthy)
  currently settles `success`; it should settle **`skipped` / "nothing to do"**. The places stages
  mark items in `src/jobs/places/stages/*.ts` (`enrich.ts` marks `success`/`skipped`/`failed`).

Decide and apply a coherent status MODEL so that **a unit of work that did nothing (nothing left to
do / already complete) is `skipped` / "nothing to do", NOT `success`** — at the run level and the
member-stage level — AND a limited run does not perpetually re-select roots that are already complete
or have only non-retryable `skipped`/given-up items. ⚠️ Distinguish "soft-stopped, retry later" from
"no-op, nothing to do" rather than conflating them — do NOT break the EXISTING `skipped` meanings (an
upstream-failed member; a quota soft-stop that SHOULD retry later).

This is the BACKEND/data-model change only. The run-detail view + IO panel display of the new status
is **T281** (depends on this task). Scope here is `src/db/store.ts`, `src/core/workflow-executor.ts`,
and `src/jobs/places/stages/` — plus tests. Do not edit dashboard files.

## Done when

- A unit test (scratch DB) proves: a root already fully processed through the terminal stage is NOT
  returned by `selectPendingRoots`; and a root whose only remaining rows are non-retryable (given-up /
  already-handled `skipped`) is likewise not perpetually re-selected. `expectsTest`.
- A unit test proves the roll-up: a run/stage that did NO actual work (empty `todo`, everything
  already complete) settles **`skipped`/"nothing to do"**, NOT `success`; a run that did real work
  still settles `success`; a mixed/failed run still rolls up as before.
- The existing `skipped` semantics (upstream-dependency-skipped member; quota soft-stop that retries
  when quota resets) still behave as before — verified by their existing tests staying green.
- Repro recorded in the worklog: a limited `places` run over already-processed places no longer
  reports success-with-no-work at the data/run-status level (it records skipped/nothing-to-do). Use
  the scratch DB / existing fixtures — do NOT make paid Places/Gemini calls.
- `npx tsc --noEmit` and `npm test` are green; CI green. (No dashboard changes in this task, so no
  dashboard build / visual-check here — that is T281.)
