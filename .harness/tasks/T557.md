## Overview
Delete the timeoutMs: 5_000 field (and its comment) from the fs service definition — it is a dashboard-editable knob that changes nothing, because nothing reads effectiveServiceTimeoutMs('fs', ...). (Default is delete; the paired question offers guarding the consumer instead.)

## Do
1. In src/services/fs.service.ts, delete the `// Short timeout ...` comment (~line 25) and the `timeoutMs: 5_000,` field (~line 26) from the ServiceDefinition. 2. Leave everything else (name, paid, rateLimitSource, category) intact. Do NOT modify effectiveServiceTimeoutMs, the plex/claude-cli readers, or places/stages/resolve.ts (the delete path leaves the fs consumer as-is).

## Done when
npx tsc --noEmit and npm test are green. grep for timeoutMs in src/services/fs.service.ts returns nothing. The plex and claude-cli effectiveServiceTimeoutMs readers are unchanged and still compile. No other service definition is touched.
