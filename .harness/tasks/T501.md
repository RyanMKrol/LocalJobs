## Overview
Cache `openfigi` ISIN→ticker resolutions — an extremely stable mapping — for 22 hours. Outcome of the
T479 caching-adoption decision; the caching mechanism already exists (T451/T476).

## Do
1. Set `cacheTtlMs: 79_200_000` (22 hours) on the `openfigi` `ServiceDefinition` in
   `src/services/openfigi.service.ts`.
2. Add a stable `cacheKey` to the `callService('openfigi', ...)` read site, which lives in
   `src/services/trading212.service.ts` (openfigi is called from within the Trading212 service's ISIN
   resolution). Key it by the ISIN being resolved: `` `openfigi:isin:${isin}` ``.
3. Cache the read (mapping lookup) only.

## Done when
- `cacheTtlMs: 79_200_000` is set on the `openfigi` service def.
- The `callService('openfigi', ...)` site in `src/services/trading212.service.ts` passes
  `openfigi:isin:<isin>` as its cacheKey.
- `npx tsc --noEmit` and `npm test` are green.
- Verified a repeated resolution of the same ISIN within 22h is served from `service_cache` (hermetic
  scratch-DB check) rather than a second live call.
