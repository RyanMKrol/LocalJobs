## Overview
Building on the new `certified` flag + `/certify` endpoint + `updateWorkflowCertified` client helper (the dependency unit), this unit adds the actual owner-facing UI: a toggle button on the workflow detail page to certify/un-certify a workflow, and a small green medal/badge shown wherever a certified workflow appears — the workflow detail page AND the Workflows list page (NOT the Overview home page — the owner explicitly did not choose that). Purely visual/informational: certifying a workflow has no functional side effects (no suppressed notifications, no scheduling change).

## Do
On the workflow detail page (`dashboard/app/workflows/[name]/page.tsx`): add a `toggleCertified()` handler mirroring the existing `toggleNotify()` handler (`async function toggleCertified() { if (p) await api.updateWorkflowCertified(name, !p.certified); }`), and add a new 'Certified' row to the existing `.kv` details panel (the same panel containing the 'Schedule'/'Enabled'/'Max concurrency'/'Notifications' rows around line 987-1069) using the SAME `<span className="toggle" onClick={...}>` click-to-toggle affordance already used for 'Enabled' and 'Notifications' (checkbox + label text + '(click to toggle)'), reading `p?.certified` — label it clearly, e.g. 'certified ✓' when on / 'not certified (click to certify)' when off. Additionally, render a small green medal badge (🏅, reusing the shared `<Pill>` component from `dashboard/app/components/Pill.tsx`, e.g. `<Pill kind="certified">🏅 Certified</Pill>`) next to the workflow name `<h1>` in the header row when `p?.certified` is truthy — placed in the existing `<div className="row">` that already holds the `<h1>{name}</h1>` + run controls, so it visually reads as attached to the workflow's identity, not buried in the details panel. On the Workflows list page (`dashboard/app/workflows/page.tsx`): render the SAME `<Pill kind="certified">🏅</Pill>` badge next to the workflow name link in the first table column (in the `<td>` currently holding `<Link href=...><strong>{p.name}</strong></Link>` plus the stuck-count button, around line 82-95) whenever `p.certified` is truthy — keep it compact (icon-only or short label) since it's inline with the name in a table cell, not a full-width header. Add a `.pill.certified` CSS rule to `dashboard/app/globals.css` alongside the other `.pill.*` kind rules (e.g. near `.pill.on`/`.pill.done` around line 336-350) using a GREEN palette consistent with the existing `--green` custom property, matching how `.pill.on`/`.pill.done` are already styled (background `rgba(63,185,80,.12)`-style tint, `--green` text, matching border) — do not invent a new color variable. Confirm the `Workflow` TypeScript interface already has `certified?: number` from the dependency unit (do not re-add it if already present after that unit lands; if it isn't landed/visible yet, coordinate via `dependsOn` — this unit assumes it exists). Update `dashboard/scripts/_dashboard-harness.mjs` per this repo's LIVING ARTIFACT rule: if its synthetic `/api/workflows` and `/api/workflows/:name` fixtures don't already model diverse boolean fields per workflow, add `certified: true` to at least one fixture workflow (and leave at least one other fixture workflow with `certified: false`/absent) so the mobile-check and visual-check harnesses actually render both the badge-present and badge-absent states across the Workflows list and at least one workflow detail page; add a `FLOWS` entry (or extend an existing detail-page flow) if a dedicated 'certified toggle clicked' interaction state is worth a distinct screenshot — use your judgment matching the existing FLOWS granularity for the other click-to-toggle rows (e.g. Notifications) on that same page, don't over-add if a static badge is already visible in the baseline PAGES shot.

## Done when
The workflow detail page has a working, click-to-toggle 'Certified' row in the details panel (mirroring the existing Enabled/Notifications toggle rows) that calls the new `/certify` endpoint via `api.updateWorkflowCertified`; when a workflow is certified, a green 🏅 badge (via the shared `<Pill kind="certified">` component) appears next to the `<h1>` workflow name on the detail page AND next to the workflow name in the Workflows list table row for that workflow — in both places the badge is absent for a non-certified workflow; `.pill.certified` in `globals.css` uses the existing `--green` custom property consistent with `.pill.on`/`.pill.done`'s styling; `dashboard/scripts/_dashboard-harness.mjs` fixtures include at least one certified and one non-certified workflow so both states are exercised; `npx tsc --noEmit`, `npm test`, and `npm --prefix dashboard run build` are all green; `node dashboard/scripts/mobile-check.mjs` passes with no new overflow; `node dashboard/scripts/visual-check.mjs` is run and its screenshots are visually inspected (not just executed) confirming the badge is actually painted/visible (not just present in the DOM, not clipped/overlapping) on both the workflow detail page and the Workflows list page, and that the toggle button/row renders correctly in both certified and not-certified states; toggling certification has no other observable side effect (no change to scheduling, notifications, or run behavior).
