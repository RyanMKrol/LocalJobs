## Do

Replace the verbose per-edge gate **text labels** on the React Flow graph with a compact **padlock
icon** marker, to cut the clutter (movie-recommendations shows ~19 repetitive key labels). In
`dashboard/app/components/DagFlow.tsx`, render a small padlock glyph at each gated edge instead of the
artifact-key text, **colour-coded by gate state** (green=passed, red=failed, grey=pending/structural),
with the artifact key + description shown on **hover** (tooltip) and the existing **click-through to
the gate detail page** preserved (`gateData.state` / `gateHref` are already computed). Also
**foreground failures, mute the happy path**: passing/structural padlocks render small/subtle; a
FAILED gate renders prominent (red, larger) so it stands out on a run view.

## Done when

- Each gated edge shows a compact state-coloured padlock (not the key text); passing/structural ones are
  subtle, failed ones are prominent.
- Hovering shows the gate key + description; clicking still opens the gate detail page.
- Theme-variable-driven (all 3 families × light/dark) + mobile-safe.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs` green.
