## Do

Add **symmetric vertical padding** inside the workflow **graph** container so the React Flow DAG has
even breathing room top and bottom (and ideally left/right) instead of hugging the top border with
only a sliver at the bottom. On a workflow detail page (e.g. `/workflows/movie-recommendations`) the
graph currently sits flush to the top of its panel. In `dashboard/app/components/DagFlow.tsx`, the
container height is computed from the graph's natural height (`graphH = maxStagesInWave * (NODE_H +
NODE_SEP) + 80`, capped at 820) and `<ReactFlow fitView fitViewOptions={{ padding: 0.15 }} />` zooms
to fit — the inset ends up asymmetric. Fix so the graph is vertically centred with equal top/bottom
margin: bump/symmetrise `fitViewOptions.padding` and/or add inner padding to the `.dag.dag-flow-wrap`
wrapper and account for it in the height calc. Box/padding styling lives in
`dashboard/app/globals.css`.

## Done when

- On a workflow detail page the DAG graph has visibly even top and bottom padding inside its
  container (not flush-top / sliver-bottom); the graph reads as vertically centred.
- It stays mobile-responsive (no new phone-width overflow; the `@media (max-width: 640px)` block) and
  theme-variable-driven.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build and the `workflow.png` /
  `workflow-run.png` screenshots in `dashboard/scripts/visual-out/` confirm the graph has symmetric
  vertical padding; `dashboard/scripts/_dashboard-harness.mjs` is updated if the UI surface changed.
