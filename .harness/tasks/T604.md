## Overview
All 156 existing movie-recs work_items ledger rows' detail.markdown field points at movies/data/out/reports/franchise-gaps.md, a stale leftover from before the franchise-gap audit was split out of movie-recommendations into its own missing-movies workflow (T468) — it should point at the actual recommendations report instead.

## Do
In src/core/recommender/notify.ts's runRecsNotify, the markWorkItem call at notify.ts:101-115 sets `detail.markdown: reportPath`, where `reportPath` comes from `writeReport(domain, recs, newRecs, now, reportDir)` (notify.ts:80, function defined at notify.ts:126-161, writing to `join(reportDir, domain.reportFilename)`). Confirm what `domain.reportFilename` currently resolves to for the movies domain (check src/workflows/movies/stages/branches.ts or wherever `moviesDomain` is constructed for its `reportFilename`/`reportDir` fields) — the idea report says existing rows point at 'franchise-gaps.md' instead of the actual recommendations report (likely 'recommendations.md'), meaning `domain.reportFilename` for movies is currently misconfigured to the old franchise-gap-audit's filename, a leftover from before T468 split that audit into missing-movies. Fix the movies domain's `reportFilename` (and reportDir if also wrong) constant so newly-notified rows' `detail.markdown` points at the CURRENT recommendations report file. Do NOT touch missing-movies' own separate franchise-gaps.md output (that workflow still legitimately owns that file — only movie-recs's OWN pointer is stale). Owner's decision (confirmed directly, no needs-human task required): leave the ~156 existing, already-written work_items rows with the stale path AS-IS — do NOT retroactively rewrite them, no data migration/UPDATE of any kind for this unit. Only fix where NEW rows point going forward.

## Done when
A fresh movie-recommendations run that notifies at least one new recommendation writes a work_items row whose detail.markdown points at the actual movies recommendations report file (the one the owner reads for the digest), not franchise-gaps.md. Confirm by inspecting the moviesDomain config's reportFilename/reportDir values directly (or via a unit test asserting the value) rather than only running the pipeline live.
