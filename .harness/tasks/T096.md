## Do

On the validation-gate detail page the two sides are labelled 'Output' (producer) and 'Input' (consumer) — job-centric labels that read backwards on a page about the GATE (the gate's input is the producer's output; the gate's output is the consumer's input). Decision (interview 2026-06-22): avoid the frame-of-reference ambiguity by using NEUTRAL DIRECTIONAL labels rather than flipping. Relabel the producer side as 'Produced →' and the consumer side as '→ Consumed' (keep the producer/consumer job names beneath each, as today). Rename the corresponding API response fields too: GET /api/workflow-runs/:id/gates/:producer/:key currently returns { gate, output, input } — rename output->produced and input->consumed (or similar neutral names) and update the client types in dashboard/app/lib/api.ts and the SideCard role prop accordingly. Update the CLAUDE.md gate-page description (which currently says 'upstream stage Output -> Gate -> downstream stage Input', and 'output = producer's produces[key], input = consumer's consumes[key]') to the new wording. NOTE: this overlaps with T088 (code-block rendering) and T089 (emphasise actual) on the same SideCard — whichever lands later should harmonise so labels, code block, and the actual-result emphasis read cleanly together.

## Done when

The gate detail page uses neutral directional labels ('Produced →' for the producer side, '→ Consumed' for the consumer side) instead of Output/Input; the API response fields and client types are renamed to match; the CLAUDE.md gate-page description reflects the new wording; it harmonises with the T088/T089 changes if those have landed; npx tsc --noEmit, npm test, the dashboard build, and mobile-check are all green.
