## Overview
Make the dashboard's clickable-span editors and sortable table headers reachable and operable by keyboard, matching the existing CronBadge accessibility pattern, with no visual change.

## Do
Three sets of controls are keyboard-inaccessible. (1) The three Edit spans — <span className="schedule-edit-link" onClick> at workflows/[name]/page.tsx:1011 (schedule) and :1057 (concurrency), and jobs/[name]/page.tsx:98 (timeout) — have no tabIndex/role/key handler. Replace each with <button className="btn-link">Edit</button> (the .btn-link class already exists in globals.css). (2) SortTh.tsx:12 renders <th onClick> with no button semantics — wrap the label in a <button> (or add role=button + tabIndex=0 + an Enter/Space key handler) and set aria-sort on the <th> reflecting the active sort direction. (3) The .toggle spans at page.tsx:1017/:1063/:1069 use a readOnly checkbox that mis-announces state — give them role="switch" + aria-checked reflecting the on/off state. Follow the CronBadge pattern at ui.tsx:246-258. Appearance must be unchanged. If a keyboard FLOWS entry is added to _dashboard-harness.mjs, update that file in the same change (living-artifact rule).

## Done when
Tab reaches every Edit editor and every sort header; Enter/Space activates them; the toggle spans expose role=switch + aria-checked and SortTh exposes aria-sort; the visual appearance is UNCHANGED (LOOK at visual-check before/after for the workflow-detail, job-detail, and sortable-header surfaces); npx tsc --noEmit, build, and mobile-check are green.
