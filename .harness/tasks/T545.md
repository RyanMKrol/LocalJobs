## Overview
Compute the grouped franchise-gaps list once per render instead of recomputing groupByCollection three separate times.

## Do
In the MovieGapsManager (moved to its own component file by R05-a; possibly the generic GroupedManager after R05-b), groupByCollection(active) is currently evaluated three times per render (at page.tsx:503, :504, :516). Memoize the active grouping with useMemo keyed on the active gaps list so it runs once; do the same for the ignored grouping (:579) if it's also recomputed. No visible change.

## Done when
The active (and ignored) collection grouping is computed once per render via useMemo; the missing-movies detail page renders identically (LOOK at visual-check); npx tsc --noEmit, build, mobile-check, and visual-check are green.
