## Do

On the overview page, hide the entire "Stuck items" SECTION (its heading + list/empty-state) when there are zero stuck items — render the section only when there is at least one stuck item. Keep the Stuck stat TILE/count at the top regardless. Apply the same empty-section hiding to the Ignored section if it has its own dedicated section.

## Done when

When nothing is stuck, the overview shows no "Stuck items" heading or section (just the count tile up top); when something is stuck the section renders as before; the dashboard build passes.
