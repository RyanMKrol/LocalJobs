## Overview
T388 blocked immediately (rung 0, attempt 0, no real escalation) because adding a PAGES entry for the single-stage claude-warmer-run page to mobile-check triggered a reported 234px spill on `.react-flow__renderer`/`.react-flow__pane`, and the builder concluded the fix needed dashboard/app/globals.css or DagFlow.tsx — both outside its scope (_dashboard-harness.mjs + StageIoLists.tsx only), so it gave up rather than fix out-of-scope files. Independently reproducing the exact same page today confirms the spill is real (still unfixed) but is NOT an actual visual bug: `.dag-flow-wrap` and `.react-flow` both have `overflow: hidden` and correctly clip it (bounding-rect right edge stays at 366-381px, well within the 402px viewport, and the top-level document itself reports 0px overflow) — only React Flow's own internal `.react-flow__renderer`/`.react-flow__pane` layers (two levels below the clipping ancestor) have `overflow-x: visible` and a large internal scrollWidth from the unzoomed canvas coordinate space. mobile-check.mjs's spill-detection heuristic (the `measure()` function) checks each element's own scrollWidth-vs-clientWidth in isolation and never accounts for an ancestor already clipping it invisible — that heuristic gap, not any dashboard CSS/component, is the actual bug and the actual fix location. This re-attempt scopes the correct file.

## Do
In `dashboard/scripts/mobile-check.mjs`'s in-page `measure()` function (used by `page.evaluate`), an element with `overflow-x: visible` and a `scrollWidth > clientWidth` should NOT be reported as an offending 'boundary-crossing' element if its own bounding rect is already fully contained within the visible viewport AND within every ancestor's clipping box up to the document body (i.e. the spill is invisible on screen because a nearer ancestor has overflow:hidden/auto/scroll and the element's rect doesn't actually extend past that ancestor's clip box or past the viewport edge). The simplest correct fix: before flagging an element, walk up its ancestor chain and skip the offender if any ancestor between it and <body> has a computed overflow-x that is NOT 'visible' (hidden/auto/scroll) AND that ancestor's own bounding rect right edge is <= the viewport width (i.e. the ancestor itself isn't already flagged as overflowing) — such an ancestor guarantees the descendant's excess scrollWidth is clipped, not painted. Keep the existing document-level `docOverflow` check unchanged (it's a real, ancestor-agnostic measure of actual page-level horizontal scroll and already correctly reports 0 for this page). Do NOT touch dashboard/app/components/DagFlow.tsx or dashboard/app/globals.css — this task confirms via investigation that no dashboard rendering code needs to change; the fix is purely in the check's own measurement logic. After fixing the heuristic, add the `claude-warmer-run` single-stage run-detail page as a `PAGES` entry in `dashboard/scripts/_dashboard-harness.mjs` (it currently only appears in `FLOWS` for visual-check, not in `PAGES` for mobile-check — this was the specific gap that exposed the false positive) and confirm it now passes cleanly. Add a short code comment at the ancestor-clip check in mobile-check.mjs explaining why it exists (referencing the React Flow internal-layer false positive) so a future reader doesn't strip it out as dead code.

## Done when
`node dashboard/scripts/mobile-check.mjs` reports the `claude-warmer-run` page (now present in PAGES) as PASS with 0 boundary-crossing elements, using the corrected ancestor-aware heuristic — not by loosening the assertion threshold or by excluding the page/selector from the check. Re-verify the fix is genuine, not a heuristic that now hides real bugs: temporarily (locally, not committed) widen one of the intentionally-overflowing test fixtures in _dashboard-harness.mjs's existing 'boundary-crossing' test content (there is deliberately-overflowing text seeded for check self-testing — grep for it) and confirm the check still correctly FAILS that genuine case, proving the ancestor-clip exemption only suppresses truly-invisible spills, not real ones; do not leave that temporary widen in the committed diff. `npx tsc --noEmit` and `npm test` pass (mobile-check.mjs/harness.mjs are plain scripts with no unit tests currently — this is a manual/scripted verification task per its facets, not `expectsTest`). Run the full `node dashboard/scripts/mobile-check.mjs` sweep across ALL existing PAGES entries and confirm everything that passed before still passes (no new false negatives introduced).
