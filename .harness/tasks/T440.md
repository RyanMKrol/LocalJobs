## Overview

Part of a repo-wide sweep giving every job a real paragraph-length `description` (surfaced on
`/jobs/[name]` and `/runs/[id]`) instead of a one-liner. This task covers the single job in the
`vercel-daily-redeploy` workflow. The mechanism was already built by the foundational task this
depends on — this is pure content authoring.

## Do

Rewrite the `description` of `src/workflows/vercel-daily-redeploy/vercel-redeploy.job.ts`
(`vercel-redeploy`) into a real 3-6 sentence paragraph. Currently: "Run \"vercel --prod --yes\"
directly in the ryankrol.co.uk checkout — a daily safety-net production deploy in case that repo's own
deploy task fails or a session forgets to author one." Expand it to explain why it runs the real
Vercel CLI directly (rather than a Deploy Hook — `ryankrol.co.uk` disconnected its Vercel Git
integration, so Deploy Hooks are no longer reliably viable), that it relies on the machine's existing
persistent Vercel CLI login (no credential to provision), the `RYANKROL_CO_UK_PATH` env var soft-skip
behavior, and its own internal 10-minute timeout-and-kill separate from the job's outer timeout. Ground
it in root `CLAUDE.md`'s `vercel-daily-redeploy` section and the job's own code. Plain prose only, no
markdown syntax.

## Done when

- The `vercel-redeploy` job has a real, accurate paragraph description grounded in its actual behavior
  — not a one-liner.
- `npx tsc --noEmit` and `npm test` are green.
