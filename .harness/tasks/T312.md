## Do

Context: this task depends on a prior backend task that added `GET /api/logs?level=&job=&workflow=&q=&windowHours=&before=&limit=` (`src/api/server.ts`), returning `{ logs: GlobalLogLine[], nextCursor: string | null }` where each `GlobalLogLine` is `{ id, ts, level: 'info'|'warn'|'error', message, source: 'job'|'workflow', jobName: string|null, workflowName: string|null, runId: string|null, workflowRunId: string|null }`. This task adds the OWNER-REQUESTED new top-level nav tab: a 'Logs' page pulling every log line from every job run AND every workflow run into one place, so errors anywhere are visible at a glance without drilling into individual run pages.

Anchor: the top nav lives in `dashboard/app/layout.tsx` (~line 57-68) as a plain `<nav>` of `<a href="...">Label</a>` links — `Overview` (`/`), `Workflows` (`/workflows`), `Services` (`/services`), `Backlog` (`/backlog`). Add a new `<a href="/logs">Logs</a>` link in the SAME nav, after `Backlog` (i.e. at the same level, last in the list) — this is a one-line addition, do not restructure the nav.

Do:

1. **`dashboard/app/lib/api.ts`**: add a `GlobalLogLine` interface matching the backend shape EXACTLY (field names verbatim: `id, ts, level, message, source, jobName, workflowName, runId, workflowRunId`), and a client helper on the `api` object, e.g.:
   ```ts
   globalLogs: (params: { level?: string; job?: string; workflow?: string; q?: string; windowHours?: number; before?: string; limit?: number }) =>
     get<{ logs: GlobalLogLine[]; nextCursor: string | null }>(`/api/logs?${buildQuery(params)}`),
   ```
   (write a small local query-string builder that omits undefined/empty values — check whether `api.ts` already has a helper like this for other multi-param endpoints before writing a new one; reuse if so.)

2. **New page `dashboard/app/logs/page.tsx`**: a client component (mirror the existing page structure/conventions in `dashboard/app/workflows/page.tsx` or `dashboard/app/backlog/page.tsx` — same panel/table CSS classes, same general layout) that:
   - Polls the new endpoint via the existing shared `usePoll` hook from `../ui` (see `dashboard/app/ui.tsx` ~line 251) — poll on an interval consistent with the app's other live-tail pages (the run/workflow-run detail pages poll at `1000`ms; a global aggregate query is heavier, so use a more modest interval, e.g. `5000`ms, matching the interval several other list-style pages already use — check nearby pages for the convention actually in use and match it rather than inventing a third value).
   - Renders filter controls that map 1:1 to backend query params: a **level filter** (info/warn/error, multi-select or three toggle pills — reuse the existing `.io-filter-bar`/`.io-filter-chip`-style pill pattern already used elsewhere in the dashboard for a consistent look, see `IoPanel` in `dashboard/app/workflow-runs/[id]/page.tsx` for the existing CSS class names to reuse rather than inventing new ones), a **job/workflow scope selector** (populate the job-name list from `GET /api/jobs` via the existing `api.jobs()` client call, and the workflow-name list from `GET /api/workflows` via the existing `api.workflows()` — do NOT invent a new endpoint for these, both already exist and are already used elsewhere in the dashboard), a **free-text search box** (debounce input before firing a new poll/query — don't fire a request on every keystroke), and an **'Errors only' quick-toggle** that is pure UI sugar for setting the level filter to `error` only (it does not need its own backend param — the idea explicitly asked for this as a convenience toggle, not a new filter dimension).
   - Default view on load: no level filter (all levels), no job/workflow scope, `windowHours` defaulting to a sensible recent window (24h) — expose this as a small window selector (e.g. 24h / 7d, matching the owner's stated options) that resets pagination when changed.
   - Renders the merged log lines newest-first, each showing: timestamp, a level badge/pill (reuse the existing colour convention from the per-run logs view — see `dashboard/app/runs/[id]/page.tsx` ~line 103's `lvl-${l.level}` class and the matching CSS in `globals.css`, do not invent new level colours), the source (job name or workflow name, as a clickable link to that run's detail page via `runId`/`workflowRunId` when present — `/runs/<runId>` or `/workflow-runs/<workflowRunId>`), and the message text.
   - A **'Load more'** button/control at the bottom of the list that calls the endpoint again with `before=<nextCursor>` and APPENDS the results to the currently displayed list (not a page replace) — hide/disable it when `nextCursor` is `null`. This is the explicit 'default recent window + explicit load-more control to go further back' behaviour the owner asked for.
   - An empty state ('No log lines match the current filters.') when `logs.length === 0`.

3. **`dashboard/app/layout.tsx`**: add the `<a href="/logs">Logs</a>` nav link as described above.

4. **`dashboard/app/globals.css`**: add whatever NEW classes are genuinely needed (e.g. a logs-page-specific layout wrapper) but REUSE existing pill/badge/panel classes wherever the shape already matches (per root `CLAUDE.md`'s shared-component reuse convention — do not duplicate `.pill`/level-badge styling). If a new enumerable label needs a pill, use the existing `<Pill>` component (`dashboard/app/components/Pill.tsx`) rather than a bare `<span className="pill ...">`.

5. **`dashboard/scripts/_dashboard-harness.mjs` (LIVING ARTIFACT — non-negotiable, see `.harness/CLAUDE.md`'s UI-task rules)**:
   - Add a synthetic fixture for the new endpoint in `fixtureFor(pathname)`: `if (pathname === '/api/logs') return { logs: [...], nextCursor: null };` — include a handful of synthetic `GlobalLogLine` rows covering BOTH `source: 'job'` and `source: 'workflow'`, at least one of each level (`info`/`warn`/`error`), so the baseline screenshot shows a realistic mixed feed with visible level colour variety. Place this fixture branch appropriately relative to the existing routes in the `if`-chain (sub-routes/more-specific paths must precede generic prefix matches — follow the existing ordering comment in that function).
   - Add `{ name: 'logs', path: '/logs' }` to the `PAGES` array (this IS the required update for adding a new page — do not skip it).
   - If the page has an interactive state worth confirming beyond the baseline load (e.g. the 'Errors only' toggle active, or a job/workflow filter applied, or the level-filter pills in an active state) add a `FLOWS` entry for it, e.g. `{ name: 'logs-errors-only', path: '/logs', actions: async (page) => { /* click the errors-only toggle */ await page.waitForTimeout(200); } }` — follow the existing `FLOWS` entries' shape/comments for the exact conventions (selectors, settle timing).

Do not touch anything outside the files listed in scope. The backend endpoint and its exact field names already exist from the prior task — do not re-implement or rename them here.

## Done when

- `npx tsc --noEmit` passes.
- `npm --prefix dashboard run build` succeeds.
- `node dashboard/scripts/mobile-check.mjs` passes (no horizontal overflow at phone width for the new Logs page — filter bar, table/list, and load-more control all fit or wrap correctly).
- `node dashboard/scripts/visual-check.mjs` passes AND you have LOOKED at the resulting screenshots in `dashboard/scripts/visual-out/` (gitignored) — specifically the new `logs` baseline screenshot (confirms the page renders: nav tab present and active, filter controls visible, a mixed feed of job- and workflow-sourced log lines with distinct level colours, load-more control present) AND any new `FLOWS` entry's screenshot — record in the worklog what you actually saw, per `.harness/CLAUDE.md`'s UI-task visual-confirmation rule (a UI element merely being present in the DOM but invisible has shipped before — T223 — do not skip this).
- The new top-level nav link ('Logs', `/logs`) appears in `dashboard/app/layout.tsx` at the same level as Overview/Workflows/Services/Backlog, and navigating to it renders the new page (no 404, no client error).
- `dashboard/scripts/_dashboard-harness.mjs` has: (a) a new `/api/logs` fixture in `fixtureFor`, (b) a new `{ name: 'logs', path: '/logs' }` entry in `PAGES`, and (c) if the page has any interactive filter state worth seeing, a matching `FLOWS` entry — this is required for a NEW page per the living-artifact rule, not optional.
- The level filter, job/workflow scope selector, free-text search, 'Errors only' quick toggle, and the recent-window default + 'Load more' pagination control are all present and wired to the real `GET /api/logs` params (verify this by reading the network calls the page makes against the real params documented in the backend task, not just that controls render).
- The page polls for updates on an interval (uses the shared `usePoll` hook — no bespoke polling logic reinvented).
- No new duplicate CSS for level-badge/pill styling — the page reuses the existing `.lvl-*`/pill conventions already established on the per-run logs view.
