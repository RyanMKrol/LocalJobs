## Do

Add a read-only "Notifications" column to the workflows overview/list page so the owner can see at
a glance which workflows have push notifications on/off, without opening each workflow's detail
page.

1. **API — `GET /api/workflows`** (`src/api/server.ts`, the handler at
   `method === 'GET' && parts[0] === 'api' && parts[1] === 'workflows' && parts.length === 2`,
   currently `const rows = listWorkflows().map((p) => ({ ...p, ...workflowView(p.name) })); return
   json(res, 200, { workflows: rows });`): this currently returns the RAW `notify_enabled` DB column,
   not the resolved effective value. Change the `.map` to compute `effective_notify_enabled` for each
   workflow the same way the single-workflow detail endpoint already does a few lines below (`GET
   /api/workflows/:name`): look up the workflow's `WorkflowDefinition` and call
   `effectiveWorkflowNotifyEnabled(wfDef)`, falling back to `p.notify_enabled !== 0` when no
   definition is found (mirror that endpoint's exact fallback logic), and include the result as
   `effective_notify_enabled` on each row in the `workflows` array response.
2. **Dashboard — `dashboard/app/workflows/page.tsx`**: add a new `<th>Notifications</th>` column,
   positioned right after the existing `Enabled` column, and a matching `<td>` per row rendering
   `<Pill kind={p.effective_notify_enabled ? 'on' : 'off'}>{p.effective_notify_enabled ? 'on' :
   'off'}</Pill>` — copy the exact pattern already used for the `Enabled` column's `<Pill
   kind={p.enabled ? 'on' : 'off'}>`. This is **read-only** (no click handler, no toggle) — editing
   still only happens on the workflow detail page's existing "Notifications" row
   (`dashboard/app/workflows/[name]/page.tsx`, `effective_notify_enabled` /
   `POST /api/workflows/:name/notify`, from T285). Do not add any click/edit behavior here.
3. **Hermetic test harness** — `dashboard/scripts/_dashboard-harness.mjs`: check whether its
   synthetic workflow-list fixture objects include an `effective_notify_enabled` field; if not, add
   it with at least one workflow `true` and one `false` in the fixture set, so the workflows-list
   `PAGES` screenshot exercises both the "on" and "off" states of the new column. No new interactive
   state is introduced (the column is read-only), so no `FLOWS` entry is needed.

## Done when

- `GET /api/workflows` returns `effective_notify_enabled` on every workflow object in the
  `workflows` array, computed via `effectiveWorkflowNotifyEnabled` (not the raw `notify_enabled`
  column) — verified by a unit/integration test that seeds a workflow with `notify_enabled_overridden
  = 0` and a manifest default of `false`, and asserts the list endpoint's `effective_notify_enabled`
  for that workflow reflects the manifest default rather than any stale raw column value.
- `dashboard/app/workflows/page.tsx` renders a "Notifications" column showing an `on`/`off` `Pill`
  per workflow, positioned after "Enabled", with no click handler attached (read-only).
- After `npm --prefix dashboard run build`, run `node dashboard/scripts/visual-check.mjs` and look at
  the workflows-list screenshot in `dashboard/scripts/visual-out/` — confirm the new "Notifications"
  column renders with visible on/off pills for at least one workflow of each state.
- Run `node dashboard/scripts/mobile-check.mjs` (after the same build) and confirm no new horizontal
  overflow is introduced by the added column at the phone-width (~402px) viewport.
- `dashboard/scripts/_dashboard-harness.mjs`'s workflow-list fixture includes at least one workflow
  with `effective_notify_enabled: true` and one with `false`.
- `npm test`, `npx tsc --noEmit`, and `npm --prefix dashboard run build` all pass.
