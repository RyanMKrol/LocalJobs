## Do

On a validation-gate detail page the **Produced →** and **→ Consumed** sides are rendered
as two `SideCard`s, but in practice they're almost always IDENTICAL: a gate is derived
from an upstream job's `produces[key]` and a downstream job's `consumes[key]`, and in this
codebase every gate references the SAME contract factory on both sides (e.g.
`fragranticaDataContract()` is both parse's `produces` and build's `consumes`), so both
panels show the same declared shape AND the same actuals — pure redundancy (the thing a
user noticed). Collapse the 1:1-identical case to a single panel, while KEEPING full
support for the asymmetric case (a future fan-in DAG — several producers feeding one
consumer — or any edge where `produces[key]` ≠ `consumes[key]`), which must still render
the two-sided diff. Decisions (interview 2026-06-23): collapse when identical, render the
asymmetric case as the full two-sided view, and make sure the framework genuinely supports
asymmetric/fan-in gates. Do all of:

1. **Detect "identical" server-side.** In `src/api/server.ts`, the gate inspection
   endpoints already return a `produced` and `consumed` side (each `{ shape, result }`):
   `GET /api/workflow-runs/:id/gates/:producer/:key` (run-scoped, with live actuals) and
   the run-agnostic `GET /api/workflows/:name/gates/:producer/:key` (shapes only). Add an
   `identical: boolean` to BOTH responses, computed by deep-comparing the two sides'
   DECLARED `shape` (summary + format + the `expectations` array by label+detail) and the
   artifact key. Base it on the declared shapes (the contract identity), NOT the live
   actuals. Treat a missing/absent shape on either side as NOT identical (so we never hide
   a side we can't confirm matches). Factor the comparison into a small pure, exported
   helper and unit-test it.

2. **Collapse the page when identical.** Update BOTH gate detail pages —
   `dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx` (run-scoped) and
   `dashboard/app/workflows/[name]/gates/[producer]/[key]/page.tsx` (definition-level) —
   so when `identical` is true they render ONE consolidated panel: the boundary
   (`producerJob ─[ key ]─▶ consumerJob` with direction), the single declared **Expected
   shape** (summary · format · the per-expectation list), and — on the run-scoped page —
   the artifact's actual per-expectation ✓/✗ + sample (from either side, they match). Keep
   the centre **Gate** block (key + description + state badge + violation-log link). When
   `identical` is FALSE, render the EXISTING two-sided `Produced → | Gate | → Consumed`
   layout unchanged, so an asymmetric gate shows both sides' distinct shapes/actuals.
   Reuse the existing `SideCard`/`pairExpectations`/`Mark` building blocks; add only the
   consolidated layout. Keep every existing link (producer/consumer/violation run logs),
   keep it mobile-responsive (the new single-panel layout must survive ~402px), and update
   `dashboard/app/globals.css` only as needed.

3. **Confirm + prove asymmetric / fan-in support in the framework.** `deriveGates`
   (`src/core/dag.ts`) is edge-scoped and `produces`/`consumes` are independent contracts
   the executor checks separately, so asymmetry SHOULD already work — verify it and make
   the SMALLEST change only if a real gap exists. Add tests that prove it end-to-end with
   synthetic fixtures: (a) a **fan-in** workflow (≥2 producer jobs whose outputs are both
   consumed by one downstream job) derives one gate per producer→consumer edge
   (`dag.test.ts`); (b) an **asymmetric** gate where the producer's `produces[key]`
   contract and the consumer's `consumes[key]` contract declare DIFFERENT shapes →
   `identical` is false, both checks run + are enforced by the executor, and the inspection
   endpoint returns both distinct sides (`server.test.ts` and/or
   `workflow-executor.test.ts`). Don't wire any asymmetric contract into the real
   places/perfumes jobs — keep the one-factory-per-key convention there; the asymmetry is
   exercised only by test fixtures.

4. **Docs.** Update `CLAUDE.md` (the gate detail page collapses the producer/consumer
   sides to one panel when their contracts are identical — the normal case — and falls
   back to the two-sided view for asymmetric gates, e.g. a fan-in DAG; `identical` comes
   from the gate inspection API) and `README.md`'s gate-page description to match.

## Done when

A gate whose producer and consumer reference the same contract (every gate in the
places/perfumes workflows today) renders as a SINGLE consolidated panel — boundary +
one Expected shape + the actual ✓/✗ + sample — with no duplicated producer/consumer
panels, on both the run-scoped and definition-level gate pages; an asymmetric gate
(different `produces[key]` vs `consumes[key]` shapes) still renders the full two-sided
diff. The gate inspection endpoints return an accurate `identical` flag (a deep shape
compare, exported + unit-tested). Tests prove fan-in gate derivation and an asymmetric
gate are handled (derived, both checks enforced, `identical:false`), and the framework
needed no capability removed to do it. CLAUDE.md + README document the collapse +
asymmetry behaviour. `npx tsc --noEmit`, `npm test`, `npm --prefix dashboard run build`,
and `node dashboard/scripts/mobile-check.mjs` are all green.
