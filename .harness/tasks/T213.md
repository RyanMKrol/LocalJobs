## Do

Make the workflow-card **status pill** always render BELOW the workflow title on the overview page,
consistently across every card. In `dashboard/app/page.tsx` (~lines 177–197) each workflow card
renders `<div className="row">` containing the title `<a><strong>{p.name}</strong></a>`, a
`<div className="spacer" />`, then the status `<span className="badge …">`. The flex row + spacer
keeps the badge on the title's line, so it only drops below when the title is long enough to wrap —
giving inconsistent layout (short titles like `perfumes`/`places` show the pill inline; long ones
like `missing-tv-seasons` push it below). Move the status badge OUT of the title's flex row so it
always sits on its own line beneath the title, for every card. Pure dashboard presentation change
(`dashboard/app/page.tsx` + `dashboard/app/globals.css` if a style rule is needed); keep the badge's
link to its run and its status colouring. Keep mobile-responsive.

## Done when

- On the overview page, every workflow card shows its status pill BELOW the title — never inline to
  the right — regardless of title length. Verify on the running dashboard that short- and
  long-titled cards both render the pill below the title.
- The badge still links to the last run and keeps its status colour.
- `node dashboard/scripts/mobile-check.mjs` passes (no phone-width overflow) and
  `npm --prefix dashboard run build` is green.
