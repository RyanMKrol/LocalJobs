## Do

The Services-page "Consumers of <service>" popup (the modal opened by clicking a service on
`/services` to see which workflows/jobs use it — built in T186) has poor spacing: there's no padding
inside the modal, the **Close** button is jammed into the top corner, and the text content runs right
up to the modal's edges. Add proper internal padding/spacing so the content breathes — padding inside
the modal body, breathing room around the Close button (so it isn't flush in the corner), and the
consumer list not touching the side walls. The modal reuses the shared `.db-modal` / modal chrome in
`dashboard/app/globals.css`; prefer fixing it via the consumers-modal's own class so other modals are
unaffected (or, if the gap is in shared chrome, verify other modals still look right).

## Done when

- The consumers popup on `/services` has visible internal padding: text/list no longer touches the
  modal edges, and the Close button has breathing room (not pinned in the corner).
- No regression to other modals that share the chrome (spot-check the Stuck popover / any other
  `.db-modal`).
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green at phone width.
