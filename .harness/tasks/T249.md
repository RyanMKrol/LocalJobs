## Do

Add clickable **column-header sorting** to the recommendation tables on the workflow detail page
(`dashboard/app/workflows/[name]/page.tsx`). Apply it to BOTH the **movie** recommendations table
(`MovieRecsManager`) and the **TV** recommendations table (the tv-recs equivalent manager) — both
render TMDB-rated rows (columns include FILM/SHOW · YEAR · GENRE · LENS · TMDB). Clicking a column
header sorts by that column; clicking again toggles ascending/descending. Default the table to
**TMDB descending** (highest-rated first) on load, and support sorting/grouping by **lens** so
same-lens picks cluster together. The row data is already present client-side (each rec carries
title/year/genre/lens/tmdbRating). Keep the sort client-side (no API change).

## Done when

- On the movie-recommendations detail page, the Recommendations table's columns are click-to-sort
  (toggle asc/desc), it defaults to TMDB descending, and sorting by lens groups same-lens picks.
- The same sortable behaviour works on the TV recommendations table.
- It stays mobile-responsive (no new phone-width overflow; the `@media (max-width: 640px)` block) and
  theme-variable-driven.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build; if the recs tables aren't already
  exercised by a `PAGES`/`FLOWS` entry with rec rows, add fixtures/a flow in
  `dashboard/scripts/_dashboard-harness.mjs` so a screenshot shows the sortable recs table, and
  confirm the sorted order renders. The harness is updated to reflect the change.
