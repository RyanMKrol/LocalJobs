## Overview
src/db/store.ts is the repo's largest file at 2,152 lines with ~131 exports spanning ≥8 unrelated domains (jobs, runs/logs, work-items+lineage, stuck/ignored, usage meter, workflows, workflow-runs, workflow logs, services+cache, admin resets). Split it by domain into src/db/store/*.ts and turn src/db/store.ts into a pure barrel (`export * from './store/runs.js'; …`) so all ~60 existing import sites and re-exports stay byte-for-byte unchanged.

## Do
Create src/db/store/{jobs,runs,workItems,workflows,services,admin}.ts (choose the domain cut along the existing `// ---- <domain> ----` section comments in store.ts: jobs = lines ~33-108, runs = ~110-421, workItems = ~423-1060 incl. work-items/lineage/stuck/ignored, and split the remainder — usage meter ~1062-1129, workflows+workflow-runs+workflow-logs ~1131-onward, services+service_usage+service_cache meter, and admin resets — into workflows.ts, services.ts, admin.ts as fits). Each sub-file imports the shared singleton `db` from `../index.js` (currently `./index.js`) plus whatever types/helpers it uses; move `toStoredPath` and any other shared private helper into a store/lib.ts or the file that owns it and import it where needed. Rewrite src/db/store.ts to ONLY `export * from './store/<domain>.js'` for each file — zero logic in the barrel. This is a PURE MOVE: no signature, SQL, or behavior changes whatsoever. Land this as its own commit, NOT bundled with any behavioral fix (per R03's sequencing note). Update the CLAUDE.md 'File map' row for `src/db/store.ts` to describe the new store/ folder + barrel.

## Done when
src/db/store.ts is a barrel of `export * from './store/*.js'` lines only; every symbol previously importable from '../db/store.js' still is (grep the ~60 import sites — none changed); `npm test` (the ~1,473-line store.test.ts + ~1,899-line server.test.ts pin every behavior) and `npx tsc --noEmit` are green with zero behavioral diff; CLAUDE.md file map updated.
