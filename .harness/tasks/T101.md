## Do

On the validation-gate detail page, the middle 'Gate' container (the gate-center card) renders its code-shaped content — notably the artifact key (gate.key, ~line 148) — as plain mono text, while the producer/consumer SIDE cards now use a dedicated code-block style (introduced by T088). Make the middle Gate card use the SAME code-block styling for its code-shaped content (the artifact key, and any other code/JSON-shaped values it shows) so all three containers read consistently and the gate is easy to read. Reuse the shared code-block class from T088 rather than inventing a new one; keep the plain-English gate description as prose. NOTE: harmonise with T088/T089/T096 which also touch this page.

## Done when

The middle Gate container uses the same code-block styling as the producer/consumer side cards for its code-shaped content (artifact key etc.); the three containers read consistently; plain-English text stays prose; dashboard build passes and mobile-check stays green.
