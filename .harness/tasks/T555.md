## Overview
Delete the never-imported MarkdownModal.tsx, port its richer renderFmValue/isFmEmpty frontmatter rendering (null-highlighting + array-joining) into OutputRenderer's MarkdownOutputBody so the behavior lives on in the code that's actually used, add a small OutputRenderer.test.ts assertion for it, and correct CLAUDE.md which wrongly lists MarkdownModal as a live component.

## Do
1. Copy the isFmEmpty + renderFmValue helpers (MarkdownModal.tsx:26-42) into dashboard/app/components/OutputRenderer.tsx and use renderFmValue in MarkdownOutputBody's frontmatter `<dd className="md-fm-val">` cell instead of the bare `{v}` (line ~44). Ensure the `md-fm-null` class already exists in globals.css (it does — used by the old modal) so the null placeholder styles correctly. 2. Delete dashboard/app/components/MarkdownModal.tsx entirely (confirm zero importers first). 3. In CLAUDE.md, remove/correct the shared-components-section lines that document MarkdownModal as live (the '<MarkdownModal>' bullet ~1285-1290 and the T458 references at ~1282 that mention forcing content through MarkdownModal) — reword so the docs match reality (renderFmValue now lives in OutputRenderer; there is no live MarkdownModal). 4. Add an assertion to dashboard/app/components/OutputRenderer.test.ts covering the ported behavior.

## Done when
MarkdownModal.tsx is deleted and grep finds zero importers/references to MarkdownModal anywhere (dashboard/ + CLAUDE.md), excluding the self-clone. renderFmValue/isFmEmpty exist ONLY in OutputRenderer.tsx (and its test). The new OutputRenderer.test.ts assertion renders a markdown-format output whose frontmatter contains (a) an empty/null value and (b) a JSON-array value, and asserts the rendered HTML shows the `md-fm-null` 'null' placeholder for the empty value AND the array joined as comma-separated text (not raw `["a","b"]`). npx tsc --noEmit, the OutputRenderer.test.ts run, and npm --prefix dashboard run build are all green. CLAUDE.md no longer describes MarkdownModal as a live component. Visual-check screenshots confirm the output popover frontmatter still renders.
