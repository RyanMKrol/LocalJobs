## Do

Make remote dashboard access friendlier than typing the tailnet IP:port on mobile — a Tailscale MagicDNS hostname, a `tailscale serve` nice URL, or a phone home-screen shortcut. Partly operational (Tailscale admin / the machine), not CI-verifiable.

## Done when

The dashboard is reachable from the phone via a friendly alias/shortcut (human confirms).
