## Overview
Owner's decision (confirmed directly, no needs-human task required): unify this fix with the existing `WorkflowDefinition.outputJob` mechanism (T348) rather than either renaming the ledger everywhere or introducing a brand-new, separate aliasing concept — extend `outputJob`'s reach so it ALSO governs the run-scoped Stage I/O endpoint, not just the workflow-definition-level Output section it covers today.

## Do
Confirmed via src/core/recommender/notify.ts:102 (`markWorkItem(domain.recsJob, recKey(r.tmdbId), 'success', {...})`) and src/workflows/movies/recs.ts:26 / src/workflows/tv-recs/recs.ts:25 (`export const RECS_JOB = 'movie-recs'` / `'tv-recs'`) that the notify ledger is keyed by the DECOUPLED logical name `domain.recsJob`, which is ALSO the job name the ignore/un-ignore API endpoints reference directly. Confirmed via src/api/server.ts's stage-io handler (`GET /api/workflow-runs/:id/stage-io?job=<job>`, ~line 1665-1677) that `jobParam` is matched literally against the DAG's own member job names — for the notify stage this is always the literal DAG name 'movie-recs-notify'/'tv-recs-notify', which never equals 'movie-recs'/'tv-recs', so `stageIoLists([jobParam], predecessors, run.id)` always queries the wrong job_name. The EXISTING `WorkflowDefinition.outputJob` field (T348, src/core/types.ts:240) only affects the workflow-DEFINITION-level Output section (`workflowTerminalItems`, GET /api/workflows/:name/output-items) today — the run-scoped stage-io endpoint does NOT currently consult it at all (confirmed: it uses the literal jobParam straight into stageIoLists). Owner's chosen fix: EXTEND `outputJob`'s reach so the SAME field also governs the run-scoped `/api/workflow-runs/:id/stage-io` endpoint — when a DAG member's outputJob is set, the stage-io handler should query the ledger under that outputJob name instead of the member's own literal job name, for BOTH that member's own tab lookup and anywhere else stageIoLists is driven by a specific job name. Wire movies.workflow.ts / tv-recs.workflow.ts's notify stage member (movie-recs-notify / tv-recs-notify) to declare `outputJob: 'movie-recs'` / `outputJob: 'tv-recs'` respectively. No data migration needed — the existing 156/134 ledger rows stay under their current job_name, only the LOOKUP changes to find them. This unit does NOT touch the actual recommendation logic in notify.ts's digest/mark-notified behavior, and does NOT touch the ignore/un-ignore API endpoints (they already correctly use 'movie-recs'/'tv-recs' directly) — only how the run-scoped Stage I/O panel discovers a stage's ledger rows when outputJob is set.

## Done when
Opening a movie-recommendations or tv-recommendations workflow run's Stage I/O panel for the movie-recs-notify / tv-recs-notify stage shows real Outputs rows (the actual notified recommendations from that run), not an empty list, for a run that genuinely notified recommendations — achieved by the stage-io endpoint consulting the member's `outputJob` field when set, with no data migration. The workflow-definition-level Output section (T348's original outputJob consumer) continues to work exactly as before — no regression there. The ignore/un-ignore dashboard controls (POST /api/movie-recs/:tmdbId/ignore etc.) continue to work exactly as before. Add/extend an API-level or store-level test proving the stage-io endpoint now returns the notify stage's real ledger rows for a fixture run via its outputJob.
