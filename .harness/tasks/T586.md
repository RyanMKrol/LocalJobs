## Overview
Re-attempt of T565, blocked after exhausting both model tiers (sonnet x2, opus x2) — all 4 attempts produced verified-correct code (tsc clean, tests green, dedup complete, behavior preserved per the audit transcript) but were audit-FAILED every time on the exact same criterion: the spec's own '## Done when' requires updating the touched workflow CLAUDE.mds + the root CLAUDE.md file-map row, but the task's `scope` array never listed a single CLAUDE.md path. The builder hit the harness scope gate when it tried to touch documentation, skipped the doc updates citing that gate, and self-reported done anyway — an unfixable trap under the original scope, repeated identically at both tiers. This re-attempt is spec-identical except `scope` now explicitly includes the 8 CLAUDE.md files the Done-when requires (root CLAUDE.md + the 7 touched workflows' own CLAUDE.md). The underlying duplication is still real and unaddressed: a repo-wide grep confirms 6 workflows still re-declare `interface PlexAllResponse<T>` inline, and TMDB-GUID extraction is still defined independently in `missing-tv-seasons/plex.ts` and `plex-language-fix/lib.ts` (cross-imported by movies/tv-recs rather than sharing one canonical home). The in-flight Plex-service migration this was sequenced after (T511/T513-T517, later re-verified by T578) has since landed — `plex-client.ts` now routes every Plex read/mutation through `callService('plex', ...)`, so there is no remaining coordination hazard; this can build directly against the current plex-client.ts.

## Do
Identical substantive work to T565's original '## Do' section: add to src/core/plex-client.ts (1) `export interface PlexAllResponse<T> { MediaContainer?: { Metadata?: T[] } }`, (2) `export extractTmdbId(item): number | null` — reads a Plex metadata item's Guid array and returns the numeric id behind its `tmdb://` GUID, or null when absent (a title with no tmdb:// GUID must be excluded/flagged exactly as today, never guessed), and (3) `export fetchSectionMetadata<T>(section, opts?)` — a thin wrapper over the existing plexGet for the `/library/sections/<n>/all` pattern (optionally `?includeGuids=1` / `?type=4`), routed through `callService('plex', ...)` consistent with how every other plex-client read is now gated (per T578's landed migration) — preserve exact cacheKey semantics call sites currently rely on (an uncached input-key probe stays uncached via `cacheKey: null`; a cached section read keeps the `plex:<path>` cache key). Then replace the per-workflow re-declarations and inline GUID extraction with imports of these shared exports across all 8 scoped call sites: movies/stages/snapshot.ts + movies/movies.ts, tv-recs/stages/tv-snapshot.ts (+ tv-recs' tv-shows.ts GUID extraction), missing-tv-seasons/stages/snapshot.ts + missing-tv-seasons/plex.ts, missing-movies/stages/snapshot.ts, plex-profiles/stages/build.ts, plex-space-saver/stages/scan.ts, and plex-language-fix/lib.ts. Do NOT change which titles are matched or how a missing GUID is handled — pure structural dedup, no behavior change. Add/extend src/core/plex-client.test.ts to cover extractTmdbId (a metadata item WITH a tmdb:// guid returns the id; one WITHOUT returns null) and the PlexAllResponse<T> typing round-trip. THEN — this time actually in scope — update each of the 7 touched workflows' own CLAUDE.md wherever it enumerates what the shared plex-client provides, and update the root CLAUDE.md's `src/core/plex-client.ts` file-map row (line ~298) to mention the three new shared exports (PlexAllResponse<T>, extractTmdbId, fetchSectionMetadata) alongside the existing plexGet/tmdbGet/resolvePlexHost/plexPutStreams/triggerButlerBackup list.

## Done when
src/core/plex-client.ts exports PlexAllResponse<T>, extractTmdbId, and fetchSectionMetadata<T> (fetchSectionMetadata routed through callService('plex', ...)). A repo-wide grep (excluding gitignored data/ trees) finds exactly ONE `interface PlexAllResponse` declaration (the core export) and no per-workflow re-declarations; all 6 snapshot/scan/build call sites import the shared type. The tmdb:// GUID extraction is defined exactly once (core's extractTmdbId); missing-tv-seasons/plex.ts and plex-language-fix/lib.ts no longer define their own copies, and movies/movies.ts + tv-recs/tv-shows.ts import from core (directly or via a thin re-export) rather than from a workflow-local implementation. Every migrated call site matches the SAME titles / handles a missing tmdb:// GUID identically to before (no behavior change). src/core/plex-client.test.ts covers extractTmdbId (present vs absent GUID) and the PlexAllResponse<T> round-trip. The root CLAUDE.md's plex-client.ts file-map row is updated to list the three new exports, AND every one of the 7 touched workflow CLAUDE.md files that mentions what the shared plex-client provides is updated to match (a workflow CLAUDE.md that never enumerated plex-client's exports needs no change — don't invent a mention where none existed). `npx tsc --noEmit` and `npm test` both green.
