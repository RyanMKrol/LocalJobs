## Do

Fix how the workflow DAG graph (`dashboard/app/components/Dag.tsx`) renders **non-adjacent /
branch-bypassing edges**, which currently misrepresent a workflow's structure. Diagnosed via T155 on
the movies workflow:

The layout is wave-based (a node's column = its distance from the root). An edge that **skips a
wave** can't sit on a single inter-wave arrow, so the Dag falls back to rendering that edge's gate
chip UNDER the consumer node with **no connector line**. Concrete case: `franchise-gaps` (wave 2) →
`movie-gaps-notify` (wave 4) skips `rec-merge`'s wave (3). So the graph shows `franchise-gaps`
co-located with the `rec-*` branches (which DO feed `rec-merge`) plus only a small "franchise-gaps"
gate chip tucked under `movie-gaps-notify`. It reads as if `franchise-gaps` feeds `rec-merge` — but
it actually **bypasses** `rec-merge` straight to notify. (The parallelism is represented fine; the
EDGES aren't.)

Make every producer→consumer edge **visibly drawn**, including skip-wave ones, so a bypassing branch
is unambiguous:
- Draw non-adjacent edges as actual **connector lines** — e.g. an SVG path layer routing from the
  producer node to the consumer node, visibly going AROUND the intervening wave/column — not just a
  gate chip under the consumer. The `franchise-gaps → movie-gaps-notify` edge should be a visible
  line that clearly bypasses `rec-merge`.
- Place the **gate mark** (the hardcoded `key` style from T133) ON its edge, including the
  non-adjacent ones, so the gate sits on the connector it guards rather than orphaned under the
  consumer node.
- Keep the existing **adjacent-wave arrows + their gate marks unchanged**.
- Stay readable at BOTH desktop and phone (~402px) widths — connector routing must not overlap
  nodes/labels or overflow; the phone layout stacks vertically, so ensure skip-edges are still
  clearly represented there too.
- Don't break the wave layout's correctness (columns = depth). This is about drawing ALL edges so
  independent/bypassing branches read correctly. (A small layout tweak that makes a pure-bypass node
  clearer — e.g. its own lane — is acceptable if it doesn't regress other workflows' graphs.)
- **Verify against the real movies run graph:** `franchise-gaps` visibly connects to
  `movie-gaps-notify`, bypassing `rec-merge`, and no longer appears to feed `rec-merge`. Also
  confirm the linear workflows (places/perfumes/TV) — which have no skip-wave edges — render exactly
  as before.

## Done when

The workflow DAG graph draws every producer→consumer edge as a visible connector — including
non-adjacent/skip-wave edges like `franchise-gaps → movie-gaps-notify` — with the gate mark sitting
on its edge, so a bypassing branch no longer looks like it feeds the intervening stage; adjacent-wave
arrows are unchanged; linear workflows render as before; it stays readable + correct at desktop and
phone widths; `npm --prefix dashboard run build` passes and `node dashboard/scripts/mobile-check.mjs`
stays green.
