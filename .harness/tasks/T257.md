## Do

Slim the root **`README.md`** down to only what a user/operator actually wants to read: what the app
is, how to install/run it, ports/services, the job list at a glance, and how to add a job at a high
level. The biggest offender is the **worked-example workflows** section (places / perfumes /
missing-tv-seasons / movie-recommendations / tv-recommendations) — currently a long wall of text.
Replace it with a **one-line summary per workflow** (what it does) and **drop the deep mechanics** from
the README. The deep architecture/conventions/data-flow already live in `CLAUDE.md`, so nothing
important is lost — do NOT create a new docs file and do NOT move the dropped detail into CLAUDE.md
(it's already covered there); just trim. While trimming, if any README content duplicates CLAUDE.md,
dedupe in favour of CLAUDE.md.

The README must stay **truthful** after trimming — every remaining statement (install steps, ports,
commands, job list) must still accurately describe the app as it is.

## Done when

- `README.md` is materially shorter, with the worked-example workflows reduced to one line each and
  their deep mechanics removed (not relocated — CLAUDE.md already holds the architecture).
- The README still accurately describes: what the app is, how to install/run it, the ports/services,
  the job list at a glance, and how to add a job at a high level — verified by re-reading it against
  the actual app (no stale or now-false statements).
- No new doc file is created; any README/CLAUDE.md duplication is deduped in favour of CLAUDE.md.
