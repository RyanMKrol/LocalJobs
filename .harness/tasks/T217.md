## Do

Build the **`tv-rec-merge`** stage for the `tv-recommendations` workflow — the TMDB-verifying,
deduping, balancing, quality-gating merge of the 8 branches' raw suggestions — mirroring
`src/jobs/movies/stages/merge.ts` (`rec-merge`, T146/T162).

Create `src/jobs/tv-recs/stages/tv-rec-merge.job.ts` (name `tv-rec-merge`) + `tv-rec-merge.ts` +
tests. It must, mirroring the movies merge:
- Gather all 8 branches' `RawSuggestion[]`, cross-branch **dedupe** (merge lenses for the same show),
  and **TMDB-verify** each suggestion using the existing TMDB client `src/jobs/plex/tmdb.ts` (resolve
  to a real TV show, capture `vote_average` + `vote_count`); drop anything that isn't a real TV show,
  is ALREADY owned (in the snapshot's owned TMDB-id set), or has been recommended before.
- **Never re-recommend:** key the dedupe ledger by the show's TMDB id under a `tv-recs` ledger job
  (mirror the movies `RECS_JOB`/`recKey` pattern in `src/jobs/movies/recs.ts`); a `success` row =
  already recommended. Respect owner ignore-to-suppress (`ignoreSurfacedItem('tv-recs', <tmdbId>)`).
- Enforce the **quality bar** from config: TMDB rating ≥ `recsMinRating` (7.0) with ≥ `recsMinVotes`
  (50) votes; **balance** per genre (`recsGenreCap`); **target** ≥ `recsTarget` (~15) via a bounded
  re-prompt top-up loop (mirror the movies top-up: `recsTopUpRounds`/`recsTopUpConcurrency`).
- Write the verified, balanced `recommendations.json` to `data/out/`.

Then add `{ job: 'tv-rec-merge', dependsOn: [ <the 8 branch job names> ] }` to
`src/jobs/tv-recs/tv-recs.workflow.ts`.

## Done when

- `tv-rec-merge` produces `data/out/recommendations.json` containing only TMDB-verified, un-owned,
  not-previously-recommended TV shows meeting the rating≥7.0/≥50-votes bar, deduped across branches
  and genre-balanced, targeting ~`recsTarget` via the bounded top-up loop.
- A show ignored via `ignoreSurfacedItem('tv-recs', <tmdbId>)` and a previously-recommended show are
  both excluded from the merge output.
- `tv-recs.workflow.ts` wires `tv-rec-merge` after the 8 branches; registry loads cleanly.
- `expectsTest`: `tv-rec-merge.test.ts` covers (from synthetic suggestion + stubbed-TMDB fixtures,
  scratch DB only) the dedupe, the quality-bar filter, the owned/already-recommended/ignored
  exclusions, and the genre balancing — mirroring `merge.test.ts`/`merge-quality.test.ts`.
- `npm test` and `npx tsc --noEmit` are green; a tiny live TMDB verification run stays within quota.
