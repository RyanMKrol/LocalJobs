## Do

Surface the **TV recommendations** in the dashboard on the `tv-recommendations` workflow detail page,
mirroring the movie-recommendations surfacing pattern established by T209 (this task `dependsOn` T209
so it reuses whatever generalized recs-surfacing approach T209 lands, and T218 so the data exists).

Build, mirroring the movies recs surfacing:
- **API (`src/api/server.ts`):** `GET /api/tv-recs` — read `tv-recs`'s `recommendations.json`, overlay
  each rec's `ignored`/`notified` ledger status, return empty (HTTP 200) when absent. Plus
  `POST /api/tv-recs/:tmdbId/ignore` calling `ignoreSurfacedItem('tv-recs', <recKey(tmdbId)>)`. Model
  on the movie-recs endpoints from T209 (themselves modelled on `/api/movie-gaps` at
  `src/api/server.ts:734`/`:762`), behind the existing loopback/token mutation guard.
- **Client (`dashboard/app/lib/api.ts`):** `tvRecs()` + `ignoreTvRec(tmdbId)` + response type.
- **UI (`dashboard/app/workflows/[name]/page.tsx`):** render a TV recommendations section on the
  `tv-recommendations` workflow detail page (gated to that workflow name, like the existing
  movies/plex manager sections), listing each rec (title, year, lens/reason, TMDB link) with a
  per-item `✕ Ignore` + confirm, mirroring the movies recs section.

Keep mobile-responsive (`@media (max-width: 640px)` in `dashboard/app/globals.css`) and
theme-variable-driven.

## Done when

- `GET /api/tv-recs` returns the TV recs overlaid with `ignored`/`notified` (empty when the workflow
  hasn't run); `POST /api/tv-recs/:tmdbId/ignore` ignores a rec via `ignoreSurfacedItem('tv-recs', …)`
  and it leaves the active list.
- The `tv-recommendations` workflow detail page shows the recommendations with working per-item Ignore;
  verify on the running dashboard (load `/workflows/tv-recommendations`, ignore a rec, confirm it
  disappears).
- `expectsTest`: a unit test in `src/api/server.test.ts` covers `POST /api/tv-recs/:id/ignore`
  (scratch DB only), mirroring the movie-recs test from T209.
- `npm test`, `npx tsc --noEmit`, `npm --prefix dashboard run build`, and
  `node dashboard/scripts/mobile-check.mjs` are all green.
