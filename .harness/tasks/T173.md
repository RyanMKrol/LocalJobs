## Do

On the Backlog page (`dashboard/app/backlog/page.tsx`), the **Done** section's list has a bottom
margin/padding that's noticeably **thicker than the spacing between rows** — there's an oversized gap
below the last row (T167 in the screenshot) before the panel's bottom edge, which looks unbalanced
against the consistent row rhythm above it.

- Find the extra bottom margin/padding on the Done list container (or the last row / list wrapper)
  and reduce it so the space below the last row **matches the inter-row spacing** (and the list's top
  inset), so the list ends cleanly and evenly.
- Check the other section lists (Harness-buildable / Needs-a-human) for the same inconsistency and
  fix them the same way for consistency.
- Don't clip rows or change the rows' own internal padding — only the trailing/bottom gap. Keep it
  correct under the default + joyful themes. Mobile-responsive.

## Done when

The Backlog Done list (and the other section lists) has bottom spacing consistent with its inter-row
spacing — no oversized gap below the last row; it looks even under the default + joyful themes; `npm
--prefix dashboard run build` passes and `node dashboard/scripts/mobile-check.mjs` stays green.
