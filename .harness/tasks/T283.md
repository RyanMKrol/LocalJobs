# T283 spec

## Do

Change the "waiting on dependencies" filter in `dashboard/app/backlog/page.tsx` so a buildable task
is only classified as "waiting" when at least one of its unmet `dependsOn` ids belongs to a task with
`gate === 'needs-human'`. Currently (`page.tsx` around lines 280-283):

```ts
const buildable = tasks.filter((t) => t.status !== 'done' && t.status !== 'failed' && t.gate == null);
const ready = buildable.filter((t) => (t.dependsOn ?? []).every((dep) => doneIds.has(dep)));
const waiting = buildable.filter((t) => (t.dependsOn ?? []).some((dep) => !doneIds.has(dep)));
```

`waiting` includes any task blocked by ANY unmet dependency, even a purely buildable one the loop
will get to on its own — that's noise the owner wants removed. `GET /api/backlog` already returns
every task (including ones only referenced as a dependency) in the flat `tasks` array with its own
`gate` field intact (`src/api/server.ts`'s `readBacklog`, spread via `...(t as object)`) — no API
change is needed.

Build an id→task lookup map from the full `tasks` array, then:
1. Tighten the `waiting` filter so a task only counts as waiting when ≥1 of its unmet `dependsOn` ids
   resolves (via the lookup) to a task whose `gate === 'needs-human'`. A task blocked solely by
   unmet buildable (`gate == null`) dependencies should no longer appear in "Waiting on dependencies"
   (it also should NOT move to "Ready" — it still has a genuinely unmet dependency, it just stops
   appearing in either visible section until that blocker resolves, which is the desired "noise
   removed" behavior).
2. Tighten the unmet-dep-id list computed around line 360
   (`(t.dependsOn ?? []).filter((dep) => !doneIds.has(dep))`, passed to `CollapsibleRow` as
   `unmetDeps` — this drives the "needs:" pill row) so it also only lists needs-human-gated unmet
   ids, not every unmet id. A task with both an unmet needs-human dep AND an unmet buildable dep
   should still appear in "Waiting on dependencies", with its "needs:" pill showing only the
   needs-human one(s).

## Done when

- `dashboard/app/backlog/page.tsx`'s `waiting` computation only includes a buildable task when ≥1 of
  its unmet `dependsOn` ids resolves (via a `tasks` id→task lookup, NOT just `doneIds`) to a task
  with `gate === 'needs-human'`. A task blocked solely by unmet buildable (`gate == null`)
  dependencies no longer appears in the "Waiting on dependencies" section.
- The "needs:" pill list rendered for a still-waiting task (the `unmetDeps` array passed to
  `CollapsibleRow`) contains only the needs-human-gated unmet dependency ids, not every unmet id.
- `dashboard/scripts/_dashboard-harness.mjs`'s synthetic `tasks` fixture (around line 126) is updated
  so this change is actually exercised and visible in screenshots — it currently has T040 depending
  on `['T001', 'T050']` where T001 is a plain buildable task (`gate: null`), which is exactly the
  "noise" case this task removes:
  - Add a new needs-human fixture task, e.g. `{ id: 'T098', title: 'A human-gated blocker', status:
    'pending', gate: 'needs-human', dependsOn: [], tags: ['infra'], do: '...', doneWhen: '...' }`.
  - Change T040's `dependsOn` from `['T001', 'T050']` to `['T098', 'T050']`. T001 stays in the
    fixture unchanged (still a valid standalone "Ready" section example) but is no longer anyone's
    dependency. T040 should now appear in "Waiting on dependencies" ONLY because of its unmet
    dependency on the needs-human T098, and its "needs:" pill should show only `T098`.
- The `backlog-dep-click` FLOW in the same file currently does:
  `await page.click('.dep-id-link:has-text("T001")')` then
  `await page.waitForSelector('#task-T001', { state: 'visible', ... })`, with a comment saying
  "T001 is in Ready (already open), should expand". Update this step to click
  `.dep-id-link:has-text("T098")` and wait for `#task-T098` to become visible instead — T098 renders
  under the "Needs a human" section (already `<details open>` by default), not Ready. Update the
  inline comment to match. The rest of the flow (expanding T040 in place, then clicking the T050 dep
  link to reach the Done section) is unaffected and should be left as-is.
- `npx tsc --noEmit` passes and `npm test` passes (repo root).
- `npm --prefix dashboard run build` succeeds.
- `node dashboard/scripts/mobile-check.mjs` passes for all pages, including `/backlog`.
- `node dashboard/scripts/visual-check.mjs` was run after the build, and the screenshots in
  `dashboard/scripts/visual-out/` (`backlog.png` / `backlog-expanded.png` and
  `backlog-dep-click.png`) were inspected and confirm: the "Waiting on dependencies" section shows
  T040 (blocked by the needs-human T098) and does NOT show any task blocked only by a buildable
  dependency, and the dep-click flow screenshot shows the T098 pill navigating correctly into the
  "Needs a human" section.
