## Do

**FRONTEND half** of decoupling the workflow Output section from markdown-only — **depends on T262**
(the declared output form + the API that serves a non-markdown form's data).

Make the workflow detail page's Output section (`WorkflowOutputSection` in
`dashboard/app/workflows/[name]/page.tsx`, T205) **dispatch rendering by the item's declared form**
instead of assuming every output is markdown. `markdown` keeps its current `react-markdown` popover
renderer (unchanged); the dispatch must make adding a NEW form's renderer (e.g. a structured size
table for T263) a clean, localized addition. Wire the client fetch in `dashboard/app/lib/api.ts` to
request the declared form's data from the T262 endpoint.

This is a refactor of the SHARED output rendering — it must **not regress existing markdown outputs**:
places and perfumes must render their `data/out/` markdown profiles in the Output section exactly as
they do now. Scope: `dashboard/app/workflows/[name]/page.tsx`, `dashboard/app/lib/api.ts`. Do not
change the backend endpoint (that's T262).

## Done when

- The Output section dispatches to a renderer by the item's declared form; `markdown` is one form with
  its current behaviour intact (places **and** perfumes output render identically — verify both); the
  dispatch is a clean extension point for a new form's renderer.
- No regression: existing markdown outputs render exactly as before.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, `npm test`, and
  `node dashboard/scripts/mobile-check.mjs` are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build; since the Output "View" opens a
  popover (an interaction), add/keep a `FLOWS` entry (with `viewport: true`) in
  `dashboard/scripts/_dashboard-harness.mjs` that opens an output preview, and its screenshot in
  `dashboard/scripts/visual-out/` confirms output still renders. The harness (`PAGES`/fixtures/`FLOWS`)
  is updated to reflect the change.
