## Do

Context: `GET /api/workflow-runs/:id/io` (in `src/api/server.ts`, the route block starting `// GET /api/workflow-runs/:id/io` around line 1741) powers the "Input → Output mapping" panel on the workflow-run detail page. Today it ALWAYS collapses the whole run down to first-wave-input → last-wave-output via `workItemIoRows(firstWave, lastWave, run.id)` (`src/db/store.ts`, ~line 502). For a workflow with 3+ stages (e.g. `places`: places-resolve → places-enrich → places-enrich-with-llm) there is currently NO way to see what an INTERMEDIATE stage (e.g. `places-enrich`) itself received and produced — only the overall first→last collapse. IMPORTANT EXCAVATION FINDING: on inspection, `inputJob`/`outputJob` on each `IoRow` are NOT per-row-varying data — they are always the WHOLE-WORKFLOW first-wave job name and last-wave job name respectively, constant across every row in a run's panel (confirmed via `_dashboard-harness.mjs`'s `ioRow()` fixture, which hardcodes `inputJob: 'places-resolve'`/`outputJob: 'places-enrich-with-llm'` on every row). So a per-job UI filter cannot be purely client-side against existing data — filtering by 'inputJob' or 'outputJob' as they exist today would only ever offer 2 possible values (always the same across all rows) and would reveal nothing about an intermediate stage. Genuine per-stage inspection requires the backend to compute a DIFFERENT join for the selected stage. Good news: `workItemIoRows(producerJobs: string[], consumerJobs: string[], workflowRunId)` in `src/db/store.ts` is ALREADY GENERIC — it does not know about 'first'/'last' wave semantics, it just joins whichever `producerJobs`' ledger rows to whichever `consumerJobs`' ledger rows by `root_key`, scoped to the roots THIS run touched (via `work_item_runs`). So NO changes are needed to `workItemIoRows`'s logic — only to how the ROUTE calls it. (Do tidy its JSDoc comment, which currently describes it as 'Input→output mapping across a workflow's first and last stages' — update it to note it is called EITHER with the whole-workflow first/last wave OR with a single stage's direct predecessor(s) and itself, since after this task both call shapes exist in the codebase.)

Do:
1. In the `GET /api/workflow-runs/:id/io` handler, read an optional `job` query param the same way the sibling `?job=&key=` output endpoint does at ~line 1783 (`url.searchParams.get('job')`) — the route already builds `url` earlier in the file via `new URL(req.url ?? '/', ...)`.
2. The route already computes `refs = getWorkflowJobs(run.workflow_name).map(m => ({ job: m.job_name, dependsOn: m.depends_on }))` and `dag = buildDag(refs)`. If the `job` query param is present:
   - Validate it names an actual member: `refs.some(r => r.job === jobParam)`. If not, return `400 { error: 'unknown job "<jobParam>" for this workflow' }`.
   - Look up its direct predecessors via `dag.dependencies.get(jobParam) ?? []` (the `Dag` type in `src/core/dag.ts` already exposes `dependencies: Map<string, string[]>`, job → its direct upstream deps — do not recompute this by hand).
   - If there are zero predecessors (jobParam IS a root/first-wave stage), set `producerJobs = [jobParam]; consumerJobs = [jobParam];` (mirrors the existing single-stage self-pairing the frontend already renders for a genuinely single-stage workflow — reuse that shape, don't invent a new one).
   - Otherwise set `producerJobs = predecessors; consumerJobs = [jobParam];`.
   - Call `workItemIoRows(producerJobs, consumerJobs, run.id)` — same function, just a different pair of job-name arrays than the default first/last-wave call.
   - Compute `emptyReason`/`note` exactly as the existing code does today (same `scoped`/`rows.length` logic, same `workflowHasRunLinkage` check), just applied to THIS call's `rows`/`scoped` result.
   - Return the SAME top-level response shape as today (`io`, `firstWave`, `lastWave`, `scoped`, `emptyReason`, `note` — `firstWave`/`lastWave` remain the WHOLE-WORKFLOW first/last wave, UNCHANGED, so the frontend can still independently know whether the OVERALL workflow is single-stage) PLUS three additive fields: `selectedJob: jobParam`, `scopedProducerJobs: producerJobs`, `scopedConsumerJobs: consumerJobs`.
3. When the `job` query param is NOT present, behavior and response values must be UNCHANGED from today for `io`/`firstWave`/`lastWave`/`scoped`/`emptyReason`/`note` — just add the three new fields with neutral defaults: `selectedJob: null`, `scopedProducerJobs: []`, `scopedConsumerJobs: []`.
4. Add focused unit test coverage (find the existing test file that exercises `GET /api/workflow-runs/:id/io` today — search `*.test.ts` for `'/io'` or `workItemIoRows` — and extend it, or add a new one) covering: (a) `?job=<a root/first-wave job>` → single-stage self-pairing (`scopedProducerJobs` and `scopedConsumerJobs` both `[job]`); (b) `?job=<a mid-DAG job with one predecessor>` → `scopedProducerJobs` = that job's direct `dependsOn` list, `scopedConsumerJobs = [job]`, and `io` rows whose `outputStatus`/`outputDetail` reflect THAT job's own `work_items` ledger row (not the workflow's overall terminal stage); (c) `?job=<unknown job name>` → 400; (d) no `job` param → response unchanged from before this task (regression guard for backward compatibility).

Do NOT touch anything under `dashboard/` in this task — a follow-up UI task (already authored, `dependsOn`s this one) consumes the new `?job=` param and the three new response fields; it needs the exact field names `selectedJob` / `scopedProducerJobs` / `scopedConsumerJobs` used verbatim above. Do NOT change `workItemIoRows`'s function body/logic — it is already generic; this task only changes how the ROUTE calls it, plus the JSDoc tidy-up described above.

## Done when

- `npx tsc --noEmit` and `npm test` both pass.
- `GET /api/workflow-runs/:id/io` with NO `job` param returns the exact same `io`/`firstWave`/`lastWave`/`scoped`/`emptyReason`/`note` shape and values as before this task, plus the three new neutral-default fields (`selectedJob: null, scopedProducerJobs: [], scopedConsumerJobs: []`).
- `GET /api/workflow-runs/:id/io?job=<X>` for a root/first-wave job returns a single-stage self-pairing (`scopedProducerJobs` and `scopedConsumerJobs` both `[X]`); for a job with direct predecessor(s) it returns `scopedProducerJobs` = that job's direct `dependsOn` list and `scopedConsumerJobs = [X]`, with `io` rows whose `outputStatus`/`outputDetail` reflect job `X`'s OWN `work_items` ledger row (not the workflow's overall terminal stage).
- `GET /api/workflow-runs/:id/io?job=<unknown job name>` returns 400 with a clear error message.
- New test coverage exists for all four cases above (root job, mid-DAG job, unknown job, unchanged default) and is part of the green `npm test` run.
- This is a pure backend/API change — no files under `dashboard/` are touched.
