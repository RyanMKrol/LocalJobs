## Do

On the job-run page (`dashboard/app/runs/[id]/page.tsx:47`) there is a small static "run" eyebrow
label rendered above the run id: `<span className="muted" style={{ fontSize: 12 }}>run</span>`. It is
a hardcoded constant — it never varies and carries no information (the job name + status badge next to
it already identify the page, and the URL is `/runs/…`). **Remove it entirely.** Delete just that
label span; leave the run id, job name, status badge, and the rest of the header untouched.

## Done when

- The job-run page (`/runs/<id>`) no longer renders the static "run" eyebrow label; the run id, job
  name, status badge and the rest of the header are unchanged and still laid out correctly.
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green.
