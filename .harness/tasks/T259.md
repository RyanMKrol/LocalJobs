## Do

**Follow-up to T250 (built but the owner marked it FAILED — it padded the wrong container).** Browser
investigation (places workflow detail page) found the graph nests as exactly three containers:

1. **innermost** — the React Flow area (the beige, dotted-background box holding the stage nodes):
   `.react-flow` / `.react-flow__pane` etc.
2. **middle** — `div.dag.dag-flow-wrap` — height ≈ the graph's natural height; computed padding is
   **18px top / 18px bottom only** (this is what T250 added — padding between the wrap and the beige
   React Flow box, and NOT on the left/right).
3. **outermost** — `div.panel` — the white card; computed vertical padding **0px**. This is the
   container the owner meant: the beige React Flow box sits flush against (or nearly against) the
   white panel's edges, and the wrap's padding is top/bottom-only so the sides still hug.

Fix so the beige React Flow box is **evenly inset on ALL FOUR sides** from the outer white `.panel`,
with symmetric, pleasant breathing room (top = bottom = left = right). ⚠️ `.panel` is a **shared
class** used by many cards across the dashboard — do NOT add padding to `.panel` globally. Target ONLY
the DAG's panel: add a specific class (e.g. `panel dag-panel`) to the panel that wraps `<DagFlow>` on
the workflow detail page (`dashboard/app/workflows/[name]/page.tsx`) and the workflow-run page
(`dashboard/app/workflow-runs/[id]/page.tsx`) and style that, and/or give `.dag-flow-wrap` symmetric
four-side padding — whichever yields even inset without changing every other panel. Account for the
padding in the graph height calc in `dashboard/app/components/DagFlow.tsx` (`graphH`) so the graph
still fits (fitView) and isn't clipped. Styling in `dashboard/app/globals.css`.

## Done when

- On a workflow detail page AND a workflow-run page, the beige/dotted React Flow graph box is inset
  from the surrounding white panel border by an even margin on all four sides (top = bottom = left =
  right) — visibly symmetric, not flush and not top/bottom-only.
- No OTHER `.panel` on any page changed (the padding is scoped to the DAG's panel/wrap only) — spot
  this isn't a global `.panel` change.
- Stays mobile-responsive (the `@media (max-width: 640px)` block) and theme-variable-driven; the graph
  still fits its container (not clipped).
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build and the `workflow.png` /
  `workflow-run.png` screenshots in `dashboard/scripts/visual-out/` confirm the graph box is evenly
  inset on all four sides inside the outer panel; `dashboard/scripts/_dashboard-harness.mjs` updated if
  the UI surface changed.
