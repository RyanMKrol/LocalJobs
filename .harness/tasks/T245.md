## Do

On the Workflows **list** view (`dashboard/app/workflows/page.tsx`), horizontally **centre the
contents of the "Last run" column**. Today the cell holds a status pill
(`StatusBadge`/`.pill` — succeeded/failed/skipped/etc.) plus a relative-time string (`fmtRelative(...)`,
e.g. "13 hours ago"), but the column is left-aligned, so the pill sits centred-ish while the time
text hugs the left and they don't line up under each other. Centre all of that cell's content
(both the pill and the time) so they align cleanly. Apply it to the "Last run" column specifically
(its `<th>` + `<td>`/contents) — not the whole table. Styling lives in
`dashboard/app/globals.css` (table-cell rules) or an inline style on that cell.

## Done when

- On `/workflows`, the "Last run" column's status pill and relative-time text are horizontally
  centred and visually aligned with each other; other columns are unchanged.
- It stays mobile-responsive (no new phone-width overflow; the `@media (max-width: 640px)` block in
  `globals.css`) and theme-variable-driven (no hardcoded colours).
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build, the `workflows.png` screenshot
  in `dashboard/scripts/visual-out/` confirms the Last-run column is centred/aligned, and
  `dashboard/scripts/_dashboard-harness.mjs` (`PAGES`/fixtures) is updated if the UI surface changed.
