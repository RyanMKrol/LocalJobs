## Overview
Three well-written dashboard test files never execute anywhere — npm test walks only src/, CI only builds the dashboard, and dashboard/package.json has no test script. Wire the two pure node:test component suites into npm test and CI, keep the Playwright nav-check local-only, and document the split.

## Do
1) Add a `test` script to dashboard/package.json that runs the two PURE suites (dashboard/app/components/OutputRenderer.test.ts, StageIoLists.test.ts) — they need react-dom/server, no browser. 2) Wire those into root `npm test` (extend CI-TEST-01's run-tests.ts walk to include dashboard/app *.test.ts, OR invoke the dashboard test script from the root flow — pick one and keep it consistent) AND add a CI step in .github/workflows/ci.yml after the dashboard build that runs them. 3) Keep dashboard/scripts/nav-check.test.ts local-only (Playwright); do NOT add it to CI. 4) Document the local-only (nav-check) vs CI (component suites) split where mobile-check/visual-check are already documented (README / CLAUDE.md file map). 5) Only if the owner confirms in scope (see open question), add the missing pure-helper tests (cronToEnglish/resolveMode/backFrom/fmtDuration/fmtRelative in ui.tsx, parseFrontmatter in MarkdownModal.tsx); otherwise defer. Owner decision (2026-07-12): ALSO add the missing pure-helper unit tests IN THIS TASK (not a follow-up) — cover cronToEnglish, resolveMode, backFrom, fmtDuration/fmtRelative, and parseFrontmatter with node:test suites under dashboard/app.

## Done when
Self-demonstrating: a deliberate break of the OUTPUT_RENDERERS format dispatch (OutputRenderer.tsx) or of detailHints (StageIoLists.tsx) turns BOTH `npm test` and CI red. The two pure suites run in `npm test` and in a CI step after the dashboard build. nav-check.test.ts is NOT run in CI and its local-only status is documented. `npm --prefix dashboard run build` still passes. Whether the extra pure-helper tests are in scope is an open owner question (pending-questions #16). New pure-helper node:test suites exist and run (under npm test + CI) for cronToEnglish, resolveMode, backFrom, fmtDuration/fmtRelative, and parseFrontmatter, each asserting real behaviour (not token tests).
