## Do

Surface the **movie recommendations** as their own distinct section on the movie-recommendations
workflow detail page, separate from the franchise gaps — and make the gaps-vs-recs distinction
clear. Today `dashboard/app/workflows/[name]/page.tsx`'s `MovieGapsManager` renders a section headed
**"Recommendations & gaps"** but only lists franchise GAPS (films you own some-but-not-all of);
actual recommendations are NOT surfaced in the UI at all — their owner-facing ignore UI was deferred
(see `.harness/LIMITATIONS.md`). The recs BACKEND already exists: the recommender writes
`moviesConfig.recsOut` (`src/jobs/movies/data/out/recommendations.json`); the recs ledger job is
`RECS_JOB = 'movie-recs'` keyed by `recKey(tmdbId)` (both in `src/jobs/movies/recs.ts`), and
`ignoreSurfacedItem('movie-recs', recKey(tmdbId))` (`src/db/store.ts:605`) already excludes a rec
from the merge + digest (T146).

Build, mirroring the existing gaps wiring:
- **API (`src/api/server.ts`):** add `GET /api/movie-recs` — read `moviesConfig.recsOut`, overlay
  each rec's `ignored`/`notified` ledger status, return empty (not an error) when the file is
  absent — modelled on the `GET /api/movie-gaps` handler at `src/api/server.ts:734`. Add
  `POST /api/movie-recs/:tmdbId/ignore` calling `ignoreSurfacedItem(RECS_JOB, recKey(tmdbId))`,
  modelled on `POST /api/movie-gaps/:tmdbId/ignore` at `src/api/server.ts:762` (positive-integer
  guard, behind the existing loopback/token mutation guard).
- **Client (`dashboard/app/lib/api.ts`):** add `movieRecs()` + `ignoreMovieRec(tmdbId)` mirroring
  `movieGaps()` / `ignoreMovieGap()` (around `dashboard/app/lib/api.ts:390`), plus the response type.
- **UI (`dashboard/app/workflows/[name]/page.tsx`):** render recommendations as their OWN clearly
  labelled section, VISUALLY DISTINCT from the franchise-gaps section (its own heading + explanatory
  copy + a list showing each rec's title, year, lens/reason, and TMDB link with a per-item
  `✕ Ignore` button + confirm), mirroring `MovieGapsManager`. Re-title/restructure so a viewer can
  tell a "gap" (own some, missing some of a franchise) from a "recommendation" (a new film to watch).
- **Docs (in scope):** update `.harness/LIMITATIONS.md` to drop the "recs ignore UI is a follow-up"
  note, and `CLAUDE.md` where the T146 note says the recs owner-facing ignore trigger is a follow-up.

Keep it mobile-responsive (the `@media (max-width: 640px)` block in `dashboard/app/globals.css`) and
theme-variable-driven.

## Done when

- `GET /api/movie-recs` returns the recommendations from `recommendations.json`, each overlaid with
  `ignored`/`notified`, and an empty list (HTTP 200) when the file is absent.
- `POST /api/movie-recs/:tmdbId/ignore` marks the rec ignored via
  `ignoreSurfacedItem('movie-recs', recKey(tmdbId))`; after calling it the rec leaves the active recs
  list (verify on the running dashboard: load `/workflows/movie-recommendations`, confirm a distinct
  Recommendations section renders and ignoring a rec removes it from the active list).
- The workflow detail page shows recommendations as a section **visually distinct** from the
  franchise-gaps section (a viewer can tell the two apart).
- `expectsTest`: a unit test in `src/api/server.test.ts` covers the new `POST /api/movie-recs/:id/ignore`
  endpoint (mirroring the existing movie-gaps ignore test) and runs against the scratch DB only.
- `npm test`, `npx tsc --noEmit`, and `npm --prefix dashboard run build` are green, and
  `node dashboard/scripts/mobile-check.mjs` passes (no phone-width overflow).
- `.harness/LIMITATIONS.md` and `CLAUDE.md` updated in the same change so the docs no longer describe
  the recs ignore UI as deferred.
