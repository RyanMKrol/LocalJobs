## Overview
The Services/Integrations page's usage Bar renders '{used} / {cap}' when a cap exists but just '{used} no limit' (no slash) when it doesn't, which reads inconsistently in the same row/table as capped cells.

## Do
In dashboard/app/integrations/page.tsx, the Bar component's no-cap branch (around lines 10-16, currently `{used} <span className="muted">no limit</span>`) renders with no separator between the used count and the 'no limit' label. Add a '/' separator so it matches the capped branch's '{used} / {cap}' shape (line 22) — render it as `{used} / <span className="muted">no limit</span>` (i.e. '3973 / no limit' instead of '3973 no limit'). This is a pure JSX text/formatting change inside the existing no-cap return block — no other logic, props, or the capped branch should change.

## Done when
The Bar component's no-cap branch renders a literal '/' character between the used count and the 'no limit' label (e.g. '3973 / no limit'), visually consistent with the capped branch's '300 / 300' format. The capped branch (cap != null) is unchanged. Verify by building the dashboard and visually confirming on the Integrations page (a service row with no cap, e.g. a rate/daily/monthly column showing 'no limit') that the '/' now appears — this is a `layer:ui`/`workType:style` change, so per the harness's visual-verification rule it is auto-covered by the loop's build/audit visual-verify hook; no extra visualVerify flag needed.
