## Do

The backlog DAG (BacklogDag in components/Dag.tsx, the /backlog DAG view) still looks poor: tasks render as loosely-styled items in left-to-right waves with a single '→' between waves, so there's no clear per-task shape and no real edges — it doesn't read as a graph. Redesign it to look like an actual directed acyclic graph: (1) wrap EVERY task in a clearly-bounded NODE SHAPE (a bordered box/card, clearly separated from its neighbours, status-coloured, showing at least the task id + title); (2) draw REAL directed edges between dependency-related nodes — an arrow from each task to each of its dependents — not just one arrow between waves, so the dependency structure is actually visible. Keep the topological ordering, the click-to-select/inspect behaviour, horizontal scroll, and phone-width responsiveness. PREFER a lightweight in-repo approach (position nodes by the existing topological waves; draw edges with SVG lines + arrowheads) over pulling in a heavy graph-layout library (e.g. dagre/d3) — honour the repo's dependency-light convention; if a library is genuinely warranted, note the tradeoff in the worklog. This supersedes/extends T076 (a first pass at nicer backlog DAG nodes).

## Done when

The /backlog DAG view renders as a genuine directed graph — each task sits in a clearly-bounded, visually-separated node shape and real directed edges (with arrowheads) connect dependent tasks rather than a single per-wave arrow; topological order, node selection/inspect, horizontal scroll, and mobile responsiveness all still work; dashboard build passes and mobile-check stays green.
