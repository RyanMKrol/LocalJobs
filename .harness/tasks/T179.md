## Do

Add a **Missing seasons** management view to the TV-seasons workflow detail page
(`/workflows/missing-tv-seasons` — currently `/workflows/plex` until T151's rename lands), mirroring
the movies **Recommendations & gaps** section (`MovieGapsManager`, built by T152) on
`/workflows/movies`. This is the natural home for the data — surface the currently-detected
complete-missing TV seasons there, with per-item ignore-to-suppress controls.

Mirror the movies pattern end-to-end:

1. **API (mirror `/api/movie-gaps`).** Add `GET /api/missing-seasons` that reads the current missing
   seasons from `plexConfig.missingOut` (`missing-seasons.json`) and overlays each `(show, season)`
   pair's notified/ignored state from the `plex-seasons-notify` ledger (keyed by `pairKey(tmdbId,
   season)` = `<tmdbId>::S<season>`, exported from `plex/stages/notify.ts`). Add `POST
   /api/missing-seasons/:tmdbId/:season/ignore` that calls `ignoreSurfacedItem('plex-seasons-notify',
   pairKey(tmdbId, season))` to suppress a season gap. Same loopback/token mutation guard as the
   movie-gaps endpoints. Add `api.missingSeasons` / `api.missingSeasonsIgnore` client methods.
2. **Ignore-to-suppress (mirror T145).** The notify stage already excludes ignored pairs from
   NOTIFICATIONS (`isWorkItemDone` treats `ignored` as done, so the `fresh` filter skips them).
   Extend `writeReport` to ALSO exclude ignored pairs from the written report via
   `ignoredItemKeys('plex-seasons-notify')` — exactly like the movies notify excludes ignored gaps —
   so an ignored season drops from both the report and notifications.
3. **Dashboard section.** On the workflow detail page (`dashboard/app/workflows/[name]/page.tsx`),
   when viewing the TV-seasons workflow, render a **"Missing seasons"** section (a
   `MissingSeasonsManager`, mirroring `MovieGapsManager`) listing each show with its complete-missing
   season(s) + tmdb status, with a per-item **Ignore** control. Put it in a bounded, **scrollable**
   panel with **themed** buttons (apply the T170 treatment from the start so it doesn't repeat that
   styling mistake), and a tidy empty state.
   - **Workflow-name gating + the rename.** The workflow is named `plex` today and becomes
     `missing-tv-seasons` after T151 (pinned last in the backlog). Gate the section by the workflow —
     ideally via a small DECLARATIVE per-workflow signal (the cleaner approach T152 also noted)
     rather than a hardcoded `name === 'plex'` string, so the T151 rename can't silently break it. If
     you do use the string, use the CURRENT name (`plex`); T151 will update it.
4. Mobile-responsive. Update CLAUDE.md / README if they describe the dashboard pages/sections.

## Done when

The TV-seasons workflow detail page has a "Missing seasons" section listing the currently-detected
complete-missing seasons (show + season + tmdb status) with per-item Ignore controls, backed by new
`GET /api/missing-seasons` + `POST /api/missing-seasons/:tmdbId/:season/ignore` endpoints (mirroring
`/api/movie-gaps`); ignoring a season suppresses it from BOTH the report and notifications
(`ignoreSurfacedItem` + `ignoredItemKeys` in the notify stage); the section is contained/scrollable
with themed buttons + a tidy empty state, gated to the TV-seasons workflow (rename-safe), and
mobile-responsive; `npx tsc --noEmit`, `npm test`, `npm --prefix dashboard run build` and
`node dashboard/scripts/mobile-check.mjs` are green; docs updated.
