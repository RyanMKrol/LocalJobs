## Do

Build a NEW MONTHLY workflow that scans my Plex MOVIE library for 'gaps' and notifies me which movies to download. NEEDS-HUMAN: like T108 it needs my private Plex server URL + token (PLEX_URL / PLEX_TOKEN), network access to my Plex server, and a movie-metadata source (e.g. TMDB API key, or a Radarr instance) to know what 'complete' looks like — the loop can't provision these, so scaffold around the gap and record failed:blocked until I supply them. WORKING DEFINITION OF 'gap' (confirm with me, it's the key design choice): within collections/franchises where I already own at least one movie (via TMDB collections / Plex collection metadata), the movies I'm MISSING — e.g. I own Star Wars I-VII but not VIII-IX. Note alternative definitions (missing from a watchlist, low-quality versions to upgrade) so I can pick; default to the collection-gap interpretation. Follow repo conventions and REUSE T108 where sensible: this can live in the same PRIVATE gitignored subfolder src/jobs/plex/ (already added to .gitignore by T108; if T108 hasn't landed, add 'src/jobs/plex/') and share the Plex connection + a metadata *.service.ts for rate-limiting; it is its own *.workflow.ts with a MONTHLY schedule. Stages e.g.: enumerate owned movies + their collections from Plex; for each collection resolve the full set from the metadata source and diff to find missing entries; push a notification (reuse src/core/notifier.ts push()) listing the gaps. Key idempotently with the work_items ledger per missing movie so I'm notified once per gap. Secrets from .env via process.env; env var NAMES (placeholders only) in .env.example; do NOT publish the job code or document it in the public README.

## Done when

A private (gitignored) monthly Plex movie-gaps workflow exists that compares my owned movies against their collections/franchises (per the agreed 'gap' definition) and pushes a notification listing missing movies to download, de-duplicated per missing movie via the work_items ledger; it reuses the Plex credentials/connection + a rate-limited metadata service; secrets come from .env (names placeholdered in .env.example) and the code stays a gitignored private subfolder. Until I provision Plex + metadata access (and confirm the 'gap' definition) it is recorded failed:blocked with the exact steps; once provisioned it is verified against the live Plex server with the smallest sample. npx tsc --noEmit and npm test stay green.
