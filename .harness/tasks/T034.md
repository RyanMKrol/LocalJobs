## Do

On the Services edit-limits form it's unclear how to clear a cap back to 'no cap' (inputs just show placeholder text). Add an explicit control (e.g. a 'No cap' / clear button per field) that sets the value to no-cap (null) unambiguously, instead of relying on typing/blanking.

## Done when

Each limit field has a clear UI way to set it back to no-cap (null); saving persists null; dashboard build passes.
