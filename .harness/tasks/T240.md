## Do

Stack the **LAST RUN** column onto two lines to fix the awkward pill↔time spacing. Today the status
pill and the relative time are crammed on one line ("Succeeded  25m ago"). Put the status pill on the
first line and the "how long ago" on the second. It's the LAST RUN cell on the Workflows list
(`dashboard/app/workflows/page.tsx`, `last_run.status` pill + a relative time derived from
`finished_at`/`started_at` — UTC strings parsed with an appended `Z`); styling in
`dashboard/app/globals.css`. If the same pill+time pattern appears elsewhere (overview recent-runs,
workflow detail), keep it consistent.

## Done when

- The LAST RUN cell shows the status pill on one line and the relative time on the next, with tidy spacing.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
