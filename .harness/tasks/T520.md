## Overview
Merge the standalone /admin-cache page's service-cache surface into the single /admin page so there is just one admin page, per the owner's request (they never intended two).

## Do
Move the service-cache UI from dashboard/app/admin-cache/page.tsx into dashboard/app/admin/page.tsx as new sections: (1) a 'Cached responses' section showing the per-service service_cache row-count table (with a Total row), loaded via api.serviceCacheCounts(); and (2) a 'Clear all cached responses' danger-zone action (its own separate danger-zone panel or a subsection, wired to api.clearServiceCache() with the existing window.confirm guard). Keep the two destructive actions FUNCTIONALLY DISTINCT — 'Delete all workflow output' (existing, api.resetAllWorkflowsOutput) and 'Clear all cached responses' (POST /api/cache/clear) remain separate API calls; this is purely a UI/page consolidation, not a merge of the two actions' behaviour. Preserve all existing behaviour: the load-on-mount + reload-after-clear, error/success messaging, and the empty-state ('cache is empty') text. Then DELETE the entire dashboard/app/admin-cache/ route folder (dashboard/app/admin-cache/page.tsx). Remove the now-dead cross-link that pointed from the cache page at /admin. There is currently NO nav link to /admin-cache in layout.tsx, so no nav change is needed. Because this alters the rendered admin surface and removes a route, update dashboard/scripts/_dashboard-harness.mjs in the SAME change: remove the `{ name: 'admin-cache', path: '/admin-cache' }` entry from the PAGES list (the /admin baseline shot already exists and now covers the merged surface). Update the root CLAUDE.md file-map entry for /admin-cache — the wording currently states the cache page is 'deliberately separate from /admin' — to reflect the new single /admin page hosting both a 'Delete all workflow output' action and a 'Clear all cached responses' cache section (docs-as-part-of-done). Build the dashboard, run node dashboard/scripts/mobile-check.mjs and node dashboard/scripts/visual-check.mjs, and LOOK at the /admin screenshot to confirm both sections render.

## Done when
The service-cache row-count table AND the 'Clear all cached responses' action render as sections within /admin (functionally identical to before, calling POST /api/cache/clear); the standalone dashboard/app/admin-cache/ route folder is removed; no functionality is lost (both destructive actions still work as distinct calls); the 'admin-cache' entry is removed from _dashboard-harness.mjs PAGES; the root CLAUDE.md file-map wording is updated to describe the unified /admin page; and dashboard build + mobile-check + visual-check are green with the /admin screenshot showing both the cache section and the existing admin sections.
