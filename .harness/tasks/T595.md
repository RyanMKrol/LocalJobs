## Overview
Owner's decision (confirmed directly, no needs-human task required): go with the LIGHTER dispatch-driven approach — no database schema change, no new workflow_runs status value. For a LIMITED manual workflow run, `runWorkflowInner` (src/core/workflow-executor.ts, around line 282-307) awaits the root stage's `inputKeys()` BEFORE the only place a `workflow_runs` DB row is created (`createWorkflowRun` at line 307) — so a root stage whose `inputKeys()` is an expensive live crawl (e.g. plex-language-fix's `discoverInputKeys()`, observed ~15 minutes for a full Plex library walk) leaves the dashboard showing nothing new for that entire window. Fix: surface the EXISTING in-process `startingWorkflows` claim (src/core/workflow-executor.ts, T105 — already correctly tracks exactly this window) as a dashboard-visible 'Starting…' signal.

## Do
Expose the `startingWorkflows` claim (src/core/workflow-executor.ts) via a small read path — either a lightweight new field on an existing workflow payload (e.g. `starting: boolean` on GET /api/workflows/:name and/or GET /api/workflows, sourced from checking whether the workflow's name is currently in the `startingWorkflows` set) or a minimal dedicated endpoint if that's cleaner given the module boundary. Have the relevant dashboard view(s) — at minimum the workflow detail page (dashboard/app/workflows/[name]/page.tsx) and the Workflows list page (dashboard/app/workflows/page.tsx) — show a transient 'Starting…' indicator while that flag is true, falling back to the normal last-run display once the real workflow_runs row appears (the existing 3s poll will naturally pick up the transition). No database schema change of any kind — `startingWorkflows` stays the in-process Map/Set it already is, just read instead of only written. Preserve the existing T105 'one active run per workflow' guard semantics untouched — this is a visibility-only change layered on top of, not a replacement for, `workflowRunInProgress`/`startingWorkflows`.

## Done when
Starting a manual limited run of a workflow whose root stage's `inputKeys()` takes a long time to resolve (verify by simulating a slow `inputKeys()` in a test fixture, or by timing plex-language-fix's discover stage if run live) shows a visible 'Starting…' indication on the dashboard within a few seconds of the API accepting the run — not only after the crawl finishes and the DB row is created. No schema/migration changes anywhere. Existing pages that read workflow run status still render correctly with no regression, `npx tsc --noEmit`, `npm test`, and `npm --prefix dashboard run build` are all green, and a screenshot via `node dashboard/scripts/visual-check.mjs` confirms the new 'Starting…' indicator actually paints (per the repo's visual-confirmation convention).
