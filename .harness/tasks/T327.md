## Do

`.harness/` has grown a genuine mix of file kinds sitting flat at the top level (everything except `designs/`, `tasks/`, `worklog/`, and the gitignored `.pending-tasks/`/`.pending-questions/`, which already group by kind). Current flat top-level inventory:

- **Docs:** `CLAUDE.md`, `HARNESS.md`, `README.md`, `LIMITATIONS.md`
- **Config/data:** `facets.json`, `harness.env`, `TASKS.json`
- **Ledgers (loop-written, committed):** `failures.jsonl`, `outcomes.jsonl`
- **Owner-owned overlay files (dashboard-written, the "three deliberate write exceptions" from root CLAUDE.md):** `human-done.json`, `manual-fail.json`, `reviews.json`
- **Scripts:** `integrate.sh`, `loop.sh`, `mark-failed.sh`, `postflight.sh`, `repo-lock.sh`, `supervise.sh`, `consolidate-ideas.sh`, `consolidate-ideas.mjs`, `policy.jq`
- **Gitignored scratch:** `IDEAS.md`

The owner's itch is purely tidiness — "things are sort of strewn all over the place" — there is no functional bug to fix.

**Starting-suggestion grouping (not binding — override freely):**
- `.harness/scripts/` — `integrate.sh`, `loop.sh`, `mark-failed.sh`, `postflight.sh`, `repo-lock.sh`, `supervise.sh`, `consolidate-ideas.sh`, `consolidate-ideas.mjs`, `policy.jq`
- `.harness/docs/` — `HARNESS.md`, `README.md`, `LIMITATIONS.md` (consider leaving `CLAUDE.md` at the top level — Claude Code auto-loads a directory's `CLAUDE.md` from that directory's path, so moving it changes when/whether it's picked up; verify this before moving it)
- `.harness/config/` — `facets.json`, `harness.env`
- `.harness/ledgers/` — `failures.jsonl`, `outcomes.jsonl`
- **Leave at top level, do NOT move:** `TASKS.json`, `human-done.json`, `manual-fail.json`, `reviews.json`, `IDEAS.md`. Reason: `src/api/server.ts` resolves several of these via a hardcoded relative path from its own file location — e.g. `const BACKLOG_PATH = fileURLToPath(new URL('../../.harness/TASKS.json', import.meta.url))`, and similarly for `REVIEWS_PATH` (`.harness/reviews.json`), `HUMAN_DONE_PATH` (`.harness/human-done.json`), and `MANUAL_FAIL_PATH` (`.harness/manual-fail.json`). Moving any of these into a subfolder means editing those constants in `src/api/server.ts` in lockstep (not just the scripts) — code, not just file layout, would need to change. `IDEAS.md` is referenced by `.harness/consolidate-ideas.mjs` (`IDEAS_PATH = path.join(REPO, '.harness/IDEAS.md')`) and by the `/idea` command.

**Confirmed cross-reference risks to check BEFORE moving any script (do not move scripts blindly):**
1. `repo-lock.sh`'s own header comment states its lock-path derivation "MUST stay byte-identical to loop.sh's `acquire_lock` ... and `src/core/repo-lock.ts`'s `resolveRepoPaths` — all three coordinate the SAME mutex." `src/core/repo-lock.ts` does not reference `.harness/repo-lock.sh`'s path directly (it re-derives the lock dir independently), but the THREE derivations (the `.sh`, `repo-lock.ts`, and `loop.sh`'s own `acquire_lock`) must keep computing the identical lock directory — moving `repo-lock.sh` itself doesn't break this (nothing hardcodes ITS path), but any edit made while reorganizing must not touch the derivation logic.
2. `mark-failed.sh` does `HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` then `LOOP_SOURCE_ONLY=1 source "$HERE/loop.sh"` — this is self-relative (uses its own script dir), so `mark-failed.sh` and `loop.sh` must move TOGETHER (stay siblings) or this line must be updated to the new relative path.
3. `consolidate-ideas.sh` does `source .harness/repo-lock.sh` — this is a **cwd-relative literal path, NOT self-relative** (unlike `mark-failed.sh`'s `$HERE`-based source) — it assumes it is invoked from the repo root. If `repo-lock.sh` moves into a subdirectory (e.g. `.harness/scripts/repo-lock.sh`), this line must be updated to match, and separately, note this script is ALREADY fragile to cwd since it hardcodes `.harness/` from repo root rather than deriving from `$HERE` like its sibling scripts — worth normalizing to `$HERE`-relative while touching this file, even though that's a robustness improvement, not required for the reorg itself.
4. `.harness/consolidate-ideas.mjs` hardcodes `path.join(REPO, '.harness/TASKS.json')`, `.harness/tasks`, `.harness/IDEAS.md`, `.harness/.pending-tasks` (via a `REPO` constant derived from its own `import.meta.url`) — moving `TASKS.json`/`tasks/`/`IDEAS.md`/`.pending-tasks/` requires updating these constants.
5. `loop.sh`, `supervise.sh`, and `postflight.sh` all anchor to their OWN directory via `HARNESS_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"` and then reference every other harness file as `$HARNESS_DIR/<file>` (e.g. `BACKLOG="$HARNESS_DIR/TASKS.json"`, `FACETS="$HARNESS_DIR/facets.json"`, `OUTCOMES="$HARNESS_DIR/outcomes.jsonl"`, `POLICY_JQ="$HARNESS_DIR/policy.jq"`, `POSTFLIGHT="$HARNESS_DIR/postflight.sh"`). This means: (a) if `loop.sh`/`supervise.sh`/`postflight.sh` themselves move to a shared `scripts/` subfolder together, `HARNESS_DIR` recomputes correctly and these three scripts keep finding each other automatically; but (b) if a file THEY reference (e.g. `facets.json`, `TASKS.json`, `outcomes.jsonl`) moves to a DIFFERENT subfolder than the scripts, every `$HARNESS_DIR/<file>` assignment referencing it inside `loop.sh`/`postflight.sh` must be updated to the new relative path (e.g. `$HARNESS_DIR/../config/facets.json` or similar).
6. `.claude/commands/local-jobs-mark-task-failed.md`, `local-jobs-loop-recover.md`, `idea.md`, `local-jobs-pre-loop-checkin.md`, and `local-jobs-convert-ideas.md` all reference `.harness/` paths in their instructions (e.g. telling Claude to run `.harness/mark-failed.sh`, read `.harness/TASKS.json`, edit `.harness/IDEAS.md`). These are prose instructions to a future Claude session, not code — they must be updated to match wherever files end up, or a future invocation will look in the wrong place.
7. The dashboard (`dashboard/app/backlog/page.tsx`, `dashboard/app/lib/api.ts`) has prose/comments referencing `.harness/TASKS.json`, `.harness/tasks/TNNN.md`, `.harness/worklog/<id>.md`, `.harness/reviews.json`, `.harness/failures.jsonl` — these are display strings and comments (not path resolution — the actual reads go through the API, which resolves via `src/api/server.ts`), but should be kept accurate if paths change.
8. Root `CLAUDE.md`'s "Autonomous build harness" section and `.harness/CLAUDE.md` reference several of these files by path in prose (e.g. `.harness/loop.sh`, `.harness/TASKS.json`, `.harness/reviews.json`) — per the root `CLAUDE.md` "Documentation is part of every change" rule, both docs must be updated in the SAME change as any reorganization.

**Suggested approach for whoever picks this up:** move one small, low-risk group at a time (e.g. docs first — pure prose, no code dependents — then config, then ledgers, then scripts last since they carry the most cross-references), verifying after each group rather than moving everything in one large commit.

## Done when

A reorganization plan has been decided (either the starting suggestion above or an owner-revised version) and, if files were moved: (1) every cross-reference identified above resolves correctly — `repo-lock.sh`'s lock-path derivation still matches `loop.sh`'s `acquire_lock` and `src/core/repo-lock.ts`'s `resolveRepoPaths` byte-for-byte; `mark-failed.sh`'s `source "$HERE/loop.sh"` still finds `loop.sh`; `consolidate-ideas.sh`'s `source .harness/repo-lock.sh` still finds `repo-lock.sh`; every `$HARNESS_DIR/<file>` reference in `loop.sh`/`supervise.sh`/`postflight.sh` still resolves; `consolidate-ideas.mjs`'s hardcoded `path.join(REPO, '.harness/...')` constants still resolve; (2) `src/api/server.ts`'s `BACKLOG_PATH`/`REVIEWS_PATH`/`HUMAN_DONE_PATH`/`MANUAL_FAIL_PATH` constants are updated if those files moved (they should NOT move per the starting suggestion, but confirm); (3) `npx tsc --noEmit` and `npm test` pass; (4) `DRY_RUN=1 .harness/loop.sh` runs without error (proves `loop.sh` still finds `TASKS.json`/`facets.json`/`harness.env`/`policy.jq`/`postflight.sh` and its own sourced helpers); (5) a real invocation of `.harness/mark-failed.sh <some-existing-done-task-id> "test"` followed by undoing it (or an equivalent dry check) confirms it still sources `loop.sh` correctly; (6) `bash .harness/consolidate-ideas.sh` --style manual check or code read confirms its `source .harness/repo-lock.sh` line still resolves; (7) root `CLAUDE.md` and `.harness/CLAUDE.md` are updated to reflect any new paths in the same change; (8) `.claude/commands/*.md` files referencing moved paths are updated. If the owner decides NOT to move anything after reviewing (tidiness itch doesn't outweigh the cross-reference risk), that is also an acceptable resolution — record the decision and close the task.
