## Do

Scaffold a new **standalone `tv-recommendations` workflow** under `src/jobs/tv-recs/` and build its
first stage: **`tv-snapshot`** (snapshot the Plex TV library by GUID + build a taste profile). This
mirrors the movies recommender (`src/jobs/movies/`) but for TV SHOWS, and is SEPARATE from the
existing `missing-tv-seasons` (`src/jobs/plex/`) workflow (which only audits completely-missing
seasons of owned shows — leave it untouched).

Create the workflow folder root files mirroring `src/jobs/movies/`:
- `tv-recs.workflow.ts` — a `WorkflowDefinition` named `tv-recommendations`, `schedule: '0 9 1 * *'`
  (monthly, like movies), `maxConcurrency: 4`. For THIS task wire ONLY `{ job: 'tv-snapshot' }` in
  `jobs` (later tasks add the branch/merge/notify stages — a manifest must never reference a job that
  doesn't exist yet, or the registry fails to load).
- `config.ts` (env-driven out paths under `src/jobs/tv-recs/data/out/`, recs knobs mirroring
  `src/jobs/movies/config.ts`: `recsTarget`, `recsMinRating` 7.0, `recsMinVotes` 50, `recsGenreCap`,
  `recsSampleSize`, etc., all env-overridable), `types.ts` (a `PlexShow`, `TvTasteProfile`,
  `RawSuggestion`, `Recommendation` mirroring `src/jobs/movies/types.ts`), and any `lib.ts` helper.
- `stages/tv-snapshot.job.ts` (default-exports the `JobDefinition`, name `tv-snapshot`) +
  `stages/tv-snapshot.ts` (impl) + `stages/tv-snapshot.test.ts`.

`tv-snapshot` must: connect to Plex reusing `src/jobs/plex/client.ts`'s `resolvePlexHost()`/`plexGet`
(import across folders — no edit needed; it self-heals a DHCP IP change, T149), read the TV library
section, build a per-show snapshot keyed by TMDB GUID (mirror `buildMovieSnapshots`/`buildOwnedSet`
in `src/jobs/movies/movies.ts`), and roll up a TV **taste profile** (per-genre / per-creator /
per-decade / per-country owned counts — mirror `buildTasteProfile`). Write `snapshot.json` +
`taste-profile.json` to `data/out/`. Re-compute fresh each run (no inputKeys — not limitable), like
the movies snapshot. Log verbosely + report per-item progress per the repo conventions. Document the
new workflow in `README.md` (job/workflow list) and `CLAUDE.md` (architecture + file map).

## Done when

- `src/jobs/tv-recs/tv-recs.workflow.ts` defines workflow `tv-recommendations` (monthly schedule,
  maxConcurrency 4) with the single member `tv-snapshot`, and the registry loads it cleanly (daemon
  starts; `npx tsc --noEmit` green).
- `tv-snapshot` writes `snapshot.json` (per-show, GUID/TMDB-keyed) + `taste-profile.json` (genre /
  creator / decade / country roll-up) to `src/jobs/tv-recs/data/out/`.
- `expectsTest`: `stages/tv-snapshot.test.ts` covers the pure snapshot + taste-profile roll-up from
  synthetic Plex section fixtures (no live Plex; scratch DB only), mirroring the movies snapshot tests.
- Verify against real Plex with a tiny sample: run the `tv-snapshot` job once and confirm it produces
  a non-empty snapshot + taste profile (this is the `live-api` verify).
- `npm test` and `npx tsc --noEmit` are green; `README.md` + `CLAUDE.md` document the new workflow.
