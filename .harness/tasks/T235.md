## Do

In the 🎨 theme picker, make **System** the FIRST option for **Mode**, matching the other two pickers
(Theme leads with `default`, Font with `System default`). The Mode picker (`ThemeControls` in
`dashboard/app/ui.tsx`, stored as `localjobs.mode`; `resolveMode(stored, osPrefersDark)`) currently
lists Dark / Light / System — reorder so System is first. Pure option-ordering tweak, no behaviour
change (System is already the effective default when nothing is stored).

## Done when

- The Mode picker lists System first, then Light/Dark; selecting each still behaves exactly as before.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
