## Do

Fix the border on the **Recent workflow runs** table on the Overview page so it's a solid, continuous
border around the ENTIRE table. Today the left/right/bottom edges have the border but the top-left/
top-right corners fade to white/transparent (likely the header row's background overpainting the
wrapper's top border, or a radius clip). Put a single solid border with matching `border-radius` +
`overflow:hidden` on the table's container so all four sides are continuous. The table is on
`dashboard/app/page.tsx`; shared table/panel CSS is in `dashboard/app/globals.css` (`.panel`, the
`.panel:has(> table)` scroll rule). Hold across all 3 theme families × light/dark.

## Done when

- The Recent-workflow-runs table shows a continuous solid border on all four sides (no faded top corners),
  in every theme family × light/dark.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
