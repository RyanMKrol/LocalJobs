## Do

Fix the Services page (`dashboard/app/services/page.tsx`) so the "used / cap" numbers in the Rate/min,
Rate/day, and Rate/month columns never wrap onto two lines. The `Bar` component (lines 8-18) renders
`<div className="mono" style={{ fontSize: 12 }}>{used} / {cap}</div>` with no `white-space: nowrap`,
so a long enough "used / cap" string (e.g. "833 / 1666") wraps inside its table cell while short
values ("0 / 30", "no limit") stay on one line. Add `whiteSpace: 'nowrap'` to that div's style (inline
or via a CSS class in `dashboard/app/globals.css`, matching whatever styling convention the rest of
this page/file already uses — check before choosing).

## Done when

- The "used / cap" text in `Bar` never wraps onto two lines regardless of the digit count.
- After `npm --prefix dashboard run build`, run `node dashboard/scripts/visual-check.mjs` and look at
  the Services page screenshot in `dashboard/scripts/visual-out/` — confirm the "used / cap" numbers
  render on one line for every service row. If the existing `service()` fixture in
  `dashboard/scripts/_dashboard-harness.mjs` doesn't already produce a long enough "used / cap" pairing
  to prove the fix, adjust the fixture so the screenshot actually exercises it.
- Run `node dashboard/scripts/mobile-check.mjs` and confirm no new horizontal overflow is introduced.
- `npm test`, `npx tsc --noEmit`, and `npm --prefix dashboard run build` all pass.
