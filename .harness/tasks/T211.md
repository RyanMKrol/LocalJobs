## Do

Make the **Film / Year / TMDB** columns line up consistently across collections on the movie
franchise-gaps list. In `dashboard/app/workflows/[name]/page.tsx`, `groupByCollection` (~line 11)
renders EACH collection in its OWN `<table>` (the table at ~line 89, headers
`Film` / `Year` / `TMDB` / blank-actions), so each table auto-sizes its columns independently and
the columns don't align from one collection to the next. Fix so the columns align across all
collections — either render a single shared table with per-collection section-header rows, or give
the per-collection tables a shared fixed column layout (`table-layout: fixed` + matching column
widths via CSS in `dashboard/app/globals.css`). Pure dashboard presentation change; do not alter the
data or the per-item ignore behaviour. Keep it mobile-responsive (the `@media (max-width: 640px)`
rules in `globals.css`; wide tables already scroll inside `.panel`).

## Done when

- On `/workflows/movie-recommendations`, the Film/Year/TMDB columns are aligned consistently across
  every collection (same column widths), not sized independently per collection. Verify by loading
  the running dashboard with multiple active collections present.
- No phone-width (402px) horizontal overflow: `node dashboard/scripts/mobile-check.mjs` passes.
- `npm --prefix dashboard run build` and `npx tsc --noEmit` are green.
- Per-item `✕ Ignore` controls still work unchanged.
