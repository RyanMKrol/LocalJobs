## Do

Make progress percentage a first-class citizen of the pipeline (today the perfumes pipeline just shows 0%). Give progress sensible denominators and a real-time roll-up from member jobs, so a pipeline run reports meaningful overall progress instead of a flat 0%.

## Done when

Pipeline progress is computed from member-job progress with sensible denominators and rolls up in real time; a unit test asserts the roll-up across member stages; the value is persisted/surfaced for the dashboard.
