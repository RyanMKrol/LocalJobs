## Overview
The manual, never-scheduled plex-language-undo.ts recovery script calls Plex's mutating stream-selection endpoint directly, bypassing the shared plex service's rate-limit/quota metering that the equivalent automated apply.ts stage already uses — this fixes the one remaining unmetered Plex mutation path in the repo.

## Do
In scripts/plex-language-undo.ts's runUndo() (currently around line 73), wrap the actual revert call in callService('plex', () => put(entry.partId, revertAudio.streamId, revertSubtitle?.streamId ?? null)) instead of calling put(...) directly — import callService from '../src/core/services.js'. Keep the wrap AT THE CALL SITE, inside runUndo's apply branch, not inside the injected `put` function itself: opts.put stays a raw `typeof plexPutStreams` (put = opts.put ?? plexPutStreams, unchanged), and the default export path (when no opts.put is injected) now flows through the real callService('plex', ...) meter exactly like src/workflows/plex-language-fix/stages/apply.ts's `await callService('plex', () => putStreams(discover.partId, afterAudio!.streamId, afterSubtitle?.streamId ?? null));` pattern. This means the existing test-injection seam is untouched: scripts/plex-language-undo.test.ts's fakes (put: async () => {...}) are passed as opts.put and are invoked directly, never routed through the real service meter, so no test starts making real Plex calls or touching the service_usage DB table. Only the REAL plexPutStreams import path (i.e. actual --apply runs against a live Plex server) goes through callService. Update the JSDoc comment above runUndo (currently describing `put` as injectable for testability) if needed so it still accurately reflects the call now being metered when no fake is injected.

## Done when
scripts/plex-language-undo.ts's runUndo() calls callService('plex', () => put(...)) at the real-Plex call site (only reached in --apply mode when opts.put is not overridden), matching the wrapping pattern already used in src/workflows/plex-language-fix/stages/apply.ts. The existing three tests in scripts/plex-language-undo.test.ts (dry-run computes without calling put; --apply reverts to before-state; a put failure during --apply is recorded failed) still pass unmodified in behavior (their injected `put` fakes are still called directly, not through callService, so they still run with zero real network/DB dependency). At least one NEW test is added confirming that when runUndo is invoked WITHOUT an injected opts.put (i.e. using the real plexPutStreams default), the revert call is routed through callService — this can be verified by monkeypatching/spying on the services module's callService export (or, more simply, by asserting via an injected `put` that is captured and separately confirming via code reading / a targeted unit test that callService wraps the default path — whichever approach the implementer finds cleanest, as long as it demonstrates the real-Plex path is metered and the fake-Plex test path is not). `npx tsc --noEmit` and `npm test` are green.
