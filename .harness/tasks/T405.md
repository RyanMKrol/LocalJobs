## Overview

T389 deletes shared framework code (the old joined `IoPanel` component, `workItemIoRows`, and the
`GET /api/workflow-runs/:id/io` endpoint) now that `StageIoPanel` has replaced it everywhere. This
task is the human review checkpoint for that deletion — confirm it's correct and complete before
treating it as fully landed.

## Do

1. Read `.harness/worklog/T389.md` — it should contain a clear summary of exactly what was deleted
   and the `grep` output proving nothing else referenced the removed code (per T389's own "Done
   when" requirement).
2. Re-run the verification grep yourself: `grep -rn "workItemIoRows\|IoPanel\b\|GET /api/workflow-runs/:id/io\b" src/ dashboard/app/` and confirm it returns nothing (or only comments/docs explicitly noting the removal).
3. Spot-check a few workflow-run pages in the running dashboard (or the latest `visual-check.mjs`
   screenshots from T389's build) to confirm nothing regressed — every run page should still show
   its Input/Output panel correctly via `StageIoPanel` alone.
4. Confirm root `CLAUDE.md`'s "Shared dashboard UI components" section and the
   `stock-digest.workflow.ts` file comment were updated to describe `StageIoPanel` as the only panel
   (not "an alternative to `IoPanel`").

## Done when

- You've personally confirmed the deletion is complete, nothing else references the removed code,
  and no workflow-run page regressed.
- If you find a problem, do not mark this done — instead flag it (e.g. via a new follow-up task, or
  by reopening T389's work) rather than silently accepting an incomplete deletion.
- Once satisfied, mark this task done via the dashboard's "Mark done" button (this is a
  `gate: "needs-human"` task — the loop cannot do this step, only you can).
