## Overview
The owner wants to SEE five genuinely different ways a 'Top of mind' section could look/sit on the dashboard home page (dashboard/app/page.tsx) before committing to one — using dummy placeholder markdown content, no real data wiring. This is a chooser task: build all five, switchable in the running dashboard, and stop there (a separate needs-human task picks the winner).

## Do
On the Overview page (`dashboard/app/page.tsx`), add FIVE distinct visual/placement variants of a read-only 'Top of mind' section, each rendering the SAME hardcoded dummy markdown string (a few lines: a heading, a short paragraph, and a bullet list, so wrapping/overflow/markdown-rendering can be judged) via `react-markdown` (reuse the same XSS-safe pattern as `dashboard/app/components/MarkdownModal.tsx` — no `rehype-raw`). Make the five variants genuinely different treatments, not CSS tweaks of one idea: (1) a hero-style banner at the very TOP of the page, above the summary tiles, full-width with heading-level prominence; (2) a compact card slotted into the existing tiles/summary grid near the top (same visual weight as a stat tile, just markdown content instead of a number); (3) a collapsible/expandable section (collapsed by default, e.g. between the Ignored and Workflows sections around the existing ~line 149-172 area) using a `<details>`/toggle pattern; (4) a sidebar-style panel — a persistent narrow column running down one side of the page (this will need a layout wrapper around the existing content; keep it simple, CSS grid/flex is fine) so it's visible alongside everything else without being inline; (5) inserted inline as its own full section BETWEEN two existing sections (e.g. between Workflows and Stuck items, around the existing ~line 200 area), styled like the page's other `<h2>`+`.panel` sections. Make the five switchable within the running dashboard via a query param, e.g. `?tom=1` through `?tom=5` (default to variant 1, or to none if you prefer an explicit `?tom=0`/no-param = 'no top-of-mind section' baseline) read via `useSearchParams` — no persistent toggle UI is needed, a query param is enough for the owner to compare by editing the URL. Do NOT wire this to Unit A's real storage/API — dummy content only, this task is purely about visual comparison. Update `dashboard/scripts/_dashboard-harness.mjs`'s `PAGES`/`FLOWS` list to add one entry per variant (five `/?tom=N` routes, or a FLOWS-style action per variant) so the visual-check screenshots capture all five for review, per the file's living-artifact rule.

## Done when
All five variants render on the Overview page, switchable via the `?tom=N` query param, each showing the same dummy markdown content rendered as markdown (not raw text) in a visibly different placement/treatment from the other four. `npx tsc --noEmit`, `npm test`, and `npm --prefix dashboard run build` all pass. `node dashboard/scripts/mobile-check.mjs` passes (no horizontal overflow at phone width for ANY of the five variants — check each). `node dashboard/scripts/visual-check.mjs` passes and produces one screenshot per variant; inspect all five and confirm each is visually distinct, fully painted (nothing blank/clipped/overlapping other content), and legible.
