## Overview
Re-attempt of T543, which built and fully verified the RecsManager/GroupedManager consolidation but reported failed:blocked because CLAUDE.md was not in its scope — the spec's own '## Done when' requires 'CLAUDE.md is current,' and CLAUDE.md's 'Shared dashboard UI components' section names the four files this task deletes (TvRecsManager.tsx, MovieRecsManager.tsx, MovieGapsManager.tsx, MissingSeasonsManager.tsx) by literal path. This unit is the same work with CLAUDE.md added to scope so the doc update the spec always required can actually land in the same change.

## Do
Same refactor as T543's design: replace TvRecsManager.tsx and MovieRecsManager.tsx with one generic RecsManager<T> component, and replace MovieGapsManager.tsx and MissingSeasonsManager.tsx with one generic GroupedManager<T> component, per the design field above. Update the four call-sites in dashboard/app/workflows/[name]/page.tsx to thin config objects. Additionally — and this is the corrected part — update CLAUDE.md in the SAME change: its 'Shared dashboard UI components' section currently documents the four old manager components by exact file path (dashboard/app/components/TvRecsManager.tsx etc.) in three separate places (a components list entry, and two other prose references elsewhere describing MovieGapsManager/groupByCollection and MissingSeasonsManager/groupByShow, and the IgnoredSection entry that lists all four managers as its consumers). Rewrite those references to describe the new RecsManager<T> and GroupedManager<T> components (their file paths, their config-object parameterization, and which call-sites use them) so the doc accurately reflects the post-refactor component layout. Update _dashboard-harness.mjs only if the rendered DOM/URL surface actually shifts (T543's investigation found it did not need changes, since routes and fixtures are unchanged — confirm this is still true).

## Done when
Exactly one implementation of the recs-manager shape (RecsManager<T>) and one of the grouped-gaps shape (GroupedManager<T>) remain — TvRecsManager.tsx, MovieRecsManager.tsx, MovieGapsManager.tsx, and MissingSeasonsManager.tsx are deleted. All four workflow detail pages (movie-recommendations, tv-recommendations, missing-movies, missing-tv-seasons) render PIXEL-EQUIVALENT to the pre-refactor look (confirmed by looking at visual-check.mjs screenshots, not just that the check exits green). npx tsc --noEmit, npm --prefix dashboard run build, mobile-check.mjs, and visual-check.mjs are all green. CLAUDE.md's 'Shared dashboard UI components' section (and any other prose mentioning the four deleted file names) no longer references TvRecsManager.tsx/MovieRecsManager.tsx/MovieGapsManager.tsx/MissingSeasonsManager.tsx by path, and instead accurately documents RecsManager.tsx and GroupedManager.tsx. _dashboard-harness.mjs is updated if (and only if) the rendered surface shifted.
