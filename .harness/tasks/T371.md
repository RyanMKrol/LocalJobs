## Overview

Add `src/jobs/CLAUDE.md`, a directory-scoped CLAUDE.md (mirroring `src/services/CLAUDE.md`'s existing
pattern) that states the "every adjacent job pair needs a validation gate" rule directly to any future
agent working inside `src/jobs/`, so the rule surfaces automatically instead of relying on someone
having read the whole root `CLAUDE.md` first.

## Do

Read `src/services/CLAUDE.md` in full first — it is short (about a dozen lines of actual content) and
states its own rationale explicitly: *"This is a local reinforcement of the identical rule in the root
`CLAUDE.md` ... It's repeated here because Claude Code loads the nearest `CLAUDE.md` when working
directly inside `src/services/`, so the rule surfaces automatically to any future session or agent
touching a service file in this directory — not just one that happened to read the root doc first."*
Mirror that exact structure and tone for `src/jobs/CLAUDE.md`: short, states ONE rule as a hard
requirement, explains why it's repeated here rather than left only in the root doc.

Also read root `CLAUDE.md`'s existing "Validation gates between workflow stages" section (the fuller
convention this file reinforces) and `src/core/gate-coverage.test.ts` (added by a task this one
depends on — read its file header/comments once it exists, so this doc accurately names the actual
test file and what it checks, rather than describing a hypothetical mechanism).

**Write `src/jobs/CLAUDE.md`** covering:

1. **The rule, stated as a hard requirement (not a suggestion):** every pair of adjacent jobs in every
   workflow's DAG (every `dependsOn` edge) MUST have a validation gate between them — a matching
   `produces`/`consumes` `ArtifactContract` key on both sides of the edge (`src/core/types.ts`,
   `src/core/dag.ts`'s `deriveGates`). State explicitly, in the owner's own words, that **a trivial
   gate is an acceptable minimum bar** — a bare `check(): { ok: true }` "the previous stage
   succeeded" contract satisfies the rule; not every boundary needs a rich shape-check like
   `places`/`perfumes`'s. (Reference `src/jobs/places/contracts.ts` and `src/jobs/perfumes/contracts.ts`
   as the richer worked examples for when a real artifact shape is worth validating.)
2. **This is mechanically enforced, not just documented** — name the actual test file
   (`src/core/gate-coverage.test.ts`) and explain in one or two sentences what it does: it walks every
   workflow the registry loads and asserts every DAG edge has a matching gate, failing `npm test` if
   one is missing. Make clear this means a new workflow LITERALLY CANNOT merge (green `npm test` is
   part of Definition of Done everywhere in this repo, per root `CLAUDE.md`) without gates at every
   stage boundary.
3. **The practical "how" for a new job**, briefly: declare `produces`/`consumes` on the `JobDefinition`
   (in the `.job.ts` file) pointing at factory functions in a `contracts.ts` at the job-folder root —
   point to `src/jobs/perfumes/stages/fetch.job.ts` as the shortest concrete wiring example, and to
   the newly-added `src/jobs/stocks-sync/contracts.ts` / `src/jobs/projects-sync/contracts.ts` /
   `src/jobs/workouts-sync/contracts.ts` / `src/jobs/stock-digest/contracts.ts` (added by sibling
   tasks in this same sweep) as additional worked examples alongside the original places/perfumes
   ones.
4. Note this file is a **local reinforcement** of the root `CLAUDE.md` convention, not a replacement
   for it — same framing `src/services/CLAUDE.md` uses for the broker read-only rule — and state the
   same "surfaces automatically because Claude Code loads the nearest CLAUDE.md" rationale for why it
   lives here as its own file rather than only in the root doc.

Do NOT duplicate the entire root `CLAUDE.md` "Validation gates between workflow stages" section
verbatim — keep this file short and pointed, same length/density class as `src/services/CLAUDE.md`,
and cross-reference the root doc for the full mechanism (shape/checks/sample, gate detail pages,
`gateFailurePrefix`, etc.) rather than re-explaining all of it here.

## Done when

- `src/jobs/CLAUDE.md` exists, is short (comparable length to `src/services/CLAUDE.md`), and states
  the every-adjacent-pair-needs-a-gate rule as a hard requirement with the trivial-minimum-bar
  carve-out explicit.
- It names the actual mechanical enforcement test file by its real path and correctly describes what
  it checks (confirm this by reading the merged `src/core/gate-coverage.test.ts` before writing this
  doc — don't guess at its name/behavior).
- It links/points to `src/jobs/places/contracts.ts`, `src/jobs/perfumes/contracts.ts`, and at least
  one of the newly-added contracts files as worked examples.
- It states its own "why this file exists as a local reinforcement" rationale, mirroring
  `src/services/CLAUDE.md`.
- Root `CLAUDE.md`'s existing "Validation gates between workflow stages" section is checked for
  accuracy against the now-fully-enforced state of the repo (all workflows gated, mechanical test
  exists) — if anything there is now stale or worth a one-line pointer to the new
  `src/jobs/CLAUDE.md`, update it in the same change (per this repo's "documentation is part of every
  change" Definition of Done rule).
- No code changes in this task — pure documentation. `npx tsc --noEmit` and `npm test` still pass
  (unaffected, but confirm nothing was accidentally broken).
