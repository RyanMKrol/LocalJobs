## Overview

Part of a repo-wide sweep giving every job a real paragraph-length `description` (surfaced on
`/jobs/[name]` and `/runs/[id]`) instead of a one-liner. This task covers the 3 jobs in the
`stock-digest` workflow. The mechanism was already built by the foundational task this depends on —
this is pure content authoring.

## Do

Rewrite the `description` string of each of the following 3 job files to a real 3-6 sentence
paragraph. Ground each in root `CLAUDE.md`'s detailed `stock-digest` section — note it's a distinct
workflow from `stocks-sync` with its OWN independent Trading212 fetch (T382, no inter-workflow
dependency; both route through the shared `src/services/trading212.service.ts`) — and the stage's
own code. Plain prose only, no markdown syntax:

- `src/workflows/stock-digest/stages/stock-portfolio-snapshot.job.ts` (`stock-portfolio-snapshot`) —
  currently: "Fetch stock-digest's OWN current open equity positions from Trading212 (read-only),
  independent of stocks-sync, resolving each position's ISIN + real-world ticker via OpenFIGI (T373),
  and write data/out/portfolio.json."
- `src/workflows/stock-digest/stages/stock-sector-lookup.job.ts` (`stock-sector-lookup`) — currently:
  "Resolve each currently-held ticker's industry via the Finnhub company-profile API (preferring the
  OpenFIGI-resolved real-world ticker, T373), idempotent per ticker via the work_items ledger, writing
  data/out/sectors.json."
- `src/workflows/stock-digest/stages/stock-digest-build.job.ts` (`stock-digest-build`) — currently:
  "Build a weekly Claude-narrated markdown digest of current stock holdings + performance movers,
  reading stock-digest's own portfolio snapshot (independent of stocks-sync). Idempotent per ISO week
  via the work_items ledger." — expand to mention the winners/losers ranking, the sector-diversification
  section (when resolved), and the soft ticker-hallucination cross-check.

## Done when

- All 3 `stock-digest` jobs have a real, accurate paragraph description grounded in their actual
  behavior — not a one-liner.
- `npx tsc --noEmit` and `npm test` are green.
