## Do

Hardcode **React Flow** as the single workflow graph renderer and remove the style switcher + the
other two styles (the T206→T207 "lock in the winner"). Specifically:
- Remove the `DagOptionSwitcher` component, the `useDagOption` hook, and the `localjobs.dag-option`
  localStorage option from `dashboard/app/ui.tsx`.
- Render `DagFlow` (React Flow) directly as the graph on the definition view
  (`dashboard/app/workflows/[name]/page.tsx`) and the run-scoped view
  (`dashboard/app/workflow-runs/[id]/page.tsx`) — no toggle.
- DELETE the now-unused Wave Columns + Swim Lanes implementation (`dashboard/app/components/Dag.tsx`)
  and its dead CSS in `dashboard/app/globals.css`. Verify nothing else imports `Dag`/the switcher first.
- Update CLAUDE.md's graph/Dag notes to describe the single React Flow view (drop the multi-style text).

## Done when

- Every workflow graph (definition + run views) renders via `DagFlow` only; the graph-style switcher is gone.
- `Dag.tsx`, `DagOptionSwitcher`, `useDagOption`, the `localjobs.dag-option` key, and their dead CSS are
  removed with no dangling imports.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green; CLAUDE.md graph notes updated.
