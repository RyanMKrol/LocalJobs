## Do

Improve the styling of the two output-management sections so they read as distinct, polished
sections: **"Recommendations & gaps"** (`MovieGapsManager`, gated to `movie-recommendations`) and
**"Missing seasons"** (`MissingSeasonsManager`, gated to `missing-tv-seasons`) — both in
`dashboard/app/workflows/[name]/page.tsx`, sharing the `.movie-gaps-scroll` styling in
`dashboard/app/globals.css`. Today they look unfinished: the per-collection/per-show tables have **no
outline/border**, the text **hugs the panel walls** (no inner padding), and there's **no
section-level background** to set the whole section apart (the faint per-collection card background
alone isn't enough).

Add: a proper section container (border + subtle background + internal padding) wrapping the whole
section so it reads as one distinct block; table outlines/borders; and inner padding so text isn't
flush to the edges. Fix BOTH sections together via their shared styling so they stay visually
consistent.

CRITICAL constraint: it must look good across **every** theme in the Theme Picker — the 3 families
(`default`, `pixel-picnic`, `sunny-8bit`) × light/dark = 6 palettes. Drive ALL colours/borders/
backgrounds from the existing CSS custom properties (`--…` variables in `globals.css`); do NOT
hardcode any colour, or it will break in some themes. Stay mobile-responsive.

## Done when

- Both the "Recommendations & gaps" and "Missing seasons" sections render with a clear section
  container (border + background + internal padding) and bordered/outlined tables, with text no longer
  hugging the edges — visibly more polished and clearly delimited as a section.
- All colours come from CSS variables (no hardcoded hex/rgb in the new rules); the sections look
  correct in all 3 theme families in BOTH light and dark mode (verify by toggling each).
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green at phone width.
