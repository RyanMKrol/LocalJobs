## Do

T097 added a human-readable cron explanation but surfaced it via the native HTML title attribute, which only appears after the browser's ~1s hover delay — not ideal. Replace it with an EXPLICIT tooltip affordance: render a small '?'-in-a-circle help icon next to each cron schedule display, and on hover/focus (or tap, for touch) show the human-readable cron text IMMEDIATELY via a custom tooltip (CSS/JS), with no native-title delay. Reuse the existing cron->English helper from T097 (don't reimplement it). Build it as a small reusable component (e.g. in ui.tsx) and apply it everywhere a cron is shown (the Workflows list + the workflow detail page; grep for any other cron render sites). Make it accessible (keyboard-focusable + appropriate aria) and touch-friendly (tap to reveal), and ensure the tooltip doesn't cause horizontal overflow at phone width. Remove the now-superseded native title-based cron tooltip.

## Done when

Each cron schedule display has an explicit '?'-in-circle icon that, on hover/focus/tap, instantly shows the human-readable cron text (no native-title delay); it reuses the T097 cron->English helper; it's keyboard-accessible and works on touch; no horizontal overflow at ~402px; the old native-title cron tooltip is gone; dashboard build passes and mobile-check stays green.
