## Do

NEEDS-HUMAN review of the joined-output prototype options built in T205. The owner reviews the
switchable options live on a workflow detail page (different layout libraries/frameworks and/or visual
styles, plus the per-workflow dedupe-key approach each uses), compares them, and **commits to one
winner** — both the rendering approach (library/style) AND the dedupe-key convention to standardize
on. Record the decision (which option won and why, and the chosen dedupe-key convention) in this
task's worklog `.harness/worklog/T206.md` so the follow-up (T207) can hardcode it. This is a taste/
design decision — the loop skips it; the owner returns to it deliberately.

## Done when

The owner has reviewed the T205 joined-output options and recorded, in `.harness/worklog/T206.md`: the
chosen rendering option (library/style) and the chosen per-workflow dedupe-key convention, with enough
detail for T207 to bake in the winner and remove the others. No code change in this task.
