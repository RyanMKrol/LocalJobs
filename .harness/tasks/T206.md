## Do

NEEDS-HUMAN review of the workflow **graph (DAG) view** prototype options built in T220. The owner
reviews the switchable graph options live on a workflow page — different graphing libraries/approaches
— comparing especially **how well each represents waves and concurrency** (the core failing of the
current `Dag.tsx` view), plus general faithfulness to the real `dependsOn` structure, gate-mark
placement, mobile/theme behaviour, and dependency weight. The owner **commits to one winner**. Record
the decision (which option/library won and why) in this task's worklog `.harness/worklog/T206.md` so
the follow-up (T207) can hardcode it. This is a taste/design decision — the loop skips it; the owner
returns to it deliberately.

## Done when

The owner has reviewed the T220 graph-view options and recorded, in `.harness/worklog/T206.md`, the
chosen graphing option (library/approach) and why, with enough detail for T207 to bake in the winner
and remove the others. No code change in this task.
