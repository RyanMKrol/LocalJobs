## Overview

The owner decided (Option A, the minimal-change shape): `movie-recommendations`'s two separately
headed sections — "Recommendations" (Claude-picked film recs) and "Franchise gaps" (films you own
some-but-not-all of in a collection) — should be nested under ONE outer "Output" `<h2>` heading, with
"Recommendations" and "Franchise gaps" demoted to `<h3>` subheadings underneath. Same two tables,
same per-item/per-collection Ignore controls, same data — only the heading structure changes.

## Do

1. In `dashboard/app/workflows/[name]/page.tsx`, `MovieRecsManager`'s top-level heading (currently
   `<h2>Recommendations</h2>`, around line 233) becomes an `<h3>`: `<h3>Recommendations</h3>`. Style it
   consistently with the file's existing `<h3>` precedent (the "Ignored (N)" subheadings further down
   in the same component use `style={{ fontSize: 15, marginTop: 0 }}`) — since this h3 is now a
   top-level subsection heading rather than a nested one inside a `.panel`, give it a bit of top margin
   for separation from the outer "Output" `<h2>` / the previous subsection, e.g.
   `style={{ fontSize: 15, marginTop: 20 }}`. Leave the descriptive paragraph immediately below it
   unchanged.
2. `MovieGapsManager`'s top-level heading (currently `<h2>Franchise gaps</h2>`, around line 377)
   becomes `<h3>Franchise gaps</h3>`, same styling approach as step 1.
3. At the bottom of the file, the render call site (around line 910-911) currently is:
   ```tsx
   {name === 'movie-recommendations' && <MovieRecsManager />}
   {name === 'movie-recommendations' && <MovieGapsManager />}
   ```
   Change this to render ONE outer "Output" heading wrapping both managers:
   ```tsx
   {name === 'movie-recommendations' && (
     <>
       <h2>Output</h2>
       <MovieRecsManager />
       <MovieGapsManager />
     </>
   )}
   ```
   `MovieRecsManager` and `MovieGapsManager` are each used ONLY at this one call site (confirmed —
   grep the file for other usages before editing; there are none), so demoting their internal headings
   to `<h3>` in steps 1-2 is safe and has no other call site to worry about.
4. **Also fix `MovieRecsManager`'s empty state while this component is already being touched** (a
   pre-existing inconsistency found during this idea's research, not previously actioned): its
   "no recommendations yet" message (currently ~line 243-247) is a bare
   `<p className="muted" style={{ fontSize: 13 }}>No recommendations yet…</p>` — unlike
   `TvRecsManager`'s equivalent empty state (`page.tsx:105-111`), which is correctly wrapped in
   `<div className="panel"><p className="empty-state-panel">…</p></div>`. Bring
   `MovieRecsManager`'s empty state in line with that same convention:
   ```tsx
   {data && data.generatedAt == null && (
     <div className="panel">
       <p className="empty-state-panel">
         No recommendations yet. Run the workflow manually — the recommended films will appear here.
       </p>
     </div>
   )}
   ```
   Keep the exact same message text — only the wrapper changes. (`MovieGapsManager`'s own empty state,
   ~line 387-392, is ALSO a bare `<p>` with no owner-cited precedent pointing at it specifically — for
   consistency within this same reorganized "Output" section, apply the identical panel-wrapped fix to
   it too: `"No audit has run yet. This workflow runs monthly (or run it manually) — the detected
   franchise gaps will appear here."` gets the same `<div className="panel"><p
   className="empty-state-panel">` treatment.)
5. No changes are needed to `TvRecsManager` or `MissingSeasonsManager` (handled by the sibling task
   `output-section-consistency-3`) or to `WorkflowOutputSection.tsx` (handled by
   `output-section-consistency-1`) — this task is scoped strictly to `movie-recommendations`'s two
   managers and their shared call site in `page.tsx`.
6. `dashboard/scripts/_dashboard-harness.mjs` already has a `workflow-movie-recs` `PAGES` entry
   (`/workflows/movie-recommendations`) that will pick up the new heading structure automatically on
   the next screenshot run — no fixture changes needed for the populated case. However, the fixture's
   `movieRecs` constant always has `generatedAt: NOW` (non-empty), so the movie-recommendations EMPTY
   state (step 4's fix) is never exercised by the existing harness. Add a way to see it: either (a) add
   a new `PAGES` entry pointing at a fixture variant with `generatedAt: null` for movie-recommendations
   if the harness already supports per-page fixture overrides, or (b) if that's not straightforward,
   note in the worklog that the empty-state fix was verified by code inspection + the identical,
   already-visually-confirmed `TvRecsManager` pattern it copies rather than a new screenshot — use your
   judgement on which is more proportionate; don't over-engineer new fixture-selection machinery for
   this one case. If you add a new `PAGES`/fixture entry, follow the living-artifact rule and document
   it inline in `_dashboard-harness.mjs`.

## Done when

- `movie-recommendations`'s detail page shows exactly ONE `<h2>Output</h2>` heading, with
  "Recommendations" and "Franchise gaps" as `<h3>` subheadings beneath it, in that order, both still
  showing their full existing tables and per-item/per-collection Ignore controls unchanged.
- `MovieRecsManager`'s and `MovieGapsManager`'s empty-state messages are both wrapped in
  `<div className="panel"><p className="empty-state-panel">…</p></div>`, matching `TvRecsManager`'s
  precedent, with the same message text as before.
- No other workflow's heading/section (`WorkflowOutputSection.tsx`'s generic "Output", TV recs,
  missing seasons) was touched by this task.
- Full per-workflow heading census (validates idea #10c's "every workflow" ask, completing it):
  confirm and record in the worklog that EVERY workflow detail page now shows exactly one section
  headed literally "Output" — the generic `WorkflowOutputSection` workflows (places, perfumes,
  listening-digest, projects-sync, claude-warmer, stocks-sync, stock-digest, workouts-sync),
  `tv-recommendations`, `missing-tv-seasons` (both fixed by the sibling task
  `output-section-consistency-3` — confirm that task's rename landed if built first, or note it's
  still pending if built before it), and now `movie-recommendations` (fixed by this task). This is the
  final piece closing out the whole "every workflow, one Output section" contract from idea #10(b).
- `npx tsc --noEmit` and `npm test` pass.
- `npm --prefix dashboard run build` succeeds.
- **Visual confirmation (this is a `layer: ui` task):** run `node dashboard/scripts/visual-check.mjs`
  and look at `workflow-movie-recs.png` — confirm ONE "Output" heading appears with "Recommendations"
  and "Franchise gaps" rendering as clearly-subordinate `<h3>` subheadings beneath it (not the same
  visual weight as the removed top-level `<h2>`s), both tables intact, Ignore buttons intact. If an
  empty-state screenshot was added per step 6, look at it too and confirm the centered panel renders
  correctly. Record what you saw in the worklog.
- `node dashboard/scripts/mobile-check.mjs` stays green.
