## Do

On the workflows page table, centre-align the VALUES in the "Stages" and "Schedule" columns (they read better centred). Leave the "Last run" and "Next" column values LEFT-aligned as they are. Centre the matching headers too if that reads cleaner.

## Done when

Stages + Schedule column values (and ideally their headers) are centre-aligned on the workflows page; Last run + Next stay left-aligned; dashboard build passes.
