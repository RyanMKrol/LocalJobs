## Overview
One slice of the Plex→callService migration blocked twice (T474, T499) by a pre-push git-hygiene guard trip (sensitive/gitignored path staged, no code landed). The spec was always correct; the fix is to shrink the diff per unit so staging named files is trivial. This unit is plex-space-saver's three Plex scan reads across 3 files.

## Do
In src/workflows/plex-space-saver/stages/scan.ts, wrap all three plexGet<...>(...) reads (the movies, shows, and episodes section listings) in callService('plex', () => plexGet<...>(...)), importing callService from ../../../core/services.js. Mirror the callService('tmdb', ...) wrapping convention used in sibling Plex workflows. Do not change arguments, return-value handling, or error handling beyond the wrap. Update scan.test.ts to account for the callService('plex', ...) wrapper (the no-limit branch passes the call through). Add a one-line note to plex-space-saver/CLAUDE.md that its Plex reads are metered via the shared plex service. GIT HYGIENE: stage ONLY these 3 files by explicit name — NEVER `git add -A`/`git add .`/a directory; never stage anything under data/, chrome-profile/, .env*, or a scratch DB.

## Done when
All three plexGet reads in scan.ts are wrapped in callService('plex', ...); a grep for a bare plexGet( in scan.ts returns nothing; scan.test.ts passes; plex-space-saver/CLAUDE.md notes the plex service; only the 3 scoped files are staged.
