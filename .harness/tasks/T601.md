## Overview
Owner's decision (confirmed directly, no needs-human task required): capture each branch's stratified sample as NEW work_items rows under the branch's own job name, distinguished from its (recs-ledger-rebuild-b's) per-suggestion output rows. This is genuinely new capture code — the sample is currently built in-memory and thrown away.

## Do
Confirmed: the stratified sample (default MOVIES_RECS_SAMPLE=50 items, src/workflows/movies/config.ts) is built in-memory inside each branch's prompt-construction call (spec.build({...}) in branch.ts:144-147, and again in merge.ts's buildDefaultTopUp for top-up rounds) and discarded — never written anywhere. Owner confirmed the capture shape: record the sampled item ids/titles as work_items rows under the branch's OWN job name (e.g. 'rec-auteur'), at minimum the sampled item ids/titles; fold in other taste-profile signals (recent titles, already-suggested exclusions) into the row detail too if cheap to do alongside, but the sampled items themselves are the required minimum. Since these rows share the branch's own job name with recs-ledger-rebuild-b's per-suggestion OUTPUT rows, they need a distinguishing marker in `detail` (e.g. `detail.kind: 'input-sample'` vs the output rows' own shape) so the two can be told apart programmatically. Because StageIoPanel's existing model (src/db/store/workItems.ts's stageIoLists) treats a stage's Inputs as 'my PREDECESSOR's ledger rows this run,' and a branch's stratified sample is NOT its predecessor's (movie-snapshot/tv-snapshot's) own output — it's a subset the branch itself computed — the stage-io lookup for a branch job needs to ALSO consult the branch's own 'input-sample'-kind rows as an additional Inputs source, alongside the real predecessor's full snapshot rows. This is the SAME class of 'read from an alternate/additional ledger source for a stage's I/O panel' problem recs-ledger-rebuild-e-build solves for the notify stage's Outputs (via unifying with outputJob) — consider whether the two can share one generalized mechanism (e.g. a per-stage override on which job name(s)/detail-kind a panel reads for Inputs vs Outputs) rather than building two bespoke special cases; use your judgment on how far to generalize, but note the precedent so you don't duplicate logic needlessly.

## Done when
Each branch's Inputs panel shows the ACTUAL sampled item set for that specific branch's run (at minimum the sampled item ids/titles), stored as work_items rows under the branch's own job name with a distinguishing `detail.kind` marker separating them from the branch's own per-suggestion output rows, and distinguishably different across branches/branch runs — not the full snapshot repeated identically under all 8 branches. A unit test proves the captured input set size matches the configured sample size (or less, if fewer items existed) and is a genuine subset of the snapshot, not the whole snapshot.
