## Do

Cancelled runs are currently lumped into the overview Failed tile. A cancelled run is a deliberate stop, not a failure. Stop counting cancelled as failed, and give it its OWN clickable Cancelled overview stat tile (filtering like the other tiles), consistent with how status is aggregated elsewhere.

## Done when

Cancelled runs no longer count as Failed; a dedicated clickable Cancelled tile shows + filters them; dashboard build passes.
