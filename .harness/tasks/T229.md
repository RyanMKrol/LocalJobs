## Do

Vertically centre the **"Clear output data"** button in the Danger zone of every workflow detail page.
Today the button is pinned to the TOP-right of its row while the left side has a heading + two-line
description, so the (shorter) button floats at the top. Centre it against the title+description block
(likely a flex `align-items: center` on the danger-zone row). Rendered in
`dashboard/app/workflows/[name]/page.tsx` (the T203 Danger zone → reset-output action); styling in
`dashboard/app/globals.css`.

## Done when

- The "Clear output data" button is vertically centred against its title+description block.
- Theme-variable-driven + mobile-safe; `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
