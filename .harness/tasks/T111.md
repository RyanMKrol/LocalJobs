## Do

One-time clean slate so the workflows can run from scratch in production with no prior TESTING data. NEEDS-HUMAN: this is destructive and operates on the LIVE production DB (data/jobs.db) and local data/ files — the loop must never run it; the agent PREPARES a safe, guarded reset and the human executes it. Deliverable: a guarded reset utility under scripts/ (e.g. scripts/reset-clean-slate.ts) that DRY-RUNS by default and requires an explicit confirm flag to actually delete, printing exactly what it will remove. CLEAR: the work_items ledger plus the run/usage history + logs — work_items, runs, run_logs, workflow_runs, workflow_run_logs, job_usage, service_usage — and every workflow's OUTPUT artifacts under each job's data/out/** (e.g. places data/out json + markdown; perfumes data/out/pages, pages-failed, fragrantica, markdown). PRESERVE (must NOT touch): data/raw/** inputs (the workflows need them), data/chrome-profile/ (warmed Cloudflare clearance — not an output), .env (secrets), the definition tables jobs/workflows/services (they re-sync from code on daemon start), and user settings — service limit overrides (limits_overridden + values) and workflow enabled toggles. Note the process for the human: stop the daemon (ensure no workflow is running), run the script with the confirm flag, then restart the daemon — it re-syncs definitions and, with an empty ledger, reprocesses all inputs from scratch. Call out that clearing service_usage resets the monthly spend meter (intended for a fresh start). NEVER git add anything under data/.

## Done when

A guarded scripts/ reset utility exists (dry-run by default, explicit confirm to delete, prints what it removes) that clears the work_items ledger + run/usage history/logs + every workflow's data/out artifacts while preserving data/raw inputs, the chrome-profile, .env, definition tables, and user service-limit/enabled settings. The human runs it, restarts the daemon, and the dashboard shows an empty ledger/run history with workflows ready to reprocess all inputs from scratch; until the human runs it the task is recorded failed:blocked with the exact steps. npx tsc --noEmit and npm test stay green; nothing under data/ is committed.
