## Overview

By this point `StageIoPanel` is proven against single-stage, linear, parallel-fan-out, and `outputJob`-override DAG shapes (T386, T387). This task is the final sweep: give every REMAINING workflow in this repo its own `/stage-io` fixture coverage in `dashboard/scripts/_dashboard-harness.mjs` (per the "LIVING ARTIFACT RULE" convention in root `CLAUDE.md` and `.harness/CLAUDE.md`), and run a full mobile-check/visual-check regression pass across the whole dashboard to confirm nothing broke anywhere now that `StageIoPanel` is universal.

## Do

As of this task, the workflows that do NOT yet have a `/stage-io` fixture (check `dashboard/scripts/_dashboard-harness.mjs`'s `fixtureFor()` for what T386/T387 already added) are: `missing-tv-seasons`, `workouts-sync`, `listening-digest`, `projects-sync`, `plex-space-saver`, and any other workflow discoverable via `src/jobs/*/  *.workflow.ts` that still lacks one — cross-check against the full workflow list (`GET /api/workflows` in a running daemon, or the workflow file map in root `CLAUDE.md`'s "What this project is" section) rather than assuming this list is exhaustive. For each: add a `<name>Members`/`<name>WorkflowRun`/`<name>StageIo` fixture set (mirroring the `stockDigest*`/`movieRecs*` patterns already in the file) and a `PAGES` entry pointing at its run-detail page, UNLESS an existing `PAGES` entry already covers a run of that workflow (check before adding a duplicate).

Run the full local Definition-of-Done sweep: `npx tsc --noEmit`, `npm test`, `npm --prefix dashboard run build`, `node dashboard/scripts/mobile-check.mjs`, `node dashboard/scripts/visual-check.mjs` — fix anything that regresses.

## Done when

- Every workflow this repo ships (per `src/jobs/*/*.workflow.ts`) has a `/stage-io` fixture set and a `PAGES` entry exercising its run-detail page in `dashboard/scripts/_dashboard-harness.mjs`.
- `node dashboard/scripts/mobile-check.mjs` reports ALL pages passing (0 horizontal overflow, 0 boundary-crossing elements) — this is the full regression check, not a partial one.
- `node dashboard/scripts/visual-check.mjs` runs successfully AND you look through the full set of screenshots in `dashboard/scripts/visual-out/` for every NEWLY-added workflow's run page this task covers — confirm each renders sensibly (tabs present, at least one populated Inputs/Outputs column, nothing blank/overlapping/clipped). Record what you saw in this task's worklog entry — name each workflow you actually looked at.
- `npx tsc --noEmit` and `npm test` both pass.
