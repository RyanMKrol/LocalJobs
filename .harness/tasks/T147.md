## Do

On the run-scoped **validation-gate detail page**
(`dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx`), the back-navigation link
reads a generic **"← workflow run"** (hardcoded — `<a href={run ? `/workflow-runs/${id}` :
'/workflows'}>← {run ? 'workflow run' : 'workflows'}</a>`). It should instead **identify the
specific run you're returning to**, the same way the rest of the dashboard does — e.g. the
workflow-run page itself already renders `← {run.workflow_name}` (linking to
`/workflows/<name>`), and a job page links back to its named workflow.

Change the gate page's back link to show the run's identity instead of the generic label, using
the `run` object it already polls (`api.workflowRun(id)` → `run.workflow_name`, and the run `id`):
make the label the workflow's name (with a "run" qualifier so it's clear it returns to a *run*,
not the workflow definition) — e.g. **`← places run`** (or `← <workflow_name>` if you judge that
cleaner). Keep the destination href unchanged (`/workflow-runs/${id}`); only the label changes.
Preserve the fallback when `run` hasn't loaded / is missing (→ `← workflows` linking to
`/workflows`).

While here, check the **definition-level** gate page
(`dashboard/app/workflows/[name]/gates/[producer]/[key]/page.tsx`) for the same generic-label
issue and align it for consistency (its back target is the workflow definition `/workflows/<name>`,
so its label should be the workflow name). Keep it mobile-responsive.

## Done when

The run-scoped validation-gate detail page's back link shows the workflow run's identity (the
workflow name, qualified as a run) instead of the generic "workflow run", linking to the same
`/workflow-runs/<id>` destination, with a sensible fallback while the run is loading; the
definition-level gate page is consistent; `npm --prefix dashboard run build` passes and
`node dashboard/scripts/mobile-check.mjs` stays green.
