## Do

Make the "back to the workflow run" link **consistent — same format AND styling — across the two
pages that have it**: the validation-gate detail page
(`dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx`) and the run/job detail page
(`dashboard/app/runs/[id]/page.tsx`).

Today they differ (see screenshots):
- The **gate** page back link reads **"← perfumes run · eb62f9e1"** — workflow name + short run id
  (the readable format approved in the prior round). ✓ good.
- The **run/job** page back link reads **"← eb62f9e1-9751-4853-9a24-8d2c2ac0baa0"** — a raw full
  UUID, no workflow name. ✗ inconsistent and ugly.

Bring them into line on the gate page's readable format (the run page comes UP to it — do NOT
regress the gate page to a raw UUID):

1. **Factor a SHARED back-link** — a small component/helper (e.g. `WorkflowRunBackLink`, or a util
   that formats `"{workflowName} run · {shortId}"`) used by BOTH pages, so the format + styling can't
   drift apart again.
2. When the back target is a **workflow run**, render **"← {workflow} run · {shortId}"** (shortId =
   first UUID segment via `id.split('-')[0]`; full id in the anchor `title`) — exactly what the gate
   page shows now. Update the run/job page (`runs/[id]/page.tsx`), which currently renders the raw
   `from`/workflow-run UUID, to use this. It already has the workflow-run id from `?from=` (and can
   resolve its `workflow_name` the same way the gate page does).
3. **Identical visual styling** on both (same class / colour / size / `←` affordance).
4. Keep the destination href unchanged (`/workflow-runs/<id>`), and a sensible fallback when there's
   no workflow-run context (e.g. a plain "← runs" / "← workflows").
5. Don't regress the gate page — the shared component must produce the same output there. The
   definition-level gate page (no run in scope) keeps showing just the workflow name. Mobile-responsive.

## Done when

The back-to-workflow-run link is visually and format-identical on the gate detail page and the
run/job detail page — both render "← {workflow} run · {shortId}" (full id in the `title` tooltip)
via a single shared component, with the run/job page no longer showing a raw full UUID; hrefs are
unchanged; there's a sensible fallback with no workflow-run context; the gate page is not regressed;
`npm --prefix dashboard run build` passes and `node dashboard/scripts/mobile-check.mjs` stays green.
