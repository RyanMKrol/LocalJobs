## Do

On the **run-scoped** validation-gate detail page
(`dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx`), the back-navigation link must
identify the **SPECIFIC RUN** you're returning to — by its **run id** — not just the workflow name
or a generic label.

⚠️ A first attempt changed the label to `← <workflow_name> run` (it currently shows **"← movies
run"** — see the screenshot). That is **NOT sufficient**: it names the workflow but not WHICH run,
so every run of a workflow shows the identical back-link text. The owner wants the link to identify
the specific run — its **id** — so it's clear which run you go back to.

REQUIREMENT:
- The back link navigates to `/workflow-runs/<runId>` (the page already has `id` from the route
  params and polls `api.workflowRun(id)`). Make the label include a **short form of the run id**
  (the first UUID segment, `id.split('-')[0]`, e.g. `2df4cfbf`) so it's compact but specific,
  alongside the workflow name for context. **Suggested format:** `← movies run · 2df4cfbf`
  (workflow name + " run · " + short id) — or `← run 2df4cfbf` if you judge that cleaner. Put the
  **full run id** in the anchor's `title` (hover tooltip).
- Keep the destination href unchanged (`/workflow-runs/<runId>`); only the label changes.
- Preserve a sensible fallback when the run hasn't loaded yet / is missing (→ `← workflows`).
- **Do NOT** add a run id to the **definition-level** gate page
  (`dashboard/app/workflows/[name]/gates/[producer]/[key]/page.tsx`): its back target is the
  workflow DEFINITION (`/workflows/<name>`), where there is no run in scope, so its label correctly
  stays the workflow name. Only the RUN-scoped page gets the run id.
- Keep it mobile-responsive.

Verify by loading a real run's gate page (like the screenshot's URL) and confirming the back link
reads e.g. `← movies run · 2df4cfbf` (the run's actual short id), not just `← movies run`.

## Done when

On the run-scoped validation-gate detail page the back link shows the **specific run's id** (a
short run-id segment, e.g. `← movies run · 2df4cfbf`, with the full id in a `title` tooltip) — not
just `<workflow> run` and not a generic label — linking to the same `/workflow-runs/<id>`; the
definition-level gate page (no run in scope) still correctly shows just the workflow name; there's a
sensible fallback while loading; `npm --prefix dashboard run build` passes and
`node dashboard/scripts/mobile-check.mjs` stays green.
