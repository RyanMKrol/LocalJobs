## Do

Surface the franchise-gaps **"you own" anchor** on the DASHBOARD movie-recommendations page. T161
added a per-collection owned-example ("You own: <title> (<year>)") but ONLY to the `franchise-gaps.md`
markdown report — the dashboard "Recommendations & gaps" section
(`MovieGapsManager` in `dashboard/app/workflows/[name]/page.tsx`, gated to `movie-recommendations`)
still shows only the MISSING films, never which film from each collection you already own. The data
already exists; it's a wiring gap with two parts:

1. **API passthrough.** `GET /api/movie-gaps` (`src/api/server.ts`, ~line 750) reads
   `franchise-gaps.json` into `FranchiseGapsFile` and returns `{ generatedAt, collectionsChecked,
   gaps }`, DROPPING the top-level `file.collectionExamples` field. Add `collectionExamples` to the
   response (it's a `Record<collectionName → { title, year }>` on `FranchiseGapsFile`,
   `src/jobs/movies/types.ts:73` — NOT a per-gap field; the per-gap `FranchiseGap` row at `types.ts:51`
   only has `collectionName`).
2. **Client type + render.** Add `collectionExamples` to the movie-gaps type in
   `dashboard/app/lib/api.ts`, and in `MovieGapsManager` render the owned example under each
   collection heading (the component already groups rows by `collectionName` via `groupByCollection`,
   so join the example by collection name) — e.g. a muted "You own: <title> (<year>)" line beneath the
   collection title. Tidy/omit when a collection has no example.

Do NOT change the markdown report (already correct). Update CLAUDE.md's T161 note if it claims the
owned example is report-only.

## Done when

- `GET /api/movie-gaps` includes `collectionExamples` in its JSON response (keyed by collection name),
  read from `franchise-gaps.json`.
- The dashboard "Recommendations & gaps" section shows, under each collection that has one, the owned
  example film ("You own: <title> (<year>)") alongside the missing list; collections without an
  example render cleanly.
- A unit test in `src/api/server.test.ts` asserts `/api/movie-gaps` returns `collectionExamples` from
  a fixture gaps file (hermetic, no live TMDB). `npm test`, `npx tsc --noEmit`,
  `npm --prefix dashboard run build`, and `node dashboard/scripts/mobile-check.mjs` are all green.
