## Overview

Remove the `plex-language-no-track-flag` job from the `plex-language-fix` workflow entirely — the
owner has decided the "this file has no track in the right language at all" notification isn't worth
keeping, and wants it dropped with no replacement.

## Do

Delete `src/workflows/plex-language-fix/stages/no-track-flag.ts`,
`stages/no-track-flag.job.ts`, and `stages/no-track-flag.test.ts`. Remove the
`{ job: 'plex-language-no-track-flag', dependsOn: ['plex-language-scan'] }` entry from the `jobs`
array in `src/workflows/plex-language-fix/plex-language-fix.workflow.ts`, and remove any mention of it
from that file's top-of-file doc comment. Remove the two API endpoints
`POST /api/plex-no-track/:itemKey/ignore` and `POST /api/plex-no-track/:itemKey/unignore` from
`src/api/server.ts` (search for `plex-no-track` in that file — both handlers are near line 1449-1470).
Remove the corresponding gate contract for the `plex-language-scan` → `plex-language-no-track-flag`
edge from `src/workflows/plex-language-fix/contracts.ts` (the gate-coverage test at
`src/core/gate-coverage.test.ts` will fail if a stale contract still references a job that's no longer
a DAG member, or if a still-active edge is left without one — confirm this file compiles and the
gate-coverage test passes after your edit). Update `src/workflows/plex-language-fix/CLAUDE.md` to
remove the `plex-language-no-track-flag` section and any other references to it in that file, and add
a short note that it was deliberately removed (owner decision — the signal wasn't worth keeping) so a
future reader doesn't wonder if this was an oversight.

After this task, `plex-language-fix` should be a two-member workflow:
`plex-language-scan` → `plex-language-apply` (unchanged otherwise) — this is an intentionally SMALL,
isolated deletion; do not touch `scan.ts`/`apply.ts`'s own logic in this task (that is a separate
follow-up task, T453).

## Done when

- `plex-language-fix.workflow.ts`'s `jobs` array has exactly 2 members: `plex-language-scan` and
  `plex-language-apply` (with `apply` depending on `scan`, unchanged).
- No file in the repo references `plex-language-no-track-flag`, `no-track-flag.ts`/`.job.ts`/`.test.ts`,
  or the `plex-no-track` API path (a repo-wide grep confirms zero hits, git history aside).
- `npx tsc --noEmit` and `npm test` both pass, including `src/core/gate-coverage.test.ts`.
- The daemon's job registry still loads cleanly (no orphaned-job error) — exercised by the existing
  registry test suite; no new test is needed solely for this, since deleting a job and its manifest
  entry together can't trigger `orphanJobNames`.
