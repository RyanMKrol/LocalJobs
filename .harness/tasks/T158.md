## Do

T140 made the markdown-preview popover's YAML frontmatter header (`.md-fm` in the `MarkdownModal` /
`parseFrontmatter` in `dashboard/app/workflow-runs/[id]/page.tsx`) **hide** fields whose value is
empty/null. The owner has decided that's wrong — silently omitting them loses information (you can't
tell a field is missing data vs absent). **Reverse the hiding and instead draw attention to them.**

- **Un-hide** the empty/null frontmatter fields (remove the T140 omission so every field renders
  again).
- For an effectively-empty value (empty string, `null`, `~`, empty array `[]`), render an explicit
  placeholder — e.g. **`null`** (or `(empty)`) — and **style it bold + an attention/muted colour**
  (a new `.md-fm-null` class in `dashboard/app/globals.css`) so missing fields visibly stand out
  rather than disappearing.
- **Keep** the other T140 improvements: the shared-column grid alignment and the readable
  array rendering (comma list / chips). Only the hiding behaviour is reversed.
- Mobile-responsive.

## Done when

The frontmatter preview shows ALL fields, including empty/null ones, with each empty/null value
rendered as an explicit **bold** `null`/empty marker (highlighted to draw attention) instead of
being hidden; the T140 column alignment and array rendering are preserved; `npm --prefix dashboard
run build` passes and `node dashboard/scripts/mobile-check.mjs` stays green.
