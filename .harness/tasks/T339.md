## Do

The owner reports, on BOTH `/services` and `/workflows`, that each table's row content (e.g. the Services 'Service' column, the Workflows 'Workflow' name column) has vertical padding that's too generous and horizontal padding that reads as basically zero. Both pages use the SAME base rule in `dashboard/app/globals.css` (around line 92): `th, td { text-align: left; padding: 10px 14px; border-bottom: 1px solid var(--border); }`. Neither `dashboard/app/services/page.tsx` nor `dashboard/app/workflows/page.tsx` appears to define a page-specific override of this rule (confirmed by grep — no `.services-table`/`.workflows-table` style class, no inline `td`/`th` padding). The `.panel` wrapper around each table (globals.css:87) has NO padding of its own (`background`/`border`/`border-radius`/`overflow: hidden` only), so the table sits edge-to-edge inside the panel and the ONLY horizontal inset comes from this one `th, td` rule — meaning the 14px is the full gap between a cell's text and the panel's border. Before changing anything: run `npm --prefix dashboard run build` then start the dashboard (or use `node dashboard/scripts/visual-check.mjs` directly, which spins up `next start` hermetically with synthetic API fixtures) and look at the current `/services` and `/workflows` screenshots in `dashboard/scripts/visual-out/` to see exactly what the owner is describing. Also check whether any MORE SPECIFIC selector overrides `th, td` in a way that's actually in effect on these two pages — search `dashboard/app/globals.css` for other `th`/`td` rules (there's a mobile-only override at the `@media (max-width: 640px)` block, `th, td { padding: 8px 10px; }`, around line 578 — leave the mobile behaviour sane/proportionate but the owner's complaint here is about the default desktop view) and for any `.table-group-header td` / `.io-table-scroll` style rules that might apply (they shouldn't, on these two pages, but confirm). Then adjust the base `th, td` padding rule in `globals.css` to reduce the vertical component and increase the horizontal component — a reasonable starting point is `padding: 8px 18px;` (tune vertical down from 10px, horizontal up from 14px), but treat this as a starting guess, not a mandate: iterate by rebuilding + re-running visual-check and comparing screenshots until the header/cell padding on both `/services` and `/workflows` visually reads as adequately spaced from the panel edges (no longer looking like text is jammed against the left border) without the rows looking bloated vertically. This is a single shared CSS rule — do not duplicate the fix per-page. Also sanity-check a handful of OTHER pages that render tables (e.g. `/workflows/[name]`, `/jobs/[name]`, the overview `/`) in the visual-check screenshots to confirm the padding change doesn't look wrong elsewhere (it's a shared rule, so it applies everywhere `th`/`td` render) — if it looks fine everywhere, no further action needed; if some other table clearly needs different treatment, leave it as-is and just note it in your summary (out of scope for this task, which is about Services + Workflows specifically).

## Done when

`npx tsc --noEmit` and `npm test` pass. `npm --prefix dashboard run build` succeeds. Run `node dashboard/scripts/visual-check.mjs` and LOOK at the resulting screenshots in `dashboard/scripts/visual-out/` for at least `/services` and `/workflows` — confirm by eye that table header/cell padding now reads as more horizontally spaced from the panel border and less vertically bloated than before, matching the owner's complaint (too much vertical, too little horizontal). Also glance at a couple of other table-bearing pages' screenshots (e.g. `/workflows/[name]` or the overview `/`) to confirm no regression. Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green (no new horizontal overflow at phone width) given the mobile `th, td` override at globals.css's `@media (max-width: 640px)` block interacts with the same selector family. This is a `layer:ui` task — visual confirmation via the screenshots is a hard requirement, not optional; do not declare done from reading the CSS diff alone.
