## Do

Hardcode the winning joined-output option chosen in the T206 review and remove the prototype
scaffolding. Read `.harness/worklog/T206.md` for the decision (the chosen rendering option +
dedupe-key convention). Then:
- Make the chosen option the single, permanent joined-output view on the workflow detail page.
- Remove the live option-switcher toggle and DELETE the unused option implementations + any now-unused
  libraries/dependencies they pulled in (clean up `package.json` if a rejected option's dependency is
  no longer used).
- Promote the chosen **dedupe-key convention** to a proper, documented convention: implement it
  cleanly (e.g. a per-workflow declaration of its output items' stable key) and wire the standard
  workflows onto it, so current AND future workflows expose deduped output the same way. Document the
  convention in CLAUDE.md (how a workflow declares its joined-output items + stable dedupe key).

Scope: `dashboard/app/components/**`, `dashboard/app/workflows/[name]/page.tsx`,
`dashboard/app/ui.tsx` (remove the toggle), `dashboard/app/lib/api.ts`, any `src/` code backing the
output feed + dedupe-key convention, `package.json`, CLAUDE.md.

## Done when

- The workflow detail page renders ONLY the chosen joined-output option (no switcher, no dead option
  code); any dependency unique to a rejected option is removed from `package.json`.
- The dedupe-key convention is implemented as a documented, reusable mechanism (CLAUDE.md describes how
  a workflow declares its output items + stable key) and the standard example workflows use it, so
  output is deduped consistently.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, `npm test`, and
  `node dashboard/scripts/mobile-check.mjs` are green; CLAUDE.md updated.
