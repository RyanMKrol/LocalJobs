## Do

Hardcode the winning workflow **graph (DAG) view** option chosen in the T206 review and remove the
prototype scaffolding. Read `.harness/worklog/T206.md` for the decision (the chosen graphing
library/approach). Then:
- Make the chosen option the single, permanent graph view for both the definition view
  (`/workflows/[name]`) and the run-scoped view, replacing the old `Dag.tsx` rendering.
- Remove the live option-switcher toggle and DELETE the unused option implementations + any
  now-unused graphing libraries/dependencies they pulled in (clean up `dashboard/package.json` so a
  rejected option's dependency is no longer installed).
- Keep everything the current view already does correctly: gate marks on the correct
  producer→consumer edges, per-stage status/gate state on the run view, mobile-responsiveness, and
  theme-variable-driven styling.

Scope: `dashboard/app/components/**`, `dashboard/app/workflows/[name]/page.tsx`,
`dashboard/app/workflow-runs/[id]/**`, `dashboard/app/ui.tsx` (remove the toggle),
`dashboard/app/globals.css`, `dashboard/app/lib/api.ts`, `dashboard/package.json`, `src/api/server.ts`,
`CLAUDE.md`.

## Done when

- The workflow graph view renders ONLY the chosen option (no switcher, no dead option code) on both
  the definition and run-scoped views; any dependency unique to a rejected option is removed from
  `dashboard/package.json`.
- The retained view still shows gate marks, per-stage run/gate state, and clearly represents
  waves/concurrency; it stays mobile-safe and theme-driven.
- `CLAUDE.md`'s description of the graph view (the `Dag.tsx` notes) is updated to match the new
  implementation.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, `npm test`, and
  `node dashboard/scripts/mobile-check.mjs` are green.
