## Do

Unify the styling of the grouped **output / manager** sections on the workflow detail pages so they
match the rest of the page's containers. Covers the Recommendations + Franchise-gaps sections on
`/workflows/movie-recommendations`, the Missing-TV-seasons section, and the equivalent tv-recommendations
section (`MovieRecsManager`, `MovieGapsManager`, `MissingSeasonsManager` in
`dashboard/app/workflows/[name]/page.tsx`; styling in `dashboard/app/globals.css`; reference the
generic `WorkflowOutputSection` from T205). Fix all of:
- **Inverted background:** the outer section panel is beige with white inner containers — the opposite
  of every other container (panel = the light/white surface on the darker page). Flip to match.
- **Inconsistent containment + spacing:** RUNS is a bare header+table while Recommendations/Franchise-gaps
  are wrapped in an extra beige container. Make every section one consistent header + container pattern
  with even spacing.
- **"Ignore all" button** jammed in the group's top-right corner (on the border-radius) — add inset/padding.
- **Per-row "Ignore" button** right-aligned in a wide empty table column — present it more tightly.
- **Group titles flush-left** against the border — add left padding.

## Done when

- The grouped sections use the same white `.panel` surface + consistent header/container/spacing as the
  rest of the page (no inverted beige nesting).
- "Ignore all" and group titles have proper inner padding (not hugging the corner/edge); the per-row
  Ignore action no longer sits in a wide empty column.
- Theme-variable-driven (all 3 families × light/dark) + mobile-safe; dashboard build + mobile-check green.
