## Do

In `dashboard/app/globals.css`, the `.table-group-header td` rule (currently `background: var(--panel-2); font-weight: 600; padding: 8px 14px;`, around line 189) is the SHARED row-group header style used by BOTH `MissingSeasonsManager`'s per-show header row and `MovieGapsManager`'s per-collection header row in `dashboard/app/workflows/[name]/page.tsx` (both render `<tr className="table-group-header">`). The owner reviewed the already-shipped result (T315 Missing Seasons, T316 Franchise Gaps) and found this row-group header too close to the page background, differentiated ONLY by bold text with no real visual separation.

Strengthen `.table-group-header td` so the group-header row is clearly visually distinct from both the surrounding panel background AND the plain data rows beneath it, holding up in BOTH light and dark mode (the dashboard's palette is CSS-variable driven via `--panel`, `--panel-2`, `--border`, `--accent` etc — see the `:root` and `[data-mode="light"]` blocks near the top of `globals.css`, and the T308 comment describing the dashboard's single sunny-8bit theme with a light/dark mode toggle). Concretely:
- Increase the background contrast beyond a flat `--panel-2` fill — e.g. blend it towards `--accent` at low opacity via `color-mix(in srgb, var(--accent) N%, var(--panel-2))`, or otherwise push it further from both `--panel` and plain data-row backgrounds so it reads as a distinct row at a glance, not via bold text alone.
- Add a real structural separator, not just colour — e.g. a `border-top`/`border-bottom` using `--border`, or an accent-tinted border consistent with other accent-driven borders already in this file (e.g. `.io-filter-chip.active`'s `border-color: var(--accent)`).
- Keep `font-weight: 600` (or similar) as a secondary cue, not the only one.
- This is a CSS-ONLY fix — do not change the row's HTML structure, `colSpan`, or content in `page.tsx`. Because both `MissingSeasonsManager` and `MovieGapsManager` share the exact same `.table-group-header` class, this single CSS change addresses both tables' complaint at once — do not duplicate a per-page fix.

## Done when

- `npx tsc --noEmit` and `npm test` (repo root) pass.
- `npm --prefix dashboard run build` succeeds.
- Run `node dashboard/scripts/visual-check.mjs` and LOOK at the resulting screenshots in `dashboard/scripts/visual-out/` for the `missing-tv-seasons` and `movie-recommendations` workflow detail pages (per the `PAGES`/`FLOWS` entries in `dashboard/scripts/_dashboard-harness.mjs`) — confirm with your own eyes that the row-group header rows (show name / collection name + "✕ Ignore all") in BOTH the Missing Seasons table and the Franchise Gaps table now read as visually distinct from the surrounding panel and from the plain data rows below them, not just bold text on a near-identical background.
- Manually confirm the fix holds in BOTH light and dark mode (toggle `data-mode` via the header's sun/moon control, or set `localStorage.setItem('localjobs.mode', 'light')` / `'dark'` and reload) — visually verify both render with clear contrast.
- Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green (no overflow regressions from the new border/background treatment).
- Record in the worklog that no `PAGES`/`FLOWS` update was needed (pure CSS, no new interactive state) — or add one if your chosen treatment introduces something worth a dedicated screenshot.
