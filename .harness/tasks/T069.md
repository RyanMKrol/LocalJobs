## Do

Make the DAG (components/Dag.tsx) prettier and easier to read. Right now nodes are essentially flat text, which is hard to scan when there are many. Give each job node a clear OUTLINE/box (bordered card — padding, rounded corners), make the connecting arrows/edges read as a flow, and keep the status colouring legible. Use your own judgement on the exact styling; the goal is a visually pleasing, scannable graph. Applies wherever the DAG renders (the workflow definition page and the workflow-run page).

## Done when

DAG job nodes render as outlined/boxed cards (not flat text) with clear flow edges and legible status colours, noticeably more scannable on both the workflow definition and workflow-run pages; dashboard build passes.
