## Do

**UI half of the skipped/no-op outcome work — depends on T258 (the backend status model).** T258
makes a run/stage that did NO actual work settle `skipped`/"nothing to do" instead of `success`.
This task **surfaces that status in the dashboard** so the owner sees "nothing to do", not a
misleading green "succeeded".

Do:
- In the workflow-run detail view (`dashboard/app/workflow-runs/[id]/page.tsx`) and the shared status
  rendering (`dashboard/app/ui.tsx` — e.g. the status badge/pill), render the `skipped`/"nothing to
  do" run + member-stage status distinctly from `succeeded` (its own pill/label), using the status
  T258 now records. Reuse the existing CSS-variable class idiom (`globals.css` `.pill*`/`StatusBadge`)
  — do NOT introduce new styling.
- The run-detail **Input→Output panel** must NOT show items as freshly "succeeded" when the run did no
  work for them — reflect the no-op/skipped state there too.

Do NOT change backend status logic here (that's T258); this task only reads + displays the status T258
produces. Scope: `dashboard/app/workflow-runs/[id]/page.tsx`, `dashboard/app/ui.tsx`.

## Done when

- A run/stage that did no work renders as **`skipped`/"nothing to do"** (distinct pill/label) in the
  run-detail view, and the IO panel does not show its items as freshly "succeeded".
- A run that did real work still renders as `succeeded` exactly as before (no regression to the normal
  case).
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, `npm test`, and
  `node dashboard/scripts/mobile-check.mjs` are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build and the screenshots in
  `dashboard/scripts/visual-out/` **confirm the skipped/"nothing to do" status renders** (visible,
  distinct from succeeded). Since this state only appears for a no-op run, add/update a `FLOWS` entry
  (and the synthetic fixtures) in `dashboard/scripts/_dashboard-harness.mjs` so a screenshot captures
  a run-detail page showing the skipped/nothing-to-do status; the harness (`PAGES`/fixtures/`FLOWS`)
  is updated to reflect it.
