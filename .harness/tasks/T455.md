## Overview
Today the 'Ignored (N)' panel in all 4 managers (MovieGapsManager, MovieRecsManager, TvRecsManager, MissingSeasonsManager) is a flat per-item table, even though MovieGapsManager and MissingSeasonsManager already group their ACTIVE list by collection/show via groupByCollection/groupByShow (page.tsx). This task makes the Ignored panel match: grouped with a per-group 'Un-ignore all' for gaps/seasons, and a single flat 'Un-ignore all' for the two recs managers (whose active lists are NOT grouped either — recs aren't naturally clustered by collection/show, and no bulk-ignore was added for them in this pass; the owner confirmed idea #11's 'ignore all' request was actually about franchise-gaps, which already had bulk-ignore via T210, so no separate recs-ignore-all task was authored). Depends on ignore-unignore-ux-b1 for the backend unignore-bulk endpoints.

## Do
In dashboard/app/lib/api.ts, add client methods calling the 4 new endpoints from ignore-unignore-ux-b1: unignoreMovieGapBulk(tmdbIds), missingSeasonsUnignoreBulk(items), unignoreMovieRecBulk(tmdbIds), unignoreTvRecBulk(tmdbIds).

In dashboard/app/workflows/[name]/page.tsx:
- MovieGapsManager's Ignored section (~page.tsx:510-539): replace the flat `ignored.map(...)` table body with `groupByCollection(ignored).map(...)`, reusing the SAME group-header markup pattern as the active list (~457-484) — a `table-group-header` row showing the collection name, with a per-group '↺ Un-ignore all' button (visible only when the group has >1 item, matching the existing 'hide redundant single-item bulk button' convention from the codebase) that confirms and calls api.unignoreMovieGapBulk(filmsInGroup.map(f => f.tmdbId)).
- MissingSeasonsManager's Ignored section (~page.tsx:703-731): same treatment using groupByShow(ignored), a per-show '↺ Un-ignore all' button calling api.missingSeasonsUnignoreBulk(seasonsInGroup.map(...)).
- MovieRecsManager's Ignored section (~page.tsx:330-356) and TvRecsManager's (~page.tsx:178-204): stay flat (no grouping — mirrors their active list per ignore-unignore-ux-a), but add ONE flat '↺ Un-ignore all' button above/beside the Ignored panel's table, visible when `ignored.length > 0`, calling api.unignoreMovieRecBulk(ignored.map(r => r.tmdbId)) / api.unignoreTvRecBulk(...).
- All new bulk un-ignore buttons should confirm(...) with a count-bearing message before firing (mirroring the existing bulk-ignore confirm wording, adapted for un-ignore: e.g. 'Un-ignore all N ...? They will be treated as brand-new and may be recommended/reported/notified again.'), and use a busy/disabled state + error surface identical in shape to the existing per-group ignore-bulk handlers.
- Update dashboard/scripts/_dashboard-harness.mjs (add a FLOWS entry if a new interactive state — e.g. an expanded/grouped Ignored panel — is worth its own screenshot) per the living-artifact rule.

## Done when
All 4 managers' Ignored panels render correctly: MovieGapsManager/MissingSeasonsManager group by collection/show with a per-group 'Un-ignore all' button that only un-ignores that group's exact currently-ignored items; MovieRecsManager/TvRecsManager show a flat 'Un-ignore all' button that un-ignores every currently-ignored rec. Un-ignored items reappear in the active list on the next poll and are treated as brand-new (never re-notified without a fresh detection, per T391 semantics already governing single-item unignore). npx tsc --noEmit, npm test, npm --prefix dashboard run build are green. node dashboard/scripts/visual-check.mjs runs clean and screenshots confirm the grouped panel + new buttons actually render (not just present in the DOM) for at least one workflow with >1 item in a group. dashboard/scripts/_dashboard-harness.mjs reflects the new UI surface.
