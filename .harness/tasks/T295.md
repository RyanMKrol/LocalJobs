## Do

Make `src/jobs/claude-warmer/claude-warm.job.ts` narrate itself more verbosely, per root
`CLAUDE.md`'s "log verbosely by default" convention. Specifically:

- Change the `ctx.log('Sending warm prompt to Claude to tick the usage window...')` line (~line 15)
  to include the actual model and prompt text being sent — reference the existing `WARM_MODEL`
  const (line 5) and the literal prompt string passed to `runClaude('hi', WARM_MODEL)` (line 17),
  e.g. `` ctx.log(`Sending warm prompt to Claude (model: ${WARM_MODEL}): "hi"`) `` — do not
  introduce a second hardcoded copy of the prompt text that could drift from the actual call; if
  practical, extract the prompt into a small named const (e.g. `WARM_PROMPT = 'hi'`) used by BOTH
  the log line and the `runClaude(...)` call, so they can never disagree.
- Also include the model in the three outcome log lines (~19 rate-limited, ~23 failed, ~26
  succeeded) so each line is self-contained when read on the run's log view without having to
  scroll up to the earlier line to know which model was used.
- Do not modify `src/services/claude.ts` (the shared `runClaude` helper) — this task is scoped to
  the one job file only.

## Done when

- The job's log output (verified by a unit test that stubs/injects `runClaude` and captures
  `ctx.log` calls — mirror however other job tests in this repo already stub a job's context/logger)
  includes the literal prompt text and the model name in the initial "sending" log line.
- The rate-limited, failed, and succeeded outcome log lines each also mention the model name.
- No other job behavior changes (retry count, timeout, error handling paths are unchanged).
- `npm test` and `npx tsc --noEmit` pass.
