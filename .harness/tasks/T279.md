## Do

**(needs-human — edits `.harness/loop.sh` (the loop's own driver, not CI-covered); build directly,
don't let the loop self-edit. Sibling of T261, which does the same for `human-done`.)**

Make `TASKS.json` status the **authoritative** terminal state by reconciling the **manual-fail**
overlay into it — mirroring T261's human-done reconcile. Add a loop pre-flight step (alongside the
T261 done-reconcile) that reads `.harness/manual-fail.json` and, for each id marked `failed: true`,
sets that task's `TASKS.json` status to a new terminal value **`failed`** and commits it (`[skip ci]`),
under the same decoupling as T261 (the dashboard still writes ONLY the overlay; the loop remains the
sole `TASKS.json` writer). Introduce the **`failed`** status into the task vocabulary/schema, and make
`select_task` treat `failed` as terminal — **skip it, never re-select** (NO auto-reopen; the actual
re-do work is tracked as separate follow-up tasks, e.g. T258/T259/T260). This will flip the existing
manually-failed tasks (T223/T238/T250) from `done` → `failed` in `TASKS.json`, which is correct (the
owner overturned them).

Keep `manual-fail.json` as the owner write-surface + the **reason** record + the **calibration input**
(the tier-demote + audit-rate correction still read the overlay — see `designs/manual-fail-signal.md`);
`status=failed` is the additive authoritative/selection/display state. The dashboard/`GET /api/backlog`
should prefer the reconciled `status=failed` (it already derives `failed` from the overlay, so this is
mostly making status the source of truth). Update the docs that say manual-fail "never modifies
TASKS.json status" (root `CLAUDE.md` overlay section, `.harness/CLAUDE.md`, the manual-fail design doc)
to describe the reconcile.

## Done when

- A loop pre-flight reconcile promotes `manual-fail.json`-failed ids to `TASKS.json` `status=failed`
  (committed), preserving the decoupling (dashboard writes only the overlay; loop is sole writer); a
  `failed` terminal status exists and `select_task` skips it (no auto-reopen). `manual-fail.json`
  remains the reason + calibration input.
- `bash -n .harness/loop.sh` passes.
- **Behavioural verification (record in the worklog):** in a scratch repo / temp fixture (NEVER the
  real backlog), a `manual-fail.json` marking a `done` task `X` failed → after the pre-flight, `X` is
  `status=failed` in that fixture `TASKS.json` and is NOT re-selected by the loop. No live anything.
- Dashboard shows a failed task as failed via the reconciled status (verify the Backlog view still
  renders the red failed state). Docs updated so they no longer claim manual-fail never touches
  `TASKS.json`.
