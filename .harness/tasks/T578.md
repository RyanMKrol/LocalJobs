## Overview
Re-attempt of T517, which blocked (correctly) because the migration chain covered only 6 of the 7 Plex-touching workflows — missing-movies/stages/snapshot.ts:29 still had a bare plexGet call, so the 'no bare Plex calls remain' verification gate could not pass. This unit now depends on the missing seventh migration (review-t517-a) plus the six done siblings, so it only builds once every Plex read/mutation actually routes through callService('plex', ...). It does the single root-CLAUDE.md file-map note no per-workflow unit owns, and serves as the repo-wide 'migration complete' verification gate that downstream tasks T477/T485/T487/T565 depend on. Deliberately tiny + docs-only so it cannot trip the git-hygiene guard.

## Do
FIRST verify all seven per-workflow units landed: run a repo-wide grep for bare plexGet(/plexPutStreams(/triggerButlerBackup( calls that are NOT inside a callService('plex', ...) wrapper. Use a NAME-ONLY grep for the function names (do NOT try to match the type parameter with a `<[^>]*>` regex — nested generics like `<PlexAllResponse<PlexMovieMeta>>` defeat `[^>]*` and caused the original T517 attempt's false-negative). For each hit, inspect the surrounding lines by hand to confirm it IS wrapped in callService('plex', ...) (the wrapper often spans multiple lines: `await callService('plex', () =>\n  plexGet<...>(...))`). Exclude: src/core/plex-client.ts itself (where the implementations live), the `?? plexPutStreams`/`?? triggerButlerBackup` default-assignment lines in plex-language-fix/stages/apply.ts, all `import` lines, comment/test-doc references, and any src/workflows/*/data/ clone directories (per the data/-scanning gotcha). After exclusions this must return NO genuinely-bare call. If a stray bare call remains, the corresponding per-workflow unit did not fully land — fail:blocked with the specific file/line rather than papering over it here (this unit does not fix stage code; it only documents and verifies). Then add a one-line note to root CLAUDE.md's src/core/plex-client.ts file-map entry (~line 298) that the shared 'plex' service now sits in front of plexGet/plexPutStreams/triggerButlerBackup (every Plex-touching workflow routes its Plex reads/mutations through callService('plex', ...), giving call-count metering + per-job consumer tracking on the Integrations page). Confirm src/core/plex-client.ts is byte-unchanged. GIT HYGIENE: stage ONLY CLAUDE.md by explicit name — never `git add -A`/`git add .`.

## Done when
A repo-wide name-only grep for plexGet(/plexPutStreams(/triggerButlerBackup(, hand-verified against callService('plex', ...) wrapping and with the documented exclusions applied (plex-client.ts, apply.ts `??` default lines, imports, comments/tests, data/ clone dirs), finds NO genuinely-bare call across all seven Plex-touching workflows — proving the migration is complete. src/core/plex-client.ts is byte-unchanged. Root CLAUDE.md's plex-client.ts file-map entry notes the 'plex' service now fronts the Plex calls. Only CLAUDE.md is staged. `npx tsc --noEmit` and `npm test` both green.
