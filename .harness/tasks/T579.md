## Overview
Re-attempt of T542: move the four inline dashboard manager components out of the 1,144-line workflows/[name]/page.tsx into their own component files and factor their duplicated busy/err/refetch handler block into one shared useAction hook — a pure refactor with no visible change. The first attempt (T542) built and fully verified this exact refactor (tsc, dashboard build, all tests, mobile-check 32/32, visual-check 53/54 with the one miss confirmed pre-existing on origin/main, and a manual pixel-equivalence check of all four screenshots) but reported failed:blocked and left the work uncommitted, because the spec's own '## Do'/'## Done when' explicitly required documenting the new components in CLAUDE.md's T268 shared-component list, while CLAUDE.md was not listed in T542's scope array — a scope-authoring gap, not a code defect. Since the work was never committed, the loop discarded it on the next tree reset; nothing from that attempt landed. This follow-up is identical in substance, with CLAUDE.md added to scope so the doc update the spec always required can actually be made in the same commit.

## Do
In dashboard/app/workflows/[name]/page.tsx, the four manager components (TvRecsManager ~66, MovieRecsManager ~240, MovieGapsManager ~416, MissingSeasonsManager ~643) are inline, making this page 4x the next-largest. Their per-action handlers repeat the same setBusy(true) -> setErr(null) -> await api.X -> await refetch() -> catch(setErr) -> finally(setBusy(false)) block ~14 times (e.g. ignore()/unignore()/unignoreAll() at :85-122). Add a useAction hook (in dashboard/app/ui.tsx or a new dashboard/app/components/useAction.ts) that owns that block and returns { run, busy, err } (or equivalent), guarding against setState-after-unmount. Then MOVE each of the four managers into its own file under dashboard/app/components/ (import them back into page.tsx). Keep groupByCollection/groupByShow with their managers. No behaviour or appearance change. If the page surface or the manager fixtures shift, update dashboard/scripts/_dashboard-harness.mjs in the same change (living-artifact rule). Document the new components in CLAUDE.md's T268 shared-component list (CLAUDE.md is in this task's scope — the doc update can and must happen in this same commit).

## Done when
The useAction hook exists once and replaces the ~14 duplicated handler blocks; each of the four managers lives in its own dashboard/app/components/ file and is imported by page.tsx; page.tsx is materially smaller (~360 lines, down from 1,144); npx tsc --noEmit, npm --prefix dashboard run build, node dashboard/scripts/mobile-check.mjs, and node dashboard/scripts/visual-check.mjs are all green and the four workflow detail pages (missing-movies, movie-recommendations, tv-recommendations, missing-tv-seasons) render PIXEL-EQUIVALENT to before (LOOK at the screenshots); _dashboard-harness.mjs is current; CLAUDE.md's T268 shared-component list documents the four new manager files and the useAction hook.
