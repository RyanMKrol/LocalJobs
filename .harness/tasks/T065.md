## Do

On the validation-gate page the producer/consumer log lines do not make it obvious WHAT the gate checks. Add a clear, VISUAL representation of the contract: a left-to-right flow — upstream stage OUTPUT → [the gate: what it validates] → downstream stage INPUT — so you can see at a glance what data the gate expects and whether it is satisfied. Surface the contract’s expected shape (key + description + the fields/columns/non-empty it asserts) and, where feasible, a small sample/summary of the ACTUAL artifact that flowed, with pass/fail per expectation. This likely needs the produces/consumes ArtifactContract to optionally declare a machine-readable expected shape (fields/columns) for display, and the gate to report expected-vs-actual rather than just ok/violations. Builds on T063 (dedicated gate page) + T064 (input/output framing).

## Done when

The gate page visually shows, left-to-right, output → gate(what it checks) → input, making it obvious what the gate validates and what actually flowed (expected shape + a sample/summary + pass/fail per expectation); contracts can declare an expected shape for display; typecheck + tests + dashboard build pass.
