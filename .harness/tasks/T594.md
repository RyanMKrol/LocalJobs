## Overview
GroupedManager's group-header 'Ignore all' / 'Un-ignore all' buttons are currently hidden for groups with only 1 item (e.g. a single-season show), so most group headers on missing-movies/missing-tv-seasons never show a bulk-ignore option. Remove the size check so the button always renders regardless of group size.

## Do
In dashboard/app/components/GroupedManager.tsx there are two near-identical group-header blocks that each currently wrap their action button in a size guard: the active-side block (currently around line 113) reads `{items.length > 1 && (<button className="btn btn-sm" onClick={() => doIgnoreGroup(gk, items)} disabled={group.busy === gk} style={{ flexShrink: 0 }}>{group.busy === gk ? 'Ignoring…' : '✕ Ignore all'}</button>)}`, and the ignored-side block (currently around line 163) reads `{items.length > 1 && (<button className="btn btn-sm" onClick={() => doUnignoreGroup(gk, items)} disabled={ignoredGroup.busy === gk} style={{ flexShrink: 0 }}>{ignoredGroup.busy === gk ? 'Un-ignoring…' : '↺ Un-ignore all'}</button>)}`. Remove the `items.length > 1 &&` condition from BOTH blocks so each button element renders unconditionally (keep everything else — the button's props, disabled state, and label logic — unchanged). The underlying `doIgnoreGroup(gk, items)`/`doUnignoreGroup(gk, items)` handlers already iterate `items` regardless of length, so a 1-item group's button click needs no other code change.

## Done when
Both `items.length > 1 &&` conditionals around the group-header action buttons in dashboard/app/components/GroupedManager.tsx are removed, so the '✕ Ignore all' button (active side) and '↺ Un-ignore all' button (ignored side) render for every group regardless of item count, including single-item groups. `npx tsc --noEmit` and `npm run build` in dashboard/ succeed. Run `node dashboard/scripts/visual-check.mjs` after building and visually confirm (via the screenshots it writes) that on the missing-tv-seasons and/or missing-movies workflow detail page, a single-item group (a show with only one missing season, or a collection with only one gap film) now shows an 'Ignore all' button in its group header, and clicking it still correctly ignores that single item (functionally exercised via the existing doIgnoreGroup/doUnignoreGroup handlers — no behavior change there).
