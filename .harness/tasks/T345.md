## Do

On the TV Recommendations workflow detail page (`/workflows/tv-recommendations`), `TvRecsManager` in `dashboard/app/workflows/[name]/page.tsx` renders a plain, un-boxed `<p className="muted">No recommendations yet. Run the workflow manually — the recommended shows will appear here.</p>` (lines ~117-121) when `data.generatedAt == null`. By contrast, once recommendations exist, the SAME component renders them inside `<div className="panel"><table>...</table></div>` (line ~130 onward) — an 'obvious white background table' look. The owner wants the empty case to use a similar boxed/panel look too: a panel containing nothing but a single sentence, both horizontally AND vertically centred within the box.

Do this in two parts:

1. **Add a small, reusable CSS convention to `dashboard/app/globals.css`** for 'a panel whose only content is one centred muted sentence' (a boxed empty-state). There is already a closely-related precedent in this codebase worth studying and reusing/adapting rather than duplicating from scratch: `.io-empty-state` (globals.css, search for 'Input→Output empty-state (T195)') is used in `dashboard/app/workflow-runs/[id]/page.tsx` for exactly this shape — a centred muted paragraph rendered INSIDE an ancestor `<div className="panel">` (see that file around 'Input → Output mapping', where `<p className="io-empty-state">{emptyMessage}</p>` sits directly inside the wrapping `.panel` div). That class already achieves horizontal centring (`text-align: center`) and a form of vertical centring (equal top/bottom padding, so the text sits equidistant from the panel's top and bottom edges since the panel has no other content to stretch it). Add a NEW, genuinely reusable class for this general convention (do not rename/touch `.io-empty-state` itself — that's IO-panel-specific naming and out of scope here; leave `workflow-runs/[id]/page.tsx` untouched). Something like `.empty-state-panel` (pick a clear, non-IO-specific name), styled to horizontally AND vertically centre a short sentence within a `.panel`-classed wrapper (e.g. `text-align: center` plus enough vertical padding, or flexbox centring with a sensible `min-height`, so it visibly reads as centred rather than just top-padded) — use your judgement on the simplest CSS that achieves genuine visual centring, matching what already works for `.io-empty-state`, but make the class name and its doc-comment describe it as the GENERAL 'boxed empty-state' convention, not tied to any one page. Add a short comment above the rule saying so (e.g. '/* Boxed empty-state convention: a panel containing nothing but a single centred sentence — for "nothing here yet" states across the dashboard */').

2. **Apply it to the TV Recommendations empty state.** In `TvRecsManager` (`dashboard/app/workflows/[name]/page.tsx`), replace the bare `<p className="muted">...</p>` at the `data.generatedAt == null` branch with `<div className="panel"><p className="<your-new-class>">No recommendations yet. Run the workflow manually — the recommended shows will appear here.</p></div>` (keep the exact existing sentence text — only the wrapping/classing changes). Do NOT touch any other empty-state text in this file (MovieRecsManager's 'No recommendations yet...' at ~line 255, Franchise Gaps' 'No audit has run yet...' at ~line 399, Missing Seasons' 'No check has run yet...' at ~line 573) or `WorkflowOutputSection.tsx`'s 'No output yet...' (~line 121) — those are noted follow-up candidates for a LATER task, explicitly out of scope here to keep this task small and independently verifiable. Also do not touch `services/page.tsx` — its 'No services defined.' empty state is ALREADY wrapped in a `<div className="panel">` (confirmed by reading the file), so it already satisfies this convention and needs no change. Likewise leave the already-boxed 'No runs yet.' `<tr><td colSpan={5} className="muted">No runs yet.</td></tr>` rows (`workflows/[name]/page.tsx` ~line 904, `jobs/[name]/page.tsx` ~line 159) alone — they already sit inside an existing table's boxed styling and already satisfy the convention as-is.

3. **Document the convention.** Add a short bullet to root `CLAUDE.md`'s Conventions section (near the existing 'Shared dashboard UI components (T268)' bullet) noting the new boxed-empty-state CSS class/convention, what it's for, and that it should be reused for future 'nothing here yet' states instead of a bare `<p className="muted">` floating on the page background — per this repo's Definition of Done requiring docs to be updated in the same change as the code.

A different, concurrently-run task/idea ('table-styling-convention') is separately addressing shared-table-component work on `services/page.tsx` and `workflows/page.tsx` (padding, panel spacing, a possible shared `CategoryTable` component) — no hard dependency exists between that work and this one; this task only touches the TV Recommendations empty state + the new reusable CSS class + this doc note.

## Done when

`npx tsc --noEmit` and `npm test` pass. `npm --prefix dashboard run build` succeeds. Run `node dashboard/scripts/visual-check.mjs` and LOOK at the resulting screenshot(s) in `dashboard/scripts/visual-out/` for the TV Recommendations workflow detail page (`/workflows/tv-recommendations`) in its empty state (no recommendations generated yet, per whatever fixture `_dashboard-harness.mjs` currently uses) — confirm by eye that the empty-state sentence now renders inside a visibly boxed/panelled container (matching the look of the populated-state table panel), with the text appearing both horizontally and vertically centred within that box, rather than as bare floating text on the page background. If the current synthetic fixtures in `dashboard/scripts/_dashboard-harness.mjs` don't already exercise the TV Recommendations empty state (i.e. the fixture always returns recommendations), update the fixture (or add a `FLOWS`/`PAGES` entry) so the empty case is actually captured by a screenshot — per the `.harness/CLAUDE.md` living-artifact rule, a UI change whose only visible effect is in a state the harness doesn't currently screenshot must add that coverage in the same change. Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green (no new horizontal overflow at phone width). Confirm root `CLAUDE.md` now documents the new boxed-empty-state convention. This is a `layer:ui` task — visual confirmation via the screenshots is a hard requirement; do not declare done from reading the CSS/JSX diff alone.
