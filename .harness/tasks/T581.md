## Overview
Re-attempt of T554, blocked because its scope (src/db/store.ts, src/db/store.test.ts) pointed at the pre-R03 file layout — R03/T529 landed the day before T554 ran and moved the exact dead-code section this task targets into src/db/store/admin.ts, which T554's scope excluded, so the agent correctly treated touching it as scope creep and gave up. Same code, same deletion, corrected file path.

## Do
1. In src/db/store/admin.ts: delete the entire 'Read-only DB browser (dashboard)' + 'Canned (predefined) read-only queries' sections (currently the tail of the file, roughly lines 264-412 — re-grep by symbol name since line numbers may have shifted again) — listDbTables, isKnownTable, the TablePage interface, browseTable, CannedQueryDef, CANNED_QUERIES, CannedQueryMeta, CannedQueryResult, listCannedQueries, runCannedQuery. Remove any now-unused imports at the top of admin.ts that only these functions needed (check db, listJobs, getWorkflowJobs/listWorkflows, listServices are still used by the remaining stale-override-audit code before removing any). 2. In src/db/store.test.ts: delete the import of browseTable/listDbTables/listCannedQueries/runCannedQuery (currently line ~6) and the two test blocks that exercise them (the browseTable pagination/injection block ~lines 838-863 and the canned-query block ~lines 882-933). Do NOT skip them — remove them entirely. 3. Confirm no /api/db route exists in src/api/server.ts (already gone since T230 — this should be a no-op confirmation, not a change). 4. If store.ts's barrel re-export (`export * from './store/admin.js'` or similar) references any of the deleted symbols by name, remove that too — but do not otherwise touch the barrel structure (that's R03/T529's territory, already done).

## Done when
npx tsc --noEmit and npm test are green with the deleted test blocks fully removed (not skipped, not commented out). A grep for listDbTables / isKnownTable / browseTable / CANNED_QUERIES / listCannedQueries / runCannedQuery / TablePage / CannedQuery across src/ and dashboard/ (excluding the projects-sync self-clone under src/workflows/projects-sync/data/repos/) returns zero remaining references. No /api/db route exists in src/api/server.ts (confirm, don't just assume).
