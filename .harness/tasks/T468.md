## Overview
Today one workflow (movie-recommendations, src/workflows/movies/movies.workflow.ts) bundles a deterministic franchise-gap audit together with a subjective 8-branch recommendation fan-out, sharing one movie-snapshot stage and pushing one combined digest. This task splits it into two independent workflows exactly the way tv-recommendations and missing-tv-seasons already are for TV: a trimmed movie-recommendations (snapshot → 8 rec branches → rec-merge → recs-only notify) and a brand-new missing-movies (its own snapshot → franchise-gaps → gaps-only notify), per the decisions recorded in split-movie-workflows-design.

## Do
Follow the concrete decisions recorded by the split-movie-workflows-design task (name/schedule/category/folder-layout/snapshot-duplication) — do not re-derive them. Implementation shape, based on the current code (read it first, this is a summary not a literal diff):

1. New job: duplicate the Plex movie snapshot fetch (currently src/workflows/movies/stages/snapshot.ts, using plexGet + buildMovieSnapshots from src/workflows/movies/movies.ts) into a new snapshot job scoped to the new missing-movies workflow, writing to its own data/out/snapshot.json under the new workflow's own config/data dir (per the design's folder-layout decision). Per the design record, this new snapshot job should likely SKIP building/writing taste-profile.json (franchise-gaps.ts never reads it).
2. Move franchise-gaps (src/workflows/movies/stages/franchise-gaps.ts/.job.ts/.test.ts) so it depends on the NEW snapshot job instead of the shared movie-snapshot, keeping its own job `name: 'franchise-gaps'` UNCHANGED and its produces/consumes contracts pointing at the new snapshot's output file.
3. Split notify.ts: today runNotify() in src/workflows/movies/stages/notify.ts reads BOTH franchise-gaps.json and recommendations.json and sends one combined digest. Split this into two independent notify stages: (a) a gaps-only notify (keep the job name 'movie-gaps-notify' and the NOTIFY_JOB ledger-key constant EXACTLY as today — per the design record this requires zero work_items migration) that only reads franchise-gaps.json and sends its own digest/report, now the terminal stage of the missing-movies workflow; (b) a recs-only notify (in movie-recommendations) that only reads recommendations.json, keeps the RECS_JOB='movie-recs' ledger key unchanged, and sends its own separate digest/report — mirroring how tv-recs-notify and plex-seasons-notify are two fully independent pushes for TV.
4. Update movies.workflow.ts's jobs[] list to drop franchise-gaps entirely, leaving movie-snapshot → 8 rec branches → rec-merge → (the new recs-only notify stage).
5. Author the new missing-movies.workflow.ts (schedule/category per the design decision) with jobs: [new-snapshot-job] → franchise-gaps → (the new gaps-only notify stage). It is NOT limitable (no member declares inputKeys()), same as its siblings.
6. Update contracts.ts (or a new contracts.ts under the new folder) so every DAG edge in BOTH workflows still has a produces/consumes gate — src/core/gate-coverage.test.ts enforces this and will fail the build if any edge is missing one. Reuse the existing movieSnapshotContract/franchiseGapsContract/recommendationsContract factory shapes as templates.
7. Update src/workflows/movies/CLAUDE.md to describe the now-recs-only workflow (drop the franchise-gaps DAG description, the ignore-to-suppress-gaps section moves to the new workflow's doc). Author a new src/workflows/missing-movies/CLAUDE.md (or extend movies/CLAUDE.md if the design chose a shared folder) describing the new workflow's DAG, stages, ledger, and ignore-to-suppress mechanism (moved verbatim from the old doc — the mechanism/endpoints are UNCHANGED, only which workflow the notify job lives under changes).
8. Update the root CLAUDE.md 'Shipped example workflows' table: it currently lists 14 workflows including movie-recommendations described as doing BOTH franchise gaps and recommendations — split this into two rows (movie-recommendations: recs-only; missing-movies: franchise-gap audit only), bringing the shipped-example count to 15, and update the T146/T145 references there if their wording assumes the combined workflow.
9. Leave the /api/movie-gaps* and /api/movie-recs* endpoints, ignoreSurfacedItem/unignoreSurfacedItem plumbing, and the work_items ledger completely untouched — per the design record these require no changes because the ledger is keyed by job name/item key, not workflow membership.

Run npx tsc --noEmit and npm test (which includes src/core/gate-coverage.test.ts) before considering this done — do not skip verifying the gate-coverage test specifically, since a missed contract on either new DAG edge fails it.

## Done when
Two independent *.workflow.ts manifests exist and both load cleanly (daemon registry has no orphan-job or duplicate-workflow-name errors): a trimmed movie-recommendations (snapshot → 8 rec branches → rec-merge → recs-only notify) and a new missing-movies (its own snapshot → franchise-gaps → gaps-only notify). Every DAG edge in both workflows has a matching produces/consumes gate. The movie-gaps-notify job name and NOTIFY_JOB/RECS_JOB ledger-key constants are unchanged from today, so no work_items migration is needed or performed. Both workflows' CLAUDE.md docs are current and accurate, and the root CLAUDE.md workflow table reflects both as separate rows. npx tsc --noEmit and npm test are both green, including src/core/gate-coverage.test.ts.
