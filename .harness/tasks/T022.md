## Do

For a repeat-until-stable pipeline (perfumes), each cycle re-runs all stages, so one pipeline run accumulates ~one row per stage per cycle (can be ~100). It is accurate but overwhelming — group/collapse the member-runs list (e.g. latest per stage, or grouped by cycle/stage) rather than a flat 100-row list.

## Done when

The pipeline-run member-runs list is grouped/collapsed (e.g. latest per stage per cycle) instead of a flat long list, while still letting you reach each member run; the dashboard build passes.
