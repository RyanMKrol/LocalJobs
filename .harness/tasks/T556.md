## Overview
Delete ~90 lines of backlog-era CSS (review/task-row/caret classes) and the legacy hand-rolled DAG classes that React Flow replaced, prune Pill.tsx's kind catalog to only the kinds actually rendered, and fold the duplicated :root block and the two @media (max-width:640px) blocks each into one — matching the documented 'responsive rules in one place' convention.

## Do
1. In globals.css, delete the backlog-era rules with zero markup users: .section-heading-summary, .caret-style-* variants, .dep-id-link, .task-row-highlight, .done-row, .task-expand-body, .review-toggle, .review-filter-bar (~lines 303-397), and the legacy .dag-inner/.dag-col DAG classes. 2. For Pill kinds: grep each kind's usage in dashboard/app/**.tsx; remove the unused kind's `.pill.<kind>` rule from globals.css AND its entry from Pill.tsx's kind catalog/type. Keep every kind still rendered (confirm .paid/.free/.done/.on/.off/.certified/.info/.failed at minimum). 3. Fold the two :root declarations (line 1 and ~700) into a single coherent :root, preserving the documented dark-default palette + the fixed --logs-* terminal palette exactly. 4. Fold the two @media (max-width:640px) blocks (~617 and ~812) into ONE block. 5. Do not alter the default/dark theme's rendered look — the untouched-default invariant must hold.

## Done when
npx tsc --noEmit and npm --prefix dashboard run build are green. Every removed CSS class and Pill kind has zero markup references in dashboard/app/**.tsx. globals.css contains exactly one :root rule and exactly one @media (max-width:640px) block. Pill.tsx's kind catalog lists only rendered kinds. node dashboard/scripts/mobile-check.mjs passes and visual-check screenshots across the pages confirm no visual regression (the default dark look is unchanged).
