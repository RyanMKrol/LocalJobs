## Do

Human review of the `stocks-sync` workflow end-to-end, now that it's built and its known issues
(T300: stocks-watch incorrectly marked skipped; T301: missing ISA account) are fixed. This is a
reminder/checkpoint, not a checklist — the owner will ask follow-up questions ad hoc during the
review rather than working through fixed criteria.

## Done when

The owner has reviewed the `stocks-sync` workflow (`stocks-snapshot` → `stocks-watch` →
`stocks-notify`) as it runs for real — the portfolio snapshot, the gain/loss reporting, and the 30%
gain notification — and is satisfied with its behavior, or has raised follow-up items (new ideas/
tasks) for anything that needs to change.
