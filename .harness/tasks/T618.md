## Overview
The vercel-daily-redeploy workflow records no work_items ledger at all today, so its dashboard Inputs & Outputs panel shows nothing on the Output side. Make its single job record ONE per-day ledger row per run describing that day's redeploy outcome (deployed / skipped / failed) so the Output panel reflects the real result, while the Input side deliberately stays empty (there genuinely is no input — a single daily trigger).

## Do
In src/workflows/vercel-daily-redeploy/vercel-redeploy.job.ts, add exactly ONE markWorkItem call per run inside runVercelRedeploy, keyed by dayKey(now) from src/core/dates.ts (import it; a same-day manual re-run must overwrite the same row, not duplicate — dayKey gives this via the (job_name, item_key) UNIQUE upsert). Cover every existing outcome branch, all currently confirmed in this file: (1) soft-skip when RYANKROL_CO_UK_PATH is unset or the checkout path is missing on disk (lines ~36-43); (2) soft-skip when callService('vercel', ...) throws QuotaExceededError (lines ~57-60); (3) a successful deploy — capture the real deploy URL (currently `deployUrl` at line ~116, only used in a log) and surface it in the ledger detail; (4) a failed deploy (deployOnce rejects on timeout ~line 109 or non-zero exit ~line 113). Each row's `detail` must describe what THAT run produced per src/workflows/CLAUDE.md's 'success detail' rule — carry an `outcome` discriminator ('deployed' | 'skipped' | 'failed'), a human-readable `name`, the `deployUrl` on success, and the specific `reason` (missing-config vs quota-exhausted) on a skip / the error message on a failure. Mirror the reference callers: stocks-fetch (src/workflows/stocks-sync/stages/stocks-fetch.ts, dayKey-keyed plain detail object) and overrides-audit-scan (src/workflows/overrides-audit/stages/scan.ts, weekKey-keyed). deployOnce currently only resolves void — thread the captured deployUrl (and, on the failure path, the error) back up to runVercelRedeploy so the single markWorkItem call can record it (e.g. have deployOnce resolve the URL, and record the ledger row from runVercelRedeploy's success/catch handlers). Follow the coordinator's answers on (a) whether the deploy-failure path records a markWorkItem(...,'failed',...) row in ADDITION to the run's own failed status, and (b) plain detail object vs file-backed detail.format:'json'+detail.path artifact — implement whichever the coordinator confirms in .harness/.pending-questions/vercel-redeploy-ledger.json's answers; if file-backed, write the artifact under this workflow's data/out/ and add data/out to scope. Update the workflow's own CLAUDE.md (src/workflows/vercel-daily-redeploy/CLAUDE.md) in the SAME change: replace the current 'No work_items ledger — pure fire-and-forget trigger, no items to track' line with an accurate description of the new per-day outcome ledger row and that the Input side stays intentionally empty. Do NOT add inputKeys() — this workflow has no input list and must stay unlimited/scheduled-only. No gate is involved (single-stage workflow, no DAG edge). OWNER-CONFIRMED (loop-prep): (a) on the deploy-FAILURE path, ALSO write a markWorkItem(..., "failed", ...) row IN ADDITION to the run own failed status. (b) Use a FILE-BACKED artifact: write a small JSON summary to this workflow data/out/ and record detail.format:"json" + detail.path (the overrides-audit/plex-space-saver pattern), so it becomes View-able once T-<#17> lands. Record ALL FOUR outcomes (deployed / skipped-missing-config / skipped-quota / failed). Because the file-backed artifact is only clickable once idea #17 (the viewable-signal fix) ships, this task dependsOn that #17 task (wired via tempId at consolidation).

## Done when
runVercelRedeploy records exactly one markWorkItem row per run keyed by dayKey(now) covering all four outcome branches, with detail carrying the outcome discriminator + deployUrl (on success) / reason (on skip/fail). A new unit test in vercel-redeploy.test.ts injects spawnFn + callServiceFn (reusing the existing fakeCtx / makeFakeChild / bypassCallService helpers) to drive each of: deployed (asserts the ledger row exists via getWorkItem for the dayKey, status per the confirmed decision, and detail contains the injected deploy URL), skipped-missing-config, skipped-quota-exhausted (each asserts the row's outcome/reason), and failed (asserts the failure is recorded per the confirmed decision on whether a 'failed' row is written). Tests run against the scratch DB (no real vercel CLI, no real checkout — mirror the existing hermetic setup). npx tsc --noEmit, npm test, and the visual-check screenshots of the vercel-daily-redeploy workflow's Output panel (if the coordinator sets visualVerify true and the panel can be fixtured) are all green, and src/workflows/vercel-daily-redeploy/CLAUDE.md no longer claims 'no work_items ledger'.
