## Do

On the run detail page (`dashboard/app/runs/[id]/page.tsx`), the job-status pill (e.g.
"✅ Succeeded") is **vertically centred against the whole title block** — the job-name `<h1>` plus
the "run" subtitle beneath it — so the pill sits slightly BELOW the job-name line and looks
misaligned. Align the pill with the **title line** (the job name) instead.

- The header is a flex row (likely `.row` with `align-items: center`) holding the title column
  (`<h1>` job name + the small "run"/type subtitle below it) and the status pill on the right.
  Centring against that two-line column drops the pill below the `<h1>` baseline.
- Fix so the pill reads as being **on the same line as the job name**: e.g. put the `<h1>` and the
  pill in one flex line (centre/baseline-aligned to each other) with the subtitle on its own line
  below, or align the pill to the top of the title column so it lines up with the `<h1>`. Don't let
  the subtitle pull the pill's vertical position.
- Check the **workflow-run detail page** (and any other page using the same title + status-badge
  header pattern) and apply the same alignment if it has the issue, for consistency.
- Mobile-responsive (the pill may wrap below the title at phone width — that's fine as long as it's
  not awkwardly mid-aligned on desktop).

## Done when

On the run detail page the status pill is aligned with the job-name title line (no longer centred
against the title+subtitle block / sitting below it); the same fix is applied to any other header
sharing the pattern (e.g. the workflow-run detail page); `npm --prefix dashboard run build` passes
and `node dashboard/scripts/mobile-check.mjs` stays green.
