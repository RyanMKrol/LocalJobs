## Do

**(needs-human — cross-repo design decision; the loop cannot build in another repo.)** Decide what a
**portable, project-agnostic backlog dashboard** should look like as a shippable part of the Claude
Skills Ralph-harness plugin (the distributable harness), so any project that scaffolds the harness can
optionally get a backlog view out of the box — not just this repo.

This repo's Backlog page is the reference: `dashboard/app/backlog/page.tsx` renders `.harness/TASKS.json`
grouped Ready / Waiting-on-deps / Needs-a-human / Done, with each task's `## Do` / `## Done when` spec
inline, dependency display, and owner controls — review/mark-reviewed (single + bulk), mark-done
(needs-human), mark-failed (manual-fail-signal) — backed by `GET /api/backlog` + the disjoint
owner-owned overlays (`reviews.json` / `human-done.json` / `manual-fail.json`). The plugin today ships
the loop machinery (loop.sh, supervise.sh, TASKS.json schema, the add-to-backlog / create-harness
skills) but NO dashboard.

DECIDE (and record the decision — e.g. a short design note in the plugin repo, alongside the T188
parity work): which portable form to pursue —
- a tiny standalone served/static dashboard the plugin drops into a project (framework-light), vs
- a framework-agnostic template projects adapt, vs
- documenting this repo's Next.js pages as the canonical reference to copy.
Plus: how it reads `TASKS.json` + the overlays without a bespoke daemon API (the local-jobs `/api/backlog`
server is project-specific), and whether the interaction controls (review/done/failed) are in scope v1
or read-only first.

The actual BUILD is a separate follow-up (also cross-repo, in the plugin) created once the form is
chosen. Relates to T188 (parity audit `.harness/` vs the plugin) — ideally sequence after it.

## Done when

- A recorded decision exists (in the plugin repo / its design notes) on the portable backlog-dashboard
  form, covering: the delivery shape (standalone app vs template vs reference), how it reads
  TASKS.json + the overlays portably, and the v1 interaction scope (read-only vs the review/done/failed
  controls). This is a human decision — mark done once that decision is captured; no code in THIS repo
  is required.
