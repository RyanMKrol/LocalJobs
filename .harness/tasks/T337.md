## Do

Add a brand-new example workflow `stock-digest` (own folder `src/jobs/stock-digest/`, own `stock-digest.workflow.ts`) that produces a weekly, Claude-narrated markdown summary of the owner's current stock holdings. This is DISTINCT from `stocks-sync` (which only snapshots + threshold-alerts) — do not add a stage to `stocks-sync`; this is its own workflow with its own schedule, per the owner's explicit direction.

Owner's decisions for this task:
- Separate new workflow, own folder, own weekly cron schedule (e.g. `'0 8 * * 1'` — Monday 08:00, deliberately AFTER `stocks-sync`'s existing daily `'0 7 * * *'` run so a same-day-fresh snapshot is typically available; document this timing choice in the workflow's header comment). `category: 'regular-maintenance'` (matches stocks-sync/listening-digest/projects-sync/claude-warmer).
- Content for THIS task: (a) a plain narrated read of current holdings (ticker, quantity, current value, % of total portfolio value) AND (b) a performance-focused section (winners/losers since purchase, biggest % movers) reusing the same gain-since-average-buy-price math `stocks-watch` already computes (`(currentPrice - averageBuyPrice) / averageBuyPrice`). Do NOT attempt a sector/diversification breakdown in this task — that is deliberately deferred to a follow-up task (stock-portfolio-digest-2) because it needs a brand-new external data source Trading212 doesn't provide.
- Markdown-only output — NO push notification. Mirrors `listening-digest` exactly in this respect (`src/jobs/listening-digest/stages/listening-digest.ts`): write a report file under `data/out/`, nothing pushed via `notifier`/`push()`.

Cross-workflow read (a NEW pattern in this repo — no existing workflow reads another workflow's `data/out/` file; document this clearly since it's a precedent):
- Import the resolved output path directly from `stocks-sync`'s own config module: `import { stocksSyncConfig } from '../stocks-sync/config.js'` (gives `portfolioJsonPath`), and reuse the existing typed shape by importing `NormalizedPosition` from `../stocks-sync/stages/stocks-snapshot.js` — do NOT redefine or duplicate that shape. This keeps the read a plain relative import within the same repo (no absolute paths, no new cross-workflow API), consistent with the 'repo is self-contained' rule in root CLAUDE.md.
- The two workflows are NOT DAG-linked (no `dependsOn` across workflow boundaries — the framework doesn't support that) — `stock-digest` simply reads whatever `portfolio.json` currently exists on disk at run time. Handle the case where the file is missing or empty (stocks-sync hasn't run yet, or ran with zero positions) gracefully: log a clear WARN and skip the run's item work (do not throw / do not crash the workflow) — mirror how other jobs in this repo soft-skip on no-input.

Implementation shape (mirror `listening-digest`'s narration pattern in `src/jobs/listening-digest/stages/listening-digest.ts` and its `config.ts`):
- `src/jobs/stock-digest/config.ts` — resolves `data/out/` path for this job's own output (e.g. `stockDigestConfig.outDir`, `reportPathFor(weekKey)`).
- `src/jobs/stock-digest/stages/stock-digest-build.job.ts` + `stock-digest-build.ts` (+ `.test.ts`) — the single stage for this task:
  1. Read `stocksSyncConfig.portfolioJsonPath`, parse the `NormalizedPosition[]`.
  2. Compute per-position gain % (reuse/port the same formula `stocks-watch` uses — consider extracting a small shared pure helper if that avoids duplicating the exact math, but do not go out of scope refactoring `stocks-watch` itself).
  3. Compute % of total portfolio value per position (`currentValue / sum(currentValue)`).
  4. Rank movers (top N winners/losers by gain %).
  5. Build a structured JSON summary object (holdings list + movers + totals) and feed it to `runClaude` (`src/services/claude.ts`) with a prompt asking for a short narrated markdown report — mirror `listening-digest`'s prompt-construction style (build the structured facts in code, let Claude narrate/format prose around them, don't let Claude invent numbers).
  6. Write the result to `data/out/stock-digest-<weekKey>.md` (ISO week key, e.g. `2026-W27`, mirroring `listening-digest`'s `monthKey` helper pattern but for weeks — add a `weekKey(date)`/`weekLabel(date)` pair of pure exported helpers, unit test them directly like `monthKey`/`monthLabel` are implicitly testable).
  7. Record the produced markdown artifact via `markWorkItem(ctx, weekKey, 'success', { detail: { name: ..., markdown: mdPath } })` (T110 convention) so it surfaces in the workflow's unified Output section (T205) with no extra wiring — this is a single-stage workflow, so it IS the terminal stage.
- Idempotency: single stage, not limitable (no `inputKeys()` — nothing to fan out over, matches `listening-digest`), idempotent per ISO week via the `work_items` ledger keyed by `weekKey` (mirrors `listening-digest`'s per-calendar-month idempotency) — a manual re-run the same week regenerates that week's file rather than duplicating it.
- `stock-digest.workflow.ts`: one-stage `WorkflowDefinition`, `schedule: '0 8 * * 1'`, `category: 'regular-maintenance'`, doc comment explaining the design (weekly, reads stocks-sync's snapshot, no push, cross-workflow file read pattern).
- Be verbose with `ctx.log()` throughout per the root CLAUDE.md logging convention (holdings read, gain computed per position, movers picked, Claude call issued, report written) — this is a hard repo-wide convention, not optional.

Docs (Definition of Done, part of this same task): add a `stock-digest` section to root `CLAUDE.md` (mirroring the existing per-workflow paragraphs for stocks-sync/listening-digest/projects-sync) and to `README.md`'s workflow list, describing the workflow, its weekly schedule, its cross-workflow read of `stocks-sync`'s `portfolio.json`, and that it is markdown-only (no push).

## Done when

`src/jobs/stock-digest/` exists with `stock-digest.workflow.ts` (weekly cron schedule, `category: 'regular-maintenance'`) and one stage `stock-digest-build` that: reads `stocks-sync`'s `data/out/portfolio.json` via a plain relative import of `stocksSyncConfig`/`NormalizedPosition` (no duplicated shape, no absolute path); computes per-position gain % and % of total portfolio value; picks top winners/losers; narrates a markdown report via `runClaude`; writes `data/out/stock-digest-<weekKey>.md`; records the artifact via `markWorkItem(..., { detail: { markdown } })`; is idempotent per ISO week via the `work_items` ledger. No push notification is sent anywhere in this workflow. The workflow is auto-discovered (no registry edits needed), appears on the dashboard Workflows page under `regular-maintenance`, and its single stage's output surfaces in the unified Output section automatically. Missing/empty `portfolio.json` is handled with a clear WARN log and a clean skip, not a crash. `npx tsc --noEmit`, `npm test` (including new unit tests for the pure helpers — gain %, weekKey/weekLabel, movers-ranking, portfolio-share) all pass. Root `CLAUDE.md` and `README.md` are updated with a `stock-digest` section describing the workflow, schedule, the cross-workflow read pattern, and the markdown-only/no-push behaviour.
