## Do

Add an explicit **Dark / Light / System** mode control inside the existing `ThemeControls` (🎨) picker
in `dashboard/app/ui.tsx`, replacing today's automatic time-of-day mode selection. Currently `data-mode`
(`light`/`dark`) is chosen from the local hour via `modeForHour(hour)` + `useTimeMode()` in
`dashboard/app/ui.tsx`, and set before first paint by the inline pre-paint script in
`dashboard/app/layout.tsx`. Change this so the viewer picks the mode:

- The picker offers three choices: **Dark** (force `data-mode="dark"`), **Light** (force
  `data-mode="light"`), and **System** (follow the OS `prefers-color-scheme`).
- Persist the choice to `localStorage` under a new key (e.g. `localjobs.mode`) alongside the existing
  theme/font attributes. Add a small pure helper (e.g. `resolveMode(stored, osPrefersDark)`) that maps
  the stored choice → effective `data-mode`, so it is unit-testable.
- **Default when no choice is stored = System** (follow the OS preference). This REPLACES the
  time-of-day behavior as the out-of-the-box default; retire `modeForHour`/`useTimeMode` time-of-day
  switching (remove or repurpose them — the 5-min re-check is no longer needed for time, but System
  mode should still react if the OS preference changes while the page is open, e.g. via a
  `matchMedia('(prefers-color-scheme: dark)')` change listener).
- Update the pre-paint inline script in `layout.tsx` to read the stored mode and apply `data-mode`
  before first paint: an explicit Dark/Light is applied directly; System (or no stored value) reads
  `window.matchMedia('(prefers-color-scheme: dark)').matches`. No light/dark flash on load.

Keep all the CLAUDE.md theme invariants: the untouched default family + its dark palette must look
exactly as today for a viewer whose effective mode resolves to dark; logs keep their fixed dark
palette in both modes. Update the CLAUDE.md theme section (`data-mode` is currently documented as
auto-by-time-of-day) and any README mention to describe the new Dark/Light/System picker defaulting to
System.

## Done when

- The 🎨 `ThemeControls` picker shows a Dark / Light / System control; selecting each sets
  `document.documentElement`'s `data-mode` correctly (Dark→dark, Light→light, System→matches OS), and
  the choice persists across reloads via `localStorage`.
- With no stored choice, the dashboard defaults to System: it renders light when the OS prefers light
  and dark when the OS prefers dark, applied before first paint (no flash). Changing the OS preference
  while the page is open flips the mode live when System is selected.
- `modeForHour`/time-of-day auto-switching no longer drives `data-mode`; the pure mode-resolution
  helper is covered by a small unit/inline test if the project's test setup can reach it (else assert
  it manually).
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green (no phone-width overflow from the added control).
- `CLAUDE.md`'s theme section (and README, if it mentions mode) are updated to describe the
  Dark/Light/System picker and the System default, replacing the time-of-day-auto description.
