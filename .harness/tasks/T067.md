## Do

On the Backlog view (/backlog), make EACH section collapsible — currently only the Done section can collapse. Apply the same collapse/expand toggle to the Harness-buildable and Needs-a-human sections (and any others), each with its count in the header, so the whole page can be tidied. Match the existing Done-section collapse pattern; each section toggles independently.

## Done when

On /backlog, every section (Harness-buildable, Needs a human, Done) has a working collapse/expand toggle with its count; sections toggle independently; dashboard build passes.
