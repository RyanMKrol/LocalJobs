## Do

Add a read-only SQLite table viewer page to the dashboard so the local DB can be browsed without building a feature per query. List tables and page through rows via a strictly read-only API path (no arbitrary mutation, no write SQL). Respect the existing read-only-client model.

## Done when

A dashboard page lists the DB tables and shows paged rows via a read-only API; there is no write/mutation path exposed; the dashboard build passes.
