## Do

Remove the redundant "(off)" hint from the Schedule column on the Workflows list. In
`dashboard/app/workflows/page.tsx` (~line 76) delete the `{p.enabled ? '' : ' (off)'}` expression
appended after the CronBadge/`manual` label. A workflow's enabled/disabled state is already shown by
the status pill / enable toggle, so the suffix is duplicate clutter.

## Done when

- The Schedule column no longer shows "(off)" for disabled workflows; enabled/disabled is still clear
  from the existing status/toggle.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
