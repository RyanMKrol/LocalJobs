## Do

Group the Services dashboard page (`dashboard/app/services/page.tsx`) into category sections
instead of one flat table, using the `category` field added to each service in T305
(`ServiceDefinition.category`, surfaced on `GET /api/services` as `category` per service — a
string: `'cli-tool'` | `'website-scrape'` | `'api'` | `'uncategorized'`). This mirrors T293, which
did the same grouping for the workflows list page.

1. Add `category: string` directly to the `Service` interface in `dashboard/app/lib/api.ts`
   (~line 221) — do this as a real typed field, not the local type-augmentation workaround
   (`WorkflowWithCategory`) T293 used for `Workflow`; there's no reason to avoid editing the
   interface directly here.
2. Define a small, ordered list of `{ key, label }` category groups in `page.tsx` (or a small
   colocated const): `cli-tool` → "CLI tools", `website-scrape` → "Website scrapes", `api` → "APIs",
   in that display order, followed by an `uncategorized` → "Uncategorized" group **rendered only
   when it has at least one service in it** (so nothing silently disappears if a future service's
   manifest omits `category`, but the group doesn't show up as permanent empty clutter today).
3. Partition the `services` array (from `api.services()`) by each row's `category` field into these
   groups, then render the EXISTING table markup (same columns: Service / Rate per min / Rate per
   day / Rate per month / edit-limits action) once per non-empty group, each under its own `<h2>`
   (or similar) heading showing the group's `label`, inside its own `.panel` (mirror how
   `dashboard/app/workflows/page.tsx` renders one `.panel` per category section after T293, and how
   other pages use multiple `.panel` blocks for separate sections). Keep the existing paid-then-name
   sort (`(a.paid?1:0)-(b.paid?1:0) || a.name.localeCompare(b.name)`) applied WITHIN each group,
   not globally across the whole list.
4. Preserve ALL existing per-row behavior (✎ Edit limits inline editing, the consumers popover
   opened by clicking the service name, the `paid`/`free`/`edited` pills) exactly as-is — this task
   only changes how rows are GROUPED and SECTIONED, not what a row contains or does.
5. Update `dashboard/scripts/_dashboard-harness.mjs`'s synthetic services-list fixture so its
   fixture services collectively cover at least 2 different categories (plus, ideally, one service
   with no `category` / `'uncategorized'`) — the services-list `PAGES` screenshot must show at
   least two distinct category section headings.

## Done when

- The Services page renders one heading + table per non-empty category, in the fixed order
  CLI tools → Website scrapes → APIs → Uncategorized (Uncategorized omitted entirely when empty).
- All existing per-row functionality (inline limit editing, consumers popover, pills) still works —
  no regression to row-level behavior, verified by keeping/adapting any existing test coverage for
  this page.
- After `npm --prefix dashboard run build`, run `node dashboard/scripts/visual-check.mjs` and look
  at the services-list screenshot in `dashboard/scripts/visual-out/` — confirm at least two distinct
  category section headings are visible with their respective services underneath.
- Run `node dashboard/scripts/mobile-check.mjs` (same build) and confirm no new horizontal overflow
  at the phone-width (~402px) viewport from the added section headings.
- `dashboard/scripts/_dashboard-harness.mjs`'s services-list fixture is updated to cover at least 2
  categories (per the Do section above).
- `npm test`, `npx tsc --noEmit`, and `npm --prefix dashboard run build` all pass.
