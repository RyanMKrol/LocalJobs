## Do

The owner reviewed the five toggleable Backlog section-header caret styles built by T113
(review task T127) and CHOSE the **spin** style — `.caret-spin` in
`dashboard/app/globals.css`: a rotating `›` chevron that rotates between collapsed and
expanded, with a subtle hover highlight on the whole summary row. Hardcode it as the SOLE
caret style, remove the live chooser + the four unused styles, AND fix two visual issues
the owner flagged with the spin caret. Do all of:

1. **Hardcode spin.** Make the `.caret-spin` rules apply unconditionally to the Backlog
   collapsible section headers — the `<summary className="section-heading-summary">`
   blocks for 'Harness-buildable', 'Needs a human', and 'Done' on `/backlog` — so the
   caret renders without any wrapper-class toggle. (Either drop the `.caret-spin` prefix so
   the rules target `.section-heading-summary` directly, or keep a single fixed wrapper —
   your call, but there must be no per-style branching left.)

2. **Remove the toggle + the four unused styles.**
   - `dashboard/app/backlog/page.tsx`: delete the caret-style selector bar (the
     `<div className="caret-style-bar">` … `{CARET_STYLES.map(...)}` block, ~lines 170-184)
     and the `caret-${caretStyle}` wrapper `<div>` (~line 192); remove the `useCaretStyle`
     import + `const [caretStyle, setCaretStyle] = useCaretStyle()` usage.
   - `dashboard/app/ui.tsx`: remove the now-unused `CARET_STYLES`, `CaretStyle`,
     `useCaretStyle`, `CARET_STYLE_KEY`, and `DEFAULT_CARET_STYLE`. First confirm nothing
     else imports them (only the backlog page should).
   - `dashboard/app/globals.css`: remove the four unused caret style rules (`.caret-bold`,
     `.caret-accent`, `.caret-circle`, `.caret-boxed`) and the selector-bar rule
     `.caret-style-bar`.

   ⚠️ **DO NOT remove `.caret-style-label` and `.caret-style-btn`** from `globals.css` —
   they are REUSED by the 'Show' reviewed-filter on the same Backlog page
   (`page.tsx` ~lines 215-220). Leave those two classes and the reviewed-filter markup
   fully intact.

3. **Fix vertical centering.** The spin caret currently sits slightly BELOW the vertical
   centre of the section title text, which looks jarring (the existing `vertical-align:
   middle` under-centres it). Adjust so the `›` glyph is optically centred against the
   title text's cap-height — e.g. tune `vertical-align`/`line-height`, or lay the summary
   out as an inline-flex row with `align-items: center`. Aim for it to look centred to the
   eye, not just mathematically.

4. **Fix horizontal padding.** Make the spacing to the LEFT of the caret and BETWEEN the
   caret and the title visually balanced and pleasing (currently the glyph has
   `margin-right: 4px` and the summary has `padding: 3px 6px; margin-left: -6px`). Keep the
   collapsed↔expanded rotation, the hover highlight, keyboard accessibility, and
   phone-width layout all working.

## Done when

On `/backlog` the three collapsible section headers always render the spin caret (rotating
`›` with the hover highlight) with NO style selector anywhere on the page; the four other
caret styles and the caret-style chooser/toggle are gone from `page.tsx`, `ui.tsx`, and
`globals.css`, while `.caret-style-label`/`.caret-style-btn` remain (still used by the
'Show' reviewed filter); the caret is optically vertically centred against the section
title text and its horizontal padding looks balanced; collapsed vs expanded stays clearly
distinct and keyboard-accessible with no phone-width overflow; `npx tsc --noEmit`,
`npm test`, `npm --prefix dashboard run build`, and `node dashboard/scripts/mobile-check.mjs`
are all green.
