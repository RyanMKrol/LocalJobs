## Do

Build a new scheduled **projects-sync** workflow that ingests the owner's GitHub repos into a NEW
DynamoDB **projects** table (the website currently live-fetches GitHub per request and does not
persist). Create `src/jobs/projects-sync/` as a **tracked** workflow (published like the
`places`/`perfumes` examples — no secrets in code; only `.env` creds and the job's `data/` stay
gitignored). On a cadence, fetch `https://api.github.com/users/<GITHUB_USERNAME>/repos`, **apply the
fork/archived filter + activity sort, and store the already-FILTERED list** (decided) to the projects
table via the shared `dynamodb` service (T252). The table is created by
`scripts/create-dynamo-tables.mjs` with a PERMANENT key — **partition key `repoId` (S)** — so each
item MUST carry a `repoId` attribute (the GitHub repo's numeric `id` as a string; stable across
renames). Idempotent upsert keyed by `repoId` (re-scan domain — repos change, so refresh fields each
run). Add a `github` rate-limit service in
`src/services/github.service.ts` (tracked; uses a token — GitHub's unauthenticated limit is low;
`GITHUB_USERNAME` + token from `.env`); conservative, friendly limits. Reference: website
`src/pages/api/github/repos.js`, `src/lib/apiCache.js`.

**Scope guard:** local-jobs ONLY — no website edits. The website keeps live-proxying GitHub as today.

## Done when

- `src/jobs/projects-sync/` exists as a tracked scheduled workflow that fetches the GitHub repos,
  filters out forks/archived + sorts by activity, and upserts the filtered list to the new projects
  table via the `dynamodb` service, idempotent (keyed by repo id/name, refreshing fields each run); a
  tracked `src/services/github.service.ts` token-authenticated rate-limit service; `GITHUB_USERNAME` +
  token documented as placeholders in `.env.example`; the job's `data/` gitignored.
- Verification is **hermetic**: tests mock the `dynamodb` service + the GitHub fetch and use the
  scratch DB — NO live AWS writes, no real-table mutation. `expectsTest`.
- `npx tsc --noEmit` and `npm test` are green; CI is green; docs (CLAUDE.md/README job list) updated.
