## Do

NEEDS-HUMAN parity audit. Do a FINAL check-in on parity between two copies of the Ralph build
harness:

- **The vendored copy in THIS repo** — `.harness/` (loop.sh, supervise.sh, HARNESS.md, CLAUDE.md,
  policy.jq, facets.json, postflight.sh, integrate.sh, designs/, etc.), which has evolved in-place
  while building `local-jobs`.
- **The source-of-truth copy in the owner's `claude-skills` plugin** — the Ralph Harness skills
  package the owner authors and DISTRIBUTES to other projects (the `ralph-harness` plugin; installed
  here as version `0.9.5`, the `ralph-loop-create-harness` / `ralph-loop-add-to-backlog` skills).

The owner has been iterating the harness here in `local-jobs` AND in the plugin, so the two have
likely drifted. The goal is to reconcile them so the distributable plugin captures every improvement
proven out here, and this repo isn't carrying private divergence that should be upstreamed (or
conversely, local-only customisations that should NOT leak into the distributed template).

Work to do WITH the owner:
- Diff the harness files in `.harness/` against the equivalent files in the `claude-skills` plugin
  source repo (NOT just the installed `0.9.5` cache — the owner's authoring repo is the real source
  of truth).
- For each divergence, DECIDE direction: upstream the local improvement into the plugin, pull a
  plugin fix down into this repo, or keep it intentionally local (and note why).
- Pay attention to anything that is `local-jobs`-specific (project name, DoD commands, scope paths,
  facet `layer` vocabulary) vs. genuinely generic harness logic that belongs in the distributable
  template.
- Confirm the two skills (`create-harness`, `add-to-backlog`) still match how the harness actually
  behaves here.

Record the audit findings and reconciliation decisions in this task's worklog
`.harness/worklog/T188.md`, and draft dependent follow-up task(s) for any concrete sync work that
results (e.g. "upstream X into the plugin", "pull plugin fix Y into .harness"). The loop SKIPS this
task; the owner returns to it deliberately.

## Done when

The owner has done a final parity check between this repo's `.harness/` and the `claude-skills`
plugin source of truth — every divergence reviewed and a direction decided (upstream / downstream /
intentionally-local) — with the findings and decisions recorded in `.harness/worklog/T188.md` and
any resulting sync work drafted as dependent buildable follow-up task(s). The audit MUST explicitly
cover the ideas → tasks paradigm (`/idea` + `/convert-ideas` commands, `.harness/IDEAS.md`, the
`.harness/CLAUDE.md` ideas section) and decide whether/how to upstream those custom Claude commands
into the `claude-skills` plugin. This task is audit + alignment only; it requires no code change
itself.
