## Do

Make the places job CODE public. Remove ONLY the 'src/jobs/places/' line from .gitignore (keep 'src/jobs/**/data/' and all secret rules). `git add` the places .ts files EXPLICITLY by path (NEVER `git add -A`); confirm via `git status` that nothing under src/jobs/places/data/ is staged. Verify a clean checkout still builds: the newly-public code must typecheck and the suite must stay green with no data/ present (use fixtures, no live API). Update README's note that 'jobs stay private' to reflect that places is now public.

## Done when

src/jobs/places/*.ts is tracked; src/jobs/places/data/** remains ignored (verify with `git check-ignore src/jobs/places/data/out`); typecheck + tests green; the pre-push guard would pass (no sensitive path staged).
