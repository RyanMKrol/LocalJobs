## Do

Surface a Cancel/Stop control for a running workflow, using the POST /api/workflow-runs/:id/cancel endpoint from T091. Add a cancelWorkflowRun(id) client method to dashboard/app/lib/api.ts (mirror the existing post<> helpers like runWorkflow/toggleWorkflow). Add a 'Cancel' button on the workflow-run detail page (workflow-runs/[id]/page.tsx) in the header row next to the status badge, shown/enabled ONLY while run.status === 'running' and disabled while the request is in flight (mirror the run()/busy pattern from workflows/[name]/page.tsx); on click call api.cancelWorkflowRun(id) with error handling, and let the 2s poll reflect the new 'cancelled' status. Optionally also expose it on the workflow detail page (workflows/[name]/page.tsx) when that workflow has an active run, linking to / acting on the active run. Update the README dashboard-pages section to mention the cancel control.

## Done when

On the workflow-run detail page a Cancel button appears while the run is running, calls the cancel endpoint, becomes disabled during the request, and the page reflects the 'cancelled' status after the next poll; the button is absent/disabled for terminal runs; README dashboard-pages updated; dashboard build passes and mobile-check stays green.
