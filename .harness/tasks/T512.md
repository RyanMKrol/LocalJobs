## Overview
One slice of the Plex→callService migration blocked twice (T474, T499) by a pre-push git-hygiene guard trip (sensitive/gitignored path staged, no code landed). The spec was always correct; the fix is to shrink the diff per unit so staging named files is trivial and a broad `git add` is never tempting. This unit is tv-recs's single Plex read across 3 files.

## Do
In src/workflows/tv-recs/stages/tv-snapshot.ts, wrap the plexGet<PlexAllResponse<PlexShowMeta>>(...) TV-section snapshot read in callService('plex', () => plexGet<...>(...)), importing callService from ../../../core/services.js. Mirror the callService('tmdb', ...) wrapping used in sibling stages of the tv-recs / missing-tv-seasons workflows. Do not change the call's arguments, return-value handling, or error handling beyond the wrap. Update tv-snapshot.test.ts to account for the callService('plex', ...) wrapper (in a test where 'plex' isn't registered, callService passes the call through — mirror the sibling tmdb test handling). Add a one-line note to tv-recs/CLAUDE.md that its Plex reads are now metered via the shared plex service. GIT HYGIENE: stage ONLY these 3 files by explicit name — NEVER `git add -A`/`git add .`/a directory; never stage anything under data/, chrome-profile/, .env*, or a scratch DB. This is why both prior attempts were blocked.

## Done when
tv-snapshot.ts's plexGet read is wrapped in callService('plex', ...) with no other behavior change; a grep for a bare plexGet( (not inside a callService('plex', ...) wrapper) in tv-snapshot.ts returns nothing; tv-snapshot.test.ts passes with the wrapper accounted for; tv-recs/CLAUDE.md notes the plex service; only the 3 scoped files are staged.
