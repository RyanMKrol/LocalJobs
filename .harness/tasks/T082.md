## Do

In the Validation gates panel on the workflow-run page (workflow-runs/[id]/page.tsx lines ~41-43) the gate-detail link reads 'detail →' but the arrow wraps onto a new line in the narrow last table cell. Keep the whole 'detail →' link on a single line (e.g. white-space:nowrap on the link or its cell). Don't change the link target or the rest of the table layout.

## Done when

On the workflow-run Validation gates panel the 'detail →' link renders entirely on one line (arrow no longer wraps) at desktop and phone widths; dashboard build passes and mobile-check stays green.
