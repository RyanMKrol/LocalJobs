## Do

Show the **full ID** in every dashboard back-navigation label (currently some show a truncated/short
id). Example: the job-run page `dashboard/app/runs/[id]/page.tsx` uses a `from` query param to render
a "← back" link whose label slices the id (e.g. `id.slice(0,8)`) — show the full id instead. AUDIT
every ID-bearing back-nav/breadcrumb across the dashboard and stop truncating, including: the
validation-gate detail pages (`dashboard/app/workflow-runs/[id]/gates/[producer]/[key]/page.tsx` back
to the run, and `dashboard/app/workflows/[name]/gates/[producer]/[key]/page.tsx`), the workflow-run
detail page (`dashboard/app/workflow-runs/[id]/page.tsx`), and the `from`-param back link on the run
page. Wherever a short-id helper is used in a back-nav label, switch to the full id.

## Done when

- Every back-navigation label that targets an ID-identified page shows the full id, not a shortened one
  (job-run page, gate detail pages, workflow-run detail, and any others found in the audit).
- Mobile-safe (long UUIDs wrap/truncate-with-ellipsis without breaking layout, but the full id is the value).
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
