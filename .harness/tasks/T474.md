## Overview
Re-attempt of T464. T464's own spec's 'Done when' explicitly required updating the 6 workflows' CLAUDE.md files plus the root CLAUDE.md's plex-client.ts file-map entry, but T464's TASKS.json `scope` array listed only the 7 code files + their 7 test files — no `.md` paths at all. The builder correctly recognized this contradiction (this repo's Definition-of-Done makes doc updates non-optional) and self-blocked rather than declare done with an incomplete deliverable, which is the right call. Verified via git log/grep that NO code from that attempt ever landed on main (the only committed change for T464 was the harness's own 'blocked, needs human — skipping' bookkeeping commit) — src/workflows/**/stages/*.ts still make bare plexGet/plexPutStreams/triggerButlerBackup calls today, with zero callService('plex', ...) wrapping anywhere in the real source tree. This unit is a full re-attempt (code + docs), not a docs-only patch.

## Do
Identical mechanical work to the original T464 spec — wrap every direct plexGet(...)/plexPutStreams(...)/triggerButlerBackup(...) call (imported from ../../core/plex-client.js or ../../../core/plex-client.js) in each of the 7 scoped stage/lib files in callService('plex', () => <original call>), importing callService from the matching relative depth (mirror the existing callService('tmdb', ...) wrapping already present in sibling stages of the same files, e.g. missing-tv-seasons/stages/season-check.ts). Do not change any call's arguments, return-value handling, or error handling beyond the wrap. plex-language-fix/stages/apply.ts additionally wraps plexPutStreams/triggerButlerBackup (mutating calls) — the wrap only adds metering, it must not change mutation semantics. Update each touched stage's unit test to account for the callService wrapper (follow whatever the same file's existing callService('tmdb', ...) tests already do as the template). Confirm via grep that src/core/plex-client.ts's own plexGet/plexPutStreams/triggerButlerBackup implementations are UNCHANGED — only call sites move. THEN, in the SAME task, update the 6 touched workflows' own CLAUDE.md (one line each, noting Plex connectivity for that workflow is now metered via the shared plex service) and add a one-line note to the root CLAUDE.md's src/core/plex-client.ts file-map entry (line ~296) mentioning the plex service now sits in front of plexGet/plexPutStreams/triggerButlerBackup calls — these two doc edits are now IN scope (the fix for the root cause: the original scope array omitted all .md paths even though the spec's Done when required them).

## Done when
Every one of the 7 code files in scope wraps its direct plexGet/plexPutStreams/triggerButlerBackup call(s) in callService('plex', ...), with no change to any other behavior; a grep for plexGet(/plexPutStreams(/triggerButlerBackup( outside of callService(...) wrappers and outside src/core/plex-client.ts itself returns nothing in the real src/ tree (excluding any src/workflows/*/data/ clone directories per this repo's known data/-scanning gotcha); npx tsc --noEmit and npm test are green including updated per-stage tests; the 6 touched workflows' own CLAUDE.md files AND the root CLAUDE.md's file-map entry for src/core/plex-client.ts are updated to note the new plex service — these doc edits are committed in the SAME commit as the code, not deferred. A live end-to-end confirmation (running a real Plex-touching workflow and seeing a plex row with real consumers on the dashboard's Integrations page) remains a manual owner follow-up post-merge, not required for this task, since the build sandbox has no LAN access to a real Plex server.
