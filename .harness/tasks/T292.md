## Do

Add a `category` field to workflows — manifest-owned, code-synced, no dashboard edit UI — so a
later task can group the workflows-list page by it. This is the backend/schema half; the
dashboard-rendering half is a separate, dependent task.

1. **`src/core/types.ts`** — add an optional `category?: string` field to `WorkflowDefinition`.
2. **`src/db/schema.sql`** — add a `category TEXT` column to the `CREATE TABLE IF NOT EXISTS
   workflows` block (~line 118), defaulting to `''` (empty string, NOT NULL DEFAULT ''), placed
   alongside the other manifest-seeded columns like `description`.
3. **`src/db/index.ts`** — add an additive migration for EXISTING databases (mirror the
   `notify_enabled` migration at ~lines 67-77: `PRAGMA table_info(workflows)` check +
   `ALTER TABLE workflows ADD COLUMN category TEXT NOT NULL DEFAULT ''`). Per the T098 rule, do
   **not** add any index on this column in `schema.sql`'s bootstrap path.
4. **`src/db/store.ts`**:
   - Add `category: string` to the `WorkflowRow` interface (~line 910).
   - `upsertWorkflowStmt` (~line 881): `category` is **always refreshed from the manifest** on every
     sync — like `description`, NOT like `schedule`/`max_concurrency`/`notify_enabled` (no
     `_overridden` column, no reconciliation branch, no dashboard edit endpoint). Add `category` to
     the `INSERT INTO workflows (...)` column list and its `VALUES (...)` placeholders, and set
     `category = excluded.category` unconditionally in the `ON CONFLICT ... DO UPDATE SET` clause
     (no `CASE WHEN ... overridden` branching, unlike the other three).
   - `syncWorkflow` (~line 894): pass `category: def.category ?? 'uncategorized'` into the
     `upsertWorkflowStmt.run({...})` call.
   - `listWorkflows()` already does `SELECT * FROM workflows` — no change needed there; `category`
     flows through automatically once it's a real column.
5. **Set `category` on every existing workflow manifest** (`*.workflow.ts` files, one line each):
   - `'second-brain'` — `src/jobs/places/places.workflow.ts`, `src/jobs/perfumes/perfumes.workflow.ts`
   - `'recommendations'` — `src/jobs/movies/movies.workflow.ts` (movie-recommendations),
     `src/jobs/tv-recs/tv-recs.workflow.ts` (tv-recommendations),
     `src/jobs/plex/plex.workflow.ts` (missing-tv-seasons)
   - `'regular-maintenance'` — `src/jobs/workouts-sync/workouts-sync.workflow.ts`,
     `src/jobs/listening-digest/listening-digest.workflow.ts`,
     `src/jobs/projects-sync/projects-sync.workflow.ts`,
     `src/jobs/claude-warmer/claude-warmer.workflow.ts`
   If a `src/jobs/stocks-sync/stocks-sync.workflow.ts` file already exists at build time, also give
   it `category: 'regular-maintenance'`; if it doesn't exist yet, skip it — an uncategorized
   workflow is expected to default to `'uncategorized'` via `syncWorkflow`'s fallback, so nothing
   is broken either way.
6. Document the `category` field's controlled values (`second-brain` / `recommendations` /
   `regular-maintenance`, defaulting to `uncategorized`) as a short new bullet under root
   `CLAUDE.md`'s workflow-authoring conventions section, next to the other manifest-property
   conventions (schedule/maxConcurrency/notifyEnabled) — make clear this one is code-owned only,
   with no dashboard edit endpoint.

## Done when

- `WorkflowDefinition` in `src/core/types.ts` has an optional `category?: string`.
- `src/db/schema.sql`'s `workflows` table has a `category TEXT NOT NULL DEFAULT ''` column, and
  `src/db/index.ts` has a matching additive `ALTER TABLE` migration guarded by a `PRAGMA
  table_info` check (mirroring the `notify_enabled` migration), with NO index added on it.
- A unit test seeds two `WorkflowDefinition`s (different `category` values, one with `category`
  omitted) through `syncWorkflow`, then asserts via `listWorkflows()`/`getWorkflow()` that: (a) each
  synced row's `category` matches its manifest value; (b) the omitted-category workflow's row is
  `'uncategorized'`; (c) re-syncing the SAME workflow with a DIFFERENT `category` value updates the
  stored value (proving there's no override/preservation behavior — category always tracks the
  manifest, unlike `schedule`/`max_concurrency`/`notify_enabled`).
- `GET /api/workflows` and `GET /api/workflows/:name` both include the workflow's `category` in
  their response (no server.ts code change should be needed beyond what `SELECT *`/spreading the row
  already provides — confirm this with a quick manual check or an existing API test, and only touch
  `src/api/server.ts` if something doesn't already flow through).
- Every existing `*.workflow.ts` manifest listed above has its `category` field set to the value
  given.
- Root `CLAUDE.md` documents the `category` field, its controlled values, and that it is
  manifest-owned only (no dashboard edit UI, no `_overridden` column).
- `npm test` and `npx tsc --noEmit` pass.
