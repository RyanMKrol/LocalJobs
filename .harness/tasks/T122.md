## Do

On the Overview page (dashboard/app/page.tsx), the workflow cards/list show a per-workflow 'stuck items' count pill. This is redundant now that stuck items have their own dedicated section/header (and the Stuck stat tile) on the same page. Remove that stuck-count pill from the Overview's workflow cards. KEEP the dedicated stuck-items section and the Stuck stat tile intact. This only affects the Overview page (/) — do NOT touch the /workflows page's stuck count (handled separately by T119).

## Done when

The Overview workflow cards no longer show a per-workflow stuck-items count pill; the dedicated stuck-items section and the Stuck tile remain; the /workflows page is unaffected; dashboard build passes and mobile-check stays green.
