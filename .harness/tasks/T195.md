## Do

In the per-run **Input → Output mapping** panel (`dashboard/app/workflow-runs/[id]/page.tsx` around
line 248), when there is no per-run mapping the panel renders a plain placeholder string — e.g.
*"Per-run input/output isn't recorded for runs created before this feature."* — which currently looks
unstyled/poor (bare text). Style this empty-state so it reads as a deliberate, tidy empty state
consistent with the rest of the dashboard: e.g. muted text, sensible padding/centering inside the
panel, matching the empty-state treatment used elsewhere (the gaps/seasons sections have tidy empty
states to mirror). Keep the wording; only improve the presentation. Use CSS variables so it works in
every theme/mode.

## Done when

- The Input→Output panel's empty-state message renders as a styled, muted, padded empty state (not
  bare unstyled text), consistent with other dashboard empty states and legible across themes/modes.
- The populated (non-empty) table rendering is unchanged.
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green.
