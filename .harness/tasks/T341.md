## Do

The owner would like (a soft "nice to have", not a hard requirement) the columns of the Services page's separate per-category tables to visually line up with each other. `dashboard/app/services/page.tsx` renders one independent `<table>` per category (lines 149-160, inside the `CATEGORY_GROUPS.map(...)`), each with the SAME 5 columns: Service, Rate / min, Rate / day, Rate / month, and a trailing actions column (see the `<thead><tr><th>Service</th><th>Rate / min</th>...` at line 157-159). Because each `<table>` sizes its own columns independently based on its own content, the numeric-column widths differ across tables — e.g. CLI tools/Website scrapes rows mostly show `<span className="muted">no limit</span>` (narrow) while APIs rows show a `Bar` component (`used / cap` text + a `.progress`/`.ubar` bar, wider, `globals.css:130`/`265`), so the same logical column ends up a different pixel width per table. Fix this WITHOUT a full rewrite: add an explicit, IDENTICAL `<colgroup>` (with fixed pixel or percentage widths for each of the 5 columns) to EVERY one of the 4 rendered tables on the page — since `CATEGORY_GROUPS.map` already returns one `<table>` per group, add the same `<colgroup><col style={{width: ...}} />...</colgroup>` markup (or a small shared local array of column widths mapped into `<col>` elements) right after the opening `<table>` tag, identically in each. Pick widths generously wide enough to fit the widest content across ALL categories (the APIS numeric bars need the most room) — inspect actual rendered content via a screenshot first. Because `table-layout` is not currently set to `fixed` (globals.css:91 is just `table-collapse`), explicit `<col>` widths act as minimums/hints under `table-layout: auto`; if visual testing shows the columns still drift, add `table-layout: fixed` scoped to just this page's tables (e.g. a `.services-table` class) rather than globally, since a global `table-layout: fixed` could visually break other pages' tables that rely on auto-sizing.

## Done when

`npx tsc --noEmit` and `npm test` pass. `npm --prefix dashboard run build` succeeds. Run `node dashboard/scripts/visual-check.mjs` and look at the `/services` screenshot in `dashboard/scripts/visual-out/` — confirm by eye that the Rate/min, Rate/day, Rate/month, and actions columns now start at the same horizontal x-position across the CLI tools, Website scrapes, APIs, and Uncategorized tables (when more than one category has rows to compare). Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green (fixed column widths must not cause new horizontal overflow at phone width — the `.panel:has(> table) { overflow-x: auto }` mobile rule at globals.css:577 should still let a too-wide table scroll within its own panel rather than spilling the page). This is a `layer:ui` task — visual confirmation via the screenshots is a hard requirement. Since this was a soft "nice to have" from the owner, a close-but-imperfect alignment (e.g. off by a few px due to font-rendering) is acceptable as long as it's a clear visible improvement over the current fully-independent column widths.
