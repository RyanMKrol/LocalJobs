## Do

On the workflow DEFINITION page (/workflows/[name]) the graph does not show validation gates at all — they currently only appear on a workflow RUN (with per-run state). Represent the gates STRUCTURALLY on the definition graph too: derive the workflow gates from the produces/consumes contracts on its member jobs (the same deriveGates the executor + run view use, already available via gatesForWorkflow) and mark them on the relevant producer-to-consumer edges, WITHOUT run state — just that a validation gate exists there, ideally clickable to its contract detail. Expose gates on the workflow-definition API endpoint if needed.

## Done when

The /workflows/[name] definition graph shows a marker on every edge that carries a validation gate (structural, no run state), consistent with how gates render on a run; dashboard build passes.
