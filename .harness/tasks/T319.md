## Do

Add an enforced output template for the `projects-sync` workflow's `project-summarize` stage, mirroring the `perfumes` workflow's template pattern (see `src/jobs/perfumes/profile.template.md` + `src/jobs/perfumes/config.ts`'s `templatePath` field + `src/jobs/perfumes/stages/build.ts`'s prompt-embeds-template-and-validates-output-shape approach — read all three as the worked reference before starting).

1. Create `src/jobs/projects-sync/project.template.md` — a markdown template (YAML frontmatter + fixed `##` section headings) that every generated project summary must follow. This is explicitly meant to become a queryable "second brain" corpus: the owner wants to later ask cross-project, high-level questions against the whole set of these files (e.g. "what sort of work am I interested in", "what kind of work have I done in domain X"). So beyond the owner's required baseline sections, design in extra structured content that supports that kind of querying. Include AT MINIMUM:
   - Frontmatter keys: `name` (repo name), `full_name` (`owner/repo`), `url`, `language`, `topics` (list), `status` (`active` | `dormant` — a judgement based on recency of `last_pushed`), `last_pushed` (date), `themes` (a list of freeform cross-project interest/domain tags, e.g. `["personal automation", "LLM tooling", "data pipeline"]` — this is what makes "what kind of work am I interested in" answerable across the corpus), `domain` (a one-line problem-domain description).
   - Body sections with these EXACT headings (they get validated post-generation, so keep them stable):
     - `## What It Is` — a short paragraph describing the project.
     - `## Tech Stack` — languages, frameworks, notable dependencies.
     - `## Status` — last commit date plus an active/dormant judgement with brief reasoning.
     - `## Structure` — notable directories / entry points / architecture at a glance.
     - `## Themes & Interests` — prose expansion of the `themes`/`domain` frontmatter: what broader interest, skill, or problem area this project reflects.
     - `## Notable Technical Approaches` — anything interesting about HOW it's built (patterns, techniques, integrations) — this is what lets a future "what work has Ryan done" query surface specifics, not just descriptions.
     - `## Sources` — the repo URL.
   - Do NOT include a "How to run it" section — the owner explicitly said it's not important here. Use perfumes' template purely as a STRUCTURAL reference (YAML frontmatter block delimited by `---`, then a markdown body of `##` sections) — design the actual fields/content fresh for this domain, don't copy perfume-specific fields.
2. Add a `templatePath` field to `projectsSyncConfig` in `src/jobs/projects-sync/config.ts`: `templatePath: process.env.PROJECTS_SYNC_TEMPLATE_PATH ?? resolve(here, 'project.template.md')` — mirror perfumes' `PERFUMES_TEMPLATE_PATH` override convention exactly. Document `PROJECTS_SYNC_TEMPLATE_PATH` in `.env.example` next to the existing `PROJECTS_SYNC_CLAUDE_MODEL` var.
3. Update `buildSummaryPrompt` in `src/jobs/projects-sync/stages/project-summarize.ts` to read the template file (falling back to an inline `FALLBACK_TEMPLATE` constant if the file is missing at runtime, mirroring perfumes' `build.ts`) and append it to the prompt with an instruction that the output must EXACTLY follow the template's frontmatter keys and section headings (mirror perfumes' `buildPrompt` instruction wording: "Produce a single Markdown file that EXACTLY follows the TEMPLATE at the end — same YAML frontmatter keys, same section headings, valid YAML, no extra keys."). IMPORTANT: keep the CURRENT content-gathering approach unchanged in this task (catalog metadata + embedded README text via `readReadme`) — do NOT change HOW content is gathered, only WHAT SHAPE the output must conform to. The content-gathering strategy is a separate, dependent follow-up task.
4. Add post-generation shape validation before writing the markdown file (mirror perfumes' `build.ts`: `if (!md.startsWith('---') || !md.includes('## Sources')) throw ...`, but check for EVERY required heading listed in step 1, not just one): the result must start with `---` and must contain every required section heading (`## What It Is`, `## Tech Stack`, `## Status`, `## Structure`, `## Themes & Interests`, `## Notable Technical Approaches`, `## Sources`). On a shape mismatch, reuse the EXISTING failure path in `runProjectSummarize` (the `catch` block already calls `markWorkItem(JOB_NAME, entry.repoId, 'failed', ...)` and logs) — just make the thrown error's message name which heading(s) or the frontmatter marker were missing, so the run log is diagnostic.
5. Update `src/jobs/projects-sync/stages/project-summarize.test.ts` to cover the new validation: one test where the injected `ClaudeSummarizer`/`summarize` stub returns a template-conformant response (accepted, work item marked success, markdown written) and one where it returns a response missing a required section (rejected, work item marked failed, no markdown written for that item).
6. Update root `CLAUDE.md`'s `projects-sync` description/file-map entry to mention the new enforced template (mirroring how the perfumes entry documents `profile.template.md`/`PERFUMES_TEMPLATE_PATH`), and update `README.md`'s `projects-sync` bullet if it currently describes the output as unstructured/ad-hoc.

## Done when

- `src/jobs/projects-sync/project.template.md` exists with the frontmatter keys and section headings described in the Do section (at minimum: `name`, `full_name`, `url`, `language`, `topics`, `status`, `last_pushed`, `themes`, `domain` in frontmatter; `## What It Is`, `## Tech Stack`, `## Status`, `## Structure`, `## Themes & Interests`, `## Notable Technical Approaches`, `## Sources` as body headings).
- `projectsSyncConfig.templatePath` exists in `src/jobs/projects-sync/config.ts`, defaults to `project.template.md`, and is overridable via `PROJECTS_SYNC_TEMPLATE_PATH` (documented in `.env.example`).
- `buildSummaryPrompt` embeds the template file's content and instructs Claude to follow it exactly; the content-gathering inputs (catalog metadata + embedded README text) are unchanged from before this task — no filesystem/tool-access change here.
- A generated markdown response is validated after the Claude call: missing the leading `---` or any required section heading causes the work item to be marked `failed` via the existing `markWorkItem`/`catch` path, with an error message naming what was missing — no new failure mechanism introduced.
- `project-summarize.test.ts` has passing tests for both the accept path (template-conformant stub response → success + markdown written) and the reject path (non-conformant stub response → failed, no markdown written).
- Root `CLAUDE.md` and `README.md` both describe the new enforced template and the `PROJECTS_SYNC_TEMPLATE_PATH` env var.
- `npm test` and `npx tsc --noEmit` pass.
