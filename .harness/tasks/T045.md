## Do

This is the human review/decision step for the joyful dashboard restyle. The toggleable
options are built by T142 (six themes — 2 dark + 4 bright — plus 5–10 display+body font
pairs, joyful accents baked into each theme, and a reduce-motion/minimal-emoji toggle, all
mix-and-match + persisted). Open the dashboard on BOTH desktop AND your phone (over Tailscale
— it's accessed remotely from a phone), switch between the themes and font pairings, toggle
reduce-motion, and CHOOSE: a preferred theme, a preferred font pairing, and your
reduce-motion/emoji preference. While reviewing, check (a) that the dense run tables,
streaming logs, gate DAG and stat grids stay legible under each theme/font, and (b)
phone-width usability — no overflow, readable small text, reachable controls. Record the
chosen theme + font + motion preference (e.g. in this task's worklog) so a follow-up
buildable task can hardcode them and remove the switchers + unused themes/fonts. The loop
skips this — it's a manual visual review.

## Done when

You've reviewed all six themes and the font pairings on BOTH desktop and your phone,
confirmed dense data stays legible, and recorded the chosen theme + font pairing + motion
preference; a follow-up can then hardcode the winners and drop the switchers + unused
options.
