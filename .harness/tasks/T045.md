## Do

This is the human review/decision step for the joyful dashboard restyle. The owner has narrowed the
appearance to a small curated set — **T184** trims the switcher to 3 theme families (`default`,
`pixel-picnic`, `sunny-8bit`, each light+dark with auto time-of-day flipping) and 3 fonts (system
default, Baloo 2, Space Mono). This task reviews that trimmed set once it's built. Open the dashboard
on BOTH desktop AND your phone (over Tailscale — it's accessed remotely from a phone), switch between
the 3 themes and 3 fonts, see the auto light/dark behaviour, toggle reduce-motion, and confirm the
final selection (or whether to hardcode a single default and drop the switcher entirely). While
reviewing, check (a) dense run tables / streaming logs / gate DAG / stat grids stay legible under
each theme + light/dark mode, and (b) phone-width usability — no overflow, readable small text,
reachable controls. Record the outcome in this task's worklog. The loop skips this — manual visual
review.

## Done when

You've reviewed the trimmed theme/font set (T184) on BOTH desktop and your phone, confirmed dense
data stays legible in both light and dark, and recorded the outcome — either keep the trimmed
switcher as-is, or pick a single default to hardcode (→ a follow-up drops the switcher) — in the
worklog.
