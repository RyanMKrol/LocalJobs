## Do

Add a **filter by item state** to the workflow-run Input→Output mapping so you can show only
**success / failed / skipped** rows. Builds on the data-model fix (T225) that introduces the `skipped`
state and stops false `success`. The I/O panel is built from `workItemIoRows(first, last, runId)` in
`src/db/store.ts` (T139), surfaced by `GET /api/workflow-runs/:id`, and rendered in
`dashboard/app/workflow-runs/[id]/page.tsx`. Surface each row's state and add a UI control (toggle/
chips) to filter the mapping by the permissible work-item states.

## Done when

- The Input→Output panel shows each row's state and offers a filter (at least success / failed /
  skipped) that narrows the displayed rows.
- The panel is clearer about which inputs produced output vs were skipped/failed.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, `npm test`, and
  `node dashboard/scripts/mobile-check.mjs` green.
