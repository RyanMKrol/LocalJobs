## Do

The owner asked for the Claude model used by the `project-summarize` stage of the `projects-sync` workflow (`src/jobs/projects-sync/stages/project-summarize.ts`) to be explicitly pinned in `.env`, AND configured with 'medium thinking' — i.e. the Claude CLI's reasoning-effort level should be set to `medium` for these calls, not left unset.

Background you need: `claude --help` (run it yourself to confirm) shows the CLI supports a real, separate flag `--effort <level>` with values `low|medium|high|xhigh|max` — this is distinct from `--model`. The shared, daemon-wide Claude CLI helper `src/services/claude.ts` (used by `projects-sync`, `movies`, `tv-recs`, `workouts-sync`, `claude-warmer` — perfumes has its own separate, untouched `src/jobs/perfumes/claude.ts`) currently exports:
```ts
export function runClaude(prompt: string, model: string): Promise<ClaudeResult> {
  return callService('claude-cli', () => spawnClaude(prompt, model));
}
function spawnClaude(prompt: string, model: string): Promise<ClaudeResult> {
  ...
  const args = ['-p', '--output-format', 'json', '--model', model, '--dangerously-skip-permissions'];
  ...
}
```
It has NO way to pass an effort level today. Add this capability, scoped tightly to exactly what's needed here — do not add unrelated flags or generic options plumbing.

1. In `src/services/claude.ts`:
   - Add an OPTIONAL third parameter to both `runClaude` and the internal `spawnClaude`: `effort?: string`.
   - Extract the CLI args array construction into a small exported pure function, e.g. `export function buildClaudeArgs(model: string, effort?: string): string[]`, that returns `['-p', '--output-format', 'json', '--model', model, ...(effort ? ['--effort', effort] : []), '--dangerously-skip-permissions']` (exact ordering doesn't matter to the CLI, but keep `--dangerously-skip-permissions` last as it is today). Have `spawnClaude` call this function instead of inlining the array, so the arg-building logic is unit-testable without spawning a real process.
   - `runClaude(prompt, model, effort?)` threads `effort` through to `spawnClaude`.
   - Every EXISTING caller (movies `src/jobs/movies/stages/recommend.ts` + `merge.ts`, tv-recs `src/jobs/tv-recs/stages/tv-rec-merge.ts` + `recommend.ts`, `src/jobs/workouts-sync/stages/workouts-progress.ts`, `src/jobs/claude-warmer/claude-warm.job.ts`) calls `runClaude(prompt, model)` with 2 args and must keep working unchanged — when `effort` is omitted, `buildClaudeArgs` must NOT include `--effort` at all (preserves current CLI default behavior for every one of those callers). Do not modify any of those other call sites.
   - Add a new test file `src/services/claude.test.ts` (there is currently no test for this service) that imports `buildClaudeArgs` and asserts: (a) `buildClaudeArgs('claude-sonnet-5')` does NOT contain `'--effort'`; (b) `buildClaudeArgs('claude-sonnet-5', 'medium')` contains `['--effort', 'medium']` in the args array. Keep it hermetic — no spawning, no network, just asserting the pure function's return value. Follow this repo's existing `node:test` + `node:assert` style (see any other `*.test.ts` in `src/` for the pattern, e.g. `src/jobs/projects-sync/stages/project-summarize.test.ts`).

2. In `src/jobs/projects-sync/stages/project-summarize.ts`:
   - Immediately after the existing `export const claudeModel = process.env.PROJECTS_SYNC_CLAUDE_MODEL ?? 'claude-sonnet-5';` (line 15), add: `export const claudeEffort = process.env.PROJECTS_SYNC_CLAUDE_EFFORT ?? 'medium';`
   - Update the local `ClaudeSummarizer` type (currently `export type ClaudeSummarizer = (prompt: string, model: string) => Promise<{ ok: boolean; text: string; error?: string }>;`) to add an optional third parameter: `(prompt: string, model: string, effort?: string) => Promise<{ ok: boolean; text: string; error?: string }>`.
   - Update the call site `const result = await summarize(prompt, claudeModel);` to `const result = await summarize(prompt, claudeModel, claudeEffort);`.
   - Check `src/jobs/projects-sync/stages/project-summarize.test.ts` — its stub `summarize` functions (e.g. `async (prompt) => { ... }`) remain valid without changes since the new third parameter is optional and the stub doesn't need to read it, but confirm `npm test` stays green; if the stub's declared function signature causes a TS mismatch against the updated `ClaudeSummarizer` type, adjust the stub signature to accept (and ignore) the optional third arg.

3. Config docs — `.env` (this file is gitignored; editing it is a LOCAL-ONLY action, never `git add .env`, never stage it): add two lines (near the end, or grouped with other projects-sync-related lines if any exist):
```
PROJECTS_SYNC_CLAUDE_MODEL=claude-sonnet-5
PROJECTS_SYNC_CLAUDE_EFFORT=medium
```

4. `.env.example`: the existing commented line `# PROJECTS_SYNC_CLAUDE_MODEL=claude-sonnet-5` (around line 116, under the projects-sync comment block) already documents the correct default — leave it as-is (still commented, matching this file's convention of documenting-but-not-setting every model env var, e.g. `MOVIES_RECS_MODEL`). Add a new commented line directly after it: `# PROJECTS_SYNC_CLAUDE_EFFORT=medium` with a short comment above explaining it sets the Claude CLI's `--effort` (reasoning-effort) level for this stage's calls, valid values `low|medium|high|xhigh|max`.

5. Root `CLAUDE.md`: the long `projects-sync` workflow description paragraph (search for `and **projects-sync**`) already mentions `PROJECTS_SYNC_CLAUDE_MODEL`. Extend that same sentence/clause to also mention `PROJECTS_SYNC_CLAUDE_EFFORT` (e.g. '... Model: `PROJECTS_SYNC_CLAUDE_MODEL` (defaults to a Sonnet 5 id) at effort `PROJECTS_SYNC_CLAUDE_EFFORT` (defaults to `medium`), shares `LOCALJOBS_CLAUDE_BIN`/`LOCALJOBS_CLAUDE_TIMEOUT_MS` via the claude-cli service.'), matching this repo's 'docs are part of every change' rule (see the top of CLAUDE.md).

Do not touch perfumes' own separate Claude helper (`src/jobs/perfumes/claude.ts`) or any other workflow's model config — this task is scoped to the shared `runClaude` capability plus projects-sync's own use of it.

## Done when

- `npx tsc --noEmit` passes with no errors.
- `npm test` passes, including the new `src/services/claude.test.ts` (asserting `buildClaudeArgs` omits `--effort` when not passed and includes `['--effort', 'medium']` when passed) and the existing `src/jobs/projects-sync/stages/project-summarize.test.ts`.
- `grep -n "effort" src/services/claude.ts` shows the new optional `effort` parameter on both `runClaude` and `spawnClaude`/`buildClaudeArgs`, and shows it is only appended to the CLI args when provided.
- `grep -n "claudeEffort" src/jobs/projects-sync/stages/project-summarize.ts` shows the new `claudeEffort` export (defaulting to `'medium'`) and its use in the `summarize(prompt, claudeModel, claudeEffort)` call.
- `grep -n "PROJECTS_SYNC_CLAUDE" .env` shows both `PROJECTS_SYNC_CLAUDE_MODEL=claude-sonnet-5` and `PROJECTS_SYNC_CLAUDE_EFFORT=medium` set.
- `git check-ignore .env` exits 0 (still gitignored) and `git status --porcelain` does NOT list `.env` as staged or modified-and-staged.
- `grep -n "PROJECTS_SYNC_CLAUDE" .env.example` shows both vars documented (commented out, with the `--effort` var's valid-values comment).
- `grep -n "PROJECTS_SYNC_CLAUDE_EFFORT" CLAUDE.md` finds a mention in the projects-sync workflow description.
- Every other existing caller of `runClaude` (movies, tv-recs, workouts-sync, claude-warmer) is untouched and still calls it with 2 arguments; `npm test` covers their existing suites and they remain green, confirming no regression from the new optional parameter.
