## Do

On the Workflows list (/workflows) the 'Last run' column shows a status pill ('running' / 'succeeded' / etc.) that is not clickable (workflows/page.tsx lines ~38-39). Make that pill a link to the workflow run it refers to — p.last_run is a WorkflowRun and already carries its id (api.ts WorkflowRun.id), so wrap the badge in an anchor to `/workflow-runs/${p.last_run.id}`. Keep the relative-time text beside it, the no-run ('—') case, and the existing badge styling.

## Done when

On /workflows each Last-run status pill is a clickable link that navigates to that workflow run's detail page (/workflow-runs/<id>); the relative time and the empty/no-run state are unchanged; dashboard build passes and mobile-check stays green.
