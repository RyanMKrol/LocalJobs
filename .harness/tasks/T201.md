## Do

Allow a workflow to run with **no concurrency cap** (unlimited), via an explicit **Unlimited** toggle
on the workflow detail page's "Max concurrency" editor (`dashboard/app/workflows/[name]/page.tsx`,
around the `saveConc`/`concDraft` editor at lines 327–450). Today `max_concurrency` must be a positive
integer ≥1 (the API validates `≥1`), so there's no way to express "unbounded".

End-to-end:
- **UI.** Add an explicit "Unlimited" checkbox/toggle beside the number input. When on, disable the
  number input and persist the unlimited state; when off, the number input applies as today. Show the
  effective value sensibly (e.g. "Unlimited" instead of a number) in the read view (where it currently
  shows `effective_max_concurrency`).
- **Storage + API.** Decide a single clear representation for "unlimited" and thread it through:
  e.g. store `max_concurrency = 0` to mean unlimited (update `updateWorkflowConcurrency` in
  `src/db/store.ts` and the `POST /api/workflows/:name/concurrency` validation in `src/api/server.ts`
  to ACCEPT the unlimited sentinel — relax the strict `≥1` rule to allow exactly that sentinel, still
  rejecting negatives/garbage). Keep the existing `max_concurrency_overridden` reconcile behaviour.
- **Effective value.** `effectiveWorkflowConcurrency(def)` (`src/core/workflow-executor.ts:46`) must
  map the unlimited sentinel to an unbounded cap (e.g. `Infinity` / a very large number) so
  `executeDag` launches all ready stages with no throttle. Verify `executeDag`'s concurrency handling
  tolerates an unbounded cap.

Update CLAUDE.md's `maxConcurrency` notes (the user-editable + code-reconciled section) to document
the unlimited option and its sentinel.

## Done when

- The workflow page's Max concurrency editor has an explicit Unlimited toggle; enabling it persists an
  unlimited state and the run uses NO concurrency cap (all ready stages launch together); disabling it
  restores a numeric cap. The read view shows "Unlimited" when set.
- `POST /api/workflows/:name/concurrency` accepts the unlimited sentinel and still rejects invalid
  input (400); `effectiveWorkflowConcurrency` returns an unbounded cap for it; the
  `max_concurrency_overridden` reconcile still works.
- A unit test covers the sentinel round-trip: `effectiveWorkflowConcurrency` returns unbounded for an
  unlimited workflow, and the store/endpoint accept+persist it (scratch DB). `npm test`,
  `npx tsc --noEmit`, `npm --prefix dashboard run build`, and `node dashboard/scripts/mobile-check.mjs`
  are green; CLAUDE.md updated.
