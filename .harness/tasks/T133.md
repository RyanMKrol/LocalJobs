## Do

The owner reviewed the five toggleable workflow-run output line-item styles built by
T116 (review task T128) and CHOSE the **meta** style — the `out-meta` rendering in
`dashboard/app/workflow-runs/[id]/page.tsx`'s `OutputCell`: the output filename as a
link plus small metadata (file size · "click to preview"), clicking opens the full
markdown popover. Hardcode it as the SOLE output line-item style and remove the live
chooser + the four unused styles. Do all of:

1. **Hardcode meta.** In `OutputCell` (page.tsx ~lines 136-242), make the `meta` branch
   (~lines 215-227) the only rendered output presentation. KEEP the shared pieces it
   relies on: the no-artifact / loading fallback (~lines 158-174), `out.bytes` +
   `fmtBytes`, and the click-to-preview popover (`open`/`onOpen`, the T110 popover +
   T115 markdown render). Remove the other four style branches (`filename` ~184-191,
   `title` ~194-201, `excerpt` ~204-212, `chip` ~230-241).

2. **Remove the toggle.** Delete the output-style selector bar from page.tsx (the
   `<div className="output-style-bar">` … `{OUTPUT_STYLES.map(...)}` block ~lines
   269-281), the `const [outputStyle, setOutputStyle] = useOutputStyle()` state (~line
   257), the `OUTPUT_STYLES`/`useOutputStyle` imports + the `OutputStyle` type import,
   and the `outputStyle` prop on `OutputCell` (drop it from the component's props and
   from the `<OutputCell … outputStyle={outputStyle} />` call site ~line 305).

3. **Remove the unused exports.** In `dashboard/app/ui.tsx` remove `OUTPUT_STYLES`,
   `OutputStyle`, `useOutputStyle`, `OUTPUT_STYLE_KEY`, and `DEFAULT_OUTPUT_STYLE`
   (first confirm only the workflow-run page imported them).

4. **Remove the unused CSS.** In `dashboard/app/globals.css` remove the output-style
   selector-bar rules (`.output-style-bar`, `.output-style-label`, `.output-style-btn`
   + `:hover`/`.active`) and the CSS for the dropped styles — `.out-filename*`,
   `.out-title*`, `.md-preview*`, `.out-chip*`. KEEP `.out-meta*`. ⚠️ Before deleting
   `.md-preview*`, confirm those classes aren't used anywhere else (e.g. the markdown
   popover) — only remove rules that are genuinely now-unused; grep the dashboard to be
   sure.

## Done when

On a workflow-run page the input→output mapping always renders the `meta` output style
(filename link + file size · click-to-preview) with NO style selector anywhere on the
page; the four other output styles and the chooser/toggle are gone from page.tsx,
ui.tsx, and globals.css (with `.out-meta*` kept and no other-style CSS left dangling);
the click-to-preview popover still works (perfumes + places artifacts), the no-output
fallback still renders gracefully, and the panel stays mobile-responsive;
`npx tsc --noEmit`, `npm test`, `npm --prefix dashboard run build`, and
`node dashboard/scripts/mobile-check.mjs` are all green.
