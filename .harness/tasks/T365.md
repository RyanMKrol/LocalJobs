## SUPERSEDED (2026-07-03) — do not action as written

The Deploy Hook approach this task describes is no longer the plan. `ryankrol.co.uk` deliberately
disconnected its Vercel Git integration on 2026-07-03 (its own harness was pushing commits too
fast, and even cancelled deploys counted against Hobby's quota) — with Git integration off, a
Deploy Hook may not even fire, since Deploy Hooks build from the connected Git repo/branch.
`vercel-daily-redeploy`/`vercel-redeploy.job.ts` was rewritten the same day to run
`vercel --prod --yes` directly via the CLI instead (deploys the current working tree, independent
of Git integration state) — see the workflow's own file comment and root `CLAUDE.md`'s
`vercel-daily-redeploy` entry for the full story. **No credential provisioning is needed at all**:
the Vercel CLI already has a persistent login session on this machine (`vercel whoami` confirmed
working) and `ryankrol.co.uk`'s `.vercel/project.json` is already linked — `RYANKROL_CO_UK_PATH`
(already set in this repo's local `.env`) is the only config, and it needs no secret.
This task is left here, gated, for the owner to mark done (or delete) via the dashboard rather than
hand-edited — see the outdated "Do"/"Done when" below for historical context only.

## Overview

Provision the actual Vercel Deploy Hook for `ryankrol.co.uk` and wire its URL into this repo's local
`.env`, so the `vercel-daily-redeploy` workflow (built by its companion task) can actually trigger a
live daily safety-net redeploy instead of soft-skipping forever.

## Do

This is a **needs-human** task — it requires the owner's own Vercel dashboard access, which neither
the autonomous loop nor any agent working in this repo can provision. The companion buildable task
(`vercel-daily-redeploy-1`, same idea conversion) scaffolds a `vercel-daily-redeploy` workflow that
reads `VERCEL_DEPLOY_HOOK_URL` from `.env` and cleanly soft-skips (logs a WARN, no-ops) whenever that
var is unset — so the code can be built and merged before this step is done; this task is what turns
the safety net from "wired but dormant" into "actually live." Mirrors the T108/T109 pattern in this
repo (`.harness/tasks/T108.md`/`T109.md`): scaffold the workflow now, gate the real credential
provisioning as its own needs-human task, record `failed:blocked` until it's supplied.

**What the owner needs to do (outside this repo, in the Vercel dashboard):**

1. **Confirm the `ryankrol.co.uk` Vercel project's account tier** (Hobby vs Pro) — this materially
   changes how urgent/relevant the safety net even is: Hobby's deployment ceiling is far lower
   (100/day) than Pro's (6000/day). This was NOT checked during the idea's research (it requires
   dashboard access) — confirm it now, and note it in the workflow's `README.md`/`CLAUDE.md`
   description if it changes the framing (e.g. if the project is actually on Pro, the "safety net"
   rationale is much less pressing and worth a one-line caveat in the docs).
2. **Check whether `ryankrol.co.uk`'s `vercel.json` (if present) has `github.enabled` set to `false`**
   — per Vercel's Deploy Hooks docs, a Deploy Hook will NOT fire if git-based deploys are disabled for
   that project. (This is a quick repo check the owner or an agent with access to the
   `~/Development/ryankrol.co.uk` checkout can do independently of the Vercel dashboard step — look
   for a `vercel.json` at that repo's root and check for a `github.enabled` key.)
3. In the Vercel dashboard: `ryankrol.co.uk` project → **Settings → Git → Deploy Hooks** → create a
   new hook. Name it something identifiable (e.g. `local-jobs-daily-safety-net`) and bind it to the
   `main` branch (the branch that auto-deploys to production per that repo's own `CLAUDE.md:248`
   confirmation that "Vercel auto-deploys `main` on push").
   - Note: a project may have at most 5 Deploy Hooks — if 5 already exist, delete/reuse one rather
     than trying to exceed the cap (Vercel will reject creation past the limit).
4. Copy the generated hook URL (format: `https://api.vercel.com/v1/integrations/deploy/prj_.../<hook-id>`)
   and add it to this repo's **local, gitignored** `.env` file as:
   ```
   VERCEL_DEPLOY_HOOK_URL=https://api.vercel.com/v1/integrations/deploy/prj_.../<hook-id>
   ```
   Do **not** commit this value anywhere — `.env` is gitignored, and `.env.example` (already updated by
   the companion buildable task) only ever holds the placeholder/name, never the real URL.
5. Restart the `local-jobs` daemon so it picks up the new env var (per root `CLAUDE.md`'s "Always
   restart what you changed" rule):
   `launchctl kickstart -k gui/$(id -u)/com.ryankrol.localjobs`
6. Verify end-to-end with the smallest possible sample: manually run the `vercel-daily-redeploy`
   workflow once from the dashboard (Workflows → vercel-daily-redeploy → ▶ Run now) and confirm in the
   Vercel dashboard's Deployments tab that a new deployment was triggered by the hook (it'll show the
   Deploy Hook as the trigger source, distinct from a git-push-triggered deploy).

## Done when

- The owner has confirmed the `ryankrol.co.uk` Vercel project's account tier (Hobby/Pro) and recorded
  it in this task's worklog.
- The owner (or an agent with access) has confirmed `ryankrol.co.uk`'s `vercel.json` does not disable
  `github.enabled` (or has fixed it if it does).
- A Vercel Deploy Hook exists for `ryankrol.co.uk`, bound to `main`.
- `VERCEL_DEPLOY_HOOK_URL` is set in `local-jobs`'s local `.env` (never committed) to the real hook
  URL, and the daemon has been restarted.
- A manual run of the `vercel-daily-redeploy` workflow successfully triggers a real Vercel deployment,
  confirmed by checking the Vercel dashboard's Deployments tab for a Deploy-Hook-triggered deployment
  matching the run's timing.
- Until all of the above is complete, this task is recorded `failed:blocked` with the exact remaining
  provisioning step(s) named — it is NOT something the loop can complete unattended.
