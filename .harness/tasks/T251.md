## Do

**(Owner manual cloud action — needs-human.)** Provision the AWS groundwork the local-jobs DynamoDB
ingestion pipelines (workouts / listens / projects) depend on:

- Create a **least-privilege IAM key** for local-jobs: read+write on the workouts/exercises tables,
  the new listens table, and the new projects table (add read on others later as needed). NOT broad
  admin.
- Create/confirm the **table names** (reuse the website's existing `WORKOUTS_TABLE`/`EXERCISES_TABLE`
  per `src/lib/constants.js` `DYNAMO_TABLES`; create new tables for listens and projects).
- Put the **key id + secret + region + table names** into local-jobs `.env` (read via `process.env`;
  never committed — matches the repo secret rule) and document the var NAMES (placeholders only) in
  `.env.example`.

## Done when

- A least-privilege IAM key exists and its id/secret/region + all required table names are present in
  local-jobs `.env` (verified by the owner), and the var names are documented as placeholders in
  `.env.example`.
- This is a human action (credentials + cloud provisioning) — there is no code/CI to run; mark it
  done once the owner confirms the `.env` is populated and the tables exist.
