## Overview
Third attempt at the plex-language-fix portion of the Plex→callService migration that T464 (blocked: scope omitted .md files), T474 (blocked: pre-push guard trip), and T499 (blocked: IDENTICAL pre-push guard trip, rung 0 attempt 0 — sensitive path staged, no code landed) all failed to land. The spec and target service (src/services/plex.service.ts) were always correct; the ONLY thing that went wrong twice was the builder staging a sensitive/gitignored path (a broad `git add -A`/`git add .` or a src/workflows/**/data/ file) so the loop's pre-push privacy guard HALTED before any commit. A prose git-hygiene warning in T499's spec demonstrably did NOT prevent it. The real lesson: shrink each unit's diff so small that staging named files is trivial and a broad add is never tempting — this unit is one workflow's 5 files (2 code, 2 test, 1 doc), not 21.

## Do
Wrap every direct Plex call in plex-language-fix in callService('plex', () => <original call>), touching ONLY the 5 scoped files. (1) plex-language-fix/lib.ts: wrap each of the 4 plexGet<...>(...) reads (the /library/sections listing, the section /all listing, the /library/metadata/<ratingKey> read, and the per-episode metadata read) in callService('plex', () => plexGet<...>(...)), importing callService from ../../core/services.js — mirror the existing callService('tmdb', ...) wrapping already present in the SAME file (lib.ts already wraps its TMDB lookup this way). (2) plex-language-fix/stages/apply.ts: the mutating calls go through injected function defaults (`const putStreams = opts.putStreams ?? plexPutStreams; const triggerBackup = opts.triggerBackup ?? triggerButlerBackup`). Wrap at the CALL SITE where putStreams(...) and triggerBackup(...) are invoked (callService('plex', () => putStreams(...)) and callService('plex', () => triggerBackup(...)) — importing callService from ../../../core/services.js) so a test-injected override still runs (callService's no-limit branch just passes through). Do NOT wrap the `?? plexPutStreams`/`?? triggerButlerBackup` default assignments, and do not change mutation semantics, arguments, return-value handling, or error handling beyond the wrap. Update lib.test.ts and apply.test.ts to account for the callService('plex', ...) wrapper — in a test where 'plex' is not a registered service, callService's no-limit branch passes the call straight through, so mirror how the file's existing callService('tmdb', ...) tests already handle it. Update plex-language-fix/CLAUDE.md with a one-line note that this workflow's Plex connectivity is now metered via the shared plex service. GIT HYGIENE (the actual root cause of both prior blocks): stage ONLY these 5 files by explicit name (`git add src/workflows/plex-language-fix/lib.ts src/workflows/plex-language-fix/stages/apply.ts ...`) — NEVER `git add -A`, `git add .`, or a directory; never stage anything under a data/ folder, chrome-profile/, .env*, or a scratch DB. Because this unit's diff is only 5 named files, an explicit add is trivial — there is no reason to reach for a broad add.

## Done when
In plex-language-fix, a grep for plexGet(, putStreams(, or triggerBackup( (allowing a `<` type-parameter) that is NOT immediately inside a callService('plex', ...) wrapper returns nothing across lib.ts and apply.ts (excluding the `?? plexPutStreams`/`?? triggerButlerBackup` default-assignment lines and src/core/plex-client.ts, which is out of scope and must stay byte-unchanged). apply.ts's mutation semantics are unchanged (a test-injected putStreams/triggerBackup override still runs). lib.test.ts and apply.test.ts pass with the wrapper accounted for. plex-language-fix/CLAUDE.md notes the plex service. Only the 5 scoped files are staged — nothing under data/, no scratch DB, no .env.
