## Do

Fix the **service-detail popup** on the Services page so its font follows the 🎨 theme/font picker
instead of a default/hardcoded face. Clicking a service opens a popover of its consumers (T186,
`GET /api/services/:name/consumers`) on `dashboard/app/services/page.tsx` (or a component it uses).
The theme system sets `data-font` on `document.documentElement` and faces flow from `--font-*` CSS
vars (`dashboard/app/globals.css`). Likely the popup hardcodes a `font-family` or is portal-rendered
outside the `html[data-font]` cascade — make it use the same `--font-*` vars / `.db-modal` chrome the
other popovers (e.g. `StuckPopover`, markdown modals) use so it inherits the themed font.

## Done when

- The service-detail popup renders in the picker-selected font across all 3 theme families × light/dark,
  matching the rest of the dashboard.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
