## Do

The owner's screenshot shows the Services page's per-category panels (CLI TOOLS / WEBSITE SCRAPES / APIS / UNCATEGORIZED, `dashboard/app/services/page.tsx` lines 149-153, `CATEGORY_GROUPS.map(...)` each rendering a sibling `<div className="panel">`) stacked with their borders touching — no visible gap between them. `dashboard/app/globals.css`'s `.panel` rule (line ~87) defines no margin. Contrast this with `dashboard/app/workflows/page.tsx` (lines 64-72), which ALREADY solves the identical problem for its own category panels via an inline style: `<div className="panel" key={key} style={index > 0 ? { marginTop: 28 } : undefined}>` — i.e. every panel after the first gets `marginTop: 28`. Fix this by adding ONE shared CSS rule to `globals.css` that creates spacing between adjacent stacked panels — e.g. `.panel + .panel { margin-top: 28px; }` (adjacent-sibling selector, matching the 28px value the Workflows page already uses, so the visual gap size stays the same on Workflows and gets added fresh on Services). Then REMOVE the now-redundant inline `style={index > 0 ? { marginTop: 28 } : undefined}` from `dashboard/app/workflows/page.tsx` line 72 (just `<div className="panel" key={key}>`) so both pages share the ONE CSS-driven mechanism instead of two different ones (this directly addresses the owner's broader complaint that the two pages solve the same problem inconsistently). IMPORTANT — before landing this, grep the dashboard for other places using `className="panel"` (there are ~31 occurrences across `dashboard/app`) and check, via `node dashboard/scripts/visual-check.mjs` screenshots of EVERY page in `dashboard/scripts/visual-out/` (not just Services/Workflows), that the new `.panel + .panel` rule doesn't introduce an unwanted DOUBLE gap anywhere a page already has its own spacing between adjacent `.panel` siblings (e.g. via a grid `gap`, a wrapping flex container, or another inline margin) — `.panel + .panel` only fires when two `.panel` elements are literally adjacent DOM siblings with nothing else between them, so most existing per-page spacing (grids/flex with a wrapping non-panel container) is unaffected, but confirm this empirically rather than assuming.

## Done when

`npx tsc --noEmit` and `npm test` pass. `npm --prefix dashboard run build` succeeds. Run `node dashboard/scripts/visual-check.mjs` and look at ALL the screenshots in `dashboard/scripts/visual-out/` — confirm `/services` now shows a visible gap between its CLI tools / Website scrapes / APIs / Uncategorized panels (previously touching), confirm `/workflows` still shows the same gap it had before (visually unchanged, now CSS-driven instead of inline), and confirm no OTHER page shows a new/doubled gap between panels that previously looked correct. Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green. This is a `layer:ui` task — visual confirmation via the screenshots (including a scan across all pages for regressions, since this is a global CSS rule) is a hard requirement.
