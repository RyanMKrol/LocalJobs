## Do

The **Recommendations & gaps** section (the `MovieGapsManager` component in
`dashboard/app/workflows/[name]/page.tsx`, built by T152) on the movies workflow detail page
(`/workflows/movies`) looks bad: it sprawls down the page uncontained, and the per-item **Ignore**
buttons are styled inconsistently with the rest of the site. Tidy it up:

1. **Contain it + make it scrollable.** Wrap the list (the gaps grouped by collection, and the
   recommendations) in a **bounded-height container** with `overflow-y: auto`, so a long list
   scrolls WITHIN the section instead of stretching the whole page (same spirit as the T148 IO-panel
   height cap). Pick a sensible max-height (e.g. a panel that shows ~8–12 rows then scrolls); keep
   the section header/title fixed above the scroll area if practical. Don't introduce horizontal
   page overflow.
2. **Style the Ignore buttons like the rest of the site.** Use the standard button styling the rest
   of the dashboard uses (the `.btn` class / the existing pill/button conventions driven by the
   theme CSS variables) instead of bespoke styling, so the Ignore controls match other buttons and
   **adapt correctly to every theme** (default + the joyful themes). They should read as first-class
   site buttons, not ad-hoc elements.
3. Keep the behaviour unchanged — grouping by collection, the per-item Ignore action
   (`api.movieGapsIgnore` → `ignoreSurfacedItem`, dropping the item from future reports +
   notifications), the active/ignored split, and the poll. Only the containment + button styling
   change.
4. **Fix the EMPTY state too.** When there's no data yet (no run, or no current gaps/recs), the
   section currently renders an awkward oversized bordered box around the placeholder text ("No audit
   has run yet. This workflow runs monthly…"). Give the empty state a tidy, consistent treatment —
   matching how the rest of the dashboard shows empty/placeholder states (a muted message, not a
   big mismatched bordered panel) — so it looks clean and intentional with no data, not just when
   populated.
5. Confirm it sits cleanly within the workflow detail page's existing section layout, and is
   **mobile-responsive** at ~402px (the scroll container + buttons must not overflow or look broken
   on a phone).

## Done when

The Recommendations & gaps section on `/workflows/movies` is a contained, fixed-max-height panel
whose list scrolls internally (no more sprawling down the page), and its Ignore buttons use the
site's standard themed button styling (consistent across the default + joyful themes); the EMPTY
state (no run / no data) renders as a tidy muted placeholder consistent with the rest of the
dashboard rather than an awkward oversized bordered box; behaviour is unchanged; it's mobile-safe;
`npm --prefix dashboard run build` passes and `node dashboard/scripts/mobile-check.mjs` stays green.
