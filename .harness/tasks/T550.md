## Overview
Adopt the shared <Pill> and <StatusBadge> components everywhere bare span markup is used, widening StatusBadge's status type to cover workflow statuses so the casts and bare spans can go.

## Do
Do NOT touch the Workflow.category field (item 5 already done, api.ts:237). (a) admin/page.tsx:190 renders a bare <span className={`pill ${…}`}> — replace with <Pill> (the same file already uses <Pill> at :117). (b) StatusBadge (ui.tsx:44) is typed { status: RunStatus }, but workflow statuses include 'partial', which forced bare <span className={`badge ${status}`}> at workflows/[name]/page.tsx:1089, workflows/page.tsx:120, and components/StageIoLists.tsx:143, plus `status as RunStatus` casts at workflow-runs/[id]/page.tsx:89 and the two gate pages (:153, :172). Widen the StatusBadge prop type to RunStatus | WorkflowRunStatus (its emoji/label maps already handle 'partial'), replace the three bare badge spans with <StatusBadge>, and delete the `as RunStatus` casts. A grep for bare pill/badge spans across dashboard/app must then be clean.

## Done when
No bare `className="pill …"` or `className={\`badge …\`}` spans remain in dashboard/app (grep-clean); StatusBadge accepts RunStatus | WorkflowRunStatus and the `as RunStatus` casts are removed; npx tsc --noEmit is green; the badges/pills render identically (LOOK at visual-check for admin, workflow-detail, workflows-list, stage-io); build and mobile-check green.
