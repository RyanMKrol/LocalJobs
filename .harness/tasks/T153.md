## Do

On the **Services** page (`dashboard/app/services/page.tsx`) the services are listed in whatever
order the API returns them, which is arbitrary. Sort them by **Type then name**: group **Free**
services first, then **Paid**, and within each group order **alphabetically by name**. (Free-first
is the sensible default — paid services are the ones with quotas worth scrutinising, but grouping
keeps them together; the owner is fine with type-then-alphabetical.)

- Implement the sort either in `dashboard/app/services/page.tsx` (sort the `services` array before
  mapping — `paid` ascending [free=0/paid=1 → free first], then `name` localeCompare) or in the
  `listServices` query in `src/db/store.ts` (`ORDER BY paid ASC, name ASC`). Page-level is fine and
  self-contained; pick one and keep it the single source of order.
- Keep the existing rows/columns and the free/paid pill unchanged — only the order changes. A small
  visual cue that the list is grouped (e.g. the free/paid pill already present is enough) is fine;
  no need for section headers unless trivial.
- Stay mobile-responsive.

## Done when

The Services page lists services grouped by type — all Free first, then all Paid — each group
alphabetical by name; the change is in one place (page sort or the `listServices` query); rows,
columns, the free/paid pill, and editing still work and it stays responsive at phone width.
`npm --prefix dashboard run build` passes (and `npm test` if `store.ts` is touched), and
`node dashboard/scripts/mobile-check.mjs` stays green.
