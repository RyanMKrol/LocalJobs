## Overview
One slice of the Plex→callService migration blocked twice (T474, T499) by a pre-push git-hygiene guard trip (sensitive/gitignored path staged, no code landed). The spec was always correct; the fix is to shrink the diff per unit so staging named files is trivial. This unit is plex-profiles, which has the most Plex reads of any workflow (~7 call sites) — the largest single slice, still only 3 files.

## Do
In src/workflows/plex-profiles/stages/build.ts, wrap EVERY plexGet<...>(...) read (the movies/shows/episodes section listings AND the per-title metadata detail call — all ~7 call sites) in callService('plex', () => plexGet<...>(...)), importing callService from ../../../core/services.js. Mirror the callService('tmdb', ...) wrapping convention used in sibling Plex workflows. Do not change arguments, return-value handling, or error handling beyond the wrap. Note: this file also defines resolveInputKeys() which calls plexGet live — wrap those reads too, since they are ordinary Plex reads. Update build.test.ts to account for the callService('plex', ...) wrapper (the no-limit branch passes the call through). Add a one-line note to plex-profiles/CLAUDE.md that its Plex reads are metered via the shared plex service. GIT HYGIENE: stage ONLY these 3 files by explicit name — NEVER `git add -A`/`git add .`/a directory; never stage anything under data/, chrome-profile/, .env*, or a scratch DB.

## Done when
Every plexGet read in build.ts (including resolveInputKeys()'s reads) is wrapped in callService('plex', ...); a grep for a bare plexGet( (not inside a callService('plex', ...) wrapper) in build.ts returns nothing; build.test.ts passes; plex-profiles/CLAUDE.md notes the plex service; only the 3 scoped files are staged.
