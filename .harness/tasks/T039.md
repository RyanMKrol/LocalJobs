## Do

Add a way to push fresh input into a pipeline's data/raw without manual file copying. The mechanism (API endpoint vs CLI vs pull-from-source) is deferred to a human decision before building.

## Done when

Mechanism decided with the human, then built so fresh input can be pushed into a pipeline's data/raw without manual copying.
