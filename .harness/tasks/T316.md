## Do

SIBLING task to a separate 'flatten Missing Seasons' task — both were authored together because the owner wants the two sections to look visually consistent, but the two tasks are ORTHOGONAL (no `dependsOn` between them) and may be built/land in either order. This task covers ONLY `MovieGapsManager` (the franchise-gaps section on the `/workflows/movie-recommendations` page) — do not touch `MissingSeasonsManager` or the missing-tv-seasons page.

In `dashboard/app/workflows/[name]/page.tsx`, rewrite `MovieGapsManager` (currently ~line 347-490) so the ACTIVE franchise-gaps list renders as ONE flat, bordered table wrapped in a single `<div className="panel">` — matching the visual pattern of this same page's own `Runs` table (~line 884-901: `<div className="panel"><table>…</table></div>`, one `<thead>`, one row per run). This REPLACES the current per-collection `<div className="panel" key={cname}>` + nested `<table className="gaps-table">` loop (~line 413-459) sitting inside a `.movie-gaps-scroll` wrapper (~line 412).

Target shape (mirrors the sibling missing-seasons task): ONE `<table>`, with a full-width ROW-GROUP HEADER row per collection, followed immediately by that collection's film rows.

Concretely:
- Wrap the whole active list in ONE `<div className="panel"><table>…</table></div>`. Delete the `.movie-gaps-scroll` div and the per-collection `.panel` divs for the ACTIVE section only. The separate 'Ignored' section below (~line 461-486) is UNCHANGED — it's already a single flat `<table>`, do not touch it.
- `<thead>`: `<tr><th>Film</th><th>Year</th><th>TMDB</th><th></th></tr>` (unchanged column set from the current inner table).
- `<tbody>`: iterate `groupByCollection(active)` (keep this helper at ~line 53 unchanged) and for each `[cname, films]` emit:
  1. ONE header row: `<tr className="table-group-header"><td colSpan={4}>` containing a flex row (reuse the existing header's flex layout, `style={{ display:'flex', alignItems:'baseline', justifyContent:'space-between', gap:8 }}`) with (a) the collection name — this stays PLAIN TEXT (it is not currently a link, do not make it one), plus, if `data?.collectionExamples?.[cname]` exists, the existing 'You own: …' line as a smaller sub-line inside the same header cell (reuse the existing muted/12px styling for that sub-line, ~line 428-431), and (b) the existing '✕ Ignore all' button. Keep the `ignoreCollection(cname, films)` handler and `busyCollection` state EXACTLY as they are today (~line 370-382) — only the surrounding JSX shell changes.
  2. Then one `<tr>` per film in `films`, same per-row content as today (film title link + 'notified' marker, year, TMDB rating, '✕ Ignore' button calling `ignore(g)` — keep the `ignore(g)` handler and `busy` state at ~line 357-368 unchanged).
- Row-group header CSS: use the SAME `.table-group-header` class name as the sibling missing-seasons task (`.table-group-header td { background: var(--panel-2); font-weight: 600; }` in `dashboard/app/globals.css`, near ~line 172-181). Since the two tasks build independently and may land in either order: check first via `grep -n 'table-group-header' dashboard/app/globals.css` — if it already exists (the sibling task landed first), reuse it as-is; if it does not exist yet, add it yourself with that exact rule/name so the sibling task (whenever it lands) reuses yours instead of duplicating. Document in a short code comment above the rule why this generic name is intentionally shared between the two workflow-detail sections.

Contrast fix (the owner confirmed this — apply within this component only, do NOT touch theme CSS variables):
- The per-film title `<a>` (linking to `https://www.themoviedb.org/movie/${g.tmdbId}`) currently inherits the global `a { color: var(--accent); }` rule. Add `style={{ color: 'var(--text)' }}` so it renders in full-contrast text instead of the theme accent color — same fix, same reasoning as the sibling task's show-title link. Keep it a real link (same href, `target="_blank" rel="noreferrer"`).
- The 'Year' and 'TMDB' rating columns are already plain (inherit `--text`, no `muted` class) — no change needed there; they are not part of the contrast problem.
- Leave the 'notified' secondary marker (`<span className="muted">notified</span>`) as-is, same reasoning as the sibling task.

ROOT-CAUSE NOTE — record this, do not act on it in this task (same finding as the sibling missing-seasons task, repeated here because this spec is self-contained): the low-contrast look is worst in the `sunny-8bit` theme family's LIGHT mode (`globals.css` ~line 679-687). Measured contrast: `--accent` (`#e8821f`) against `--panel` (`#fff9ec`) is ~2.6:1; `--muted` (`#9c7e44`) against `--panel` is ~3.65:1 — both under the WCAG AA 4.5:1 floor for body text. Backlog tasks T308/T309 plan to hardcode the whole dashboard onto exactly this sunny-8bit+baloo combo, which would make this contrast gap permanent and app-wide. Do NOT edit the sunny-8bit palette in this task — instead, write this measurement into `.harness/worklog/<this-task's-id>.md` so it's visible to whoever works T308/T309.

DEAD-CSS CLEANUP (coordinate with the sibling task, whichever lands second does this): both this task and the sibling missing-seasons task stop using `.movie-gaps-scroll` and `.gaps-table` (this task also stops using `.gaps-table` specifically — check `grep -n 'gaps-table' dashboard/app/globals.css dashboard/app/workflows/\[name\]/page.tsx` for remaining references after your change). If, after your change, NEITHER `.movie-gaps-scroll` NOR `.gaps-table` is referenced anywhere in `dashboard/app/` any more (i.e. the sibling task already landed and removed its own usage), delete those now-dead CSS rules from `globals.css`. If the sibling task has NOT landed yet (its usage of `.movie-gaps-scroll` is still present), leave both CSS rules in place — do not delete a class still in use by the other section.

Finally, update `dashboard/scripts/_dashboard-harness.mjs`:
- The `workflow-movie-recs` PAGES entry (`/workflows/movie-recommendations`) ALREADY EXISTS — no new PAGES entry is needed for this task.
- However, check the current `/api/movie-gaps` fixture in `fixtureFor()`: as of this writing it returns `{ generatedAt: NOW, gaps: [], collectionsChecked: 0, collectionExamples: {} }` — an EMPTY `gaps` array, meaning the franchise-gaps table never actually renders any rows on the existing `workflow-movie-recs` screenshot today. UPDATE this fixture (do not just leave it) to return several non-empty `gaps` matching the `MovieGap`/`MovieGaps` shape in `dashboard/app/lib/api.ts` (`{ collectionId, collectionName, tmdbId, title, year, tmdbRating, notified, ignored }`): at least 2 distinct `collectionName` groups; at least one collection with 2+ films (to exercise multiple rows under one row-group header); at least one film with `notified: true`; at least one film with `ignored: true` (to exercise the 'Ignored' panel below); a `collectionExamples` entry for at least one collection (to exercise the 'You own: …' sub-line); reuse the file's `LONG` constant for one film title.
- No new `FLOWS` entry is required — same reasoning as the sibling task, no new interactive state beyond baseline.

## Done when

- `npx tsc --noEmit` and `npm test` pass from the repo root; `npm --prefix dashboard run build` passes.
- `node dashboard/scripts/mobile-check.mjs` passes (no phone-width overflow introduced by the new table).
- `node dashboard/scripts/visual-check.mjs` runs clean AND you have actually LOOKED at the generated `dashboard/scripts/visual-out/workflow-movie-recs.png` screenshot with your own eyes and confirmed: (a) the franchise-gaps list now actually renders rows (the fixture was empty before — verify the updated fixture produces visible content); (b) it renders as ONE single bordered table in ONE panel — no separate boxed/rounded panel per collection; (c) each collection appears as one full-width row-group header row (name + optional 'You own: …' sub-line + Ignore-all button) immediately followed by its film rows; (d) the film-title links are clearly legible (full-contrast, not washed-out orange-on-cream) in whatever theme the hermetic harness's default (unseeded) run renders; (e) the default theme still looks correct.
- Re-read the diff and confirm the `ignore(g)`/`ignoreCollection(cname, films)` handlers, their `busy`/`busyCollection` state, and their `api.ignoreMovieGap`/`api.ignoreMovieGapBulk` calls + `confirm()` dialogs are BYTE-IDENTICAL in behavior to before this change — only the surrounding JSX table shell changed.
- The separate 'Ignored' table below the active list is confirmed unchanged.
- `grep -n 'table-group-header' dashboard/app/globals.css` shows exactly ONE definition of the class (either you added it, or you correctly reused the sibling task's existing one — no duplicate rule).
- `.harness/worklog/<this-task's-id>.md` explicitly records the sunny-8bit light-mode contrast measurement (~2.6:1 accent-on-panel, ~3.65:1 muted-on-panel, both under WCAG AA 4.5:1) as a flag for whoever builds T308/T309.
- Confirm via a diff review that `MissingSeasonsManager` and the missing-tv-seasons page were NOT touched by this task — that's the sibling task's scope.
- The `/api/movie-gaps` fixture in `dashboard/scripts/_dashboard-harness.mjs` is confirmed non-empty (`gaps.length > 0`) after this task, where it was empty before.
