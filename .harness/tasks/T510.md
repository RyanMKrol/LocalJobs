## Overview
Cache the `dynamodb` read (a table `Scan`) for 22 hours. Reads only — nothing in local-jobs writes to
DynamoDB (confirmed T399), and no write path may be introduced. Outcome of the T479 caching-adoption
decision; the caching mechanism already exists (T451/T476).

## Do
1. Set `cacheTtlMs: 79_200_000` (22 hours) on the `dynamodb` `ServiceDefinition` in
   `src/services/dynamodb.service.ts`.
2. Add a stable `cacheKey` to the `callService('dynamodb', () => dynamoScan(...))` read site in
   `src/workflows/perfumes/lib.ts` (around line 18), keyed by the scanned table name:
   `` `dynamodb:scan:${table}` `` (the table is `perfumesConfig.perfumeRatingsTable`).
3. Cache the read (`Scan`) only; do NOT add caching to any write path (none exists today, and must
   not be introduced).

## Done when
- `cacheTtlMs: 79_200_000` is set on the `dynamodb` service def.
- The `dynamoScan` call site in `perfumes/lib.ts` passes a stable `dynamodb:scan:<table>` cacheKey.
- `npx tsc --noEmit` and `npm test` are green.
- Verified a repeated scan of the same table within 22h is served from `service_cache` (hermetic
  scratch-DB check) rather than a second live call.
