## Do

The owner wants a standing contract: EVERY workflow detail page that renders `WorkflowOutputSection` must always show an Output section — a heading plus either the item table OR an explicit "no output" message — never silently render nothing. Currently `dashboard/app/components/WorkflowOutputSection.tsx` line ~105 has:
```
if (!error && items.length === 0 && data !== undefined) return null;
```
This returns null (renders NOTHING, not even the `<h2>Output</h2>` heading) whenever there are zero items and no error — regardless of WHY (a bug like the listening-digest one fixed in a sibling task, a workflow that hasn't run yet, or a workflow that genuinely never produces per-item output). Below that early return, the component ALREADY has the correct empty-state markup written (currently unreachable dead code):
```
{data && items.length === 0 && (
  <p className="muted" style={{ fontSize: 13 }}>
    No output yet. Run the workflow — produced items will appear here.
  </p>
)}
```

1. Delete the premature early-return line (`if (!error && items.length === 0 && data !== undefined) return null;`). Do not otherwise restructure the component — once that line is gone, the existing (previously dead) empty-state paragraph above becomes reachable and the component naturally always renders the `<div className="output-section"><h2>Output</h2>...` wrapper plus either the table (items present), the empty-state message (loaded, zero items), or the error message (fetch failed). Confirm the loading state (`data === undefined`, i.e. before the first poll resolves) does not show a premature/incorrect empty-state flash — with the guard removed, during loading neither the items-table branch nor the `data && items.length === 0` empty-state branch renders (both require `data` truthy), so only the heading + description show while waiting, which is fine.
2. LIVING ARTIFACT update (mandatory per root `CLAUDE.md`'s dashboard visual-confirmation rule) — `dashboard/scripts/_dashboard-harness.mjs` currently has NO fixture route for `.../output-items` at all: it falls through to the generic `pathname.startsWith('/api/workflows/')` catch-all (returns `{ workflow: workflow() }`), so `api.workflowOutputItems(...)` receives the wrong shape, `items` is always `[]`, and — before this fix — the section was ALWAYS hidden in every hermetic screenshot, meaning neither the "has items" nor the new guaranteed-empty state has ever actually been visually exercised. Fix this in the same change:
   a. In `fixtureFor`, BEFORE the generic `if (pathname.startsWith('/api/workflows/')) return { workflow: workflow() };` catch-all, add a specific route match for `pathname.endsWith('/output-items')`:
      - When `pathname === '/api/workflows/places/output-items'`, return a small non-empty `{ items: [...], terminalJobs: ['places-enrich-with-llm'] }` — e.g. two `OutputItem`-shaped rows (`{ jobName, itemKey, name, hasMarkdown, updatedAt }`), at least one with `hasMarkdown: true` and one with `name: null` — exercising the populated table + View-button rendering for the first time.
      - For any OTHER `.../output-items` path, return `{ items: [], terminalJobs: [] }` — the guaranteed explicit empty state this task adds.
   b. Add a new `PAGES` entry that renders the empty state on a real, non-excluded workflow, e.g. `{ name: 'workflow-empty-output', path: '/workflows/workouts-sync' }` (`workouts-sync` is a real example workflow in this repo and is not in `WORKFLOWS_WITH_SPECIFIC_MANAGERS`, so it renders the generic `WorkflowOutputSection`).
3. Build the dashboard (`npm --prefix dashboard run build`), run `node dashboard/scripts/mobile-check.mjs` (must stay green), then run `node dashboard/scripts/visual-check.mjs` and LOOK at both the updated `workflow.png` (now shows a real populated Output table for `places`) and the new `workflow-empty-output.png` (shows the heading + "No output yet…" message) screenshots in the gitignored `dashboard/scripts/visual-out/` — confirm both paint correctly, nothing is blank/clipped/overlapping, and the empty-state message reads sensibly at both desktop and (via mobile-check) phone width.

## Done when

- The early `return null` guard in `WorkflowOutputSection.tsx` is removed; the section always renders a heading, and shows either the item table, the explicit "No output yet…" message, or the error message.
- `_dashboard-harness.mjs` has a dedicated `output-items` fixture route (populated for `places`, empty for everything else) and a new `PAGES` entry exercising the empty state, per the living-artifact rule.
- `npm --prefix dashboard run build`, `node dashboard/scripts/mobile-check.mjs`, and `node dashboard/scripts/visual-check.mjs` all pass, and both the new empty-state screenshot and the newly-populated `places` output table screenshot were visually confirmed (described in the worklog) to render correctly.
- `npx tsc --noEmit` and `npm test` pass.
