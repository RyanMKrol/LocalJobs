## Do

Add a new **"Enabled"** column to the Workflows **list** view (`dashboard/app/workflows/page.tsx`),
rendered as a **pill** (not plain text): a green "on" pill when the workflow is enabled, a muted/grey
"off" pill when disabled. Read the workflow's `enabled` flag (already present on the `GET /api/workflows`
payload). Add the pill style to `dashboard/app/globals.css` reusing the existing pill idiom (the
`.pill` base + a new `.pill.on` / `.pill.off`, or the closest existing variant — they're all
theme-variable-driven, like `.pill.done`/`.pill.reviewed`/`.pill.failed`). This is display-only — the
control to CHANGE enabled already exists on the workflow detail page; here we only surface the state.

Scope this task to the enabled pill column only. (The owner's broader "use a pill for any enumeration"
idea is aspirational, not part of this task.)

## Done when

- The Workflows list shows an "Enabled" column with an on/off pill reflecting each workflow's
  `enabled` state (green on / muted off), reusing the existing `.pill` styling.
- It stays mobile-responsive (no new phone-width overflow; the `@media (max-width: 640px)` block) and
  theme-variable-driven (no hardcoded colours).
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs`
  are green.
- `node dashboard/scripts/visual-check.mjs` was run after the build, the `workflows.png` screenshot in
  `dashboard/scripts/visual-out/` shows the Enabled pill column rendering for both on and off states
  (ensure the fixtures in `dashboard/scripts/_dashboard-harness.mjs` include an enabled AND a disabled
  workflow so both pills are exercised — update that file as part of this change), and the harness is
  current.
