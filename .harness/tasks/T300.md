## Do

Fix `stocks-watch` (`src/jobs/stocks-sync/stages/stocks-watch.ts`, from T290) getting marked
`skipped` by the framework's noop-detection mechanism (`hasJobAdvancedAnyItem` in
`src/core/workflow-executor.ts`) even though it did real work — reading the portfolio and computing
`gainPct` for every position. This happens because `runStocksWatch` currently only calls
`markWorkItem` in two narrow branches (a fresh breach, or resetting a stale notified-flag), so in the
common case (nothing breaching, or an already-notified position still above threshold) it calls
`markWorkItem` zero times, tripping the "no ledger writes = nothing to do" heuristic.

Split the single `stocks-watch` stage into TWO stages in the `stocks-sync` workflow:

1. **The check stage** — keep the name `stocks-watch`. It reads `data/out/portfolio.json` and, for
   EVERY position, EVERY run, computes `gainPct` (reuse the existing `gainPct`/`isBreaching` helpers
   in `stocks-watch.ts` unchanged) and calls `markWorkItem('stocks-watch', ticker, 'success', {
   detail: { gainPct, breaching: <bool> } })` UNCONDITIONALLY for every position it checks — not just
   in the fresh-breach/reset branches. This guarantees at least one `markWorkItem` call whenever
   there's at least one position, so this stage can never be misclassified as noop. Determine which
   positions are FRESH breaches this run using the SAME logic already in the current code (a position
   with `gainPct >= 30` that was NOT already marked notified in a separate ledger row — you'll need a
   second ledger key or a `detail` field to track "already notified" state distinctly from "was
   checked this run", since the check stage now marks every position `success` every run regardless of
   breach status). Write the list of this run's fresh-breach tickers (with their gain/price details)
   to a new small artifact, e.g. `data/out/fresh-breaches.json` (empty array if none), so the next
   stage doesn't have to re-derive breach status from the ledger.
2. **A new dependent stage, `stocks-notify`** — reads `data/out/fresh-breaches.json`. If non-empty,
   sends exactly ONE push via the existing `push()` helper from `src/core/notifier.ts`, reusing
   `buildDigest`/`formatBreachLine` from `stocks-watch.ts` unchanged (one line per breaching ticker,
   e.g. `AAPL +32% since last buy ($150.00 → $198.00)`). If empty, this stage does nothing and is
   EXPECTED to legitimately show as `skipped`/noop — that's correct, desired behavior (per the
   owner's framing: the *decision* to notify is fine to show as skipped; it was the *checking* work
   being mislabeled skipped that was wrong).
3. Preserve exactly: idempotency (notify once per breach episode; a position that drops back below
   30% after being notified resets so a later re-crossing notifies again), the exact push content/
   format, and "exactly ONE push even if multiple positions breach in the same run."
4. Update `src/jobs/stocks-sync/stocks-sync.workflow.ts`: wire `stocks-notify` as a member
   `dependsOn: ['stocks-watch']` (in addition to the existing `stocks-snapshot` → `stocks-watch` edge).
5. Update root `CLAUDE.md`'s `stocks-sync` paragraph and `README.md`'s bullet to describe the 3-stage
   shape (snapshot → check → notify) and the corrected skip semantics: the check stage (`stocks-watch`)
   always reports success when it ran; only `stocks-notify` can legitimately show as skipped, meaning
   "nothing to send this run."

## Done when

- `stocks-sync.workflow.ts` has 3 member jobs: `stocks-snapshot` → `stocks-watch` → `stocks-notify`.
- A unit test demonstrates: a run where NO position breaches results in `stocks-watch` NOT being
  noop/skipped (it called `markWorkItem` for every checked position) while `stocks-notify` IS
  noop/skipped (no push sent) — i.e. reproduce the exact scenario the framework's
  `hasJobAdvancedAnyItem` check would evaluate, confirming `stocks-watch`'s ledger activity means it's
  never misclassified.
- A unit test demonstrates the full idempotency chain still holds across the two stages: a position
  newly crossing 30% → `data/out/fresh-breaches.json` includes it → `stocks-notify` sends one push
  naming it; a subsequent run where it's still above 30% → not in `fresh-breaches.json` again (already
  notified) → `stocks-notify` sends nothing; a run where it drops back below 30% → check stage resets
  the notified-flag; a LATER re-crossing → notified again.
- A run where multiple positions freshly breach in the same cycle results in exactly ONE push from
  `stocks-notify`, listing all of them (not one push per position).
- Root `CLAUDE.md` and `README.md` describe the 3-stage shape and the corrected skip semantics.
- `npm test` and `npx tsc --noEmit` pass.
