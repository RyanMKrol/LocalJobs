## Do

Two components in `dashboard/app/workflows/[name]/page.tsx` render a per-group "✕ Ignore all" button unconditionally, even when the group has only ONE item — in that case it does exactly the same thing as the single item's own row-level "✕ Ignore" button, so showing both is redundant duplication:
- `MissingSeasonsManager` (currently ~lines 586-638): `groupByShow(active).map(([meta, nums]) => ...)` renders the per-show `<tr className="table-group-header">` header (title/link + "✕ Ignore all" button calling `ignoreShow(meta, nums)`) unconditionally for every show, regardless of `nums.length`.
- `MovieGapsManager` (currently ~lines 413-444): `groupByCollection(active).map(([cname, films]) => ...)` renders the per-collection header (collection name + "You own: ..." text + "✕ Ignore all" button calling `ignoreCollection(cname, films)`) unconditionally for every collection, regardless of `films.length`.

In BOTH components, only render the "✕ Ignore all" button when the group has MORE THAN ONE item (`nums.length > 1` for Missing Seasons, `films.length > 1` for Franchise Gaps). When a group has exactly one item, keep rendering the rest of the group-header row as-is (title/link, and for `MovieGapsManager` the "You own: ..." example text) — just omit the button, since the single row below already offers an equivalent "✕ Ignore" action. Do not change the button's behavior, confirmation copy, or the `ignoreShow`/`ignoreCollection` handlers when the button IS shown (2+ items); do not touch `groupByShow`/`groupByCollection` themselves — this is a conditional-render change scoped to the existing button JSX inside each group-header row. Keep the flex header row (`display: flex; justify-content: space-between`) looking correct when the button is omitted — the title should not awkwardly stretch or misalign just because the trailing button is gone.

## Done when

- `npx tsc --noEmit` and `npm test` pass; `npm --prefix dashboard run build` succeeds.
- Run `node dashboard/scripts/visual-check.mjs` and LOOK at the screenshots for the `missing-tv-seasons` and `movie-recommendations` workflow detail pages. The hermetic fixtures in `dashboard/scripts/_dashboard-harness.mjs` already include BOTH cases for each table — e.g. missing-seasons fixture show `tmdbId: 500` ("The Wire") has 2 seasons (a multi-item group) while `tmdbId: 501`/`502` each have exactly 1 season (single-item groups); movie-gaps fixture collection 1 ("The Bourne Collection") has 2 films while collection 2 (the LONG-named one) has exactly 1 film. Confirm from the screenshots that single-item groups show NO "✕ Ignore all" button (only the single row's own "✕ Ignore") while multi-item groups still show it, for BOTH tables. If the fixtures ever stop covering both cases, update `_dashboard-harness.mjs` in this same change (per the LIVING ARTIFACT rule) and note it in the worklog.
- Run `node dashboard/scripts/mobile-check.mjs` and confirm it stays green.
