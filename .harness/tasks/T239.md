## Do

Vertically centre the footnote caption in the workflow-run **Input → Output** panel — "One output is
shown per input (fan-out is collapsed to its first output)." — which currently hugs the top/left of
its band instead of sitting centred. It's a full-width caption row under the last I/O mapping row,
rendered in `dashboard/app/workflow-runs/[id]/page.tsx`; styling in `dashboard/app/globals.css`.
Likely a flex `align-items:center` / line-height / padding tweak on that caption cell.

## Done when

- The "fan-out is collapsed…" caption is vertically centred within its band.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
