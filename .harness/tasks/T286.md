## Do

Rename every command file under `.claude/commands/` to carry a shared `local-jobs-` prefix, so each
command's invocation gains a distinguishing prefix in the type-ahead. **Important: Claude Code has NO
subdirectory/namespace mechanism for project-local commands** — putting files in a subfolder does
NOT produce a `local-jobs:command`-style colon namespace (that colon form only exists for genuine
installed plugins). The only supported way to add a shared prefix is a plain filename rename, which
changes the invocation string itself (e.g. `.claude/commands/idea.md` → invoked as `/idea` becomes
`.claude/commands/local-jobs-idea.md` → invoked as `/local-jobs-idea`). Do NOT attempt a subdirectory
or any colon-namespace approach — it will not work.

As of this task's authoring, the commands are: `idea.md`, `convert-ideas.md`, `loop-recover.md`,
`mark-task-failed.md`. **Rename every `.md` file that exists in `.claude/commands/` at build time**
(not just these four — a task built earlier in the backlog, e.g. T284's new `pre-loop-checkin.md`,
may already exist by the time this task runs) by prefixing its filename with `local-jobs-`, e.g.
`idea.md` → `local-jobs-idea.md`, `pre-loop-checkin.md` → `local-jobs-pre-loop-checkin.md`. Use `git
mv` for each so history is preserved.

After renaming, **every reference to the OLD invocation form must be updated to the new one** across
the repo's CURRENT/living documentation (do NOT touch historical records — see exclusions below):
- The command files themselves may cross-reference each other by old name (e.g.
  `.claude/commands/mark-task-failed.md` currently says `` `/convert-ideas` `` — update to
  `` `/local-jobs-convert-ideas` ``). Grep every renamed file's own body for the OTHER old command
  names and fix them.
- `CLAUDE.md` (repo root) and `.harness/CLAUDE.md` and `.harness/HARNESS.md` and
  `.harness/designs/manual-fail-signal.md` — grep each for `/idea`, `/convert-ideas`,
  `/loop-recover`, `/mark-task-failed` (and any other command name that existed at build time) and
  update every hit to its `local-jobs-`-prefixed form.
- **Do NOT edit anything under `.harness/worklog/`** or other tasks' spec files under
  `.harness/tasks/` (e.g. `T188.md`, `T278.md`) — those are historical/point-in-time records, not
  living docs, and are deliberately left alone (same convention as leaving worklog narrative
  untouched during a model migration).

Grep broadly (excluding `node_modules/`, `.git/`, `.harness/worklog/`) to make sure no live reference
to an old `/command-name` invocation is missed.

## Done when

- Every file in `.claude/commands/` (whatever set exists at build time) has been renamed via `git mv`
  to carry a `local-jobs-` filename prefix, so each is now invoked as `/local-jobs-<original-name>`.
- No subdirectory or colon-namespace was introduced — plain flat renamed files only.
- Every cross-reference to an old `/command-name` invocation in the command files themselves,
  `CLAUDE.md`, `.harness/CLAUDE.md`, `.harness/HARNESS.md`, and `.harness/designs/manual-fail-signal.md`
  is updated to the new `/local-jobs-command-name` form — verify with
  `grep -rn "/idea\b\|/convert-ideas\b\|/loop-recover\b\|/mark-task-failed\b" CLAUDE.md .harness/CLAUDE.md .harness/HARNESS.md .harness/designs/ .claude/commands/`
  (adjust the pattern list for whatever commands actually existed at build time) returning NO hits
  outside `.harness/worklog/` or other tasks' `.harness/tasks/*.md` files.
- `.harness/worklog/**` and other tasks' `.harness/tasks/*.md` files are left untouched.
- `npx tsc --noEmit` and `npm test` still pass (this task touches no application code, but the
  Definition of Done still applies).
