## Do

The owner reviewed the five toggleable validation-gate display styles built by T099
(review task T126) and CHOSE the **key** style — the `gs-key` rendering in
`dashboard/app/components/Dag.tsx`: a tiny pill showing just the artifact key (no
producer name), state-coloured (passed/failed/pending/structural) and clickable
through to the gate detail page. Hardcode it as the SOLE gate display style on BOTH
graph views (the workflow-run DAG and the structure-only workflow graph) and remove
the live chooser + the four unused styles (`icon`, `dot`, `connector`/"Arrow",
`lock`). Do all of:

1. **Hardcode key.** In `Dag.tsx`, make gate marks always render as the `key` pill
   (the artifact key, `gateContent` → `g.key`). Remove the per-style branching:
   simplify/inline `gateContent` (~lines 60-66) to always return `g.key`, drop the
   `style`/`eff` plumbing in `GateMarks` (~lines 76-92) so it always uses the `key`
   class (`dag-gate gs-key <state>`), and remove the `connector`-specific arrow-
   colouring path (the `style === 'connector'` handling and wherever the `gate-conn`
   class is applied to the arrow glyph). Every gate mark MUST stay clickable to its
   detail page (keep the `g.href` `<a>` vs `<span>` logic) and stay state-coloured on
   BOTH graph views, at desktop (→) and phone (↓) widths, including the non-adjacent
   producer→consumer fallback that renders under the consumer node.

2. **Remove the toggle.** Delete `GateStyleBar` (~lines 100-117) and its render site
   (`{hasGates && <GateStyleBar … />}` ~line 207), the `const [style, setStyle] =
   useGateStyle()` (~line 153), and the `GATE_STYLES`/`useGateStyle` imports + the
   `GateStyle` type import.

3. **Remove the unused exports.** In `dashboard/app/ui.tsx` remove `GATE_STYLES`,
   `GateStyle`, `useGateStyle`, `GATE_STYLE_KEY`, and `DEFAULT_GATE_STYLE` (first
   confirm only `Dag.tsx` imported them).

4. **Remove the unused CSS.** In `dashboard/app/globals.css` remove the gate-style
   selector-bar rules (`.gate-style-bar`, `.gate-style-label`, `.gate-style-btn` +
   `:hover`/`.active`) and the connector arrow-colouring rules (`.dag-arrow-glyph
   .gate-conn*`, including the phone-width `::after` variants ~lines 482-484), plus any
   CSS exclusive to the dropped `icon`/`dot`/`lock` styles (`.gs-icon`, `.gs-dot`,
   `.gs-lock`, etc.). ⚠️ KEEP every rule the `key` pill depends on — the `.dag-gates`,
   `.dag-gate`, `.gs-key`, and the gate STATE colours (`.passed`/`.failed`/`.pending`/
   `.structural`). Grep to confirm which `.gs-*`/`.dag-gate` rules the key style needs
   before deleting, so the key pill keeps its colour + layout intact.

## Done when

On both graph views (workflow-run DAG and structure-only workflow graph) every
validation gate always renders as the `key` pill (artifact key, state-coloured) with
NO gate-style selector anywhere; the four other gate styles, the chooser/toggle, and
the connector arrow-colouring path are removed from Dag.tsx, ui.tsx, and globals.css
(with the `key`-pill CSS + gate state colours kept); every gate mark stays clickable to
its detail page and state-coloured at desktop and phone widths, including the
non-adjacent consumer-node fallback; `npx tsc --noEmit`, `npm test`,
`npm --prefix dashboard run build`, and `node dashboard/scripts/mobile-check.mjs` are
all green.
