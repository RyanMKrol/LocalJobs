## Overview

The owner wants every workflow detail page to use exactly ONE section heading, literally "Output".
Two workflows (`tv-recommendations` and `missing-tv-seasons`) already have exactly one output
section each, but its heading text is wrong ("TV Recommendations" and "Missing seasons"
respectively) — this is the easy half of that ask: a heading-text-only rename, no structural change.
(The `movie-recommendations` workflow's harder case — it has TWO separately-headed sections that
need reorganizing under one "Output" heading — is a separate, not-yet-shaped task pending an owner
decision on the merge shape; do not attempt that here.)

## Do

1. In `dashboard/app/workflows/[name]/page.tsx`, `TvRecsManager` (the section rendered for the
   `tv-recommendations` workflow) currently has `<h2>TV Recommendations</h2>` (around line 95).
   Change it to `<h2>Output</h2>`. Leave the descriptive paragraph immediately below it unchanged —
   it already explains what the section shows ("Shows recommended for you by the Claude-powered
   recommender branches…"), which stays accurate regardless of the heading text.
2. In the same file, `MissingSeasonsManager` (rendered for the `missing-tv-seasons` workflow)
   currently has `<h2>Missing seasons</h2>` (around line 554). Change it to `<h2>Output</h2>`. Leave
   its descriptive paragraph unchanged for the same reason.
3. Do NOT touch `MovieRecsManager`'s `<h2>Recommendations</h2>` or `MovieGapsManager`'s
   `<h2>Franchise gaps</h2>` (the two `movie-recommendations` sections) — that reorganization is a
   separate task blocked on an owner decision about the merge shape (single heading with `<h3>`
   subsections vs. a fully combined table vs. something else). Changing either of THOSE two headings
   to "Output" here would produce two sections both literally called "Output" on the same page, which
   is not the intended fix.
4. Confirm (grep `dashboard/app/workflows/[name]/page.tsx` for `<h2>`) that after this change, the
   full workflow-heading census is: `WorkflowOutputSection.tsx` → "Output" (unchanged, already
   correct, used by every non-managed workflow), `tv-recommendations` → "Output" (fixed by this
   task), `missing-tv-seasons` → "Output" (fixed by this task), and `movie-recommendations` → still
   "Recommendations" + "Franchise gaps" (intentionally untouched, tracked separately). This satisfies
   the "validate this holds for every workflow" ask for the part of the contract this task addresses
   — record this census in the worklog.
5. No CSS, fixture, or `_dashboard-harness.mjs` changes are needed — `PAGES` already has
   `workflow-tv-recs` (`/workflows/tv-recommendations`) and `workflow-missing-tv-seasons`
   (`/workflows/missing-tv-seasons`) entries that will pick up the new heading text automatically on
   the next screenshot run; no heading text is asserted anywhere in the harness fixtures (confirmed via
   grep for the old strings).

## Done when

- `TvRecsManager`'s heading reads `<h2>Output</h2>` (was "TV Recommendations").
- `MissingSeasonsManager`'s heading reads `<h2>Output</h2>` (was "Missing seasons").
- `MovieRecsManager` ("Recommendations") and `MovieGapsManager` ("Franchise gaps") headings are
  unchanged.
- The worklog records the full heading census from step 4 above, confirming which workflows now say
  "Output" and which (movie-recommendations) is intentionally still pending a separate task.
- `npx tsc --noEmit` and `npm test` pass.
- `npm --prefix dashboard run build` succeeds.
- **Visual confirmation (this is a `layer: ui` task):** run `node dashboard/scripts/visual-check.mjs`
  and look at `workflow-tv-recs.png` (confirm the section heading now reads "Output") and
  `workflow-missing-tv-seasons.png` (confirm the section heading now reads "Output") — as well as
  `workflow-movie-recs.png` to confirm its two headings ("Recommendations" / "Franchise gaps") were
  correctly left untouched. Record what you saw in the worklog.
- `node dashboard/scripts/mobile-check.mjs` stays green.
