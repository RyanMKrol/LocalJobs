## Do

Group the workflows overview/list page (`dashboard/app/workflows/page.tsx`) into category sections
instead of one flat table, using the `category` field added to each workflow in T292
(`WorkflowDefinition.category`, surfaced on `GET /api/workflows` as `category` per workflow — a
string: `'second-brain'` | `'recommendations'` | `'regular-maintenance'` | `'uncategorized'`).

1. Define a small, ordered list of `{ key, label }` category groups in `page.tsx` (or a small
   colocated const): `second-brain` → "Second brain", `recommendations` → "Recommendations",
   `regular-maintenance` → "Regular maintenance", in that display order, followed by an
   `uncategorized` → "Uncategorized" group **rendered only when it has at least one workflow in
   it** (so nothing silently disappears if a future workflow's manifest omits `category`, but the
   group doesn't show up as permanent empty clutter today).
2. Partition the `workflows` array from `api.workflows()` by each row's `category` field into these
   groups, then render the EXISTING table markup (same columns: Workflow / Enabled / Notifications
   (from T291, if already built — if not yet built, keep whatever columns currently exist) / Stages
   / Schedule / Last run / Next / Run button) once per non-empty group, each under its own `<h2>` (or
   similar) heading showing the group's `label`, inside its own `.panel` (mirror how other pages use
   multiple `.panel` blocks for separate sections).
3. Preserve ALL existing per-row behavior (run button, stuck-item popover, links to workflow detail)
   exactly as-is — this task only changes how rows are GROUPED and SECTIONED, not what a row
   contains or does.
4. Update `dashboard/scripts/_dashboard-harness.mjs`'s synthetic workflows-list fixture so its
   fixture workflows collectively cover at least 2 different categories (plus, ideally, one workflow
   with no `category` / `'uncategorized'`) — the workflows-list `PAGES` screenshot must show at least
   two distinct category section headings.

## Done when

- The workflows list page renders one heading + table per non-empty category, in the fixed order
  Second brain → Recommendations → Regular maintenance → Uncategorized (Uncategorized omitted
  entirely when empty).
- All existing per-row functionality (run button, stuck popover, links) still works — no regression
  to row-level behavior, verified by keeping/adapting any existing test coverage for this page.
- After `npm --prefix dashboard run build`, run `node dashboard/scripts/visual-check.mjs` and look at
  the workflows-list screenshot in `dashboard/scripts/visual-out/` — confirm at least two distinct
  category section headings are visible with their respective workflows underneath.
- Run `node dashboard/scripts/mobile-check.mjs` (same build) and confirm no new horizontal overflow
  at the phone-width (~402px) viewport from the added section headings.
- `dashboard/scripts/_dashboard-harness.mjs`'s workflows-list fixture is updated to cover at least 2
  categories (per the Do section above).
- `npm test`, `npx tsc --noEmit`, and `npm --prefix dashboard run build` all pass.
