## Do

Remove the inert per-minute rate limit from the `claude-cli` service. This was investigated
in T046 and established to be safe: `ratePerMinute: 20` was added arbitrarily at publish
time (T004), the perfumes workflow is the only consumer and calls `claude -p`
**sequentially** with **≤1 call ever in flight** (linear DAG + one-active-run-per-workflow +
long per-call durations), so throughput is far below 20/min and the cap **never triggers**.
It also doesn't mirror a real limit (the actual constraint is the ~5-hour plan window, not a
per-minute rate). Do:

1. In the `claude-cli` service definition, remove `ratePerMinute` (omit it, or set it to
   `null`) so the service has no per-minute rate cap. **Locate the file wherever it lives:**
   `src/jobs/perfumes/claude-cli.service.ts` today, OR `src/services/claude-cli.service.ts`
   if T044 (services moved to a top-level dir) has already landed — find it by the
   `name: 'claude-cli'` ServiceDefinition, don't assume the path.
2. Fix the service `description`/comment so it no longer claims rate-gating — it's a free,
   sequential, single-in-flight CLI; describe it honestly (e.g. "Claude Code CLI (`claude
   -p`). Free under the plan; sequential use, no rate cap.").
3. Confirm usage is still metered without the cap — `callService`'s no-rate-limit path still
   calls `recordServiceCall(name)`, so the Services dashboard keeps showing claude-cli call
   counts (no code change needed there; just verify). Confirm nothing tests/asserts the
   `ratePerMinute: 20` value (nothing does today).

## Done when

The `claude-cli` service no longer declares a per-minute rate limit; its description no
longer claims rate-gating; claude calls still record usage (Services page still shows
call counts) and the service remains editable via the dashboard if a limit is ever wanted
later. `npx tsc --noEmit` and `npm test` are green, and the daemon loads cleanly after a
restart.
