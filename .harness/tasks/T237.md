## Do

When a **needs-human** task is marked done (the "Mark done" button, T208), make it move into the
**done** section of the Backlog view. Today it stays in the "Needs you"/needs-human grouping because
the page sections by `status`/`gate` and a human-done task keeps `status: pending` + `gate:
needs-human` (TASKS.json is intentionally not modified — only the `.harness/human-done.json` overlay
flag is, surfaced as `done=true` by `GET /api/backlog`). Fix the section logic in
`dashboard/app/backlog/page.tsx` so a task with the overlaid `done=true` renders in the done section
regardless of gate, and ensure the list re-sections immediately after the action (no manual refresh).

## Done when

- Marking a needs-human task done moves it into the done section right away (overlay `done=true`
  honoured by the section logic).
- It no longer lingers in the needs-human section; `npm --prefix dashboard run build` + mobile-check green.
