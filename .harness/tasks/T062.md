## Do

Add a workflow that runs ONCE PER DAY and inspects the new logs produced by other workflows runs since the last inspection (run_logs / workflow framework logs on the box), evaluates any errors for FRAMEWORK-LEVEL problems (not job-specific bad-data), and — where a fix is safe — proposes and applies it. Envisioned as a one-step workflow whose single job instructs Claude (headless) to read the recent logs and propose fixes. NEEDS-HUMAN: discuss the design first — how Claude runs autonomously and safely APPLIES/commits framework fixes (vs just proposing), how to distinguish framework-level vs job-level errors, how it interacts with the build loop / CI / the always-push rule, guardrails against runaway or unreviewed changes, and how findings are surfaced (dashboard / notification / a drafted task in the backlog).

## Done when

Design agreed with the human; then a daily workflow reviews new run logs, identifies framework-level errors, and proposes (and where safe, applies) fixes within the agreed guardrails.
