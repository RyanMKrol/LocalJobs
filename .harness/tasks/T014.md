## Do

Build a framework feature to prune work_items whose item_key is no longer in a job's current input (the case that orphaned the 3 old-id perfume rows after their ids were corrected). A job exposes its current input key-set; a MANUAL-triggered prune (API/CLI, never automatic) removes work_items not in that set and surfaces what it removed. The user clears the 3 orphaned perfume rows via this feature.

## Done when

A manual prune removes work_items whose item_key is absent from the job's current input set and keeps current ones; it is never triggered automatically; the removal is surfaced (API/log); unit tests cover keeps-current + removes-orphaned.
