## Do

Niche houses have low vote counts = low-confidence community data. In the build step, down-weight low-sample Fragrantica signal against the LLM's own web research using a CONTINUOUS confidence weight (e.g. votes/(votes+k)). ALSO compute the ecosystem-wide vote/weight distribution across the scraped Fragrantica corpus to calibrate what counts as high vs low weight, and feed that distribution in as an input. Make the weighting explicit in the output. Verify against cached fixtures, no live calls.

## Done when

Build blends Fragrantica vs LLM via a continuous sample-size confidence weight calibrated against the corpus-wide distribution; the chosen weighting is explicit in the built profile; unit tests cover a low-sample and a high-sample fixture and assert the relative weighting.
