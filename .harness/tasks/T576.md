## Overview
listening-digest's runListeningDigest inlines two nearly identical ~60-line passes (a 1-month and a trailing-3-month digest) that differ only in period/key/label. Collapse them into one shared runPass() helper called twice, with zero behaviour change.

## Do
In src/workflows/listening-digest/stages/listening-digest.ts, extract the duplicated per-pass body (fetch top albums -> fetch top tracks -> filterRealAlbums -> renderDigestMarkdown -> write <outDir>/listening-digest-<key>.md -> markWorkItem) into a single helper, e.g. runPass(ctx, { period, key, label, outDir, username, fetchTopAlbums, fetchTopTracks, now }), and call it twice from runListeningDigest: once for the 1-month pass ({ period: config.period, key: monthKey(now), label: monthLabel(now) }) and once for the trailing pass ({ period: config.trailingPeriod, key: `${key}-3month`, label: `${label} (Trailing 3 Months)` }). Behaviour MUST be byte-preserving: identical output filenames, identical ledger keys (<YYYY-MM> and <YYYY-MM>-3month under job 'lastfm-digest'), identical detail shapes, identical callService('lastfm', ...) cacheKeys per period, equivalent log-line content, and the same ctx.progress calls (50 between passes, 100 after both). Leave monthKey/monthLabel/toArray/filterRealAlbums/renderDigestMarkdown and the fetcher factories unchanged. This pairs with idea B28's month-label fix but must land independently — do NOT depend on B28.

## Done when
runListeningDigest calls a single shared runPass() helper twice instead of inlining two near-identical bodies, and the two passes produce byte-identical output (filenames, ledger keys, cacheKeys, detail shapes, progress steps) to the pre-refactor code. Existing listening-digest.test.ts passes unchanged. npx tsc --noEmit and npm test pass.
