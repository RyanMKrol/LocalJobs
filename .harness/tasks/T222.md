## Do

Disable zoom/pan on the workflow graph so it's a static, read-only diagram instead of an interactive
canvas. In `dashboard/app/components/DagFlow.tsx` turn off React Flow's `zoomOnScroll`,
`zoomOnPinch`, `zoomOnDoubleClick`, `panOnScroll`, and `panOnDrag`, and remove/hide the `<Controls>`
(+/−/fit) widget. KEEP `fitView` so the whole DAG is sized to the container (container height already
scales with the graph). Node click-through (links) must still work — only the canvas zoom/pan is
disabled, not node interaction. Applies to both the definition and run-scoped views.

## Done when

- The graph no longer zooms (scroll/pinch/double-click) or pans (drag/scroll); the zoom Controls are gone.
- The full DAG is still visible (fitView) and nodes + gate marks remain clickable.
- `npm --prefix dashboard run build`, `npx tsc --noEmit`, and `node dashboard/scripts/mobile-check.mjs` green.
