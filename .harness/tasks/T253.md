## Do

Build a new scheduled **workouts-sync** workflow in local-jobs that ingests Hevy workouts into the
SAME DynamoDB tables the website already uses (take over the WRITER; reuse the existing schema, zero
website read-change). Create `src/jobs/workouts-sync/` as a **tracked** workflow (published like the
`places`/`perfumes` examples — the code has no secrets; only `.env` creds and the job's `data/` stay
gitignored, the latter already via `src/jobs/**/data/`). The workflow pulls Hevy on a cadence
(paginate `https://api.hevyapp.com/v1/workouts?page=&pageSize=10` with `HEVY_API_KEY`) and `Put`s each
workout + its exercises into `WORKOUTS_TABLE` / `EXERCISES_TABLE` via the shared `dynamodb` service
(T252). Idempotent via the `work_items` ledger keyed by Hevy workout id (append / re-scan domain, like
missing-tv-seasons — skip already-synced ids). Add a `hevy` rate-limit/quota service in
`src/services/hevy.service.ts` (tracked, generic, reads `HEVY_API_KEY` from env); conservative,
friendly limits (nothing time-sensitive). Reference shapes: website `src/lib/workoutBackfill.js`,
`src/lib/constants.js`, `src/pages/api/workouts/backfill.js`.

**Scope guard:** local-jobs ONLY. Do NOT touch any website code — the website keeps its own
`POST /api/workouts/backfill` and keeps reading the tables (both writing the same tables is fine:
idempotent `Put`s keyed by workout id). Retiring the website backfill is a separate, later idea.

## Done when

- `src/jobs/workouts-sync/` exists as a tracked scheduled workflow (manifest + stage(s)) that pulls
  Hevy and writes workouts+exercises to the existing Dynamo tables via the `dynamodb` service,
  idempotent via `work_items` keyed by Hevy workout id; a tracked `src/services/hevy.service.ts`
  rate-limits Hevy; `HEVY_API_KEY` documented as a placeholder in `.env.example`; the job's `data/`
  is gitignored (no secrets committed).
- Verification is **hermetic**: unit tests mock the `dynamodb` service (and the Hevy fetch) and use
  the scratch DB — they make NO live AWS writes and do NOT mutate the real Dynamo tables.
  `expectsTest`.
- `npx tsc --noEmit` and `npm test` are green; CI is green. Docs (CLAUDE.md/README job list) updated
  to mention the workflow.
