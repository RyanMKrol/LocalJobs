## Do

On the Services page the 'Edit limits' text boxes (services/page.tsx) use basic styling the user dislikes. Restyle them to match the workflow-run limit input — the nicer text box on the workflow detail Run UI (workflows/[name]/page.tsx, polished by T100), which the user likes. Make the three Services edit inputs (rate/min, rate/day, rate/month) use that same look; ideally EXTRACT A SHARED input class/component used by both the run-limit input and the Services inputs so they stay consistent going forward. Keep all existing behaviour: blank = null/no-limit, the non-negative-integer validation, the per-field ✕ clear buttons, and the 'no limit' wording (from T106). NOTE: harmonise with T106 (services wording) which also touches these inputs.

## Done when

The Services edit-limits inputs visually match the workflow-run limit input styling (ideally via a shared class/component); their behaviour is unchanged (blank = null, validation, ✕ clear buttons, 'no limit' wording); dashboard build passes and mobile-check stays green.
