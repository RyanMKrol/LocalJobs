## Do

Add a **duplicate-idea detection** pass to the `/convert-ideas` flow so the same (or near-same) idea
captured more than once in `.harness/IDEAS.md` is caught and reconciled before it becomes two backlog
tasks. In `.claude/commands/convert-ideas.md`, extend **Step 0 ("Read the inbox / build the
worklist")** with a de-dup step: before converting anything, scan the `## Inbox` bullets for ones that
are the **same idea or substantially overlap** — SEMANTIC similarity, not just exact-text match (the
same idea is often phrased differently) — and **surface the suspected duplicate group(s) to the owner**
(via `AskUserQuestion`) to merge or drop, rather than minting two tasks for one idea. Do NOT auto-merge
silently; the owner decides.

Be explicit that this is distinct from the existing "related ideas" handling: two RELATED-but-distinct
ideas (one a foundation the other builds on) become a `dependsOn` edge, NOT a merge — the de-dup pass
targets genuine DUPLICATES (the same idea appearing twice). (Worked precedent: in a prior sweep the
gate pair — the padlock-fix idea and the "are gates still shown?" idea — was hand-folded because they
overlapped; this makes that overlap check a STANDARD, explicit step instead of relying on the agent
happening to notice.) Also add a one-line note to `.harness/CLAUDE.md`'s "Ideas inbox & the two-step
flow" section that convert begins with a de-dup pass. Convert-side only (do not touch `/idea`).

## Done when

- `.claude/commands/convert-ideas.md` Step 0 includes a de-dup pass: scan the inbox for
  same/substantially-overlapping bullets (semantic), surface suspected duplicate groups to the owner
  to merge/drop before shaping, explicitly distinguished from the related-ideas → `dependsOn` handling
  (dupes are merged/dropped; related-but-distinct ideas are NOT).
- `.harness/CLAUDE.md`'s two-step-flow section notes that convert starts with this de-dup pass.
- No code change (command-doc + convention only); `/idea` (capture-side) is unchanged.
