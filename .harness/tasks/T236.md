## Do

Make the workflow **Run** buttons pop with a high-contrast, inviting accent so the primary action
doesn't blend into the theme background (in Sunny-8bit the bg + button are both yellowish; in
Pixel-picnic both beige). Use a contrasting accent — a blue like the "needs-human" pill on the Backlog
view is a good reference. Introduce/derive a primary-action accent var in `dashboard/app/globals.css`
(reuse the needs-human-pill colour or add a dedicated `--run` var) that works across all 3 theme
families × light/dark and doesn't alter the untouched `default` look. Run buttons appear on the
Workflows LIST (`dashboard/app/workflows/page.tsx`) and DETAIL (`dashboard/app/workflows/[name]/page.tsx`)
— keep them consistent, and preserve the disabled/"Running…" state (T105).

## Done when

- The Run buttons clearly stand out from the page background in every theme family × light/dark
  (accessible contrast), consistent on the list + detail pages, with the Running/disabled state intact.
- `npm --prefix dashboard run build` + `node dashboard/scripts/mobile-check.mjs` green.
