## Overview
Confirmed by grep and direct file read: neither src/workflows/missing-movies/stages/snapshot.ts nor src/workflows/missing-movies/stages/franchise-gaps.ts call markWorkItem anywhere — work_items has ZERO rows for job_name='plex-movie-snapshot' or 'franchise-gaps'. missing-tv-seasons' equivalent two stages (plex-tv-snapshot, tmdb-season-check) already do this correctly (630 real per-item rows each), and this is the pattern to mirror. This is a separate, standalone unit from the movie-recommendations/tv-recommendations fixes above — different workflow, no shared code path — though similar in spirit.

## Do
1) In src/workflows/missing-movies/stages/snapshot.ts's runSnapshot (the whole function currently has no markWorkItem import or call at all), add one markWorkItem('plex-movie-snapshot', <key>, 'success', { detail: {...} }) call per movie in the `movies` array (built at line ~34 via `buildMovieSnapshots(meta)`), mirroring missing-tv-seasons/stages/snapshot.ts:19-29's `recordSnapshotLedger` pattern: key by tmdbId if present else the Plex ratingKey, detail describing the movie (title, tmdbId, year — check src/workflows/movies/types.ts's PlexMovieMeta/movie-snapshot item shape, since missing-movies reuses movies/movies.ts's buildMovieSnapshots). Extract as its own exported, unit-testable function per the missing-tv-seasons convention. This stage explicitly RE-SCANS FRESH every run (its own doc comment: 'RE-SCANS FRESH every run (no skip-if-done) — the workflow's ledger lives only in the notify stage') — keep that; this is purely visibility rows, not a skip-if-done idempotency change. 2) In src/workflows/missing-movies/stages/franchise-gaps.ts's runFranchiseGaps, add markWorkItem calls covering what this stage checks/finds — this needs its OWN design call since franchise-gaps is collection-centric, not a flat per-item list like snapshot: the natural per-item ledger grain here mirrors season-check.ts's per-show success/failed pattern (season-check.ts:59-133, four markWorkItem call sites) but adapted to franchise-gaps' TWO-PASS collection structure (pass 1 resolves each owned movie's collection via /movie/{id}, pass 2 fetches each distinct collection's parts via /collection/{id} and finds gaps — franchise-gaps.ts:73-127). RECOMMENDED approach (confirm reasonable, don't over-think): key by collection id (one row per DISTINCT collection checked in pass 2, at the point `collectionGaps(detail, owned, now)` is computed, franchise-gaps.ts:110), detail describing that collection's outcome — collection name, how many gaps found, the gap titles (mirroring season-check's `completeMissingSeasons` list-in-detail pattern) — success whether or not gaps were found (a collection with zero gaps is still a legitimate checked outcome, same as season-check's 'checked successfully, no missing seasons' branch at season-check.ts:105-115). A collection fetch that errors (the catch block at franchise-gaps.ts:118-125, excluding the QuotaExceededError early-break at :119-122 which should NOT be marked failed since it's an intentional defer-to-next-run) should record a 'failed' row per root CLAUDE.md's item-loop-job convention. Do NOT key by individual gap (a collection with 3 gaps is one collection-level outcome, not 3 rows) — this is the 'needs a design call on the right key granularity' point the idea itself flagged; collection-id granularity is the recommended default matching season-check's per-show (not per-missing-season) granularity, but note this choice explicitly in the PR/worklog so it's easy to revisit.

## Done when
After a plex-movie-snapshot run over M movies, work_items has M rows for job_name='plex-movie-snapshot' (currently 0), keyed by tmdbId-or-ratingKey with per-movie detail. After a franchise-gaps run checking C distinct collections, work_items has C rows for job_name='franchise-gaps' (currently 0), keyed by collection id, each describing that collection's checked outcome (gaps found or none) — a collection whose fetch errored (non-quota) is marked failed. This resolves the Inputs & Outputs panel gap for missing-movies described in the idea: movie-gaps-notify's Inputs panel should now show real predecessor rows once franchise-gaps records them (no code change needed in movie-gaps-notify itself — verify this self-resolves once franchise-gaps' ledger is populated). Add/extend unit tests for both stages' new markWorkItem behavior, following missing-tv-seasons/stages/snapshot.test.ts and season-check.test.ts's existing test shapes as models.
