## Do

The ntfy notifications API keeps returning HTTP 429 because we hammered it (e.g. per-stage notifications during repeatUntilStable cycling) and we keep trying anyway. Add backoff to the ntfy sender (sendNtfy in src/core/notifier.ts) so that after a 429 we STOP attempting sends until a cooldown elapses. Use EXPONENTIAL backoff WITH A HARD CAP: on each consecutive 429 increase the cooldown (e.g. start ~30s, double each time, cap at ~10-15 min); HONOR ntfy's Retry-After response header as the cooldown when present (prefer it over the computed value); while within the cooldown window, SKIP the ntfy send entirely (don't even hit the API) and log that it's suppressed; RESET the backoff on the next successful send. Keep the backoff state in-memory in the long-lived daemon process (module-level in notifier.ts — resetting on restart is fine). This must NOT affect the macOS notification path (not rate-limited), and the notifier must still NEVER throw (a broken notifier must not affect job execution). Make the backoff knobs (base, cap) env-overridable via config.ts. Add unit tests: a 429 sets a cooldown, subsequent sends within the window are skipped (no fetch), Retry-After is honored, and a success resets the backoff. NOTE: complements T112 (which reduces per-stage notification volume during cycling) — together they stop the 429 storm.

## Done when

After the ntfy API returns 429, further ntfy sends are skipped until a cooldown elapses (exponential backoff with a hard cap, honoring Retry-After when present), and a successful send resets the backoff; the macOS notification path is unaffected and the notifier never throws; backoff base/cap are env-overridable; unit tests cover 429→cooldown→skip→reset (including Retry-After); npx tsc --noEmit and npm test are green.
