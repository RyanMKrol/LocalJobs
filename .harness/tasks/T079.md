## Do

Two chrome-profile folders exist on disk: the framework-level data/chrome-profile (the one ACTUALLY used — config.ts:19 chromeProfileDir -> browser.ts defaultChromeProfileDir -> perfumes/config.ts:15 profileDir -> fetch.ts:107) and an ORPHANED job-level one at src/jobs/perfumes/data/chrome-profile that NO code references. Keep a single framework-level shared profile: (1) delete the stale local folder src/jobs/perfumes/data/chrome-profile (LOCAL-ONLY cleanup — chrome-profile/ and data/ are gitignored and tripwired by the pre-push guard, so this produces NO tracked diff; never `git add` anything under data/ or chrome-profile/). (2) Because the deletion itself isn't CI-verifiable, the committable deliverable is a regression test locking in the invariant: add a unit test (extend perfumes/config.test.ts) asserting perfumesConfig.profileDir === defaultChromeProfileDir (i.e. the framework-level data/chrome-profile, env-overridable via LOCALJOBS_CHROME_PROFILE) so a job-level profile can't silently creep back in. Confirm grep shows no remaining reference to a job-level profile path.

## Done when

src/jobs/perfumes/data/chrome-profile no longer exists locally; the framework-level data/chrome-profile remains and is the only profile any code points at; a unit test asserts perfumesConfig.profileDir === defaultChromeProfileDir; `npx tsc --noEmit` and `npm test` are green. (No tracked diff is expected from the folder deletion itself — only the new test.)
