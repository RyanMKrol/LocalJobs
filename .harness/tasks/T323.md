## Do

Add a new top-level "Admin" nav tab and page to the dashboard, exposing the bulk output-delete capability built in the dependency task (`POST /api/workflows/reset-output-all`, consumed via the already-added `api.resetAllWorkflowsOutput()` client helper in `dashboard/app/lib/api.ts`).

1. **Nav tab.** In `dashboard/app/layout.tsx`, add an `<a href="/admin">Admin</a>` link to the `<nav>` block (currently Overview / Workflows / Services / Backlog / Logs, around lines 48-53) — append it as the last tab, after Logs.
2. **New page** at `dashboard/app/admin/page.tsx` (client component, matching the existing page conventions — poll/fetch via `app/lib/api.ts`, reuse existing shared components/classes from `dashboard/app/ui.tsx` / `dashboard/app/components/` per the "reuse, don't re-implement" convention, e.g. `<RunButton>`/`<Pill>` styling idioms and the existing `.btn-danger` / danger-zone panel styling pattern used on the per-workflow page).
3. **Bulk delete action — confirmation strength.** This is a SINGLE action that destroys output across EVERY workflow at once — strictly more dangerous than the existing per-workflow "Clear output data" button, which only uses a native `confirm()` dialog. A bare `confirm()` is judged too weak for a fleet-wide destructive action, so use a STRONGER confirmation: render a clearly-labelled danger panel explaining exactly what will happen (mirror the wording style of the existing per-workflow confirm message: what gets deleted — work item ledgers, run history/logs, `data/out/**` files for ALL workflows — and what's preserved — `data/raw/**`, settings, service limits/usage) with a text input where the owner must type an exact confirmation phrase (e.g. `DELETE ALL OUTPUT`) before the actual delete button becomes enabled/clickable. Do NOT rely on `confirm()` alone for this page.
4. **Calling the endpoint.** On confirmed click, call `api.resetAllWorkflowsOutput()`, show a loading/busy state on the button (disable it, label change, mirroring `resetOutput()`'s `resetting`/`resetResult`/`resetErr` state pattern on the per-workflow page), and on success render the per-workflow results returned (`results[]`) as a small table/list — which workflows were reset (with counts) vs skipped (with reason) — plus the aggregate totals. On error, show the error message inline (no silent failure).
5. **No other Admin capability for now** — this page currently has exactly one section/action (the bulk output delete). Keep the page minimal; don't scaffold placeholder sections for hypothetical future admin features.
6. **Update the living-artifact test harness** `dashboard/scripts/_dashboard-harness.mjs` in the SAME change (non-negotiable per repo convention): add an `{ name: 'admin', path: '/admin' }` entry to `PAGES`; add a synthetic `/api/workflows/reset-output-all` fixture response (a realistic `{ ok: true, totalWorkflows, resetCount, skippedCount, results: [...] }` payload covering both a reset and a skipped workflow, so the results table has something to render) to the fixture dispatcher (see how `/api/workflows` and other endpoints are stubbed nearby, e.g. around line 290); and add a `FLOWS` entry (e.g. `admin-confirm-typed`) that navigates to `/admin`, types the confirmation phrase into the input via Playwright's `page.fill(...)`, and screenshots the now-enabled delete button state — so the confirmation UI is actually visible in a captured screenshot, not just the pre-typed disabled state shown by the baseline `PAGES` shot.
7. Ensure the page and its confirmation panel are mobile-responsive per the repo's `@media (max-width: 640px)` convention in `dashboard/app/globals.css` — no horizontal overflow, `.panel:has(> table)` handles the results table scroll if needed.

## Done when

- `dashboard/app/layout.tsx`'s `<nav>` includes a new `Admin` tab linking to `/admin`, positioned after `Logs`.
- `dashboard/app/admin/page.tsx` exists, renders a single "delete all workflow output" danger section that requires typing an exact confirmation phrase into a text input before the destructive action button is enabled (a native `confirm()` alone is NOT sufficient per this task's decision) — verify by reading the component code that the button's `disabled` state is derived from comparing the typed input to the required phrase.
- Clicking the enabled button calls `api.resetAllWorkflowsOutput()` (from the dependency task) and renders the returned per-workflow results (reset vs skipped, with counts/reasons) plus aggregate totals; a failed call surfaces an inline error message.
- `dashboard/scripts/_dashboard-harness.mjs` has been updated in this SAME change: `PAGES` includes an `admin` entry for `/admin`, a synthetic fixture exists for `POST /api/workflows/reset-output-all` (or the GET-equivalent stub pattern used for other endpoints) covering both a reset and a skipped workflow result, and a new `FLOWS` entry drives typing the confirmation phrase and screenshots the enabled-button state.
- `npm --prefix dashboard run build` succeeds.
- Per the `layer:ui` floor (non-negotiable): after building, run `node dashboard/scripts/visual-check.mjs` and LOOK at the resulting screenshots in `dashboard/scripts/visual-out/` for BOTH the baseline `/admin` page (nav tab visible, danger panel with disabled button and empty confirmation input) AND the new `FLOWS` screenshot (confirmation phrase typed, button now enabled/highlighted as dangerous) — confirm with your own eyes that the Admin tab renders in the nav, the page is not blank/broken, and the confirmation flow is visibly distinct pre/post-typing. Record what you saw in the task's worklog entry.
- Also run `node dashboard/scripts/mobile-check.mjs` and confirm the new `/admin` page has no horizontal overflow at phone width.
- `npx tsc --noEmit` and `npm test` stay green (repo-wide Definition of Done).
