## Overview

Part of a repo-wide sweep giving every job a real paragraph-length `description` (surfaced on
`/jobs/[name]` and `/runs/[id]`) instead of a one-liner. This task covers the single job in the
`plex-space-saver` workflow. The mechanism was already built by the foundational task this depends on
— this is pure content authoring.

## Do

Rewrite the `description` of `src/workflows/plex-space-saver/stages/scan.job.ts`
(`plex-space-saver-scan`) into a real 3-6 sentence paragraph. Currently: "Scans the Plex movie + TV
library and writes a biggest-first disk-size breakdown (report only, no deletion suggestions)."
Expand it to cover: the per-title granularity (one row per movie, one row per TV show summing every
episode across every season), that size comes from the Plex API's `Media.Part.size` field (never a
filesystem walk), that it re-scans fresh every run (an audit, not a build) with weekly idempotency via
the work_items ledger, and that it is explicitly report-only — it never flags or suggests deletions.
Ground it in root `CLAUDE.md`'s `plex-space-saver` section and the job's own code. Plain prose only,
no markdown syntax.

## Done when

- The `plex-space-saver-scan` job has a real, accurate paragraph description grounded in its actual
  behavior — not a one-liner.
- `npx tsc --noEmit` and `npm test` are green.
