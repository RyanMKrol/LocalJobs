## Overview
The two recommender workflows write their recs-history rows with DIVERGED schemas: movies/stages/notify.ts's appendHistory pushes `{ tmdbId, title, year, at }` while tv-recs/stages/tv-recs-notify.ts's appendHistory pushes only `{ title, year }`. R01 requires aligning history on the richer movies shape so a single shared runRecsNotify can be extracted and so future id-based history dedup is possible. Do this alignment as behavior-preserving prep BEFORE the extraction.

## Do
In src/workflows/tv-recs/stages/tv-recs-notify.ts, change the `appendHistory` function so each pushed history row is `{ tmdbId: r.tmdbId, title: r.title, year: r.year, at: now.toISOString() }` — matching movies/stages/notify.ts's appendHistory exactly (movies pushes `{ tmdbId, title, year, at }`). Update the tv-recs RecsHistoryFile / recommended-row type in src/workflows/tv-recs/types.ts to carry the added `tmdbId` and `at` fields (make them present on new rows; keep the parse tolerant of OLD rows that lack them — the existing corrupt/legacy-tolerant try/catch parse stays, and reading a legacy row without tmdbId/at must not crash). Confirm nothing downstream in tv-recs relies on the old 2-field shape (the history file is fed back into branch prompts as 'already suggested' context and is bounded by the history-context/recent-window limits — those consume title/year, which are unchanged). Do NOT touch movies here (it already has the target shape) and do NOT extract the shared notify module in this unit. Update tv-recs/CLAUDE.md to document that recs-history rows now carry tmdbId + at (aligned with movies).

## Done when
tv-recs/stages/tv-recs-notify.ts appendHistory writes `{ tmdbId, title, year, at }` rows identical in shape to movies/stages/notify.ts; the tv-recs history type in types.ts includes tmdbId + at; parsing an existing legacy 2-field history file still works (no crash, treated as valid). A test in tv-recs-notify.test.ts asserts a newly appended history row includes tmdbId and at, AND that a pre-existing file of old {title,year}-only rows loads without error. movies is untouched. tv-recs/CLAUDE.md documents the aligned schema. Definition of Done: `npx tsc --noEmit` and `npm test` both green.
