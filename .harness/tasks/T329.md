## Do

The workflow-run detail page's `IoPanel` component (`dashboard/app/workflow-runs/[id]/page.tsx`, function starting ~line 243) renders a plain, non-sortable `<table>` of the run's Input → Output mapping. Its `<thead>` (lines ~343-350) has 2 columns when `singleStage` is true (`{inputHeader}`, `State`) or 4 when it's false (`{inputHeader}`, `State`, `{outputHeader}`, `Output state`) — all plain `<th>` cells, no click handler, no sort state. On a workflow with a huge fan-in/fan-out mismatch (e.g. `workouts-sync`'s `hevy-sync → workouts-progress`, 302 input rows but only ONE row ever carries real output, since `workouts-progress` is idempotent per calendar month), the one meaningful output row is buried in default order among hundreds of `—`/`—` rows. Add clickable, sortable column headers to `IoPanel`'s table so the owner can sort by any visible column and surface the rows that matter.

This codebase already has a WORKING sortable-header pattern to mirror: `dashboard/app/workflows/[name]/page.tsx` defines a `SortTh` component (~line 28) — a `<th className="sort-th...">` that takes `{ label, col, active, dir, onSort }`, shows a ▲/▼ arrow when active, and is styled via the existing `.sort-th` / `.sort-th-active` / `.sort-th:hover` rules already in `dashboard/app/globals.css` (~line 175) — driven by local `sortCol`/`sortDir` state + a `handleSort(col)` callback (toggles asc/desc on repeat-click of the same column, else defaults to desc on a new column) in both `MovieRecsManager` and `TvRecsManager`. This is the pattern to extend, NOT a new sort mechanism.

However, `SortTh` today is NOT exported/shared — it's a private helper in `workflows/[name]/page.tsx`, hardcoded to a `RecSortCol` union type (`'title' | 'year' | 'genre' | 'lens' | 'tmdb'`) that has nothing to do with IoPanel's columns (input key/name, input status, output key/name, output status). Per this repo's CLAUDE.md T268 convention ("Shared dashboard UI components — reuse, don't re-implement... When adding a new reusable element, add it to `dashboard/app/components/`"), the right move is to EXTRACT and GENERALIZE `SortTh` into a shared, generically-typed component at `dashboard/app/components/SortTh.tsx` (parameterize the column type, e.g. `SortTh<Col extends string>({ label, col, active, dir, onSort }: { label: string; col: Col; active: Col; dir: 'asc' | 'desc'; onSort: (c: Col) => void })`), then:
  1. Update `dashboard/app/workflows/[name]/page.tsx` to import the shared `SortTh` from `../../components/SortTh` instead of defining its own — remove the local definition, keep `sortRecs`/`RecSortCol`/`SortDir` local since those are specific to the rec managers (or keep `SortDir` shared too if it's generically reusable — your call, but don't leave a duplicate `SortTh`).
  2. In `IoPanel` (`dashboard/app/workflow-runs/[id]/page.tsx`), add local sort state (`sortCol`/`sortDir`, e.g. a union like `'input' | 'state' | 'output' | 'outputState'`) and a `handleSort` callback mirroring the toggle-asc-desc-on-repeat-click behaviour from `MovieRecsManager`/`TvRecsManager`.
  3. Write a small local sort helper (analogous to `sortRecs`) that sorts the `filtered` array (the array AFTER the existing state-filter chips are applied — line ~278's `filtered`) by the chosen column: `input` → `row.inputKey` (or the displayed label if present) via `localeCompare`; `state` → `row.inputStatus` via `localeCompare` (or a sensible status ordering if you prefer, but plain string sort is fine and matches the existing `sortRecs` string-column precedent); `output` → `row.outputKey ?? ''` via `localeCompare`; `outputState` → `row.outputStatus ?? ''` via `localeCompare`. Sort AFTER filtering, so it composes correctly with the existing filter-chip state.
  4. Replace the 2 or 4 plain `<th>` cells (lines ~345-348) with `<SortTh>` instances wired to the new state — only render the Output/Output-state `SortTh`s when `!singleStage` (matching the existing conditional column rendering). Use the existing `inputHeader`/`outputHeader` computed labels as the `label` prop so the per-job-scoped header text (T314) is preserved.
  5. Reuse the existing `.sort-th`/`.sort-th-active` CSS as-is — do not invent new classes unless the generalized component genuinely needs one (if so, add it near the existing `.sort-th` rules in `globals.css`).

Pick a sensible default sort (e.g. unsorted / natural incoming order with no `SortTh` marked active until the owner clicks one — this differs from `MovieRecsManager`'s default-desc-by-tmdb since IoPanel has no obviously-dominant column; simplest is: no default sort column, first click on any header sorts by it descending, exactly like the existing `handleSort` semantics already establish for a fresh column). Document your choice in a short code comment.

Also update `dashboard/scripts/_dashboard-harness.mjs`'s `FLOWS` array (per this repo's living-artifact rule) with a NEW flow entry that clicks one of `IoPanel`'s sortable headers on the `/workflow-runs/1` route (the existing synthetic fixture already has enough varied rows — see the `io-panel-job-scoped` flow immediately above for the pattern to mirror, including the `:text-is()` note about chip text overlap) and waits for the resulting re-sorted `table th.sort-th-active` state, similar to the existing `movie-recs-sorted-by-lens` flow. This is REQUIRED, not optional — it's how `visual-check.mjs` will actually exercise + screenshot the new interactive state.

## Done when

- `npx tsc --noEmit` and `npm test` pass.
- `npm --prefix dashboard run build` succeeds (dashboard changed).
- `IoPanel`'s table headers are clickable and sortable: clicking a header sorts the CURRENTLY FILTERED rows by that column, toggles ascending/descending on repeat clicks of the same header, and shows a ▲/▼ indicator on the active column, matching the interaction semantics of the existing `MovieRecsManager`/`TvRecsManager` sort pattern.
- The sort composes correctly with the existing state-filter chips (sorting operates on the filtered subset, not the full unfiltered list) and with the existing per-job filter pills (T313/T314) — switching jobs or filters doesn't leave the UI in a broken/stale sort state.
- The sortable-header visual affordance (`.sort-th`/`.sort-th-active` classes, ▲/▼ arrow) is reused from the existing pattern, not reinvented — and the underlying `SortTh` component is now a SHARED component in `dashboard/app/components/SortTh.tsx`, imported by BOTH `IoPanel` and the pre-existing `MovieRecsManager`/`TvRecsManager` call sites (no duplicate `SortTh` definitions left in the codebase).
- `dashboard/scripts/_dashboard-harness.mjs`'s `FLOWS` array has a new entry exercising the IoPanel header-click interaction (per the living-artifact rule — a UI-surface change without a matching FLOWS/PAGES update is not done).
- Run `node dashboard/scripts/mobile-check.mjs` and keep it green (no new horizontal overflow from the sortable headers at 402px).
- Run `node dashboard/scripts/visual-check.mjs` (after building) and LOOK at the resulting screenshots in `dashboard/scripts/visual-out/` — confirm the new sort indicator/arrow actually renders (painted, not just present in the DOM) on both the baseline `workflow-run` page and the new sort-click FLOWS screenshot, and that nothing overlaps/clips.
- No regression to the existing `io-panel-job-scoped` flow (T314's per-job filter pills) — it should still work exactly as before alongside the new sorting.
