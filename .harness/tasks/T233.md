## Do

Split the buildable (non-done, non-gated) tasks on the Backlog view into TWO sections: **Ready** (all
`dependsOn` satisfied) and **Waiting on dependencies** (≥1 unfinished dependency). In the Waiting
section, each task shows a small **pill** naming the unmet dependency id(s) it's blocked on (e.g.
"needs: T214"). Mirrors the loop's own ready/waiting distinction (STATUS.md). The data is already on
each task from `GET /api/backlog` (`dependsOn`, `gate`, `status`); rendered in
`dashboard/app/backlog/page.tsx`. Keep `needs-human`/gated tasks in their existing "Needs you" grouping
(don't fold them into Ready/Waiting).

## Done when

- Buildable tasks are split into "Ready" (deps all done) and "Waiting on dependencies" (≥1 dep not done).
- Each waiting task shows a pill listing its still-unfinished `dependsOn` ids.
- needs-human/gated tasks stay in their own section. `npm --prefix dashboard run build` + mobile-check green.
