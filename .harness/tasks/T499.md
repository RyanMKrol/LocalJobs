## Overview
Re-attempt of T474 (blocked). T474 was NOT blocked for a spec reason — its scope was already correctly widened (from T464's gap) to include all 7 code files, their 7 tests, the 6 workflow CLAUDE.md files, and the root CLAUDE.md. It failed at rung 0 / attempt 0 with a pre-push guard trip: 'sensitive path staged'. The builder staged a gitignored/sensitive path (almost certainly a src/workflows/**/data/ file, a scratch DB, or the result of a broad `git add -A`/`git add .`), the harness pre-push privacy guard HALTED before the run could commit, and NO code ever landed on main. The 5 read-heavy stage files still make bare plexGet<...>(...) calls and apply.ts still invokes bare plexPutStreams/triggerButlerBackup via its injected-default pattern; tmdb calls are already wrapped in callService('tmdb', ...) as the template. The plex service (src/services/plex.service.ts) already exists. This re-attempt is the SAME mechanical migration, with an explicit git-hygiene guardrail so the guard does not trip again.

## Do
Wrap every direct Plex call in callService('plex', () => <original call>). For the 6 read stages (plex-language-fix/lib.ts, tv-recs/stages/tv-snapshot.ts, missing-tv-seasons/stages/snapshot.ts, plex-profiles/stages/build.ts, movies/stages/snapshot.ts, plex-space-saver/stages/scan.ts): wrap each plexGet<...>(...) call, importing callService from ../../core/services.js or ../../../core/services.js at the matching relative depth — mirror the callService('tmdb', ...) wrapping already present in sibling stages (e.g. missing-tv-seasons/stages/season-check.ts, movies/stages/franchise-gaps.ts). For plex-language-fix/stages/apply.ts: the actual calls go through injected function defaults (`const putStreams = opts.putStreams ?? plexPutStreams; const triggerBackup = opts.triggerBackup ?? triggerButlerBackup`), so wrap at the CALL SITE where putStreams(...) / triggerBackup(...) are invoked (callService('plex', () => putStreams(...)) etc.) so a test-injected override still bypasses metering exactly as it does today — do NOT wrap the `?? plexPutStreams` default assignment, and do not change mutation semantics. Do not alter any call's arguments, return-value handling, or error handling beyond the wrap. Update each touched stage's test to account for the callService('plex', ...) wrapper, following the same file's existing callService('tmdb', ...) test handling as the template (in tests where 'plex' is not a registered service, callService's no-limit branch just passes the call through — mirror how the tmdb tests handle that). Confirm via grep that src/core/plex-client.ts's own plexGet/plexPutStreams/triggerButlerBackup IMPLEMENTATIONS are unchanged — only call sites move. THEN in the SAME commit, update the 6 touched workflows' own CLAUDE.md (one line each: Plex connectivity for that workflow is now metered via the shared plex service) and add a one-line note to the root CLAUDE.md's src/core/plex-client.ts file-map entry (~line 296) that the plex service now sits in front of plexGet/plexPutStreams/triggerButlerBackup calls. GIT HYGIENE (the actual root cause): stage ONLY the explicitly-scoped files by name — NEVER `git add -A` or `git add .`; never stage anything under a data/ folder, a chrome-profile/, .env*, or a scratch DB. This is why the prior attempt was blocked.

## Done when
A grep for plexGet(, plexPutStreams(, or triggerButlerBackup( (allowing a `<` type-parameter, e.g. plexGet<...>() ) that is NOT immediately inside a callService('plex', ...) wrapper returns nothing in the real src/ tree, excluding src/core/plex-client.ts itself (where the implementations live) and any src/workflows/*/data/ clone directories (per the data/-scanning gotcha); every one of the 7 scoped code files routes its Plex call(s) through callService('plex', ...) with no other behavior change; src/core/plex-client.ts is byte-unchanged; npx tsc --noEmit and npm test are both green including the updated per-stage tests; and the 6 touched workflow CLAUDE.md files plus the root CLAUDE.md file-map entry are updated and committed in the SAME commit as the code. Only the explicitly-scoped files are staged — nothing under data/, no scratch DB, no .env. (Live end-to-end confirmation of a real plex row on the Integrations page is a manual owner follow-up post-merge, not required here, since the sandbox has no LAN access to a Plex server.)
