## Overview
Once Unit A (perfumes-fields-a) plumbs the 8 owner-authored fields onto PerfumeInput, this task actually gets them into the generated profile markdown — wiring the already-existing-but-blank `rating` frontmatter key for real, adding 5 new frontmatter keys for the fields that don't already have a home, and filling the already-blank Personal Notes / Application sections.

## Do
Follow the DESIGN confirmed in the pending-questions DoD answer for this cluster (perfumes-fields) — restated here as the default if unanswered: keep these 8 fields completely SEPARATE from the existing Fragrantica-vs-LLM confidence-blended fields (do not touch confidenceClause/confidenceWeight/voteDistribution or the `season`/`longevity`/`sillage` keys they populate — those stay exactly as they are today). Concretely: (1) in profile.template.md, add 5 new frontmatter keys under a clearly-commented 'Personal (from your own PerfumeRatings row)' block: `date_added` (string, `DD-MM-YYYY`, null if unknown), `ownership` (`Sample` | `Travel size` | `Full bottle`, null if unknown), `personal_longevity` (0-8 int, null if unknown), `personal_projection` (1-4 int, null if unknown), `personal_seasons` (array, `[]` if unknown) — placed near the existing `rating`/`status` personal block. (2) Update the existing `rating: null # your own score, 1-10` key's comment/instruction so it's actually populated from the real value (see prompt change below) instead of hardcoded null; leave `status` untouched (still owned/decant/sample/wishlist, still defaulting to "owned"). (3) Update the `## Personal Notes` section's placeholder comment to note it gets filled from the owner's own description when present. (4) Update the `## Application` section's placeholder comment similarly for applicationSpots. (5) In stages/build.ts, extract a new exported helper `personalFieldsClause(p: PerfumeInput): string` (mirroring the existing `notesMappingClause`/`confidenceClause` pattern — same file, same export style, same kind of unit test coverage) that returns a prompt clause listing each of the 8 fields' ACTUAL values (when present on `p`) with an explicit 'copy these EXACT values verbatim into the corresponding frontmatter key/section — do not alter, reinterpret, or invent additional values' instruction, and explicit per-field fallback instructions for when a field is ABSENT (write `null`/`[]`/leave the section's placeholder-style 'not recorded yet' text, exactly as the template's other honest-gap conventions already do — see how `notesMappingClause` handles an empty notes pyramid for the established tone). Wire `personalFieldsClause(p)` into `buildPrompt`'s prompt array (call it where the existing hardcoded 'Leave personal scaffolding blank: rating: null, status: "owned", and empty Personal Notes / Application' line is, replacing that line's rating-related instruction — keep the `status: "owned"` and blank-Personal/Application defaults ONLY as what the NEW clause falls back to when the corresponding Dynamo field is absent, not as a blanket instruction anymore). (6) Add `build.test.ts` coverage for `personalFieldsClause`: a fully-populated `PerfumeInput` fixture asserts every one of the 8 values appears verbatim in the returned clause text; a fixture with none of the 8 fields set asserts the clause instructs null/`[]`/'not recorded' fallbacks and does NOT contain any fabricated-looking placeholder value. (7) Update src/workflows/perfumes/CLAUDE.md's `perfumes-build` bullet to describe the new personal-fields pass-through (per this repo's Definition-of-Done rule that docs are updated in the same change as the code).

## Done when
`npm test` is green including new build.test.ts assertions that `personalFieldsClause` (a) embeds every populated field's real value verbatim and instructs verbatim/no-invention, and (b) falls back to null/[]/'not recorded' wording when a field is absent, without ever fabricating a value. `npx tsc --noEmit` is clean. profile.template.md's frontmatter contains the 5 new keys (`date_added`, `ownership`, `personal_longevity`, `personal_projection`, `personal_seasons`) plus the existing `rating` key now reachable from real data, and the `## Personal Notes`/`## Application` placeholder comments describe the new pass-through. Manually confirm end-to-end by running the perfumes-build stage in dry-run mode (`PERFUMES_DRY_RUN=1`) or, if verifying against a real Claude call, a single real perfume with the smallest possible sample per this repo's harness verification rule, and read the resulting `data/out/markdown/<id>.md` to confirm the new fields/sections render as expected for both a perfume with all 8 fields present and one with none of them — this is a content-correctness read, not a dashboard screenshot (the generated markdown isn't part of the dashboard's own rendered UI surface, so the project's visual-check.mjs screenshot tooling doesn't apply here). src/workflows/perfumes/CLAUDE.md's perfumes-build description mentions the new personal-fields pass-through.
