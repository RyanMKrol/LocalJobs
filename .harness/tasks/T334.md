## Do

Context: T319 (`.harness/tasks/T319.md`) added the current 7-heading `project.template.md` (`## What It Is`, `## Tech Stack`, `## Status`, `## Structure`, `## Themes & Interests`, `## Notable Technical Approaches`, `## Sources`) and T320 gave `project-summarize` real read-only filesystem tool-access to the cloned repo via `runClaudeWithRepoAccess` (`src/jobs/projects-sync/claude-repo.ts`), replacing the old ad-hoc 3-line output the owner's original screenshot showed. Both of those are already `status: done` and shipped — this task is NOT about re-adding headers or repo access, those exist. The gap: neither T319 nor T320 told Claude to write SUBSTANTIVE content per section. `project.template.md`'s section placeholders read like single-sentence prompts (e.g. `## What It Is` says just "A short paragraph describing the project: what it does and why it exists."), and `buildSummaryPrompt` in `src/jobs/projects-sync/stages/project-summarize.ts` (~line 98-131) never asks for length/depth at all — so even with 7 headings and real repo exploration, a terse one-sentence-per-section response is a fully valid response Claude could return, wasting the fact that a whole agent is reading through the actual repository. Fix the DEPTH problem, not the structure problem:

1. In `src/jobs/projects-sync/project.template.md`, reword each section's placeholder guidance to explicitly call for multi-sentence/multi-paragraph substantive prose drawing on the ACTUAL repo exploration (package.json, source layout, README, other docs), not a one-line restatement of the catalog metadata. Keep the exact `##` heading text and frontmatter keys unchanged (still validated by `templateShapeViolations` in `project-summarize.ts`) — only the body guidance text changes.
2. In `buildSummaryPrompt`, add an explicit instruction on depth, e.g. something like: "Each section should be a couple of substantive paragraphs of real prose grounded in what you actually found exploring the repo (specific files, patterns, dependencies, structure) — not a single generic sentence. If a section genuinely has little to say for a small/simple/dormant project, say so honestly and briefly rather than padding, but do not default to brevity when there is real substance to draw from your exploration." This must NOT create pressure to invent facts — keep the existing 'NEVER invent facts you cannot support' instruction intact alongside the new depth instruction.
3. Do not touch `runClaudeWithRepoAccess`, the CLI invocation shape, the allowed-tools list, or the `claude-cli` service quota wiring from T320 — this task only changes prompt/template TEXT.
4. Update `project-summarize.test.ts` only if the existing tests assert exact template/prompt string content that this change would break (adjust assertions to match, don't remove coverage). No new test is required purely for a prompt-wording change (there is no deterministic way to unit-test 'the LLM wrote enough paragraphs'), but if you add any new validation logic (see Done-when) it must be covered.
5. Update root `CLAUDE.md`'s `projects-sync` description if it summarizes what `project-summarize`'s output looks like, to reflect the more substantive per-section depth expectation.

## Done when

- `project.template.md`'s per-section guidance text explicitly asks for multi-sentence/paragraph substantive content grounded in real repo exploration, not one-liners, while keeping the same 7 `##` headings and frontmatter keys unchanged.
- `buildSummaryPrompt`'s prompt text includes an explicit instruction that sections should contain real, substantive prose (a couple of paragraphs where the project supports it) rather than single-sentence summaries, while preserving the existing 'never invent facts' honesty instruction.
- `runClaudeWithRepoAccess`'s invocation shape (cwd, `--add-dir`, `--allowedTools`), the `claude-cli` service routing, and the post-generation `templateShapeViolations` heading/frontmatter check are all UNCHANGED by this task.
- Regenerate (or manually draft, if a real Claude CLI call is judged too costly to justify for verification alone — but prefer an actual small-sample re-run of `project-summarize` limited to 1-2 repos to observe real output) at least one sample project markdown file under the new prompt and confirm each section now contains multiple sentences of real, non-generic content — record the before/after in the worklog.
- `npx tsc --noEmit` and `npm test` pass.
- Root `CLAUDE.md`'s `projects-sync` description (if it characterizes output verbosity) is updated to match.
