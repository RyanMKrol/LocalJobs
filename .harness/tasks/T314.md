## Do

Context: this task depends on a prior backend task that added an optional `?job=<jobName>` query param to `GET /api/workflow-runs/:id/io` (`src/api/server.ts`). When `job` is given, the response's `io` rows are re-scoped to that ONE stage's own input→output pairing (input = that job's direct predecessor(s)' ledger row(s), output = that job's OWN `work_items` ledger row) instead of the whole-run first-wave→last-wave collapse — and the response gains three fields: `selectedJob: string | null`, `scopedProducerJobs: string[]`, `scopedConsumerJobs: string[]` (all neutral/empty when no `job` param is given). This task wires that up in the dashboard: a NEW row of per-stage filter pills, one per workflow member job, that — when clicked — REPLACES the currently-displayed Input/Output columns with that specific stage's own pairing (this is a genuine data re-fetch via the new query param, NOT a client-side re-filter of already-loaded rows, because the existing rows never contained per-intermediate-stage data in the first place). This pill row sits ABOVE the existing state-filter pill row (`all/success/failed/skipped`) as an INDEPENDENT second filter dimension: selecting a job pill changes WHICH DATASET is loaded (server-side); the state pills continue to filter WITHIN whatever dataset is currently loaded (client-side, unchanged mechanics).

Anchor: `IoPanel` in `dashboard/app/workflow-runs/[id]/page.tsx` (currently `function IoPanel({ runId, data }: { runId: string; data: WorkflowIo })`, ~line 243). The parent `WorkflowRunDetail` component currently owns the IO poll itself: `const { data: ioData } = usePoll(() => api.workflowRunIo(id), 5000, [id]);` (~line 372) and passes `ioData` down as `data`; it also already fetches the full workflow definition via `const { data: pdata } = usePoll(() => api.workflow(run?.workflow_name ?? '__none__'), 5000, [run?.workflow_name]); const workflow = pdata?.workflow;` (~line 380-385) — `workflow.jobs` is a `WorkflowMember[]` (`{ job_name: string; depends_on: string[] }[]`, see `dashboard/app/lib/api.ts` ~line 44) giving the ordered list of every member job, which is exactly what's needed to render one pill per stage.

Do:
1. In `dashboard/app/lib/api.ts`: extend the `WorkflowIo` interface (~line 194) with the three new optional fields matching the backend exactly: `selectedJob: string | null; scopedProducerJobs: string[]; scopedConsumerJobs: string[];`. Extend the `api.workflowRunIo` client helper (~line 481, currently `(id: string) => get<WorkflowIo>(\`/api/workflow-runs/${id}/io\`)`) to accept an optional second `job?: string` param and append `?job=${encodeURIComponent(job)}` to the URL when given.
2. Move ownership of the IO poll INTO `IoPanel` itself (rather than the parent), so job-pill selection can drive its own fetch: change `IoPanel`'s signature to accept `{ runId, members }: { runId: string; members: WorkflowMember[] }` (import `WorkflowMember` from `../../lib/api`), add local state `const [selectedJob, setSelectedJob] = useState<string | null>(null);` (null = the existing whole-run default view), and internally do `const { data } = usePoll(() => api.workflowRunIo(runId, selectedJob ?? undefined), 5000, [runId, selectedJob]);`. Update the parent `WorkflowRunDetail` to drop its own `ioData` poll and instead render `{workflow && <IoPanel runId={id} members={workflow.jobs} />}` (gated on `workflow` being loaded, same gating style already used for the `DagFlow` panel above it). Handle `data` possibly being `undefined` while loading (the panel already returns `null` early when there's nothing to show — extend that guard).
3. Generalize the existing `singleStage` derivation (~line 260, currently `firstWave.length > 0 && firstWave[0] === lastWave?.[0]`) so it reflects whichever dataset is currently loaded: when `selectedJob` is set, `singleStage = data.scopedProducerJobs?.length === 1 && data.scopedProducerJobs[0] === data.scopedConsumerJobs?.[0]`; when `selectedJob` is null, keep the existing whole-run `firstWave[0] === lastWave?.[0]` check. This makes the panel correctly collapse to the single-column view when the selected job is a root/first-wave stage (no predecessor), reusing the SAME rendering path the whole-run single-stage case already uses — do not build a second rendering path.
4. Render a NEW pill row ABOVE the existing `.io-filter-bar` (state pills), only when `members.length > 1` (a genuinely single-stage workflow has nothing to pick between): an 'All stages' pill (active when `selectedJob === null`, restores the whole-run first→last view) followed by one pill per `members[i].job_name` in the array's given order (this is the same order the page's Member runs table and DagFlow already display). Give this row its own CSS class in `dashboard/app/globals.css` (e.g. `.io-job-filter-bar` / `.io-job-filter-chip`, mirroring the existing `.io-filter-bar`/`.io-filter-chip` structure) so it reads as a visually DISTINCT, second filter dimension, not merged into the state-pill row. Clicking a job pill calls `setSelectedJob(jobName)` (or `null` for 'All stages') AND resets the existing local state-filter (`setFilter('all')`) since the state counts belong to whichever dataset just got (re)loaded.
5. When `selectedJob` is set and the view is NOT single-stage, relabel the existing 'Input'/'Output' column headers to name the actual jobs, e.g. `Input (${data.scopedProducerJobs.join(', ')})` / `Output (${selectedJob})`, so it's unambiguous which stage's own pairing is being shown. In the default ('All stages') view, leave the headers exactly as they are today ('Input'/'Output').
6. Update `dashboard/scripts/_dashboard-harness.mjs` (the LIVING ARTIFACT the whole dashboard test suite depends on — see its own header comment and `.harness/CLAUDE.md`'s UI-task rules): the fake `/api/**` router currently returns the same `workflowIo` fixture for any `/workflow-runs/<id>/io` request regardless of query string (~line 254, `if (pathname.endsWith('/io') && pathname.startsWith('/api/workflow-runs/')) return workflowIo;`). Extend it to inspect the `job` search param (the router already has access to the parsed request URL — check how the nearby `/output` route or other query-param-aware fixtures in this file read it) and, when `job=places-enrich` is requested, return a DIFFERENT fixture (`workflowIoScopedToEnrich` or similar) with `selectedJob: 'places-enrich'`, `scopedProducerJobs: ['places-resolve']`, `scopedConsumerJobs: ['places-enrich']`, and 2-3 `io` rows whose `outputJob`/`outputStatus`/`outputDetail` reflect `places-enrich`'s OWN ledger (distinct from the existing `workflowIo` fixture's `places-enrich-with-llm` output data), so a screenshot of this state visibly differs from the default view. Add a new entry to the `FLOWS` array (see its header comment ~line 294 for the exact shape: `{ name, path, waitFor?, actions(page) }`) named something like `io-panel-job-scoped`, `path: '/workflow-runs/1'`, with `actions` that clicks the new 'places-enrich' job pill (e.g. `await page.click('.io-job-filter-chip:has-text("places-enrich")')` — adjust the selector to whatever you actually render) and waits briefly for the re-fetch to settle before the screenshot is taken.

Do not touch anything outside the four files listed in scope. The backend `?job=` param and its three response fields already exist from the prior task — do not re-implement them in `src/`.

## Done when

- `npx tsc --noEmit` passes.
- `npm --prefix dashboard run build` succeeds.
- `node dashboard/scripts/mobile-check.mjs` passes (no horizontal overflow at phone width for the new pill row, including on a workflow with several member jobs).
- `node dashboard/scripts/visual-check.mjs` passes AND you have LOOKED at the resulting screenshots in `dashboard/scripts/visual-out/` (gitignored) — specifically the baseline `workflow-run` screenshot (confirms the new per-stage pill row renders above the existing state-filter pill row, visually distinct, on a multi-stage workflow) AND the new `FLOWS` entry's screenshot added in this task (confirms clicking a job pill actually swaps the Input/Output columns to that stage's own data with relabeled headers) — record in the worklog what you saw, per `.harness/CLAUDE.md`'s UI-task visual-confirmation rule.
- `dashboard/scripts/_dashboard-harness.mjs` has a NEW `FLOWS` entry capturing the job-scoped filter state (non-negotiable per the living-artifact rule for any new interactive UI state), and its fixtures return genuinely different `io` data for a scoped `?job=` request vs. the default request.
- On a 2-stage workflow (e.g. `stocks-sync`: `stocks-snapshot` → `stocks-watch`) the job-pill row still renders (per-stage inspection is useful even at 2 stages, per the idea's own screenshot); on a genuinely single-stage workflow (1 member job) the job-pill row does not render at all.
- Selecting a job pill resets the existing state-filter pills back to 'all' and the state-filter row continues to work correctly (client-side, unchanged) against whichever dataset is currently loaded.
- Selecting a root/first-wave job pill (no predecessor) collapses to the existing single-column rendering (reusing the current single-stage code path), not a broken half-populated table.
