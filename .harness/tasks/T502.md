## Overview
Cache `trading212-instruments` metadata lookups (a stable instrument-metadata mapping) for 22 hours.
Outcome of the T479 caching-adoption decision; the caching mechanism already exists (T451/T476).

## Do
1. Set `cacheTtlMs: 79_200_000` (22 hours) on the `trading212-instruments` `ServiceDefinition` in
   `src/services/trading212-instruments.service.ts`.
2. Add a stable `cacheKey` to BOTH `callService('trading212-instruments', ...)` read sites:
   - `src/workflows/stock-digest/stages/stock-portfolio-snapshot.ts`
   - `src/workflows/stocks-sync/stages/stocks-resolve-names.ts`
   Key it by the ISIN (or ticker) being resolved: `` `t212-instruments:${isinOrTicker}` ``.
3. Cache reads only.

## Done when
- `cacheTtlMs: 79_200_000` is set on the `trading212-instruments` service def.
- Both call sites above pass a stable `t212-instruments:<isin-or-ticker>` cacheKey.
- `npx tsc --noEmit` and `npm test` are green.
- Verified a repeated lookup of the same instrument within 22h is served from `service_cache`
  (hermetic scratch-DB check) rather than a second live call.
