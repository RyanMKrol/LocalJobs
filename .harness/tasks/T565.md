## Overview
interface PlexAllResponse<T> { MediaContainer?: { Metadata?: T[] } } is re-declared in at least 8 places (movies, tv-recs, missing-tv-seasons, missing-movies, plex-profiles, plex-space-saver snapshots/scans), and the TMDB-GUID extraction pattern (matching a title's `tmdb://` GUID off the `/all?includeGuids=1` metadata) is duplicated across movies, tv-recs (tv-shows.ts), missing-tv-seasons/plex.ts, and plex-language-fix/lib.ts. R07 proposes exporting PlexAllResponse<T>, extractTmdbId, and fetchSectionMetadata<T>(section, opts) from src/core/plex-client.ts (already the blessed shared Plex home). ⚠️ CO-ORDINATION: these plex-client additions OVERLAP the in-flight Plex-service migration (pending T511, T513-T517 moving Plex reads onto callService('plex')). This unit should land CONSISTENTLY with wherever that migration parks the shared Plex read helpers — sequence it after the migration (or fold into it) per the owner's answer to the pending question; do NOT hard-depend on the migration tasks here unless the owner confirms an ordering, but the spec must call the overlap out so the builder reconciles rather than conflicts.

## Do
Add to src/core/plex-client.ts (the shared Plex home): (1) export interface PlexAllResponse<T> { MediaContainer?: { Metadata?: T[] } } — the exact shape re-declared in the snapshot/scan stages; (2) export extractTmdbId(item): number | null (or string per existing convention) — the shared logic that reads a Plex metadata item's Guid array and returns the numeric id behind its `tmdb://` GUID, or null when absent (a title with no tmdb:// GUID must be handled exactly as today: excluded/flagged, never guessed); (3) export fetchSectionMetadata<T>(section, opts?) — a thin wrapper over the existing plexGet for the `/library/sections/<n>/all` (optionally `?includeGuids=1` / `?type=4`) pattern the copies share, returning the Metadata array. Then replace the per-workflow re-declarations and inline extraction with imports of these shared exports across: movies/stages/snapshot.ts + movies/movies.ts, tv-recs/stages/tv-snapshot.ts (+ tv-recs' tv-shows.ts GUID extraction), missing-tv-seasons/stages/snapshot.ts + missing-tv-seasons/plex.ts, missing-movies/stages/snapshot.ts, plex-profiles/stages/build.ts, plex-space-saver/stages/scan.ts, and plex-language-fix/lib.ts. ⚠️ RECONCILE WITH THE PLEX-SERVICE MIGRATION FIRST: pending tasks T511 + T513-T517 move each workflow's Plex reads onto callService('plex'). Before adding fetchSectionMetadata / migrating a call site here, check whether that workflow's migration task has already landed and where it parked the shared read helper; land these three exports CONSISTENTLY with that (e.g. if fetchSectionMetadata should itself route through callService('plex'), do so). If the migration has not landed for a given workflow, still add the pure PlexAllResponse<T> type + extractTmdbId (which are migration-neutral) and note in the worklog which call sites were left for the migration to convert. Do NOT change which titles are matched or how a missing GUID is handled — pure structural dedup. Add/extend src/core/plex-client.test.ts to cover extractTmdbId (a metadata item WITH a tmdb:// guid returns the id; one WITHOUT returns null) and the PlexAllResponse<T> typing round-trip. Update each touched workflow's CLAUDE.md note about the shared plex-client if it enumerates what the client provides, and the root CLAUDE.md's plex-client.ts file-map row. Owner decision (2026-07-12): DEFER these plex-client additions until AFTER the Plex-service migration lands (dependsOn T517, the migration capstone), so they are consistent with wherever that migration parks shared Plex read helpers.

## Done when
src/core/plex-client.ts exports PlexAllResponse<T>, extractTmdbId, and (consistent with the Plex-service migration) fetchSectionMetadata<T>; a repo-wide grep finds no remaining per-workflow `interface PlexAllResponse` re-declarations (all import the shared type) and the tmdb:// GUID extraction is defined once (extractTmdbId) rather than inlined per workflow. Every migrated call site matches the SAME titles / handles a missing tmdb:// GUID identically to before (no behavior change). The additions are landed consistently with the Plex-service migration (T511/T513-T517) — either routed through callService('plex') where that migration expects it, or left migration-neutral (type + extractTmdbId) with a worklog note for the call sites the migration still owns. src/core/plex-client.test.ts covers extractTmdbId (present vs absent GUID). Touched CLAUDE.mds + root file-map row updated. Definition of Done: `npx tsc --noEmit` and `npm test` both green.
