## Overview
The owner's screenshot showing a native confirm() popup on every single-item 'Ignore' click is MissingSeasonsManager's, not TvRecsManager's — confirmed the dialog text ('Ignore "The Mole (2022)" S2? ...') exactly matches page.tsx:574-576. The owner confirmed the scope is MissingSeasonsManager ONLY, and the confirm should be gone entirely (not replaced with a toast). Ignoring is already low-stakes (fully reversible any time via 'Un-ignore', per T145), so removing this friction here does not change the underlying safety semantics.

## Do
In dashboard/app/workflows/[name]/page.tsx, remove the `if (!confirm(...)) return;` guard from MissingSeasonsManager's single-item `ignore()` function ONLY (page.tsx:576) — so clicking a row's '✕ Ignore' button fires the API call immediately, with the existing busy/disabled + error-surfacing behavior unchanged (only the confirm gate is removed, nothing else about the function changes). Do NOT touch: MissingSeasonsManager's `unignore()` confirm() (page.tsx:591), its bulk 'Ignore all' confirm() (ignoreShow, page.tsx:606), or ANY other manager's single-item/bulk ignore/unignore confirm()s (TvRecsManager, MovieRecsManager, MovieGapsManager all keep their existing confirm() dialogs unchanged) — this task is scoped to MissingSeasonsManager's single-item Ignore action only.

## Done when
Clicking a single-item '✕ Ignore' button in MissingSeasonsManager ignores the item immediately with no browser confirm dialog. Every other confirm() in this file — MissingSeasonsManager's own Un-ignore and bulk Ignore/Un-ignore-all, and all of TvRecsManager/MovieRecsManager/MovieGapsManager's confirms — are unchanged. npx tsc --noEmit, npm test, and npm --prefix dashboard run build are green. node dashboard/scripts/visual-check.mjs runs clean (no dialog to screenshot, so this is mostly a regression check that nothing else broke).
