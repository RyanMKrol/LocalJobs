## Overview
Fixes `plex-language-fix`'s self-referential `inputKeys()` trap — the bug that originally triggered this whole investigation (a manually-limited run silently no-op'd right after "Clear output data" wiped its ledger). `discoverInputKeys()` currently reads back this job's OWN prior `work_items` success rows instead of querying Plex live. This task depends on T474 (migrates all 6 Plex-touching workflows' direct `plexGet` calls onto `callService('plex', ...)`) since the fix should route through that shared service, not a raw, unmetered `plexGet` call.

## Do
Current (broken) state, verified this session: `discoverInputKeys()` in `src/workflows/plex-language-fix/stages/discover.ts:23-25` calls `ledgerSuccessRows(JOB_NAME)` (`JOB_NAME='plex-language-discover'`) — reads back its OWN job's prior `work_items` success rows via the `work_items` ledger, not Plex itself. Change `discoverInputKeys()` to do a live Plex library walk instead, mirroring the SAFE reference pattern already used by `plex-profiles`'s `resolveInputKeys()` (`src/workflows/plex-profiles/stages/build.ts:41-51`, which calls `plexGet` live against Plex's list endpoints every time with an explicit doc-comment: "Re-fetches the two list endpoints fresh... rather than caching, since a stale key list would misrepresent what the library currently contains"). Once T474 lands, this call should go through `callService('plex', () => plexGet(...))` exactly like every other Plex read in this workflow, not a bare `plexGet` call. The live walk should enumerate the same candidate file set `discover.ts`'s main `run()` function already walks (reuse that logic/helper rather than writing a second, parallel Plex-walking implementation). Known, accepted tradeoff (already discussed and accepted by the owner): every MANUAL limited-run request now pays the cost of a full/partial library crawl just to compute candidates — this is the correct tradeoff versus silently no-opping on a reset ledger. Update `plex-language-fix/CLAUDE.md` to describe the new live-source `discoverInputKeys()`.

## Done when
`discoverInputKeys()` in `src/workflows/plex-language-fix/stages/discover.ts` no longer calls `ledgerSuccessRows(JOB_NAME)` — it performs a live Plex walk (reusing `discover.ts`'s own existing file-enumeration logic where possible) routed through `callService('plex', ...)` (available once T474 lands — this task's `dependsOn: ["T474"]` enforces the build order). A test scenario proves the fix: simulate a reset (empty the `plex-language-discover` ledger rows) and confirm `discoverInputKeys()` still returns the correct candidate file set from a live Plex walk, not an empty array. `npx tsc --noEmit` and `npm test` are green including updated coverage in `src/workflows/plex-language-fix/stages/discover.test.ts`. `plex-language-fix/CLAUDE.md` documents the new live-source root selection. This task does NOT add the `inputKeysService` field itself (that's T487, which depends on this task landing first).
