## Do

When you open a job/run from within a workflow run, the back-nav header reads "← workflow run {id}" (lowercase). Drop the words "workflow run" — show just the run id (a leading "#" is fine, e.g. "← #42"). Update both the backFrom helper’s workflow-run label (ui.tsx) and the runs/[id] fallback that also builds "workflow run {id}".

## Done when

The back-link to a workflow run shows just the run id (no "workflow run" words); both the ?from-derived label and the runs/[id] fallback are updated; dashboard build passes.
