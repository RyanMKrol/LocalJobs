## Do

Polish the styling of an **expanded Backlog task** row. From the screenshot (an expanded task showing
DO / DONE WHEN / scope / tags): (1) add **inner padding** around the rendered fields/markdown (DO,
DONE WHEN, spec body, dependsOn/tags) — currently it's tight against the edges; (2) give the expansion
background a **corner radius** (rounded, not square); (3) add **bottom padding** so the last element
(e.g. tag pills) isn't flush against the bottom edge. Overall: a contained card with consistent inner
padding + rounded background. The expandable rows + rendered spec live in
`dashboard/app/backlog/page.tsx`; styling in `dashboard/app/globals.css`.

## Done when

- The expanded region reads as a contained card: even inner padding on all sides (incl. bottom) and a
  rounded background.
- Theme-variable-driven (all 3 families × light/dark) + mobile-safe; dashboard build + mobile-check green.
