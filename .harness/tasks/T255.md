## Do

Add a **one-time history backfill** for the listens pipeline: load the owner's full existing Last.fm
scrobble history into the new DynamoDB listens table built by T254, so the table isn't limited to
scrobbles seen since the live sync started. Last.fm exposes paginated `user.getRecentTracks` — walk
all pages from the beginning and write each scrobble via the shared `dynamodb` service, reusing the
SAME listens table + key shape (scrobble timestamp + track id) and the SAME `work_items` idempotency
as the live sync, so re-running is safe and never double-stores. Live within the (tracked)
`src/jobs/listens-sync/` folder — a backfill stage/job or a one-shot manual workflow. Route through
the `lastfm` rate-limit service with conservative, friendly pacing (history is large — many pages;
pace it kindly, nothing is time-sensitive).

## Done when

- A backfill (stage or one-shot job within `src/jobs/listens-sync/`) walks Last.fm
  `user.getRecentTracks` from the start, writing every scrobble to the listens table via the
  `dynamodb` service, idempotent via `work_items` (same key shape as the live sync — safe to re-run),
  paced through the `lastfm` service.
- Verification is **hermetic**: tests mock the `dynamodb` service + paginated Last.fm responses and
  use the scratch DB — NO live AWS writes, no real-table mutation. `expectsTest`.
- `npx tsc --noEmit` and `npm test` are green; CI is green; docs updated.
