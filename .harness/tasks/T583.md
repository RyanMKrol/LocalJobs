## Overview
Re-attempt of T487, blocked by a scope-authoring bug: the original task's `scope` array named the wrong filenames for 4 of the 5 job files (`resolve.ts`/`discover.ts`/`github-sync.ts`/`build.ts` — the impl files) instead of the `*.job.ts` files that actually hold the JobDefinition object the `inputKeysService` field must be added to (`resolve.job.ts`/`discover.job.ts`/`github-sync.job.ts`/`build.job.ts`), and also omitted `src/workflows/perfumes/CLAUDE.md` even though the spec's own '## Do' text required updating all 5 touched workflows' CLAUDE.md files. The builder correctly followed the spec text, touched the right files, and was flagged for scope-creep on exactly those 5 files. This re-attempt corrects the scope array to list the real `.job.ts` files plus all 5 CLAUDE.md files — no other change to the original design.

## Do
Same as the original T487 design (see `design` field above): add the `inputKeysService?: string` field to `JobDefinition` in `src/core/types.ts`, and retrofit it onto the 5 jobs that declare `inputKeys()` — perfumes' find-url.job.ts (`'dynamodb'`), places' resolve.job.ts (`'fs'`), plex-language-fix's discover.job.ts (`'plex'`), projects-sync's github-sync.job.ts (`'github'`), plex-profiles' build.job.ts (`'plex'`, adding a supplementary callService('plex', ...) wrap around resolveInputKeys()'s plexGet calls if T474/T578 didn't already cover it). Update all 5 touched workflows' own CLAUDE.md plus the root src/workflows/CLAUDE.md to document the new field.

## Done when
All 5 jobs that declare `inputKeys()` (perfumes' find-url.job.ts, places' resolve.job.ts, plex-language-fix's discover.job.ts, projects-sync's github-sync.job.ts, plex-profiles' build.job.ts) also declare a correct `inputKeysService` value naming a real, registered service (`dynamodb`, `fs`, `plex`, `github`, `plex` respectively) on their JobDefinition object. `src/core/types.ts`'s `JobDefinition` has the new optional `inputKeysService?: string` field with a doc comment. `npx tsc --noEmit` and `npm test` are green. Each of the 5 touched workflows' own CLAUDE.md (perfumes, places, plex-language-fix, projects-sync, plex-profiles) documents its root stage's `inputKeysService` value, and the root `src/workflows/CLAUDE.md` documents the field + its enforcement. This task does NOT add the enforcing test itself (that's T488/its replacement, which depends on this unit).
