## Do

The back-link shown on the job-run page (`/runs/<id>`) and the validation-gate pages currently
renders a label in the form `<workflow_name> run.<id>` (workflow name + the word "run" + a dot + the
id of the thing it navigates back to). The owner wants the label to show **just the id** of the
target it's going back to — drop the `<workflow_name> run.` prefix.

The component is `WorkflowRunBackLink`, exported from `dashboard/app/ui.tsx` and used by
`dashboard/app/runs/[id]/page.tsx` (line ~33) and the validation-gate page(s) under
`dashboard/app/workflow-runs/[id]/gates/...` and/or `dashboard/app/workflows/[name]/gates/...`. Change
the rendered label so it shows only the target id (the workflow-RUN id it links back to), not the
`<workflow_name> run.` preamble. Keep the link target (href) and the fallback behaviour unchanged —
only the visible label text changes. Confirm the change is consistent everywhere `WorkflowRunBackLink`
is used (job-run page AND gate pages).

## Done when

- On `/runs/<id>` and the validation-gate pages, the back link shows just the id of the destination
  it returns to, with no `<workflow_name> run.` prefix; the link still navigates to the same place and
  the fallback (when there's no parent workflow run) still works.
- `npm --prefix dashboard run build` succeeds, `npx tsc --noEmit` is clean, and
  `node dashboard/scripts/mobile-check.mjs` is green.
