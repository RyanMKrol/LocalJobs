## Do

Add a **"dismiss entire collection"** control to the grouped output lists on the workflow detail
page (`dashboard/app/workflows/[name]/page.tsx`): the movie franchise gaps grouped by collection
(`groupByCollection`, each collection a `.panel` with an `<h3>` + table, render ~lines 86–114) and
the missing-TV-seasons grouped by show (`groupByShow`, render ~lines 218–252). At each group's
header (`<h3>`) add a **"✕ Ignore all"** button that, after a **confirmation prompt** (matching the
existing per-item ignore confirm + the bulk stuck-ignore UX), ignores EVERY currently-active member
of that group in one action.

**CRITICAL semantics — this is NOT a standing rule on the collection.** Dismissing a collection must
behave EXACTLY as if each currently-surfaced item had been individually ignored via
`ignoreSurfacedItem(jobName, itemKey)` (T145) — it ignores only the exact item keys surfaced RIGHT
NOW. If a collection shows 3 items today and the owner dismisses it, a 4th item appearing in a later
run (a new item key) MUST surface fresh and NOT be auto-ignored. Do not persist any
collection-level "ignored" flag.

Implementation: add a bulk store helper + endpoints that loop `ignoreSurfacedItem` over the given
keys — e.g. `POST /api/movie-gaps/ignore-bulk { tmdbIds: number[] }` (each →
`ignoreSurfacedItem(MOVIE_GAPS_JOB, gapKey(id))`) and `POST /api/missing-seasons/ignore-bulk
{ items: { tmdbId, season }[] }` (each → `ignoreSurfacedItem(PLEX_SEASONS_JOB, pairKey(tmdbId,
season))`), modelled on the existing per-item ignore handlers at `src/api/server.ts:762` / `:801`
and behind the same loopback/token mutation guard. Wire matching client methods in
`dashboard/app/lib/api.ts`. (A client-side loop over the existing per-item endpoints is an acceptable
alternative, but the bulk-helper approach is preferred so the resurface semantics can be unit-tested.)

Covers BOTH workflows (movies gaps-by-collection AND TV seasons-by-show). **Recommendations-dismiss
is explicitly OUT OF SCOPE** (deferred until recs are surfaced by T209). Keep mobile-responsive.

## Done when

- Each movie collection panel and each TV show panel shows an "✕ Ignore all" control at its header
  that, after a confirm, ignores all currently-active members of that group (they move to the
  Ignored section / leave the active list). Verify on the running dashboard: load
  `/workflows/movie-recommendations` and `/workflows/missing-tv-seasons`, dismiss a collection/show,
  and confirm every member becomes ignored.
- `expectsTest`: a unit test (in `src/db/store.test.ts` or `src/api/server.test.ts`, scratch DB only)
  proves the resurface semantics — after a bulk-dismiss of a collection's current keys, a NEW item
  key for the SAME collection is NOT treated as ignored (`isWorkItemDone` / `ignoredItemKeys` does
  not include it).
- `npm test`, `npx tsc --noEmit`, `npm --prefix dashboard run build`, and
  `node dashboard/scripts/mobile-check.mjs` are all green.
- `CLAUDE.md` updated to document the bulk collection-dismiss controls + their "ignore-the-current-
  items-only, not a standing rule" semantics.
