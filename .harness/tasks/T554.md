## Overview
Remove the ~150-line read-only DB-browser / canned-queries layer in store.ts that was orphaned when its /api/db endpoints and /db page were deleted in T230 — it is now exercised only by its own tests. (Default is delete; the paired question offers restore instead.)

## Do
1. In src/db/store.ts: delete the entire 'Read-only DB browser (dashboard)' section — listDbTables, isKnownTable, browseTable, the TablePage type, the CANNED_QUERIES array, CannedQueryDef/CannedQueryMeta/CannedQueryResult types, listCannedQueries, and runCannedQuery (locate by symbol name; roughly lines 2004-2152 today but re-grep since R03 may shift them). Remove any imports that only these functions used. 2. In src/db/store.test.ts: delete the import of browseTable/listDbTables/listCannedQueries/runCannedQuery (line ~6) and the two test blocks that exercise them (the browseTable pagination/injection block ~840-863 and the canned-query block ~884-933). Do NOT skip them — remove them. 3. Do not touch the /api/db routes (already gone since T230) — confirm none remain in src/api/server.ts. NOTE the R03 overlap in the commit/worklog but do not depend on R03.

## Done when
npx tsc --noEmit and npm test are green with the deleted test blocks fully removed (not skipped). A grep for listDbTables / isKnownTable / browseTable / CANNED_QUERIES / listCannedQueries / runCannedQuery / TablePage / CannedQuery across src/ and dashboard/ (excluding the projects-sync self-clone) returns no remaining references. No /api/db route reappears in server.ts.
