## Overview
Both movies' movie-snapshot and tv-recs' tv-snapshot stages currently record exactly ONE combined work_items row per run (a bulk marker keyed by the run's date, detail `{ name, movies/shows: <count>, path, format: 'json' }`, added deliberately in T571) instead of one row per movie/show found — unlike missing-tv-seasons' plex-tv-snapshot, which already correctly writes 630 real per-show rows (src/workflows/missing-tv-seasons/stages/snapshot.ts:19-29's recordSnapshotLedger, one markWorkItem per show keyed by tmdbId-or-ratingKey). This resolves both idea #16's explicit ask (recommender snapshot stages should be one row per item) and idea #14's flagged-but-unverified note about movie-snapshot's single bulk row — they are the SAME fix applied to the SAME two files, so this is one unit, not two.

## Do
In src/workflows/movies/stages/snapshot.ts, the current terminal write (lines 75-81) is: `` markWorkItem(SNAPSHOT_JOB, dayKey(now), 'success', { detail: { name: 'Movie library snapshot', movies: movies.length, path: moviesConfig.snapshotOut, format: 'json' } }) ``. Replace this bulk marker with ONE markWorkItem call PER movie in the `movies` array (built at line 51 via `buildMovieSnapshots(meta)`), mirroring missing-tv-seasons/stages/snapshot.ts's `recordSnapshotLedger` pattern exactly: key each row by the movie's resolved tmdbId if present, else its Plex ratingKey (same fallback shape as missing-tv-seasons' `snapshotItemKey`), with `detail` describing that movie (name/title, tmdbId, year — whatever PlexMovieMeta/the built movie-snapshot item exposes; check src/workflows/movies/types.ts for the exact per-movie shape before writing detail keys). Extract this as its own exported function (e.g. `recordSnapshotLedger(movies)`) for unit-testability, matching missing-tv-seasons' convention of a separately-exported, separately-tested ledger-recording function. Apply the IDENTICAL fix to src/workflows/tv-recs/stages/tv-snapshot.ts's equivalent bulk marker (find its current markWorkItem call — same T571 bulk-row pattern is documented in this workflow's own recommend.ts doc comments, so tv-snapshot.ts almost certainly has an analogous single-row write; confirm its exact current shape before editing) — one row per SHOW, keyed the same tmdbId-or-ratingKey way. Both stages currently re-scan fresh every run (no skip-if-done) — keep that; this is purely adding per-item VISIBILITY rows (T571's own stated purpose), not changing the stage's idempotency model. This does not affect missing-movies' OWN separate snapshot.ts (that workflow deliberately duplicates its own snapshot per T468 and is covered by its own unit, recs-ledger-rebuild-h, below).

## Done when
After a movie-snapshot run over M movies, work_items has M rows for job_name='movie-snapshot' (not 1), each keyed by that movie's tmdbId-or-ratingKey with detail describing that specific movie. Same for tv-snapshot over S shows → S rows for job_name='tv-snapshot' (confirm the tv-recs snapshot job's actual DAG name from tv-recs.workflow.ts before asserting this in a test — it may not be literally 'tv-snapshot'). A same-day re-run upserts the same rows (idempotent, no duplicates — dayKey is no longer the ledger key, the item's own id is, so re-runs across days also don't accumulate stale historical rows per item, they update in place). Add/extend a unit test asserting the per-item row count matches the movie/show count for both stages, mirroring missing-tv-seasons/stages/snapshot.test.ts's existing recordSnapshotLedger test shape.
