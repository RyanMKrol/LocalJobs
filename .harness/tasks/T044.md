## Do

The example job folders (`src/jobs/perfumes`, `src/jobs/places`) are a flat ~20-file dump
under a `data/` folder — hard to read. Reorganize them for human-readability AND fix an
architectural wart found while reviewing this (interview 2026-06-23): **service definitions
live INSIDE a workflow folder, but services are daemon-wide shared dependencies** (their
rate-limit/quota is coordinated globally by service NAME via `service_usage`), so they
should be a top-level concern, not buried in one workflow. Decisions made: (layout) a flat
`stages/` subfolder per job + shared files at the job root; (services) pull all `*.service.ts`
OUT to a top-level `src/services/` and make each **self-contained** (Option B — owns its
limits via env, imports nothing from any workflow); (scope) do BOTH perfumes and places,
perfumes first then places, keeping them consistent. Implement all of:

### Part 1 — Services → top-level, self-contained `src/services/`
1. Create `src/services/` (a sibling of `src/core` / `src/jobs`; tracked/public). `git mv` the
   four service definitions there: `claude-cli.service.ts` + `fragrantica.service.ts` (from
   perfumes) and `gemini.service.ts` + `google-places.service.ts` (from places). Fix their
   `'../../core/types.js'` import → `'../core/types.js'` (one level shallower now).
2. Make each service self-contained — import NOTHING from a workflow's `config.ts`; read its
   own limits from env with the SAME defaults so behaviour and `.env` are unchanged:
   - `claude-cli`: already standalone (`ratePerMinute: 20`).
   - `fragrantica`: `minIntervalMs`/`maxJitterMs` from `PERFUMES_FETCH_DELAY_MS ?? 12000` /
     `PERFUMES_FETCH_JITTER_MS ?? 6000` (perfumes `config.ts` KEEPS those fields — `fetch.ts`
     still uses them; the service just reads the same env independently).
   - `gemini`: `ratePerMinute: GEMINI_RATE_PER_MIN ?? 10`, `monthlyCap: PLACES_LLM_MONTHLY_CAP
     ?? 2000`, `dailyCap: PLACES_LLM_DAILY_CAP ?? floor(monthly/30)`, `paid: true`.
   - `google-places`: `ratePerMinute: PLACES_RATE_PER_MIN ?? 30`, `monthlyCap:
     PLACES_ENRICH_MONTHLY_CAP ?? 1000`, `dailyCap: PLACES_ENRICH_DAILY_CAP ??
     floor(monthly/30)`, `paid: true`.
   - Put the `daily = monthly / 30` math (today `DAILY_SPEND_DIVISOR` in places config) in
     `src/services/` — inline per service or a tiny `src/services/lib.ts` helper. Keep the
     existing env var names working (you MAY add service-scoped aliases like
     `GEMINI_MONTHLY_CAP` as primary with `PLACES_LLM_MONTHLY_CAP` as fallback — if you do,
     document them in `.env.example`, names/placeholders only).
3. Teach the registry to discover services from the new dir: in `src/jobs/registry.ts` also
   walk `resolve(__dirname, '..', 'services')` for `*.service.ts` (KEEP scanning `src/jobs`
   too, so a private job may still colocate a service). Leave the orphan-job check and
   job/workflow discovery unchanged.
4. The workflow configs shed what's now service-owned: remove `dailyCap`/`monthlyCap` from
   places `llmConfig` and `enrichConfig` and remove `DAILY_SPEND_DIVISOR` from places
   `config.ts` (verified: only the two services + the cap-math test reference them —
   `enrich.ts`/`llm-enrich.ts` already read caps from the SERVICE object `svc.*`). KEEP
   `resolveConfig`'s own caps (the resolver is `job_usage`-governed, not a service) and
   perfumes `fetchDelayMs`/`fetchJitterMs`. Move the cap-math assertions in
   `src/jobs/places/config.test.ts` to a new `src/services/*.test.ts` that asserts each paid
   service seeds `dailyCap = floor(monthlyCap/30)` and the month-of-daily-runs-fits-monthly
   invariant.

### Part 2 — Each job folder → `stages/` + shared at root
5. For perfumes AND places, `git mv` each stage's files (`<stage>.job.ts` + its `<stage>.ts`
   impl + any `<stage>.test.ts`) into a flat `stages/` subfolder. KEEP at the JOB ROOT (so
   `data/`/template path resolution via `resolve(here, …)` is untouched): the `*.workflow.ts`,
   `config.ts` (+`config.test.ts`), `types.ts`, `contracts.ts`, perfumes `lib.ts` +
   `progress.test.ts` + `profile.template.md` + `claude.ts` (the Claude-CLI wrapper — it
   depends on `perfumesConfig`, so it stays a perfumes helper, NOT a service), places
   `parse.ts` (a shared parser helper used by ingest+resolve), and `data/`.
6. Fix the moved stage files' relative imports one level up: `./config.js`→`../config.js`,
   `./lib.js`→`../lib.js`, `./types.js`→`../types.js`, `./contracts.js`→`../contracts.js`,
   perfumes `./claude.js`→`../claude.js`, places `./parse.js`→`../parse.js`. Cross-stage
   imports WITHIN `stages/` stay `./` (e.g. `build.ts`/`build.test.ts` → `./parse.js`).
   Confirm the recursive registry still discovers the `*.job.ts` files in `stages/`.

### Part 3 — Verify + docs
7. Stage every move with `git mv` and stage files EXPLICITLY (privacy guard — never
   `git add -A`; `src/services/` + the example folders are public/tracked, `data/` stays
   ignored). Update `CLAUDE.md`: the **File map** (new `src/services/` row; the per-job
   `stages/` + shared-at-root layout), the **Services** convention (services are a
   TOP-LEVEL, self-contained, daemon-wide concern in `src/services/` that own their limits
   and import nothing from a workflow; `DAILY_SPEND_DIVISOR` now lives with the services),
   and **How to add a job / service** (a new service → `src/services/<name>.service.ts`; a
   new stage → the job's `stages/`). Update `README.md` if it describes the job layout, and
   `.env.example` only if you added new env aliases.

If this genuinely can't fit one context window, record a worklog note splitting it
(Part 1 services-out, then Part 2 stages-reorg — they're independent) rather than
half-applying.

## Done when

All four service definitions live in a top-level `src/services/`, each self-contained (no
import from any workflow config; limits read from env with unchanged defaults; the
`daily = monthly/30` math lives in `src/services/`); the registry discovers them there (the
daemon still loads 4 services, 8 jobs, 2 workflows) and places `config.ts` no longer carries
the gemini/google-places caps or `DAILY_SPEND_DIVISOR`. Both `perfumes/` and `places/` have
their per-stage files grouped under a flat `stages/` subfolder with the workflow manifest,
shared helpers, config, template and `data/` at the job root, and all relative imports fixed.
`claude.ts` stays a perfumes helper. CLAUDE.md (file map + Services convention + add-a-job/
service guidance) + README reflect the new structure. `npx tsc --noEmit` and `npm test` are
green (registry + the relocated cap-math test included), and the daemon loads cleanly after a
restart.
