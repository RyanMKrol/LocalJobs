## Do

Cron schedule expressions (e.g. perfumes `0 2 * * *`) wrap onto two lines even on a wide desktop. Keep the schedule on one line where possible (e.g. white-space:nowrap on the schedule cell/mono span). Keep this in mind for the mobile styling pass too.

## Done when

Schedule cron expressions render on a single line on desktop; dashboard build passes.
