## Do

The Services page is read-only. Add the ability to adjust a service's rate_per_minute / daily_cap / monthly_cap from the UI, persisted to the services table (overriding the code default). Reconcile code-sync vs user-edits the same way the job enabled-toggle does (user override wins / is preserved across code sync).

## Done when

Service rate_per_minute / daily_cap / monthly_cap are editable from the Services page and persisted; a code-sync does not clobber a user override (reconciled like the enabled toggle); unit tests cover persistence + the reconcile rule.
