## Do

Represent validation gates on the pipeline DAG graph: render a gate marker on the producer->consumer edge in a pipeline RUN, make a FAILED gate visually distinct (e.g. red), and make the gate-failure run/logs reachable by clicking it. Expose gate state via the API so the dashboard can render it. Gates show only on a pipeline RUN, never on the structure-only /pipelines/[name] view.

## Done when

Gates appear on the pipeline-run DAG between producer->consumer; a failed gate is visually distinct and links to its failure logs; gates do NOT show on the structure-only /pipelines/[name] graph; dashboard build passes.
