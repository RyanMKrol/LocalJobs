## Do

Investigate why the claude-cli service is configured with a per-minute rate limit (rate_per_minute). Work out whether that cap is actually necessary/correct — is the Claude CLI genuinely rate-limited per minute, or is the value arbitrary/leftover? Then decide whether to keep, adjust, or remove it.

## Done when

The reason for the claude-cli per-minute rate limit is understood and a decision is recorded (keep / adjust / remove).
