## Do

Confirmed via a direct read-only query of `data/jobs.db` (2026-07-02) that the following rows are genuinely orphaned — no corresponding `*.job.ts`/`*.workflow.ts`/`*.service.ts` exists anywhere under `src/` for any of them (verified with `find`/`grep` across `src/jobs`, `src/services`, `dashboard/`):

- `workflows`: `listens-backfill` (enabled=1, schedule=NULL), `listens-sync` (enabled=1, schedule='0 */4 * * *')
- `workflow_jobs`: `listens-backfill → lastfm-backfill` (depends_on `[]`), `listens-sync → lastfm-sync` (depends_on `[]`)
- `jobs`: `lastfm-backfill`, `lastfm-sync`
- `work_items`: 99 rows for `job_name='lastfm-sync'` (0 for `lastfm-backfill`)
- `work_item_runs`: 99 rows for `job_name='lastfm-sync'`
- `runs`: 12 rows for `job_name='lastfm-sync'` (0 for `lastfm-backfill`), each with associated `run_logs` rows (375 total across those run ids)
- `workflow_runs`: 12 rows for `workflow_name='listens-sync'` (0 for `listens-backfill`), each with associated `workflow_run_logs` rows (60 total across those workflow_run ids)
- `services`: one `spotify` row (description 'Spotify Web API — album art enrichment for listens-sync.', rate_per_minute=60, daily_cap=NULL, monthly_cap=NULL, paid=0, category='') — no `spotify.service.ts` exists anywhere in the codebase
- `service_consumers`: one row `(service_name='spotify', job_name='lastfm-sync')`
- `service_usage`: 99 rows for `service='spotify'`

These are pure leftover DB rows from an old, since-rewritten Last.fm/Spotify sync design, superseded by the current `listening-digest`/`lastfm-digest` workflow (which is unrelated code, untouched by this task — do not modify `src/jobs/listening-digest/**`). None of the four identifiers (`listens-backfill`, `listens-sync`, `lastfm-backfill`, `lastfm-sync`, `spotify`) appear anywhere under `src/` or `dashboard/` outside these DB rows (a `grep -rn` across both trees was already run and confirmed clean other than an unrelated GitHub-repo-name coincidence in `src/jobs/projects-sync/data/out/` fixture data, which is untouched output data, not code, and irrelevant here).

1. Add small, reusable, transaction-wrapped admin helpers to `src/db/store.ts` (following the existing convention that ALL SQL lives there — do not inline `db.prepare` calls in the script):
   - `deleteWorkflowCompletely(workflowName: string): { workflows: number; workflowJobs: number; workflowRuns: number; workflowRunLogs: number }` — deletes, in one `db.transaction`, all `workflow_run_logs` rows whose `workflow_run_id` belongs to a `workflow_runs` row for that workflow, then those `workflow_runs` rows, then the `workflow_jobs` rows, then the `workflows` row itself. Returns counts of deleted rows per table for logging/verification. A workflow name that doesn't exist is a no-op returning all zeros (idempotent).
   - `deleteJobCompletely(jobName: string): { jobs: number; runs: number; runLogs: number; workItems: number; workItemRuns: number }` — deletes, in one `db.transaction`, all `run_logs` rows whose `run_id` belongs to a `runs` row for that job, then those `runs` rows, then `work_item_runs` rows for that job_name, then `work_items` rows for that job_name, then the `jobs` row itself. Returns counts. No-op / idempotent if the job doesn't exist.
   - `deleteServiceCompletely(serviceName: string): { services: number; serviceConsumers: number; serviceUsage: number }` — deletes, in one `db.transaction`, `service_usage` rows for that service, `service_consumers` rows for that service_name, then the `services` row itself. Returns counts. No-op / idempotent if the service doesn't exist.
   Follow the existing patterns already in `store.ts` (e.g. `pruneOrphanedWorkItems`, `resetWorkflowOutput`) for `db.transaction` usage and prepared-statement style.
2. Add a one-off admin script `scripts/cleanup-listens-spotify.ts` (mirroring the style/header-comment conventions of `scripts/backfill-service-usage.ts` — a runnable `npx tsx scripts/cleanup-listens-spotify.ts` script with a clear top-of-file comment explaining what it does, why, and that it's idempotent/safe to re-run) that:
   - Imports the three new store helpers.
   - Calls `deleteWorkflowCompletely('listens-backfill')`, `deleteWorkflowCompletely('listens-sync')`, `deleteJobCompletely('lastfm-backfill')`, `deleteJobCompletely('lastfm-sync')`, `deleteServiceCompletely('spotify')`, in that order (workflows/workflow_jobs before jobs, since `workflow_jobs` and `runs.job_name`/`workflows` don't have enforced FK cascade — deleting jobs first would leave `workflow_jobs` rows dangling if the workflow delete were skipped, so keep this order for safety even though both are ultimately removed).
   - Prints a clear before/after summary of what was deleted (counts per table, per identifier) to stdout, and a final "done" line.
   - Makes NO other DB or network calls — this is local-SQLite-only, no paid/remote calls, consistent with `backfill-service-usage.ts`.
3. Add unit test coverage for the three new `store.ts` helpers in the existing `store.test.ts` (or equivalent), using the standard scratch-DB test harness (per `CLAUDE.md`'s test-DB guard) — seed a synthetic workflow/job/service + related rows (work_items, runs, run_logs, workflow_runs, workflow_run_logs, work_item_runs, service_consumers, service_usage), call each delete helper, and assert: (a) the target rows and ALL related rows across every table listed above are gone, (b) unrelated rows (a second job/workflow/service in the fixture) are untouched, (c) calling the same helper a second time on an already-deleted name is a no-op that doesn't throw and returns zero counts.
4. Run the script once against the REAL `data/jobs.db` (not a scratch DB — this is the actual cleanup) via `npx tsx scripts/cleanup-listens-spotify.ts`, and verify the results (see Done when).
5. Update root `CLAUDE.md`'s Gotchas or File map section only if you judge the new script/helpers need a durable pointer for future readers (a one-line mention of `scripts/cleanup-listens-spotify.ts` as a one-off precedent for cleaning up orphaned workflow/job/service DB rows is enough — do not invent a new "auto-prune on registry sync" feature; that was considered during conversion and deliberately left out of scope as a bigger, separate design decision the owner didn't ask for here).

## Done when

1. `npx tsc --noEmit` and `npm test` are both green.
2. The new `store.test.ts` (or equivalent) coverage for `deleteWorkflowCompletely`/`deleteJobCompletely`/`deleteServiceCompletely` passes and asserts full cross-table cleanup + idempotency as described above.
3. After running `npx tsx scripts/cleanup-listens-spotify.ts` against the real `data/jobs.db`, the following `sqlite3 data/jobs.db` queries all return EMPTY / zero, confirming full removal with no dangling references:
   - `SELECT * FROM workflows WHERE name IN ('listens-backfill','listens-sync');`
   - `SELECT * FROM workflow_jobs WHERE workflow_name IN ('listens-backfill','listens-sync');`
   - `SELECT * FROM jobs WHERE name IN ('lastfm-backfill','lastfm-sync');`
   - `SELECT * FROM work_items WHERE job_name IN ('lastfm-backfill','lastfm-sync');`
   - `SELECT * FROM work_item_runs WHERE job_name IN ('lastfm-backfill','lastfm-sync');`
   - `SELECT * FROM runs WHERE job_name IN ('lastfm-backfill','lastfm-sync');`
   - `SELECT * FROM run_logs WHERE run_id IN (SELECT id FROM runs WHERE job_name IN ('lastfm-backfill','lastfm-sync'));` (vacuously empty since the runs are already gone, but confirms no orphaned run_logs survive)
   - `SELECT * FROM workflow_runs WHERE workflow_name IN ('listens-backfill','listens-sync');`
   - `SELECT * FROM workflow_run_logs WHERE workflow_run_id IN (SELECT id FROM workflow_runs WHERE workflow_name IN ('listens-backfill','listens-sync'));`
   - `SELECT * FROM services WHERE name='spotify';`
   - `SELECT * FROM service_consumers WHERE service_name='spotify';`
   - `SELECT * FROM service_usage WHERE service='spotify';`
4. Running `npx tsx scripts/cleanup-listens-spotify.ts` a SECOND time against the same DB completes without error and reports zero rows deleted for every identifier (idempotent no-op).
5. `src/jobs/listening-digest/**` (the real, current, unrelated `lastfm-digest` job) is completely untouched — its workflow/job/work_items/service (`lastfm`) rows and files are unaffected; confirm with `SELECT * FROM workflows WHERE name='listening-digest';` and `SELECT * FROM services WHERE name='lastfm';` still returning their normal rows.
6. Daemon restart (`launchctl kickstart -k gui/$(id -u)/com.ryankrol.localjobs`) succeeds cleanly (the registry still loads with no orphan-job errors — these were pure DB rows with no code, so this should already be unaffected, but confirm no regression), and the dashboard's Workflows list no longer shows `listens-backfill`/`listens-sync`, and the Services page no longer shows the uncategorized `spotify` row.
