## Overview
The ISO-week key helper weekKey is copy-pasted verbatim in three places — stock-digest/lib.ts, plex-space-saver/stages/scan.ts (~lines 10-17), overrides-audit/stages/scan.ts (~lines 10-17, whose comment literally says 'Mirrors plex-space-saver's weekKey') — plus a dayKey in stocks-sync/stages/stocks-snapshot.ts imported by sibling stages, and monthKey variants in the monthlies. R07 proposes a single src/core/dates.ts (weekKey, dayKey, and optionally monthKey), unit-tested once including the ISO-week year-boundary cases the copies risk diverging on. This is a pure move + import-update, no behavior change.

## Do
Create src/core/dates.ts exporting weekKey(date: Date): string (the ISO-8601 week key, e.g. '2026-W28' — copy the identical implementation the three copies share: UTC-normalize, Thursday-of-week determines the numbering year, pad the week to 2 digits) and dayKey(date: Date): string (the existing stocks-sync implementation). Optionally also export monthKey if the two monthly variants (listening-digest, workouts-sync) are genuinely identical — but the workouts-sync monthKey takes (year, monthIndex0) not a Date, so if the signatures differ, DO NOT force them together; move only the ones that are true verbatim copies and note the mismatch in the worklog rather than changing a signature. Then replace the duplicated definitions with imports of the shared helper: stock-digest/lib.ts (re-export weekKey from core so its call sites and weekLabel keep working, or import + delete the local copy — pick whichever keeps stock-digest's public surface intact, since its tests and other stages import weekKey from lib.ts), plex-space-saver/stages/scan.ts (import weekKey, delete local copy), overrides-audit/stages/scan.ts (import weekKey, delete local copy — its scan.test.ts imports weekKey; keep that import path resolving), and stocks-sync/stages/stocks-snapshot.ts (export/re-export dayKey from core, since stocks-fetch.ts and stocks-resolve-names.ts import dayKey from stocks-snapshot.js — keep that import path working). Write src/core/dates.test.ts with ISO-week boundary cases: a Monday and a Friday in the same week map to the same key (as overrides-audit/scan.test.ts already asserts for 2026-W28), plus the year-boundary cases the copies risk getting wrong (e.g. 2025-01-01, a date in the final ISO week of a year that belongs to the next numbering year, and a date in early January that belongs to the previous year's week 52/53). Keep every existing weekKey/dayKey call site behaving identically (no key values change for any date). Update stock-digest/CLAUDE.md's weekKey reference to point at src/core/dates.ts. Update the root CLAUDE.md file map to list src/core/dates.ts if the file map enumerates core modules.

## Done when
src/core/dates.ts exports weekKey (and dayKey, and monthKey only if truly identical across its copies) as the single definition; a repo-wide grep finds exactly one `function weekKey` and one `function dayKey` definition (the rest are imports/re-exports). stock-digest/lib.ts, plex-space-saver/stages/scan.ts, overrides-audit/stages/scan.ts, and stocks-sync/stages/stocks-snapshot.ts all use the shared helper, with every existing import path that pulls weekKey/dayKey from those modules still resolving (stock-digest's lib.ts and stocks-sync's stocks-snapshot.ts re-export as needed). src/core/dates.test.ts asserts same-week-different-day equality AND at least two ISO-week year-boundary cases. All existing weekKey/dayKey tests still pass (no key value changes for any date). stock-digest/CLAUDE.md (and the root file map if applicable) reference the new location. Definition of Done: `npx tsc --noEmit` and `npm test` both green.
