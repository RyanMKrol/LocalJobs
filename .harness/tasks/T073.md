## Do

On the Backlog LIST view, the next-eligible task is labelled with a "▶ in progress" pill (statusPill), which is misleading — nothing is actively building it (TASKS.json has no in_progress status, and the loop may not even be running). A not-done task should show its REAL status (pending). Show "pending" in the list and drop the next-task highlight/border there. Keep the DAG view’s accurately-labelled "next" node highlight as-is.

## Done when

On /backlog a pending task shows "pending" (no "in progress" label) and no special list highlight; the DAG view keeps its "next" node; dashboard build passes.
