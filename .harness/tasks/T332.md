## Do

On the `movie-recommendations` workflow's DAG graph, the owner sees the `franchise-gaps` node visually merging into/overlapping the `rec-auteur` node directly above it — but ONLY on an ACTUAL RUN's graph view (`/workflow-runs/[id]`, which passes `statusByJob`+`gates` into `DagFlow`), not on the structure-only definition view (`/workflows/[name]`, which passes neither). Both views render the SAME 9-node fan-out column (movie-snapshot's 8 rec-* recommender branches + franchise-gaps, all depending only on movie-snapshot, feeding into rec-merge → movie-gaps-notify — see `src/jobs/movies/movies.workflow.ts`). Only the rec-auteur/franchise-gaps pair (the bottom two of that stack) shows a near-zero gap, and only in run view.

Investigate `dashboard/app/components/DagFlow.tsx` (the single component behind both call sites: `dashboard/app/workflows/[name]/page.tsx` line ~897 passes `structuralGates` only; `dashboard/app/workflow-runs/[id]/page.tsx` line ~455 passes `statusByJob`, `runIdByJob`, and `gates`). Confirmed facts from reading the code (use these as strong leads, but CONFIRM the actual mechanism before fixing — do not just guess-patch):

1. **Dagre's raw layout math is identical in both views.** `buildLayout()` feeds `g.setNode(m.job_name, { width: NODE_W, height: NODE_H })` (fixed 180x58, line ~228) and `g.setEdge(dep, m.job_name)` for every `depends_on` edge (line ~229) — gates are NOT added to the dagre graph at all, only used afterward to decorate edge `data`. Since `members` comes from the same `GET /api/workflows/:name` payload in both pages, dagre's computed `x`/`y` for every node should be byte-identical between structural and run views. This means the DIFFERENCE the owner sees must come from something OUTSIDE dagre's raw coordinate math — either (a) extra DOM content growing a node's ACTUAL rendered height beyond the `NODE_H=58` dagre assumed, or (b) an overlay element dagre never accounted for at all, or (c) React Flow's `fitView` computing a different bounding box/zoom because of run-view-only elements. Rule out or confirm each.

2. **Lead A — the extra status line grows real node height beyond what dagre assumed.** `StageNode`'s `data.showStatus` is `!!statusByJob` (true ONLY in run view) — when true it renders an extra `.dag-node-status` div (line 54) below the name. `.dag-node` (globals.css ~line 210) has NO fixed height — it sizes to content (padding `10px 12px 10px 14px` + the name line +, in run view only, the status line `font-size:11px; margin-top:3px`). Dagre's Y-spacing (`NODE_SEP=28`) assumes every node is EXACTLY `NODE_H=58` tall; if the real run-view rendered height (with the status line) exceeds 58, it eats directly into the 28px gap reserved between vertically-adjacent nodes in the same rank — for EVERY node in the 9-node column, not just this pair. This could explain why run view is uniformly tighter than structural view, but does not by itself explain why ONLY this one pair visibly merges while every other pair in the same column stays fine — there must be something about this specific pair that makes it the closest-to-zero baseline gap even before the status-line growth (see Lead B).

3. **Lead B — franchise-gaps' X (column) and Y (vertical order) are computed via DIFFERENT logic paths, and Y is never reconciled to the overridden X.** The code explicitly overrides each node's X: `colX(m.job_name) + NODE_W / 2` (line 258) is derived from `computeLayers(members)` — the TRUE DAG wave depth (a manual BFS in `computeLayers`, lines 181-203) — NOT from dagre's own rank. The inline comment (lines 219-224) explicitly says dagre's own ranker would otherwise pull franchise-gaps toward `movie-gaps-notify` (its consumer, 2 hops away via `rec-merge`'s rank) and "strand it between columns", which is WHY the X override exists. But the Y coordinate is taken straight from dagre's own computation (`const { y } = g.node(m.job_name)`, line 257) — i.e. Y is NOT re-derived for the wave-pinned column franchise-gaps is actually DRAWN in; it's whatever vertical position dagre assigned it within ITS OWN (different) rank. Dagre's non-overlap guarantee for Y only holds among nodes dagre placed in the SAME rank — if dagre's rank for franchise-gaps differs from the rec-* branches' rank (plausible, given the comment above), its Y could coincidentally land very close to whichever node dagre's OWN rank hierarchy places nearest it, independent of the visual wave-1 column ordering. This would produce an inherently tight (though not necessarily alarming) baseline gap for this ONE pair in BOTH views — since this part of the computation is view-independent — which Lead A's run-view-only height growth (or Lead C below) could then be tipping into a fully visible overlap ONLY in run view, while leaving every other (naturally roomier) pair fine in both views. Confirm by logging/inspecting `g.node('franchise-gaps')` vs `g.node('rec-auteur')` (and the other 7 branches) for the real movie-recommendations DAG, and comparing to `computeLayers`'s wave assignment.

4. **Lead C — the gate padlock overlay itself is unaccounted-for in dagre's box math.** Every wave-1 node (all 8 branches AND franchise-gaps) has an edge FROM `movie-snapshot` carrying a gate (`movie-snapshot` contract, consumed by both `franchise-gaps` and each `rec-*` branch — see `src/jobs/movies/contracts.ts` `movieSnapshotContract()` and `src/jobs/movies/stages/recommend.ts` `consumes: [movieSnapshotContract()]`). In run view (`gates` populated), `GateEdge` renders an absolutely-positioned `EdgeLabelRenderer` HTML overlay (the padlock, lines 125-161) at each edge's own bezier-path midpoint — a DOM element that dagre's layout NEVER accounts for as occupying space (dagre only lays out the node bounding boxes, not this overlay). Since `movie-snapshot` sits at a single point while the 9 downstream nodes' Y-positions are spread across the whole column, each edge's own midpoint Y differs — worth checking whether the midpoint of the `movie-snapshot → franchise-gaps` edge (or the `franchise-gaps → movie-gaps-notify` / `rec-auteur → rec-merge` edges, which cross OTHER ranks) visually lands on or very near the rec-auteur/franchise-gaps boundary, compounding the apparent merge. This is run-view-only (padlocks only render when `gates` is populated) and independent of dagre's node Y math, so it's a candidate for the actual view-differentiator even if Lead B's baseline tightness turns out not to be real.

**Your job:** actually confirm which of these (singly or in combination) is the real cause — instrument the code (temporary console.log of dagre's per-node x/y, or inspect real rendered DOM `getBoundingClientRect()` for both nodes in run view) rather than guessing, THEN fix it. A likely-correct direction once confirmed: derive Y positions from the SAME wave-column grouping used for X (i.e. re-run dagre's ordering — or a simpler manual pass — WITHIN each wave-computed column so every node's Y is self-consistent with what's actually drawn beside it, instead of trusting dagre's rank-scoped Y for a node whose X was overridden to a different column), and/or give `.dag-node` a `min-height` matching (or exceeding) `NODE_H` plus the status line so real DOM height can never silently shrink dagre's reserved gap. Do not regress the structural view's currently-correct spacing, and do not remove the gate padlocks (they're a load-bearing feature per T204/T223 — root CLAUDE.md's gate-rendering section).

**Important — the existing hermetic visual-check fixtures do NOT currently reproduce this bug at all.** I confirmed `dashboard/scripts/_dashboard-harness.mjs`'s `fixtureFor()` routes BOTH `/api/workflows/movie-recommendations` (structural page) and `/api/workflow-runs/1` (run page, used by the `workflow-run` PAGES entry) through the SAME generic 3-node `places`-shaped fixture (`workflow()`/`workflowRun()` defaults, `members` = `places-resolve → places-enrich → places-enrich-with-llm`) regardless of the requested workflow name — there is currently NO fixture anywhere in the harness that models the real movie-recommendations 9-node fan-out (8 `rec-*` branches + `franchise-gaps` all depending on `movie-snapshot`, feeding `rec-merge` → `movie-gaps-notify`), with `statusByJob` and matching `gates` populated the way a real completed run would have them. So neither the `workflow-movie-recs` nor `workflow-run` PAGES entries can currently show this bug in a screenshot, even though the `workflow-movie-recs` name suggests it should.

As part of this task, per the LIVING ARTIFACT rule (root `CLAUDE.md` + `.harness/CLAUDE.md`), extend `_dashboard-harness.mjs` to add a dedicated fixture that actually models this shape: a `members` array matching `src/jobs/movies/movies.workflow.ts` (movie-snapshot, franchise-gaps, the 8 `rec-*` branches, rec-merge, movie-gaps-notify), a `statusByJob` with every stage `success`, and a `gates` array covering at least the `movie-snapshot` gate into each wave-1 node (per `src/jobs/movies/contracts.ts`), routed through a NEW `/api/workflow-runs/<id>` id (e.g. `movie-recs-run`) so it doesn't collide with the existing generic `workflow-run` fixture, and add a matching `PAGES` (or `FLOWS`) entry so `visual-check.mjs` captures it. This is required groundwork to actually SEE and verify the fix — without it there is no way to visually confirm the reported symptom is gone.

## Done when

The actual root cause of the rec-auteur/franchise-gaps overlap has been confirmed (not merely guessed) and documented in the worklog — cite what was inspected (dagre's raw x/y per node, and/or real rendered DOM measurements) and which of the leads above (or another mechanism found during investigation) was the true cause. A fix has been applied to `dashboard/app/components/DagFlow.tsx` (and/or `dashboard/app/globals.css`) that resolves it without regressing: the structural (definition-view) DAG's spacing, every OTHER node pair's spacing in both views, and the gate padlock rendering (still visible and clickable in run view). `dashboard/scripts/_dashboard-harness.mjs` has been extended with a fixture that models the real movie-recommendations 9-node fan-out shape (matching `src/jobs/movies/movies.workflow.ts`'s member/dependency list) in a RUN-scoped view (`statusByJob` + `gates` populated, mirroring a real completed run), reachable via a `PAGES` or `FLOWS` entry. `npx tsc --noEmit`, `npm test`, and `npm --prefix dashboard run build` are all green. Per the `facets.layer == ui` floor: `node dashboard/scripts/visual-check.mjs` has been run after the build, and its output screenshot for the NEW run-scoped movie-recommendations fixture entry has actually been LOOKED AT (not just structurally checked) and confirms rec-auteur and franchise-gaps now have a normal, consistent visible gap between them — matching every other adjacent node pair in that same 9-node column — with no overlap/merging. The existing `workflow-movie-recs` (structural) and `workflow-run` (generic) screenshots are also checked and confirmed unchanged/unregressed.
