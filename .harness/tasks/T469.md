## Overview
Once split-movie-workflows-backend lands two independent workflows (movie-recommendations = recs-only, missing-movies = gaps-only), the dashboard's already-separate MovieGapsManager and MovieRecsManager components (dashboard/app/workflows/[name]/page.tsx) need to render on the RIGHT workflow's detail page instead of both unconditionally on movie-recommendations.

## Do
In dashboard/app/workflows/[name]/page.tsx: today WORKFLOWS_WITH_SPECIFIC_MANAGERS = new Set(['movie-recommendations', 'missing-tv-seasons', 'tv-recommendations']) and both <MovieRecsManager /> and <MovieGapsManager /> render unconditionally near the end of the page (around the WORKFLOWS_WITH_SPECIFIC_MANAGERS gate / WorkflowOutputSection fallback). Add 'missing-movies' to WORKFLOWS_WITH_SPECIFIC_MANAGERS, and change the render logic so MovieGapsManager only renders when name === 'missing-movies' and MovieRecsManager only renders when name === 'movie-recommendations' (mirror how MissingSeasonsManager/TvRecsManager are already gated to their own single workflow name, if they are — check the existing pattern for those two before inventing a new one). The MovieGapsManager component itself calls the SAME /api/movie-gaps* endpoints as before (untouched by the backend split) — only which page it's rendered from changes.

Update dashboard/scripts/_dashboard-harness.mjs (the shared PAGES/FLOWS + synthetic API fixtures used by both mobile-check.mjs and visual-check.mjs) to add the new missing-movies workflow page alongside the existing movie-recommendations entry, per the LIVING ARTIFACT RULE in the root CLAUDE.md — this file must be updated in the SAME change as the UI surface change or future runs will not catch regressions on the new page.

Also double check dashboard/app/lib/api.ts's ignoreMovieGapBulk / movie-gaps helpers don't hardcode any workflow-name assumption (they shouldn't — the endpoints are keyed by job name, not workflow name — but confirm).

## Done when
The missing-movies workflow's detail page shows MovieGapsManager (franchise gaps + bulk ignore/unignore, exactly as it worked before the split); the movie-recommendations detail page shows only MovieRecsManager (recommendations). dashboard/scripts/_dashboard-harness.mjs is updated to cover the new page. npm --prefix dashboard run build succeeds; node dashboard/scripts/mobile-check.mjs is green; node dashboard/scripts/visual-check.mjs is green AND the screenshots for both the new missing-movies page and the trimmed movie-recommendations page have been visually inspected to confirm each shows exactly the right manager, nothing blank/duplicated/missing.
