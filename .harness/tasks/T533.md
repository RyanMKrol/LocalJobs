## Overview
listGlobalLogs() in store.ts builds a LIKE filter from the user's `q` search string (job-logs branch ~lines 299-302, workflow-logs branch ~lines 350-353) as `%${q.toLowerCase()}%`. It's parameterized (no SQL injection) but `%`/`_` in `q` are treated as LIKE wildcards, so `q=%` matches EVERYTHING and a literal `_` matches any char — not literal-substring search semantics.

## Do
In store.ts's `listGlobalLogs` (BOTH the job-logs branch ~299-302 and the workflow-logs branch ~350-353 — note store.ts may by now be split into store/ files by the store-split unit, so search for these LIKE clauses wherever they live), escape the user's `q` before wrapping in `%…%`: replace `\` → `\\`, `%` → `\%`, `_` → `\_` (a small `escapeLike(s)` helper), and add `ESCAPE '\'` to each `LIKE ?` clause so the backslash is the literal-escape char. Keep the `LOWER(...)` + toLowerCase() case-insensitive behavior. Add a store.test.ts case proving `q='100%'` or `q='a_b'` matches only literal-substring rows (seed a log containing the literal and one that would match a wildcard interpretation, assert only the literal matches).

## Done when
both LIKE clauses in listGlobalLogs escape %/_/\ in user q and use `ESCAPE '\'`; a new store.test.ts case proves `q='%'` / `q='a_b'` do literal-substring matching (do NOT match everything / any-char); `npx tsc --noEmit` + `npm test` green.
