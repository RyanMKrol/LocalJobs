## Overview
The 'T201: unlimited workflow runs all independent stages without throttling' subtest in src/core/workflow-executor.test.ts (line 661-674) asserts elapsed < SLOW_MS * 3 (750ms) as an absolute wall-clock ceiling to prove 4 stages ran in parallel. This is flaky under real machine load (observed failing at 771ms) and fails the whole test file, which fails npm test / LOCAL_DOD / the loop's structural_checks gate for unrelated tasks. Make this assertion robust to machine variance.

## Do
In src/core/workflow-executor.test.ts, change the 'T201: unlimited workflow runs all independent stages without throttling' subtest (currently around line 661-674) so its overlap assertion is no longer a bare absolute-ms ceiling (`elapsed < SLOW_MS * 3`). Follow the pattern already used by the neighboring 'parallelism (T156)' subtest (line 498-554), which explicitly avoids absolute-ms assertions (see its comment at line 499-503) by comparing the unlimited/parallel run's elapsed time against a same-run serial baseline (e.g. run the same 4 slow-* stages a second time with maxConcurrency:1, or otherwise derive an equivalent baseline) and asserting a generous relative ratio (e.g. `elapsed * 1.8 < serialElapsed`) instead of a fixed millisecond ceiling. Do not weaken the test's actual guarantee — it must still fail if the 4 stages ran serially instead of in parallel under maxConcurrency:0.

## Done when
The T201 'unlimited workflow runs all independent stages without throttling' subtest in src/core/workflow-executor.test.ts contains no bare absolute-millisecond ceiling assertion (no more `elapsed < SLOW_MS * 3` / `elapsed < 750`-style check) — the overlap proof is a relative comparison against a same-run baseline measurement, matching the pattern already used by the T156 'parallelism' subtest. `npm test` passes. Running `node --test --import tsx src/core/workflow-executor.test.ts` (or the project's npm test invocation of this file) 5 times in a row locally shows no flake. The test still fails as expected if the parallel path is artificially forced serial (verify by temporarily hardcoding maxConcurrency effectively to 1 in the unlimited path and confirming the assertion trips, then reverting).
