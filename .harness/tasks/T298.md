## Do

Retire DynamoDB from `workouts-sync` and replace it with a local, full-history JSON accumulator, so
a later stage (T299) can compute long-range per-exercise progress trends without needing DynamoDB —
matching this repo's local-markdown-first direction (the same move already made for
`listening-digest` and the `projects-sync`/`stocks-sync` redesigns this session).

1. **`src/jobs/workouts-sync/stages/hevy-sync.ts`** — remove the `dynamoPut` import and the
   DynamoDB write entirely (currently gated via `callService('dynamodb', () => dynamoPut(...))`,
   mirroring the shape removed from `projects-sync`'s `github-sync.ts` in a companion task this
   session). Instead of upserting each workout into `WORKOUTS_TABLE`/`EXERCISES_TABLE`, APPEND each
   newly-synced workout's full data (the `HevyWorkout` shape already fetched — `id`, `title`,
   `start_time`, and every exercise's `title` + each set's `weight_kg`/`reps`/`set_type`) to a
   job-local, persistent JSON file, e.g. `data/out/workouts-history.json` (an array, one entry per
   workout, growing over time — read the existing file, append only genuinely NEW workouts this run
   — determined the same way the existing `work_items` ledger already determines "new" via
   `isWorkItemDone`/`markWorkItem` keyed by workout id — then write the file back). This file is the
   full local mirror of Hevy history that T299's analysis stage will read; do not derive it from a
   single day's window.
2. Update `src/jobs/workouts-sync/workouts-sync.workflow.ts`: change `schedule` from `'0 6 * * *'`
   (daily) to a monthly cron (e.g. `'0 6 1 * *'`, the 1st of the month) — the underlying Hevy sync no
   longer feeds anything that needs same-day freshness now that DynamoDB is gone, and a monthly cadence
   matches the report-generation stage T299 will add. Update the workflow's doc comment accordingly.
3. Remove `WORKOUTS_TABLE`/`EXERCISES_TABLE`/AWS DynamoDB creds from being required by THIS workflow —
   check whether `HEVY_API_KEY` is the only credential this job still needs, and leave
   `WORKOUTS_TABLE`/`EXERCISES_TABLE` in `.env.example` ONLY if some other still-active workflow needs
   them (grep the codebase first — if nothing else references them, remove those two lines and their
   comment from `.env.example`, since AWS creds may still be needed by OTHER services and shouldn't be
   removed wholesale).
4. Update root `CLAUDE.md`'s `workouts-sync` paragraph and `README.md`'s matching bullet: no more
   DynamoDB, accumulates full workout history to a local JSON file, runs monthly (T299, landing
   separately, adds the actual progress-report stage — mention that a second stage exists/will exist
   for the report if T299 hasn't landed yet, so the docs don't overclaim).

## Done when

- `hevy-sync.ts` contains no reference to `dynamoPut` or the `dynamodb` service.
- A unit test (injected fake Hevy fetcher, mirroring the existing `HevyFetcher` injectable pattern
  already in this file) demonstrates: running the stage twice with the SAME Hevy data only appends
  each workout to `data/out/workouts-history.json` ONCE (idempotent — the second run adds nothing
  new), and running it with an EXTRA new workout on the second call appends only that one new entry.
- `workouts-sync.workflow.ts`'s `schedule` is a monthly cron (not `'0 6 * * *'`).
- If `WORKOUTS_TABLE`/`EXERCISES_TABLE` are confirmed (by grep) to be unused by any other active
  workflow, they're removed from `.env.example`; otherwise they're left in place with a note of what
  still needs them.
- Root `CLAUDE.md` and `README.md` describe the new local-JSON-accumulator behavior and monthly
  schedule for `workouts-sync`, with no remaining mention of DynamoDB for this workflow.
- `npm test` and `npx tsc --noEmit` pass.
