## Do

Build a new scheduled **listens-sync** workflow that pulls Last.fm scrobbles into a NEW DynamoDB
**listens** table — establishing durable listening history (the website currently does NOT persist
listens; it live-fetches Last.fm per request). Create `src/jobs/listens-sync/` as a **tracked**
workflow (published like the `places`/`perfumes` examples — no secrets in code; only `.env` creds and
the job's `data/` stay gitignored). On a cadence, fetch Last.fm `user.getRecentTracks`
(`LAST_FM_API_KEY` / `LAST_FM_USERNAME`), optionally enrich album art via Spotify
(`SPOTIFY_CLIENT_ID`/`SECRET`), and write each scrobble to the new listens table via the shared
`dynamodb` service (T252). **Schema (decided + already created by `scripts/create-dynamo-tables.mjs`,
PERMANENT keys):** the listens table's primary key is **partition key `trackId` (S) + sort key
`scrobbledAt` (N, epoch seconds)** — so every item you write MUST carry those exact attributes
(`trackId`, `scrobbledAt`); that (track, scrobble-time) pair is unique, so a scrobble is never
double-stored. Idempotent via the `work_items` ledger keyed by that same composite. Add `lastfm` (and optional `spotify`) rate-limit/quota service(s) in
`src/services/` (tracked, generic, creds from env); conservative, friendly limits. Reference: website
`src/pages/api/lastfm/*`, `src/lib/apiCache.js`, `src/lib/dynamo.js`.

This task is the LIVE forward sync only; the one-time history backfill is a separate task (T255).

**Scope guard:** local-jobs ONLY — no website edits. The website keeps live-proxying Last.fm as today.

## Done when

- `src/jobs/listens-sync/` exists as a tracked scheduled workflow that fetches recent Last.fm
  scrobbles and writes them to the new listens table (keyed by scrobble timestamp + track id) via the
  `dynamodb` service, idempotent via `work_items`; optional Spotify enrichment; tracked
  `lastfm`(/`spotify`) service(s) in `src/services/`; new env vars documented as placeholders in
  `.env.example`; the job's `data/` gitignored.
- Verification is **hermetic**: tests mock the `dynamodb` service + the Last.fm/Spotify fetches and
  use the scratch DB — NO live AWS writes, no mutation of real tables. `expectsTest`.
- `npx tsc --noEmit` and `npm test` are green; CI is green; docs (CLAUDE.md/README job list) updated.
