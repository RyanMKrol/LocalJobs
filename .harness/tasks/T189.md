## Do

Stop sending the **per-stage** push notification (`notifyStage`) from a workflow run. In
`src/core/workflow-executor.ts`, `runWorkflow` currently calls `notifyStage(def.name, workflowRunId,
job, status, log)` (around line 291) every time a member stage settles, and maintains a
per-stage notification-dedup map (`lastNotifiedStatus`, the `repeatUntilStable` T112 dedup, ~lines
280–291). Remove the per-stage push entirely — including for failed/timed-out stages — so the ONLY
notification a workflow run emits is the existing aggregate `notifyWorkflow(...)` call at the end of
`runWorkflow` (~line 404). Remove the now-dead `lastNotifiedStatus` dedup machinery and the
`notifyStage` import in this file. The dashboard's per-stage run **status** must keep updating exactly
as today — this change touches ONLY the external ntfy/macOS push, not run-status recording. Decide
whether to keep `notifyStage` in `src/core/notifier.ts` (leave it exported but unused, or delete it
if nothing else references it — grep first).

Update `CLAUDE.md`: the bullets describing "Per-stage notifications are deduped during cycling (T112)"
and any "notifyStage is fired per-stage" wording in the architecture + `src/core/notifier.ts` file-map
row must be rewritten to state that a workflow now notifies ONLY on workflow completion (no per-stage
pushes, failures included), and the per-stage cycling-dedup is gone.

## Done when

- `runWorkflow` no longer calls `notifyStage` on any path (success, failure, timeout, or cycling); the
  only notification it emits is the end-of-run `notifyWorkflow`. (`grep -n notifyStage
  src/core/workflow-executor.ts` returns nothing.)
- The `lastNotifiedStatus` per-stage dedup map and its associated `notifyStage` import are removed from
  `src/core/workflow-executor.ts`, with no remaining dead references.
- Dashboard per-stage run status is unaffected — member runs still record `running`/`succeeded`/
  `failed`/`cancelled` to the DB exactly as before (no change to status-writing code paths).
- `src/core/workflow-executor.test.ts` has a test asserting that across a multi-stage workflow run,
  `notifyStage` is **never** invoked while `notifyWorkflow` is invoked exactly once at the end (inject/
  spy on the notifier). `npm test` passes.
- `npx tsc --noEmit` is clean and `CLAUDE.md`'s per-stage-notification + T112-dedup notes are updated
  to match the new behavior.
