## Overview
The plex-language-fix DAG ends in the repo's only stage that mutates external state (it PUTs audio/subtitle track choices to Plex), yet its evaluate->apply gate asserts nothing. Give that one gate a real, cheap check that catches malformed change decisions before the mutation happens.

## Do
In src/workflows/plex-language-fix/contracts.ts, replace plexLanguageEvaluateContract's trivial always-ok check() with a real assertion of the evaluate->apply boundary: read the plex-language-evaluate work_items rows read-only via stages/ledger.ts (ledgerSuccessRows, as apply.ts already does) and assert that EVERY row whose detail.status === 'change' has a numeric detail.proposedAudio.streamId AND a non-null detail.currentAudio — the exact malformation apply.ts (~53-56) currently skips at runtime. Keep the existing fromChecks(checks[], sample) shape so the gate detail page renders; make an empty ledger or an all-'skip' ledger pass trivially; on any violation return ok:false and include the offending itemKey(s) in the failing check's actual + the sample so the gate page names them. Also update the contract's shape.expectations label/detail + the top-level description to describe the real bar (not the old 'ledger is readable' minimum). Leave plexLanguageDiscoverContract and plexLanguageResolveContract as the sanctioned trivial gates. Update src/workflows/plex-language-fix/CLAUDE.md's gate note to record that the evaluate->apply gate now asserts a real pre-mutation bar. NOTE (not a dep): this relates to pending T511 (plex onto callService('plex')) but is orthogonal — do not hard-depend on it.

## Done when
plexLanguageEvaluateContract.check() returns ok:false (naming the offending itemKey) when the evaluate ledger contains a 'change' row lacking a numeric proposedAudio.streamId or a currentAudio, and ok:true for a valid ledger / all-skip / empty ledger — read read-only, no paid/remote call. A new unit test seeds both the valid and malformed cases and asserts the outcomes. The gate-coverage test still passes (the boundary keeps a matching gate). CLAUDE.md updated. Existing tests green; npx tsc --noEmit and npm test pass.
